# Local clone of the generator used by `scripts/generate.nu`.
# CI clones it fresh each run; locally you can `git clone https://github.com/lassoColombo/nu-http-client-generator _generator`.
_generator
_generator/
