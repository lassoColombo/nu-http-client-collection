# Auto-generated client for Loket.nl API vV2
# Source: https://api.apis.guru/v2/specs/loket.nl/V2/openapi.json
# Auth: --token flag or $env.LOKET_NL_API_TOKEN

const BASE_URL = "https://api.loket.nl/v2"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o LOKET_NL_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, dry_run: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  if $dry_run { return {method: $method, url: $req_url, headers: $auth.headers, query_string: $auth.query, content_type: $ct, timeout: $timeout, body: $body} }
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def base-url-completer [] { ["https://api.loket.nl/v2" "https://api.loket-acc.nl/v2" "https://k8s.loket.nl/globalfilter-microservice"] }
def auth-scheme-completer [] { ["bearer"] }

# Completers for enum parameters
def accept-completer [] { ["image/jpeg" "image/jpg" "image/png"] }
def accept-completer-1 [] { ["application/json;version=2018-01-01" "application/json;version=YYYY-MM-DD"] }
def accept-completer-2 [] { ["application/json;version=2018-01-01" "application/pdf"] }
def accept-completer-3 [] { ["application/json;version=2018-01-01" "application/json;version=2019-12-16"] }
def accept-completer-4 [] { ["application/json;version=2018-12-31" "application/json;version=2021-03-29"] }
def action-completer [] { ["cancel" "confirm"] }
def paymentType-completer [] { ["Other" "Salary"] }
def batchBooking-completer [] { ["BtchBookgExclude" "BtchBookgFalse" "BtchBookgTrue"] }
def accept-completer-5 [] { ["application/json;version=2018-01-01" "text/csv"] }
def accept-completer-6 [] { ["application/pdf" "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"] }
def reportType-completer [] { ["aggregatedPerGeneralLedgerAccount" "nonAggregated"] }
def groupingType-completer [] { ["perDistributionUnit" "perPeriod" "perPeriodPerDistributionUnit"] }
def reportType-completer-1 [] { ["baseForCalculationBalancesAtStartOfYear" "leaveAccrualWithinYear" "leaveBalancesAtStartOfYear"] }
def accept-completer-7 [] { ["application/pdf" "text/csv"] }
def accept-completer-8 [] { ["application/json;version=2018-01-01" "application/json;version=2019-02-11"] }
def groupBy-completer [] { ["Department" "Function"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "dry-run" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "achmeainsurancecontracts GetAchmeaInsuranceContractsByUserId" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# list of insurance contracts
#
# GET /achmeainsurancecontracts
# operationId: GetAchmeaInsuranceContractsByUserId
export def "achmeainsurancecontracts GetAchmeaInsuranceContractsByUserId" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/achmeainsurancecontracts" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# list of wage percentages for an insurance contract
#
# GET /achmeainsurancecontracts/{achmeaInsuranceContractId}/insuredwagecalculationpercentages
# operationId: GetWagePercentagesByAchmeaInsuranceContractId
export def "achmeainsurancecontracts-insuredwagecalculationpercentages GetWagePercentagesByAchmeaInsuranceContractId" [
  achmeaInsuranceContractId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/achmeainsurancecontracts/($achmeaInsuranceContractId)/insuredwagecalculationpercentages" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Acquire the AOW date
#
# GET /aowDate
# operationId: GetAowDateByDateOfBirth
export def "aow-date GetAowDateByDateOfBirth" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --dateOfBirth: string # The date of birth of the employee (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "dateOfBirth" $dateOfBirth "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/aowDate" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Authorizations for the application
#
# GET /applications/{applicationId}/authorizations
# operationId: GetApplicationAuthorizationsByApplicationId
export def "applications-authorizations GetApplicationAuthorizationsByApplicationId" [
  applicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applications/($applicationId)/authorizations")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the application logo
#
# GET /applications/{applicationId}/logo
# operationId: GetApplicationLogo
export def "applications-logo GetApplicationLogo" [
  applicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applications/($applicationId)/logo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Acquire company information
#
# GET /chamberofcommerce/{chamberOfCommerceNumber}/companyinformation
# operationId: GetCompanyInformationByChamberOfCommerceNumber
export def "chamberofcommerce-companyinformation GetCompanyInformationByChamberOfCommerceNumber" [
  chamberOfCommerceNumber: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/chamberofcommerce/($chamberOfCommerceNumber)/companyinformation")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of functions
#
# GET /datanewbusiness/functions
# operationId: GetDataNewBusinessFunctions
export def "datanewbusiness-functions GetDataNewBusinessFunctions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/datanewbusiness/functions")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get Data New Business token
#
# GET /datanewbusiness/token
# operationId: GetDataNewBusinessToken
export def "datanewbusiness-token GetDataNewBusinessToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/datanewbusiness/token")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Acquire the address for a combination of `postalCode` and `houseNumber`
#
# GET /locationservices/address
# operationId: GetAddressByPostalCodeAndHouseNumber
export def "locationservices-address GetAddressByPostalCodeAndHouseNumber" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --postalCode: string # A valid postal code (e.g. 5037AA)
  --houseNumber: int # A valid house number (without addition) (e.g. 1)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "postalCode" $postalCode "scalar") (serialize-qp "houseNumber" $houseNumber "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/locationservices/address" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of providers
#
# GET /providers
# operationId: GetProvidersByUser
export def "providers GetProvidersByUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/providers" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a specific applicationset
#
# DELETE /providers/applicationsets/{applicationSetId}
# operationId: DeleteProviderApplicationSetByApplicationSetId
export def "providers-applicationsets DeleteProviderApplicationSetByApplicationSetId" [
  applicationSetId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/applicationsets/($applicationSetId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an applicationset
#
# PUT /providers/applicationsets/{applicationSetId}
# operationId: PutProviderApplicationSetByApplicationSetId
export def "providers-applicationsets PutProviderApplicationSetByApplicationSetId" [
  applicationSetId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/applicationsets/($applicationSetId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of a authorization set
#
# GET /providers/authorizationsets/{authorizationsetid}
# operationId: GetAuthorizationSetsByAuthorizationSetId
export def "providers-authorizationsets GetAuthorizationSetsByAuthorizationSetId" [
  authorizationsetid: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --authorizationSetId: string # The unique identifier of an authorization set (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "authorizationSetId" $authorizationSetId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/authorizationsets/($authorizationsetid)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an EmailIdentity for a provider
#
# DELETE /providers/emailidentities/{emailIdentityId}
# operationId: DeleteEmailIdentityByEmailIdentityId
export def "providers-emailidentities DeleteEmailIdentityByEmailIdentityId" [
  emailIdentityId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/emailidentities/($emailIdentityId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Provider emailIdentity test email
#
# POST /providers/emailidentities/{emailIdentityId}/sendtestemail
# operationId: PostSendProviderTestemailByEmailIdentityId
export def "providers-emailidentities-sendtestemail PostSendProviderTestemailByEmailIdentityId" [
  emailIdentityId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/emailidentities/($emailIdentityId)/sendtestemail")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of employers
#
# GET /providers/employers
# operationId: GetEmployersByUserId
export def "providers-employers GetEmployersByUserId" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/providers/employers" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Mark one or more announcements as read.
#
# PATCH /providers/employers/announcements
# operationId: PatchAnnouncements
export def "providers-employers-announcements PatchAnnouncements" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/announcements")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete an benefit in kind type record
#
# DELETE /providers/employers/benefitinkindtypes/{benefitInKindTypeId}
# operationId: DeleteBenefitInKindTypeByBenefitInKindTypeId
export def "providers-employers-benefitinkindtypes DeleteBenefitInKindTypeByBenefitInKindTypeId" [
  benefitInKindTypeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/benefitinkindtypes/($benefitInKindTypeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an benefit in kind type
#
# GET /providers/employers/benefitinkindtypes/{benefitInKindTypeId}
# operationId: GetBenefitInKindTypeByBenefitInKindTypeId
export def "providers-employers-benefitinkindtypes GetBenefitInKindTypeByBenefitInKindTypeId" [
  benefitInKindTypeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/benefitinkindtypes/($benefitInKindTypeId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an benefit in kind type record
#
# PUT /providers/employers/benefitinkindtypes/{benefitInKindTypeId}
# operationId: PutBenefitInKindTypeByBenefitInKindTypeId
export def "providers-employers-benefitinkindtypes PutBenefitInKindTypeByBenefitInKindTypeId" [
  benefitInKindTypeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/benefitinkindtypes/($benefitInKindTypeId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Import concept employees via a file
#
# PATCH /providers/employers/conceptemployees/import/{payrollAdministrationId}
# operationId: PatchImportConceptEmployeesByPayrollAdministrationId
export def "providers-employers-conceptemployees-import PatchImportConceptEmployeesByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --conceptEmployeeId: string # The unique identifier of the concept employee (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "conceptEmployeeId" $conceptEmployeeId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/conceptemployees/import/($payrollAdministrationId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "multipart/form-data" $body
}

# Paygrade metadata for concept employee
#
# GET /providers/employers/conceptemployees/metadata/payrollAdministration/{payrollAdministrationId}/payScale/{payscaleKey}/paygrade/{paygradeKey}
# operationId: GetConceptEmployeePayGradeMetaDataByPayrollAdministrationIdAndPayscaleKeyAndPayGradeKey
export def "providers-employers-conceptemployees-metadata-payroll-administration-pay-scale-paygrade GetConceptEmployeePayGradeMetaDataByPayrollAdministrationIdAndPayscaleKeyAndPayGradeKey" [
  payrollAdministrationId: string
  payscaleKey: int
  paygradeKey: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --date: string # A valid date (xxxx-xx-xx) (format: date)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "date" $date "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/conceptemployees/metadata/payrollAdministration/($payrollAdministrationId)/payScale/($payscaleKey)/paygrade/($paygradeKey)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a concept employee with fewer fields.
#
# GET /providers/employers/conceptemployees/minimized/{conceptEmployeeId}
# operationId: GetConceptEmployeeMinimizedByConceptEmployeeId
export def "providers-employers-conceptemployees-minimized GetConceptEmployeeMinimizedByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/minimized/($conceptEmployeeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a concept employee with fewer fields.
#
# PUT /providers/employers/conceptemployees/minimized/{conceptEmployeeId}
# operationId: PutConceptEmployeeMinizedByConceptEmployeeId
export def "providers-employers-conceptemployees-minimized PutConceptEmployeeMinizedByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/minimized/($conceptEmployeeId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a concept employee
#
# DELETE /providers/employers/conceptemployees/{conceptEmployeeId}
# operationId: DeleteConceptEmployeeByConceptEmployeeId
export def "providers-employers-conceptemployees DeleteConceptEmployeeByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a concept employee
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}
# operationId: GetConceptEmployeeByConceptEmployeeId
export def "providers-employers-conceptemployees GetConceptEmployeeByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a concept employee
#
# PUT /providers/employers/conceptemployees/{conceptEmployeeId}
# operationId: PutConceptEmployeeByConceptEmployeeId
export def "providers-employers-conceptemployees PutConceptEmployeeByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Initiate assessment
#
# POST /providers/employers/conceptemployees/{conceptEmployeeId}/assessment/initiate
# operationId: PostInitiateAssessmentByConceptEmployeeId
export def "providers-employers-conceptemployees-assessment-initiate PostInitiateAssessmentByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/assessment/initiate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Citizen service number of a concept employee
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/citizenservicenumber
# operationId: GetCitizenServiceNumberByConceptEmployeeId
export def "providers-employers-conceptemployees-citizenservicenumber GetCitizenServiceNumberByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/citizenservicenumber")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update the citizen service number of a concept employee
#
# PUT /providers/employers/conceptemployees/{conceptEmployeeId}/citizenservicenumber
# operationId: PutCitizenServiceNumberByConceptEmployeeId
export def "providers-employers-conceptemployees-citizenservicenumber PutCitizenServiceNumberByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/citizenservicenumber")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Convert a concept employee to an employee
#
# PATCH /providers/employers/conceptemployees/{conceptEmployeeId}/convertToEmployee
# operationId: PatchConvertToEmployeeByConceptEmployeeId
export def "providers-employers-conceptemployees-convert-to-employee PatchConvertToEmployeeByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --X-ValidateOnly: oneof<nothing, bool> # If set to true only the validations will be performed no changes will be committed.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/convertToEmployee")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match, "X-ValidateOnly": $X_ValidateOnly} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of concept employee dossier documents
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/documents
# operationId: GetDocumentsByConceptEmployeeId
export def "providers-employers-conceptemployees-documents GetDocumentsByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload a document to the concept employee dossier
#
# POST /providers/employers/conceptemployees/{conceptEmployeeId}/documents
# operationId: PostDocumentByConceptEmployeeId
export def "providers-employers-conceptemployees-documents PostDocumentByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a document in the concept employee dossier
#
# DELETE /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}
# operationId: DeleteDocumentByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents DeleteDocumentByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download concept employee dossier document
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}
# operationId: GetDocumentByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents GetDocumentByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)")
  let extra_headers = {"Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an concept employee dossier document
#
# PUT /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}
# operationId: PutDocumentByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents PutDocumentByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get audittrail for the document in the concept employee dossier
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}/audittrail
# operationId: GetAuditTrailByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents-audittrail GetAuditTrailByConceptEmployeeIdAndDocumentId" [
  documentId: string
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)/audittrail" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete signature - Conceptemployee
#
# DELETE /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}/signature
# operationId: DeleteSignatureByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents-signature DeleteSignatureByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)/signature")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Signature details - Conceptemployee
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}/signature
# operationId: GetSignatureByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents-signature GetSignatureByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)/signature")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate signature - Conceptemployee
#
# POST /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}/signature/initiate
# operationId: PostInitiateSignatureByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents-signature-initiate PostInitiateSignatureByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)/signature/initiate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Retrieve the report of the signing - Conceptemployee
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/documents/{documentId}/signature/report
# operationId: GetSignatureReportByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documents-signature-report GetSignatureReportByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documents/($documentId)/signature/report")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Generate a document for an concept employee
#
# POST /providers/employers/conceptemployees/{conceptEmployeeId}/documenttemplates/{documentId}/generatedocument
# operationId: PostGenerateDocumentByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documenttemplates-generatedocument PostGenerateDocumentByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documenttemplates/($documentId)/generatedocument")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Generate a document for an concept employee - preview
#
# POST /providers/employers/conceptemployees/{conceptEmployeeId}/documenttemplates/{documentId}/generatedocument/preview
# operationId: PostGenerateDocumentPreviewByConceptEmployeeIdAndDocumentId
export def "providers-employers-conceptemployees-documenttemplates-generatedocument-preview PostGenerateDocumentPreviewByConceptEmployeeIdAndDocumentId" [
  conceptEmployeeId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/documenttemplates/($documentId)/generatedocument/preview")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete the complete concept employee dossier
#
# DELETE /providers/employers/conceptemployees/{conceptEmployeeId}/dossier"
# operationId: DeleteDossierByConceptEmployeeId
export def "providers-employers-conceptemployees-dossier DeleteDossierByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/dossier"")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Document count
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/dossier"
# operationId: GetDocumentCountByConceptEmployeeId
export def "providers-employers-conceptemployees-dossier GetDocumentCountByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/dossier"")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change status of a preboarding trajectory
#
# PATCH /providers/employers/conceptemployees/{conceptEmployeeId}/preboardingtrajectory
# operationId: PatchPreboardingTrajectoryByConceptEmployeeId
export def "providers-employers-conceptemployees-preboardingtrajectory PatchPreboardingTrajectoryByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/preboardingtrajectory")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Add a preboarding trajectory
#
# POST /providers/employers/conceptemployees/{conceptEmployeeId}/preboardingtrajectory
# operationId: PostPreboardingTrajectoryByConceptEmployeeId
export def "providers-employers-conceptemployees-preboardingtrajectory PostPreboardingTrajectoryByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/preboardingtrajectory")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of a Concept employee self service portal status
#
# GET /providers/employers/conceptemployees/{conceptEmployeeId}/selfservice
# operationId: GetConceptEmployeeSelfServiceByConceptEmployeeId
export def "providers-employers-conceptemployees-selfservice GetConceptEmployeeSelfServiceByConceptEmployeeId" [
  conceptEmployeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --employeeId: string # The unique identifier of the employee (format: uuid)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "employeeId" $employeeId "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/conceptemployees/($conceptEmployeeId)/selfservice" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a contract code record
#
# DELETE /providers/employers/contractcodes/{contractCodeId}
# operationId: DeleteContractCodeByContractCodeId
export def "providers-employers-contractcodes DeleteContractCodeByContractCodeId" [
  contractCodeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/contractcodes/($contractCodeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a contract code
#
# GET /providers/employers/contractcodes/{contractCodeId}
# operationId: GetContractCodeByContractCodeId
export def "providers-employers-contractcodes GetContractCodeByContractCodeId" [
  contractCodeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/contractcodes/($contractCodeId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a contract code record
#
# PUT /providers/employers/contractcodes/{contractCodeId}
# operationId: PutContractCodeByContractCodeId
export def "providers-employers-contractcodes PutContractCodeByContractCodeId" [
  contractCodeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/contractcodes/($contractCodeId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a custom field record
#
# DELETE /providers/employers/customfields/{customFieldId}
# operationId: DeleteCustomFieldByCustomFieldId
export def "providers-employers-customfields DeleteCustomFieldByCustomFieldId" [
  customFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/customfields/($customFieldId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a custom field
#
# GET /providers/employers/customfields/{customFieldId}
# operationId: GetCustomFieldByCustomFieldId
export def "providers-employers-customfields GetCustomFieldByCustomFieldId" [
  customFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/customfields/($customFieldId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a custom field record
#
# PUT /providers/employers/customfields/{customFieldId}
# operationId: PutCustomFieldByCustomFieldId
export def "providers-employers-customfields PutCustomFieldByCustomFieldId" [
  customFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/customfields/($customFieldId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a custom holiday record
#
# DELETE /providers/employers/customholidays/{customHolidayId}
# operationId: DeleteCustomHolidayByCustomHolidayId
export def "providers-employers-customholidays DeleteCustomHolidayByCustomHolidayId" [
  customHolidayId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/customholidays/($customHolidayId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a custom holiday
#
# GET /providers/employers/customholidays/{customHolidayId}
# operationId: GetCustomHolidayByCustomHolidayId
export def "providers-employers-customholidays GetCustomHolidayByCustomHolidayId" [
  customHolidayId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/customholidays/($customHolidayId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a custom holiday record
#
# PUT /providers/employers/customholidays/{customHolidayId}
# operationId: PutCustomHolidayByCustomHolidayId
export def "providers-employers-customholidays PutCustomHolidayByCustomHolidayId" [
  customHolidayId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/customholidays/($customHolidayId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of a department
#
# GET /providers/employers/departments/{departmentId}
# operationId: GetDepartmentByDepartmentId
export def "providers-employers-departments GetDepartmentByDepartmentId" [
  departmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/departments/($departmentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a department
#
# PUT /providers/employers/departments/{departmentId}
# operationId: PutDepartmentByDepartmentId
export def "providers-employers-departments PutDepartmentByDepartmentId" [
  departmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/departments/($departmentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an education further indication record
#
# DELETE /providers/employers/educationfurtherindications/{educationFurtherIndicationId}
# operationId: DeleteEducationFurtherIndicationByEducationFurtherIndicationId
export def "providers-employers-educationfurtherindications DeleteEducationFurtherIndicationByEducationFurtherIndicationId" [
  educationFurtherIndicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/educationfurtherindications/($educationFurtherIndicationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an education further indication
#
# GET /providers/employers/educationfurtherindications/{educationFurtherIndicationId}
# operationId: GetEducationFurtherIndicationByEducationFurtherIndicationId
export def "providers-employers-educationfurtherindications GetEducationFurtherIndicationByEducationFurtherIndicationId" [
  educationFurtherIndicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/educationfurtherindications/($educationFurtherIndicationId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an education further indication record
#
# PUT /providers/employers/educationfurtherindications/{educationFurtherIndicationId}
# operationId: PutEducationFurtherIndicationByEducationFurtherIndicationId
export def "providers-employers-educationfurtherindications PutEducationFurtherIndicationByEducationFurtherIndicationId" [
  educationFurtherIndicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/educationfurtherindications/($educationFurtherIndicationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an education type record
#
# DELETE /providers/employers/educationtypes/{educationTypeId}
# operationId: DeleteEducationTypeByEducationTypeId
export def "providers-employers-educationtypes DeleteEducationTypeByEducationTypeId" [
  educationTypeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/educationtypes/($educationTypeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an education type
#
# GET /providers/employers/educationtypes/{educationTypeId}
# operationId: GetEducationTypeByEducationTypeId
export def "providers-employers-educationtypes GetEducationTypeByEducationTypeId" [
  educationTypeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/educationtypes/($educationTypeId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an education type record
#
# PUT /providers/employers/educationtypes/{educationTypeId}
# operationId: PutEducationTypeByEducationTypeId
export def "providers-employers-educationtypes PutEducationTypeByEducationTypeId" [
  educationTypeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/educationtypes/($educationTypeId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an EmailIdentity for an employer
#
# DELETE /providers/employers/emailidentities/{emailIdentityId}
export def "providers-employers-emailidentities delete" [
  emailIdentityId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/emailidentities/($emailIdentityId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Employer emailIdentity test email
#
# POST /providers/employers/emailidentities/{emailIdentityId}/sendtestemail
# operationId: PostSendEmployerTestemailByEmailIdentityId
export def "providers-employers-emailidentities-sendtestemail PostSendEmployerTestemailByEmailIdentityId" [
  emailIdentityId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/emailidentities/($emailIdentityId)/sendtestemail")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific record
#
# DELETE /providers/employers/employees/absences/absencecontactHistory/{absencecontactHistoryId}
# operationId: DeleteAbsenceContactHistoryByAbsenceContactHistoryId
export def "providers-employers-employees-absences-absencecontact-history DeleteAbsenceContactHistoryByAbsenceContactHistoryId" [
  absencecontactHistoryId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/absencecontactHistory/($absencecontactHistoryId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a contact history
#
# GET /providers/employers/employees/absences/absencecontactHistory/{absencecontactHistoryId}
# operationId: GetAbsenceContactHistoryByAbsenceContactHistoryId
export def "providers-employers-employees-absences-absencecontact-history GetAbsenceContactHistoryByAbsenceContactHistoryId" [
  absencecontactHistoryId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/absencecontactHistory/($absencecontactHistoryId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a contact history
#
# PUT /providers/employers/employees/absences/absencecontactHistory/{absencecontactHistoryId}
# operationId: PutAbsenceContactHistoryByAbsenceContactHistoryId
export def "providers-employers-employees-absences-absencecontact-history PutAbsenceContactHistoryByAbsenceContactHistoryId" [
  absencecontactHistoryId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/absencecontactHistory/($absencecontactHistoryId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2020-03-16" $body
}

# Delete a specific Absence Progress record
#
# DELETE /providers/employers/employees/absences/absenceprogress/{absenceProgressId}
# operationId: DeleteAbsenceProgressByAbsenceProgressId
export def "providers-employers-employees-absences-absenceprogress DeleteAbsenceProgressByAbsenceProgressId" [
  absenceProgressId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/absenceprogress/($absenceProgressId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an Absence Progress
#
# GET /providers/employers/employees/absences/absenceprogress/{absenceProgressId}
# operationId: GetAbsenceProgressByAbsenceProgressId
export def "providers-employers-employees-absences-absenceprogress GetAbsenceProgressByAbsenceProgressId" [
  absenceProgressId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/absenceprogress/($absenceProgressId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an absence progress
#
# PUT /providers/employers/employees/absences/absenceprogress/{absenceProgressId}
# operationId: PutAbsenceProgressByAbsenceProgress
export def "providers-employers-employees-absences-absenceprogress PutAbsenceProgressByAbsenceProgress" [
  absenceProgressId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/absenceprogress/($absenceProgressId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2020-03-16" $body
}

# Delete a specific absence record
#
# DELETE /providers/employers/employees/absences/{absenceId}
# operationId: DeleteAbsenceByAbsenceId
export def "providers-employers-employees-absences DeleteAbsenceByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an Absence
#
# GET /providers/employers/employees/absences/{absenceId}
# operationId: GetAbsenceByAbsenceId
export def "providers-employers-employees-absences GetAbsenceByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a Absence
#
# PUT /providers/employers/employees/absences/{absenceId}
# operationId: PutAbsenceByAbsence
export def "providers-employers-employees-absences PutAbsenceByAbsence" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2020-03-16" $body
}

# Contact history for an absence
#
# GET /providers/employers/employees/absences/{absenceId}/absencecontactHistory
# operationId: GetAbsenceContactsHistoryByAbsenceId
export def "providers-employers-employees-absences-absencecontact-history GetAbsenceContactsHistoryByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)/absencecontactHistory" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a contact history for an Absence
#
# POST /providers/employers/employees/absences/{absenceId}/absencecontactHistory
# operationId: PostAbsenceContactHistoryByAbsenceId
export def "providers-employers-employees-absences-absencecontact-history PostAbsenceContactHistoryByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)/absencecontactHistory")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of absence progress for an absence
#
# GET /providers/employers/employees/absences/{absenceId}/absenceprogress
# operationId: GetAbsenceProgressByAbsenceId
export def "providers-employers-employees-absences-absenceprogress GetAbsenceProgressByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)/absenceprogress" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an Absence Progress for an Absence
#
# POST /providers/employers/employees/absences/{absenceId}/absenceprogress
# operationId: PostAbsenceProgressByAbsenceId
export def "providers-employers-employees-absences-absenceprogress PostAbsenceProgressByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)/absenceprogress")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2020-03-16" $body
}

# Close an absence
#
# PATCH /providers/employers/employees/absences/{absenceId}/closeabsence
# operationId: PatchCloseAbsenceByAbsenceId
export def "providers-employers-employees-absences-closeabsence PatchCloseAbsenceByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)/closeabsence")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2020-03-16" $body
}

# Reopen an absence
#
# PATCH /providers/employers/employees/absences/{absenceId}/reopenabsence
# operationId: PatchReopenAbsenceByAbsenceId
export def "providers-employers-employees-absences-reopenabsence PatchReopenAbsenceByAbsenceId" [
  absenceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/absences/($absenceId)/reopenabsence")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2020-03-16" $body
}

# Delete a specific child record
#
# DELETE /providers/employers/employees/children/{childId}
# operationId: DeleteChildByChildId
export def "providers-employers-employees-children DeleteChildByChildId" [
  childId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/children/($childId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a child
#
# GET /providers/employers/employees/children/{childId}
# operationId: GetChildByChildId
export def "providers-employers-employees-children GetChildByChildId" [
  childId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/children/($childId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a child
#
# PUT /providers/employers/employees/children/{childId}
# operationId: PutChildByChildId
export def "providers-employers-employees-children PutChildByChildId" [
  childId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/children/($childId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific contact
#
# DELETE /providers/employers/employees/contacts/{contactId}
# operationId: DeleteContactByContactId
export def "providers-employers-employees-contacts DeleteContactByContactId" [
  contactId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/contacts/($contactId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a single contact
#
# GET /providers/employers/employees/contacts/{contactId}
# operationId: GetContactByContactId
export def "providers-employers-employees-contacts GetContactByContactId" [
  contactId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/contacts/($contactId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a contact
#
# PUT /providers/employers/employees/contacts/{contactId}
# operationId: PutContactByContactId
export def "providers-employers-employees-contacts PutContactByContactId" [
  contactId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/contacts/($contactId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an employee custom field record
#
# DELETE /providers/employers/employees/customfields/{employeeCustomFieldId}
# operationId: DeleteEmployeeCustomFieldByEmployeeCustomFieldId
export def "providers-employers-employees-customfields DeleteEmployeeCustomFieldByEmployeeCustomFieldId" [
  employeeCustomFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/customfields/($employeeCustomFieldId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an employee custom field
#
# GET /providers/employers/employees/customfields/{employeeCustomFieldId}
# operationId: GetEmployeeCustomFieldByEmployeeCustomFieldId
export def "providers-employers-employees-customfields GetEmployeeCustomFieldByEmployeeCustomFieldId" [
  employeeCustomFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/customfields/($employeeCustomFieldId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an employee custom field record
#
# PUT /providers/employers/employees/customfields/{employeeCustomFieldId}
# operationId: PutEmployeeCustomFieldByEmployeeCustomFieldId
export def "providers-employers-employees-customfields PutEmployeeCustomFieldByEmployeeCustomFieldId" [
  employeeCustomFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/customfields/($employeeCustomFieldId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific education
#
# DELETE /providers/employers/employees/educations/{employeeEducationId}
# operationId: DeleteEducationByEducationId
export def "providers-employers-employees-educations DeleteEducationByEducationId" [
  employeeEducationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/educations/($employeeEducationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a single education
#
# GET /providers/employers/employees/educations/{employeeEducationId}
# operationId: GetEducationByEducationId
export def "providers-employers-employees-educations GetEducationByEducationId" [
  employeeEducationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/educations/($employeeEducationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a Education
#
# PUT /providers/employers/employees/educations/{employeeEducationId}
# operationId: PutEducationByEducationId
export def "providers-employers-employees-educations PutEducationByEducationId" [
  employeeEducationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/educations/($employeeEducationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Accept or reject wage proposals
#
# PATCH /providers/employers/employees/employments/WageProposals
# operationId: PatchWageProposalsByWageProposalId
export def "providers-employers-employees-employments-wage-proposals PatchWageProposalsByWageProposalId" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/employees/employments/WageProposals")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific abp fund record
#
# DELETE /providers/employers/employees/employments/abpfunds/{abpFundId}
# operationId: DeleteAbpFundByAbpFundId
export def "providers-employers-employees-employments-abpfunds DeleteAbpFundByAbpFundId" [
  abpFundId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/abpfunds/($abpFundId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an abp fund
#
# GET /providers/employers/employees/employments/abpfunds/{abpFundId}
# operationId: GetAbpFundByAbpFundId
export def "providers-employers-employees-employments-abpfunds GetAbpFundByAbpFundId" [
  abpFundId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/abpfunds/($abpFundId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a abp fund record
#
# PUT /providers/employers/employees/employments/abpfunds/{abpFundId}
# operationId: PutAbpFundByAbpFundId
export def "providers-employers-employees-employments-abpfunds PutAbpFundByAbpFundId" [
  abpFundId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/abpfunds/($abpFundId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an attachment of earnings record
#
# DELETE /providers/employers/employees/employments/attachmentsofearnings/{attachmentOfEarningsId}
# operationId: DeleteAttachmentOfEarningsByAttachmentOfEarningsId
export def "providers-employers-employees-employments-attachmentsofearnings DeleteAttachmentOfEarningsByAttachmentOfEarningsId" [
  attachmentOfEarningsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/attachmentsofearnings/($attachmentOfEarningsId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the details of an attachment of earnings record
#
# GET /providers/employers/employees/employments/attachmentsofearnings/{attachmentOfEarningsId}
# operationId: GetAttachmentOfEarningsByAttachmentOfEarningsId
export def "providers-employers-employees-employments-attachmentsofearnings GetAttachmentOfEarningsByAttachmentOfEarningsId" [
  attachmentOfEarningsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/attachmentsofearnings/($attachmentOfEarningsId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an attachment of earnings record
#
# PUT /providers/employers/employees/employments/attachmentsofearnings/{attachmentOfEarningsId}
# operationId: PutAttachmentOfEarningsByAttachmentOfEarningsId
export def "providers-employers-employees-employments-attachmentsofearnings PutAttachmentOfEarningsByAttachmentOfEarningsId" [
  attachmentOfEarningsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/attachmentsofearnings/($attachmentOfEarningsId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific record
#
# DELETE /providers/employers/employees/employments/basesforcalculation/{baseforcalculationId}
# operationId: DeleteBaseForCalculationByBaseForCalculationId
export def "providers-employers-employees-employments-basesforcalculation DeleteBaseForCalculationByBaseForCalculationId" [
  baseforcalculationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/basesforcalculation/($baseforcalculationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an base for employment fund calculations
#
# GET /providers/employers/employees/employments/basesforcalculation/{baseforcalculationId}
# operationId: GetBaseForCalculationByBaseForCalculationId
export def "providers-employers-employees-employments-basesforcalculation GetBaseForCalculationByBaseForCalculationId" [
  baseforcalculationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/basesforcalculation/($baseforcalculationId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the properties of a base for calculation
#
# PUT /providers/employers/employees/employments/basesforcalculation/{baseforcalculationId}
# operationId: PutBaseForCalculationByBaseForCalculationId
export def "providers-employers-employees-employments-basesforcalculation PutBaseForCalculationByBaseForCalculationId" [
  baseforcalculationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/basesforcalculation/($baseforcalculationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific record
#
# DELETE /providers/employers/employees/employments/basesforemploymentfundcalculation/{baseforemploymentfundcalculationId}
# operationId: DeleteBaseForEmploymentFundCalculationByBaseForEmploymentFundCalculationId
export def "providers-employers-employees-employments-basesforemploymentfundcalculation DeleteBaseForEmploymentFundCalculationByBaseForEmploymentFundCalculationId" [
  baseforemploymentfundcalculationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/basesforemploymentfundcalculation/($baseforemploymentfundcalculationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an base for employment fund calculations
#
# GET /providers/employers/employees/employments/basesforemploymentfundcalculation/{baseforemploymentfundcalculationId}
# operationId: GetBaseForEmploymentFundCalculationByBaseForEmploymentFundCalculationId
export def "providers-employers-employees-employments-basesforemploymentfundcalculation GetBaseForEmploymentFundCalculationByBaseForEmploymentFundCalculationId" [
  baseforemploymentfundcalculationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/basesforemploymentfundcalculation/($baseforemploymentfundcalculationId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the properties of a base for employment fund calculation
#
# PUT /providers/employers/employees/employments/basesforemploymentfundcalculation/{baseforemploymentfundcalculationId}
# operationId: PutBaseForEmploymentFundCalculationByBaseForEmploymentFundCalculationId
export def "providers-employers-employees-employments-basesforemploymentfundcalculation PutBaseForEmploymentFundCalculationByBaseForEmploymentFundCalculationId" [
  baseforemploymentfundcalculationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/basesforemploymentfundcalculation/($baseforemploymentfundcalculationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific benefit or deduction record
#
# DELETE /providers/employers/employees/employments/benefitsAndDeductions/{benefitsanddeductionsId}
# operationId: DeleteBenefitsAndDeductionsByBenefitsAndDeductionsId
export def "providers-employers-employees-employments-benefits-and-deductions DeleteBenefitsAndDeductionsByBenefitsAndDeductionsId" [
  benefitsanddeductionsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/benefitsAndDeductions/($benefitsanddeductionsId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Detail of a benefit or deduction record
#
# GET /providers/employers/employees/employments/benefitsAndDeductions/{benefitsanddeductionsId}
# operationId: GetBenefitsAndDeductionsBybenefitsAndDeductionsId
export def "providers-employers-employees-employments-benefits-and-deductions GetBenefitsAndDeductionsBybenefitsAndDeductionsId" [
  benefitsanddeductionsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/benefitsAndDeductions/($benefitsanddeductionsId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a benefit or deduction record
#
# PUT /providers/employers/employees/employments/benefitsAndDeductions/{benefitsanddeductionsId}
# operationId: PutBenefitsAndDeductionsByBenefitsAndDeductionsId
export def "providers-employers-employees-employments-benefits-and-deductions PutBenefitsAndDeductionsByBenefitsAndDeductionsId" [
  benefitsanddeductionsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/benefitsAndDeductions/($benefitsanddeductionsId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a benefit in kind record
#
# DELETE /providers/employers/employees/employments/benefitsinkind/{benefitInKindId}
# operationId: DeleteBenefitInKindByBenefitInKindId
export def "providers-employers-employees-employments-benefitsinkind DeleteBenefitInKindByBenefitInKindId" [
  benefitInKindId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/benefitsinkind/($benefitInKindId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an benefit in kind
#
# GET /providers/employers/employees/employments/benefitsinkind/{benefitInKindId}
# operationId: GetBenefitInKindByBenefitInKindId
export def "providers-employers-employees-employments-benefitsinkind GetBenefitInKindByBenefitInKindId" [
  benefitInKindId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/benefitsinkind/($benefitInKindId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a benefit in kind record
#
# PUT /providers/employers/employees/employments/benefitsinkind/{benefitInKindId}
# operationId: PutBenefitInKindByBenefitInKindId
export def "providers-employers-employees-employments-benefitsinkind PutBenefitInKindByBenefitInKindId" [
  benefitInKindId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/benefitsinkind/($benefitInKindId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific company car record
#
# DELETE /providers/employers/employees/employments/companycars/{companyCarId}
# operationId: DeleteCompanyCarByCompanyCarId
export def "providers-employers-employees-employments-companycars DeleteCompanyCarByCompanyCarId" [
  companyCarId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/companycars/($companyCarId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a company car record
#
# GET /providers/employers/employees/employments/companycars/{companyCarId}
# operationId: GetCompanyCarByCompanyCarId
export def "providers-employers-employees-employments-companycars GetCompanyCarByCompanyCarId" [
  companyCarId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/companycars/($companyCarId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a company car record
#
# PUT /providers/employers/employees/employments/companycars/{companyCarId}
# operationId: PutCompanyCarByCompanyCarId
export def "providers-employers-employees-employments-companycars PutCompanyCarByCompanyCarId" [
  companyCarId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/companycars/($companyCarId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a cost per hour record
#
# DELETE /providers/employers/employees/employments/costperhour/{costperhourId}
# operationId: DeleteCostPerHourByCostPerHourId
export def "providers-employers-employees-employments-costperhour DeleteCostPerHourByCostPerHourId" [
  costperhourId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --costPerHourId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "costPerHourId" $costPerHourId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/costperhour/($costperhourId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of cost per hour
#
# GET /providers/employers/employees/employments/costperhour/{costperhourId}
# operationId: GetCostPerHourByCostPerHourId
export def "providers-employers-employees-employments-costperhour GetCostPerHourByCostPerHourId" [
  costperhourId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --costPerHourId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "costPerHourId" $costPerHourId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/costperhour/($costperhourId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a cost per hour record
#
# PUT /providers/employers/employees/employments/costperhour/{costperhourId}
# operationId: PutCostperhourByCostperhourId
export def "providers-employers-employees-employments-costperhour PutCostperhourByCostperhourId" [
  costperhourId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --costPerHourId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "costPerHourId" $costPerHourId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/costperhour/($costperhourId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an employment custom field record
#
# DELETE /providers/employers/employees/employments/customfields/{employmentCustomFieldId}
# operationId: DeleteEmploymentCustomFieldByEmploymentCustomFieldId
export def "providers-employers-employees-employments-customfields DeleteEmploymentCustomFieldByEmploymentCustomFieldId" [
  employmentCustomFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/customfields/($employmentCustomFieldId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an employment custom field
#
# GET /providers/employers/employees/employments/customfields/{employmentCustomFieldId}
# operationId: GetEmploymentCustomFieldByEmploymentCustomFieldId
export def "providers-employers-employees-employments-customfields GetEmploymentCustomFieldByEmploymentCustomFieldId" [
  employmentCustomFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/customfields/($employmentCustomFieldId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an employment custom field record
#
# PUT /providers/employers/employees/employments/customfields/{employmentCustomFieldId}
# operationId: PutEmploymentCustomFieldByEmploymentCustomFieldId
export def "providers-employers-employees-employments-customfields PutEmploymentCustomFieldByEmploymentCustomFieldId" [
  employmentCustomFieldId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/customfields/($employmentCustomFieldId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a custom notification record
#
# DELETE /providers/employers/employees/employments/customnotifications/{customNotificationId}
# operationId: DeleteCustomNotificationByCustomNotificationId
export def "providers-employers-employees-employments-customnotifications DeleteCustomNotificationByCustomNotificationId" [
  customNotificationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/customnotifications/($customNotificationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a custom notification
#
# GET /providers/employers/employees/employments/customnotifications/{customNotificationId}
# operationId: GetCustomNotificationByCustomNotificationId
export def "providers-employers-employees-employments-customnotifications GetCustomNotificationByCustomNotificationId" [
  customNotificationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/customnotifications/($customNotificationId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a custom notification record
#
# PUT /providers/employers/employees/employments/customnotifications/{customNotificationId}
# operationId: PutCustomNotificationByCustomNotificationId
export def "providers-employers-employees-employments-customnotifications PutCustomNotificationByCustomNotificationId" [
  customNotificationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/customnotifications/($customNotificationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Process approved declarations
#
# PATCH /providers/employers/employees/employments/declarations/process
# operationId: PatchProcessDeclarationsByDeclarationId
export def "providers-employers-employees-employments-declarations-process PatchProcessDeclarationsByDeclarationId" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/employees/employments/declarations/process")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Review (accept or reject) open declarations
#
# PATCH /providers/employers/employees/employments/declarations/review
# operationId: PatchReviewDeclarationsByDeclarationIds
export def "providers-employers-employees-employments-declarations-review PatchReviewDeclarationsByDeclarationIds" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/employees/employments/declarations/review")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of a declaration
#
# GET /providers/employers/employees/employments/declarations/{declarationId}
# operationId: GetDeclarationsByDeclarationId
export def "providers-employers-employees-employments-declarations GetDeclarationsByDeclarationId" [
  declarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/declarations/($declarationId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download attachment
#
# GET /providers/employers/employees/employments/declarations/{declarationId}/attachment
# operationId: GetAttachmentByDeclarationId
export def "providers-employers-employees-employments-declarations-attachment GetAttachmentByDeclarationId" [
  declarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/declarations/($declarationId)/attachment")
  let extra_headers = {"Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Audit trail of a declaration
#
# GET /providers/employers/employees/employments/declarations/{declarationId}/audittrail
# operationId: GetDeclarationAuditTrailByDeclarationId
export def "providers-employers-employees-employments-declarations-audittrail GetDeclarationAuditTrailByDeclarationId" [
  declarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/declarations/($declarationId)/audittrail" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a specific Deviating AWF contributions record
#
# DELETE /providers/employers/employees/employments/deviatingawfcontributions/{deviatingAwfContributionId}
# operationId: DeleteDeviatingAwfContributionByDeviatingAwfContributionId
export def "providers-employers-employees-employments-deviatingawfcontributions DeleteDeviatingAwfContributionByDeviatingAwfContributionId" [
  deviatingAwfContributionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/deviatingawfcontributions/($deviatingAwfContributionId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a Deviating AWF contributions record
#
# GET /providers/employers/employees/employments/deviatingawfcontributions/{deviatingAwfContributionId}
# operationId: GetDeviatingAwfContributionByDeviatingAwfContributionId
export def "providers-employers-employees-employments-deviatingawfcontributions GetDeviatingAwfContributionByDeviatingAwfContributionId" [
  deviatingAwfContributionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/deviatingawfcontributions/($deviatingAwfContributionId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a Deviating AWF contributions record
#
# PUT /providers/employers/employees/employments/deviatingawfcontributions/{deviatingAwfContributionId}
# operationId: PutDeviatingAwfContributionByDeviatingAwfContributionId
export def "providers-employers-employees-employments-deviatingawfcontributions PutDeviatingAwfContributionByDeviatingAwfContributionId" [
  deviatingAwfContributionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/deviatingawfcontributions/($deviatingAwfContributionId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a deviating hourly wage record
#
# DELETE /providers/employers/employees/employments/deviatinghourlywages/{deviatingHourlyWageId}
# operationId: DeleteDeviatingHourlyWageByDeviatingHourlyWageId
export def "providers-employers-employees-employments-deviatinghourlywages DeleteDeviatingHourlyWageByDeviatingHourlyWageId" [
  deviatingHourlyWageId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/deviatinghourlywages/($deviatingHourlyWageId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an deviating hourly wage
#
# GET /providers/employers/employees/employments/deviatinghourlywages/{deviatingHourlyWageId}
# operationId: GetDeviatingHourlyWageByDeviatingHourlyWageId
export def "providers-employers-employees-employments-deviatinghourlywages GetDeviatingHourlyWageByDeviatingHourlyWageId" [
  deviatingHourlyWageId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/deviatinghourlywages/($deviatingHourlyWageId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a deviating hourly wage record
#
# PUT /providers/employers/employees/employments/deviatinghourlywages/{deviatingHourlyWageId}
# operationId: PutDeviatingHourlyWageByDeviatingHourlyWageId
export def "providers-employers-employees-employments-deviatinghourlywages PutDeviatingHourlyWageByDeviatingHourlyWageId" [
  deviatingHourlyWageId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/deviatinghourlywages/($deviatingHourlyWageId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific employment fund record
#
# DELETE /providers/employers/employees/employments/employmentfunds/{employmentFundId}
# operationId: DeleteEmploymentFundByEmploymentFundId
export def "providers-employers-employees-employments-employmentfunds DeleteEmploymentFundByEmploymentFundId" [
  employmentFundId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/employmentfunds/($employmentFundId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an employment fund
#
# GET /providers/employers/employees/employments/employmentfunds/{employmentFundId}
# operationId: GetEmploymentFundByEmploymentFundId
export def "providers-employers-employees-employments-employmentfunds GetEmploymentFundByEmploymentFundId" [
  employmentFundId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/employmentfunds/($employmentFundId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a employment fund record
#
# PUT /providers/employers/employees/employments/employmentfunds/{employmentFundId}
# operationId: PutEmploymentFundByEmploymentFundId
export def "providers-employers-employees-employments-employmentfunds PutEmploymentFundByEmploymentFundId" [
  employmentFundId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/employmentfunds/($employmentFundId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific fiscal company car record
#
# DELETE /providers/employers/employees/employments/fiscalcompanycars/{fiscalCompanyCarId}
# operationId: DeleteFiscalCompanyCarByFiscalCompanyCarId
export def "providers-employers-employees-employments-fiscalcompanycars DeleteFiscalCompanyCarByFiscalCompanyCarId" [
  fiscalCompanyCarId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/fiscalcompanycars/($fiscalCompanyCarId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a fiscal company car record
#
# GET /providers/employers/employees/employments/fiscalcompanycars/{fiscalCompanyCarId}
# operationId: GetFiscalCompanyCarByFiscalCompanyCarId
export def "providers-employers-employees-employments-fiscalcompanycars GetFiscalCompanyCarByFiscalCompanyCarId" [
  fiscalCompanyCarId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/fiscalcompanycars/($fiscalCompanyCarId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a fiscal company car record
#
# PUT /providers/employers/employees/employments/fiscalcompanycars/{fiscalCompanyCarId}
# operationId: PutFiscalCompanyCarByFiscalCompanyCarId
export def "providers-employers-employees-employments-fiscalcompanycars PutFiscalCompanyCarByFiscalCompanyCarId" [
  fiscalCompanyCarId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/fiscalcompanycars/($fiscalCompanyCarId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific fiscal properties record
#
# DELETE /providers/employers/employees/employments/fiscalproperties/{fiscalPropertiesId}
# operationId: DeleteFiscalPropertiesByFiscalPropertiesId
export def "providers-employers-employees-employments-fiscalproperties DeleteFiscalPropertiesByFiscalPropertiesId" [
  fiscalPropertiesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/fiscalproperties/($fiscalPropertiesId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a fiscal record
#
# GET /providers/employers/employees/employments/fiscalproperties/{fiscalPropertiesId}
# operationId: GetFiscalPropertiesByFiscalPropertiesId
export def "providers-employers-employees-employments-fiscalproperties GetFiscalPropertiesByFiscalPropertiesId" [
  fiscalPropertiesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/fiscalproperties/($fiscalPropertiesId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a fiscal record
#
# PUT /providers/employers/employees/employments/fiscalproperties/{fiscalPropertiesId}
# operationId: PutFiscalPropertiesByFiscalPropertiesId
export def "providers-employers-employees-employments-fiscalproperties PutFiscalPropertiesByFiscalPropertiesId" [
  fiscalPropertiesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/fiscalproperties/($fiscalPropertiesId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific Healthcare Insurance Act record
#
# DELETE /providers/employers/employees/employments/healthcareinsuranceactconfigurations/{healthcareInsuranceActConfigurationId}
# operationId: DeleteHealthcareInsuranceActConfigurationByHealthcareInsuranceActConfigurationId
export def "providers-employers-employees-employments-healthcareinsuranceactconfigurations DeleteHealthcareInsuranceActConfigurationByHealthcareInsuranceActConfigurationId" [
  healthcareInsuranceActConfigurationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/healthcareinsuranceactconfigurations/($healthcareInsuranceActConfigurationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a Healthcare Insurance Act record
#
# GET /providers/employers/employees/employments/healthcareinsuranceactconfigurations/{healthcareInsuranceActConfigurationId}
# operationId: GetHealthcareInsuranceActConfigurationByHealthcareInsuranceActConfigurationId
export def "providers-employers-employees-employments-healthcareinsuranceactconfigurations GetHealthcareInsuranceActConfigurationByHealthcareInsuranceActConfigurationId" [
  healthcareInsuranceActConfigurationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/healthcareinsuranceactconfigurations/($healthcareInsuranceActConfigurationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a Healthcare Insurance Act record
#
# PUT /providers/employers/employees/employments/healthcareinsuranceactconfigurations/{healthcareInsuranceActConfigurationId}
# operationId: PutHealthcareInsuranceActConfigurationByHealthcareInsuranceActConfigurationId
export def "providers-employers-employees-employments-healthcareinsuranceactconfigurations PutHealthcareInsuranceActConfigurationByHealthcareInsuranceActConfigurationId" [
  healthcareInsuranceActConfigurationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/healthcareinsuranceactconfigurations/($healthcareInsuranceActConfigurationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific journal allocation record
#
# DELETE /providers/employers/employees/employments/journalallocations/{journalAllocationId}
# operationId: DeleteJournalAllocationByJournalAllocationId
export def "providers-employers-employees-employments-journalallocations DeleteJournalAllocationByJournalAllocationId" [
  journalAllocationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/journalallocations/($journalAllocationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a journal allocation
#
# GET /providers/employers/employees/employments/journalallocations/{journalAllocationId}
# operationId: GetJournalAllocationByJournalAllocationId
export def "providers-employers-employees-employments-journalallocations GetJournalAllocationByJournalAllocationId" [
  journalAllocationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/journalallocations/($journalAllocationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a journal allocation record
#
# PUT /providers/employers/employees/employments/journalallocations/{journalAllocationId}
# operationId: PutJournalAllocationByJournalAllocationId
export def "providers-employers-employees-employments-journalallocations PutJournalAllocationByJournalAllocationId" [
  journalAllocationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/journalallocations/($journalAllocationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific leave record
#
# DELETE /providers/employers/employees/employments/leave/{leaveId}
# operationId: DeleteLeaveByLeaveId
export def "providers-employers-employees-employments-leave DeleteLeaveByLeaveId" [
  leaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/leave/($leaveId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# A single leave entry
#
# GET /providers/employers/employees/employments/leave/{leaveId}
# operationId: GetLeaveByLeaveId
export def "providers-employers-employees-employments-leave GetLeaveByLeaveId" [
  leaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/leave/($leaveId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a leave
#
# PUT /providers/employers/employees/employments/leave/{leaveId}
# operationId: PutLeaveByLeaveId
export def "providers-employers-employees-employments-leave PutLeaveByLeaveId" [
  leaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/leave/($leaveId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get the values for the metadata fields
#
# GET /providers/employers/employees/employments/leave/{leaveId}/metadata
# operationId: GetLeaveMetadataByLeaveId
export def "providers-employers-employees-employments-leave-metadata GetLeaveMetadataByLeaveId" [
  leaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/leave/($leaveId)/metadata" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the status of leave requests
#
# PATCH /providers/employers/employees/employments/leaverequests
# operationId: PatchLeaveRequestsByLeaveRequestIds
export def "providers-employers-employees-employments-leaverequests PatchLeaveRequestsByLeaveRequestIds" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/employees/employments/leaverequests")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of a leave request
#
# GET /providers/employers/employees/employments/leaverequests/{leaveRequestId}
# operationId: GetLeaveRequestByLeaveRequestId
export def "providers-employers-employees-employments-leaverequests GetLeaveRequestByLeaveRequestId" [
  leaveRequestId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/leaverequests/($leaveRequestId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the details of a leave request
#
# PUT /providers/employers/employees/employments/leaverequests/{leaveRequestId}
# operationId: PutLeaveRequestByLeaveRequestId
export def "providers-employers-employees-employments-leaverequests PutLeaveRequestByLeaveRequestId" [
  leaveRequestId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/leaverequests/($leaveRequestId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an employment note record
#
# DELETE /providers/employers/employees/employments/notes/{noteId}
# operationId: DeleteEmploymentNoteByNoteId
export def "providers-employers-employees-employments-notes DeleteEmploymentNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/notes/($noteId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an employment note
#
# GET /providers/employers/employees/employments/notes/{noteId}
# operationId: GetEmploymentNoteByNoteId
export def "providers-employers-employees-employments-notes GetEmploymentNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/notes/($noteId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an employment note record
#
# PUT /providers/employers/employees/employments/notes/{noteId}
# operationId: PutEmploymentNoteByNoteId
export def "providers-employers-employees-employments-notes PutEmploymentNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/notes/($noteId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific organizational entity record
#
# DELETE /providers/employers/employees/employments/organizationalentities/{organizationalEntityId}
# operationId: DeleteOrganizationalEntityByOrganizationalEntityId
export def "providers-employers-employees-employments-organizationalentities DeleteOrganizationalEntityByOrganizationalEntityId" [
  organizationalEntityId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/organizationalentities/($organizationalEntityId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an organizational entity
#
# GET /providers/employers/employees/employments/organizationalentities/{organizationalEntityId}
# operationId: GetOrganizationalEntityByOrganizationalEntityId
export def "providers-employers-employees-employments-organizationalentities GetOrganizationalEntityByOrganizationalEntityId" [
  organizationalEntityId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/organizationalentities/($organizationalEntityId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an organizational entity
#
# PUT /providers/employers/employees/employments/organizationalentities/{organizationalEntityId}
# operationId: PutOrganizationalEntityByOrganizationalEntityId
export def "providers-employers-employees-employments-organizationalentities PutOrganizationalEntityByOrganizationalEntityId" [
  organizationalEntityId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/organizationalentities/($organizationalEntityId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific other payroll variables record
#
# DELETE /providers/employers/employees/employments/otherPayrollVariables/{otherPayrollVariablesId}
# operationId: DeleteOtherPayrollVariablesByOtherPayrollVariablesId
export def "providers-employers-employees-employments-other-payroll-variables DeleteOtherPayrollVariablesByOtherPayrollVariablesId" [
  otherPayrollVariablesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/otherPayrollVariables/($otherPayrollVariablesId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of other payroll variables for an employment
#
# GET /providers/employers/employees/employments/otherPayrollVariables/{otherPayrollVariablesId}
# operationId: GetOtherPayrollVariablesByOtherPayrollVariablesId
export def "providers-employers-employees-employments-other-payroll-variables GetOtherPayrollVariablesByOtherPayrollVariablesId" [
  otherPayrollVariablesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/otherPayrollVariables/($otherPayrollVariablesId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the details of an other payroll variables record
#
# PUT /providers/employers/employees/employments/otherPayrollVariables/{otherPayrollVariablesId}
# operationId: PutOtherPayrollVariablesByOtherPayrollVariablesId
export def "providers-employers-employees-employments-other-payroll-variables PutOtherPayrollVariablesByOtherPayrollVariablesId" [
  otherPayrollVariablesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/otherPayrollVariables/($otherPayrollVariablesId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a payment information non-SEPA record
#
# DELETE /providers/employers/employees/employments/paymentinformationnonsepa/{paymentInformationNonSepaId}
# operationId: DeletePaymentInformationNonSepaByPaymentInformationNonSepaId
export def "providers-employers-employees-employments-paymentinformationnonsepa DeletePaymentInformationNonSepaByPaymentInformationNonSepaId" [
  paymentInformationNonSepaId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationnonsepa/($paymentInformationNonSepaId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the details of a payment information non-SEPA
#
# GET /providers/employers/employees/employments/paymentinformationnonsepa/{paymentInformationNonSepaId}
# operationId: GetPaymentInformationNonSepaBypaymentInformationNonSepaId
export def "providers-employers-employees-employments-paymentinformationnonsepa GetPaymentInformationNonSepaBypaymentInformationNonSepaId" [
  paymentInformationNonSepaId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationnonsepa/($paymentInformationNonSepaId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the details of a payment information non-SEPA
#
# PUT /providers/employers/employees/employments/paymentinformationnonsepa/{paymentInformationNonSepaId}
# operationId: PutPaymentInformationNonSepaByPaymentInformationNonSepaId
export def "providers-employers-employees-employments-paymentinformationnonsepa PutPaymentInformationNonSepaByPaymentInformationNonSepaId" [
  paymentInformationNonSepaId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationnonsepa/($paymentInformationNonSepaId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific SEPA payment-record record
#
# DELETE /providers/employers/employees/employments/paymentinformationsepa/{paymentInformationSepaId}
# operationId: DeletePaymentInformationSepaBypaymentInformationSepaId
export def "providers-employers-employees-employments-paymentinformationsepa DeletePaymentInformationSepaBypaymentInformationSepaId" [
  paymentInformationSepaId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationsepa/($paymentInformationSepaId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the details of a payment information sepa
#
# GET /providers/employers/employees/employments/paymentinformationsepa/{paymentInformationSepaId}
# operationId: GetPaymentInformationSepaBypaymentInformationSepaId
export def "providers-employers-employees-employments-paymentinformationsepa GetPaymentInformationSepaBypaymentInformationSepaId" [
  paymentInformationSepaId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationsepa/($paymentInformationSepaId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# change the details of a payment information sepa
#
# PUT /providers/employers/employees/employments/paymentinformationsepa/{paymentInformationSepaId}
# operationId: PutPaymentInformationSepaByPaymentInformationSepaId
export def "providers-employers-employees-employments-paymentinformationsepa PutPaymentInformationSepaByPaymentInformationSepaId" [
  paymentInformationSepaId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationsepa/($paymentInformationSepaId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a payment information SEPA separate payment record
#
# DELETE /providers/employers/employees/employments/paymentinformationsepaseparatepayments/{paymentInformationSepaSeparatePaymentId}
# operationId: DeletePaymentInformationSepaSeparatePaymentByPaymentInformationSepaSeparatePaymentId
export def "providers-employers-employees-employments-paymentinformationsepaseparatepayments DeletePaymentInformationSepaSeparatePaymentByPaymentInformationSepaSeparatePaymentId" [
  paymentInformationSepaSeparatePaymentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationsepaseparatepayments/($paymentInformationSepaSeparatePaymentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the details of a payment information SEPA separate payment record
#
# GET /providers/employers/employees/employments/paymentinformationsepaseparatepayments/{paymentInformationSepaSeparatePaymentId}
# operationId: GetPaymentInformationSepaSeparatePaymentByPaymentInformationSepaSeparatePaymentId
export def "providers-employers-employees-employments-paymentinformationsepaseparatepayments GetPaymentInformationSepaSeparatePaymentByPaymentInformationSepaSeparatePaymentId" [
  paymentInformationSepaSeparatePaymentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationsepaseparatepayments/($paymentInformationSepaSeparatePaymentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a payment information separate payment record
#
# PUT /providers/employers/employees/employments/paymentinformationsepaseparatepayments/{paymentInformationSepaSeparatePaymentId}
# operationId: PutPaymentInformationSepaSeparatePaymentByPaymentInformationSepaSeparatePaymentId
export def "providers-employers-employees-employments-paymentinformationsepaseparatepayments PutPaymentInformationSepaSeparatePaymentByPaymentInformationSepaSeparatePaymentId" [
  paymentInformationSepaSeparatePaymentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/paymentinformationsepaseparatepayments/($paymentInformationSepaSeparatePaymentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific payroll period data record
#
# DELETE /providers/employers/employees/employments/payrollperioddata/{payrollperioddataId}
# operationId: DeletePayrollPeriodDataByPayrollPeriodDataId
export def "providers-employers-employees-employments-payrollperioddata DeletePayrollPeriodDataByPayrollPeriodDataId" [
  payrollperioddataId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/payrollperioddata/($payrollperioddataId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a payroll period data
#
# GET /providers/employers/employees/employments/payrollperioddata/{payrollperioddataId}
# operationId: GetPayrollPeriodDataByPayrollPeriodDataId
export def "providers-employers-employees-employments-payrollperioddata GetPayrollPeriodDataByPayrollPeriodDataId" [
  payrollperioddataId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/payrollperioddata/($payrollperioddataId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an payroll period data
#
# PUT /providers/employers/employees/employments/payrollperioddata/{payrollperioddataId}
# operationId: PutPayrollPeriodDataByPayrollPeriodDataId
export def "providers-employers-employees-employments-payrollperioddata PutPayrollPeriodDataByPayrollPeriodDataId" [
  payrollperioddataId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/payrollperioddata/($payrollperioddataId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a pension benefit record
#
# DELETE /providers/employers/employees/employments/pensionbenefits/{pensionBenefitId}
# operationId: DeletePensionBenefitByPensionBenefitId
export def "providers-employers-employees-employments-pensionbenefits DeletePensionBenefitByPensionBenefitId" [
  pensionBenefitId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/pensionbenefits/($pensionBenefitId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an pension benefit
#
# GET /providers/employers/employees/employments/pensionbenefits/{pensionBenefitId}
# operationId: GetPensionBenefitByPensionBenefitId
export def "providers-employers-employees-employments-pensionbenefits GetPensionBenefitByPensionBenefitId" [
  pensionBenefitId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/pensionbenefits/($pensionBenefitId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a pension benefit record
#
# PUT /providers/employers/employees/employments/pensionbenefits/{pensionBenefitId}
# operationId: PutPensionBenefitByPensionBenefitId
export def "providers-employers-employees-employments-pensionbenefits PutPensionBenefitByPensionBenefitId" [
  pensionBenefitId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/pensionbenefits/($pensionBenefitId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an protected earnings record
#
# DELETE /providers/employers/employees/employments/protectedearnings/{protectedEarningsId}
# operationId: DeleteProtectedEarningsByProtectedEarningsId
export def "providers-employers-employees-employments-protectedearnings DeleteProtectedEarningsByProtectedEarningsId" [
  protectedEarningsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/protectedearnings/($protectedEarningsId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the details of an protected earnings record
#
# GET /providers/employers/employees/employments/protectedearnings/{protectedEarningsId}
# operationId: GetProtectedEarningsByProtectedEarningsId
export def "providers-employers-employees-employments-protectedearnings GetProtectedEarningsByProtectedEarningsId" [
  protectedEarningsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/protectedearnings/($protectedEarningsId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an protected earnings record
#
# PUT /providers/employers/employees/employments/protectedearnings/{protectedEarningsId}
# operationId: PutProtectedEarningsByProtectedEarningsId
export def "providers-employers-employees-employments-protectedearnings PutProtectedEarningsByProtectedEarningsId" [
  protectedEarningsId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/protectedearnings/($protectedEarningsId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific social security benefits record
#
# DELETE /providers/employers/employees/employments/socialsecuritybenefits/{socialSecurityBenefitId}
# operationId: DeleteSocialSecurityBenefitBySocialSecurityBenefitId
export def "providers-employers-employees-employments-socialsecuritybenefits DeleteSocialSecurityBenefitBySocialSecurityBenefitId" [
  socialSecurityBenefitId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/socialsecuritybenefits/($socialSecurityBenefitId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a social security benefits record
#
# GET /providers/employers/employees/employments/socialsecuritybenefits/{socialSecurityBenefitId}
# operationId: GetSocialSecurityBenefitBySocialSecurityBenefitId
export def "providers-employers-employees-employments-socialsecuritybenefits GetSocialSecurityBenefitBySocialSecurityBenefitId" [
  socialSecurityBenefitId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/socialsecuritybenefits/($socialSecurityBenefitId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a social security benefits record
#
# PUT /providers/employers/employees/employments/socialsecuritybenefits/{socialSecurityBenefitId}
# operationId: PutSocialSecurityBenefitBySocialSecurityBenefitId
export def "providers-employers-employees-employments-socialsecuritybenefits PutSocialSecurityBenefitBySocialSecurityBenefitId" [
  socialSecurityBenefitId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/socialsecuritybenefits/($socialSecurityBenefitId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific social security record
#
# DELETE /providers/employers/employees/employments/socialsecurityconfigurations/{socialSecurityConfigurationId}
# operationId: DeleteSocialSecurityConfigurationBySocialSecurityConfigurationId
export def "providers-employers-employees-employments-socialsecurityconfigurations DeleteSocialSecurityConfigurationBySocialSecurityConfigurationId" [
  socialSecurityConfigurationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/socialsecurityconfigurations/($socialSecurityConfigurationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a social security record
#
# GET /providers/employers/employees/employments/socialsecurityconfigurations/{socialSecurityConfigurationId}
# operationId: GetSocialSecurityConfigurationBySocialSecurityConfigurationId
export def "providers-employers-employees-employments-socialsecurityconfigurations GetSocialSecurityConfigurationBySocialSecurityConfigurationId" [
  socialSecurityConfigurationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/socialsecurityconfigurations/($socialSecurityConfigurationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a social security record
#
# PUT /providers/employers/employees/employments/socialsecurityconfigurations/{socialSecurityConfigurationId}
# operationId: PutSocialSecurityConfigurationBySocialSecurityConfigurationId
export def "providers-employers-employees-employments-socialsecurityconfigurations PutSocialSecurityConfigurationBySocialSecurityConfigurationId" [
  socialSecurityConfigurationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/socialsecurityconfigurations/($socialSecurityConfigurationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a wachtgeld record
#
# DELETE /providers/employers/employees/employments/wachtgeld/{wachtegeldenId}
# operationId: DeleteWachtgeldByWachtgeldId
export def "providers-employers-employees-employments-wachtgeld DeleteWachtgeldByWachtgeldId" [
  wachtegeldenId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --wachtgeldId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "wachtgeldId" $wachtgeldId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/wachtgeld/($wachtegeldenId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of wachtgeld
#
# GET /providers/employers/employees/employments/wachtgeld/{wachtegeldenId}
# operationId: GetWachtgeldByWachtgeldId
export def "providers-employers-employees-employments-wachtgeld GetWachtgeldByWachtgeldId" [
  wachtegeldenId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --wachtgeldId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "wachtgeldId" $wachtgeldId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/wachtgeld/($wachtegeldenId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a wachtgeld record
#
# PUT /providers/employers/employees/employments/wachtgeld/{wachtegeldenId}
# operationId: PutWachtgeldByWachtgeldId
export def "providers-employers-employees-employments-wachtgeld PutWachtgeldByWachtgeldId" [
  wachtegeldenId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --wachtgeldId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "wachtgeldId" $wachtgeldId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/wachtgeld/($wachtegeldenId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific wage record
#
# DELETE /providers/employers/employees/employments/wages/{wageId}
# operationId: DeleteWageByWageId
export def "providers-employers-employees-employments-wages DeleteWageByWageId" [
  wageId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/wages/($wageId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a single wage
#
# GET /providers/employers/employees/employments/wages/{wageId}
# operationId: GetWageByWageId
export def "providers-employers-employees-employments-wages GetWageByWageId" [
  wageId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/wages/($wageId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a wage
#
# PUT /providers/employers/employees/employments/wages/{wageId}
# operationId: PutWageByWageId
export def "providers-employers-employees-employments-wages PutWageByWageId" [
  wageId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/wages/($wageId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific working hours record
#
# DELETE /providers/employers/employees/employments/workinghours/{workinghoursId}
# operationId: DeleteWorkingHoursByWorkingHoursId
export def "providers-employers-employees-employments-workinghours DeleteWorkingHoursByWorkingHoursId" [
  workinghoursId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/workinghours/($workinghoursId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of the working hours
#
# GET /providers/employers/employees/employments/workinghours/{workinghoursId}
# operationId: GetWorkingHoursByWorkingHoursId
export def "providers-employers-employees-employments-workinghours GetWorkingHoursByWorkingHoursId" [
  workinghoursId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/workinghours/($workinghoursId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the working hours
#
# PUT /providers/employers/employees/employments/workinghours/{workinghoursId}
# operationId: PutWorkingHoursByWorkingHoursId
export def "providers-employers-employees-employments-workinghours PutWorkingHoursByWorkingHoursId" [
  workinghoursId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/workinghours/($workinghoursId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of an employment
#
# GET /providers/employers/employees/employments/{employmentId}
# operationId: GetEmploymentByEmploymentId
export def "providers-employers-employees-employments GetEmploymentByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2019-12-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the type of an employment
#
# PATCH /providers/employers/employees/employments/{employmentId}
# operationId: PatchTypeOfEmploymentCategoryTypeFieldsByEmploymentId
export def "providers-employers-employees-employments PatchTypeOfEmploymentCategoryTypeFieldsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2019-12-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2019-12-16" $body
}

# Edit the details of an employment
#
# PUT /providers/employers/employees/employments/{employmentId}
# operationId: PutEmploymentByEmploymentId
export def "providers-employers-employees-employments PutEmploymentByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2019-12-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2019-12-16" $body
}

# List of abp funds the employment partakes in
#
# GET /providers/employers/employees/employments/{employmentId}/abpfunds
# operationId: GetAbpFundsByEmploymentId
export def "providers-employers-employees-employments-abpfunds GetAbpFundsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/abpfunds" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Activate an abp fund for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/abpfunds
# operationId: PostAbpFundByEmploymentId
export def "providers-employers-employees-employments-abpfunds PostAbpFundByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/abpfunds")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of attachment of earnings records
#
# GET /providers/employers/employees/employments/{employmentId}/attachmentsofearnings
# operationId: GetAttachmentOfEarningsByEmploymentId
export def "providers-employers-employees-employments-attachmentsofearnings GetAttachmentOfEarningsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/attachmentsofearnings" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new attachment of earnings record
#
# POST /providers/employers/employees/employments/{employmentId}/attachmentsofearnings
# operationId: PostAttachmentOfEarningsByEmploymentId
export def "providers-employers-employees-employments-attachmentsofearnings PostAttachmentOfEarningsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/attachmentsofearnings")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "applicationjson;version=2018-01-01" $body
}

# List of bases for employment fund calculations.
#
# GET /providers/employers/employees/employments/{employmentId}/basesforcalculation
# operationId: GetBasesForCalculationByEmploymentId
export def "providers-employers-employees-employments-basesforcalculation GetBasesForCalculationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/basesforcalculation" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a base for calculation
#
# POST /providers/employers/employees/employments/{employmentId}/basesforcalculation
# operationId: PostBaseFundCalculationByEmploymentId
export def "providers-employers-employees-employments-basesforcalculation PostBaseFundCalculationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/basesforcalculation")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of bases for employment fund calculations.
#
# GET /providers/employers/employees/employments/{employmentId}/basesforemploymentfundcalculation
# operationId: GetBasesForEmploymentFundCalculationByEmploymentId
export def "providers-employers-employees-employments-basesforemploymentfundcalculation GetBasesForEmploymentFundCalculationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/basesforemploymentfundcalculation" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a base for employment fund calculation
#
# POST /providers/employers/employees/employments/{employmentId}/basesforemploymentfundcalculation
# operationId: PostBaseForEmploymentFundCalculationByEmploymentId
export def "providers-employers-employees-employments-basesforemploymentfundcalculation PostBaseForEmploymentFundCalculationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/basesforemploymentfundcalculation")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List Benefits in kind
#
# GET /providers/employers/employees/employments/{employmentId}/benefitsInKind
# operationId: GetBenefitsInKindByEmploymentId
export def "providers-employers-employees-employments-benefits-in-kind GetBenefitsInKindByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/benefitsInKind" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a Benefit in kind for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/benefitsInKind
# operationId: PostBenefitInKindByEmploymentId
export def "providers-employers-employees-employments-benefits-in-kind PostBenefitInKindByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/benefitsInKind")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# list of benefits and deductions
#
# GET /providers/employers/employees/employments/{employmentId}/benefitsanddeductions
# operationId: GetBenefitsAndDeductionsByEmploymentId
export def "providers-employers-employees-employments-benefitsanddeductions GetBenefitsAndDeductionsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/benefitsanddeductions" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a benefit or deduction record for the employment
#
# POST /providers/employers/employees/employments/{employmentId}/benefitsanddeductions
# operationId: PostbenefitsAndDeductionsByEmploymentId
export def "providers-employers-employees-employments-benefitsanddeductions PostbenefitsAndDeductionsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/benefitsanddeductions")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of  company cars for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/companycars
# operationId: GetCompanyCarsByEmploymentId
export def "providers-employers-employees-employments-companycars GetCompanyCarsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/companycars" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a company car record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/companycars
# operationId: PostCompanyCarByEmploymentId
export def "providers-employers-employees-employments-companycars PostCompanyCarByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/companycars")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Correct the start (employment) date for an employment.
#
# PATCH /providers/employers/employees/employments/{employmentId}/correctstartdate
# operationId: PatchCorrectStartDateByEmploymentId
export def "providers-employers-employees-employments-correctstartdate PatchCorrectStartDateByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/correctstartdate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of costs per hour for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/costperhour
# operationId: GetCostPerHourByEmploymentId
export def "providers-employers-employees-employments-costperhour GetCostPerHourByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --includeEstimation: oneof<nothing, bool> # include an estimation if no cost per unit records are present (e.g. true)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "includeEstimation" $includeEstimation "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/costperhour" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a cost per hour for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/costperhour
# operationId: PostCostPerHourByEmploymentId
export def "providers-employers-employees-employments-costperhour PostCostPerHourByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/costperhour")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List employment custom fields for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/customfields
# operationId: GetEmploymentCustomFieldssByEmploymentId
export def "providers-employers-employees-employments-customfields GetEmploymentCustomFieldssByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/customfields" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an employment custom field for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/customfields
# operationId: PostEmploymentCustomFieldByEmploymentId
export def "providers-employers-employees-employments-customfields PostEmploymentCustomFieldByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/customfields")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List custom notification
#
# GET /providers/employers/employees/employments/{employmentId}/customnotifications
# operationId: GetCustomNotificationsByEmploymentId
export def "providers-employers-employees-employments-customnotifications GetCustomNotificationsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/customnotifications" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a custom notification for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/customnotifications
# operationId: PostCustomNotificationByEmploymentId
export def "providers-employers-employees-employments-customnotifications PostCustomNotificationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/customnotifications")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of declarations for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/declarations
# operationId: GetDeclarationsByEmploymentId
export def "providers-employers-employees-employments-declarations GetDeclarationsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/declarations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of Deviating AWF contributions records for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/deviatingawfcontributions
# operationId: GetDeviatingAwfContributionByEmploymentId
export def "providers-employers-employees-employments-deviatingawfcontributions GetDeviatingAwfContributionByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/deviatingawfcontributions" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a Deviating AWF contributions record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/deviatingawfcontributions
# operationId: PostDeviatingAwfContributionByEmploymentId
export def "providers-employers-employees-employments-deviatingawfcontributions PostDeviatingAwfContributionByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/deviatingawfcontributions")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List deviating hourly wage
#
# GET /providers/employers/employees/employments/{employmentId}/deviatinghourlywages
# operationId: GetDeviatingHourlyWagesByEmploymentId
export def "providers-employers-employees-employments-deviatinghourlywages GetDeviatingHourlyWagesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/deviatinghourlywages" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a deviating hourly wage for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/deviatinghourlywages
# operationId: PostDeviatingHourlyWageByEmploymentId
export def "providers-employers-employees-employments-deviatinghourlywages PostDeviatingHourlyWageByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/deviatinghourlywages")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of employment dossier documents
#
# GET /providers/employers/employees/employments/{employmentId}/documents
# operationId: GetDocumentsByEmploymentId
export def "providers-employers-employees-employments-documents GetDocumentsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload a document to the employment dossier
#
# POST /providers/employers/employees/employments/{employmentId}/documents
# operationId: PostDocumentByEmploymentId
export def "providers-employers-employees-employments-documents PostDocumentByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a document in the employment dossier
#
# DELETE /providers/employers/employees/employments/{employmentId}/documents/{documentId}
# operationId: DeleteDocumentByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents DeleteDocumentByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download employment dossier document
#
# GET /providers/employers/employees/employments/{employmentId}/documents/{documentId}
# operationId: GetDocumentByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents GetDocumentByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)")
  let extra_headers = {"Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an employment dossier document
#
# PUT /providers/employers/employees/employments/{employmentId}/documents/{documentId}
# operationId: PutDocumentByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents PutDocumentByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get audittrail for the document in the employment dossier
#
# GET /providers/employers/employees/employments/{employmentId}/documents/{documentId}/audittrail
# operationId: GetAuditTrailByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents-audittrail GetAuditTrailByEmploymentIdAndDocumentId" [
  documentId: string
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)/audittrail" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete signature - Employment
#
# DELETE /providers/employers/employees/employments/{employmentId}/documents/{documentId}/signature
# operationId: DeleteSignatureByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents-signature DeleteSignatureByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)/signature")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Signature details - Employment
#
# GET /providers/employers/employees/employments/{employmentId}/documents/{documentId}/signature
# operationId: GetSignatureByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents-signature GetSignatureByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)/signature")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate signature - Employment
#
# POST /providers/employers/employees/employments/{employmentId}/documents/{documentId}/signature/initiate
# operationId: PostInitiateSignatureByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents-signature-initiate PostInitiateSignatureByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)/signature/initiate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Retrieve the report of the signing - Employment
#
# GET /providers/employers/employees/employments/{employmentId}/documents/{documentId}/signature/report
# operationId: GetSignatureReportByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documents-signature-report GetSignatureReportByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documents/($documentId)/signature/report")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Generate a document for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/documenttemplates/{documentId}/generatedocument
# operationId: PostGenerateDocumentByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documenttemplates-generatedocument PostGenerateDocumentByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documenttemplates/($documentId)/generatedocument")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Generate a document for an employment - preview
#
# POST /providers/employers/employees/employments/{employmentId}/documenttemplates/{documentId}/generatedocument/preview
# operationId: PostGenerateDocumentPreviewByEmploymentIdAndDocumentId
export def "providers-employers-employees-employments-documenttemplates-generatedocument-preview PostGenerateDocumentPreviewByEmploymentIdAndDocumentId" [
  employmentId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/documenttemplates/($documentId)/generatedocument/preview")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Activate employee profile for the employment
#
# PATCH /providers/employers/employees/employments/{employmentId}/employeeprofile
# operationId: PatchEmployeeProfileByEmploymentId
export def "providers-employers-employees-employments-employeeprofile PatchEmployeeProfileByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/employeeprofile")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of funds the employment partakes in
#
# GET /providers/employers/employees/employments/{employmentId}/employmentfunds
# operationId: GetEmploymentFundsByEmploymentId
export def "providers-employers-employees-employments-employmentfunds GetEmploymentFundsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/employmentfunds" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Activate a fund for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/employmentfunds
# operationId: PostEmploymentFundByEmploymentId
export def "providers-employers-employees-employments-employmentfunds PostEmploymentFundByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/employmentfunds")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of fiscal company cars for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/fiscalcompanycars
# operationId: GetFiscalCompanyCarsByEmploymentId
export def "providers-employers-employees-employments-fiscalcompanycars GetFiscalCompanyCarsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/fiscalcompanycars" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a fiscal company car record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/fiscalcompanycars
# operationId: PostFiscalCompanyCarByEmploymentId
export def "providers-employers-employees-employments-fiscalcompanycars PostFiscalCompanyCarByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/fiscalcompanycars")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of fiscal properties for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/fiscalproperties
# operationId: GetFiscalPropertiesByEmploymentId
export def "providers-employers-employees-employments-fiscalproperties GetFiscalPropertiesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/fiscalproperties" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a fiscal record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/fiscalproperties
# operationId: PostFiscalPropertiesByEmploymentId
export def "providers-employers-employees-employments-fiscalproperties PostFiscalPropertiesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/fiscalproperties")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of Healthcare Insurance Act records for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/healthcareinsuranceactconfigurations
# operationId: GetHealthcareInsuranceActConfigurationByEmploymentId
export def "providers-employers-employees-employments-healthcareinsuranceactconfigurations GetHealthcareInsuranceActConfigurationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/healthcareinsuranceactconfigurations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a Healthcare Insurance Act record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/healthcareinsuranceactconfigurations
# operationId: PostHealthcareInsuranceActConfigurationByEmploymentId
export def "providers-employers-employees-employments-healthcareinsuranceactconfigurations PostHealthcareInsuranceActConfigurationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/healthcareinsuranceactconfigurations")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of journal allocations for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/journalallocations
# operationId: GetJournalAllocationsByEmploymentId
export def "providers-employers-employees-employments-journalallocations GetJournalAllocationsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/journalallocations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new journal allocation record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/journalallocations
# operationId: PostJournalAllocationByEmploymentId
export def "providers-employers-employees-employments-journalallocations PostJournalAllocationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/journalallocations")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of all the leave entries
#
# GET /providers/employers/employees/employments/{employmentId}/leave
# operationId: GetLeaveByEmploymentId
export def "providers-employers-employees-employments-leave GetLeaveByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a leave record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/leave
# operationId: PostLeaveByEmploymentId
export def "providers-employers-employees-employments-leave PostLeaveByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leave")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get the default values to be used when POSTing a new record
#
# GET /providers/employers/employees/employments/{employmentId}/leave/defaults
# operationId: GetLeaveDefaultsByEmploymentId
export def "providers-employers-employees-employments-leave-defaults GetLeaveDefaultsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leave/defaults" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the values for the metadata fields
#
# GET /providers/employers/employees/employments/{employmentId}/leave/metadata
# operationId: GetLeaveMetadataByEmploymentId
export def "providers-employers-employees-employments-leave-metadata GetLeaveMetadataByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leave/metadata" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the leave overview for an employment and year
#
# GET /providers/employers/employees/employments/{employmentId}/leave/overview/{year}
# operationId: GetLeaveOverviewByEmploymentIdandYear
export def "providers-employers-employees-employments-leave-overview GetLeaveOverviewByEmploymentIdandYear" [
  employmentId: string
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leave/overview/($year)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the proposed number of leave hours for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/leave/proposedleavehours
# operationId: GetProposedLeaveHoursByEmploymentId
export def "providers-employers-employees-employments-leave-proposedleavehours GetProposedLeaveHoursByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --startDate: string # The start date of the leave period (e.g. 2022-08-01)
  --endDate: string # The end date of the leave period (e.g. 2022-08-07)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leave/proposedleavehours" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of yearly leave balances for each leave type.
#
# GET /providers/employers/employees/employments/{employmentId}/leavebalances
# operationId: GetLeaveBalancesByEmploymentId
export def "providers-employers-employees-employments-leavebalances GetLeaveBalancesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leavebalances" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get list of linked leave policies for the employment
#
# GET /providers/employers/employees/employments/{employmentId}/leavepolicies
# operationId: GetLeavePoliciesByEmploymentId
export def "providers-employers-employees-employments-leavepolicies GetLeavePoliciesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leavepolicies" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Link or unlink leave policies for the employment
#
# PATCH /providers/employers/employees/employments/{employmentId}/leavepolicies
# operationId: PatchLeavePoliciesByEmploymentId
export def "providers-employers-employees-employments-leavepolicies PatchLeavePoliciesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leavepolicies")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# leave requests by employment
#
# GET /providers/employers/employees/employments/{employmentId}/leaverequests
# operationId: GetLeaveRequestsByEmploymentId
export def "providers-employers-employees-employments-leaverequests GetLeaveRequestsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/leaverequests" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of notes for an employment.
#
# GET /providers/employers/employees/employments/{employmentId}/notes
# operationId: GetEmploymentNotesByEmploymentId
export def "providers-employers-employees-employments-notes GetEmploymentNotesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/notes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a note for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/notes
# operationId: PostEmploymentNoteByEmploymentId
export def "providers-employers-employees-employments-notes PostEmploymentNoteByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/notes")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of organizational entities for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/organizationalentities
# operationId: GetOrganizationalEntitiesByEmploymentId
export def "providers-employers-employees-employments-organizationalentities GetOrganizationalEntitiesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/organizationalentities" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an organizational entity for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/organizationalentities
# operationId: PostOrganizationalEntityByEmploymentId
export def "providers-employers-employees-employments-organizationalentities PostOrganizationalEntityByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/organizationalentities")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of other payroll variables for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/otherPayrollVariables
# operationId: GetOtherPayrollVariablesByEmploymentId
export def "providers-employers-employees-employments-other-payroll-variables GetOtherPayrollVariablesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/otherPayrollVariables" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add other payroll variables for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/otherPayrollVariables
# operationId: PostOtherPayrollVariablesByEmploymentId
export def "providers-employers-employees-employments-other-payroll-variables PostOtherPayrollVariablesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/otherPayrollVariables")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of payment information non-SEPA
#
# GET /providers/employers/employees/employments/{employmentId}/paymentinformationnonsepa
# operationId: GetPaymentInformationNonSepaByEmploymentId
export def "providers-employers-employees-employments-paymentinformationnonsepa GetPaymentInformationNonSepaByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/paymentinformationnonsepa" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new payment information non-SEPA record
#
# POST /providers/employers/employees/employments/{employmentId}/paymentinformationnonsepa
# operationId: PostPaymentInformationNonSepaByEmploymentId
export def "providers-employers-employees-employments-paymentinformationnonsepa PostPaymentInformationNonSepaByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/paymentinformationnonsepa")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "applicationjson;version=2018-01-01" $body
}

# Get a list of payment information sepa
#
# GET /providers/employers/employees/employments/{employmentId}/paymentinformationsepa
# operationId: GetPaymentInformationSepaByEmploymentId
export def "providers-employers-employees-employments-paymentinformationsepa GetPaymentInformationSepaByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/paymentinformationsepa" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# create a new payment information sepa record
#
# POST /providers/employers/employees/employments/{employmentId}/paymentinformationsepa
# operationId: PostPaymentInformationSepaByEmploymentId
export def "providers-employers-employees-employments-paymentinformationsepa PostPaymentInformationSepaByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/paymentinformationsepa")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "applicationjson;version=2018-01-01" $body
}

# Get a list of payment information SEPA separate payment records
#
# GET /providers/employers/employees/employments/{employmentId}/paymentinformationsepaseparatepayments
# operationId: GetPaymentInformationSepaSeparatePaymentByEmploymentId
export def "providers-employers-employees-employments-paymentinformationsepaseparatepayments GetPaymentInformationSepaSeparatePaymentByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/paymentinformationsepaseparatepayments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new payment information SEPA separate payment record
#
# POST /providers/employers/employees/employments/{employmentId}/paymentinformationsepaseparatepayments
# operationId: PostPaymentInformationSepaSeparatePaymentByEmploymentId
export def "providers-employers-employees-employments-paymentinformationsepaseparatepayments PostPaymentInformationSepaSeparatePaymentByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/paymentinformationsepaseparatepayments")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "applicationjson;version=2018-01-01" $body
}

# Overview of the data used in a payroll simulation
#
# GET /providers/employers/employees/employments/{employmentId}/payrollSimulatorData
# operationId: GetPayrollSimulatorDataByEmploymentId
export def "providers-employers-employees-employments-payroll-simulator-data GetPayrollSimulatorDataByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payrollSimulatorData" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# list of payroll period data
#
# GET /providers/employers/employees/employments/{employmentId}/payrollperioddata
# operationId: GetPayrollPeriodDataByEmploymentId
export def "providers-employers-employees-employments-payrollperioddata GetPayrollPeriodDataByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payrollperioddata" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Insert or update payroll period data
#
# PATCH /providers/employers/employees/employments/{employmentId}/payrollperioddata
# operationId: PatchPayrollPeriodDataByEmploymentId
export def "providers-employers-employees-employments-payrollperioddata PatchPayrollPeriodDataByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payrollperioddata" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Create an payroll period data record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/payrollperioddata
# operationId: PostPayrollPeriodDataByEmploymentId
export def "providers-employers-employees-employments-payrollperioddata PostPayrollPeriodDataByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payrollperioddata")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get payroll period results of an employment for a year
#
# GET /providers/employers/employees/employments/{employmentId}/payrollperiodresults/year/{year}
# operationId: GetPayrollPeriodResultsByEmploymentIdAndYear
export def "providers-employers-employees-employments-payrollperiodresults-year GetPayrollPeriodResultsByEmploymentIdAndYear" [
  employmentId: string
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payrollperiodresults/year/($year)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of payslips for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/payslips
# operationId: GetPayslipsByEmploymentId
export def "providers-employers-employees-employments-payslips GetPayslipsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payslips" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download a cummulative payslip for a single payroll period
#
# GET /providers/employers/employees/employments/{employmentId}/payslips/summary/{payrollPeriodId}
# operationId: GetPayrollPeriodSummaryByEmploymentIdAndPayrollPeriodId
export def "providers-employers-employees-employments-payslips-summary GetPayrollPeriodSummaryByEmploymentIdAndPayrollPeriodId" [
  employmentId: string
  payrollPeriodId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payslips/summary/($payrollPeriodId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download payslips of an employment for a payrollrun
#
# GET /providers/employers/employees/employments/{employmentId}/payslips/{payrollrunId}
# operationId: GetPayslipsByEmploymentIdAndPayrollRunId
export def "providers-employers-employees-employments-payslips GetPayslipsByEmploymentIdAndPayrollRunId" [
  employmentId: string
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-2 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payslips/($payrollrunId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download payslips for an employment and year
#
# GET /providers/employers/employees/employments/{employmentId}/payslips/{year}
# operationId: GetPayslipsByEmploymentIdAndYear
export def "providers-employers-employees-employments-payslips GetPayslipsByEmploymentIdAndYear" [
  employmentId: string
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/payslips/($year)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List pension benefit
#
# GET /providers/employers/employees/employments/{employmentId}/pensionbenefits
# operationId: GetPensionBenefitsByEmploymentId
export def "providers-employers-employees-employments-pensionbenefits GetPensionBenefitsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/pensionbenefits" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a pension benefit for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/pensionbenefits
# operationId: PostPensionBenefitByEmploymentId
export def "providers-employers-employees-employments-pensionbenefits PostPensionBenefitByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/pensionbenefits")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of protected earnings records
#
# GET /providers/employers/employees/employments/{employmentId}/protectedearnings
# operationId: GetProtectedEarningsByEmploymentId
export def "providers-employers-employees-employments-protectedearnings GetProtectedEarningsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/protectedearnings" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new protected earnings record
#
# POST /providers/employers/employees/employments/{employmentId}/protectedearnings
# operationId: PostProtectedEarningsByEmploymentId
export def "providers-employers-employees-employments-protectedearnings PostProtectedEarningsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/protectedearnings")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "applicationjson;version=2018-01-01" $body
}

# Undo termination for an employment.
#
# PATCH /providers/employers/employees/employments/{employmentId}/reinstate
# operationId: PatchReinstateByEmploymentId
export def "providers-employers-employees-employments-reinstate PatchReinstateByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/reinstate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of social security benefits records for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/socialsecuritybenefits
# operationId: GetSocialSecurityBenefitByEmploymentId
export def "providers-employers-employees-employments-socialsecuritybenefits GetSocialSecurityBenefitByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/socialsecuritybenefits" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a social security benefits record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/socialsecuritybenefits
# operationId: PostSocialSecurityBenefitByEmploymentId
export def "providers-employers-employees-employments-socialsecuritybenefits PostSocialSecurityBenefitByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/socialsecuritybenefits")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of social security records for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/socialsecurityconfigurations
# operationId: GetSocialSecurityConfigurationByEmploymentId
export def "providers-employers-employees-employments-socialsecurityconfigurations GetSocialSecurityConfigurationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/socialsecurityconfigurations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a social security record for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/socialsecurityconfigurations
# operationId: PostSocialSecurityConfigurationByEmploymentId
export def "providers-employers-employees-employments-socialsecurityconfigurations PostSocialSecurityConfigurationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/socialsecurityconfigurations")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Terminate an employment.
#
# PATCH /providers/employers/employees/employments/{employmentId}/terminate
# operationId: PatchTerminateByEmploymentId
export def "providers-employers-employees-employments-terminate PatchTerminateByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/terminate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Create transition compensation employment for existing employment
#
# POST /providers/employers/employees/employments/{employmentId}/transitioncompensation
# operationId: PostTransitionCompensationEmploymentByEmploymentId
export def "providers-employers-employees-employments-transitioncompensation PostTransitionCompensationEmploymentByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/transitioncompensation")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Calculate transition compensation
#
# POST /providers/employers/employees/employments/{employmentId}/transitioncompensation/calculate
# operationId: PostCalculateTransitionCompensationByEmploymentId
export def "providers-employers-employees-employments-transitioncompensation-calculate PostCalculateTransitionCompensationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/transitioncompensation/calculate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get default input parameters transition compensation
#
# GET /providers/employers/employees/employments/{employmentId}/transitioncompensation/calculate/defaults
# operationId: GetCalculateTransitionCompensationDefaultsByEmploymentId
export def "providers-employers-employees-employments-transitioncompensation-calculate-defaults GetCalculateTransitionCompensationDefaultsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/transitioncompensation/calculate/defaults")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List wachtgeld
#
# GET /providers/employers/employees/employments/{employmentId}/wachtgeld
# operationId: GetWachtgeldByEmploymentId
export def "providers-employers-employees-employments-wachtgeld GetWachtgeldByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/wachtgeld" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a wachtgeld for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/wachtgeld
# operationId: PostWachtgeldByEmploymentId
export def "providers-employers-employees-employments-wachtgeld PostWachtgeldByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/wachtgeld")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Perform a wage projection
#
# POST /providers/employers/employees/employments/{employmentId}/wageprojection
# operationId: PostWageProjectionByEmploymentId
export def "providers-employers-employees-employments-wageprojection PostWageProjectionByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/wageprojection")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Perform a wage projection based on a default CLA configuration
#
# POST /providers/employers/employees/employments/{employmentId}/wageprojection/basedOnDefaultClaConfiguration
# operationId: PostWageprojectionBasedOnDefaultClaConfigurationByEmploymentId
export def "providers-employers-employees-employments-wageprojection-based-on-default-cla-configuration PostWageprojectionBasedOnDefaultClaConfigurationByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/wageprojection/basedOnDefaultClaConfiguration")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of wages for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/wages
# operationId: GetWagesByEmploymentId
export def "providers-employers-employees-employments-wages GetWagesByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/wages" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an wage for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/wages
# operationId: PostWageByEmploymentId
export def "providers-employers-employees-employments-wages PostWageByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/wages")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Download wage sheets for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/wagesheet/{year}
# operationId: GetWageSheetByEmploymentIdAndYear
export def "providers-employers-employees-employments-wagesheet GetWageSheetByEmploymentIdAndYear" [
  employmentId: string
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/wagesheet/($year)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of working hours for an employment
#
# GET /providers/employers/employees/employments/{employmentId}/workinghours
# operationId: GetWorkingHoursByEmploymentId
export def "providers-employers-employees-employments-workinghours GetWorkingHoursByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/workinghours" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create the working hours for an employment
#
# POST /providers/employers/employees/employments/{employmentId}/workinghours
# operationId: PostWorkingHoursByEmploymentId
export def "providers-employers-employees-employments-workinghours PostWorkingHoursByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/workinghours")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Year-end statements for the employment
#
# GET /providers/employers/employees/employments/{employmentId}/yearendstatements
# operationId: GetYearEndStatementsByEmploymentId
export def "providers-employers-employees-employments-yearendstatements GetYearEndStatementsByEmploymentId" [
  employmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/yearendstatements" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download year-end statement of an employment for a year
#
# GET /providers/employers/employees/employments/{employmentId}/yearendstatements/{year}
# operationId: GetYearEndStatementForYearByEmploymentId
export def "providers-employers-employees-employments-yearendstatements GetYearEndStatementForYearByEmploymentId" [
  employmentId: string
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/employments/($employmentId)/yearendstatements/($year)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an employee note record
#
# DELETE /providers/employers/employees/notes/{noteId}
# operationId: DeleteEmployeeNoteByNoteId
export def "providers-employers-employees-notes DeleteEmployeeNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/notes/($noteId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an employee note
#
# GET /providers/employers/employees/notes/{noteId}
# operationId: GetEmployeeNoteByNoteId
export def "providers-employers-employees-notes GetEmployeeNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/notes/($noteId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an employee note record
#
# PUT /providers/employers/employees/notes/{noteId}
# operationId: PutEmployeeNoteByNoteId
export def "providers-employers-employees-notes PutEmployeeNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/notes/($noteId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific partner record
#
# DELETE /providers/employers/employees/partners/{partnerId}
# operationId: DeletePartnerByPartnerId
export def "providers-employers-employees-partners DeletePartnerByPartnerId" [
  partnerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/partners/($partnerId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a single partner
#
# GET /providers/employers/employees/partners/{partnerId}
# operationId: GetPartnerByPartnerId
export def "providers-employers-employees-partners GetPartnerByPartnerId" [
  partnerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/partners/($partnerId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details for a partner
#
# PUT /providers/employers/employees/partners/{partnerId}
# operationId: PutPartnerByPartnerId
export def "providers-employers-employees-partners PutPartnerByPartnerId" [
  partnerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/partners/($partnerId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Change the employee's self service (ESS) access
#
# PATCH /providers/employers/employees/selfservice
# operationId: PatchEmployeeSelfServiceAccessByEmployeeId
export def "providers-employers-employees-selfservice PatchEmployeeSelfServiceAccessByEmployeeId" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/employees/selfservice")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of an employee
#
# GET /providers/employers/employees/{employeeId}
# operationId: GetEmployeeByEmployeeId
export def "providers-employers-employees GetEmployeeByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-4 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-12-31")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an employee
#
# PUT /providers/employers/employees/{employeeId}
# operationId: PutEmployeeByEmployeeId
export def "providers-employers-employees PutEmployeeByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-4 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-12-31")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-12-31" $body
}

# List of absences for an employee
#
# GET /providers/employers/employees/{employeeId}/absences
# operationId: GetAbsencesByEmployeeId
export def "providers-employers-employees-absences GetAbsencesByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/absences" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an Absence for an employee
#
# POST /providers/employers/employees/{employeeId}/absences
# operationId: PostAbsenceByEmployeeId
export def "providers-employers-employees-absences PostAbsenceByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/absences")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2020-03-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2020-03-16" $body
}

# Download the absence overview for an employee
#
# GET /providers/employers/employees/{employeeId}/absences/overview
# operationId: GetAbsenceOverviewByEmployeeId
export def "providers-employers-employees-absences-overview GetAbsenceOverviewByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --startDate: string # The start date (e.g. 2020-08-01)
  --endDate: string # The end date (e.g. 2020-08-31)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/absences/overview" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get Benify URL for an employee
#
# GET /providers/employers/employees/{employeeId}/benifyurl
# operationId: GetBenifyUrlByEmployeeId
export def "providers-employers-employees-benifyurl GetBenifyUrlByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/benifyurl")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Start the change address workflow for an employee
#
# POST /providers/employers/employees/{employeeId}/changeaddressrequest
# operationId: PostChangeAddressRequestByEmployeeId
export def "providers-employers-employees-changeaddressrequest PostChangeAddressRequestByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/changeaddressrequest")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Start the change contactinformation workflow for an employee
#
# POST /providers/employers/employees/{employeeId}/changecontactinformationrequest
# operationId: PostChangeContactInformationRequestByEmployeeId
export def "providers-employers-employees-changecontactinformationrequest PostChangeContactInformationRequestByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/changecontactinformationrequest")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of children for an employee
#
# GET /providers/employers/employees/{employeeId}/children
# operationId: GetChildrenByEmployeeId
export def "providers-employers-employees-children GetChildrenByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/children" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an child for an employee
#
# POST /providers/employers/employees/{employeeId}/children
# operationId: PostChildByEmployeeId
export def "providers-employers-employees-children PostChildByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/children")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Citizen service number of an employee
#
# GET /providers/employers/employees/{employeeId}/citizenservicenumber
# operationId: GetCitizenServiceNumberByEmployeeId
export def "providers-employers-employees-citizenservicenumber GetCitizenServiceNumberByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/citizenservicenumber")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update the citizen service number of an employee
#
# PUT /providers/employers/employees/{employeeId}/citizenservicenumber
# operationId: PutCitizenServiceNumberByEmployeeId
export def "providers-employers-employees-citizenservicenumber PutCitizenServiceNumberByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/citizenservicenumber")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of contacts for an employee
#
# GET /providers/employers/employees/{employeeId}/contacts
# operationId: GetContactsByEmployeeId
export def "providers-employers-employees-contacts GetContactsByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/contacts" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a contact for an employee
#
# POST /providers/employers/employees/{employeeId}/contacts
# operationId: PostContactByEmployeeId
export def "providers-employers-employees-contacts PostContactByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/contacts")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List employee custom fields for an employee
#
# GET /providers/employers/employees/{employeeId}/customfields
# operationId: GetEmployeeCustomFieldssByEmployeeId
export def "providers-employers-employees-customfields GetEmployeeCustomFieldssByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/customfields" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an employee custom field for an employee
#
# POST /providers/employers/employees/{employeeId}/customfields
# operationId: PostEmployeeCustomFieldByEmployeeId
export def "providers-employers-employees-customfields PostEmployeeCustomFieldByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/customfields")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of educations for an employee
#
# GET /providers/employers/employees/{employeeId}/educations
# operationId: GetEducationsByEmployeeId
export def "providers-employers-employees-educations GetEducationsByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/educations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a education for an employee
#
# POST /providers/employers/employees/{employeeId}/educations
# operationId: PostEducationByEmployeeId
export def "providers-employers-employees-educations PostEducationByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/educations")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of employments for an employee
#
# GET /providers/employers/employees/{employeeId}/employments
# operationId: GetEmploymentsByEmployeeId
export def "providers-employers-employees-employments GetEmploymentsByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/employments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2019-12-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create employment for existing employee
#
# POST /providers/employers/employees/{employeeId}/employments
# operationId: PostEmploymentByEmployeeId
export def "providers-employers-employees-employments PostEmploymentByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/employments")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of notes for an employee.
#
# GET /providers/employers/employees/{employeeId}/notes
# operationId: GetEmployeeNotesByEmployeeId
export def "providers-employers-employees-notes GetEmployeeNotesByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/notes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a note for an employee
#
# POST /providers/employers/employees/{employeeId}/notes
# operationId: PostEmployeeNoteByEmployeeId
export def "providers-employers-employees-notes PostEmployeeNoteByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/notes")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of occupational disabilities for an employee
#
# GET /providers/employers/employees/{employeeId}/occupationaldisabilities
# operationId: GetOccupationalDisabilitiesByEmployeeId
export def "providers-employers-employees-occupationaldisabilities GetOccupationalDisabilitiesByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/occupationaldisabilities" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of partners for an employee
#
# GET /providers/employers/employees/{employeeId}/partners
# operationId: GetPartnersByEmployeeId
export def "providers-employers-employees-partners GetPartnersByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/partners" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create the partner for an employee
#
# POST /providers/employers/employees/{employeeId}/partners
# operationId: PostPartnerByEmployeeId
export def "providers-employers-employees-partners PostPartnerByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/partners")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete employee photo
#
# DELETE /providers/employers/employees/{employeeId}/photo
# operationId: DeleteEmployeePhotoByEmployeeId
export def "providers-employers-employees-photo DeleteEmployeePhotoByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/photo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Photo of an employee
#
# GET /providers/employers/employees/{employeeId}/photo
# operationId: GetEmployeePhotoByEmployeeId
export def "providers-employers-employees-photo GetEmployeePhotoByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/photo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Post employee photo
#
# POST /providers/employers/employees/{employeeId}/photo
# operationId: PostEmployeePhotoByEmployeeId
export def "providers-employers-employees-photo PostEmployeePhotoByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/photo")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Photo of an employee
#
# GET /providers/employers/employees/{employeeId}/photo/{version}
# operationId: GetEmployeePhotoByEmployeeIdAndVersion
export def "providers-employers-employees-photo GetEmployeePhotoByEmployeeIdAndVersion" [
  employeeId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/photo/($version)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set the date on which to revoke ESS access
#
# PATCH /providers/employers/employees/{employeeId}/revokeEmployeeSelfServiceAccess
# operationId: PatchRevokeEmployeeSelfServiceAccessByEmployeeId
export def "providers-employers-employees-revoke-employee-self-service-access PatchRevokeEmployeeSelfServiceAccessByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/revokeEmployeeSelfServiceAccess")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of and employees self service portal status
#
# GET /providers/employers/employees/{employeeId}/selfservice
# operationId: GetEmployeeSelfServiceByEmployeeId
export def "providers-employers-employees-selfservice GetEmployeeSelfServiceByEmployeeId" [
  employeeId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/employees/($employeeId)/selfservice" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a function
#
# GET /providers/employers/functions/{functionId}
# operationId: GetFunctionByFunctionId
export def "providers-employers-functions GetFunctionByFunctionId" [
  functionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/functions/($functionId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a function
#
# PUT /providers/employers/functions/{functionId}
# operationId: PutFunctionByFunctionId
export def "providers-employers-functions PutFunctionByFunctionId" [
  functionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/functions/($functionId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of employer last modified version numbers
#
# GET /providers/employers/lastmodifiedversionnumbers
# operationId: GetEmployerLastModifiedVersionNumbersByUser
export def "providers-employers-lastmodifiedversionnumbers GetEmployerLastModifiedVersionNumbersByUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/providers/employers/lastmodifiedversionnumbers" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an age based leave record
#
# DELETE /providers/employers/leavePolicies/agebasedleave/{ageBasedLeaveId}
# operationId: DeleteAgeBasedLeaveByAgeBasedLeaveId
export def "providers-employers-leave-policies-agebasedleave DeleteAgeBasedLeaveByAgeBasedLeaveId" [
  ageBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/agebasedleave/($ageBasedLeaveId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# An age based leave record
#
# GET /providers/employers/leavePolicies/agebasedleave/{ageBasedLeaveId}
# operationId: GetAgeBasedLeaveByAgeBasedLeaveId
export def "providers-employers-leave-policies-agebasedleave GetAgeBasedLeaveByAgeBasedLeaveId" [
  ageBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavePolicies/agebasedleave/($ageBasedLeaveId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an age based leave
#
# PUT /providers/employers/leavePolicies/agebasedleave/{ageBasedLeaveId}
# operationId: PutAgeBasedLeaveByAgeBasedLeaveId
export def "providers-employers-leave-policies-agebasedleave PutAgeBasedLeaveByAgeBasedLeaveId" [
  ageBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/agebasedleave/($ageBasedLeaveId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a wage based leave record
#
# DELETE /providers/employers/leavePolicies/wagebasedleave/{wageBasedLeaveId}
# operationId: DeleteWageBasedLeaveByWageBasedLeaveId
export def "providers-employers-leave-policies-wagebasedleave DeleteWageBasedLeaveByWageBasedLeaveId" [
  wageBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/wagebasedleave/($wageBasedLeaveId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# A wage based leave record
#
# GET /providers/employers/leavePolicies/wagebasedleave/{wageBasedLeaveId}
# operationId: GetWageBasedLeaveByWageBasedLeaveId
export def "providers-employers-leave-policies-wagebasedleave GetWageBasedLeaveByWageBasedLeaveId" [
  wageBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavePolicies/wagebasedleave/($wageBasedLeaveId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a wage based leave
#
# PUT /providers/employers/leavePolicies/wagebasedleave/{wageBasedLeaveId}
# operationId: PutWageBasedLeaveByWageBasedLeaveId
export def "providers-employers-leave-policies-wagebasedleave PutWageBasedLeaveByWageBasedLeaveId" [
  wageBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/wagebasedleave/($wageBasedLeaveId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete an years of service based leave record
#
# DELETE /providers/employers/leavePolicies/yearsofservicebasedleave/{yearsOfServiceBasedLeaveId}
# operationId: DeleteYearsOfServiceBasedLeaveByYearsOfServiceBasedLeaveId
export def "providers-employers-leave-policies-yearsofservicebasedleave DeleteYearsOfServiceBasedLeaveByYearsOfServiceBasedLeaveId" [
  yearsOfServiceBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/yearsofservicebasedleave/($yearsOfServiceBasedLeaveId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# A years of service based leave record
#
# GET /providers/employers/leavePolicies/yearsofservicebasedleave/{yearsOfServiceBasedLeaveId}
# operationId: GetYearsOfServiceBasedLeaveByYearsOfServiceBasedLeaveId
export def "providers-employers-leave-policies-yearsofservicebasedleave GetYearsOfServiceBasedLeaveByYearsOfServiceBasedLeaveId" [
  yearsOfServiceBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavePolicies/yearsofservicebasedleave/($yearsOfServiceBasedLeaveId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a years of service based leave
#
# PUT /providers/employers/leavePolicies/yearsofservicebasedleave/{yearsOfServiceBasedLeaveId}
# operationId: PutYearsOFServiceBasedLeaveByYearsOfServiceBasedLeaveId
export def "providers-employers-leave-policies-yearsofservicebasedleave PutYearsOFServiceBasedLeaveByYearsOfServiceBasedLeaveId" [
  yearsOfServiceBasedLeaveId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/yearsofservicebasedleave/($yearsOfServiceBasedLeaveId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a leave policy
#
# DELETE /providers/employers/leavePolicies/{leavePolicyId}
# operationId: DeleteLeavePolicyByLeavePolicyId
export def "providers-employers-leave-policies DeleteLeavePolicyByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# A single leave policy
#
# GET /providers/employers/leavePolicies/{leavePolicyId}
# operationId: GetLeavePolicyByLeavePolicyId
export def "providers-employers-leave-policies GetLeavePolicyByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a leave policy
#
# PUT /providers/employers/leavePolicies/{leavePolicyId}
# operationId: PutLeavePolicyByLeavePolicyId
export def "providers-employers-leave-policies PutLeavePolicyByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Age based leave
#
# GET /providers/employers/leavePolicies/{leavePolicyId}/agebasedleave
# operationId: GetAgeBasedLeaveByLeavePolicyId
export def "providers-employers-leave-policies-agebasedleave GetAgeBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)/agebasedleave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an age based leave record
#
# POST /providers/employers/leavePolicies/{leavePolicyId}/agebasedleave
# operationId: PostAgeBasedLeaveByLeavePolicyId
export def "providers-employers-leave-policies-agebasedleave PostAgeBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)/agebasedleave")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Wage based leave
#
# GET /providers/employers/leavePolicies/{leavePolicyId}/wagebasedleave
# operationId: GetWageBasedLeaveByLeavePolicyId
export def "providers-employers-leave-policies-wagebasedleave GetWageBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)/wagebasedleave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a wage based leave record
#
# POST /providers/employers/leavePolicies/{leavePolicyId}/wagebasedleave
# operationId: PostWageBasedLeaveByLeavePolicyId
export def "providers-employers-leave-policies-wagebasedleave PostWageBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)/wagebasedleave")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# years of service based leave
#
# GET /providers/employers/leavePolicies/{leavePolicyId}/yearsofservicebasedleave
# operationId: GetYearsOfServiceBasedLeaveByLeavePolicyId
export def "providers-employers-leave-policies-yearsofservicebasedleave GetYearsOfServiceBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)/yearsofservicebasedleave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a years of service based leave record
#
# POST /providers/employers/leavePolicies/{leavePolicyId}/yearsofservicebasedleave
# operationId: PostYearsOfServiceBasedLeaveByLeavePolicyId
export def "providers-employers-leave-policies-yearsofservicebasedleave PostYearsOfServiceBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavePolicies/($leavePolicyId)/yearsofservicebasedleave")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get list of linked employments for the leave policy
#
# GET /providers/employers/leavepolicies/{leavePolicyId}/employments
# operationId: GetEmploymentsByLeavePolicyId
export def "providers-employers-leavepolicies-employments GetEmploymentsByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/leavepolicies/($leavePolicyId)/employments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create or delete multiple linked employments for the leave policy
#
# PATCH /providers/employers/leavepolicies/{leavePolicyId}/employments
# operationId: PatchEmploymentsByLeavePolicyId
export def "providers-employers-leavepolicies-employments PatchEmploymentsByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/leavepolicies/($leavePolicyId)/employments")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# List of employers with fewer fields for performance reasons
#
# GET /providers/employers/minimized
# operationId: GetEmployersMinimizedByUser
export def "providers-employers-minimized GetEmployersMinimizedByUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/providers/employers/minimized" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an employer note record
#
# DELETE /providers/employers/notes/{noteId}
# operationId: DeleteEmployerNoteByNoteId
export def "providers-employers-notes DeleteEmployerNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/notes/($noteId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an employer note
#
# GET /providers/employers/notes/{noteId}
# operationId: GetEmployerNoteByNoteId
export def "providers-employers-notes GetEmployerNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/notes/($noteId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an employer note record
#
# PUT /providers/employers/notes/{noteId}
# operationId: PutEmployerNoteByNoteId
export def "providers-employers-notes PutEmployerNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/notes/($noteId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Mark one or more notifications as read.
#
# PATCH /providers/employers/notifications
# operationId: PatchNotifications
export def "providers-employers-notifications PatchNotifications" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/notifications")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# MDV notifications
#
# PATCH /providers/employers/payrollAdministrations/{payrollAdministrationId}/mdvNotifications
# operationId: PatchMdvNotificationsByPayrollAdministrationId
export def "providers-employers-payroll-administrations-mdv-notifications PatchMdvNotificationsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrollAdministrations/($payrollAdministrationId)/mdvNotifications")
  let extra_headers = {"Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Detail APG pension declaration for an administration
#
# GET /providers/employers/payrolladministrations/apgpensiondeclarations/{apgPensionDeclarationId}
# operationId: GetApgPensionDeclarationByApgPensionDeclarationId
export def "providers-employers-payrolladministrations-apgpensiondeclarations GetApgPensionDeclarationByApgPensionDeclarationId" [
  apgPensionDeclarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/apgpensiondeclarations/($apgPensionDeclarationId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the status of a apg pension declaration
#
# PATCH /providers/employers/payrolladministrations/apgpensiondeclarations/{apgPensionDeclarationId}
# operationId: PatchApgPensionDeclarationByApgPensionDeclarationId
export def "providers-employers-payrolladministrations-apgpensiondeclarations PatchApgPensionDeclarationByApgPensionDeclarationId" [
  apgPensionDeclarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  action: string@action-completer # The action to perform on the apg pensiondeclaration
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/apgpensiondeclarations/($apgPensionDeclarationId)")
  let body = {action: $action} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Download the message of a of an APG pension declaration
#
# GET /providers/employers/payrolladministrations/apgpensiondeclarations/{apgPensionDeclarationId}/message
# operationId: GetMessageByApgPensionDeclarationId
export def "providers-employers-payrolladministrations-apgpensiondeclarations-message GetMessageByApgPensionDeclarationId" [
  apgPensionDeclarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/apgpensiondeclarations/($apgPensionDeclarationId)/message")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/xml"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the overview report of an APG pension declaration
#
# GET /providers/employers/payrolladministrations/apgpensiondeclarations/{apgPensionDeclarationId}/overview
# operationId: GetOverviewByApgPensionDeclarationId
export def "providers-employers-payrolladministrations-apgpensiondeclarations-overview GetOverviewByApgPensionDeclarationId" [
  apgPensionDeclarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/apgpensiondeclarations/($apgPensionDeclarationId)/overview")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a journal run
#
# GET /providers/employers/payrolladministrations/journalruns/{journalrunId}
# operationId: GetJournalRunByJournalRunId
export def "providers-employers-payrolladministrations-journalruns GetJournalRunByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the status of a journal run
#
# PATCH /providers/employers/payrolladministrations/journalruns/{journalrunId}
# operationId: PatchJournalRunByJournalRunId
export def "providers-employers-payrolladministrations-journalruns PatchJournalRunByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Download the journal run
#
# POST /providers/employers/payrolladministrations/journalruns/{journalrunId}/download
# operationId: PostDownloadJournalRunExportByJournalRunId
export def "providers-employers-payrolladministrations-journalruns-download PostDownloadJournalRunExportByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)/download")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Errors of a journal run
#
# GET /providers/employers/payrolladministrations/journalruns/{journalrunId}/errors
# operationId: GetJournalRunErrorsByJournalRunId
export def "providers-employers-payrolladministrations-journalruns-errors GetJournalRunErrorsByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)/errors")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List export attempts for a journal run
#
# GET /providers/employers/payrolladministrations/journalruns/{journalrunId}/exportAuditTrail
# operationId: GetJournalRunExportAuditTrailByJournalRunId
export def "providers-employers-payrolladministrations-journalruns-export-audit-trail GetJournalRunExportAuditTrailByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)/exportAuditTrail" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get journal results for a journal run
#
# GET /providers/employers/payrolladministrations/journalruns/{journalrunId}/results
# operationId: GetJournalRunResultsByJournalRunId
export def "providers-employers-payrolladministrations-journalruns-results GetJournalRunResultsByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)/results")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get journal run overview for a journal run
#
# GET /providers/employers/payrolladministrations/journalruns/{journalrunId}/runoverview
# operationId: GetRunOverviewByJournalRunId
export def "providers-employers-payrolladministrations-journalruns-runoverview GetRunOverviewByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reportJournalNumber: int # Dagboeknummer. (format: int32, e.g. 123)
  --reportVoucherNumber: int # Boekstuknummer. (format: int32, e.g. 456)
  --reportDescription: string # Omschrijving to be shown on the overview. (e.g. 123)
  --reportYear: int # A valid year to be shown on the report. (format: int32, e.g. 2018)
  --reportPeriodNumber: int # A valid period number to be shown on the report. (format: int32, e.g. 01)
  --reportDate: string # A valid booking date to be shown on the report. (format: date, e.g. 2018-01-01)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportJournalNumber" $reportJournalNumber "scalar") (serialize-qp "reportVoucherNumber" $reportVoucherNumber "scalar") (serialize-qp "reportDescription" $reportDescription "scalar") (serialize-qp "reportYear" $reportYear "scalar") (serialize-qp "reportPeriodNumber" $reportPeriodNumber "scalar") (serialize-qp "reportDate" $reportDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)/runoverview" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "text/csv"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get journal run overview per employment for a journal run
#
# GET /providers/employers/payrolladministrations/journalruns/{journalrunId}/runoverviewperemployment
# operationId: GetRunOverviewPerEmploymentByJournalRunId
export def "providers-employers-payrolladministrations-journalruns-runoverviewperemployment GetRunOverviewPerEmploymentByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reportJournalNumber: int # Dagboeknummer. (format: int32, e.g. 123)
  --reportVoucherNumber: int # Boekstuknummer. (format: int32, e.g. 456)
  --reportDescription: string # Omschrijving to be shown on the overview. (e.g. 123)
  --reportYear: int # A valid year to be shown on the report. (format: int32, e.g. 2018)
  --reportPeriodNumber: int # A valid period number to be shown on the report. (format: int32, e.g. 01)
  --reportDate: string # A valid booking date to be shown on the report. (format: date, e.g. 2018-01-01)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportJournalNumber" $reportJournalNumber "scalar") (serialize-qp "reportVoucherNumber" $reportVoucherNumber "scalar") (serialize-qp "reportDescription" $reportDescription "scalar") (serialize-qp "reportYear" $reportYear "scalar") (serialize-qp "reportPeriodNumber" $reportPeriodNumber "scalar") (serialize-qp "reportDate" $reportDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)/runoverviewperemployment" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "text/csv"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Send the journal run
#
# POST /providers/employers/payrolladministrations/journalruns/{journalrunId}/send
# operationId: PostSendJournalRunExportByJournalRunId
export def "providers-employers-payrolladministrations-journalruns-send PostSendJournalRunExportByJournalRunId" [
  journalrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/journalruns/($journalrunId)/send")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Change the status of payrollruns
#
# PATCH /providers/employers/payrolladministrations/payrollruns
# operationId: PatchPayrollrunsByPayrollRunIds
export def "providers-employers-payrolladministrations-payrollruns PatchPayrollrunsByPayrollRunIds" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/payrolladministrations/payrollruns")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a payroll run note record
#
# DELETE /providers/employers/payrolladministrations/payrollruns/notes/{noteId}
# operationId: DeletePayrollRunNoteByNoteId
export def "providers-employers-payrolladministrations-payrollruns-notes DeletePayrollRunNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/notes/($noteId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a payroll run note
#
# GET /providers/employers/payrolladministrations/payrollruns/notes/{noteId}
# operationId: GetPayrollRunNoteByNoteId
export def "providers-employers-payrolladministrations-payrollruns-notes GetPayrollRunNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/notes/($noteId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit a payroll run note record
#
# PUT /providers/employers/payrolladministrations/payrollruns/notes/{noteId}
# operationId: PutPayrollRunNoteByNoteId
export def "providers-employers-payrolladministrations-payrollruns-notes PutPayrollRunNoteByNoteId" [
  noteId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/notes/($noteId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Download a SEPA file
#
# GET /providers/employers/payrolladministrations/payrollruns/sepafiles/{sepafileId}
# operationId: GetSepaFileBySepaFileId
export def "providers-employers-payrolladministrations-payrollruns-sepafiles GetSepaFileBySepaFileId" [
  sepafileId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --paymentType: string@paymentType-completer # Indicates the type of payment for the SEPA file. In case salary is selected the following will be included in the SEPA xml file ```   <CtgyPurp>     <Cd>SALA</Cd>   </CtgyPurp> ``` (default: Salary)
  --batchBooking: string@batchBooking-completer # Determines whether the Batch Booking \<BtchBookg\> tag is included in the SEPA file and if it is which value it is set to.  By defualt this value is set to Exclude.  Possible values for this parameter are: - Exclude: Do not include \<BtchBookg\> tag in SEPA file - IncludeTrue: Include \<BtchBookg\> tag in SEPA file and set it to 'True' - IncludeFalse: Include \<BtchBookg\> tag in SEPA file and set it to 'False'  (default: BtchBookgExclude)
  --requestedExecutionDate: string # This value is used when creating the sepa file to set the value of \<ReqdExctnDt\> (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "paymentType" $paymentType "scalar") (serialize-qp "batchBooking" $batchBooking "scalar") (serialize-qp "requestedExecutionDate" $requestedExecutionDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/sepafiles/($sepafileId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/xml"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a payrollrun
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}
# operationId: GetPayrollRunByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns GetPayrollRunByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get payroll run balance sheet
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/BalanceSheet
# operationId: GetPayrollRunBalanceSheetByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-balance-sheet GetPayrollRunBalanceSheetByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-5 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/BalanceSheet")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the default set of downloads for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/defaultset
# operationId: GetDefaultSetByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-defaultset GetDefaultSetByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/defaultset")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download errors and warnings for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/errorsandwarnings
# operationId: GetErrorsAndWarningsByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-errorsandwarnings GetErrorsAndWarningsByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/errorsandwarnings")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the journal entries by distribution Unit.
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/journalentriesperdistributionunitoverviews
# operationId: GetJournalEntriesPerDistributionUnitOverviewsByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-journalentriesperdistributionunitoverviews GetJournalEntriesPerDistributionUnitOverviewsByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/journalentriesperdistributionunitoverviews")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of notes for a payroll run.
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/notes
# operationId: GetPayrollRunNotesByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-notes GetPayrollRunNotesByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/notes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a note for a payroll run
#
# POST /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/notes
# operationId: PostPayrollRunNoteByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-notes PostPayrollRunNoteByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/notes")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Download payment overviews for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/paymentoverviews
# operationId: GetPaymentOverviewsByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-paymentoverviews GetPaymentOverviewsByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/paymentoverviews")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download payroll control register for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/payrollcontrolregister
# operationId: GetPayrollControlRegisterByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-payrollcontrolregister GetPayrollControlRegisterByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/payrollcontrolregister")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download payslips for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/payslips
# operationId: GetPayslipsByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-payslips GetPayslipsByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/payslips")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get results for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/results
# operationId: GetPayrollRunResultsByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-results GetPayrollRunResultsByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/results")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download run overviews for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/runoverviews
# operationId: GetRunOverviewsByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-runoverviews GetRunOverviewsByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/runoverviews")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of SEPA files for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/sepafiles
# operationId: GetSepaFilesByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-sepafiles GetSepaFilesByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/sepafiles")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download a total SEPA file
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/totalsepafile
# operationId: GetTotalSepaFileByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-totalsepafile GetTotalSepaFileByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --paymentType: string@paymentType-completer # Indicates the type of payment for the SEPA file. In case salary is selected the following will be included in the SEPA xml file ```   <CtgyPurp>     <Cd>SALA</Cd>   </CtgyPurp> ``` (default: Salary)
  --batchBooking: string@batchBooking-completer # Determines whether the Batch Booking \<BtchBookg\> tag is included in the SEPA file and if it is which value it is set to.  By defualt this value is set to Exclude.  Possible values for this parameter are: - Exclude: Do not include \<BtchBookg\> tag in SEPA file - IncludeTrue: Include \<BtchBookg\> tag in SEPA file and set it to 'True' - IncludeFalse: Include \<BtchBookg\> tag in SEPA file and set it to 'False'  (default: BtchBookgExclude)
  --requestedExecutionDate: string # This value is used when creating the sepa file to set the value of \<ReqdExctnDt\> (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "paymentType" $paymentType "scalar") (serialize-qp "batchBooking" $batchBooking "scalar") (serialize-qp "requestedExecutionDate" $requestedExecutionDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/totalsepafile" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/xml"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Undo payroll run (verwijderen loonrun)
#
# POST /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/undo
# operationId: UndoPayrollRunByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-undo UndoPayrollRunByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/undo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download wage sheets for a payroll run
#
# GET /providers/employers/payrolladministrations/payrollruns/{payrollrunId}/wagesheets
# operationId: GetWageSheetsByPayrollRunId
export def "providers-employers-payrolladministrations-payrollruns-wagesheets GetWageSheetsByPayrollRunId" [
  payrollrunId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-6 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrollruns/($payrollrunId)/wagesheets")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/pdf")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the status of payroll tax returns
#
# PATCH /providers/employers/payrolladministrations/payrolltaxreturns
# operationId: PatchPayrollTaxReturnsByPayrollTaxReturnIds
export def "providers-employers-payrolladministrations-payrolltaxreturns PatchPayrollTaxReturnsByPayrollTaxReturnIds" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/payrolladministrations/payrolltaxreturns")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Change the status of payroll tax returns
#
# PATCH /providers/employers/payrolladministrations/payrolltaxreturns/{messageReference}
# operationId: PatchPayrollTaxReturnByMessageReference
export def "providers-employers-payrolladministrations-payrolltaxreturns PatchPayrollTaxReturnByMessageReference" [
  messageReference: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  action: string@action-completer # The action to perform on the payroll tax return
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($messageReference)")
  let body = {action: $action} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Send the response message for the payroll tax return
#
# POST /providers/employers/payrolladministrations/payrolltaxreturns/{messageReference}/sendresponsemessagebymessagereference
# operationId: PostSendResponseMessageByMessageReference
export def "providers-employers-payrolladministrations-payrolltaxreturns-sendresponsemessagebymessagereference PostSendResponseMessageByMessageReference" [
  messageReference: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --responseMessageFile: string # base64 encoded xml file of the response message (e.g. )
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($messageReference)/sendresponsemessagebymessagereference")
  let body = {responseMessageFile: $responseMessageFile} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Details of a tax return for an administration
#
# GET /providers/employers/payrolladministrations/payrolltaxreturns/{payrolltaxreturnId}
# operationId: GetPayrollTaxReturnByPayrollTaxReturnId
export def "providers-employers-payrolladministrations-payrolltaxreturns GetPayrollTaxReturnByPayrollTaxReturnId" [
  payrolltaxreturnId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($payrolltaxreturnId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Pay payroll taxes using IDEAL
#
# GET /providers/employers/payrolladministrations/payrolltaxreturns/{payrolltaxreturnId}/ideal
# operationId: GetIdealUrlByPayrollTaxReturnId
export def "providers-employers-payrolladministrations-payrolltaxreturns-ideal GetIdealUrlByPayrollTaxReturnId" [
  payrolltaxreturnId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($payrolltaxreturnId)/ideal")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the status of the IDEAL payment
#
# GET /providers/employers/payrolladministrations/payrolltaxreturns/{payrolltaxreturnId}/idealPaymentStatus
# operationId: GetIdealPaymentStatusByPayrollTaxReturnId
export def "providers-employers-payrolladministrations-payrolltaxreturns-ideal-payment-status GetIdealPaymentStatusByPayrollTaxReturnId" [
  payrolltaxreturnId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($payrolltaxreturnId)/idealPaymentStatus")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the message of a payroll tax return
#
# GET /providers/employers/payrolladministrations/payrolltaxreturns/{payrolltaxreturnId}/message
# operationId: GetMessageByPayrollTaxReturnId
export def "providers-employers-payrolladministrations-payrolltaxreturns-message GetMessageByPayrollTaxReturnId" [
  payrolltaxreturnId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($payrolltaxreturnId)/message")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/xml"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the overview report of a payroll tax return
#
# GET /providers/employers/payrolladministrations/payrolltaxreturns/{payrolltaxreturnId}/overview
# operationId: GetOverviewByPayrollTaxReturnId
export def "providers-employers-payrolladministrations-payrolltaxreturns-overview GetOverviewByPayrollTaxReturnId" [
  payrolltaxreturnId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($payrolltaxreturnId)/overview")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Send the response message for the payroll tax return
#
# POST /providers/employers/payrolladministrations/payrolltaxreturns/{payrolltaxreturnId}/sendresponsemessage
# operationId: PostSendResponseMessageByPayrollTaxReturnId
export def "providers-employers-payrolladministrations-payrolltaxreturns-sendresponsemessage PostSendResponseMessageByPayrollTaxReturnId" [
  payrolltaxreturnId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --responseMessageFile: string # base64 encoded xml file of the response message (e.g. )
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/payrolltaxreturns/($payrolltaxreturnId)/sendresponsemessage")
  let body = {responseMessageFile: $responseMessageFile} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Detail UPA pension declaration
#
# GET /providers/employers/payrolladministrations/upapensiondeclarations/{upaPensionDeclarationId}
# operationId: GetUpaPensionDeclarationByUpaPensionDeclarationId
export def "providers-employers-payrolladministrations-upapensiondeclarations GetUpaPensionDeclarationByUpaPensionDeclarationId" [
  upaPensionDeclarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/upapensiondeclarations/($upaPensionDeclarationId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the status of a upa pension declaration
#
# PATCH /providers/employers/payrolladministrations/upapensiondeclarations/{upaPensionDeclarationId}
# operationId: PatchUpaPensionDeclarationByUpaPensionDeclarationId
export def "providers-employers-payrolladministrations-upapensiondeclarations PatchUpaPensionDeclarationByUpaPensionDeclarationId" [
  upaPensionDeclarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  action: string@action-completer # The action to perform on the upa pensiondeclaration
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/upapensiondeclarations/($upaPensionDeclarationId)")
  let body = {action: $action} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Download the message of a of an UPA pension declaration
#
# GET /providers/employers/payrolladministrations/upapensiondeclarations/{upaPensionDeclarationId}/message
# operationId: GetMessageByUpaPensionDeclarationId
export def "providers-employers-payrolladministrations-upapensiondeclarations-message GetMessageByUpaPensionDeclarationId" [
  upaPensionDeclarationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/upapensiondeclarations/($upaPensionDeclarationId)/message")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/xml"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download accumulated basic journal results report
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/accumulatedbasicjournalresultsreport
# operationId: GetAccumulatedBasicJournalResultsReportByPayrollAdministrationId
export def "providers-employers-payrolladministrations-accumulatedbasicjournalresultsreport GetAccumulatedBasicJournalResultsReportByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reportType: string@reportType-completer # The type of report that is requested. (e.g. nonAggregated)
  --groupingType: string@groupingType-completer # The type of grouping that is requested. This affects the way the data is presented within the given report. (e.g. perPeriod)
  --startPeriod: int # First period (=from) of the requested timebox. Use the format 'YYYYPP' (e.g. 201901)
  --endPeriod: int # Last period (=up to and including) of the requested timebox. Use the following 'YYYYPP' (e.g. 201912)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/pdf)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportType" $reportType "scalar") (serialize-qp "groupingType" $groupingType "scalar") (serialize-qp "startPeriod" $startPeriod "scalar") (serialize-qp "endPeriod" $endPeriod "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/accumulatedbasicjournalresultsreport" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download accumulations and balances reports
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/accumulationsandbalancesreport
# operationId: GetAccumulationsAndBalancesReportByPayrollAdministrationId
export def "providers-employers-payrolladministrations-accumulationsandbalancesreport GetAccumulationsAndBalancesReportByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-7 # Response content type
  --reportType: string@reportType-completer-1 # The type of report that is requested. (e.g. baseForCalculationBalancesAtStartOfYear)
  --reportYear: int # A valid year for which the information is requested. (e.g. 2023)
  --includeEmploymentsThatEndedBeforeStartOfTheYear: oneof<nothing, bool> # If set to true employments with an enddate before the start of the reportYear will be included in the output. (e.g. false)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/pdf)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportType" $reportType "scalar") (serialize-qp "reportYear" $reportYear "scalar") (serialize-qp "includeEmploymentsThatEndedBeforeStartOfTheYear" $includeEmploymentsThatEndedBeforeStartOfTheYear "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/accumulationsandbalancesreport" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/pdf")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download annual payroll tax return report
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/annualpayrolltaxreturnreport
# operationId: GetAnnualPayrollTaxReturnReportByPayrollAdministrationId
export def "providers-employers-payrolladministrations-annualpayrolltaxreturnreport GetAnnualPayrollTaxReturnReportByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reportYear: int # A valid year for which the information is requested. (e.g. 2023)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportYear" $reportYear "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/annualpayrolltaxreturnreport" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download annual wage sheet report
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/annualwagesheetreport
# operationId: GetAnnualWagesheetReportByPayrollAdministrationId
export def "providers-employers-payrolladministrations-annualwagesheetreport GetAnnualWagesheetReportByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-7 # Response content type
  --reportYear: int # A valid year for which the information is requested. (e.g. 2023)
  --startPeriod: int # First period (=from) of the requested timebox. Use the format 'YYYYPP' (e.g. 201901)
  --endPeriod: int # Last period (=up to and including) of the requested timebox. Use the following 'YYYYPP' (e.g. 201912)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/pdf)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportYear" $reportYear "scalar") (serialize-qp "startPeriod" $startPeriod "scalar") (serialize-qp "endPeriod" $endPeriod "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/annualwagesheetreport" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/pdf")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of APG pension declarations for an administration
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/apgpensiondeclarations
# operationId: GetApgPensionDeclarationsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-apgpensiondeclarations GetApgPensionDeclarationsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/apgpensiondeclarations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate a APG pension declaration
#
# POST /providers/employers/payrolladministrations/{payrollAdministrationId}/apgpensiondeclarations/initiate
# operationId: PostInitiateApgPensionDeclarationByPayrollAdministrationId
export def "providers-employers-payrolladministrations-apgpensiondeclarations-initiate PostInitiateApgPensionDeclarationByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/apgpensiondeclarations/initiate")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download attachment of earnings report
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/attachmentsofearningsreport
# operationId: GetAttachmentsOfEarningsReportByPayrollAdministrationId
export def "providers-employers-payrolladministrations-attachmentsofearningsreport GetAttachmentsOfEarningsReportByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reportYear: int # A valid year for which the information is requested. (e.g. 2023)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/pdf)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportYear" $reportYear "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/attachmentsofearningsreport" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "text/csv"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get payroll period data audit trail
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/auditTrail/PayrollPeriodData
export def "providers-employers-payrolladministrations-audit-trail-payroll-period-data get" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --startDate: string # The start date (e.g. 2020-08-01)
  --endDate: string # The end date (e.g. 2020-08-31)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/auditTrail/PayrollPeriodData" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Available payroll component sets
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/availablepayrollcomponentsets
# operationId: GetAvailablePayrollComponentSetsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-availablepayrollcomponentsets GetAvailablePayrollComponentSetsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/availablepayrollcomponentsets")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download WAB deviations report
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/deviatingpremiumswab
# operationId: GetDeviatingPremiumsWabReportByPayrollAdministrationId
export def "providers-employers-payrolladministrations-deviatingpremiumswab GetDeviatingPremiumsWabReportByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reportYear: int # A valid year for which the information is requested. (e.g. 2023)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/pdf)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportYear" $reportYear "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/deviatingpremiumswab" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the audit trail for the employment payroll data
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/employmentPayrollDataAuditTrail
# operationId: GetEmploymentPayrollDataAuditTrailByPayrollAdministrationId
export def "providers-employers-payrolladministrations-employment-payroll-data-audit-trail GetEmploymentPayrollDataAuditTrailByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --startDate: string # The start date (e.g. 2020-08-01)
  --endDate: string # The end date (e.g. 2020-08-31)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/employmentPayrollDataAuditTrail" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of journal runs for an administration
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/journalruns
# operationId: GetJournalRunsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-journalruns GetJournalRunsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/journalruns" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate journal run (journaliseren)
#
# POST /providers/employers/payrolladministrations/{payrollAdministrationId}/journalruns/initiate
# operationId: PostInitiateJournalRunByPayrollAdministrationId
export def "providers-employers-payrolladministrations-journalruns-initiate PostInitiateJournalRunByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/journalruns/initiate")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the audit trail for the payroll period data
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollPeriodDataAuditTrail
# operationId: GetPayrollPeriodDataAuditTrailByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payroll-period-data-audit-trail GetPayrollPeriodDataAuditTrailByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --startDate: string # The start date (e.g. 2020-08-01)
  --endDate: string # The end date (e.g. 2020-08-31)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollPeriodDataAuditTrail" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of payroll period data on payroll administration level
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollPeriods/{payrollPeriodId}/payrollperioddata
# operationId: GetPayrollPeriodDataByPayrollAdministrationIdAndPayrollPeriodId
export def "providers-employers-payrolladministrations-payroll-periods-payrollperioddata GetPayrollPeriodDataByPayrollAdministrationIdAndPayrollPeriodId" [
  payrollAdministrationId: string
  payrollPeriodId: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --payslipType: int # Controles which payroll period data is returned either for the normal payslip of the tarif payslip. (format: int32)
  --payrollComponentSet: string # A predefined set of payroll components to filter which payroll components are returned.  __Note:__ Either `payrollComponentSet` or `payrollComponentCategory` must have a value.  Mutually exclusive with `payrollComponentCategory`. (format: uuid)
  --payrollComponentCategory: int # A predefined category of payroll components to filter which payroll components are returned.  __Note:__ Either `payrollComponentSet` or `payrollComponentCategory` must have a value.  Mutually exclusive with `payrollComponentSet`. (format: int32)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "payslipType" $payslipType "scalar") (serialize-qp "payrollComponentSet" $payrollComponentSet "scalar") (serialize-qp "payrollComponentCategory" $payrollComponentCategory "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollPeriods/($payrollPeriodId)/payrollperioddata" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download payroll administration configuration report
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrolladministrationsettingsreport
# operationId: GetPayrollAdministrationSettingsReportByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrolladministrationsettingsreport GetPayrollAdministrationSettingsReportByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --reportYear: int # A valid year for which the information is requested. (e.g. 2023)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportYear" $reportYear "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrolladministrationsettingsreport" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Payroll components
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollcomponents/year/{year}
# operationId: GetPayrollComponentsByPayrollAdministrationIdAndYear
export def "providers-employers-payrolladministrations-payrollcomponents-year GetPayrollComponentsByPayrollAdministrationIdAndYear" [
  payrollAdministrationId: string
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollcomponents/year/($year)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of all available payroll periods and runs
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollperiods
# operationId: GetPayrollPeriodsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrollperiods GetPayrollPeriodsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollperiods")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the results of the given payroll period and the one before
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollperiods/compare
# operationId: GetComparePayrollPeriodsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrollperiods-compare GetComparePayrollPeriodsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --payrollPeriodId: int # The unique identifier of the payroll period (format: int32, e.g. 202001)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "payrollPeriodId" $payrollPeriodId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollperiods/compare" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initialise Payroll Period (automatische processen)
#
# POST /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollperiods/initialise
# operationId: PostInitialisePayrollPeriodByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrollperiods-initialise PostInitialisePayrollPeriodByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollperiods/initialise")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of all available payroll periods
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollperiods/minimized
# operationId: GetPayrollPeriodsMinimizedByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrollperiods-minimized GetPayrollPeriodsMinimizedByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollperiods/minimized")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get payroll results for the given payroll period
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollperiods/{payrollPeriodId}/payrollresults
# operationId: GetPayrollPeriodResultsByPayrollAdministrationIdAndPayrollPeriodId
export def "providers-employers-payrolladministrations-payrollperiods-payrollresults GetPayrollPeriodResultsByPayrollAdministrationIdAndPayrollPeriodId" [
  payrollAdministrationId: string
  payrollPeriodId: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --payrollComponents: list # Can be used to filter what payroll components are returned. Cannot be used in combination with payrollComponentSet. (e.g. 70,151,12)
  --payrollComponentSet: string # A predifined set of payroll components to filter what payroll components are returned (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "payrollComponents" $payrollComponents "multi") (serialize-qp "payrollComponentSet" $payrollComponentSet "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollperiods/($payrollPeriodId)/payrollresults" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Process Information about a payroll on administration level
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollprocessoverview
# operationId: GetPayrollProcessOverviewByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrollprocessoverview GetPayrollProcessOverviewByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --payrollPeriodIdStart: int # The unique identifier of the first payroll period to return (format: int32, e.g. 202004)
  --payrollPeriodIdEnd: int # The unique identifier of the last payroll period to return (format: int32, e.g. 202006)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "payrollPeriodIdStart" $payrollPeriodIdStart "scalar") (serialize-qp "payrollPeriodIdEnd" $payrollPeriodIdEnd "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollprocessoverview" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of payroll runs for an administration
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollruns
# operationId: GetPayrollRunsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrollruns GetPayrollRunsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollruns" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate payroll run (verlonen)
#
# POST /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollruns/initiate
# operationId: PostInitiatePayrollRunByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrollruns-initiate PostInitiatePayrollRunByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollruns/initiate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Initiationvalues of a payrollrun
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrollruns/{periodId}/initiationvalues
# operationId: GetPayrollrunInitiationvaluesByPayrollAdministrationIdAndPeriodId
export def "providers-employers-payrolladministrations-payrollruns-initiationvalues GetPayrollrunInitiationvaluesByPayrollAdministrationIdAndPeriodId" [
  payrollAdministrationId: string
  periodId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --payrollrunId: string # The unique identifier of the payroll run (format: uuid)
  --payrollPeriodId: int # The unique identifier of the payroll period (format: int32, e.g. 202103)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "payrollrunId" $payrollrunId "scalar") (serialize-qp "payrollPeriodId" $payrollPeriodId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrollruns/($periodId)/initiationvalues" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of tax returns for an administration
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrolltaxreturns
# operationId: GetPayrollTaxReturnsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrolltaxreturns GetPayrollTaxReturnsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrolltaxreturns" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate payroll tax return (aanmaken loonaangifte)
#
# POST /providers/employers/payrolladministrations/{payrollAdministrationId}/payrolltaxreturns/initiate
# operationId: PostInitiatePayrollTaxReturnByPayrollAdministrationId
export def "providers-employers-payrolladministrations-payrolltaxreturns-initiate PostInitiatePayrollTaxReturnByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrolltaxreturns/initiate")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Download the SEPA file for a payroll tax return
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/payrolltaxreturns/{payrolltaxreturnId}/sepafile
# operationId: GetSepaFileByPayrollAdministrationIdAndPayrollTaxReturnId
export def "providers-employers-payrolladministrations-payrolltaxreturns-sepafile GetSepaFileByPayrollAdministrationIdAndPayrollTaxReturnId" [
  payrollAdministrationId: string
  payrolltaxreturnId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --requestedExecutionDate: string # This value is used when creating the sepa file to set the value of \<ReqdExctnDt\> (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "requestedExecutionDate" $requestedExecutionDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/payrolltaxreturns/($payrolltaxreturnId)/sepafile" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/xml"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Send an email to inform the salary administrator
#
# POST /providers/employers/payrolladministrations/{payrollAdministrationId}/periodreadyforpayroll
# operationId: PostPeriodReadyForPayrollByPayrollAdministrationId
export def "providers-employers-payrolladministrations-periodreadyforpayroll PostPeriodReadyForPayrollByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/periodreadyforpayroll")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of UPA pension declarations
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/upapensiondeclarations
# operationId: GetUpaPensionDeclarationsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-upapensiondeclarations GetUpaPensionDeclarationsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/upapensiondeclarations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate a UPA pension declaration
#
# POST /providers/employers/payrolladministrations/{payrollAdministrationId}/upapensiondeclarations/initiate
# operationId: PostInitiateUpaPensionDeclarationByPayrollAdministrationId
export def "providers-employers-payrolladministrations-upapensiondeclarations-initiate PostInitiateUpaPensionDeclarationByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/upapensiondeclarations/initiate")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Year-end statements for the payroll administration
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/yearendstatements
# operationId: GetYearEndStatementsByPayrollAdministrationId
export def "providers-employers-payrolladministrations-yearendstatements GetYearEndStatementsByPayrollAdministrationId" [
  payrollAdministrationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/yearendstatements" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download year-end statement of a payroll administration for a year
#
# GET /providers/employers/payrolladministrations/{payrollAdministrationId}/yearendstatements/{year}
# operationId: GetYearEndStatementForYearByPayrollAdministrationId
export def "providers-employers-payrolladministrations-yearendstatements GetYearEndStatementForYearByPayrollAdministrationId" [
  payrollAdministrationId: string
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --includeEmployeeSelfService: oneof<nothing, bool> # If true; then the year-end statements for employments of which the employee has access to the employee self service module will be included in the response. If false; this specific group will be excluded. (e.g. true)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "includeEmployeeSelfService" $includeEmployeeSelfService "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/payrolladministrations/($payrollAdministrationId)/yearendstatements/($year)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of download requests for an employment
#
# GET /providers/employers/users/downloadrequests
# operationId: GetDownloadRequestsByBearerToken
export def "providers-employers-users-downloadrequests GetDownloadRequestsByBearerToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/providers/employers/users/downloadrequests" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a download request
#
# POST /providers/employers/users/downloadrequests
# operationId: PostDownloadRequestByBearerToken
export def "providers-employers-users-downloadrequests PostDownloadRequestByBearerToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers/employers/users/downloadrequests")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific download request record
#
# DELETE /providers/employers/users/downloadrequests/{downloadRequestId}
# operationId: DeleteDownloadRequestByDownloadRequestId
export def "providers-employers-users-downloadrequests DeleteDownloadRequestByDownloadRequestId" [
  downloadRequestId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/downloadrequests/($downloadRequestId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a download request record
#
# GET /providers/employers/users/downloadrequests/{downloadRequestId}
# operationId: GetDownloadRequestByDownloadRequestId
export def "providers-employers-users-downloadrequests GetDownloadRequestByDownloadRequestId" [
  downloadRequestId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/downloadrequests/($downloadRequestId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download file of download request
#
# GET /providers/employers/users/downloadrequests/{downloadRequestId}/file
# operationId: GetFileByDownloadRequestId
export def "providers-employers-users-downloadrequests-file GetFileByDownloadRequestId" [
  downloadRequestId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/downloadrequests/($downloadRequestId)/file")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/vnd.rar"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Configured external tenant for an employer user
#
# GET /providers/employers/users/{employerUserId}/configuredexternaltenant
# operationId: GetConfiguredExternalTenantByEmployerUserId
export def "providers-employers-users-configuredexternaltenant GetConfiguredExternalTenantByEmployerUserId" [
  employerUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/users/($employerUserId)/configuredexternaltenant" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an external tenant for an employer user
#
# PATCH /providers/employers/users/{employerUserId}/configuredexternaltenant
# operationId: PatchConfiguredExternalTenantByEmployerUserId
export def "providers-employers-users-configuredexternaltenant PatchConfiguredExternalTenantByEmployerUserId" [
  employerUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($employerUserId)/configuredexternaltenant")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific user record
#
# DELETE /providers/employers/users/{userId}
# operationId: DeleteEmployerUserByUserId
export def "providers-employers-users DeleteEmployerUserByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an user
#
# GET /providers/employers/users/{userId}
# operationId: GetEmployerUserByUserId
export def "providers-employers-users GetEmployerUserByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Resend or change the status of an invite for an employer user.
#
# PATCH /providers/employers/users/{userId}
# operationId: PatchEmployerUserInviteByUserId
export def "providers-employers-users PatchEmployerUserInviteByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Edit the details of an user
#
# PUT /providers/employers/users/{userId}
# operationId: PutEmployerUserByUserId
export def "providers-employers-users PutEmployerUserByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of employers linked to the user
#
# GET /providers/employers/users/{userId}/employers
# operationId: GetLinkedEmployersByUserId
export def "providers-employers-users-employers GetLinkedEmployersByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/users/($userId)/employers" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of all the integrations for the employer user
#
# GET /providers/employers/users/{userId}/integrations
# operationId: GetEmployerUserIntegrationsByUserId
export def "providers-employers-users-integrations GetEmployerUserIntegrationsByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)/integrations")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an integration
#
# DELETE /providers/employers/users/{userId}/integrations/{applicationId}
# operationId: DeleteEmployerUserIntegrationByUserIdAndApplicationId
export def "providers-employers-users-integrations DeleteEmployerUserIntegrationByUserIdAndApplicationId" [
  userId: string
  applicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)/integrations/($applicationId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change an integration
#
# PUT /providers/employers/users/{userId}/integrations/{applicationId}
# operationId: PutEmployerUserIntegrationByIntegrationId
export def "providers-employers-users-integrations PutEmployerUserIntegrationByIntegrationId" [
  userId: string
  applicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)/integrations/($applicationId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Download the application logo
#
# GET /providers/employers/users/{userId}/integrations/{applicationId}/logo
# operationId: GetEmployerUserIntegrationLogoByUserIdAndApplicationId
export def "providers-employers-users-integrations-logo GetEmployerUserIntegrationLogoByUserIdAndApplicationId" [
  applicationId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)/integrations/($applicationId)/logo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# The notification settings for the user
#
# GET /providers/employers/users/{userId}/notificationsettings
# operationId: GetEmployerUserNotificationSettingsByUserId
export def "providers-employers-users-notificationsettings GetEmployerUserNotificationSettingsByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/users/($userId)/notificationsettings" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the employer user notification settings
#
# PATCH /providers/employers/users/{userId}/notificationsettings
# operationId: PatchEmployerUserNotificationSettingsByUserId
export def "providers-employers-users-notificationsettings PatchEmployerUserNotificationSettingsByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/users/($userId)/notificationsettings")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of a workflow
#
# GET /providers/employers/workflows/{workflowId}
# operationId: GetWorkflowByWorkflowId
export def "providers-employers-workflows GetWorkflowByWorkflowId" [
  workflowId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/workflows/($workflowId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the workflow
#
# PUT /providers/employers/workflows/{workflowId}
# operationId: PutWorkflowByWorkflowId
export def "providers-employers-workflows PutWorkflowByWorkflowId" [
  workflowId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/workflows/($workflowId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Allows the user to transition the workflow.
#
# POST /providers/employers/workflows/{workflowId}/transition
# operationId: TransitionWorkflowByWorkflowId
export def "providers-employers-workflows-transition TransitionWorkflowByWorkflowId" [
  workflowId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/workflows/($workflowId)/transition")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific workflow trigger mapping record
#
# DELETE /providers/employers/workflowtriggermappings/{workflowTriggerMappingId}
# operationId: DeleteFiscalPropertiesByworkflowTriggerId
export def "providers-employers-workflowtriggermappings DeleteFiscalPropertiesByworkflowTriggerId" [
  workflowTriggerMappingId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/workflowtriggermappings/($workflowTriggerMappingId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a workflow trigger mapping record
#
# GET /providers/employers/workflowtriggermappings/{workflowTriggerMappingId}
# operationId: GetWorkflowTriggerMappingByWorkflowTriggerMappingId
export def "providers-employers-workflowtriggermappings GetWorkflowTriggerMappingByWorkflowTriggerMappingId" [
  workflowTriggerMappingId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/workflowtriggermappings/($workflowTriggerMappingId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a workflow trigger mapping record
#
# PUT /providers/employers/workflowtriggermappings/{workflowTriggerMappingId}
# operationId: PutWorkflowTriggerByWorkflowTriggerId
export def "providers-employers-workflowtriggermappings PutWorkflowTriggerByWorkflowTriggerId" [
  workflowTriggerMappingId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/workflowtriggermappings/($workflowTriggerMappingId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Details of an employer
#
# GET /providers/employers/{employerId}
# operationId: GetEmployerByEmployerId
export def "providers-employers GetEmployerByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an employer
#
# PUT /providers/employers/{employerId}
# operationId: PutEmployerByEmployerId
export def "providers-employers PutEmployerByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of actual organizational entities for an employer
#
# GET /providers/employers/{employerId}/actualorganizationalentities
# operationId: GetActualOrganizationalEntitiesByEmployerId
export def "providers-employers-actualorganizationalentities GetActualOrganizationalEntitiesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/actualorganizationalentities" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of actual wages for an employer
#
# GET /providers/employers/{employerId}/actualwages
# operationId: GetActualWagesByEmployerId
export def "providers-employers-actualwages GetActualWagesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/actualwages" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of actual working hours for an employer
#
# GET /providers/employers/{employerId}/actualworkinghours
# operationId: GetActualWorkingHoursByEmployerId
export def "providers-employers-actualworkinghours GetActualWorkingHoursByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/actualworkinghours" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of administrations for an employer
#
# GET /providers/employers/{employerId}/administrations
# operationId: GetAdministrationsByEmployerId
export def "providers-employers-administrations GetAdministrationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/administrations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of announcements for an employer.
#
# GET /providers/employers/{employerId}/announcements
# operationId: GetAnnouncementsByEmployerId
export def "providers-employers-announcements GetAnnouncementsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/announcements" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of all the applications available to the employer
#
# GET /providers/employers/{employerId}/applications
# operationId: GetAvailableApplicationsByEmployerId
export def "providers-employers-applications GetAvailableApplicationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/applications")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of all the users linking the applications and the employer
#
# GET /providers/employers/{employerId}/applications/{applicationId}/users
# operationId: GetLinkedUsersByEmployerIdAndApplicationId
export def "providers-employers-applications-users GetLinkedUsersByEmployerIdAndApplicationId" [
  employerId: string
  applicationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/applications/($applicationId)/users")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of assessments
#
# GET /providers/employers/{employerId}/assessments
# operationId: GetAssessmentsByEmployerId
export def "providers-employers-assessments GetAssessmentsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/assessments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Workflows assigned to the user
#
# GET /providers/employers/{employerId}/assignedworkflows
# operationId: GetAssignedWorkflowsByEmployerId
export def "providers-employers-assignedworkflows GetAssignedWorkflowsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/assignedworkflows" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the employee data audit trail
#
# GET /providers/employers/{employerId}/auditTrail/EmployeeData
# operationId: GetEmployeeDataAuditTrailByEmployerId
export def "providers-employers-audit-trail-employee-data GetEmployeeDataAuditTrailByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --startDate: string # The start date (e.g. 2020-08-01)
  --endDate: string # The end date (e.g. 2020-08-31)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/auditTrail/EmployeeData" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of authorized activities with regard to the employer
#
# GET /providers/employers/{employerId}/authorizations
# operationId: GetAuthorizationsByEmployerId
export def "providers-employers-authorizations GetAuthorizationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-8 # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/authorizations")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-01-01")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List benefit in kind types for an employer
#
# GET /providers/employers/{employerId}/benefitinkindtypes
# operationId: GetBenefitInKindTypesByEmployerId
export def "providers-employers-benefitinkindtypes GetBenefitInKindTypesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/benefitinkindtypes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an benefit in kind type for an employer
#
# POST /providers/employers/{employerId}/benefitinkindtypes
# operationId: PostBenefitInKindTypeByEmployerId
export def "providers-employers-benefitinkindtypes PostBenefitInKindTypeByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/benefitinkindtypes")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of billable items
#
# GET /providers/employers/{employerId}/billableitems
# operationId: GetBillableItemsByEmployerId
export def "providers-employers-billableitems GetBillableItemsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/billableitems" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of concept employee for an employer
#
# GET /providers/employers/{employerId}/conceptemployees
# operationId: GetConceptEmployeesByEmployerId
export def "providers-employers-conceptemployees GetConceptEmployeesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/conceptemployees" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a concept employee for an employer
#
# POST /providers/employers/{employerId}/conceptemployees
# operationId: PostConceptEmployeeByEmployerId
export def "providers-employers-conceptemployees PostConceptEmployeeByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/conceptemployees")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of Concept employee's and their ESS (WNL) status
#
# GET /providers/employers/{employerId}/conceptemployees/selfservice
# operationId: GetConceptEmployeeSelfServiceByEmployerId
export def "providers-employers-conceptemployees-selfservice GetConceptEmployeeSelfServiceByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/conceptemployees/selfservice" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List contract codes for an employer
#
# GET /providers/employers/{employerId}/contractcodes
# operationId: GetContractCodesByEmployerId
export def "providers-employers-contractcodes GetContractCodesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/contractcodes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a contract code for an employer
#
# POST /providers/employers/{employerId}/contractcodes
# operationId: PostContractCodeByEmployerId
export def "providers-employers-contractcodes PostContractCodeByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/contractcodes")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List custom fields for an employer
#
# GET /providers/employers/{employerId}/customfields
# operationId: GetCustomFieldsByEmployerId
export def "providers-employers-customfields GetCustomFieldsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/customfields" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a custom field for an employer
#
# POST /providers/employers/{employerId}/customfields
# operationId: PostCustomFieldByEmployerId
export def "providers-employers-customfields PostCustomFieldByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/customfields")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List custom holidays for an employer
#
# GET /providers/employers/{employerId}/customholidays
# operationId: GetCustomHolidaysByEmployerId
export def "providers-employers-customholidays GetCustomHolidaysByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/customholidays" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a custom holiday for an employer
#
# POST /providers/employers/{employerId}/customholidays
# operationId: PostCustomHolidayByEmployerId
export def "providers-employers-customholidays PostCustomHolidayByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/customholidays")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Dashboard of an employer
#
# GET /providers/employers/{employerId}/dashboard
# operationId: GetEmployerDashboardByEmployerId
export def "providers-employers-dashboard GetEmployerDashboardByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/dashboard")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the list dashboard licenses
#
# GET /providers/employers/{employerId}/dashboardLicenses
# operationId: GetDashboardLicensesByEmployerId
export def "providers-employers-dashboard-licenses GetDashboardLicensesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/dashboardLicenses" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the dashboard license
#
# PUT /providers/employers/{employerId}/dashboardLicenses
# operationId: PutDashboardLicensesByEmployerId
export def "providers-employers-dashboard-licenses PutDashboardLicensesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/dashboardLicenses")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get Data New Business token for an employer
#
# GET /providers/employers/{employerId}/datanewbusinesstoken
# operationId: GetDataNewBusinessTokenByEmployerId
export def "providers-employers-datanewbusinesstoken GetDataNewBusinessTokenByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/datanewbusinesstoken")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of departments
#
# GET /providers/employers/{employerId}/departments
# operationId: GetDepartmentsByEmployerId
export def "providers-employers-departments GetDepartmentsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/departments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a department for an employer
#
# POST /providers/employers/{employerId}/departments
# operationId: PostDepartmentByEmployerId
export def "providers-employers-departments PostDepartmentByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/departments")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of employer dossier documents
#
# GET /providers/employers/{employerId}/documents
# operationId: GetDocumentsByEmployerId
export def "providers-employers-documents GetDocumentsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload a document to the employer dossier
#
# POST /providers/employers/{employerId}/documents
# operationId: PostDocumentByEmployerId
export def "providers-employers-documents PostDocumentByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of authorizations
#
# GET /providers/employers/{employerId}/documents/authorizations
# operationId: GetDossierAuthorizationsByEmployerId
export def "providers-employers-documents-authorizations GetDossierAuthorizationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/authorizations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the authorization matrix
#
# PUT /providers/employers/{employerId}/documents/authorizations
# operationId: PutDossierAuthorizationsByEmployerId
export def "providers-employers-documents-authorizations PutDossierAuthorizationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/authorizations")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete complete dossier
#
# DELETE /providers/employers/{employerId}/documents/completedossier
# operationId: DeleteCompleteDossierByEmployerId
export def "providers-employers-documents-completedossier DeleteCompleteDossierByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/completedossier")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Generate complete dossier
#
# POST /providers/employers/{employerId}/documents/completedossier/initiate
# operationId: PostInitiateCompleteDossierByEmployerId
export def "providers-employers-documents-completedossier-initiate PostInitiateCompleteDossierByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/completedossier/initiate")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a document in the employer dossier
#
# DELETE /providers/employers/{employerId}/documents/{documentId}
# operationId: DeleteDocumentByEmployerIdAndDocumentId
export def "providers-employers-documents DeleteDocumentByEmployerIdAndDocumentId" [
  employerId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/($documentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download employer dossier document
#
# GET /providers/employers/{employerId}/documents/{documentId}
# operationId: GetDocumentByEmployerIdAndDocumentId
export def "providers-employers-documents GetDocumentByEmployerIdAndDocumentId" [
  employerId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/($documentId)")
  let extra_headers = {"Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an employer dossier document
#
# PUT /providers/employers/{employerId}/documents/{documentId}
# operationId: PutDocumentByEmployerIdAndDocumentId
export def "providers-employers-documents PutDocumentByEmployerIdAndDocumentId" [
  employerId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/($documentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get audittrail for the document in the employer dossier
#
# GET /providers/employers/{employerId}/documents/{documentId}/audittrail
# operationId: GetAuditTrailByEmployerIdAndDocumentId
export def "providers-employers-documents-audittrail GetAuditTrailByEmployerIdAndDocumentId" [
  documentId: string
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/documents/($documentId)/audittrail" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of document templates
#
# GET /providers/employers/{employerId}/documenttemplates
# operationId: GetDocumentTemplatesByEmployerId
export def "providers-employers-documenttemplates GetDocumentTemplatesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/documenttemplates" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload a document template to the employer dossier
#
# POST /providers/employers/{employerId}/documenttemplates
# operationId: PostDocumentTemplateByEmployerId
export def "providers-employers-documenttemplates PostDocumentTemplateByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documenttemplates")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a document template in the employer dossier
#
# DELETE /providers/employers/{employerId}/documenttemplates/{documentId}
# operationId: DeleteDocumentTemplateByEmployerIdAndDocumentId
export def "providers-employers-documenttemplates DeleteDocumentTemplateByEmployerIdAndDocumentId" [
  employerId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documenttemplates/($documentId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download template document
#
# GET /providers/employers/{employerId}/documenttemplates/{documentId}
# operationId: GetDocumentTemplateByEmployerIdAndDocumentId
export def "providers-employers-documenttemplates GetDocumentTemplateByEmployerIdAndDocumentId" [
  employerId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documenttemplates/($documentId)")
  let extra_headers = {"Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/vnd.openxmlformats-officedocument.wordprocessingml.document"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an document template
#
# PUT /providers/employers/{employerId}/documenttemplates/{documentId}
# operationId: PutDocumentTemplateByEmployerIdAndDocumentId
export def "providers-employers-documenttemplates PutDocumentTemplateByEmployerIdAndDocumentId" [
  employerId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documenttemplates/($documentId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Generate documents for selected employments
#
# POST /providers/employers/{employerId}/documenttemplates/{documentId}/generatedocuments
# operationId: PostGenerateDocumentsByEmployerIdAndDocumentId
export def "providers-employers-documenttemplates-generatedocuments PostGenerateDocumentsByEmployerIdAndDocumentId" [
  employerId: string
  documentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/documenttemplates/($documentId)/generatedocuments")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List education further indications for an employer
#
# GET /providers/employers/{employerId}/educationfurtherindications
# operationId: GetEducationFurtherIndicationsByEmployerId
export def "providers-employers-educationfurtherindications GetEducationFurtherIndicationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/educationfurtherindications" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an education further indication for an employer
#
# POST /providers/employers/{employerId}/educationfurtherindications
# operationId: PostEducationFurtherIndicationByEmployerId
export def "providers-employers-educationfurtherindications PostEducationFurtherIndicationByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/educationfurtherindications")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List education types for an employer
#
# GET /providers/employers/{employerId}/educationtypes
# operationId: GetEducationTypesByEmployerId
export def "providers-employers-educationtypes GetEducationTypesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/educationtypes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an education type for an employer
#
# POST /providers/employers/{employerId}/educationtypes
# operationId: PostEducationTypeByEmployerId
export def "providers-employers-educationtypes PostEducationTypeByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/educationtypes")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of EmailIdentities for an employer
#
# GET /providers/employers/{employerId}/emailidentities
# operationId: GetEmailIdentitiesByEmployerId
export def "providers-employers-emailidentities GetEmailIdentitiesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/emailidentities" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an EmailIdentity for an employer
#
# POST /providers/employers/{employerId}/emailidentities
# operationId: PostEmailIdentityByEmployerId
export def "providers-employers-emailidentities PostEmailIdentityByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/emailidentities")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Verify an EmailIdentity for an employer
#
# POST /providers/employers/{employerId}/emailidentities/verify
# operationId: PostVerifyEmailIdentityByEmployerId
export def "providers-employers-emailidentities-verify PostVerifyEmailIdentityByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/emailidentities/verify")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List email templates for an employer
#
# GET /providers/employers/{employerId}/emailtemplates
# operationId: GetEmailTemplatesByEmployerId
export def "providers-employers-emailtemplates GetEmailTemplatesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/emailtemplates" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an email template record
#
# DELETE /providers/employers/{employerId}/emailtemplates/{emailTemplateId}
# operationId: DeleteEmailTemplateByEmployerIdAndEmailTemplateId
export def "providers-employers-emailtemplates DeleteEmailTemplateByEmployerIdAndEmailTemplateId" [
  employerId: string
  emailTemplateId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --emailtemplateId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "emailtemplateId" $emailtemplateId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/emailtemplates/($emailTemplateId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an email template
#
# GET /providers/employers/{employerId}/emailtemplates/{emailTemplateId}
# operationId: GetEmailTemplateByEmployerIdAndEmailTemplateId
export def "providers-employers-emailtemplates GetEmailTemplateByEmployerIdAndEmailTemplateId" [
  employerId: string
  emailTemplateId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --emailtemplateId: string # The unique identifier of the record (format: uuid)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "emailtemplateId" $emailtemplateId "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/emailtemplates/($emailTemplateId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit an email template record
#
# PUT /providers/employers/{employerId}/emailtemplates/{emailTemplateId}
# operationId: PutEmailTemplateByEmployerIdAndEmailTemplateId
export def "providers-employers-emailtemplates PutEmailTemplateByEmployerIdAndEmailTemplateId" [
  employerId: string
  emailTemplateId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --emailtemplateId: string # The unique identifier of the record (format: uuid)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "emailtemplateId" $emailtemplateId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/emailtemplates/($emailTemplateId)" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of employees for an employer
#
# GET /providers/employers/{employerId}/employees
# operationId: GetEmployeesByEmployerId
export def "providers-employers-employees GetEmployeesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-4 # Response content type
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output  The default option is `application/json;version=yyyy-MM-dd`. For the export functionality two other options are supporten:   * text/csv;version=yyyy-MM-dd   * application/vnd.openxmlformats-officedocument.spreadsheetml.sheet;version=yyyy-MM-dd (e.g. application/json;version=2018-01-01)
  --X-ReportInput: string # Allows the user set what values are returnd in the "export" (csv or excel).
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "X-ReportInput": $X_ReportInput} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "application/json;version=2018-12-31")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of Partners for an employer
#
# GET /providers/employers/{employerId}/employees/actualpartners
# operationId: GetActualPartnersByEmployerId
export def "providers-employers-employees-actualpartners GetActualPartnersByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/actualpartners" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of Children for an employer
#
# GET /providers/employers/{employerId}/employees/children
# operationId: GetChildrenByEmployerId
export def "providers-employers-employees-children GetChildrenByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/children" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of Contacts for an employer
#
# GET /providers/employers/{employerId}/employees/contacts
# operationId: GetContactsByEmployerId
export def "providers-employers-employees-contacts GetContactsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/contacts" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of employments for an employer
#
# GET /providers/employers/{employerId}/employees/employments
# operationId: GetEmploymentsByEmployerId
export def "providers-employers-employees-employments GetEmploymentsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2019-12-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of wage proposals for the employments of an employer
#
# GET /providers/employers/{employerId}/employees/employments/WageProposals
# operationId: GetWageProposalsByEmployerId
export def "providers-employers-employees-employments-wage-proposals GetWageProposalsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/WageProposals" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of actual benefitsanddeductions for an employer
#
# GET /providers/employers/{employerId}/employees/employments/actualbenefitsanddeductions
# operationId: GetActualBenefitsAndDeductionsByEmployerId
export def "providers-employers-employees-employments-actualbenefitsanddeductions GetActualBenefitsAndDeductionsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/actualbenefitsanddeductions" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create benefits and deductions for multiple employments
#
# POST /providers/employers/{employerId}/employees/employments/benefitsanddeductions
# operationId: PostCollectiveBenefitsAndDeductionsByEmployerId
export def "providers-employers-employees-employments-benefitsanddeductions PostCollectiveBenefitsAndDeductionsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/benefitsanddeductions")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List absences per employment (for calendar)
#
# GET /providers/employers/{employerId}/employees/employments/calendar/absences
# operationId: GetCalendarAbsencesByEmployerId
export def "providers-employers-employees-employments-calendar-absences GetCalendarAbsencesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --startDate: string # A valid date (yyyy-MM-dd). First day of the timebox for which calendar data will be collected. (format: date)
  --endDate: string # A valid date (yyyy-MM-dd). Last day of the timebox for which calendar data will be collected. (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/calendar/absences" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List available hours per employment (for calendar)
#
# GET /providers/employers/{employerId}/employees/employments/calendar/availablehours
# operationId: GetCalendarAvailableHoursByEmployerId
export def "providers-employers-employees-employments-calendar-availablehours GetCalendarAvailableHoursByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --startDate: string # A valid date (yyyy-MM-dd). First day of the timebox for which calendar data will be collected. (format: date)
  --endDate: string # A valid date (yyyy-MM-dd). Last day of the timebox for which calendar data will be collected. (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/calendar/availablehours" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List leave per employment (for calendar)
#
# GET /providers/employers/{employerId}/employees/employments/calendar/leave
# operationId: GetCalendarLeaveByEmployerId
export def "providers-employers-employees-employments-calendar-leave GetCalendarLeaveByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --startDate: string # A valid date (yyyy-MM-dd). First day of the timebox for which calendar data will be collected. (format: date)
  --endDate: string # A valid date (yyyy-MM-dd). Last day of the timebox for which calendar data will be collected. (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/calendar/leave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List leave requests per employment (for calendar)
#
# GET /providers/employers/{employerId}/employees/employments/calendar/leaverequests
# operationId: GetCalendarLeaveRequestsByEmployerId
export def "providers-employers-employees-employments-calendar-leaverequests GetCalendarLeaveRequestsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --startDate: string # A valid date (yyyy-MM-dd). First day of the timebox for which calendar data will be collected. (format: date)
  --endDate: string # A valid date (yyyy-MM-dd). Last day of the timebox for which calendar data will be collected. (format: date)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/calendar/leaverequests" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of employments with additional entities
#
# GET /providers/employers/{employerId}/employees/employments/comprehensive
# operationId: GetEmploymentsComprehensiveByEmployerId
export def "providers-employers-employees-employments-comprehensive GetEmploymentsComprehensiveByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for choosing the date dependent data. (format: date, e.g. 2022-01-01)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/comprehensive" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2019-12-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of declarations for the employees of an employer
#
# GET /providers/employers/{employerId}/employees/employments/declarations
# operationId: GetDeclarationsForEmployerId
export def "providers-employers-employees-employments-declarations GetDeclarationsForEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/declarations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of declarations with attachment
#
# GET /providers/employers/{employerId}/employees/employments/declarations/withattachment
# operationId: GetDeclarationsWithAttachmentByEmployerId
export def "providers-employers-employees-employments-declarations-withattachment GetDeclarationsWithAttachmentByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/declarations/withattachment" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of employment version numbers
#
# GET /providers/employers/{employerId}/employees/employments/lastmodifiedversionnumbers
# operationId: GetEmploymentLastModifiedVersionNumbersByUser
export def "providers-employers-employees-employments-lastmodifiedversionnumbers GetEmploymentLastModifiedVersionNumbersByUser" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/lastmodifiedversionnumbers" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of leave requests for the employees of an employer
#
# GET /providers/employers/{employerId}/employees/employments/leaverequests
# operationId: GetLeaveRequestsForEmployer
export def "providers-employers-employees-employments-leaverequests GetLeaveRequestsForEmployer" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/leaverequests" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of employments with fewer fields for performance reasons
#
# GET /providers/employers/{employerId}/employees/employments/minimized
# operationId: GetEmploymentsMinimizedByEmployerId
export def "providers-employers-employees-employments-minimized GetEmploymentsMinimizedByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/minimized" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2019-12-16"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create wages for multiple employments
#
# POST /providers/employers/{employerId}/employees/employments/wages
# operationId: PostCollectiveWageByEmployerId
export def "providers-employers-employees-employments-wages PostCollectiveWageByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/employments/wages")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of employee's and their ESS (WNL) status
#
# GET /providers/employers/{employerId}/employees/selfservice
# operationId: GetEmployeeSelfServiceByEmployerId
export def "providers-employers-employees-selfservice GetEmployeeSelfServiceByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/selfservice" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Change the ESS access for several employees
#
# PATCH /providers/employers/{employerId}/employees/selfservice
# operationId: PatchEmployeeSelfServiceAccessByEmployerId
export def "providers-employers-employees-selfservice PatchEmployeeSelfServiceAccessByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/employees/selfservice")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Employment templates
#
# GET /providers/employers/{employerId}/employmenttemplates
# operationId: GetEmploymentTemplatesByEmployerId
export def "providers-employers-employmenttemplates GetEmploymentTemplatesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/employmenttemplates")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get available external tenants (employer scope)
#
# GET /providers/employers/{employerId}/externaltenants
# operationId: GetExternalTenantsByEmployerId
export def "providers-employers-externaltenants GetExternalTenantsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/externaltenants")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of functions
#
# GET /providers/employers/{employerId}/functions
# operationId: GetFunctionsByEmployerId
export def "providers-employers-functions GetFunctionsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/functions" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a function for an employer
#
# POST /providers/employers/{employerId}/functions
# operationId: PostFunctionByEmployerId
export def "providers-employers-functions PostFunctionByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/functions")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Post leave for multiple employments.
#
# POST /providers/employers/{employerId}/leave
# operationId: PostCollectiveLeaveByEmployerId
export def "providers-employers-leave PostCollectiveLeaveByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/leave")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get the proposed number of leave hours for all employments of an employer
#
# GET /providers/employers/{employerId}/leave/proposedleavehours
# operationId: GetProposedLeaveHoursByEmployerId
export def "providers-employers-leave-proposedleavehours GetProposedLeaveHoursByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --startDate: string # The start date of the leave period (e.g. 2022-08-01)
  --endDate: string # The end date of the leave period (e.g. 2022-08-07)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/leave/proposedleavehours" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of all the leave policies
#
# GET /providers/employers/{employerId}/leavePolicies
# operationId: GetLeavePoliciesByEmployerId
export def "providers-employers-leave-policies GetLeavePoliciesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavePolicies" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a leave policy based on an existing leave policy
#
# PATCH /providers/employers/{employerId}/leavePolicies
# operationId: PatchCopyLeavePolicyByEmployerId
export def "providers-employers-leave-policies PatchCopyLeavePolicyByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavePolicies")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Create a leave policy
#
# POST /providers/employers/{employerId}/leavePolicies
# operationId: PostLeavePolicyByEmployerId
export def "providers-employers-leave-policies PostLeavePolicyByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavePolicies")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# The leave balance for all employments
#
# GET /providers/employers/{employerId}/leavebalances
# operationId: GetLeaveBalancesByEmployerId
export def "providers-employers-leavebalances GetLeaveBalancesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for the following logic   1. To determine the active `function` and `department` of the employment   2. To calculate `usageAfterToday` and `usageThroughToday`   3. To determine the year for which the leave balance is returned  If not provided, "today" is used (format: date, e.g. 2020-12-31)
  --leaveType: list # Select which `leaveType` will be returned. Based on the provided 'leaveType.key'(s)  If not provided, the `leaveType` 16 (Verlof) is returned (16 is the sum of leaveType 1 (bovenwettelijk verlof) and 15 (wettelijk verlof))
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "leaveType" $leaveType "multi") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavebalances" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Summed up leave balance total and grouped by
#
# GET /providers/employers/{employerId}/leavebalances/grouped
# operationId: GetLeaveBalancesGroupedByEmployerId
export def "providers-employers-leavebalances-grouped GetLeaveBalancesGroupedByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --referenceDate: string # Set a reference date for the following logic   1. To determine the active `function` and `department` of the employment   2. To calculate `usageAfterToday` and `usageThroughToday`   3. To determine the year for which the leave balance is returned  If not provided, "today" is used (format: date, e.g. 2020-12-31)
  --leaveType: list # Select which `leaveType` will be returned. Based on the provided 'leaveType.key'(s)  If not provided, the `leaveType` 16 (Verlof) is returned (16 is the sum of leaveType 1 (bovenwettelijk verlof) and 15 (wettelijk verlof))
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --groupBy: string@groupBy-completer # Select the groupBy
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referenceDate" $referenceDate "scalar") (serialize-qp "leaveType" $leaveType "multi") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar") (serialize-qp "groupBy" $groupBy "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavebalances/grouped" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of leave types
#
# GET /providers/employers/{employerId}/leavetypes
# operationId: GetLeaveTypesByEmployerId
export def "providers-employers-leavetypes GetLeaveTypesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavetypes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a leave type
#
# GET /providers/employers/{employerId}/leavetypes/{leaveTypeId}
# operationId: GetLeaveTypeByEmployerIdAndLeaveTypeId
export def "providers-employers-leavetypes GetLeaveTypeByEmployerIdAndLeaveTypeId" [
  leaveTypeId: string
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavetypes/($leaveTypeId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a leave type
#
# PUT /providers/employers/{employerId}/leavetypes/{leaveTypeId}
# operationId: PutLeaveTypeByEmployerIdAndLeaveTypeId
export def "providers-employers-leavetypes PutLeaveTypeByEmployerIdAndLeaveTypeId" [
  leaveTypeId: string
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/leavetypes/($leaveTypeId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete the employer logo
#
# DELETE /providers/employers/{employerId}/logo
# operationId: DeleteEmployerLogoByEmployerId
export def "providers-employers-logo DeleteEmployerLogoByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/logo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Logo of an employer
#
# GET /providers/employers/{employerId}/logo
# operationId: GetEmployerLogoByEmployerId
export def "providers-employers-logo GetEmployerLogoByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/logo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload a logo for the employer
#
# POST /providers/employers/{employerId}/logo
# operationId: PostEmployerLogoByEmployerId
export def "providers-employers-logo PostEmployerLogoByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/logo")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Logo of an employer
#
# GET /providers/employers/{employerId}/logo/{version}
# operationId: GetEmployerLogoByEmployerIdAndVersion
export def "providers-employers-logo GetEmployerLogoByEmployerIdAndVersion" [
  employerId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/logo/($version)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of modules enabled for the employer
#
# GET /providers/employers/{employerId}/modules
# operationId: GetModulesByEmployerId
export def "providers-employers-modules GetModulesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/modules")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of national holidays
#
# GET /providers/employers/{employerId}/nationalholidays
# operationId: GetNationalHolidaysByEmployerId
export def "providers-employers-nationalholidays GetNationalHolidaysByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/nationalholidays" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a national holiday
#
# GET /providers/employers/{employerId}/nationalholidays/{nationalHolidayId}
# operationId: GetNationalHolidaysByEmployerIdAndNationalHolidayId
export def "providers-employers-nationalholidays GetNationalHolidaysByEmployerIdAndNationalHolidayId" [
  nationalHolidayId: string
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/nationalholidays/($nationalHolidayId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of a national holidays
#
# PUT /providers/employers/{employerId}/nationalholidays/{nationalHolidayId}
# operationId: PutNationalHolidayByEmployerIdAndNationalHolidayId
export def "providers-employers-nationalholidays PutNationalHolidayByEmployerIdAndNationalHolidayId" [
  nationalHolidayId: string
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/nationalholidays/($nationalHolidayId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of notes for an employer.
#
# GET /providers/employers/{employerId}/notes
# operationId: GetEmployerNotesByEmployerId
export def "providers-employers-notes GetEmployerNotesByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/notes" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a note for an employer
#
# POST /providers/employers/{employerId}/notes
# operationId: PostEmployerNoteByEmployerId
export def "providers-employers-notes PostEmployerNoteByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/notes")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get a list of notifications for an employer.
#
# GET /providers/employers/{employerId}/notifications
# operationId: GetNotificationsByEmployerId
export def "providers-employers-notifications GetNotificationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/notifications" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of payroll administrations for an employer
#
# GET /providers/employers/{employerId}/payrolladministrations
# operationId: GetPayrollAdministrationsByEmployerId
export def "providers-employers-payrolladministrations GetPayrollAdministrationsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/payrolladministrations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the status of proforma for this employer
#
# GET /providers/employers/{employerId}/proforma
# operationId: GetProformaStatusByEmployerId
export def "providers-employers-proforma GetProformaStatusByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/proforma")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initialize the proforma environment
#
# POST /providers/employers/{employerId}/proforma/initialize
# operationId: PostInitializeProformaByEmployerId
export def "providers-employers-proforma-initialize PostInitializeProformaByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/proforma/initialize")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Provider logo
#
# GET /providers/employers/{employerId}/providerlogo
# operationId: GetProviderLogoByEmployerId
export def "providers-employers-providerlogo GetProviderLogoByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/providerlogo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Provider logo
#
# GET /providers/employers/{employerId}/providerlogo/{version}
# operationId: GetProviderLogoByEmployerIdAndVersion
export def "providers-employers-providerlogo GetProviderLogoByEmployerIdAndVersion" [
  employerId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/providerlogo/($version)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of user accessible departments
#
# GET /providers/employers/{employerId}/useraccessibledepartments
# operationId: GetUserAccessibleDepartmentsByEmployerId
export def "providers-employers-useraccessibledepartments GetUserAccessibleDepartmentsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/useraccessibledepartments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of users for an employer
#
# GET /providers/employers/{employerId}/users
# operationId: GetEmployerUsersByEmployerId
export def "providers-employers-users GetEmployerUsersByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/users" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=SSO_gebruiker"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an SSO or Azure AD user for an employer
#
# POST /providers/employers/{employerId}/users
# operationId: PostEmployerUserByEmployerId
export def "providers-employers-users PostEmployerUserByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/users")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get the list of departments that the user has access to
#
# GET /providers/employers/{employerId}/users/departments
# operationId: GetEmployerUsersLinkedDepartmentsByEmployerId
export def "providers-employers-users-departments GetEmployerUsersLinkedDepartmentsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/users/departments" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Send an invite for an employer user
#
# POST /providers/employers/{employerId}/users/invite
# operationId: PostInviteEmployerUserByEmployerId
export def "providers-employers-users-invite PostInviteEmployerUserByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/users/invite")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Link an existing employer user to this employer.
#
# POST /providers/employers/{employerId}/users/link
# operationId: PostLinkExistingUserByEmployerId
export def "providers-employers-users-link PostLinkExistingUserByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/users/link")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# The authorizations for the user
#
# GET /providers/employers/{employerId}/users/{userId}/authorizations
# operationId: GetEmployerUserAuthorizationsByEmployerIdAndUserId
export def "providers-employers-users-authorizations GetEmployerUserAuthorizationsByEmployerIdAndUserId" [
  employerId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/users/($userId)/authorizations" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the employer user authorizations
#
# PATCH /providers/employers/{employerId}/users/{userId}/authorizations
# operationId: PatchEmployerUserAuthorizationsByUserId
export def "providers-employers-users-authorizations PatchEmployerUserAuthorizationsByUserId" [
  employerId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/users/($userId)/authorizations")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Link or unlink departments from an user.
#
# PATCH /providers/employers/{employerId}/users/{userId}/departments
# operationId: PatchEmployerUserLinkedDepartmentsByEmployerIdAndUserId
export def "providers-employers-users-departments PatchEmployerUserLinkedDepartmentsByEmployerIdAndUserId" [
  employerId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/users/($userId)/departments")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Workflows for the employer
#
# GET /providers/employers/{employerId}/workflows
# operationId: GetWorkflowsByEmployerId
export def "providers-employers-workflows GetWorkflowsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/workflows" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Workflowtrigger to workflowtemplate mapping
#
# GET /providers/employers/{employerId}/workflowtriggermappings
# operationId: GetWorkflowTriggerMappingsByEmployerId
export def "providers-employers-workflowtriggermappings GetWorkflowTriggerMappingsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/employers/($employerId)/workflowtriggermappings" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a workflow trigger mapping record for an employer
#
# POST /providers/employers/{employerId}/workflowtriggermappings
# operationId: PostWorkflowTriggerMappingByEmployerId
export def "providers-employers-workflowtriggermappings PostWorkflowTriggerMappingByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/employers/($employerId)/workflowtriggermappings")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# A single leave policy
#
# GET /providers/leavePolicies/{leavePolicyId}
# operationId: GetProviderLeavePolicyByLeavePolicyId
export def "providers-leave-policies GetProviderLeavePolicyByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/leavePolicies/($leavePolicyId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Age based leave
#
# GET /providers/leavePolicies/{leavePolicyId}/agebasedleave
# operationId: GetProviderAgeBasedLeaveByLeavePolicyId
export def "providers-leave-policies-agebasedleave GetProviderAgeBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/leavePolicies/($leavePolicyId)/agebasedleave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Wage based leave
#
# GET /providers/leavePolicies/{leavePolicyId}/wagebasedleave
# operationId: GetProviderWageBasedLeaveByLeavePolicyId
export def "providers-leave-policies-wagebasedleave GetProviderWageBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/leavePolicies/($leavePolicyId)/wagebasedleave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# years of service based leave
#
# GET /providers/leavePolicies/{leavePolicyId}/yearsofservicebasedleave
# operationId: GetProviderYearsOfServiceBasedLeaveByLeavePolicyId
export def "providers-leave-policies-yearsofservicebasedleave GetProviderYearsOfServiceBasedLeaveByLeavePolicyId" [
  leavePolicyId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/leavePolicies/($leavePolicyId)/yearsofservicebasedleave" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of a notification set
#
# GET /providers/notificationsets/{notificationSetId}
# operationId: GetNotificationSetByNotificationSetId
export def "providers-notificationsets GetNotificationSetByNotificationSetId" [
  notificationSetId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/notificationsets/($notificationSetId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Configured external tenant for a provider user
#
# GET /providers/users/{providerUserId}/configuredexternaltenant
# operationId: GetConfiguredExternalTenantByProviderUserId
export def "providers-users-configuredexternaltenant GetConfiguredExternalTenantByProviderUserId" [
  providerUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/users/($providerUserId)/configuredexternaltenant" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add an external tenant for a provider user
#
# PATCH /providers/users/{providerUserId}/configuredexternaltenant
# operationId: PatchConfiguredExternalTenantByProviderUserId
export def "providers-users-configuredexternaltenant PatchConfiguredExternalTenantByProviderUserId" [
  providerUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/users/($providerUserId)/configuredexternaltenant")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of applicationsets for a provider
#
# GET /providers/{providerId}/applicationsets
# operationId: GetApplicationsetsByProviderId
export def "providers-applicationsets GetApplicationsetsByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/applicationsets" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an applicationset for a provider
#
# POST /providers/{providerId}/applicationsets
# operationId: PostApplicationSetByProviderId
export def "providers-applicationsets PostApplicationSetByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/applicationsets")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List of authorized activities with regard to the provider
#
# GET /providers/{providerId}/authorizations
# operationId: GetAuthorizationsByProviderId
export def "providers-authorizations GetAuthorizationsByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/authorizations")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of authorization sets
#
# GET /providers/{providerId}/authorizationsets
# operationId: GetAuthorizationSetsByProviderId
export def "providers-authorizationsets GetAuthorizationSetsByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/authorizationsets" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of billable items
#
# GET /providers/{providerId}/billableitems
# operationId: GetBillableItemsByProviderId
export def "providers-billableitems GetBillableItemsByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/billableitems" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of EmailIdentities for a provider
#
# GET /providers/{providerId}/emailidentities
# operationId: GetEmailIdentitiesByProviderId
export def "providers-emailidentities GetEmailIdentitiesByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/emailidentities" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an EmailIdentity for a provider
#
# POST /providers/{providerId}/emailidentities
# operationId: PostEmailIdentityByProviderId
export def "providers-emailidentities PostEmailIdentityByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/emailidentities")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Verify an EmailIdentity for a provider
#
# POST /providers/{providerId}/emailidentities/verify
# operationId: PostVerifyEmailIdentityByProviderId
export def "providers-emailidentities-verify PostVerifyEmailIdentityByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/emailidentities/verify")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# List email templates for an provider
#
# GET /providers/{providerId}/emailtemplates
# operationId: GetEmailTemplatesByProviderId
export def "providers-emailtemplates GetEmailTemplatesByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/emailtemplates" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Details of an email template
#
# GET /providers/{providerId}/emailtemplates/{emailTemplateId}
# operationId: GetEmailTemplateByProviderIdAndEmailTemplateId
export def "providers-emailtemplates GetEmailTemplateByProviderIdAndEmailTemplateId" [
  providerId: string
  emailTemplateId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --emailtemplateId: string # The unique identifier of the record (format: uuid)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "emailtemplateId" $emailtemplateId "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/emailtemplates/($emailTemplateId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an employer
#
# POST /providers/{providerId}/employers
# operationId: PostEmployerByProviderId
export def "providers-employers PostEmployerByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/employers")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get available external tenants (provider scope)
#
# GET /providers/{providerId}/externaltenants
# operationId: GetExternalTenantsByProviderId
export def "providers-externaltenants GetExternalTenantsByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/externaltenants")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of all the leave policies at provider level.
#
# GET /providers/{providerId}/leavePolicies
# operationId: GetProviderLeavePoliciesByProviderId
export def "providers-leave-policies GetProviderLeavePoliciesByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/leavePolicies" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the provider logo
#
# GET /providers/{providerId}/logo
# operationId: GetProviderLogo
export def "providers-logo GetProviderLogo" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/logo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Download the provider logo
#
# GET /providers/{providerId}/logo/{version}
# operationId: GetProviderLogoByVersion
export def "providers-logo GetProviderLogoByVersion" [
  providerId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($providerId)/logo/($version)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of notification sets
#
# GET /providers/{providerId}/notificationsets
# operationId: GetNotificationSetsByProviderId
export def "providers-notificationsets GetNotificationSetsByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/notificationsets" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Payroll status for each administration.
#
# GET /providers/{providerId}/payrollprocessstatus
# operationId: GetPayrollProcessStatusByProviderId
export def "providers-payrollprocessstatus GetPayrollProcessStatusByProviderId" [
  providerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --payrollPeriodId: int # The unique identifier of the payroll period to return (format: int32, e.g. 202201)
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "payrollPeriodId" $payrollPeriodId "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($providerId)/payrollprocessstatus" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of employers using Qwoater
#
# GET /qwoater/employers
# operationId: GetQwoaterEmployersByUser
export def "qwoater-employers GetQwoaterEmployersByUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/qwoater/employers" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Acquire the additional tax liability by the license plate number (company car)
#
# GET /rdwservices/additionaltaxliability
# operationId: GetAdditionalTaxliabilityByLicensePlateNumber
export def "rdwservices-additionaltaxliability GetAdditionalTaxliabilityByLicensePlateNumber" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --licensePlateNumber: string # A valid vehicle registration (license plate) number. (e.g. 52LRX4)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "licensePlateNumber" $licensePlateNumber "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/rdwservices/additionaltaxliability" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get current user
#
# GET /user
# operationId: GetUserByToken
export def "user GetUserByToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit current user
#
# PUT /user
# operationId: PutUserByToken
export def "user PutUserByToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get user filter settings
#
# GET /user/filtersettings/{employerId}
# operationId: GetUserFilterSettingsByEmployerId
export def "user-filtersettings GetUserFilterSettingsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default "https://k8s.loket.nl/globalfilter-microservice")
  let full_url = (build-url $base $"/user/filtersettings/($employerId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the user filter settings
#
# PUT /user/filtersettings/{employerId}
# operationId: PutUserFilterSettingsByEmployerId
export def "user-filtersettings PutUserFilterSettingsByEmployerId" [
  employerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default "https://k8s.loket.nl/globalfilter-microservice")
  let full_url = (build-url $base $"/user/filtersettings/($employerId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete user photo
#
# DELETE /user/photo
# operationId: DeleteUserPhotoByToken
export def "user-photo DeleteUserPhotoByToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/photo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Photo of an user
#
# GET /user/photo
# operationId: GetUserPhotoByToken
export def "user-photo GetUserPhotoByToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/photo")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = ($accept | default "image/jpeg")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Post user photo
#
# POST /user/photo
# operationId: PostUserPhotoByToken
export def "user-photo PostUserPhotoByToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user/photo")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Delete a specific applicationset for a user
#
# DELETE /users/applicationsets/{applicationSetId}
# operationId: DeleteUserApplicationSetByApplicationSetId
export def "users-applicationsets DeleteUserApplicationSetByApplicationSetId" [
  applicationSetId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
]: nothing -> record<version: record<obsoleteDate: string, resourceVersion: string>, messages: table<code: int, description: string, id: string, properties: list, type: string>, content: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/applicationsets/($applicationSetId)")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Edit the details of an applicationset for a user
#
# PUT /users/applicationsets/{applicationSetId}
# operationId: PutUserApplicationSetByApplicationSetId
export def "users-applicationsets PutUserApplicationSetByApplicationSetId" [
  applicationSetId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --If-Match: string # Input the value acquired in the ETag header for concurrency control. Please note this is optional (and not recommended for most types of integrations).
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/applicationsets/($applicationSetId)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept, "If-Match": $If_Match} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Workflows initiated by the user
#
# GET /users/initiatedworkflows
# operationId: GetInitiatedWorkflowsByBearerToken
export def "users-initiatedworkflows GetInitiatedWorkflowsByBearerToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/users/initiatedworkflows" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of all the integrations for the logon user
#
# GET /users/integrations
# operationId: GetIntegrationsByBearerToken
export def "users-integrations GetIntegrationsByBearerToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/integrations")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of applicationsets for a user
#
# GET /users/{userId}/applicationsets
# operationId: GetApplicationsetsByUserId
export def "users-applicationsets GetApplicationsetsByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($userId)/applicationsets" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an applicationset for a user
#
# POST /users/{userId}/applicationsets
# operationId: PostApplicationSetByUserId
export def "users-applicationsets PostApplicationSetByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($userId)/applicationsets")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json;version=2018-01-01" $body
}

# Get Yourcampus URL for a user
#
# GET /users/{userId}/yourcampusurl
# operationId: GetYourcampusUrlByUserId
export def "users-yourcampusurl GetYourcampusUrlByUserId" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($userId)/yourcampusurl")
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Wage projection Collective labor agreements defaults
#
# GET /wageprojection/cladata/{collectiveLaborAgreementId}
# operationId: GetClaDataForWageProjectionByCollectiveLaborAgreementId
export def "wageprojection-cladata GetClaDataForWageProjectionByCollectiveLaborAgreementId" [
  collectiveLaborAgreementId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/wageprojection/cladata/($collectiveLaborAgreementId)" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List of collective labor agreements (CLA)
#
# GET /wageprojection/collectivelaboragreements
# operationId: GetCollectiveLaborAgreementsByUserId
export def "wageprojection-collectivelaboragreements GetCollectiveLaborAgreementsByUserId" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --filter: string # Filter the collection (e.g. cancellationPeriodTimeUnit.key eq 4)
  --orderBy: string # Order the collection on one or more fields (e.g. -companyName,address.houseNumber)
  --pageNumber: int # Specifies which page should be returned (format: int32, default: 1, e.g. 2)
  --pageSize: int # Specifies the number of objects per page (format: int32, default: 250, e.g. 20)
  --Authorization: string # A valid Bearer token for authorizing the request. (e.g. Bearer <TOKEN>)
  --Accept: string # The accept header is used to influence what type of output is returned and in most cases the version of the output (e.g. application/json;version=2018-01-01)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageNumber" $pageNumber "scalar") (serialize-qp "pageSize" $pageSize "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/wageprojection/collectivelaboragreements" $qp)
  let extra_headers = {"Authorization": $Authorization, "Accept": $Accept} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json;version=2018-01-01"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}
