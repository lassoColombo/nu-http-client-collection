# Auto-generated client for Generated API v1.0
# Source: https://www.streak.com/api/openapi.json
# Auth: --token flag or $env.GENERATED_API_TOKEN

const BASE_URL = "http://localhost"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o GENERATED_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($name)[($in.k)]=($in.v)" }) }
  if not $is_list { return [$"($name)=($value)"] }
  match $style {
    "multi" => { $value | each {|v| $"($name)=($v)" } }
    "csv" => { let joined = ($value | each { $in | into string } | str join ","); [$"($name)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string } | str join "%20"); [$"($name)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string } | str join "\t"); [$"($name)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string } | str join "|"); [$"($name)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($name)[]=($v)" } }
    _ => { $value | each {|v| $"($name)=($v)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def bool-completer [] { ["'true'" "'false'"] }
def base-url-completer [] { ["http://localhost"] }
def auth-scheme-completer [] { ["bearer"] }


# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "billing-backfill-pro-plus-trial-end post" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# Enqueue Pro+ trial-end metadata backfill
#
# POST /billingBackfill/proPlusTrialEnd
export def "billing-backfill-pro-plus-trial-end post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dryRun: string@bool-completer # default: false
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "dryRun" $dryRun "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/billingBackfill/proPlusTrialEnd" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get current user
#
# GET /v1/users/me
export def "users-me get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<email: string, creationTimestamp: int, lastUpdatedTimestamp: int, lastSavedTimestamp: int, lastSeenTimestamp: int, googleProfileId: string, googleProfilePhotoUrl: string, googleProfileLink: string, googleProfileFullName: string, googleProfileFirstName: string, googleProfileLastName: string, timezoneId: string, userType: string, securityReportOnTeamsPipelines: bool, externalSharingRestrictionOnTeamsPipelines: bool, automaticallySendInvoiceEmails: bool, emailsToSendInvoiceTo: list<string>, customerSpecifiedInvoiceData: string, firstOauthTimestamp: int, wantsTaskDigestEmail: bool, displayName: string, key: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/v1/users/me")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update user by key
#
# POST /v1/users/{userKey}
export def "users post" [
  userKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --automaticallySendInvoiceEmails: string@bool-completer # Whether invoice emails should be sent automatically. (nullable)
  --emailsToSendInvoiceTo: list # Who should receive invoices (nullable)
  --customerSpecifiedInvoiceData: string # Custom data that should be shown on the invoice (nullable)
]: any -> record<email: string, creationTimestamp: int, lastUpdatedTimestamp: int, lastSavedTimestamp: int, lastSeenTimestamp: int, googleProfileId: string, googleProfilePhotoUrl: string, googleProfileLink: string, googleProfileFullName: string, googleProfileFirstName: string, googleProfileLastName: string, timezoneId: string, userType: string, securityReportOnTeamsPipelines: bool, externalSharingRestrictionOnTeamsPipelines: bool, automaticallySendInvoiceEmails: bool, emailsToSendInvoiceTo: list<string>, customerSpecifiedInvoiceData: string, firstOauthTimestamp: int, wantsTaskDigestEmail: bool, displayName: string, key: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v1/users/($userKey)")
  let body = {automaticallySendInvoiceEmails: $automaticallySendInvoiceEmails, emailsToSendInvoiceTo: $emailsToSendInvoiceTo, customerSpecifiedInvoiceData: $customerSpecifiedInvoiceData} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get user by key
#
# GET /v1/users/{userKey}
export def "users get" [
  userKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<email: string, creationTimestamp: int, lastUpdatedTimestamp: int, lastSavedTimestamp: int, lastSeenTimestamp: int, googleProfileId: string, googleProfilePhotoUrl: string, googleProfileLink: string, googleProfileFullName: string, googleProfileFirstName: string, googleProfileLastName: string, timezoneId: string, userType: string, securityReportOnTeamsPipelines: bool, externalSharingRestrictionOnTeamsPipelines: bool, automaticallySendInvoiceEmails: bool, emailsToSendInvoiceTo: list<string>, customerSpecifiedInvoiceData: string, firstOauthTimestamp: int, wantsTaskDigestEmail: bool, displayName: string, key: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v1/users/($userKey)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List comments
#
# GET /v2/boxes/{boxKey}/comments
export def "boxes-comments get" [
  boxKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<hasNextPage: bool, results: table<key: string, commentKey: string, boxKey: string, pipelineKey: string, creatorKey: string, newsfeedEntryKey: string, timestamp: int, message: string, mentions: list, parent: string, children: list, reactionSummary: list, lastSavedTimestamp: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v2/boxes/($boxKey)/comments")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create comment
#
# POST /v2/boxes/{boxKey}/comments
# --mentions item shape: {userKey: string, offset?: int, length?: int}
export def "boxes-comments post" [
  boxKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  message: string # Comment text.
  --mentions: list # Mention ranges in the comment text. (nullable) — item shape: {userKey: string, offset?: int, length?: int}
  --parent: string # Parent comment key when creating a reply. (nullable)
  --searchForAtMentions: string@bool-completer # Whether to scan the comment text for unstructured @mentions of users display name, first name or email address. (nullable)
]: any -> record<key: string, commentKey: string, boxKey: string, pipelineKey: string, creatorKey: string, newsfeedEntryKey: string, timestamp: int, message: string, mentions: table<userKey: string, offset: int, length: int>, parent: string, children: list<any>, reactionSummary: table<emoji: string, count: int, users: list>, lastSavedTimestamp: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v2/boxes/($boxKey)/comments")
  let body = {message: $message, mentions: $mentions, parent: $parent, searchForAtMentions: $searchForAtMentions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Update comment
#
# POST /v2/comments/{commentKey}
# --mentions item shape: {userKey: string, offset?: int, length?: int}
export def "comments post" [
  commentKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  message: string # New comment text.
  --mentions: list # New mention ranges for the comment text. Omit this field to keep existing mentions unchanged. (nullable) — item shape: {userKey: string, offset?: int, length?: int}
]: any -> record<key: string, commentKey: string, boxKey: string, pipelineKey: string, creatorKey: string, newsfeedEntryKey: string, timestamp: int, message: string, mentions: table<userKey: string, offset: int, length: int>, parent: string, children: list<any>, reactionSummary: table<emoji: string, count: int, users: list>, lastSavedTimestamp: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v2/comments/($commentKey)")
  let body = {message: $message, mentions: $mentions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get comment
#
# GET /v2/comments/{commentKey}
export def "comments get" [
  commentKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<key: string, commentKey: string, boxKey: string, pipelineKey: string, creatorKey: string, newsfeedEntryKey: string, timestamp: int, message: string, mentions: table<userKey: string, offset: int, length: int>, parent: string, children: list<any>, reactionSummary: table<emoji: string, count: int, users: list>, lastSavedTimestamp: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v2/comments/($commentKey)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete comment
#
# DELETE /v2/comments/{commentKey}
export def "comments delete" [
  commentKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<success: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v2/comments/($commentKey)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# React to comment
#
# POST /v2/comments/{commentKey}/react
export def "comments-react post" [
  commentKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  emoji: string # Emoji to add or remove as a reaction.
]: any -> record<key: string, commentKey: string, boxKey: string, pipelineKey: string, creatorKey: string, newsfeedEntryKey: string, timestamp: int, message: string, mentions: table<userKey: string, offset: int, length: int>, parent: string, children: list<any>, reactionSummary: table<emoji: string, count: int, users: list>, lastSavedTimestamp: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v2/comments/($commentKey)/react")
  let body = {emoji: $emoji} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Remove comment reaction
#
# POST /v2/comments/{commentKey}/unreact
export def "comments-unreact post" [
  commentKey: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  emoji: string # Emoji to add or remove as a reaction.
]: any -> record<key: string, commentKey: string, boxKey: string, pipelineKey: string, creatorKey: string, newsfeedEntryKey: string, timestamp: int, message: string, mentions: table<userKey: string, offset: int, length: int>, parent: string, children: list<any>, reactionSummary: table<emoji: string, count: int, users: list>, lastSavedTimestamp: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/v2/comments/($commentKey)/unreact")
  let body = {emoji: $emoji} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}
