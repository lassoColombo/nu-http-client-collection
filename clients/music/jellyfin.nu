# Auto-generated client for Jellyfin API v10.11.11
# Source: https://api.jellyfin.org/openapi/jellyfin-openapi-stable.json
# Auth: --token flag or $env.JELLYFIN_API_TOKEN

const BASE_URL = "http://localhost"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o JELLYFIN_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "bearer" => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, dry_run: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  if $dry_run { return {method: $method, url: $req_url, headers: $auth.headers, query_string: $auth.query, content_type: $ct, timeout: $timeout, body: $body} }
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def base-url-completer [] { ["http://localhost"] }
def auth-scheme-completer [] { ["bearer"] }

# Completers for enum parameters
def accept-completer [] { ["application/json" "application/json; profile="CamelCase"" "application/json; profile="PascalCase""] }
def subtitleMethod-completer [] { ["Drop" "Embed" "Encode" "External" "Hls"] }
def context-completer [] { ["Static" "Streaming"] }
def accept-completer-1 [] { ["application/json" "application/json; profile="CamelCase"" "application/json; profile="PascalCase"" "text/css"] }
def ImageSavingConvention-completer [] { ["Compatible" "Legacy"] }
def ChapterImageResolution-completer [] { ["MatchSource" "P1080" "P144" "P1440" "P2160" "P240" "P360" "P480" "P720"] }
def accept-completer-2 [] { ["application/x-javascript" "text/html"] }
def ScrollDirection-completer [] { ["Horizontal" "Vertical"] }
def SortOrder-completer [] { ["Ascending" "Descending"] }
def format-completer [] { ["Bmp" "Gif" "Jpg" "Png" "Svg" "Webp"] }
def metadataRefreshMode-completer [] { ["Default" "FullRefresh" "None" "ValidationOnly"] }
def imageRefreshMode-completer [] { ["Default" "FullRefresh" "None" "ValidationOnly"] }
def ExtraType-completer [] { ["BehindTheScenes" "Clip" "DeletedScene" "Featurette" "Interview" "Sample" "Scene" "Short" "ThemeSong" "ThemeVideo" "Trailer" "Unknown"] }
def Video3DFormat-completer [] { ["FullSideBySide" "FullTopAndBottom" "HalfSideBySide" "HalfTopAndBottom" "MVC"] }
def PlayAccess-completer [] { ["Full" "None"] }
def Type-completer [] { ["AggregateFolder" "Audio" "AudioBook" "BasePluginFolder" "Book" "BoxSet" "Channel" "ChannelFolderItem" "CollectionFolder" "Episode" "Folder" "Genre" "LiveTvChannel" "LiveTvProgram" "ManualPlaylistsFolder" "Movie" "MusicAlbum" "MusicArtist" "MusicGenre" "MusicVideo" "Person" "Photo" "PhotoAlbum" "Playlist" "PlaylistsFolder" "Program" "Recording" "Season" "Series" "Studio" "Trailer" "TvChannel" "TvProgram" "UserRootFolder" "UserView" "Video" "Year"] }
def CollectionType-completer [] { ["books" "boxsets" "folders" "homevideos" "livetv" "movies" "music" "musicvideos" "photos" "playlists" "trailers" "tvshows" "unknown"] }
def VideoType-completer [] { ["BluRay" "Dvd" "Iso" "VideoFile"] }
def LocationType-completer [] { ["FileSystem" "Offline" "Remote" "Virtual"] }
def IsoType-completer [] { ["BluRay" "Dvd"] }
def MediaType-completer [] { ["Audio" "Book" "Photo" "Unknown" "Video"] }
def ImageOrientation-completer [] { ["BottomLeft" "BottomRight" "LeftBottom" "LeftTop" "RightBottom" "RightTop" "TopLeft" "TopRight"] }
def ChannelType-completer [] { ["Radio" "TV"] }
def Audio-completer [] { ["Atmos" "Dolby" "DolbyDigital" "Mono" "Stereo" "Thx"] }
def accept-completer-3 [] { ["audio/*" "video/*"] }
def libraryContentType-completer [] { ["books" "boxsets" "folders" "homevideos" "livetv" "movies" "music" "musicvideos" "photos" "playlists" "trailers" "tvshows" "unknown"] }
def collectionType-completer [] { ["books" "boxsets" "homevideos" "mixed" "movies" "music" "musicvideos" "tvshows"] }
def type-completer [] { ["Radio" "TV"] }
def sortOrder-completer [] { ["Ascending" "Descending"] }
def status-completer [] { ["Cancelled" "Completed" "ConflictedNotOk" "ConflictedOk" "Error" "InProgress" "New"] }
def KeepUntil-completer [] { ["UntilDate" "UntilDeleted" "UntilSpaceNeeded" "UntilWatched"] }
def DayPattern-completer [] { ["Daily" "Weekdays" "Weekends"] }
def Status-completer [] { ["Cancelled" "Completed" "ConflictedNotOk" "ConflictedOk" "Error" "InProgress" "New"] }
def mediaType-completer [] { ["Audio" "Book" "Photo" "Unknown" "Video"] }
def playMethod-completer [] { ["DirectPlay" "DirectStream" "Transcode"] }
def repeatMode-completer [] { ["RepeatAll" "RepeatNone" "RepeatOne"] }
def PlayMethod-completer [] { ["DirectPlay" "DirectStream" "Transcode"] }
def RepeatMode-completer [] { ["RepeatAll" "RepeatNone" "RepeatOne"] }
def PlaybackOrder-completer [] { ["Default" "Shuffle"] }
def type-completer-1 [] { ["Art" "Backdrop" "Banner" "Box" "BoxRear" "Chapter" "Disc" "Logo" "Menu" "Primary" "Profile" "Screenshot" "Thumb"] }
def Name-completer [] { ["Back" "ChannelDown" "ChannelUp" "DisplayContent" "DisplayMessage" "GoHome" "GoToSearch" "GoToSettings" "Guide" "MoveDown" "MoveLeft" "MoveRight" "MoveUp" "Mute" "NextLetter" "PageDown" "PageUp" "Play" "PlayMediaSource" "PlayNext" "PlayState" "PlayTrailers" "PreviousLetter" "Select" "SendKey" "SendString" "SetAudioStreamIndex" "SetMaxStreamingBitrate" "SetPlaybackOrder" "SetRepeatMode" "SetShuffleQueue" "SetSubtitleStreamIndex" "SetVolume" "TakeScreenshot" "ToggleContextMenu" "ToggleFullscreen" "ToggleMute" "ToggleOsd" "ToggleOsdMenu" "ToggleStats" "Unmute" "VolumeDown" "VolumeUp"] }
def playCommand-completer [] { ["PlayInstantMix" "PlayLast" "PlayNext" "PlayNow" "PlayShuffle"] }
def itemType-completer [] { ["AggregateFolder" "Audio" "AudioBook" "BasePluginFolder" "Book" "BoxSet" "Channel" "ChannelFolderItem" "CollectionFolder" "Episode" "Folder" "Genre" "LiveTvChannel" "LiveTvProgram" "ManualPlaylistsFolder" "Movie" "MusicAlbum" "MusicArtist" "MusicGenre" "MusicVideo" "Person" "Photo" "PhotoAlbum" "Playlist" "PlaylistsFolder" "Program" "Recording" "Season" "Series" "Studio" "Trailer" "TvChannel" "TvProgram" "UserRootFolder" "UserView" "Video" "Year"] }
def Mode-completer [] { ["Queue" "QueueNext"] }
def Mode-completer-1 [] { ["RepeatAll" "RepeatNone" "RepeatOne"] }
def Mode-completer-2 [] { ["Shuffle" "Sorted"] }
def sortBy-completer [] { ["AirTime" "AiredEpisodeOrder" "Album" "AlbumArtist" "Artist" "CommunityRating" "CriticRating" "DateCreated" "DateLastContentAdded" "DatePlayed" "Default" "IndexNumber" "IsFavoriteOrLiked" "IsFolder" "IsPlayed" "IsUnplayed" "Name" "OfficialRating" "ParentIndexNumber" "PlayCount" "PremiereDate" "ProductionYear" "Random" "Runtime" "SeriesDatePlayed" "SeriesSortName" "SortName" "StartDate" "Studio" "VideoBitRate"] }
def transcodingProtocol-completer [] { ["hls" "http"] }
def SyncPlayAccess-completer [] { ["CreateAndJoinGroups" "JoinGroups" "None"] }
def SubtitleMode-completer [] { ["Always" "Default" "None" "OnlyForced" "Smart"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "dry-run" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "system-activity-log-entries GetLogEntries" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# Gets activity log entries.
#
# GET /System/ActivityLog/Entries
# operationId: GetLogEntries
export def "system-activity-log-entries GetLogEntries" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --minDate: string # Optional. The minimum date. Format = ISO. (format: date-time)
  --hasUserId: oneof<nothing, bool> # Optional. Filter log entries if it has user id, or not.
]: nothing -> record<Items: table<Id: int, Name: string, Overview: string, ShortOverview: string, Type: string, ItemId: string, Date: string, UserId: string, UserPrimaryImageTag: string, Severity: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "minDate" $minDate "scalar") (serialize-qp "hasUserId" $hasUserId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/System/ActivityLog/Entries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all keys.
#
# GET /Auth/Keys
# operationId: GetKeys
export def "auth-keys GetKeys" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Items: table<Id: int, AccessToken: string, DeviceId: string, AppName: string, AppVersion: string, DeviceName: string, UserId: string, IsActive: bool, DateCreated: string, DateRevoked: string, DateLastActivity: string, UserName: string>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Auth/Keys")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new api key.
#
# POST /Auth/Keys
# operationId: CreateKey
export def "auth-keys CreateKey" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --app: string # Name of the app using the authentication key.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "app" $app "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Auth/Keys" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove an api key.
#
# DELETE /Auth/Keys/{key}
# operationId: RevokeKey
export def "auth-keys RevokeKey" [
  key: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Auth/Keys/($key)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all artists from a given item, folder, or the entire library.
#
# GET /Artists
# operationId: GetArtists
export def "artists GetArtists" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --minCommunityRating: float # Optional filter by minimum community rating. (format: double)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --searchTerm: string # Optional. Search term.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --excludeItemTypes: list # Optional. If specified, results will be filtered out based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --filters: list # Optional. Specify additional filters to apply.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not.
  --mediaTypes: list # Optional filter by MediaType. Allows multiple, comma delimited.
  --genres: list # Optional. If specified, results will be filtered based on genre. This allows multiple, pipe delimited.
  --genreIds: list # Optional. If specified, results will be filtered based on genre id. This allows multiple, pipe delimited.
  --officialRatings: list # Optional. If specified, results will be filtered based on OfficialRating. This allows multiple, pipe delimited.
  --tags: list # Optional. If specified, results will be filtered based on tag. This allows multiple, pipe delimited.
  --years: list # Optional. If specified, results will be filtered based on production year. This allows multiple, comma delimited.
  --enableUserData: oneof<nothing, bool> # Optional, include user data.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --person: string # Optional. If specified, results will be filtered to include only those containing the specified person.
  --personIds: list # Optional. If specified, results will be filtered to include only those containing the specified person ids.
  --personTypes: list # Optional. If specified, along with Person, results will be filtered to include only those containing the specified person and PersonType. Allows multiple, comma-delimited.
  --studios: list # Optional. If specified, results will be filtered based on studio. This allows multiple, pipe delimited.
  --studioIds: list # Optional. If specified, results will be filtered based on studio id. This allows multiple, pipe delimited.
  --userId: string # User id. (format: uuid)
  --nameStartsWithOrGreater: string # Optional filter by items whose name is sorted equally or greater than a given input string.
  --nameStartsWith: string # Optional filter by items whose name is sorted equally than a given input string.
  --nameLessThan: string # Optional filter by items whose name is equally or lesser than a given input string.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited.
  --sortOrder: list # Sort Order - Ascending,Descending.
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
  --enableTotalRecordCount: oneof<nothing, bool> # Total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "minCommunityRating" $minCommunityRating "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "filters" $filters "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "mediaTypes" $mediaTypes "multi") (serialize-qp "genres" $genres "multi") (serialize-qp "genreIds" $genreIds "multi") (serialize-qp "officialRatings" $officialRatings "multi") (serialize-qp "tags" $tags "multi") (serialize-qp "years" $years "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "person" $person "scalar") (serialize-qp "personIds" $personIds "multi") (serialize-qp "personTypes" $personTypes "multi") (serialize-qp "studios" $studios "multi") (serialize-qp "studioIds" $studioIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "nameStartsWithOrGreater" $nameStartsWithOrGreater "scalar") (serialize-qp "nameStartsWith" $nameStartsWith "scalar") (serialize-qp "nameLessThan" $nameLessThan "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Artists" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an artist by name.
#
# GET /Artists/{name}
# operationId: GetArtistByName
export def "artists GetArtistByName" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Artists/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all album artists from a given item, folder, or the entire library.
#
# GET /Artists/AlbumArtists
# operationId: GetAlbumArtists
export def "artists-album-artists GetAlbumArtists" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --minCommunityRating: float # Optional filter by minimum community rating. (format: double)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --searchTerm: string # Optional. Search term.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --excludeItemTypes: list # Optional. If specified, results will be filtered out based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --filters: list # Optional. Specify additional filters to apply.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not.
  --mediaTypes: list # Optional filter by MediaType. Allows multiple, comma delimited.
  --genres: list # Optional. If specified, results will be filtered based on genre. This allows multiple, pipe delimited.
  --genreIds: list # Optional. If specified, results will be filtered based on genre id. This allows multiple, pipe delimited.
  --officialRatings: list # Optional. If specified, results will be filtered based on OfficialRating. This allows multiple, pipe delimited.
  --tags: list # Optional. If specified, results will be filtered based on tag. This allows multiple, pipe delimited.
  --years: list # Optional. If specified, results will be filtered based on production year. This allows multiple, comma delimited.
  --enableUserData: oneof<nothing, bool> # Optional, include user data.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --person: string # Optional. If specified, results will be filtered to include only those containing the specified person.
  --personIds: list # Optional. If specified, results will be filtered to include only those containing the specified person ids.
  --personTypes: list # Optional. If specified, along with Person, results will be filtered to include only those containing the specified person and PersonType. Allows multiple, comma-delimited.
  --studios: list # Optional. If specified, results will be filtered based on studio. This allows multiple, pipe delimited.
  --studioIds: list # Optional. If specified, results will be filtered based on studio id. This allows multiple, pipe delimited.
  --userId: string # User id. (format: uuid)
  --nameStartsWithOrGreater: string # Optional filter by items whose name is sorted equally or greater than a given input string.
  --nameStartsWith: string # Optional filter by items whose name is sorted equally than a given input string.
  --nameLessThan: string # Optional filter by items whose name is equally or lesser than a given input string.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited.
  --sortOrder: list # Sort Order - Ascending,Descending.
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
  --enableTotalRecordCount: oneof<nothing, bool> # Total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "minCommunityRating" $minCommunityRating "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "filters" $filters "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "mediaTypes" $mediaTypes "multi") (serialize-qp "genres" $genres "multi") (serialize-qp "genreIds" $genreIds "multi") (serialize-qp "officialRatings" $officialRatings "multi") (serialize-qp "tags" $tags "multi") (serialize-qp "years" $years "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "person" $person "scalar") (serialize-qp "personIds" $personIds "multi") (serialize-qp "personTypes" $personTypes "multi") (serialize-qp "studios" $studios "multi") (serialize-qp "studioIds" $studioIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "nameStartsWithOrGreater" $nameStartsWithOrGreater "scalar") (serialize-qp "nameStartsWith" $nameStartsWith "scalar") (serialize-qp "nameLessThan" $nameLessThan "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Artists/AlbumArtists" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio stream.
#
# GET /Audio/{itemId}/stream
# operationId: GetAudioStream
@deprecated --flag deviceProfileId
export def "audio-stream GetAudioStream" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --container: string # The audio container.
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "container" $container "scalar") (serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/stream" $qp)
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio stream.
#
# HEAD /Audio/{itemId}/stream
# operationId: HeadAudioStream
@deprecated --flag deviceProfileId
export def "audio-stream HeadAudioStream" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --container: string # The audio container.
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "container" $container "scalar") (serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/stream" $qp)
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio stream.
#
# GET /Audio/{itemId}/stream.{container}
# operationId: GetAudioStreamByContainer
@deprecated --flag deviceProfileId
export def "audio-stream-container GetAudioStreamByContainer" [
  itemId: string
  container: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/stream.($container)" $qp)
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio stream.
#
# HEAD /Audio/{itemId}/stream.{container}
# operationId: HeadAudioStreamByContainer
@deprecated --flag deviceProfileId
export def "audio-stream-container HeadAudioStreamByContainer" [
  itemId: string
  container: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/stream.($container)" $qp)
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of all currently present backups in the backup directory.
#
# GET /Backup
# operationId: ListBackups
export def "backup ListBackups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<ServerVersion: string, BackupEngineVersion: string, DateCreated: string, Path: string, Options: record<Metadata: bool, Trickplay: bool, Subtitles: bool, Database: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Backup")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates a new Backup.
#
# POST /Backup/Create
# operationId: CreateBackup
export def "backup-create CreateBackup" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Metadata: oneof<nothing, bool> # Gets or sets a value indicating whether the archive contains the Metadata contents.
  --Trickplay: oneof<nothing, bool> # Gets or sets a value indicating whether the archive contains the Trickplay contents.
  --Subtitles: oneof<nothing, bool> # Gets or sets a value indicating whether the archive contains the Subtitle contents.
  --Database: oneof<nothing, bool> # Gets or sets a value indicating whether the archive contains the Database contents.
]: any -> record<ServerVersion: string, BackupEngineVersion: string, DateCreated: string, Path: string, Options: record<Metadata: bool, Trickplay: bool, Subtitles: bool, Database: bool>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Backup/Create")
  let body = {Metadata: $Metadata, Trickplay: $Trickplay, Subtitles: $Subtitles, Database: $Database} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets the descriptor from an existing archive is present.
#
# GET /Backup/Manifest
# operationId: GetBackup
export def "backup-manifest GetBackup" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The data to start a restore process.
]: nothing -> record<ServerVersion: string, BackupEngineVersion: string, DateCreated: string, Path: string, Options: record<Metadata: bool, Trickplay: bool, Subtitles: bool, Database: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Backup/Manifest" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Restores to a backup by restarting the server and applying the backup.
#
# POST /Backup/Restore
# operationId: StartRestoreBackup
export def "backup-restore StartRestoreBackup" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ArchiveFileName: string # Gets or Sets the name of the backup archive to restore from. Must be present in MediaBrowser.Common.Configuration.IApplicationPaths.BackupPath.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Backup/Restore")
  let body = {ArchiveFileName: $ArchiveFileName} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets branding configuration.
#
# GET /Branding/Configuration
# operationId: GetBrandingOptions
export def "branding-configuration GetBrandingOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<LoginDisclaimer: string, CustomCss: string, SplashscreenEnabled: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Branding/Configuration")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets branding css.
#
# GET /Branding/Css
# operationId: GetBrandingCss
export def "branding-css GetBrandingCss" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Branding/Css")
  let accept_val = ($accept | default "text/css")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets branding css.
#
# GET /Branding/Css.css
# operationId: GetBrandingCss_2
export def "branding-csscss GetBrandingCss-by-" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-1 # Response content type
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Branding/Css.css")
  let accept_val = ($accept | default "text/css")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available channels.
#
# GET /Channels
# operationId: GetChannels
export def "channels GetChannels" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User Id to filter by. Use System.Guid.Empty to not filter by user. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --supportsLatestItems: oneof<nothing, bool> # Optional. Filter by channels that support getting latest items.
  --supportsMediaDeletion: oneof<nothing, bool> # Optional. Filter by channels that support media deletion.
  --isFavorite: oneof<nothing, bool> # Optional. Filter by channels that are favorite.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "supportsLatestItems" $supportsLatestItems "scalar") (serialize-qp "supportsMediaDeletion" $supportsMediaDeletion "scalar") (serialize-qp "isFavorite" $isFavorite "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Channels" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get channel features.
#
# GET /Channels/{channelId}/Features
# operationId: GetChannelFeatures
export def "channels-features GetChannelFeatures" [
  channelId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Name: string, Id: string, CanSearch: bool, MediaTypes: list<string>, ContentTypes: list<string>, MaxPageSize: int, AutoRefreshLevels: int, DefaultSortFields: list<string>, SupportsSortOrderToggle: bool, SupportsLatestMedia: bool, CanFilter: bool, SupportsContentDownloading: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Channels/($channelId)/Features")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get channel items.
#
# GET /Channels/{channelId}/Items
# operationId: GetChannelItems
export def "channels-items GetChannelItems" [
  channelId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --folderId: string # Optional. Folder Id. (format: uuid)
  --userId: string # Optional. User Id. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --sortOrder: list # Optional. Sort Order - Ascending,Descending.
  --filters: list # Optional. Specify additional filters to apply.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "folderId" $folderId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "filters" $filters "multi") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "fields" $qp_fields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Channels/($channelId)/Items" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all channel features.
#
# GET /Channels/Features
# operationId: GetAllChannelFeatures
export def "channels-features GetAllChannelFeatures" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Id: string, CanSearch: bool, MediaTypes: list<string>, ContentTypes: list<string>, MaxPageSize: int, AutoRefreshLevels: int, DefaultSortFields: list<string>, SupportsSortOrderToggle: bool, SupportsLatestMedia: bool, CanFilter: bool, SupportsContentDownloading: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Channels/Features")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets latest channel items.
#
# GET /Channels/Items/Latest
# operationId: GetLatestChannelItems
export def "channels-items-latest GetLatestChannelItems" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. User Id. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --filters: list # Optional. Specify additional filters to apply.
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --channelIds: list # Optional. Specify one or more channel id's, comma delimited.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "filters" $filters "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "channelIds" $channelIds "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/Channels/Items/Latest" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload a document.
#
# POST /ClientLog/Document
# operationId: LogFile
export def "client-log-document LogFile" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --body: record
]: any -> record<FileName: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ClientLog/Document")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "text/plain" $body
}

# Creates a new collection.
#
# POST /Collections
# operationId: CreateCollection
export def "collections CreateCollection" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --name: string # The name of the collection.
  --ids: list # Item Ids to add to the collection.
  --parentId: string # Optional. Create the collection within a specific folder. (format: uuid)
  --isLocked: oneof<nothing, bool> # Whether or not to lock the new collection. (default: false)
]: nothing -> record<Id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "ids" $ids "multi") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "isLocked" $isLocked "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Collections" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Adds items to a collection.
#
# POST /Collections/{collectionId}/Items
# operationId: AddToCollection
export def "collections-items AddToCollection" [
  collectionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ids: list # Item ids, comma delimited.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ids" $ids "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Collections/($collectionId)/Items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Removes items from a collection.
#
# DELETE /Collections/{collectionId}/Items
# operationId: RemoveFromCollection
export def "collections-items RemoveFromCollection" [
  collectionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ids: list # Item ids, comma delimited.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ids" $ids "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Collections/($collectionId)/Items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets application configuration.
#
# GET /System/Configuration
# operationId: GetConfiguration
export def "system-configuration GetConfiguration" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<LogFileRetentionDays: int, IsStartupWizardCompleted: bool, CachePath: string, PreviousVersion: string, PreviousVersionStr: string, EnableMetrics: bool, EnableNormalizedItemByNameIds: bool, IsPortAuthorized: bool, QuickConnectAvailable: bool, EnableCaseSensitiveItemIds: bool, DisableLiveTvChannelUserDataName: bool, MetadataPath: string, PreferredMetadataLanguage: string, MetadataCountryCode: string, SortReplaceCharacters: list<string>, SortRemoveCharacters: list<string>, SortRemoveWords: list<string>, MinResumePct: int, MaxResumePct: int, MinResumeDurationSeconds: int, MinAudiobookResume: int, MaxAudiobookResume: int, InactiveSessionThreshold: int, LibraryMonitorDelay: int, LibraryUpdateDuration: int, CacheSize: int, ImageSavingConvention: record, MetadataOptions: table<ItemType: string, DisabledMetadataSavers: list, LocalMetadataReaderOrder: list, DisabledMetadataFetchers: list, MetadataFetcherOrder: list, DisabledImageFetchers: list, ImageFetcherOrder: list>, SkipDeserializationForBasicTypes: bool, ServerName: string, UICulture: string, SaveMetadataHidden: bool, ContentTypes: table<Name: string, Value: string>, RemoteClientBitrateLimit: int, EnableFolderView: bool, EnableGroupingMoviesIntoCollections: bool, EnableGroupingShowsIntoCollections: bool, DisplaySpecialsWithinSeasons: bool, CodecsUsed: list<string>, PluginRepositories: table<Name: string, Url: string, Enabled: bool>, EnableExternalContentInSuggestions: bool, ImageExtractionTimeoutMs: int, PathSubstitutions: table<From: string, To: string>, EnableSlowResponseWarning: bool, SlowResponseThresholdMs: int, CorsHosts: list<string>, ActivityLogRetentionDays: int, LibraryScanFanoutConcurrency: int, LibraryMetadataRefreshConcurrency: int, AllowClientLogUpload: bool, DummyChapterDuration: int, ChapterImageResolution: record, ParallelImageEncodingLimit: int, CastReceiverApplications: table<Id: string, Name: string>, TrickplayOptions: record<EnableHwAcceleration: bool, EnableHwEncoding: bool, EnableKeyFrameOnlyExtraction: bool, ScanBehavior: record, ProcessPriority: record, Interval: int, WidthResolutions: list<int>, TileWidth: int, TileHeight: int, Qscale: int, JpegQuality: int, ProcessThreads: int>, EnableLegacyAuthorization: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Configuration")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates application configuration.
#
# POST /System/Configuration
# operationId: UpdateConfiguration
# --MetadataOptions item shape: {ItemType?: string, DisabledMetadataSavers?: list, LocalMetadataReaderOrder?: list, DisabledMetadataFetchers?: list, MetadataFetcherOrder?: list, DisabledImageFetchers?: list, ImageFetcherOrder?: list}
# --ContentTypes item shape: {Name?: string, Value?: string}
# --PluginRepositories item shape: {Name?: string, Url?: string, Enabled?: bool}
# --PathSubstitutions item shape: {From?: string, To?: string}
# --CastReceiverApplications item shape: {Id?: string, Name?: string}
export def "system-configuration UpdateConfiguration" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --LogFileRetentionDays: int # Gets or sets the number of days we should retain log files. (format: int32)
  --IsStartupWizardCompleted: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is first run.
  --CachePath: string # Gets or sets the cache path. (nullable)
  --PreviousVersion: string # Gets or sets the last known version that was ran using the configuration. (nullable)
  --PreviousVersionStr: string # Gets or sets the stringified PreviousVersion to be stored/loaded, because System.Version itself isn't xml-serializable. (nullable)
  --EnableMetrics: oneof<nothing, bool> # Gets or sets a value indicating whether to enable prometheus metrics exporting.
  --EnableNormalizedItemByNameIds: oneof<nothing, bool>
  --IsPortAuthorized: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is port authorized.
  --QuickConnectAvailable: oneof<nothing, bool> # Gets or sets a value indicating whether quick connect is available for use on this server.
  --EnableCaseSensitiveItemIds: oneof<nothing, bool> # Gets or sets a value indicating whether [enable case-sensitive item ids].
  --DisableLiveTvChannelUserDataName: oneof<nothing, bool>
  --MetadataPath: string # Gets or sets the metadata path.
  --PreferredMetadataLanguage: string # Gets or sets the preferred metadata language.
  --MetadataCountryCode: string # Gets or sets the metadata country code.
  --SortReplaceCharacters: list # Gets or sets characters to be replaced with a ' ' in strings to create a sort name.
  --SortRemoveCharacters: list # Gets or sets characters to be removed from strings to create a sort name.
  --SortRemoveWords: list # Gets or sets words to be removed from strings to create a sort name.
  --MinResumePct: int # Gets or sets the minimum percentage of an item that must be played in order for playstate to be updated. (format: int32)
  --MaxResumePct: int # Gets or sets the maximum percentage of an item that can be played while still saving playstate. If this percentage is crossed playstate will be reset to the beginning and the item will be marked watched. (format: int32)
  --MinResumeDurationSeconds: int # Gets or sets the minimum duration that an item must have in order to be eligible for playstate updates.. (format: int32)
  --MinAudiobookResume: int # Gets or sets the minimum minutes of a book that must be played in order for playstate to be updated. (format: int32)
  --MaxAudiobookResume: int # Gets or sets the remaining minutes of a book that can be played while still saving playstate. If this percentage is crossed playstate will be reset to the beginning and the item will be marked watched. (format: int32)
  --InactiveSessionThreshold: int # Gets or sets the threshold in minutes after a inactive session gets closed automatically. If set to 0 the check for inactive sessions gets disabled. (format: int32)
  --LibraryMonitorDelay: int # Gets or sets the delay in seconds that we will wait after a file system change to try and discover what has been added/removed Some delay is necessary with some items because their creation is not atomic.  It involves the creation of several different directories and files. (format: int32)
  --LibraryUpdateDuration: int # Gets or sets the duration in seconds that we will wait after a library updated event before executing the library changed notification. (format: int32)
  --CacheSize: int # Gets or sets the maximum amount of items to cache. (format: int32)
  --ImageSavingConvention: any@ImageSavingConvention-completer # Gets or sets the image saving convention.
  --MetadataOptions: list # item shape: {ItemType?: string, DisabledMetadataSavers?: list, LocalMetadataReaderOrder?: list, DisabledMetadataFetchers?: list, MetadataFetcherOrder?: list, DisabledImageFetchers?: list, ImageFetcherOrder?: list}
  --SkipDeserializationForBasicTypes: oneof<nothing, bool>
  --ServerName: string
  --UICulture: string
  --SaveMetadataHidden: oneof<nothing, bool>
  --ContentTypes: list # item shape: {Name?: string, Value?: string}
  --RemoteClientBitrateLimit: int # format: int32
  --EnableFolderView: oneof<nothing, bool>
  --EnableGroupingMoviesIntoCollections: oneof<nothing, bool>
  --EnableGroupingShowsIntoCollections: oneof<nothing, bool>
  --DisplaySpecialsWithinSeasons: oneof<nothing, bool>
  --CodecsUsed: list
  --PluginRepositories: list # item shape: {Name?: string, Url?: string, Enabled?: bool}
  --EnableExternalContentInSuggestions: oneof<nothing, bool>
  --ImageExtractionTimeoutMs: int # format: int32
  --PathSubstitutions: list # item shape: {From?: string, To?: string}
  --EnableSlowResponseWarning: oneof<nothing, bool> # Gets or sets a value indicating whether slow server responses should be logged as a warning.
  --SlowResponseThresholdMs: int # Gets or sets the threshold for the slow response time warning in ms. (format: int64)
  --CorsHosts: list # Gets or sets the cors hosts.
  --ActivityLogRetentionDays: int # Gets or sets the number of days we should retain activity logs. (nullable, format: int32)
  --LibraryScanFanoutConcurrency: int # Gets or sets the how the library scan fans out. (format: int32)
  --LibraryMetadataRefreshConcurrency: int # Gets or sets the how many metadata refreshes can run concurrently. (format: int32)
  --AllowClientLogUpload: oneof<nothing, bool> # Gets or sets a value indicating whether clients should be allowed to upload logs.
  --DummyChapterDuration: int # Gets or sets the dummy chapter duration in seconds, use 0 (zero) or less to disable generation altogether. (format: int32)
  --ChapterImageResolution: any@ChapterImageResolution-completer # Gets or sets the chapter image resolution.
  --ParallelImageEncodingLimit: int # Gets or sets the limit for parallel image encoding. (format: int32)
  --CastReceiverApplications: list # Gets or sets the list of cast receiver applications. — item shape: {Id?: string, Name?: string}
  --TrickplayOptions: any # Gets or sets the trickplay options.
  --EnableLegacyAuthorization: oneof<nothing, bool> # Gets or sets a value indicating whether old authorization methods are allowed.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Configuration")
  let body = {LogFileRetentionDays: $LogFileRetentionDays, IsStartupWizardCompleted: $IsStartupWizardCompleted, CachePath: $CachePath, PreviousVersion: $PreviousVersion, PreviousVersionStr: $PreviousVersionStr, EnableMetrics: $EnableMetrics, EnableNormalizedItemByNameIds: $EnableNormalizedItemByNameIds, IsPortAuthorized: $IsPortAuthorized, QuickConnectAvailable: $QuickConnectAvailable, EnableCaseSensitiveItemIds: $EnableCaseSensitiveItemIds, DisableLiveTvChannelUserDataName: $DisableLiveTvChannelUserDataName, MetadataPath: $MetadataPath, PreferredMetadataLanguage: $PreferredMetadataLanguage, MetadataCountryCode: $MetadataCountryCode, SortReplaceCharacters: $SortReplaceCharacters, SortRemoveCharacters: $SortRemoveCharacters, SortRemoveWords: $SortRemoveWords, MinResumePct: $MinResumePct, MaxResumePct: $MaxResumePct, MinResumeDurationSeconds: $MinResumeDurationSeconds, MinAudiobookResume: $MinAudiobookResume, MaxAudiobookResume: $MaxAudiobookResume, InactiveSessionThreshold: $InactiveSessionThreshold, LibraryMonitorDelay: $LibraryMonitorDelay, LibraryUpdateDuration: $LibraryUpdateDuration, CacheSize: $CacheSize, ImageSavingConvention: $ImageSavingConvention, MetadataOptions: $MetadataOptions, SkipDeserializationForBasicTypes: $SkipDeserializationForBasicTypes, ServerName: $ServerName, UICulture: $UICulture, SaveMetadataHidden: $SaveMetadataHidden, ContentTypes: $ContentTypes, RemoteClientBitrateLimit: $RemoteClientBitrateLimit, EnableFolderView: $EnableFolderView, EnableGroupingMoviesIntoCollections: $EnableGroupingMoviesIntoCollections, EnableGroupingShowsIntoCollections: $EnableGroupingShowsIntoCollections, DisplaySpecialsWithinSeasons: $DisplaySpecialsWithinSeasons, CodecsUsed: $CodecsUsed, PluginRepositories: $PluginRepositories, EnableExternalContentInSuggestions: $EnableExternalContentInSuggestions, ImageExtractionTimeoutMs: $ImageExtractionTimeoutMs, PathSubstitutions: $PathSubstitutions, EnableSlowResponseWarning: $EnableSlowResponseWarning, SlowResponseThresholdMs: $SlowResponseThresholdMs, CorsHosts: $CorsHosts, ActivityLogRetentionDays: $ActivityLogRetentionDays, LibraryScanFanoutConcurrency: $LibraryScanFanoutConcurrency, LibraryMetadataRefreshConcurrency: $LibraryMetadataRefreshConcurrency, AllowClientLogUpload: $AllowClientLogUpload, DummyChapterDuration: $DummyChapterDuration, ChapterImageResolution: $ChapterImageResolution, ParallelImageEncodingLimit: $ParallelImageEncodingLimit, CastReceiverApplications: $CastReceiverApplications, TrickplayOptions: $TrickplayOptions, EnableLegacyAuthorization: $EnableLegacyAuthorization} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a named configuration.
#
# GET /System/Configuration/{key}
# operationId: GetNamedConfiguration
export def "system-configuration GetNamedConfiguration" [
  key: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/System/Configuration/($key)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates named configuration.
#
# POST /System/Configuration/{key}
# operationId: UpdateNamedConfiguration
export def "system-configuration UpdateNamedConfiguration" [
  key: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/System/Configuration/($key)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Updates branding configuration.
#
# POST /System/Configuration/Branding
# operationId: UpdateBrandingConfiguration
export def "system-configuration-branding UpdateBrandingConfiguration" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --LoginDisclaimer: string # Gets or sets the login disclaimer. (nullable)
  --CustomCss: string # Gets or sets the custom CSS. (nullable)
  --SplashscreenEnabled: oneof<nothing, bool> # Gets or sets a value indicating whether to enable the splashscreen.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Configuration/Branding")
  let body = {LoginDisclaimer: $LoginDisclaimer, CustomCss: $CustomCss, SplashscreenEnabled: $SplashscreenEnabled} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a default MetadataOptions object.
#
# GET /System/Configuration/MetadataOptions/Default
# operationId: GetDefaultMetadataOptions
export def "system-configuration-metadata-options-default GetDefaultMetadataOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<ItemType: string, DisabledMetadataSavers: list<string>, LocalMetadataReaderOrder: list<string>, DisabledMetadataFetchers: list<string>, MetadataFetcherOrder: list<string>, DisabledImageFetchers: list<string>, ImageFetcherOrder: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Configuration/MetadataOptions/Default")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a dashboard configuration page.
#
# GET /web/ConfigurationPage
# operationId: GetDashboardConfigurationPage
export def "web-configuration-page GetDashboardConfigurationPage" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-2 # Response content type
  --name: string # The name of the page.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/web/ConfigurationPage" $qp)
  let accept_val = ($accept | default "text/html")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the configuration pages.
#
# GET /web/ConfigurationPages
# operationId: GetConfigurationPages
export def "web-configuration-pages GetConfigurationPages" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --enableInMainMenu: oneof<nothing, bool> # Whether to enable in the main menu.
]: nothing -> table<Name: string, EnableInMainMenu: bool, MenuSection: string, MenuIcon: string, DisplayName: string, PluginId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "enableInMainMenu" $enableInMainMenu "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/web/ConfigurationPages" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get Devices.
#
# GET /Devices
# operationId: GetDevices
export def "devices GetDevices" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Gets or sets the user identifier. (format: uuid)
]: nothing -> record<Items: table<Name: string, CustomName: string, AccessToken: string, Id: string, LastUserName: string, AppName: string, AppVersion: string, LastUserId: string, DateLastActivity: string, Capabilities: record, IconUrl: string>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Devices" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Deletes a device.
#
# DELETE /Devices
# operationId: DeleteDevice
export def "devices DeleteDevice" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string # Device Id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Devices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get info for a device.
#
# GET /Devices/Info
# operationId: GetDeviceInfo
export def "devices-info GetDeviceInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --id: string # Device Id.
]: nothing -> record<Name: string, CustomName: string, AccessToken: string, Id: string, LastUserName: string, AppName: string, AppVersion: string, LastUserId: string, DateLastActivity: string, Capabilities: record<PlayableMediaTypes: list<string>, SupportedCommands: list<string>, SupportsMediaControl: bool, SupportsPersistentIdentifier: bool, DeviceProfile: record<Name: string, Id: string, MaxStreamingBitrate: int, MaxStaticBitrate: int, MusicStreamingTranscodingBitrate: int, MaxStaticMusicBitrate: int, DirectPlayProfiles: list, TranscodingProfiles: list, ContainerProfiles: list, CodecProfiles: list, SubtitleProfiles: list>, AppStoreUrl: string, IconUrl: string>, IconUrl: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Devices/Info" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get options for a device.
#
# GET /Devices/Options
# operationId: GetDeviceOptions
export def "devices-options GetDeviceOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --id: string # Device Id.
]: nothing -> record<Id: int, DeviceId: string, CustomName: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Devices/Options" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update device options.
#
# POST /Devices/Options
# operationId: UpdateDeviceOptions
export def "devices-options UpdateDeviceOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string # Device Id.
  --Id: int # Gets or sets the id. (format: int32)
  --DeviceId: string # Gets or sets the device id. (nullable)
  --CustomName: string # Gets or sets the custom name. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Devices/Options" $qp)
  let body = {Id: $Id, DeviceId: $DeviceId, CustomName: $CustomName} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get Display Preferences.
#
# GET /DisplayPreferences/{displayPreferencesId}
# operationId: GetDisplayPreferences
export def "display-preferences GetDisplayPreferences" [
  displayPreferencesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
  --client: string # Client.
]: nothing -> record<Id: string, ViewType: string, SortBy: string, IndexBy: string, RememberIndexing: bool, PrimaryImageHeight: int, PrimaryImageWidth: int, CustomPrefs: record, ScrollDirection: record, ShowBackdrop: bool, RememberSorting: bool, SortOrder: record, ShowSidebar: bool, Client: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "client" $client "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/DisplayPreferences/($displayPreferencesId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Display Preferences.
#
# POST /DisplayPreferences/{displayPreferencesId}
# operationId: UpdateDisplayPreferences
export def "display-preferences UpdateDisplayPreferences" [
  displayPreferencesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # User Id. (format: uuid)
  --client: string # Client.
  --Id: string # Gets or sets the user id. (nullable)
  --ViewType: string # Gets or sets the type of the view. (nullable)
  --SortBy: string # Gets or sets the sort by. (nullable)
  --IndexBy: string # Gets or sets the index by. (nullable)
  --RememberIndexing: oneof<nothing, bool> # Gets or sets a value indicating whether [remember indexing].
  --PrimaryImageHeight: int # Gets or sets the height of the primary image. (format: int32)
  --PrimaryImageWidth: int # Gets or sets the width of the primary image. (format: int32)
  --CustomPrefs: record # Gets or sets the custom prefs.
  --ScrollDirection: any@ScrollDirection-completer # An enum representing the axis that should be scrolled.
  --ShowBackdrop: oneof<nothing, bool> # Gets or sets a value indicating whether to show backdrops on this item.
  --RememberSorting: oneof<nothing, bool> # Gets or sets a value indicating whether [remember sorting].
  --SortOrder: any@SortOrder-completer # An enum representing the sorting order.
  --ShowSidebar: oneof<nothing, bool> # Gets or sets a value indicating whether [show sidebar].
  --Client: string # Gets or sets the client. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "client" $client "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/DisplayPreferences/($displayPreferencesId)" $qp)
  let body = {Id: $Id, ViewType: $ViewType, SortBy: $SortBy, IndexBy: $IndexBy, RememberIndexing: $RememberIndexing, PrimaryImageHeight: $PrimaryImageHeight, PrimaryImageWidth: $PrimaryImageWidth, CustomPrefs: $CustomPrefs, ScrollDirection: $ScrollDirection, ShowBackdrop: $ShowBackdrop, RememberSorting: $RememberSorting, SortOrder: $SortOrder, ShowSidebar: $ShowSidebar, Client: $Client} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a video stream using HTTP live streaming.
#
# GET /Audio/{itemId}/hls1/{playlistId}/{segmentId}.{container}
# operationId: GetHlsAudioSegment
@deprecated --flag deviceProfileId
export def "audio-hls1 GetHlsAudioSegment" [
  itemId: string
  playlistId: string
  segmentId: int
  container: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --runtimeTicks: int # The position of the requested segment in ticks. (format: int64)
  --actualSegmentLengthTicks: int # The length of the requested segment in ticks. (format: int64)
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --maxStreamingBitrate: int # Optional. The maximum streaming bitrate. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "runtimeTicks" $runtimeTicks "scalar") (serialize-qp "actualSegmentLengthTicks" $actualSegmentLengthTicks "scalar") (serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/hls1/($playlistId)/($segmentId).($container)" $qp)
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio stream using HTTP live streaming.
#
# GET /Audio/{itemId}/main.m3u8
# operationId: GetVariantHlsAudioPlaylist
@deprecated --flag deviceProfileId
export def "audio-mainm3u8 GetVariantHlsAudioPlaylist" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --maxStreamingBitrate: int # Optional. The maximum streaming bitrate. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/main.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio hls playlist stream.
#
# GET /Audio/{itemId}/master.m3u8
# operationId: GetMasterHlsAudioPlaylist
@deprecated --flag deviceProfileId
export def "audio-masterm3u8 GetMasterHlsAudioPlaylist" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --maxStreamingBitrate: int # Optional. The maximum streaming bitrate. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAdaptiveBitrateStreaming: oneof<nothing, bool> # Enable adaptive bitrate streaming. (default: false)
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAdaptiveBitrateStreaming" $enableAdaptiveBitrateStreaming "scalar") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/master.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio hls playlist stream.
#
# HEAD /Audio/{itemId}/master.m3u8
# operationId: HeadMasterHlsAudioPlaylist
@deprecated --flag deviceProfileId
export def "audio-masterm3u8 HeadMasterHlsAudioPlaylist" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --maxStreamingBitrate: int # Optional. The maximum streaming bitrate. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAdaptiveBitrateStreaming: oneof<nothing, bool> # Enable adaptive bitrate streaming. (default: false)
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAdaptiveBitrateStreaming" $enableAdaptiveBitrateStreaming "scalar") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/master.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video stream using HTTP live streaming.
#
# GET /Videos/{itemId}/hls1/{playlistId}/{segmentId}.{container}
# operationId: GetHlsVideoSegment
@deprecated --flag deviceProfileId
export def "videos-hls1 GetHlsVideoSegment" [
  itemId: string
  playlistId: string
  segmentId: int
  container: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --runtimeTicks: int # The position of the requested segment in ticks. (format: int64)
  --actualSegmentLengthTicks: int # The length of the requested segment in ticks. (format: int64)
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The desired segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
  --alwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Whether to always burn in subtitles when transcoding. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "runtimeTicks" $runtimeTicks "scalar") (serialize-qp "actualSegmentLengthTicks" $actualSegmentLengthTicks "scalar") (serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar") (serialize-qp "alwaysBurnInSubtitleWhenTranscoding" $alwaysBurnInSubtitleWhenTranscoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/hls1/($playlistId)/($segmentId).($container)" $qp)
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a hls live stream.
#
# GET /Videos/{itemId}/live.m3u8
# operationId: GetLiveHlsStream
@deprecated --flag deviceProfileId
export def "videos-livem3u8 GetLiveHlsStream" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --container: string # The audio container.
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --maxWidth: int # Optional. The max width. (format: int32)
  --maxHeight: int # Optional. The max height. (format: int32)
  --enableSubtitlesInManifest: oneof<nothing, bool> # Optional. Whether to enable subtitles in the manifest.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
  --alwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Whether to always burn in subtitles when transcoding. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "container" $container "scalar") (serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "enableSubtitlesInManifest" $enableSubtitlesInManifest "scalar") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar") (serialize-qp "alwaysBurnInSubtitleWhenTranscoding" $alwaysBurnInSubtitleWhenTranscoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/live.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video stream using HTTP live streaming.
#
# GET /Videos/{itemId}/main.m3u8
# operationId: GetVariantHlsVideoPlaylist
@deprecated --flag deviceProfileId
export def "videos-mainm3u8 GetVariantHlsVideoPlaylist" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
  --alwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Whether to always burn in subtitles when transcoding. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar") (serialize-qp "alwaysBurnInSubtitleWhenTranscoding" $alwaysBurnInSubtitleWhenTranscoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/main.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video hls playlist stream.
#
# GET /Videos/{itemId}/master.m3u8
# operationId: GetMasterHlsVideoPlaylist
@deprecated --flag deviceProfileId
export def "videos-masterm3u8 GetMasterHlsVideoPlaylist" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAdaptiveBitrateStreaming: oneof<nothing, bool> # Enable adaptive bitrate streaming. (default: false)
  --enableTrickplay: oneof<nothing, bool> # Enable trickplay image playlists being added to master playlist. (default: true)
  --enableAudioVbrEncoding: oneof<nothing, bool> # Whether to enable Audio Encoding. (default: true)
  --alwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Whether to always burn in subtitles when transcoding. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAdaptiveBitrateStreaming" $enableAdaptiveBitrateStreaming "scalar") (serialize-qp "enableTrickplay" $enableTrickplay "scalar") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar") (serialize-qp "alwaysBurnInSubtitleWhenTranscoding" $alwaysBurnInSubtitleWhenTranscoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/master.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video hls playlist stream.
#
# HEAD /Videos/{itemId}/master.m3u8
# operationId: HeadMasterHlsVideoPlaylist
@deprecated --flag deviceProfileId
export def "videos-masterm3u8 HeadMasterHlsVideoPlaylist" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAdaptiveBitrateStreaming: oneof<nothing, bool> # Enable adaptive bitrate streaming. (default: false)
  --enableTrickplay: oneof<nothing, bool> # Enable trickplay image playlists being added to master playlist. (default: true)
  --enableAudioVbrEncoding: oneof<nothing, bool> # Whether to enable Audio Encoding. (default: true)
  --alwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Whether to always burn in subtitles when transcoding. (default: false)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAdaptiveBitrateStreaming" $enableAdaptiveBitrateStreaming "scalar") (serialize-qp "enableTrickplay" $enableTrickplay "scalar") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar") (serialize-qp "alwaysBurnInSubtitleWhenTranscoding" $alwaysBurnInSubtitleWhenTranscoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/master.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get Default directory browser.
#
# GET /Environment/DefaultDirectoryBrowser
# operationId: GetDefaultDirectoryBrowser
export def "environment-default-directory-browser GetDefaultDirectoryBrowser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Path: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Environment/DefaultDirectoryBrowser")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the contents of a given directory in the file system.
#
# GET /Environment/DirectoryContents
# operationId: GetDirectoryContents
export def "environment-directory-contents GetDirectoryContents" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path.
  --includeFiles: oneof<nothing, bool> # An optional filter to include or exclude files from the results. true/false. (default: false)
  --includeDirectories: oneof<nothing, bool> # An optional filter to include or exclude folders from the results. true/false. (default: false)
]: nothing -> table<Name: string, Path: string, Type: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "includeFiles" $includeFiles "scalar") (serialize-qp "includeDirectories" $includeDirectories "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Environment/DirectoryContents" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available drives from the server's file system.
#
# GET /Environment/Drives
# operationId: GetDrives
export def "environment-drives GetDrives" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Path: string, Type: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Environment/Drives")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets network paths.
#
# GET /Environment/NetworkShares
# DEPRECATED
# operationId: GetNetworkShares
@deprecated
export def "environment-network-shares GetNetworkShares" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Path: string, Type: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Environment/NetworkShares")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the parent path of a given path.
#
# GET /Environment/ParentPath
# operationId: GetParentPath
export def "environment-parent-path GetParentPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path.
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Environment/ParentPath" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Validates path.
#
# POST /Environment/ValidatePath
# operationId: ValidatePath
export def "environment-validate-path ValidatePath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ValidateWritable: oneof<nothing, bool> # Gets or sets a value indicating whether validate if path is writable.
  --Path: string # Gets or sets the path. (nullable)
  --IsFile: oneof<nothing, bool> # Gets or sets is path file. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Environment/ValidatePath")
  let body = {ValidateWritable: $ValidateWritable, Path: $Path, IsFile: $IsFile} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets legacy query filters.
#
# GET /Items/Filters
# operationId: GetQueryFiltersLegacy
export def "items-filters GetQueryFiltersLegacy" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. User id. (format: uuid)
  --parentId: string # Optional. Parent id. (format: uuid)
  --includeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --mediaTypes: list # Optional. Filter by MediaType. Allows multiple, comma delimited.
]: nothing -> record<Genres: list<string>, Tags: list<string>, OfficialRatings: list<string>, Years: list<int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "mediaTypes" $mediaTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/Items/Filters" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets query filters.
#
# GET /Items/Filters2
# operationId: GetQueryFilters
export def "items-filters2 GetQueryFilters" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. User id. (format: uuid)
  --parentId: string # Optional. Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --includeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --isAiring: oneof<nothing, bool> # Optional. Is item airing.
  --isMovie: oneof<nothing, bool> # Optional. Is item movie.
  --isSports: oneof<nothing, bool> # Optional. Is item sports.
  --isKids: oneof<nothing, bool> # Optional. Is item kids.
  --isNews: oneof<nothing, bool> # Optional. Is item news.
  --isSeries: oneof<nothing, bool> # Optional. Is item series.
  --recursive: oneof<nothing, bool> # Optional. Search recursive.
]: nothing -> record<Genres: table<Name: string, Id: string>, Tags: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "isAiring" $isAiring "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "recursive" $recursive "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Items/Filters2" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all genres from a given item, folder, or the entire library.
#
# GET /Genres
# operationId: GetGenres
export def "genres GetGenres" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --searchTerm: string # The search term.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --excludeItemTypes: list # Optional. If specified, results will be filtered out based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be filtered in based on item type. This allows multiple, comma delimited.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --userId: string # User id. (format: uuid)
  --nameStartsWithOrGreater: string # Optional filter by items whose name is sorted equally or greater than a given input string.
  --nameStartsWith: string # Optional filter by items whose name is sorted equally than a given input string.
  --nameLessThan: string # Optional filter by items whose name is equally or lesser than a given input string.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited.
  --sortOrder: list # Sort Order - Ascending,Descending.
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
  --enableTotalRecordCount: oneof<nothing, bool> # Optional. Include total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "nameStartsWithOrGreater" $nameStartsWithOrGreater "scalar") (serialize-qp "nameStartsWith" $nameStartsWith "scalar") (serialize-qp "nameLessThan" $nameLessThan "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Genres" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a genre, by name.
#
# GET /Genres/{genreName}
# operationId: GetGenre
export def "genres GetGenre" [
  genreName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Genres/($genreName)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the specified audio segment for an audio item.
#
# GET /Audio/{itemId}/hls/{segmentId}/stream.aac
# operationId: GetHlsAudioSegmentLegacyAac
export def "audio-hls-streamaac GetHlsAudioSegmentLegacyAac" [
  itemId: string
  segmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Audio/($itemId)/hls/($segmentId)/stream.aac")
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the specified audio segment for an audio item.
#
# GET /Audio/{itemId}/hls/{segmentId}/stream.mp3
# operationId: GetHlsAudioSegmentLegacyMp3
export def "audio-hls-streammp3 GetHlsAudioSegmentLegacyMp3" [
  itemId: string
  segmentId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Audio/($itemId)/hls/($segmentId)/stream.mp3")
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a hls video segment.
#
# GET /Videos/{itemId}/hls/{playlistId}/{segmentId}.{segmentContainer}
# operationId: GetHlsVideoSegmentLegacy
export def "videos-hls GetHlsVideoSegmentLegacy" [
  itemId: string
  playlistId: string
  segmentId: string
  segmentContainer: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Videos/($itemId)/hls/($playlistId)/($segmentId).($segmentContainer)")
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a hls video playlist.
#
# GET /Videos/{itemId}/hls/{playlistId}/stream.m3u8
# operationId: GetHlsPlaylistLegacy
export def "videos-hls-streamm3u8 GetHlsPlaylistLegacy" [
  itemId: string
  playlistId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Videos/($itemId)/hls/($playlistId)/stream.m3u8")
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Stops an active encoding.
#
# DELETE /Videos/ActiveEncodings
# operationId: StopEncodingProcess
export def "videos-active-encodings StopEncodingProcess" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --playSessionId: string # The play session id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Videos/ActiveEncodings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get artist image by name.
#
# GET /Artists/{name}/Images/{imageType}/{imageIndex}
# operationId: GetArtistImage
export def "artists-images GetArtistImage" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Artists/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get artist image by name.
#
# HEAD /Artists/{name}/Images/{imageType}/{imageIndex}
# operationId: HeadArtistImage
export def "artists-images HeadArtistImage" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Artists/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Generates or gets the splashscreen.
#
# GET /Branding/Splashscreen
# operationId: GetSplashscreen
export def "branding-splashscreen GetSplashscreen" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Branding/Splashscreen" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Uploads a custom splashscreen. The body is expected to the image contents base64 encoded.
#
# POST /Branding/Splashscreen
# operationId: UploadCustomSplashscreen
export def "branding-splashscreen UploadCustomSplashscreen" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Branding/Splashscreen")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "image/*" $body
}

# Delete a custom splashscreen.
#
# DELETE /Branding/Splashscreen
# operationId: DeleteCustomSplashscreen
export def "branding-splashscreen DeleteCustomSplashscreen" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Branding/Splashscreen")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get genre image by name.
#
# GET /Genres/{name}/Images/{imageType}
# operationId: GetGenreImage
export def "genres-images GetGenreImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Genres/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get genre image by name.
#
# HEAD /Genres/{name}/Images/{imageType}
# operationId: HeadGenreImage
export def "genres-images HeadGenreImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Genres/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get genre image by name.
#
# GET /Genres/{name}/Images/{imageType}/{imageIndex}
# operationId: GetGenreImageByIndex
export def "genres-images GetGenreImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Genres/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get genre image by name.
#
# HEAD /Genres/{name}/Images/{imageType}/{imageIndex}
# operationId: HeadGenreImageByIndex
export def "genres-images HeadGenreImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Genres/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get item image infos.
#
# GET /Items/{itemId}/Images
# operationId: GetItemImageInfos
export def "items-images GetItemImageInfos" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<ImageType: record, ImageIndex: int, ImageTag: string, Path: string, BlurHash: string, Height: int, Width: int, Size: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/Images")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an item's image.
#
# DELETE /Items/{itemId}/Images/{imageType}
# operationId: DeleteItemImage
export def "items-images DeleteItemImage" [
  itemId: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --imageIndex: int # The image index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set item image.
#
# POST /Items/{itemId}/Images/{imageType}
# operationId: SetItemImage
export def "items-images SetItemImage" [
  itemId: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "image/*" $body
}

# Gets the item's image.
#
# GET /Items/{itemId}/Images/{imageType}
# operationId: GetItemImage
export def "items-images GetItemImage" [
  itemId: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Optional. The MediaBrowser.Model.Drawing.ImageFormat of the returned image.
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the item's image.
#
# HEAD /Items/{itemId}/Images/{imageType}
# operationId: HeadItemImage
export def "items-images HeadItemImage" [
  itemId: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Optional. The MediaBrowser.Model.Drawing.ImageFormat of the returned image.
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an item's image.
#
# DELETE /Items/{itemId}/Images/{imageType}/{imageIndex}
# operationId: DeleteItemImageByIndex
export def "items-images DeleteItemImageByIndex" [
  itemId: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)/($imageIndex)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set item image.
#
# POST /Items/{itemId}/Images/{imageType}/{imageIndex}
# operationId: SetItemImageByIndex
export def "items-images SetItemImageByIndex" [
  itemId: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)/($imageIndex)")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "image/*" $body
}

# Gets the item's image.
#
# GET /Items/{itemId}/Images/{imageType}/{imageIndex}
# operationId: GetItemImageByIndex
export def "items-images GetItemImageByIndex" [
  itemId: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Optional. The MediaBrowser.Model.Drawing.ImageFormat of the returned image.
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the item's image.
#
# HEAD /Items/{itemId}/Images/{imageType}/{imageIndex}
# operationId: HeadItemImageByIndex
export def "items-images HeadItemImageByIndex" [
  itemId: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Optional. The MediaBrowser.Model.Drawing.ImageFormat of the returned image.
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the item's image.
#
# GET /Items/{itemId}/Images/{imageType}/{imageIndex}/{tag}/{format}/{maxWidth}/{maxHeight}/{percentPlayed}/{unplayedCount}
# operationId: GetItemImage2
export def "items-images GetItemImage2" [
  itemId: string
  imageType: string
  maxWidth: int
  maxHeight: int
  tag: string
  format: string
  percentPlayed: float
  unplayedCount: int
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)/($imageIndex)/($tag)/($format)/($maxWidth)/($maxHeight)/($percentPlayed)/($unplayedCount)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the item's image.
#
# HEAD /Items/{itemId}/Images/{imageType}/{imageIndex}/{tag}/{format}/{maxWidth}/{maxHeight}/{percentPlayed}/{unplayedCount}
# operationId: HeadItemImage2
export def "items-images HeadItemImage2" [
  itemId: string
  imageType: string
  maxWidth: int
  maxHeight: int
  tag: string
  format: string
  percentPlayed: float
  unplayedCount: int
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)/($imageIndex)/($tag)/($format)/($maxWidth)/($maxHeight)/($percentPlayed)/($unplayedCount)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates the index for an item image.
#
# POST /Items/{itemId}/Images/{imageType}/{imageIndex}/Index
# operationId: UpdateItemImageIndex
export def "items-images-index UpdateItemImageIndex" [
  itemId: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --newIndex: int # New image index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "newIndex" $newIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Images/($imageType)/($imageIndex)/Index" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get music genre image by name.
#
# GET /MusicGenres/{name}/Images/{imageType}
# operationId: GetMusicGenreImage
export def "music-genres-images GetMusicGenreImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/MusicGenres/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get music genre image by name.
#
# HEAD /MusicGenres/{name}/Images/{imageType}
# operationId: HeadMusicGenreImage
export def "music-genres-images HeadMusicGenreImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/MusicGenres/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get music genre image by name.
#
# GET /MusicGenres/{name}/Images/{imageType}/{imageIndex}
# operationId: GetMusicGenreImageByIndex
export def "music-genres-images GetMusicGenreImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/MusicGenres/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get music genre image by name.
#
# HEAD /MusicGenres/{name}/Images/{imageType}/{imageIndex}
# operationId: HeadMusicGenreImageByIndex
export def "music-genres-images HeadMusicGenreImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/MusicGenres/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get person image by name.
#
# GET /Persons/{name}/Images/{imageType}
# operationId: GetPersonImage
export def "persons-images GetPersonImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Persons/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get person image by name.
#
# HEAD /Persons/{name}/Images/{imageType}
# operationId: HeadPersonImage
export def "persons-images HeadPersonImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Persons/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get person image by name.
#
# GET /Persons/{name}/Images/{imageType}/{imageIndex}
# operationId: GetPersonImageByIndex
export def "persons-images GetPersonImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Persons/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get person image by name.
#
# HEAD /Persons/{name}/Images/{imageType}/{imageIndex}
# operationId: HeadPersonImageByIndex
export def "persons-images HeadPersonImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Persons/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get studio image by name.
#
# GET /Studios/{name}/Images/{imageType}
# operationId: GetStudioImage
export def "studios-images GetStudioImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Studios/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get studio image by name.
#
# HEAD /Studios/{name}/Images/{imageType}
# operationId: HeadStudioImage
export def "studios-images HeadStudioImage" [
  name: string
  imageType: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
  --imageIndex: int # Image index. (format: int32)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar") (serialize-qp "imageIndex" $imageIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Studios/($name)/Images/($imageType)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get studio image by name.
#
# GET /Studios/{name}/Images/{imageType}/{imageIndex}
# operationId: GetStudioImageByIndex
export def "studios-images GetStudioImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Studios/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get studio image by name.
#
# HEAD /Studios/{name}/Images/{imageType}/{imageIndex}
# operationId: HeadStudioImageByIndex
export def "studios-images HeadStudioImageByIndex" [
  name: string
  imageType: string
  imageIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
  --maxWidth: int # The maximum image width to return. (format: int32)
  --maxHeight: int # The maximum image height to return. (format: int32)
  --percentPlayed: float # Optional. Percent to render for the percent played overlay. (format: double)
  --unplayedCount: int # Optional. Unplayed count overlay to render. (format: int32)
  --width: int # The fixed image width to return. (format: int32)
  --height: int # The fixed image height to return. (format: int32)
  --quality: int # Optional. Quality setting, from 0-100. Defaults to 90 and should suffice in most cases. (format: int32)
  --fillWidth: int # Width of box to fill. (format: int32)
  --fillHeight: int # Height of box to fill. (format: int32)
  --blur: int # Optional. Blur image. (format: int32)
  --backgroundColor: string # Optional. Apply a background color for transparent images.
  --foregroundLayer: string # Optional. Apply a foreground layer on top of the image.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "percentPlayed" $percentPlayed "scalar") (serialize-qp "unplayedCount" $unplayedCount "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "quality" $quality "scalar") (serialize-qp "fillWidth" $fillWidth "scalar") (serialize-qp "fillHeight" $fillHeight "scalar") (serialize-qp "blur" $blur "scalar") (serialize-qp "backgroundColor" $backgroundColor "scalar") (serialize-qp "foregroundLayer" $foregroundLayer "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Studios/($name)/Images/($imageType)/($imageIndex)" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Sets the user image.
#
# POST /UserImage
# operationId: PostUserImage
export def "user-image PostUserImage" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # User Id. (format: uuid)
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/UserImage" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "image/*" $body
}

# Delete the user's image.
#
# DELETE /UserImage
# operationId: DeleteUserImage
export def "user-image DeleteUserImage" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # User Id. (format: uuid)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/UserImage" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get user profile image.
#
# GET /UserImage
# operationId: GetUserImage
export def "user-image GetUserImage" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # User id. (format: uuid)
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/UserImage" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get user profile image.
#
# HEAD /UserImage
# operationId: HeadUserImage
export def "user-image HeadUserImage" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # User id. (format: uuid)
  --tag: string # Optional. Supply the cache tag from the item object to receive strong caching headers.
  --format: string@format-completer # Determines the output format of the image - original,gif,jpg,png.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "format" $format "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/UserImage" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given album.
#
# GET /Albums/{itemId}/InstantMix
# operationId: GetInstantMixFromAlbum
export def "albums-instant-mix GetInstantMixFromAlbum" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Albums/($itemId)/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given artist.
#
# GET /Artists/{itemId}/InstantMix
# operationId: GetInstantMixFromArtists
export def "artists-instant-mix GetInstantMixFromArtists" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Artists/($itemId)/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given artist.
#
# GET /Artists/InstantMix
# DEPRECATED
# operationId: GetInstantMixFromArtists2
@deprecated
export def "artists-instant-mix GetInstantMixFromArtists2" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --id: string # The item id. (format: uuid)
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/Artists/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given item.
#
# GET /Items/{itemId}/InstantMix
# operationId: GetInstantMixFromItem
export def "items-instant-mix GetInstantMixFromItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given genre.
#
# GET /MusicGenres/{name}/InstantMix
# operationId: GetInstantMixFromMusicGenreByName
export def "music-genres-instant-mix GetInstantMixFromMusicGenreByName" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/MusicGenres/($name)/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given genre.
#
# GET /MusicGenres/InstantMix
# operationId: GetInstantMixFromMusicGenreById
export def "music-genres-instant-mix GetInstantMixFromMusicGenreById" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --id: string # The item id. (format: uuid)
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/MusicGenres/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given playlist.
#
# GET /Playlists/{itemId}/InstantMix
# operationId: GetInstantMixFromPlaylist
export def "playlists-instant-mix GetInstantMixFromPlaylist" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Playlists/($itemId)/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates an instant playlist based on a given song.
#
# GET /Songs/{itemId}/InstantMix
# operationId: GetInstantMixFromSong
export def "songs-instant-mix GetInstantMixFromSong" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Songs/($itemId)/InstantMix" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the item's external id info.
#
# GET /Items/{itemId}/ExternalIdInfos
# operationId: GetExternalIdInfos
export def "items-external-id-infos GetExternalIdInfos" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Key: string, Type: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/ExternalIdInfos")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Applies search criteria to an item and refreshes metadata.
#
# POST /Items/RemoteSearch/Apply/{itemId}
# operationId: ApplySearchCriteria
# --Artists item shape: {Name?: string, ProviderIds?: record, ProductionYear?: int, IndexNumber?: int, IndexNumberEnd?: int, ParentIndexNumber?: int, PremiereDate?: string, ImageUrl?: string, SearchProviderName?: string, Overview?: string, AlbumArtist?: any, Artists?: list}
export def "items-remote-search-apply ApplySearchCriteria" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --replaceAllImages: oneof<nothing, bool> # Optional. Whether or not to replace all images. Default: True. (default: true)
  --Name: string # Gets or sets the name. (nullable)
  --ProviderIds: record # Gets or sets the provider ids. (nullable)
  --ProductionYear: int # Gets or sets the year. (nullable, format: int32)
  --IndexNumber: int # nullable, format: int32
  --IndexNumberEnd: int # nullable, format: int32
  --ParentIndexNumber: int # nullable, format: int32
  --PremiereDate: string # nullable, format: date-time
  --ImageUrl: string # nullable
  --SearchProviderName: string # nullable
  --Overview: string # nullable
  --AlbumArtist: any # nullable
  --Artists: list # nullable — item shape: {Name?: string, ProviderIds?: record, ProductionYear?: int, IndexNumber?: int, IndexNumberEnd?: int, ParentIndexNumber?: int, PremiereDate?: string, ImageUrl?: string, SearchProviderName?: string, Overview?: string, AlbumArtist?: any, Artists?: list}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "replaceAllImages" $replaceAllImages "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/RemoteSearch/Apply/($itemId)" $qp)
  let body = {Name: $Name, ProviderIds: $ProviderIds, ProductionYear: $ProductionYear, IndexNumber: $IndexNumber, IndexNumberEnd: $IndexNumberEnd, ParentIndexNumber: $ParentIndexNumber, PremiereDate: $PremiereDate, ImageUrl: $ImageUrl, SearchProviderName: $SearchProviderName, Overview: $Overview, AlbumArtist: $AlbumArtist, Artists: $Artists} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get book remote search.
#
# POST /Items/RemoteSearch/Book
# operationId: GetBookRemoteSearchResults
export def "items-remote-search-book GetBookRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/Book")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get box set remote search.
#
# POST /Items/RemoteSearch/BoxSet
# operationId: GetBoxSetRemoteSearchResults
export def "items-remote-search-box-set GetBoxSetRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/BoxSet")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get movie remote search.
#
# POST /Items/RemoteSearch/Movie
# operationId: GetMovieRemoteSearchResults
export def "items-remote-search-movie GetMovieRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/Movie")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get music album remote search.
#
# POST /Items/RemoteSearch/MusicAlbum
# operationId: GetMusicAlbumRemoteSearchResults
export def "items-remote-search-music-album GetMusicAlbumRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/MusicAlbum")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get music artist remote search.
#
# POST /Items/RemoteSearch/MusicArtist
# operationId: GetMusicArtistRemoteSearchResults
export def "items-remote-search-music-artist GetMusicArtistRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/MusicArtist")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get music video remote search.
#
# POST /Items/RemoteSearch/MusicVideo
# operationId: GetMusicVideoRemoteSearchResults
export def "items-remote-search-music-video GetMusicVideoRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/MusicVideo")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get person remote search.
#
# POST /Items/RemoteSearch/Person
# operationId: GetPersonRemoteSearchResults
export def "items-remote-search-person GetPersonRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/Person")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get series remote search.
#
# POST /Items/RemoteSearch/Series
# operationId: GetSeriesRemoteSearchResults
export def "items-remote-search-series GetSeriesRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/Series")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get trailer remote search.
#
# POST /Items/RemoteSearch/Trailer
# operationId: GetTrailerRemoteSearchResults
export def "items-remote-search-trailer GetTrailerRemoteSearchResults" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --SearchInfo: any # nullable
  --ItemId: string # format: uuid
  --SearchProviderName: string # Gets or sets the provider name to search within if set. (nullable)
  --IncludeDisabledProviders: oneof<nothing, bool> # Gets or sets a value indicating whether disabled providers should be included.
]: any -> table<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record<Name: string, ProviderIds: record, ProductionYear: int, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, PremiereDate: string, ImageUrl: string, SearchProviderName: string, Overview: string, AlbumArtist: record, Artists: list>, Artists: list<any>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Items/RemoteSearch/Trailer")
  let body = {SearchInfo: $SearchInfo, ItemId: $ItemId, SearchProviderName: $SearchProviderName, IncludeDisabledProviders: $IncludeDisabledProviders} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Refreshes metadata for an item.
#
# POST /Items/{itemId}/Refresh
# operationId: RefreshItem
export def "items-refresh RefreshItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --metadataRefreshMode: string@metadataRefreshMode-completer # (Optional) Specifies the metadata refresh mode. (default: None)
  --imageRefreshMode: string@imageRefreshMode-completer # (Optional) Specifies the image refresh mode. (default: None)
  --replaceAllMetadata: oneof<nothing, bool> # (Optional) Determines if metadata should be replaced. Only applicable if mode is FullRefresh. (default: false)
  --replaceAllImages: oneof<nothing, bool> # (Optional) Determines if images should be replaced. Only applicable if mode is FullRefresh. (default: false)
  --regenerateTrickplay: oneof<nothing, bool> # (Optional) Determines if trickplay images should be replaced. Only applicable if mode is FullRefresh. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "metadataRefreshMode" $metadataRefreshMode "scalar") (serialize-qp "imageRefreshMode" $imageRefreshMode "scalar") (serialize-qp "replaceAllMetadata" $replaceAllMetadata "scalar") (serialize-qp "replaceAllImages" $replaceAllImages "scalar") (serialize-qp "regenerateTrickplay" $regenerateTrickplay "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Refresh" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets items based on a query.
#
# GET /Items
# operationId: GetItems
export def "items GetItems" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id supplied as query parameter; this is required when not using an API key. (format: uuid)
  --maxOfficialRating: string # Optional filter by maximum official rating (PG, PG-13, TV-MA, etc).
  --hasThemeSong: oneof<nothing, bool> # Optional filter by items with theme songs.
  --hasThemeVideo: oneof<nothing, bool> # Optional filter by items with theme videos.
  --hasSubtitles: oneof<nothing, bool> # Optional filter by items with subtitles.
  --hasSpecialFeature: oneof<nothing, bool> # Optional filter by items with special features.
  --hasTrailer: oneof<nothing, bool> # Optional filter by items with trailers.
  --adjacentTo: string # Optional. Return items that are siblings of a supplied item. (format: uuid)
  --indexNumber: int # Optional filter by index number. (format: int32)
  --parentIndexNumber: int # Optional filter by parent index number. (format: int32)
  --hasParentalRating: oneof<nothing, bool> # Optional filter by items that have or do not have a parental rating.
  --isHd: oneof<nothing, bool> # Optional filter by items that are HD or not.
  --is4K: oneof<nothing, bool> # Optional filter by items that are 4K or not.
  --locationTypes: list # Optional. If specified, results will be filtered based on LocationType. This allows multiple, comma delimited.
  --excludeLocationTypes: list # Optional. If specified, results will be filtered based on the LocationType. This allows multiple, comma delimited.
  --isMissing: oneof<nothing, bool> # Optional filter by items that are missing episodes or not.
  --isUnaired: oneof<nothing, bool> # Optional filter by items that are unaired episodes or not.
  --minCommunityRating: float # Optional filter by minimum community rating. (format: double)
  --minCriticRating: float # Optional filter by minimum critic rating. (format: double)
  --minPremiereDate: string # Optional. The minimum premiere date. Format = ISO. (format: date-time)
  --minDateLastSaved: string # Optional. The minimum last saved date. Format = ISO. (format: date-time)
  --minDateLastSavedForUser: string # Optional. The minimum last saved date for the current user. Format = ISO. (format: date-time)
  --maxPremiereDate: string # Optional. The maximum premiere date. Format = ISO. (format: date-time)
  --hasOverview: oneof<nothing, bool> # Optional filter by items that have an overview or not.
  --hasImdbId: oneof<nothing, bool> # Optional filter by items that have an IMDb id or not.
  --hasTmdbId: oneof<nothing, bool> # Optional filter by items that have a TMDb id or not.
  --hasTvdbId: oneof<nothing, bool> # Optional filter by items that have a TVDb id or not.
  --isMovie: oneof<nothing, bool> # Optional filter for live tv movies.
  --isSeries: oneof<nothing, bool> # Optional filter for live tv series.
  --isNews: oneof<nothing, bool> # Optional filter for live tv news.
  --isKids: oneof<nothing, bool> # Optional filter for live tv kids.
  --isSports: oneof<nothing, bool> # Optional filter for live tv sports.
  --excludeItemIds: list # Optional. If specified, results will be filtered by excluding item ids. This allows multiple, comma delimited.
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --recursive: oneof<nothing, bool> # When searching within folders, this determines whether or not the search will be recursive. true/false.
  --searchTerm: string # Optional. Filter based on a search term.
  --sortOrder: list # Sort Order - Ascending, Descending.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines.
  --excludeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be filtered based on the item type. This allows multiple, comma delimited.
  --filters: list # Optional. Specify additional filters to apply. This allows multiple, comma delimited. Options: IsFolder, IsNotFolder, IsUnplayed, IsPlayed, IsFavorite, IsResumable, Likes, Dislikes.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not.
  --mediaTypes: list # Optional filter by MediaType. Allows multiple, comma delimited.
  --imageTypes: list # Optional. If specified, results will be filtered based on those containing image types. This allows multiple, comma delimited.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
  --isPlayed: oneof<nothing, bool> # Optional filter by items that are played, or not.
  --genres: list # Optional. If specified, results will be filtered based on genre. This allows multiple, pipe delimited.
  --officialRatings: list # Optional. If specified, results will be filtered based on OfficialRating. This allows multiple, pipe delimited.
  --tags: list # Optional. If specified, results will be filtered based on tag. This allows multiple, pipe delimited.
  --years: list # Optional. If specified, results will be filtered based on production year. This allows multiple, comma delimited.
  --enableUserData: oneof<nothing, bool> # Optional, include user data.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --person: string # Optional. If specified, results will be filtered to include only those containing the specified person.
  --personIds: list # Optional. If specified, results will be filtered to include only those containing the specified person id.
  --personTypes: list # Optional. If specified, along with Person, results will be filtered to include only those containing the specified person and PersonType. Allows multiple, comma-delimited.
  --studios: list # Optional. If specified, results will be filtered based on studio. This allows multiple, pipe delimited.
  --artists: list # Optional. If specified, results will be filtered based on artists. This allows multiple, pipe delimited.
  --excludeArtistIds: list # Optional. If specified, results will be filtered based on artist id. This allows multiple, pipe delimited.
  --artistIds: list # Optional. If specified, results will be filtered to include only those containing the specified artist id.
  --albumArtistIds: list # Optional. If specified, results will be filtered to include only those containing the specified album artist id.
  --contributingArtistIds: list # Optional. If specified, results will be filtered to include only those containing the specified contributing artist id.
  --albums: list # Optional. If specified, results will be filtered based on album. This allows multiple, pipe delimited.
  --albumIds: list # Optional. If specified, results will be filtered based on album id. This allows multiple, pipe delimited.
  --ids: list # Optional. If specific items are needed, specify a list of item id's to retrieve. This allows multiple, comma delimited.
  --videoTypes: list # Optional filter by VideoType (videofile, dvd, bluray, iso). Allows multiple, comma delimited.
  --minOfficialRating: string # Optional filter by minimum official rating (PG, PG-13, TV-MA, etc).
  --isLocked: oneof<nothing, bool> # Optional filter by items that are locked.
  --isPlaceHolder: oneof<nothing, bool> # Optional filter by items that are placeholders.
  --hasOfficialRating: oneof<nothing, bool> # Optional filter by items that have official ratings.
  --collapseBoxSetItems: oneof<nothing, bool> # Whether or not to hide items behind their boxsets.
  --minWidth: int # Optional. Filter by the minimum width of the item. (format: int32)
  --minHeight: int # Optional. Filter by the minimum height of the item. (format: int32)
  --maxWidth: int # Optional. Filter by the maximum width of the item. (format: int32)
  --maxHeight: int # Optional. Filter by the maximum height of the item. (format: int32)
  --is3D: oneof<nothing, bool> # Optional filter by items that are 3D, or not.
  --seriesStatus: list # Optional filter by Series Status. Allows multiple, comma delimited.
  --nameStartsWithOrGreater: string # Optional filter by items whose name is sorted equally or greater than a given input string.
  --nameStartsWith: string # Optional filter by items whose name is sorted equally than a given input string.
  --nameLessThan: string # Optional filter by items whose name is equally or lesser than a given input string.
  --studioIds: list # Optional. If specified, results will be filtered based on studio id. This allows multiple, pipe delimited.
  --genreIds: list # Optional. If specified, results will be filtered based on genre id. This allows multiple, pipe delimited.
  --enableTotalRecordCount: oneof<nothing, bool> # Optional. Enable the total record count. (default: true)
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "maxOfficialRating" $maxOfficialRating "scalar") (serialize-qp "hasThemeSong" $hasThemeSong "scalar") (serialize-qp "hasThemeVideo" $hasThemeVideo "scalar") (serialize-qp "hasSubtitles" $hasSubtitles "scalar") (serialize-qp "hasSpecialFeature" $hasSpecialFeature "scalar") (serialize-qp "hasTrailer" $hasTrailer "scalar") (serialize-qp "adjacentTo" $adjacentTo "scalar") (serialize-qp "indexNumber" $indexNumber "scalar") (serialize-qp "parentIndexNumber" $parentIndexNumber "scalar") (serialize-qp "hasParentalRating" $hasParentalRating "scalar") (serialize-qp "isHd" $isHd "scalar") (serialize-qp "is4K" $is4K "scalar") (serialize-qp "locationTypes" $locationTypes "multi") (serialize-qp "excludeLocationTypes" $excludeLocationTypes "multi") (serialize-qp "isMissing" $isMissing "scalar") (serialize-qp "isUnaired" $isUnaired "scalar") (serialize-qp "minCommunityRating" $minCommunityRating "scalar") (serialize-qp "minCriticRating" $minCriticRating "scalar") (serialize-qp "minPremiereDate" $minPremiereDate "scalar") (serialize-qp "minDateLastSaved" $minDateLastSaved "scalar") (serialize-qp "minDateLastSavedForUser" $minDateLastSavedForUser "scalar") (serialize-qp "maxPremiereDate" $maxPremiereDate "scalar") (serialize-qp "hasOverview" $hasOverview "scalar") (serialize-qp "hasImdbId" $hasImdbId "scalar") (serialize-qp "hasTmdbId" $hasTmdbId "scalar") (serialize-qp "hasTvdbId" $hasTvdbId "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "excludeItemIds" $excludeItemIds "multi") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "recursive" $recursive "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "filters" $filters "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "mediaTypes" $mediaTypes "multi") (serialize-qp "imageTypes" $imageTypes "multi") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "isPlayed" $isPlayed "scalar") (serialize-qp "genres" $genres "multi") (serialize-qp "officialRatings" $officialRatings "multi") (serialize-qp "tags" $tags "multi") (serialize-qp "years" $years "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "person" $person "scalar") (serialize-qp "personIds" $personIds "multi") (serialize-qp "personTypes" $personTypes "multi") (serialize-qp "studios" $studios "multi") (serialize-qp "artists" $artists "multi") (serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "artistIds" $artistIds "multi") (serialize-qp "albumArtistIds" $albumArtistIds "multi") (serialize-qp "contributingArtistIds" $contributingArtistIds "multi") (serialize-qp "albums" $albums "multi") (serialize-qp "albumIds" $albumIds "multi") (serialize-qp "ids" $ids "multi") (serialize-qp "videoTypes" $videoTypes "multi") (serialize-qp "minOfficialRating" $minOfficialRating "scalar") (serialize-qp "isLocked" $isLocked "scalar") (serialize-qp "isPlaceHolder" $isPlaceHolder "scalar") (serialize-qp "hasOfficialRating" $hasOfficialRating "scalar") (serialize-qp "collapseBoxSetItems" $collapseBoxSetItems "scalar") (serialize-qp "minWidth" $minWidth "scalar") (serialize-qp "minHeight" $minHeight "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "is3D" $is3D "scalar") (serialize-qp "seriesStatus" $seriesStatus "multi") (serialize-qp "nameStartsWithOrGreater" $nameStartsWithOrGreater "scalar") (serialize-qp "nameStartsWith" $nameStartsWith "scalar") (serialize-qp "nameLessThan" $nameLessThan "scalar") (serialize-qp "studioIds" $studioIds "multi") (serialize-qp "genreIds" $genreIds "multi") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar") (serialize-qp "enableImages" $enableImages "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Items" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Deletes items from the library and filesystem.
#
# DELETE /Items
# operationId: DeleteItems
export def "items DeleteItems" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ids: list # The item ids.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ids" $ids "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/Items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get Item User Data.
#
# GET /UserItems/{itemId}/UserData
# operationId: GetItemUserData
export def "user-items-user-data GetItemUserData" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
]: nothing -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserItems/($itemId)/UserData" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update Item User Data.
#
# POST /UserItems/{itemId}/UserData
# operationId: UpdateItemUserData
export def "user-items-user-data UpdateItemUserData" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
  --Rating: float # Gets or sets the rating. (nullable, format: double)
  --PlayedPercentage: float # Gets or sets the played percentage. (nullable, format: double)
  --UnplayedItemCount: int # Gets or sets the unplayed item count. (nullable, format: int32)
  --PlaybackPositionTicks: int # Gets or sets the playback position ticks. (nullable, format: int64)
  --PlayCount: int # Gets or sets the play count. (nullable, format: int32)
  --IsFavorite: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is favorite. (nullable)
  --Likes: oneof<nothing, bool> # Gets or sets a value indicating whether this MediaBrowser.Model.Dto.UpdateUserItemDataDto is likes. (nullable)
  --LastPlayedDate: string # Gets or sets the last played date. (nullable, format: date-time)
  --Played: oneof<nothing, bool> # Gets or sets a value indicating whether this MediaBrowser.Model.Dto.UserItemDataDto is played. (nullable)
  --Key: string # Gets or sets the key. (nullable)
  --ItemId: string # Gets or sets the item identifier. (nullable)
]: any -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserItems/($itemId)/UserData" $qp)
  let body = {Rating: $Rating, PlayedPercentage: $PlayedPercentage, UnplayedItemCount: $UnplayedItemCount, PlaybackPositionTicks: $PlaybackPositionTicks, PlayCount: $PlayCount, IsFavorite: $IsFavorite, Likes: $Likes, LastPlayedDate: $LastPlayedDate, Played: $Played, Key: $Key, ItemId: $ItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets items based on a query.
#
# GET /UserItems/Resume
# operationId: GetResumeItems
export def "user-items-resume GetResumeItems" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
  --startIndex: int # The start index. (format: int32)
  --limit: int # The item limit. (format: int32)
  --searchTerm: string # The search term.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines.
  --mediaTypes: list # Optional. Filter by MediaType. Allows multiple, comma delimited.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --excludeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be filtered based on the item type. This allows multiple, comma delimited.
  --enableTotalRecordCount: oneof<nothing, bool> # Optional. Enable the total record count. (default: true)
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output. (default: true)
  --excludeActiveSessions: oneof<nothing, bool> # Optional. Whether to exclude the currently active sessions. (default: false)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "mediaTypes" $mediaTypes "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "excludeActiveSessions" $excludeActiveSessions "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/UserItems/Resume" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates an item.
#
# POST /Items/{itemId}
# operationId: UpdateItem
# --ExternalUrls item shape: {Name?: string, Url?: string}
# --MediaSources item shape: {Protocol?: "File"|"Http"|"Rtmp"|"Rtsp"|"Udp"|"Rtp"|"Ftp", Id?: string, Path?: string, EncoderPath?: string, EncoderProtocol?: "File"|"Http"|"Rtmp"|"Rtsp"|"Udp"|"Rtp"|"Ftp", Type?: "Default"|"Grouping"|"Placeholder", Container?: string, Size?: int, Name?: string, IsRemote?: bool, ETag?: string, RunTimeTicks?: int, ReadAtNativeFramerate?: bool, IgnoreDts?: bool, IgnoreIndex?: bool, GenPtsInput?: bool, SupportsTranscoding?: bool, SupportsDirectStream?: bool, SupportsDirectPlay?: bool, IsInfiniteStream?: bool, UseMostCompatibleTranscodingProfile?: bool, RequiresOpening?: bool, OpenToken?: string, RequiresClosing?: bool, LiveStreamId?: string, BufferMs?: int, RequiresLooping?: bool, SupportsProbing?: bool, VideoType?: "VideoFile"|"Iso"|"Dvd"|"BluRay", IsoType?: "Dvd"|"BluRay", Video3DFormat?: "HalfSideBySide"|"FullSideBySide"|"FullTopAndBottom"|"HalfTopAndBottom"|"MVC", MediaStreams?: list, MediaAttachments?: list, Formats?: list, Bitrate?: int, FallbackMaxStreamingBitrate?: int, Timestamp?: "None"|"Zero"|"Valid", RequiredHttpHeaders?: record, TranscodingUrl?: string, TranscodingSubProtocol?: "http"|"hls", TranscodingContainer?: string, AnalyzeDurationMs?: int, DefaultAudioStreamIndex?: int, DefaultSubtitleStreamIndex?: int, HasSegments?: bool}
# --RemoteTrailers item shape: {Url?: string, Name?: string}
# --People item shape: {Name?: string, Id?: string, Role?: string, Type?: "Unknown"|"Actor"|"Director"|"Composer"|"Writer"|"GuestStar"|"Producer"|"Conductor"|"Lyricist"|"Arranger"|"Engineer"|"Mixer"|"Remixer"|"Creator"|"Artist"|"AlbumArtist"|"Author"|"Illustrator"|"Penciller"|"Inker"|"Colorist"|"Letterer"|"CoverArtist"|"Editor"|"Translator", PrimaryImageTag?: string, ImageBlurHashes?: record}
# --Studios item shape: {Name?: string, Id?: string}
# --GenreItems item shape: {Name?: string, Id?: string}
# --ArtistItems item shape: {Name?: string, Id?: string}
# --AlbumArtists item shape: {Name?: string, Id?: string}
# --MediaStreams item shape: {Codec?: string, CodecTag?: string, Language?: string, ColorRange?: string, ColorSpace?: string, ColorTransfer?: string, ColorPrimaries?: string, DvVersionMajor?: int, DvVersionMinor?: int, DvProfile?: int, DvLevel?: int, RpuPresentFlag?: int, ElPresentFlag?: int, BlPresentFlag?: int, DvBlSignalCompatibilityId?: int, Rotation?: int, Comment?: string, TimeBase?: string, CodecTimeBase?: string, Title?: string, Hdr10PlusPresentFlag?: bool, LocalizedUndefined?: string, LocalizedDefault?: string, LocalizedForced?: string, LocalizedExternal?: string, LocalizedHearingImpaired?: string, NalLengthSize?: string, IsInterlaced?: bool, IsAVC?: bool, ChannelLayout?: string, BitRate?: int, BitDepth?: int, RefFrames?: int, PacketLength?: int, Channels?: int, SampleRate?: int, IsDefault?: bool, IsForced?: bool, IsHearingImpaired?: bool, Height?: int, Width?: int, AverageFrameRate?: float, RealFrameRate?: float, Profile?: string, Type?: "Audio"|"Video"|"Subtitle"|"EmbeddedImage"|"Data"|"Lyric", AspectRatio?: string, Index?: int, Score?: int, IsExternal?: bool, DeliveryMethod?: "Encode"|"Embed"|"External"|"Hls"|"Drop", DeliveryUrl?: string, IsExternalUrl?: bool, SupportsExternalStream?: bool, Path?: string, PixelFormat?: string, Level?: float, IsAnamorphic?: bool}
# --ImageBlurHashes shape: {Primary?: record, Art?: record, Backdrop?: record, Banner?: record, Logo?: record, Thumb?: record, Disc?: record, Box?: record, Screenshot?: record, Menu?: record, Chapter?: record, BoxRear?: record, Profile?: record}
# --Chapters item shape: {StartPositionTicks?: int, Name?: string, ImagePath?: string, ImageDateModified?: string, ImageTag?: string}
export def "items UpdateItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Name: string # Gets or sets the name. (nullable)
  --OriginalTitle: string # nullable
  --ServerId: string # Gets or sets the server identifier. (nullable)
  --Id: string # Gets or sets the id. (format: uuid)
  --Etag: string # Gets or sets the etag. (nullable)
  --SourceType: string # Gets or sets the type of the source. (nullable)
  --PlaylistItemId: string # Gets or sets the playlist item identifier. (nullable)
  --DateCreated: string # Gets or sets the date created. (nullable, format: date-time)
  --DateLastMediaAdded: string # nullable, format: date-time
  --ExtraType: any@ExtraType-completer # nullable
  --AirsBeforeSeasonNumber: int # nullable, format: int32
  --AirsAfterSeasonNumber: int # nullable, format: int32
  --AirsBeforeEpisodeNumber: int # nullable, format: int32
  --CanDelete: oneof<nothing, bool> # nullable
  --CanDownload: oneof<nothing, bool> # nullable
  --HasLyrics: oneof<nothing, bool> # nullable
  --HasSubtitles: oneof<nothing, bool> # nullable
  --PreferredMetadataLanguage: string # nullable
  --PreferredMetadataCountryCode: string # nullable
  --Container: string # nullable
  --SortName: string # Gets or sets the name of the sort. (nullable)
  --ForcedSortName: string # nullable
  --Video3DFormat: any@Video3DFormat-completer # Gets or sets the video3 D format. (nullable)
  --PremiereDate: string # Gets or sets the premiere date. (nullable, format: date-time)
  --ExternalUrls: list # Gets or sets the external urls. (nullable) — item shape: {Name?: string, Url?: string}
  --MediaSources: list # Gets or sets the media versions. (nullable) — item shape: {Protocol?: "File"|"Http"|"Rtmp"|"Rtsp"|"Udp"|"Rtp"|"Ftp", Id?: string, Path?: string, EncoderPath?: string, EncoderProtocol?: "File"|"Http"|"Rtmp"|"Rtsp"|"Udp"|"Rtp"|"Ftp", Type?: "Default"|"Grouping"|"Placeholder", Container?: string, Size?: int, Name?: string, IsRemote?: bool, ETag?: string, RunTimeTicks?: int, ReadAtNativeFramerate?: bool, IgnoreDts?: bool, IgnoreIndex?: bool, GenPtsInput?: bool, SupportsTranscoding?: bool, SupportsDirectStream?: bool, SupportsDirectPlay?: bool, IsInfiniteStream?: bool, UseMostCompatibleTranscodingProfile?: bool, RequiresOpening?: bool, OpenToken?: string, RequiresClosing?: bool, LiveStreamId?: string, BufferMs?: int, RequiresLooping?: bool, SupportsProbing?: bool, VideoType?: "VideoFile"|"Iso"|"Dvd"|"BluRay", IsoType?: "Dvd"|"BluRay", Video3DFormat?: "HalfSideBySide"|"FullSideBySide"|"FullTopAndBottom"|"HalfTopAndBottom"|"MVC", MediaStreams?: list, MediaAttachments?: list, Formats?: list, Bitrate?: int, FallbackMaxStreamingBitrate?: int, Timestamp?: "None"|"Zero"|"Valid", RequiredHttpHeaders?: record, TranscodingUrl?: string, TranscodingSubProtocol?: "http"|"hls", TranscodingContainer?: string, AnalyzeDurationMs?: int, DefaultAudioStreamIndex?: int, DefaultSubtitleStreamIndex?: int, HasSegments?: bool}
  --CriticRating: float # Gets or sets the critic rating. (nullable, format: float)
  --ProductionLocations: list # nullable
  --Path: string # Gets or sets the path. (nullable)
  --EnableMediaSourceDisplay: oneof<nothing, bool> # nullable
  --OfficialRating: string # Gets or sets the official rating. (nullable)
  --CustomRating: string # Gets or sets the custom rating. (nullable)
  --ChannelId: string # Gets or sets the channel identifier. (nullable, format: uuid)
  --ChannelName: string # nullable
  --Overview: string # Gets or sets the overview. (nullable)
  --Taglines: list # Gets or sets the taglines. (nullable)
  --Genres: list # Gets or sets the genres. (nullable)
  --CommunityRating: float # Gets or sets the community rating. (nullable, format: float)
  --CumulativeRunTimeTicks: int # Gets or sets the cumulative run time ticks. (nullable, format: int64)
  --RunTimeTicks: int # Gets or sets the run time ticks. (nullable, format: int64)
  --PlayAccess: any@PlayAccess-completer # Gets or sets the play access. (nullable)
  --AspectRatio: string # Gets or sets the aspect ratio. (nullable)
  --ProductionYear: int # Gets or sets the production year. (nullable, format: int32)
  --IsPlaceHolder: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is place holder. (nullable)
  --Number: string # Gets or sets the number. (nullable)
  --ChannelNumber: string # nullable
  --IndexNumber: int # Gets or sets the index number. (nullable, format: int32)
  --IndexNumberEnd: int # Gets or sets the index number end. (nullable, format: int32)
  --ParentIndexNumber: int # Gets or sets the parent index number. (nullable, format: int32)
  --RemoteTrailers: list # Gets or sets the trailer urls. (nullable) — item shape: {Url?: string, Name?: string}
  --ProviderIds: record # Gets or sets the provider ids. (nullable)
  --IsHD: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is HD. (nullable)
  --IsFolder: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is folder. (nullable)
  --ParentId: string # Gets or sets the parent id. (nullable, format: uuid)
  --Type: any@Type-completer # The base item kind.
  --People: list # Gets or sets the people. (nullable) — item shape: {Name?: string, Id?: string, Role?: string, Type?: "Unknown"|"Actor"|"Director"|"Composer"|"Writer"|"GuestStar"|"Producer"|"Conductor"|"Lyricist"|"Arranger"|"Engineer"|"Mixer"|"Remixer"|"Creator"|"Artist"|"AlbumArtist"|"Author"|"Illustrator"|"Penciller"|"Inker"|"Colorist"|"Letterer"|"CoverArtist"|"Editor"|"Translator", PrimaryImageTag?: string, ImageBlurHashes?: record}
  --Studios: list # Gets or sets the studios. (nullable) — item shape: {Name?: string, Id?: string}
  --GenreItems: list # nullable — item shape: {Name?: string, Id?: string}
  --ParentLogoItemId: string # Gets or sets whether the item has a logo, this will hold the Id of the Parent that has one. (nullable, format: uuid)
  --ParentBackdropItemId: string # Gets or sets whether the item has any backdrops, this will hold the Id of the Parent that has one. (nullable, format: uuid)
  --ParentBackdropImageTags: list # Gets or sets the parent backdrop image tags. (nullable)
  --LocalTrailerCount: int # Gets or sets the local trailer count. (nullable, format: int32)
  --UserData: any # Gets or sets the user data for this item based on the user it's being requested for. (nullable)
  --RecursiveItemCount: int # Gets or sets the recursive item count. (nullable, format: int32)
  --ChildCount: int # Gets or sets the child count. (nullable, format: int32)
  --SeriesName: string # Gets or sets the name of the series. (nullable)
  --SeriesId: string # Gets or sets the series id. (nullable, format: uuid)
  --SeasonId: string # Gets or sets the season identifier. (nullable, format: uuid)
  --SpecialFeatureCount: int # Gets or sets the special feature count. (nullable, format: int32)
  --DisplayPreferencesId: string # Gets or sets the display preferences id. (nullable)
  --Status: string # Gets or sets the status. (nullable)
  --AirTime: string # Gets or sets the air time. (nullable)
  --AirDays: list # Gets or sets the air days. (nullable)
  --Tags: list # Gets or sets the tags. (nullable)
  --PrimaryImageAspectRatio: float # Gets or sets the primary image aspect ratio, after image enhancements. (nullable, format: double)
  --Artists: list # Gets or sets the artists. (nullable)
  --ArtistItems: list # Gets or sets the artist items. (nullable) — item shape: {Name?: string, Id?: string}
  --Album: string # Gets or sets the album. (nullable)
  --CollectionType: any@CollectionType-completer # Gets or sets the type of the collection. (nullable)
  --DisplayOrder: string # Gets or sets the display order. (nullable)
  --AlbumId: string # Gets or sets the album id. (nullable, format: uuid)
  --AlbumPrimaryImageTag: string # Gets or sets the album image tag. (nullable)
  --SeriesPrimaryImageTag: string # Gets or sets the series primary image tag. (nullable)
  --AlbumArtist: string # Gets or sets the album artist. (nullable)
  --AlbumArtists: list # Gets or sets the album artists. (nullable) — item shape: {Name?: string, Id?: string}
  --SeasonName: string # Gets or sets the name of the season. (nullable)
  --MediaStreams: list # Gets or sets the media streams. (nullable) — item shape: {Codec?: string, CodecTag?: string, Language?: string, ColorRange?: string, ColorSpace?: string, ColorTransfer?: string, ColorPrimaries?: string, DvVersionMajor?: int, DvVersionMinor?: int, DvProfile?: int, DvLevel?: int, RpuPresentFlag?: int, ElPresentFlag?: int, BlPresentFlag?: int, DvBlSignalCompatibilityId?: int, Rotation?: int, Comment?: string, TimeBase?: string, CodecTimeBase?: string, Title?: string, Hdr10PlusPresentFlag?: bool, LocalizedUndefined?: string, LocalizedDefault?: string, LocalizedForced?: string, LocalizedExternal?: string, LocalizedHearingImpaired?: string, NalLengthSize?: string, IsInterlaced?: bool, IsAVC?: bool, ChannelLayout?: string, BitRate?: int, BitDepth?: int, RefFrames?: int, PacketLength?: int, Channels?: int, SampleRate?: int, IsDefault?: bool, IsForced?: bool, IsHearingImpaired?: bool, Height?: int, Width?: int, AverageFrameRate?: float, RealFrameRate?: float, Profile?: string, Type?: "Audio"|"Video"|"Subtitle"|"EmbeddedImage"|"Data"|"Lyric", AspectRatio?: string, Index?: int, Score?: int, IsExternal?: bool, DeliveryMethod?: "Encode"|"Embed"|"External"|"Hls"|"Drop", DeliveryUrl?: string, IsExternalUrl?: bool, SupportsExternalStream?: bool, Path?: string, PixelFormat?: string, Level?: float, IsAnamorphic?: bool}
  --VideoType: any@VideoType-completer # Gets or sets the type of the video. (nullable)
  --PartCount: int # Gets or sets the part count. (nullable, format: int32)
  --MediaSourceCount: int # nullable, format: int32
  --ImageTags: record # Gets or sets the image tags. (nullable)
  --BackdropImageTags: list # Gets or sets the backdrop image tags. (nullable)
  --ScreenshotImageTags: list # Gets or sets the screenshot image tags. (nullable)
  --ParentLogoImageTag: string # Gets or sets the parent logo image tag. (nullable)
  --ParentArtItemId: string # Gets or sets whether the item has fan art, this will hold the Id of the Parent that has one. (nullable, format: uuid)
  --ParentArtImageTag: string # Gets or sets the parent art image tag. (nullable)
  --SeriesThumbImageTag: string # Gets or sets the series thumb image tag. (nullable)
  --ImageBlurHashes: record # Gets or sets the blurhashes for the image tags. Maps image type to dictionary mapping image tag to blurhash value. (nullable) — shape: {Primary?: record, Art?: record, Backdrop?: record, Banner?: record, Logo?: record, Thumb?: record, Disc?: record, Box?: record, Screenshot?: record, Menu?: record, Chapter?: record, BoxRear?: record, Profile?: record}
  --SeriesStudio: string # Gets or sets the series studio. (nullable)
  --ParentThumbItemId: string # Gets or sets the parent thumb item id. (nullable, format: uuid)
  --ParentThumbImageTag: string # Gets or sets the parent thumb image tag. (nullable)
  --ParentPrimaryImageItemId: string # Gets or sets the parent primary image item identifier. (nullable, format: uuid)
  --ParentPrimaryImageTag: string # Gets or sets the parent primary image tag. (nullable)
  --Chapters: list # Gets or sets the chapters. (nullable) — item shape: {StartPositionTicks?: int, Name?: string, ImagePath?: string, ImageDateModified?: string, ImageTag?: string}
  --Trickplay: record # Gets or sets the trickplay manifest. (nullable)
  --LocationType: any@LocationType-completer # Gets or sets the type of the location. (nullable)
  --IsoType: any@IsoType-completer # Gets or sets the type of the iso. (nullable)
  --MediaType: any@MediaType-completer # Media types. (default: Unknown)
  --EndDate: string # Gets or sets the end date. (nullable, format: date-time)
  --LockedFields: list # Gets or sets the locked fields. (nullable)
  --TrailerCount: int # Gets or sets the trailer count. (nullable, format: int32)
  --MovieCount: int # Gets or sets the movie count. (nullable, format: int32)
  --SeriesCount: int # Gets or sets the series count. (nullable, format: int32)
  --ProgramCount: int # nullable, format: int32
  --EpisodeCount: int # Gets or sets the episode count. (nullable, format: int32)
  --SongCount: int # Gets or sets the song count. (nullable, format: int32)
  --AlbumCount: int # Gets or sets the album count. (nullable, format: int32)
  --ArtistCount: int # nullable, format: int32
  --MusicVideoCount: int # Gets or sets the music video count. (nullable, format: int32)
  --LockData: oneof<nothing, bool> # Gets or sets a value indicating whether [enable internet providers]. (nullable)
  --Width: int # nullable, format: int32
  --Height: int # nullable, format: int32
  --CameraMake: string # nullable
  --CameraModel: string # nullable
  --Software: string # nullable
  --ExposureTime: float # nullable, format: double
  --FocalLength: float # nullable, format: double
  --ImageOrientation: any@ImageOrientation-completer # nullable
  --Aperture: float # nullable, format: double
  --ShutterSpeed: float # nullable, format: double
  --Latitude: float # nullable, format: double
  --Longitude: float # nullable, format: double
  --Altitude: float # nullable, format: double
  --IsoSpeedRating: int # nullable, format: int32
  --SeriesTimerId: string # Gets or sets the series timer identifier. (nullable)
  --ProgramId: string # Gets or sets the program identifier. (nullable)
  --ChannelPrimaryImageTag: string # Gets or sets the channel primary image tag. (nullable)
  --StartDate: string # Gets or sets the start date of the recording, in UTC. (nullable, format: date-time)
  --CompletionPercentage: float # Gets or sets the completion percentage. (nullable, format: double)
  --IsRepeat: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is repeat. (nullable)
  --EpisodeTitle: string # Gets or sets the episode title. (nullable)
  --ChannelType: any@ChannelType-completer # Gets or sets the type of the channel. (nullable)
  --Audio: any@Audio-completer # Gets or sets the audio. (nullable)
  --IsMovie: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is movie. (nullable)
  --IsSports: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is sports. (nullable)
  --IsSeries: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is series. (nullable)
  --IsLive: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is live. (nullable)
  --IsNews: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is news. (nullable)
  --IsKids: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is kids. (nullable)
  --IsPremiere: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is premiere. (nullable)
  --TimerId: string # Gets or sets the timer identifier. (nullable)
  --NormalizationGain: float # Gets or sets the gain required for audio normalization. (nullable, format: float)
  --CurrentProgram: any # Gets or sets the current program. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)")
  let body = {Name: $Name, OriginalTitle: $OriginalTitle, ServerId: $ServerId, Id: $Id, Etag: $Etag, SourceType: $SourceType, PlaylistItemId: $PlaylistItemId, DateCreated: $DateCreated, DateLastMediaAdded: $DateLastMediaAdded, ExtraType: $ExtraType, AirsBeforeSeasonNumber: $AirsBeforeSeasonNumber, AirsAfterSeasonNumber: $AirsAfterSeasonNumber, AirsBeforeEpisodeNumber: $AirsBeforeEpisodeNumber, CanDelete: $CanDelete, CanDownload: $CanDownload, HasLyrics: $HasLyrics, HasSubtitles: $HasSubtitles, PreferredMetadataLanguage: $PreferredMetadataLanguage, PreferredMetadataCountryCode: $PreferredMetadataCountryCode, Container: $Container, SortName: $SortName, ForcedSortName: $ForcedSortName, Video3DFormat: $Video3DFormat, PremiereDate: $PremiereDate, ExternalUrls: $ExternalUrls, MediaSources: $MediaSources, CriticRating: $CriticRating, ProductionLocations: $ProductionLocations, Path: $Path, EnableMediaSourceDisplay: $EnableMediaSourceDisplay, OfficialRating: $OfficialRating, CustomRating: $CustomRating, ChannelId: $ChannelId, ChannelName: $ChannelName, Overview: $Overview, Taglines: $Taglines, Genres: $Genres, CommunityRating: $CommunityRating, CumulativeRunTimeTicks: $CumulativeRunTimeTicks, RunTimeTicks: $RunTimeTicks, PlayAccess: $PlayAccess, AspectRatio: $AspectRatio, ProductionYear: $ProductionYear, IsPlaceHolder: $IsPlaceHolder, Number: $Number, ChannelNumber: $ChannelNumber, IndexNumber: $IndexNumber, IndexNumberEnd: $IndexNumberEnd, ParentIndexNumber: $ParentIndexNumber, RemoteTrailers: $RemoteTrailers, ProviderIds: $ProviderIds, IsHD: $IsHD, IsFolder: $IsFolder, ParentId: $ParentId, Type: $Type, People: $People, Studios: $Studios, GenreItems: $GenreItems, ParentLogoItemId: $ParentLogoItemId, ParentBackdropItemId: $ParentBackdropItemId, ParentBackdropImageTags: $ParentBackdropImageTags, LocalTrailerCount: $LocalTrailerCount, UserData: $UserData, RecursiveItemCount: $RecursiveItemCount, ChildCount: $ChildCount, SeriesName: $SeriesName, SeriesId: $SeriesId, SeasonId: $SeasonId, SpecialFeatureCount: $SpecialFeatureCount, DisplayPreferencesId: $DisplayPreferencesId, Status: $Status, AirTime: $AirTime, AirDays: $AirDays, Tags: $Tags, PrimaryImageAspectRatio: $PrimaryImageAspectRatio, Artists: $Artists, ArtistItems: $ArtistItems, Album: $Album, CollectionType: $CollectionType, DisplayOrder: $DisplayOrder, AlbumId: $AlbumId, AlbumPrimaryImageTag: $AlbumPrimaryImageTag, SeriesPrimaryImageTag: $SeriesPrimaryImageTag, AlbumArtist: $AlbumArtist, AlbumArtists: $AlbumArtists, SeasonName: $SeasonName, MediaStreams: $MediaStreams, VideoType: $VideoType, PartCount: $PartCount, MediaSourceCount: $MediaSourceCount, ImageTags: $ImageTags, BackdropImageTags: $BackdropImageTags, ScreenshotImageTags: $ScreenshotImageTags, ParentLogoImageTag: $ParentLogoImageTag, ParentArtItemId: $ParentArtItemId, ParentArtImageTag: $ParentArtImageTag, SeriesThumbImageTag: $SeriesThumbImageTag, ImageBlurHashes: $ImageBlurHashes, SeriesStudio: $SeriesStudio, ParentThumbItemId: $ParentThumbItemId, ParentThumbImageTag: $ParentThumbImageTag, ParentPrimaryImageItemId: $ParentPrimaryImageItemId, ParentPrimaryImageTag: $ParentPrimaryImageTag, Chapters: $Chapters, Trickplay: $Trickplay, LocationType: $LocationType, IsoType: $IsoType, MediaType: $MediaType, EndDate: $EndDate, LockedFields: $LockedFields, TrailerCount: $TrailerCount, MovieCount: $MovieCount, SeriesCount: $SeriesCount, ProgramCount: $ProgramCount, EpisodeCount: $EpisodeCount, SongCount: $SongCount, AlbumCount: $AlbumCount, ArtistCount: $ArtistCount, MusicVideoCount: $MusicVideoCount, LockData: $LockData, Width: $Width, Height: $Height, CameraMake: $CameraMake, CameraModel: $CameraModel, Software: $Software, ExposureTime: $ExposureTime, FocalLength: $FocalLength, ImageOrientation: $ImageOrientation, Aperture: $Aperture, ShutterSpeed: $ShutterSpeed, Latitude: $Latitude, Longitude: $Longitude, Altitude: $Altitude, IsoSpeedRating: $IsoSpeedRating, SeriesTimerId: $SeriesTimerId, ProgramId: $ProgramId, ChannelPrimaryImageTag: $ChannelPrimaryImageTag, StartDate: $StartDate, CompletionPercentage: $CompletionPercentage, IsRepeat: $IsRepeat, EpisodeTitle: $EpisodeTitle, ChannelType: $ChannelType, Audio: $Audio, IsMovie: $IsMovie, IsSports: $IsSports, IsSeries: $IsSeries, IsLive: $IsLive, IsNews: $IsNews, IsKids: $IsKids, IsPremiere: $IsPremiere, TimerId: $TimerId, NormalizationGain: $NormalizationGain, CurrentProgram: $CurrentProgram} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Deletes an item from the library and filesystem.
#
# DELETE /Items/{itemId}
# operationId: DeleteItem
export def "items DeleteItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an item from a user's library.
#
# GET /Items/{itemId}
# operationId: GetItem
export def "items GetItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates an item's content type.
#
# POST /Items/{itemId}/ContentType
# operationId: UpdateItemContentType
export def "items-content-type UpdateItemContentType" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --contentType: string # The content type of the item.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "contentType" $contentType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/ContentType" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets metadata editor info for an item.
#
# GET /Items/{itemId}/MetadataEditor
# operationId: GetMetadataEditorInfo
export def "items-metadata-editor GetMetadataEditorInfo" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<ParentalRatingOptions: table<Name: string, Value: int, RatingScore: record>, Countries: table<Name: string, DisplayName: string, TwoLetterISORegionName: string, ThreeLetterISORegionName: string>, Cultures: table<Name: string, DisplayName: string, TwoLetterISOLanguageName: string, ThreeLetterISOLanguageName: string, ThreeLetterISOLanguageNames: list>, ExternalIdInfos: table<Name: string, Key: string, Type: record>, ContentType: record, ContentTypeOptions: table<Name: string, Value: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/MetadataEditor")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets similar items.
#
# GET /Albums/{itemId}/Similar
# operationId: GetSimilarAlbums
export def "albums-similar GetSimilarAlbums" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --excludeArtistIds: list # Exclude artist ids.
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Albums/($itemId)/Similar" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets similar items.
#
# GET /Artists/{itemId}/Similar
# operationId: GetSimilarArtists
export def "artists-similar GetSimilarArtists" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --excludeArtistIds: list # Exclude artist ids.
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Artists/($itemId)/Similar" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all parents of an item.
#
# GET /Items/{itemId}/Ancestors
# operationId: GetAncestors
export def "items-ancestors GetAncestors" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
]: nothing -> table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Ancestors" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets critic review for an item.
#
# GET /Items/{itemId}/CriticReviews
# DEPRECATED
# operationId: GetCriticReviews
@deprecated
export def "items-critic-reviews GetCriticReviews" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/CriticReviews")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Downloads item media.
#
# GET /Items/{itemId}/Download
# operationId: GetDownload
export def "items-download GetDownload" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/Download")
  let accept_val = ($accept | default "video/*")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the original file of an item.
#
# GET /Items/{itemId}/File
# operationId: GetFile
export def "items-file GetFile" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer-3 # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/File")
  let accept_val = ($accept | default "video/*")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets similar items.
#
# GET /Items/{itemId}/Similar
# operationId: GetSimilarItems
export def "items-similar GetSimilarItems" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --excludeArtistIds: list # Exclude artist ids.
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Similar" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get theme songs and videos for an item.
#
# GET /Items/{itemId}/ThemeMedia
# operationId: GetThemeMedia
export def "items-theme-media GetThemeMedia" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --inheritFromParent: oneof<nothing, bool> # Optional. Determines whether or not parent items should be searched for theme media. (default: false)
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
  --sortOrder: list # Optional. Sort Order - Ascending, Descending.
]: nothing -> record<ThemeVideosResult: record<Items: list<record>, TotalRecordCount: int, StartIndex: int, OwnerId: string>, ThemeSongsResult: record<Items: list<record>, TotalRecordCount: int, StartIndex: int, OwnerId: string>, SoundtrackSongsResult: record<Items: list<record>, TotalRecordCount: int, StartIndex: int, OwnerId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "inheritFromParent" $inheritFromParent "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/ThemeMedia" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get theme songs for an item.
#
# GET /Items/{itemId}/ThemeSongs
# operationId: GetThemeSongs
export def "items-theme-songs GetThemeSongs" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --inheritFromParent: oneof<nothing, bool> # Optional. Determines whether or not parent items should be searched for theme media. (default: false)
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
  --sortOrder: list # Optional. Sort Order - Ascending, Descending.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int, OwnerId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "inheritFromParent" $inheritFromParent "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/ThemeSongs" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get theme videos for an item.
#
# GET /Items/{itemId}/ThemeVideos
# operationId: GetThemeVideos
export def "items-theme-videos GetThemeVideos" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --inheritFromParent: oneof<nothing, bool> # Optional. Determines whether or not parent items should be searched for theme media. (default: false)
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
  --sortOrder: list # Optional. Sort Order - Ascending, Descending.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int, OwnerId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "inheritFromParent" $inheritFromParent "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/ThemeVideos" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get item counts.
#
# GET /Items/Counts
# operationId: GetItemCounts
export def "items-counts GetItemCounts" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Get counts from a specific user's library. (format: uuid)
  --isFavorite: oneof<nothing, bool> # Optional. Get counts of favorite items.
]: nothing -> record<MovieCount: int, SeriesCount: int, EpisodeCount: int, ArtistCount: int, ProgramCount: int, TrailerCount: int, SongCount: int, AlbumCount: int, MusicVideoCount: int, BoxSetCount: int, BookCount: int, ItemCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "isFavorite" $isFavorite "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Items/Counts" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the library options info.
#
# GET /Libraries/AvailableOptions
# operationId: GetLibraryOptionsInfo
export def "libraries-available-options GetLibraryOptionsInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --libraryContentType: string@libraryContentType-completer # Library content type.
  --isNewLibrary: oneof<nothing, bool> # Whether this is a new library. (default: false)
]: nothing -> record<MetadataSavers: table<Name: string, DefaultEnabled: bool>, MetadataReaders: table<Name: string, DefaultEnabled: bool>, SubtitleFetchers: table<Name: string, DefaultEnabled: bool>, LyricFetchers: table<Name: string, DefaultEnabled: bool>, MediaSegmentProviders: table<Name: string, DefaultEnabled: bool>, TypeOptions: table<Type: string, MetadataFetchers: list, ImageFetchers: list, SupportedImageTypes: list, DefaultImageOptions: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "libraryContentType" $libraryContentType "scalar") (serialize-qp "isNewLibrary" $isNewLibrary "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Libraries/AvailableOptions" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that new movies have been added by an external source.
#
# POST /Library/Media/Updated
# operationId: PostUpdatedMedia
# --Updates item shape: {Path?: string, UpdateType?: string}
export def "library-media-updated PostUpdatedMedia" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Updates: list # Gets or sets the list of updates. — item shape: {Path?: string, UpdateType?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Library/Media/Updated")
  let body = {Updates: $Updates} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets all user media folders.
#
# GET /Library/MediaFolders
# operationId: GetMediaFolders
export def "library-media-folders GetMediaFolders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --isHidden: oneof<nothing, bool> # Optional. Filter by folders that are marked hidden, or not.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "isHidden" $isHidden "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/MediaFolders" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that new movies have been added by an external source.
#
# POST /Library/Movies/Added
# operationId: PostAddedMovies
export def "library-movies-added PostAddedMovies" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tmdbId: string # The tmdbId.
  --imdbId: string # The imdbId.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tmdbId" $tmdbId "scalar") (serialize-qp "imdbId" $imdbId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/Movies/Added" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that new movies have been added by an external source.
#
# POST /Library/Movies/Updated
# operationId: PostUpdatedMovies
export def "library-movies-updated PostUpdatedMovies" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tmdbId: string # The tmdbId.
  --imdbId: string # The imdbId.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tmdbId" $tmdbId "scalar") (serialize-qp "imdbId" $imdbId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/Movies/Updated" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of physical paths from virtual folders.
#
# GET /Library/PhysicalPaths
# operationId: GetPhysicalPaths
export def "library-physical-paths GetPhysicalPaths" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> list<string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Library/PhysicalPaths")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Starts a library scan.
#
# POST /Library/Refresh
# operationId: RefreshLibrary
export def "library-refresh RefreshLibrary" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Library/Refresh")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that new episodes of a series have been added by an external source.
#
# POST /Library/Series/Added
# operationId: PostAddedSeries
export def "library-series-added PostAddedSeries" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tvdbId: string # The tvdbId.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tvdbId" $tvdbId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/Series/Added" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that new episodes of a series have been added by an external source.
#
# POST /Library/Series/Updated
# operationId: PostUpdatedSeries
export def "library-series-updated PostUpdatedSeries" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --tvdbId: string # The tvdbId.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "tvdbId" $tvdbId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/Series/Updated" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets similar items.
#
# GET /Movies/{itemId}/Similar
# operationId: GetSimilarMovies
export def "movies-similar GetSimilarMovies" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --excludeArtistIds: list # Exclude artist ids.
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Movies/($itemId)/Similar" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets similar items.
#
# GET /Shows/{itemId}/Similar
# operationId: GetSimilarShows
export def "shows-similar GetSimilarShows" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --excludeArtistIds: list # Exclude artist ids.
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Shows/($itemId)/Similar" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets similar items.
#
# GET /Trailers/{itemId}/Similar
# operationId: GetSimilarTrailers
export def "trailers-similar GetSimilarTrailers" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --excludeArtistIds: list # Exclude artist ids.
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Trailers/($itemId)/Similar" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all virtual folders.
#
# GET /Library/VirtualFolders
# operationId: GetVirtualFolders
export def "library-virtual-folders GetVirtualFolders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Locations: list<string>, CollectionType: record, LibraryOptions: record<Enabled: bool, EnablePhotos: bool, EnableRealtimeMonitor: bool, EnableLUFSScan: bool, EnableChapterImageExtraction: bool, ExtractChapterImagesDuringLibraryScan: bool, EnableTrickplayImageExtraction: bool, ExtractTrickplayImagesDuringLibraryScan: bool, PathInfos: list, SaveLocalMetadata: bool, EnableInternetProviders: bool, EnableAutomaticSeriesGrouping: bool, EnableEmbeddedTitles: bool, EnableEmbeddedExtrasTitles: bool, EnableEmbeddedEpisodeInfos: bool, AutomaticRefreshIntervalDays: int, PreferredMetadataLanguage: string, MetadataCountryCode: string, SeasonZeroDisplayName: string, MetadataSavers: list, DisabledLocalMetadataReaders: list, LocalMetadataReaderOrder: list, DisabledSubtitleFetchers: list, SubtitleFetcherOrder: list, DisabledMediaSegmentProviders: list, MediaSegmentProviderOrder: list, SkipSubtitlesIfEmbeddedSubtitlesPresent: bool, SkipSubtitlesIfAudioTrackMatches: bool, SubtitleDownloadLanguages: list, RequirePerfectSubtitleMatch: bool, SaveSubtitlesWithMedia: bool, SaveLyricsWithMedia: bool, SaveTrickplayWithMedia: bool, DisabledLyricFetchers: list, LyricFetcherOrder: list, PreferNonstandardArtistsTag: bool, UseCustomTagDelimiters: bool, CustomTagDelimiters: list, DelimiterWhitelist: list, AutomaticallyAddToCollection: bool, AllowEmbeddedSubtitles: record, TypeOptions: list>, ItemId: string, PrimaryImageItemId: string, RefreshProgress: float, RefreshStatus: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Library/VirtualFolders")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Adds a virtual folder.
#
# POST /Library/VirtualFolders
# operationId: AddVirtualFolder
export def "library-virtual-folders AddVirtualFolder" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --name: string # The name of the virtual folder.
  --collectionType: string@collectionType-completer # The type of the collection.
  --paths: list # The paths of the virtual folder.
  --refreshLibrary: oneof<nothing, bool> # Whether to refresh the library. (default: false)
  --LibraryOptions: any # Gets or sets library options. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "collectionType" $collectionType "scalar") (serialize-qp "paths" $paths "multi") (serialize-qp "refreshLibrary" $refreshLibrary "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/VirtualFolders" $qp)
  let body = {LibraryOptions: $LibraryOptions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Removes a virtual folder.
#
# DELETE /Library/VirtualFolders
# operationId: RemoveVirtualFolder
export def "library-virtual-folders RemoveVirtualFolder" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --name: string # The name of the folder.
  --refreshLibrary: oneof<nothing, bool> # Whether to refresh the library. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "refreshLibrary" $refreshLibrary "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/VirtualFolders" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update library options.
#
# POST /Library/VirtualFolders/LibraryOptions
# operationId: UpdateLibraryOptions
export def "library-virtual-folders-library-options UpdateLibraryOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Id: string # Gets or sets the library item id. (format: uuid)
  --LibraryOptions: any # Gets or sets library options. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Library/VirtualFolders/LibraryOptions")
  let body = {Id: $Id, LibraryOptions: $LibraryOptions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Renames a virtual folder.
#
# POST /Library/VirtualFolders/Name
# operationId: RenameVirtualFolder
export def "library-virtual-folders-name RenameVirtualFolder" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --name: string # The name of the virtual folder.
  --newName: string # The new name.
  --refreshLibrary: oneof<nothing, bool> # Whether to refresh the library. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "newName" $newName "scalar") (serialize-qp "refreshLibrary" $refreshLibrary "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/VirtualFolders/Name" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a media path to a library.
#
# POST /Library/VirtualFolders/Paths
# operationId: AddMediaPath
export def "library-virtual-folders-paths AddMediaPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --refreshLibrary: oneof<nothing, bool> # Whether to refresh the library. (default: false)
  Name: string # Gets or sets the name of the library.
  --Path: string # Gets or sets the path to add. (nullable)
  --PathInfo: any # Gets or sets the path info. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "refreshLibrary" $refreshLibrary "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/VirtualFolders/Paths" $qp)
  let body = {Name: $Name, Path: $Path, PathInfo: $PathInfo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Remove a media path.
#
# DELETE /Library/VirtualFolders/Paths
# operationId: RemoveMediaPath
export def "library-virtual-folders-paths RemoveMediaPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --name: string # The name of the library.
  --path: string # The path to remove.
  --refreshLibrary: oneof<nothing, bool> # Whether to refresh the library. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "path" $path "scalar") (serialize-qp "refreshLibrary" $refreshLibrary "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Library/VirtualFolders/Paths" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a media path.
#
# POST /Library/VirtualFolders/Paths/Update
# operationId: UpdateMediaPath
export def "library-virtual-folders-paths-update UpdateMediaPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  Name: string # Gets or sets the library name.
  PathInfo: any # Gets or sets library folder path information.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Library/VirtualFolders/Paths/Update")
  let body = {Name: $Name, PathInfo: $PathInfo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get channel mapping options.
#
# GET /LiveTv/ChannelMappingOptions
# operationId: GetChannelMappingOptions
export def "live-tv-channel-mapping-options GetChannelMappingOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --providerId: string # Provider id.
]: nothing -> record<TunerChannels: table<Name: string, ProviderChannelName: string, ProviderChannelId: string, Id: string>, ProviderChannels: table<Name: string, Id: string>, Mappings: table<Name: string, Value: string>, ProviderName: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "providerId" $providerId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/ChannelMappingOptions" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set channel mappings.
#
# POST /LiveTv/ChannelMappings
# operationId: SetChannelMapping
export def "live-tv-channel-mappings SetChannelMapping" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  ProviderId: string # Gets or sets the provider id.
  TunerChannelId: string # Gets or sets the tuner channel id.
  ProviderChannelId: string # Gets or sets the provider channel id.
]: any -> record<Name: string, ProviderChannelName: string, ProviderChannelId: string, Id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/ChannelMappings")
  let body = {ProviderId: $ProviderId, TunerChannelId: $TunerChannelId, ProviderChannelId: $ProviderChannelId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets available live tv channels.
#
# GET /LiveTv/Channels
# operationId: GetLiveTvChannels
export def "live-tv-channels GetLiveTvChannels" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --type: string@type-completer # Optional. Filter by channel type.
  --userId: string # Optional. Filter by user and attach user data. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --isMovie: oneof<nothing, bool> # Optional. Filter for movies.
  --isSeries: oneof<nothing, bool> # Optional. Filter for series.
  --isNews: oneof<nothing, bool> # Optional. Filter for news.
  --isKids: oneof<nothing, bool> # Optional. Filter for kids.
  --isSports: oneof<nothing, bool> # Optional. Filter for sports.
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --isFavorite: oneof<nothing, bool> # Optional. Filter by channels that are favorites, or not.
  --isLiked: oneof<nothing, bool> # Optional. Filter by channels that are liked, or not.
  --isDisliked: oneof<nothing, bool> # Optional. Filter by channels that are disliked, or not.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # "Optional. The image types to include in the output.
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --sortBy: list # Optional. Key to sort by.
  --sortOrder: string@sortOrder-completer # Optional. Sort order.
  --enableFavoriteSorting: oneof<nothing, bool> # Optional. Incorporate favorite and like status into channel sorting. (default: false)
  --addCurrentProgram: oneof<nothing, bool> # Optional. Adds current program info to each channel. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "type" $type "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "isLiked" $isLiked "scalar") (serialize-qp "isDisliked" $isDisliked "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "enableFavoriteSorting" $enableFavoriteSorting "scalar") (serialize-qp "addCurrentProgram" $addCurrentProgram "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Channels" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a live tv channel.
#
# GET /LiveTv/Channels/{channelId}
# operationId: GetChannel
export def "live-tv-channels GetChannel" [
  channelId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/LiveTv/Channels/($channelId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get guide info.
#
# GET /LiveTv/GuideInfo
# operationId: GetGuideInfo
export def "live-tv-guide-info GetGuideInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<StartDate: string, EndDate: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/GuideInfo")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available live tv services.
#
# GET /LiveTv/Info
# operationId: GetLiveTvInfo
export def "live-tv-info GetLiveTvInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Services: table<Name: string, HomePageUrl: string, Status: record, StatusMessage: string, Version: string, HasUpdateAvailable: bool, IsVisible: bool, Tuners: list>, IsEnabled: bool, EnabledUsers: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/Info")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Adds a listings provider.
#
# POST /LiveTv/ListingProviders
# operationId: AddListingProvider
# --ChannelMappings item shape: {Name?: string, Value?: string}
export def "live-tv-listing-providers AddListingProvider" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --pw: string # Password.
  --validateListings: oneof<nothing, bool> # Validate listings. (default: false)
  --validateLogin: oneof<nothing, bool> # Validate login. (default: false)
  --Id: string # nullable
  --Type: string # nullable
  --Username: string # nullable
  --Password: string # nullable
  --ListingsId: string # nullable
  --ZipCode: string # nullable
  --Country: string # nullable
  --Path: string # nullable
  --EnabledTuners: list # nullable
  --EnableAllTuners: oneof<nothing, bool>
  --NewsCategories: list # nullable
  --SportsCategories: list # nullable
  --KidsCategories: list # nullable
  --MovieCategories: list # nullable
  --ChannelMappings: list # nullable — item shape: {Name?: string, Value?: string}
  --MoviePrefix: string # nullable
  --PreferredLanguage: string # nullable
  --UserAgent: string # nullable
]: any -> record<Id: string, Type: string, Username: string, Password: string, ListingsId: string, ZipCode: string, Country: string, Path: string, EnabledTuners: list<string>, EnableAllTuners: bool, NewsCategories: list<string>, SportsCategories: list<string>, KidsCategories: list<string>, MovieCategories: list<string>, ChannelMappings: table<Name: string, Value: string>, MoviePrefix: string, PreferredLanguage: string, UserAgent: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "pw" $pw "scalar") (serialize-qp "validateListings" $validateListings "scalar") (serialize-qp "validateLogin" $validateLogin "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/ListingProviders" $qp)
  let body = {Id: $Id, Type: $Type, Username: $Username, Password: $Password, ListingsId: $ListingsId, ZipCode: $ZipCode, Country: $Country, Path: $Path, EnabledTuners: $EnabledTuners, EnableAllTuners: $EnableAllTuners, NewsCategories: $NewsCategories, SportsCategories: $SportsCategories, KidsCategories: $KidsCategories, MovieCategories: $MovieCategories, ChannelMappings: $ChannelMappings, MoviePrefix: $MoviePrefix, PreferredLanguage: $PreferredLanguage, UserAgent: $UserAgent} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete listing provider.
#
# DELETE /LiveTv/ListingProviders
# operationId: DeleteListingProvider
export def "live-tv-listing-providers DeleteListingProvider" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string # Listing provider id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/ListingProviders" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets default listings provider info.
#
# GET /LiveTv/ListingProviders/Default
# operationId: GetDefaultListingProvider
export def "live-tv-listing-providers-default GetDefaultListingProvider" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Id: string, Type: string, Username: string, Password: string, ListingsId: string, ZipCode: string, Country: string, Path: string, EnabledTuners: list<string>, EnableAllTuners: bool, NewsCategories: list<string>, SportsCategories: list<string>, KidsCategories: list<string>, MovieCategories: list<string>, ChannelMappings: table<Name: string, Value: string>, MoviePrefix: string, PreferredLanguage: string, UserAgent: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/ListingProviders/Default")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available lineups.
#
# GET /LiveTv/ListingProviders/Lineups
# operationId: GetLineups
export def "live-tv-listing-providers-lineups GetLineups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --id: string # Provider id.
  --type: string # Provider type.
  --location: string # Location.
  --country: string # Country.
]: nothing -> table<Name: string, Id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "country" $country "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/ListingProviders/Lineups" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available countries.
#
# GET /LiveTv/ListingProviders/SchedulesDirect/Countries
# operationId: GetSchedulesDirectCountries
export def "live-tv-listing-providers-schedules-direct-countries GetSchedulesDirectCountries" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/ListingProviders/SchedulesDirect/Countries")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a live tv recording stream.
#
# GET /LiveTv/LiveRecordings/{recordingId}/stream
# operationId: GetLiveRecordingFile
export def "live-tv-live-recordings-stream GetLiveRecordingFile" [
  recordingId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/LiveRecordings/($recordingId)/stream")
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a live tv channel stream.
#
# GET /LiveTv/LiveStreamFiles/{streamId}/stream.{container}
# operationId: GetLiveStreamFile
export def "live-tv-live-stream-files-stream-container GetLiveStreamFile" [
  streamId: string
  container: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/LiveStreamFiles/($streamId)/stream.($container)")
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available live tv epgs.
#
# GET /LiveTv/Programs
# operationId: GetLiveTvPrograms
export def "live-tv-programs GetLiveTvPrograms" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --channelIds: list # The channels to return guide information for.
  --userId: string # Optional. Filter by user id. (format: uuid)
  --minStartDate: string # Optional. The minimum premiere start date. (format: date-time)
  --hasAired: oneof<nothing, bool> # Optional. Filter by programs that have completed airing, or not.
  --isAiring: oneof<nothing, bool> # Optional. Filter by programs that are currently airing, or not.
  --maxStartDate: string # Optional. The maximum premiere start date. (format: date-time)
  --minEndDate: string # Optional. The minimum premiere end date. (format: date-time)
  --maxEndDate: string # Optional. The maximum premiere end date. (format: date-time)
  --isMovie: oneof<nothing, bool> # Optional. Filter for movies.
  --isSeries: oneof<nothing, bool> # Optional. Filter for series.
  --isNews: oneof<nothing, bool> # Optional. Filter for news.
  --isKids: oneof<nothing, bool> # Optional. Filter for kids.
  --isSports: oneof<nothing, bool> # Optional. Filter for sports.
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Name, StartDate.
  --sortOrder: list # Sort Order - Ascending,Descending.
  --genres: list # The genres to return guide information for.
  --genreIds: list # The genre ids to return guide information for.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --seriesTimerId: string # Optional. Filter by series timer id.
  --librarySeriesId: string # Optional. Filter by library series id. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableTotalRecordCount: oneof<nothing, bool> # Retrieve total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "channelIds" $channelIds "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "minStartDate" $minStartDate "scalar") (serialize-qp "hasAired" $hasAired "scalar") (serialize-qp "isAiring" $isAiring "scalar") (serialize-qp "maxStartDate" $maxStartDate "scalar") (serialize-qp "minEndDate" $minEndDate "scalar") (serialize-qp "maxEndDate" $maxEndDate "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "genres" $genres "multi") (serialize-qp "genreIds" $genreIds "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "seriesTimerId" $seriesTimerId "scalar") (serialize-qp "librarySeriesId" $librarySeriesId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Programs" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available live tv epgs.
#
# POST /LiveTv/Programs
# operationId: GetPrograms
export def "live-tv-programs GetPrograms" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --ChannelIds: list # Gets or sets the channels to return guide information for. (nullable)
  --UserId: string # Gets or sets optional. Filter by user id. (nullable, format: uuid)
  --MinStartDate: string # Gets or sets the minimum premiere start date. (nullable, format: date-time)
  --HasAired: oneof<nothing, bool> # Gets or sets filter by programs that have completed airing, or not. (nullable)
  --IsAiring: oneof<nothing, bool> # Gets or sets filter by programs that are currently airing, or not. (nullable)
  --MaxStartDate: string # Gets or sets the maximum premiere start date. (nullable, format: date-time)
  --MinEndDate: string # Gets or sets the minimum premiere end date. (nullable, format: date-time)
  --MaxEndDate: string # Gets or sets the maximum premiere end date. (nullable, format: date-time)
  --IsMovie: oneof<nothing, bool> # Gets or sets filter for movies. (nullable)
  --IsSeries: oneof<nothing, bool> # Gets or sets filter for series. (nullable)
  --IsNews: oneof<nothing, bool> # Gets or sets filter for news. (nullable)
  --IsKids: oneof<nothing, bool> # Gets or sets filter for kids. (nullable)
  --IsSports: oneof<nothing, bool> # Gets or sets filter for sports. (nullable)
  --StartIndex: int # Gets or sets the record index to start at. All items with a lower index will be dropped from the results. (nullable, format: int32)
  --Limit: int # Gets or sets the maximum number of records to return. (nullable, format: int32)
  --SortBy: list # Gets or sets specify one or more sort orders, comma delimited. Options: Name, StartDate. (nullable)
  --SortOrder: list # Gets or sets sort order. (nullable)
  --Genres: list # Gets or sets the genres to return guide information for. (nullable)
  --GenreIds: list # Gets or sets the genre ids to return guide information for. (nullable)
  --EnableImages: oneof<nothing, bool> # Gets or sets include image information in output. (nullable)
  --EnableTotalRecordCount: oneof<nothing, bool> # Gets or sets a value indicating whether retrieve total record count. (default: true)
  --ImageTypeLimit: int # Gets or sets the max number of images to return, per image type. (nullable, format: int32)
  --EnableImageTypes: list # Gets or sets the image types to include in the output. (nullable)
  --EnableUserData: oneof<nothing, bool> # Gets or sets include user data. (nullable)
  --SeriesTimerId: string # Gets or sets filter by series timer id. (nullable)
  --LibrarySeriesId: string # Gets or sets filter by library series id. (nullable, format: uuid)
  --Fields: list # Gets or sets specify additional fields of information to return in the output. (nullable)
]: any -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/Programs")
  let body = {ChannelIds: $ChannelIds, UserId: $UserId, MinStartDate: $MinStartDate, HasAired: $HasAired, IsAiring: $IsAiring, MaxStartDate: $MaxStartDate, MinEndDate: $MinEndDate, MaxEndDate: $MaxEndDate, IsMovie: $IsMovie, IsSeries: $IsSeries, IsNews: $IsNews, IsKids: $IsKids, IsSports: $IsSports, StartIndex: $StartIndex, Limit: $Limit, SortBy: $SortBy, SortOrder: $SortOrder, Genres: $Genres, GenreIds: $GenreIds, EnableImages: $EnableImages, EnableTotalRecordCount: $EnableTotalRecordCount, ImageTypeLimit: $ImageTypeLimit, EnableImageTypes: $EnableImageTypes, EnableUserData: $EnableUserData, SeriesTimerId: $SeriesTimerId, LibrarySeriesId: $LibrarySeriesId, Fields: $Fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a live tv program.
#
# GET /LiveTv/Programs/{programId}
# operationId: GetProgram
export def "live-tv-programs GetProgram" [
  programId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/LiveTv/Programs/($programId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets recommended live tv epgs.
#
# GET /LiveTv/Programs/Recommended
# operationId: GetRecommendedPrograms
export def "live-tv-programs-recommended GetRecommendedPrograms" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. filter by user id. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --isAiring: oneof<nothing, bool> # Optional. Filter by programs that are currently airing, or not.
  --hasAired: oneof<nothing, bool> # Optional. Filter by programs that have completed airing, or not.
  --isSeries: oneof<nothing, bool> # Optional. Filter for series.
  --isMovie: oneof<nothing, bool> # Optional. Filter for movies.
  --isNews: oneof<nothing, bool> # Optional. Filter for news.
  --isKids: oneof<nothing, bool> # Optional. Filter for kids.
  --isSports: oneof<nothing, bool> # Optional. Filter for sports.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --genreIds: list # The genres to return guide information for.
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableUserData: oneof<nothing, bool> # Optional. include user data.
  --enableTotalRecordCount: oneof<nothing, bool> # Retrieve total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "isAiring" $isAiring "scalar") (serialize-qp "hasAired" $hasAired "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "genreIds" $genreIds "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Programs/Recommended" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets live tv recordings.
#
# GET /LiveTv/Recordings
# operationId: GetRecordings
export def "live-tv-recordings GetRecordings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --channelId: string # Optional. Filter by channel id.
  --userId: string # Optional. Filter by user and attach user data. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --status: string@status-completer # Optional. Filter by recording status.
  --isInProgress: oneof<nothing, bool> # Optional. Filter by recordings that are in progress, or not.
  --seriesTimerId: string # Optional. Filter by recordings belonging to a series timer.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --isMovie: oneof<nothing, bool> # Optional. Filter for movies.
  --isSeries: oneof<nothing, bool> # Optional. Filter for series.
  --isKids: oneof<nothing, bool> # Optional. Filter for kids.
  --isSports: oneof<nothing, bool> # Optional. Filter for sports.
  --isNews: oneof<nothing, bool> # Optional. Filter for news.
  --isLibraryItem: oneof<nothing, bool> # Optional. Filter for is library item.
  --enableTotalRecordCount: oneof<nothing, bool> # Optional. Return total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "channelId" $channelId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "isInProgress" $isInProgress "scalar") (serialize-qp "seriesTimerId" $seriesTimerId "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isLibraryItem" $isLibraryItem "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Recordings" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a live tv recording.
#
# GET /LiveTv/Recordings/{recordingId}
# operationId: GetRecording
export def "live-tv-recordings GetRecording" [
  recordingId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/LiveTv/Recordings/($recordingId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Deletes a live tv recording.
#
# DELETE /LiveTv/Recordings/{recordingId}
# operationId: DeleteRecording
export def "live-tv-recordings DeleteRecording" [
  recordingId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/Recordings/($recordingId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets recording folders.
#
# GET /LiveTv/Recordings/Folders
# operationId: GetRecordingFolders
export def "live-tv-recordings-folders GetRecordingFolders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user and attach user data. (format: uuid)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Recordings/Folders" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets live tv recording groups.
#
# GET /LiveTv/Recordings/Groups
# DEPRECATED
# operationId: GetRecordingGroups
@deprecated
export def "live-tv-recordings-groups GetRecordingGroups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user and attach user data. (format: uuid)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Recordings/Groups" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get recording group.
#
# GET /LiveTv/Recordings/Groups/{groupId}
# DEPRECATED
# operationId: GetRecordingGroup
@deprecated
export def "live-tv-recordings-groups GetRecordingGroup" [
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/Recordings/Groups/($groupId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets live tv recording series.
#
# GET /LiveTv/Recordings/Series
# DEPRECATED
# operationId: GetRecordingsSeries
@deprecated
export def "live-tv-recordings-series GetRecordingsSeries" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --channelId: string # Optional. Filter by channel id.
  --userId: string # Optional. Filter by user and attach user data. (format: uuid)
  --groupId: string # Optional. Filter by recording group.
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --status: string@status-completer # Optional. Filter by recording status.
  --isInProgress: oneof<nothing, bool> # Optional. Filter by recordings that are in progress, or not.
  --seriesTimerId: string # Optional. Filter by recordings belonging to a series timer.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --enableTotalRecordCount: oneof<nothing, bool> # Optional. Return total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "channelId" $channelId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "groupId" $groupId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "isInProgress" $isInProgress "scalar") (serialize-qp "seriesTimerId" $seriesTimerId "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Recordings/Series" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets live tv series timers.
#
# GET /LiveTv/SeriesTimers
# operationId: GetSeriesTimers
export def "live-tv-series-timers GetSeriesTimers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --sortBy: string # Optional. Sort by SortName or Priority.
  --sortOrder: string@sortOrder-completer # Optional. Sort in Ascending or Descending order.
]: nothing -> record<Items: table<Id: string, Type: string, ServerId: string, ExternalId: string, ChannelId: string, ExternalChannelId: string, ChannelName: string, ChannelPrimaryImageTag: string, ProgramId: string, ExternalProgramId: string, Name: string, Overview: string, StartDate: string, EndDate: string, ServiceName: string, Priority: int, PrePaddingSeconds: int, PostPaddingSeconds: int, IsPrePaddingRequired: bool, ParentBackdropItemId: string, ParentBackdropImageTags: list, IsPostPaddingRequired: bool, KeepUntil: record, RecordAnyTime: bool, SkipEpisodesInLibrary: bool, RecordAnyChannel: bool, KeepUpTo: int, RecordNewOnly: bool, Days: list, DayPattern: record, ImageTags: record, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sortBy" $sortBy "scalar") (serialize-qp "sortOrder" $sortOrder "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/SeriesTimers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates a live tv series timer.
#
# POST /LiveTv/SeriesTimers
# operationId: CreateSeriesTimer
export def "live-tv-series-timers CreateSeriesTimer" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Id: string # Gets or sets the Id of the recording. (nullable)
  --Type: string # nullable
  --ServerId: string # Gets or sets the server identifier. (nullable)
  --ExternalId: string # Gets or sets the external identifier. (nullable)
  --ChannelId: string # Gets or sets the channel id of the recording. (format: uuid)
  --ExternalChannelId: string # Gets or sets the external channel identifier. (nullable)
  --ChannelName: string # Gets or sets the channel name of the recording. (nullable)
  --ChannelPrimaryImageTag: string # nullable
  --ProgramId: string # Gets or sets the program identifier. (nullable)
  --ExternalProgramId: string # Gets or sets the external program identifier. (nullable)
  --Name: string # Gets or sets the name of the recording. (nullable)
  --Overview: string # Gets or sets the description of the recording. (nullable)
  --StartDate: string # Gets or sets the start date of the recording, in UTC. (format: date-time)
  --EndDate: string # Gets or sets the end date of the recording, in UTC. (format: date-time)
  --ServiceName: string # Gets or sets the name of the service. (nullable)
  --Priority: int # Gets or sets the priority. (format: int32)
  --PrePaddingSeconds: int # Gets or sets the pre padding seconds. (format: int32)
  --PostPaddingSeconds: int # Gets or sets the post padding seconds. (format: int32)
  --IsPrePaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is pre padding required.
  --ParentBackdropItemId: string # Gets or sets the Id of the Parent that has a backdrop if the item does not have one. (nullable)
  --ParentBackdropImageTags: list # Gets or sets the parent backdrop image tags. (nullable)
  --IsPostPaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is post padding required.
  --KeepUntil: any@KeepUntil-completer
  --RecordAnyTime: oneof<nothing, bool> # Gets or sets a value indicating whether [record any time].
  --SkipEpisodesInLibrary: oneof<nothing, bool>
  --RecordAnyChannel: oneof<nothing, bool> # Gets or sets a value indicating whether [record any channel].
  --KeepUpTo: int # format: int32
  --RecordNewOnly: oneof<nothing, bool> # Gets or sets a value indicating whether [record new only].
  --Days: list # Gets or sets the days. (nullable)
  --DayPattern: any@DayPattern-completer # Gets or sets the day pattern. (nullable)
  --ImageTags: record # Gets or sets the image tags. (nullable)
  --ParentThumbItemId: string # Gets or sets the parent thumb item id. (nullable)
  --ParentThumbImageTag: string # Gets or sets the parent thumb image tag. (nullable)
  --ParentPrimaryImageItemId: string # Gets or sets the parent primary image item identifier. (nullable, format: uuid)
  --ParentPrimaryImageTag: string # Gets or sets the parent primary image tag. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/SeriesTimers")
  let body = {Id: $Id, Type: $Type, ServerId: $ServerId, ExternalId: $ExternalId, ChannelId: $ChannelId, ExternalChannelId: $ExternalChannelId, ChannelName: $ChannelName, ChannelPrimaryImageTag: $ChannelPrimaryImageTag, ProgramId: $ProgramId, ExternalProgramId: $ExternalProgramId, Name: $Name, Overview: $Overview, StartDate: $StartDate, EndDate: $EndDate, ServiceName: $ServiceName, Priority: $Priority, PrePaddingSeconds: $PrePaddingSeconds, PostPaddingSeconds: $PostPaddingSeconds, IsPrePaddingRequired: $IsPrePaddingRequired, ParentBackdropItemId: $ParentBackdropItemId, ParentBackdropImageTags: $ParentBackdropImageTags, IsPostPaddingRequired: $IsPostPaddingRequired, KeepUntil: $KeepUntil, RecordAnyTime: $RecordAnyTime, SkipEpisodesInLibrary: $SkipEpisodesInLibrary, RecordAnyChannel: $RecordAnyChannel, KeepUpTo: $KeepUpTo, RecordNewOnly: $RecordNewOnly, Days: $Days, DayPattern: $DayPattern, ImageTags: $ImageTags, ParentThumbItemId: $ParentThumbItemId, ParentThumbImageTag: $ParentThumbImageTag, ParentPrimaryImageItemId: $ParentPrimaryImageItemId, ParentPrimaryImageTag: $ParentPrimaryImageTag} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a live tv series timer.
#
# GET /LiveTv/SeriesTimers/{timerId}
# operationId: GetSeriesTimer
export def "live-tv-series-timers GetSeriesTimer" [
  timerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Id: string, Type: string, ServerId: string, ExternalId: string, ChannelId: string, ExternalChannelId: string, ChannelName: string, ChannelPrimaryImageTag: string, ProgramId: string, ExternalProgramId: string, Name: string, Overview: string, StartDate: string, EndDate: string, ServiceName: string, Priority: int, PrePaddingSeconds: int, PostPaddingSeconds: int, IsPrePaddingRequired: bool, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, IsPostPaddingRequired: bool, KeepUntil: record, RecordAnyTime: bool, SkipEpisodesInLibrary: bool, RecordAnyChannel: bool, KeepUpTo: int, RecordNewOnly: bool, Days: list<string>, DayPattern: record, ImageTags: record, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/SeriesTimers/($timerId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Cancels a live tv series timer.
#
# DELETE /LiveTv/SeriesTimers/{timerId}
# operationId: CancelSeriesTimer
export def "live-tv-series-timers CancelSeriesTimer" [
  timerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/SeriesTimers/($timerId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a live tv series timer.
#
# POST /LiveTv/SeriesTimers/{timerId}
# operationId: UpdateSeriesTimer
export def "live-tv-series-timers UpdateSeriesTimer" [
  timerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Id: string # Gets or sets the Id of the recording. (nullable)
  --Type: string # nullable
  --ServerId: string # Gets or sets the server identifier. (nullable)
  --ExternalId: string # Gets or sets the external identifier. (nullable)
  --ChannelId: string # Gets or sets the channel id of the recording. (format: uuid)
  --ExternalChannelId: string # Gets or sets the external channel identifier. (nullable)
  --ChannelName: string # Gets or sets the channel name of the recording. (nullable)
  --ChannelPrimaryImageTag: string # nullable
  --ProgramId: string # Gets or sets the program identifier. (nullable)
  --ExternalProgramId: string # Gets or sets the external program identifier. (nullable)
  --Name: string # Gets or sets the name of the recording. (nullable)
  --Overview: string # Gets or sets the description of the recording. (nullable)
  --StartDate: string # Gets or sets the start date of the recording, in UTC. (format: date-time)
  --EndDate: string # Gets or sets the end date of the recording, in UTC. (format: date-time)
  --ServiceName: string # Gets or sets the name of the service. (nullable)
  --Priority: int # Gets or sets the priority. (format: int32)
  --PrePaddingSeconds: int # Gets or sets the pre padding seconds. (format: int32)
  --PostPaddingSeconds: int # Gets or sets the post padding seconds. (format: int32)
  --IsPrePaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is pre padding required.
  --ParentBackdropItemId: string # Gets or sets the Id of the Parent that has a backdrop if the item does not have one. (nullable)
  --ParentBackdropImageTags: list # Gets or sets the parent backdrop image tags. (nullable)
  --IsPostPaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is post padding required.
  --KeepUntil: any@KeepUntil-completer
  --RecordAnyTime: oneof<nothing, bool> # Gets or sets a value indicating whether [record any time].
  --SkipEpisodesInLibrary: oneof<nothing, bool>
  --RecordAnyChannel: oneof<nothing, bool> # Gets or sets a value indicating whether [record any channel].
  --KeepUpTo: int # format: int32
  --RecordNewOnly: oneof<nothing, bool> # Gets or sets a value indicating whether [record new only].
  --Days: list # Gets or sets the days. (nullable)
  --DayPattern: any@DayPattern-completer # Gets or sets the day pattern. (nullable)
  --ImageTags: record # Gets or sets the image tags. (nullable)
  --ParentThumbItemId: string # Gets or sets the parent thumb item id. (nullable)
  --ParentThumbImageTag: string # Gets or sets the parent thumb image tag. (nullable)
  --ParentPrimaryImageItemId: string # Gets or sets the parent primary image item identifier. (nullable, format: uuid)
  --ParentPrimaryImageTag: string # Gets or sets the parent primary image tag. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/SeriesTimers/($timerId)")
  let body = {Id: $Id, Type: $Type, ServerId: $ServerId, ExternalId: $ExternalId, ChannelId: $ChannelId, ExternalChannelId: $ExternalChannelId, ChannelName: $ChannelName, ChannelPrimaryImageTag: $ChannelPrimaryImageTag, ProgramId: $ProgramId, ExternalProgramId: $ExternalProgramId, Name: $Name, Overview: $Overview, StartDate: $StartDate, EndDate: $EndDate, ServiceName: $ServiceName, Priority: $Priority, PrePaddingSeconds: $PrePaddingSeconds, PostPaddingSeconds: $PostPaddingSeconds, IsPrePaddingRequired: $IsPrePaddingRequired, ParentBackdropItemId: $ParentBackdropItemId, ParentBackdropImageTags: $ParentBackdropImageTags, IsPostPaddingRequired: $IsPostPaddingRequired, KeepUntil: $KeepUntil, RecordAnyTime: $RecordAnyTime, SkipEpisodesInLibrary: $SkipEpisodesInLibrary, RecordAnyChannel: $RecordAnyChannel, KeepUpTo: $KeepUpTo, RecordNewOnly: $RecordNewOnly, Days: $Days, DayPattern: $DayPattern, ImageTags: $ImageTags, ParentThumbItemId: $ParentThumbItemId, ParentThumbImageTag: $ParentThumbImageTag, ParentPrimaryImageItemId: $ParentPrimaryImageItemId, ParentPrimaryImageTag: $ParentPrimaryImageTag} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets the live tv timers.
#
# GET /LiveTv/Timers
# operationId: GetTimers
export def "live-tv-timers GetTimers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --channelId: string # Optional. Filter by channel id.
  --seriesTimerId: string # Optional. Filter by timers belonging to a series timer.
  --isActive: oneof<nothing, bool> # Optional. Filter by timers that are active.
  --isScheduled: oneof<nothing, bool> # Optional. Filter by timers that are scheduled.
]: nothing -> record<Items: table<Id: string, Type: string, ServerId: string, ExternalId: string, ChannelId: string, ExternalChannelId: string, ChannelName: string, ChannelPrimaryImageTag: string, ProgramId: string, ExternalProgramId: string, Name: string, Overview: string, StartDate: string, EndDate: string, ServiceName: string, Priority: int, PrePaddingSeconds: int, PostPaddingSeconds: int, IsPrePaddingRequired: bool, ParentBackdropItemId: string, ParentBackdropImageTags: list, IsPostPaddingRequired: bool, KeepUntil: record, Status: record, SeriesTimerId: string, ExternalSeriesTimerId: string, RunTimeTicks: int, ProgramInfo: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "channelId" $channelId "scalar") (serialize-qp "seriesTimerId" $seriesTimerId "scalar") (serialize-qp "isActive" $isActive "scalar") (serialize-qp "isScheduled" $isScheduled "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Timers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates a live tv timer.
#
# POST /LiveTv/Timers
# operationId: CreateTimer
export def "live-tv-timers CreateTimer" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Id: string # Gets or sets the Id of the recording. (nullable)
  --Type: string # nullable
  --ServerId: string # Gets or sets the server identifier. (nullable)
  --ExternalId: string # Gets or sets the external identifier. (nullable)
  --ChannelId: string # Gets or sets the channel id of the recording. (format: uuid)
  --ExternalChannelId: string # Gets or sets the external channel identifier. (nullable)
  --ChannelName: string # Gets or sets the channel name of the recording. (nullable)
  --ChannelPrimaryImageTag: string # nullable
  --ProgramId: string # Gets or sets the program identifier. (nullable)
  --ExternalProgramId: string # Gets or sets the external program identifier. (nullable)
  --Name: string # Gets or sets the name of the recording. (nullable)
  --Overview: string # Gets or sets the description of the recording. (nullable)
  --StartDate: string # Gets or sets the start date of the recording, in UTC. (format: date-time)
  --EndDate: string # Gets or sets the end date of the recording, in UTC. (format: date-time)
  --ServiceName: string # Gets or sets the name of the service. (nullable)
  --Priority: int # Gets or sets the priority. (format: int32)
  --PrePaddingSeconds: int # Gets or sets the pre padding seconds. (format: int32)
  --PostPaddingSeconds: int # Gets or sets the post padding seconds. (format: int32)
  --IsPrePaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is pre padding required.
  --ParentBackdropItemId: string # Gets or sets the Id of the Parent that has a backdrop if the item does not have one. (nullable)
  --ParentBackdropImageTags: list # Gets or sets the parent backdrop image tags. (nullable)
  --IsPostPaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is post padding required.
  --KeepUntil: any@KeepUntil-completer
  --Status: any@Status-completer # Gets or sets the status.
  --SeriesTimerId: string # Gets or sets the series timer identifier. (nullable)
  --ExternalSeriesTimerId: string # Gets or sets the external series timer identifier. (nullable)
  --RunTimeTicks: int # Gets or sets the run time ticks. (nullable, format: int64)
  --ProgramInfo: any # Gets or sets the program information. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/Timers")
  let body = {Id: $Id, Type: $Type, ServerId: $ServerId, ExternalId: $ExternalId, ChannelId: $ChannelId, ExternalChannelId: $ExternalChannelId, ChannelName: $ChannelName, ChannelPrimaryImageTag: $ChannelPrimaryImageTag, ProgramId: $ProgramId, ExternalProgramId: $ExternalProgramId, Name: $Name, Overview: $Overview, StartDate: $StartDate, EndDate: $EndDate, ServiceName: $ServiceName, Priority: $Priority, PrePaddingSeconds: $PrePaddingSeconds, PostPaddingSeconds: $PostPaddingSeconds, IsPrePaddingRequired: $IsPrePaddingRequired, ParentBackdropItemId: $ParentBackdropItemId, ParentBackdropImageTags: $ParentBackdropImageTags, IsPostPaddingRequired: $IsPostPaddingRequired, KeepUntil: $KeepUntil, Status: $Status, SeriesTimerId: $SeriesTimerId, ExternalSeriesTimerId: $ExternalSeriesTimerId, RunTimeTicks: $RunTimeTicks, ProgramInfo: $ProgramInfo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a timer.
#
# GET /LiveTv/Timers/{timerId}
# operationId: GetTimer
export def "live-tv-timers GetTimer" [
  timerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Id: string, Type: string, ServerId: string, ExternalId: string, ChannelId: string, ExternalChannelId: string, ChannelName: string, ChannelPrimaryImageTag: string, ProgramId: string, ExternalProgramId: string, Name: string, Overview: string, StartDate: string, EndDate: string, ServiceName: string, Priority: int, PrePaddingSeconds: int, PostPaddingSeconds: int, IsPrePaddingRequired: bool, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, IsPostPaddingRequired: bool, KeepUntil: record, Status: record, SeriesTimerId: string, ExternalSeriesTimerId: string, RunTimeTicks: int, ProgramInfo: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/Timers/($timerId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Cancels a live tv timer.
#
# DELETE /LiveTv/Timers/{timerId}
# operationId: CancelTimer
export def "live-tv-timers CancelTimer" [
  timerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/Timers/($timerId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a live tv timer.
#
# POST /LiveTv/Timers/{timerId}
# operationId: UpdateTimer
export def "live-tv-timers UpdateTimer" [
  timerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Id: string # Gets or sets the Id of the recording. (nullable)
  --Type: string # nullable
  --ServerId: string # Gets or sets the server identifier. (nullable)
  --ExternalId: string # Gets or sets the external identifier. (nullable)
  --ChannelId: string # Gets or sets the channel id of the recording. (format: uuid)
  --ExternalChannelId: string # Gets or sets the external channel identifier. (nullable)
  --ChannelName: string # Gets or sets the channel name of the recording. (nullable)
  --ChannelPrimaryImageTag: string # nullable
  --ProgramId: string # Gets or sets the program identifier. (nullable)
  --ExternalProgramId: string # Gets or sets the external program identifier. (nullable)
  --Name: string # Gets or sets the name of the recording. (nullable)
  --Overview: string # Gets or sets the description of the recording. (nullable)
  --StartDate: string # Gets or sets the start date of the recording, in UTC. (format: date-time)
  --EndDate: string # Gets or sets the end date of the recording, in UTC. (format: date-time)
  --ServiceName: string # Gets or sets the name of the service. (nullable)
  --Priority: int # Gets or sets the priority. (format: int32)
  --PrePaddingSeconds: int # Gets or sets the pre padding seconds. (format: int32)
  --PostPaddingSeconds: int # Gets or sets the post padding seconds. (format: int32)
  --IsPrePaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is pre padding required.
  --ParentBackdropItemId: string # Gets or sets the Id of the Parent that has a backdrop if the item does not have one. (nullable)
  --ParentBackdropImageTags: list # Gets or sets the parent backdrop image tags. (nullable)
  --IsPostPaddingRequired: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is post padding required.
  --KeepUntil: any@KeepUntil-completer
  --Status: any@Status-completer # Gets or sets the status.
  --SeriesTimerId: string # Gets or sets the series timer identifier. (nullable)
  --ExternalSeriesTimerId: string # Gets or sets the external series timer identifier. (nullable)
  --RunTimeTicks: int # Gets or sets the run time ticks. (nullable, format: int64)
  --ProgramInfo: any # Gets or sets the program information. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/Timers/($timerId)")
  let body = {Id: $Id, Type: $Type, ServerId: $ServerId, ExternalId: $ExternalId, ChannelId: $ChannelId, ExternalChannelId: $ExternalChannelId, ChannelName: $ChannelName, ChannelPrimaryImageTag: $ChannelPrimaryImageTag, ProgramId: $ProgramId, ExternalProgramId: $ExternalProgramId, Name: $Name, Overview: $Overview, StartDate: $StartDate, EndDate: $EndDate, ServiceName: $ServiceName, Priority: $Priority, PrePaddingSeconds: $PrePaddingSeconds, PostPaddingSeconds: $PostPaddingSeconds, IsPrePaddingRequired: $IsPrePaddingRequired, ParentBackdropItemId: $ParentBackdropItemId, ParentBackdropImageTags: $ParentBackdropImageTags, IsPostPaddingRequired: $IsPostPaddingRequired, KeepUntil: $KeepUntil, Status: $Status, SeriesTimerId: $SeriesTimerId, ExternalSeriesTimerId: $ExternalSeriesTimerId, RunTimeTicks: $RunTimeTicks, ProgramInfo: $ProgramInfo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets the default values for a new timer.
#
# GET /LiveTv/Timers/Defaults
# operationId: GetDefaultTimer
export def "live-tv-timers-defaults GetDefaultTimer" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --programId: string # Optional. To attach default values based on a program.
]: nothing -> record<Id: string, Type: string, ServerId: string, ExternalId: string, ChannelId: string, ExternalChannelId: string, ChannelName: string, ChannelPrimaryImageTag: string, ProgramId: string, ExternalProgramId: string, Name: string, Overview: string, StartDate: string, EndDate: string, ServiceName: string, Priority: int, PrePaddingSeconds: int, PostPaddingSeconds: int, IsPrePaddingRequired: bool, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, IsPostPaddingRequired: bool, KeepUntil: record, RecordAnyTime: bool, SkipEpisodesInLibrary: bool, RecordAnyChannel: bool, KeepUpTo: int, RecordNewOnly: bool, Days: list<string>, DayPattern: record, ImageTags: record, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "programId" $programId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Timers/Defaults" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Adds a tuner host.
#
# POST /LiveTv/TunerHosts
# operationId: AddTunerHost
export def "live-tv-tuner-hosts AddTunerHost" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Id: string # nullable
  --Url: string # nullable
  --Type: string # nullable
  --DeviceId: string # nullable
  --FriendlyName: string # nullable
  --ImportFavoritesOnly: oneof<nothing, bool>
  --AllowHWTranscoding: oneof<nothing, bool>
  --AllowFmp4TranscodingContainer: oneof<nothing, bool>
  --AllowStreamSharing: oneof<nothing, bool>
  --FallbackMaxStreamingBitrate: int # format: int32
  --EnableStreamLooping: oneof<nothing, bool>
  --Source: string # nullable
  --TunerCount: int # format: int32
  --UserAgent: string # nullable
  --IgnoreDts: oneof<nothing, bool>
  --ReadAtNativeFramerate: oneof<nothing, bool>
]: any -> record<Id: string, Url: string, Type: string, DeviceId: string, FriendlyName: string, ImportFavoritesOnly: bool, AllowHWTranscoding: bool, AllowFmp4TranscodingContainer: bool, AllowStreamSharing: bool, FallbackMaxStreamingBitrate: int, EnableStreamLooping: bool, Source: string, TunerCount: int, UserAgent: string, IgnoreDts: bool, ReadAtNativeFramerate: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/TunerHosts")
  let body = {Id: $Id, Url: $Url, Type: $Type, DeviceId: $DeviceId, FriendlyName: $FriendlyName, ImportFavoritesOnly: $ImportFavoritesOnly, AllowHWTranscoding: $AllowHWTranscoding, AllowFmp4TranscodingContainer: $AllowFmp4TranscodingContainer, AllowStreamSharing: $AllowStreamSharing, FallbackMaxStreamingBitrate: $FallbackMaxStreamingBitrate, EnableStreamLooping: $EnableStreamLooping, Source: $Source, TunerCount: $TunerCount, UserAgent: $UserAgent, IgnoreDts: $IgnoreDts, ReadAtNativeFramerate: $ReadAtNativeFramerate} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Deletes a tuner host.
#
# DELETE /LiveTv/TunerHosts
# operationId: DeleteTunerHost
export def "live-tv-tuner-hosts DeleteTunerHost" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string # Tuner host id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/TunerHosts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get tuner host types.
#
# GET /LiveTv/TunerHosts/Types
# operationId: GetTunerHostTypes
export def "live-tv-tuner-hosts-types GetTunerHostTypes" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/LiveTv/TunerHosts/Types")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Resets a tv tuner.
#
# POST /LiveTv/Tuners/{tunerId}/Reset
# operationId: ResetTuner
export def "live-tv-tuners-reset ResetTuner" [
  tunerId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/LiveTv/Tuners/($tunerId)/Reset")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Discover tuners.
#
# GET /LiveTv/Tuners/Discover
# operationId: DiscoverTuners
export def "live-tv-tuners-discover DiscoverTuners" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --newDevicesOnly: oneof<nothing, bool> # Only discover new tuners. (default: false)
]: nothing -> table<Id: string, Url: string, Type: string, DeviceId: string, FriendlyName: string, ImportFavoritesOnly: bool, AllowHWTranscoding: bool, AllowFmp4TranscodingContainer: bool, AllowStreamSharing: bool, FallbackMaxStreamingBitrate: int, EnableStreamLooping: bool, Source: string, TunerCount: int, UserAgent: string, IgnoreDts: bool, ReadAtNativeFramerate: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "newDevicesOnly" $newDevicesOnly "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Tuners/Discover" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Discover tuners.
#
# GET /LiveTv/Tuners/Discvover
# operationId: DiscvoverTuners
export def "live-tv-tuners-discvover DiscvoverTuners" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --newDevicesOnly: oneof<nothing, bool> # Only discover new tuners. (default: false)
]: nothing -> table<Id: string, Url: string, Type: string, DeviceId: string, FriendlyName: string, ImportFavoritesOnly: bool, AllowHWTranscoding: bool, AllowFmp4TranscodingContainer: bool, AllowStreamSharing: bool, FallbackMaxStreamingBitrate: int, EnableStreamLooping: bool, Source: string, TunerCount: int, UserAgent: string, IgnoreDts: bool, ReadAtNativeFramerate: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "newDevicesOnly" $newDevicesOnly "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveTv/Tuners/Discvover" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets known countries.
#
# GET /Localization/Countries
# operationId: GetCountries
export def "localization-countries GetCountries" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, DisplayName: string, TwoLetterISORegionName: string, ThreeLetterISORegionName: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Localization/Countries")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets known cultures.
#
# GET /Localization/Cultures
# operationId: GetCultures
export def "localization-cultures GetCultures" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, DisplayName: string, TwoLetterISOLanguageName: string, ThreeLetterISOLanguageName: string, ThreeLetterISOLanguageNames: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Localization/Cultures")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets localization options.
#
# GET /Localization/Options
# operationId: GetLocalizationOptions
export def "localization-options GetLocalizationOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Value: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Localization/Options")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets known parental ratings.
#
# GET /Localization/ParentalRatings
# operationId: GetParentalRatings
export def "localization-parental-ratings GetParentalRatings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Value: int, RatingScore: record<score: int, subScore: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Localization/ParentalRatings")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an item's lyrics.
#
# GET /Audio/{itemId}/Lyrics
# operationId: GetLyrics
export def "audio-lyrics GetLyrics" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Metadata: record<Artist: string, Album: string, Title: string, Author: string, Length: int, By: string, Offset: int, Creator: string, Version: string, IsSynced: bool>, Lyrics: table<Text: string, Start: int, Cues: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Audio/($itemId)/Lyrics")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload an external lyric file.
#
# POST /Audio/{itemId}/Lyrics
# operationId: UploadLyrics
export def "audio-lyrics UploadLyrics" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --fileName: string # Name of the file being uploaded.
  --body: record
]: any -> record<Metadata: record<Artist: string, Album: string, Title: string, Author: string, Length: int, By: string, Offset: int, Creator: string, Version: string, IsSynced: bool>, Lyrics: table<Text: string, Start: int, Cues: list>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fileName" $fileName "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/Lyrics" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "text/plain" $body
}

# Deletes an external lyric file.
#
# DELETE /Audio/{itemId}/Lyrics
# operationId: DeleteLyrics
export def "audio-lyrics DeleteLyrics" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Audio/($itemId)/Lyrics")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search remote lyrics.
#
# GET /Audio/{itemId}/RemoteSearch/Lyrics
# operationId: SearchRemoteLyrics
export def "audio-remote-search-lyrics SearchRemoteLyrics" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Id: string, ProviderName: string, Lyrics: record<Metadata: record, Lyrics: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Audio/($itemId)/RemoteSearch/Lyrics")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Downloads a remote lyric.
#
# POST /Audio/{itemId}/RemoteSearch/Lyrics/{lyricId}
# operationId: DownloadRemoteLyrics
export def "audio-remote-search-lyrics DownloadRemoteLyrics" [
  itemId: string
  lyricId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Metadata: record<Artist: string, Album: string, Title: string, Author: string, Length: int, By: string, Offset: int, Creator: string, Version: string, IsSynced: bool>, Lyrics: table<Text: string, Start: int, Cues: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Audio/($itemId)/RemoteSearch/Lyrics/($lyricId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the remote lyrics.
#
# GET /Providers/Lyrics/{lyricId}
# operationId: GetRemoteLyrics
export def "providers-lyrics GetRemoteLyrics" [
  lyricId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Metadata: record<Artist: string, Album: string, Title: string, Author: string, Length: int, By: string, Offset: int, Creator: string, Version: string, IsSynced: bool>, Lyrics: table<Text: string, Start: int, Cues: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Providers/Lyrics/($lyricId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets live playback media info for an item.
#
# GET /Items/{itemId}/PlaybackInfo
# operationId: GetPlaybackInfo
export def "items-playback-info GetPlaybackInfo" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
]: nothing -> record<MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, PlaySessionId: string, ErrorCode: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/PlaybackInfo" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets live playback media info for an item.
#
# POST /Items/{itemId}/PlaybackInfo
# operationId: GetPostedPlaybackInfo
@deprecated --flag userId
@deprecated --flag maxStreamingBitrate
@deprecated --flag startTimeTicks
@deprecated --flag audioStreamIndex
@deprecated --flag subtitleStreamIndex
@deprecated --flag maxAudioChannels
@deprecated --flag mediaSourceId
@deprecated --flag liveStreamId
@deprecated --flag autoOpenLiveStream
@deprecated --flag enableDirectPlay
@deprecated --flag enableDirectStream
@deprecated --flag enableTranscoding
@deprecated --flag allowVideoStreamCopy
@deprecated --flag allowAudioStreamCopy
export def "items-playback-info GetPostedPlaybackInfo" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (DEPRECATED, format: uuid)
  --maxStreamingBitrate: int # The maximum streaming bitrate. (DEPRECATED, format: int32)
  --startTimeTicks: int # The start time in ticks. (DEPRECATED, format: int64)
  --audioStreamIndex: int # The audio stream index. (DEPRECATED, format: int32)
  --subtitleStreamIndex: int # The subtitle stream index. (DEPRECATED, format: int32)
  --maxAudioChannels: int # The maximum number of audio channels. (DEPRECATED, format: int32)
  --mediaSourceId: string # The media source id. (DEPRECATED)
  --liveStreamId: string # The livestream id. (DEPRECATED)
  --autoOpenLiveStream: oneof<nothing, bool> # Whether to auto open the livestream. (DEPRECATED)
  --enableDirectPlay: oneof<nothing, bool> # Whether to enable direct play. Default: true. (DEPRECATED)
  --enableDirectStream: oneof<nothing, bool> # Whether to enable direct stream. Default: true. (DEPRECATED)
  --enableTranscoding: oneof<nothing, bool> # Whether to enable transcoding. Default: true. (DEPRECATED)
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether to allow to copy the video stream. Default: true. (DEPRECATED)
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether to allow to copy the audio stream. Default: true. (DEPRECATED)
  --UserId: string # Gets or sets the playback userId. (nullable, format: uuid)
  --MaxStreamingBitrate: int # Gets or sets the max streaming bitrate. (nullable, format: int32)
  --StartTimeTicks: int # Gets or sets the start time in ticks. (nullable, format: int64)
  --AudioStreamIndex: int # Gets or sets the audio stream index. (nullable, format: int32)
  --SubtitleStreamIndex: int # Gets or sets the subtitle stream index. (nullable, format: int32)
  --MaxAudioChannels: int # Gets or sets the max audio channels. (nullable, format: int32)
  --MediaSourceId: string # Gets or sets the media source id. (nullable)
  --LiveStreamId: string # Gets or sets the live stream id. (nullable)
  --DeviceProfile: any # A MediaBrowser.Model.Dlna.DeviceProfile represents a set of metadata which determines which content a certain device is able to play. <br /> Specifically, it defines the supported <see cref="P:MediaBrowser.Model.Dlna.DeviceProfile.ContainerProfiles">containers</see> and <see cref="P:MediaBrowser.Model.Dlna.DeviceProfile.CodecProfiles">codecs</see> (video and/or audio, including codec profiles and levels) the device is able to direct play (without transcoding or remuxing), as well as which <see cref="P:MediaBrowser.Model.Dlna.DeviceProfile.TranscodingProfiles">containers/codecs to transcode to</see> in case it isn't. (nullable)
  --EnableDirectPlay: oneof<nothing, bool> # Gets or sets a value indicating whether to enable direct play. (nullable)
  --EnableDirectStream: oneof<nothing, bool> # Gets or sets a value indicating whether to enable direct stream. (nullable)
  --EnableTranscoding: oneof<nothing, bool> # Gets or sets a value indicating whether to enable transcoding. (nullable)
  --AllowVideoStreamCopy: oneof<nothing, bool> # Gets or sets a value indicating whether to enable video stream copy. (nullable)
  --AllowAudioStreamCopy: oneof<nothing, bool> # Gets or sets a value indicating whether to allow audio stream copy. (nullable)
  --AutoOpenLiveStream: oneof<nothing, bool> # Gets or sets a value indicating whether to auto open the live stream. (nullable)
  --AlwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Gets or sets a value indicating whether always burn in subtitles when transcoding. (nullable)
]: any -> record<MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, PlaySessionId: string, ErrorCode: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "autoOpenLiveStream" $autoOpenLiveStream "scalar") (serialize-qp "enableDirectPlay" $enableDirectPlay "scalar") (serialize-qp "enableDirectStream" $enableDirectStream "scalar") (serialize-qp "enableTranscoding" $enableTranscoding "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/PlaybackInfo" $qp)
  let body = {UserId: $UserId, MaxStreamingBitrate: $MaxStreamingBitrate, StartTimeTicks: $StartTimeTicks, AudioStreamIndex: $AudioStreamIndex, SubtitleStreamIndex: $SubtitleStreamIndex, MaxAudioChannels: $MaxAudioChannels, MediaSourceId: $MediaSourceId, LiveStreamId: $LiveStreamId, DeviceProfile: $DeviceProfile, EnableDirectPlay: $EnableDirectPlay, EnableDirectStream: $EnableDirectStream, EnableTranscoding: $EnableTranscoding, AllowVideoStreamCopy: $AllowVideoStreamCopy, AllowAudioStreamCopy: $AllowAudioStreamCopy, AutoOpenLiveStream: $AutoOpenLiveStream, AlwaysBurnInSubtitleWhenTranscoding: $AlwaysBurnInSubtitleWhenTranscoding} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Closes a media source.
#
# POST /LiveStreams/Close
# operationId: CloseLiveStream
export def "live-streams-close CloseLiveStream" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --liveStreamId: string # The livestream id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "liveStreamId" $liveStreamId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveStreams/Close" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Opens a media source.
#
# POST /LiveStreams/Open
# operationId: OpenLiveStream
export def "live-streams-open OpenLiveStream" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --openToken: string # The open token.
  --userId: string # The user id. (format: uuid)
  --playSessionId: string # The play session id.
  --maxStreamingBitrate: int # The maximum streaming bitrate. (format: int32)
  --startTimeTicks: int # The start time in ticks. (format: int64)
  --audioStreamIndex: int # The audio stream index. (format: int32)
  --subtitleStreamIndex: int # The subtitle stream index. (format: int32)
  --maxAudioChannels: int # The maximum number of audio channels. (format: int32)
  --itemId: string # The item id. (format: uuid)
  --enableDirectPlay: oneof<nothing, bool> # Whether to enable direct play. Default: true.
  --enableDirectStream: oneof<nothing, bool> # Whether to enable direct stream. Default: true.
  --alwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Always burn-in subtitle when transcoding.
  --OpenToken: string # Gets or sets the open token. (nullable)
  --UserId: string # Gets or sets the user id. (nullable, format: uuid)
  --PlaySessionId: string # Gets or sets the play session id. (nullable)
  --MaxStreamingBitrate: int # Gets or sets the max streaming bitrate. (nullable, format: int32)
  --StartTimeTicks: int # Gets or sets the start time in ticks. (nullable, format: int64)
  --AudioStreamIndex: int # Gets or sets the audio stream index. (nullable, format: int32)
  --SubtitleStreamIndex: int # Gets or sets the subtitle stream index. (nullable, format: int32)
  --MaxAudioChannels: int # Gets or sets the max audio channels. (nullable, format: int32)
  --ItemId: string # Gets or sets the item id. (nullable, format: uuid)
  --EnableDirectPlay: oneof<nothing, bool> # Gets or sets a value indicating whether to enable direct play. (nullable)
  --EnableDirectStream: oneof<nothing, bool> # Gets or sets a value indicating whether to enable direct stream. (nullable)
  --AlwaysBurnInSubtitleWhenTranscoding: oneof<nothing, bool> # Gets or sets a value indicating whether always burn in subtitles when transcoding. (nullable)
  --DeviceProfile: any # A MediaBrowser.Model.Dlna.DeviceProfile represents a set of metadata which determines which content a certain device is able to play. <br /> Specifically, it defines the supported <see cref="P:MediaBrowser.Model.Dlna.DeviceProfile.ContainerProfiles">containers</see> and <see cref="P:MediaBrowser.Model.Dlna.DeviceProfile.CodecProfiles">codecs</see> (video and/or audio, including codec profiles and levels) the device is able to direct play (without transcoding or remuxing), as well as which <see cref="P:MediaBrowser.Model.Dlna.DeviceProfile.TranscodingProfiles">containers/codecs to transcode to</see> in case it isn't. (nullable)
  --DirectPlayProtocols: list # Gets or sets the device play protocols.
]: any -> record<MediaSource: record<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list<record>, MediaAttachments: list<record>, Formats: list<string>, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "openToken" $openToken "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "itemId" $itemId "scalar") (serialize-qp "enableDirectPlay" $enableDirectPlay "scalar") (serialize-qp "enableDirectStream" $enableDirectStream "scalar") (serialize-qp "alwaysBurnInSubtitleWhenTranscoding" $alwaysBurnInSubtitleWhenTranscoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/LiveStreams/Open" $qp)
  let body = {OpenToken: $OpenToken, UserId: $UserId, PlaySessionId: $PlaySessionId, MaxStreamingBitrate: $MaxStreamingBitrate, StartTimeTicks: $StartTimeTicks, AudioStreamIndex: $AudioStreamIndex, SubtitleStreamIndex: $SubtitleStreamIndex, MaxAudioChannels: $MaxAudioChannels, ItemId: $ItemId, EnableDirectPlay: $EnableDirectPlay, EnableDirectStream: $EnableDirectStream, AlwaysBurnInSubtitleWhenTranscoding: $AlwaysBurnInSubtitleWhenTranscoding, DeviceProfile: $DeviceProfile, DirectPlayProtocols: $DirectPlayProtocols} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Tests the network with a request with the size of the bitrate.
#
# GET /Playback/BitrateTest
# operationId: GetBitrateTestBytes
export def "playback-bitrate-test GetBitrateTestBytes" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --size: int # The bitrate. Defaults to 102400. (format: int32, default: 102400)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "size" $size "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Playback/BitrateTest" $qp)
  let accept_val = "application/octet-stream"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all media segments based on an itemId.
#
# GET /MediaSegments/{itemId}
# operationId: GetItemSegments
export def "media-segments GetItemSegments" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --includeSegmentTypes: list # Optional filter of requested segment types.
]: nothing -> record<Items: table<Id: string, ItemId: string, Type: record, StartTicks: int, EndTicks: int>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "includeSegmentTypes" $includeSegmentTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/MediaSegments/($itemId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets movie recommendations.
#
# GET /Movies/Recommendations
# operationId: GetMovieRecommendations
export def "movies-recommendations GetMovieRecommendations" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. The fields to return.
  --categoryLimit: int # The max number of categories to return. (format: int32, default: 5)
  --itemLimit: int # The max number of items to return per category. (format: int32, default: 8)
]: nothing -> table<Items: list<record>, RecommendationType: record, BaselineItemName: string, CategoryId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "categoryLimit" $categoryLimit "scalar") (serialize-qp "itemLimit" $itemLimit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Movies/Recommendations" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all music genres from a given item, folder, or the entire library.
#
# GET /MusicGenres
# DEPRECATED
# operationId: GetMusicGenres
@deprecated
export def "music-genres GetMusicGenres" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --searchTerm: string # The search term.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --excludeItemTypes: list # Optional. If specified, results will be filtered out based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be filtered in based on item type. This allows multiple, comma delimited.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --userId: string # User id. (format: uuid)
  --nameStartsWithOrGreater: string # Optional filter by items whose name is sorted equally or greater than a given input string.
  --nameStartsWith: string # Optional filter by items whose name is sorted equally than a given input string.
  --nameLessThan: string # Optional filter by items whose name is equally or lesser than a given input string.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited.
  --sortOrder: list # Sort Order - Ascending,Descending.
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
  --enableTotalRecordCount: oneof<nothing, bool> # Optional. Include total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "nameStartsWithOrGreater" $nameStartsWithOrGreater "scalar") (serialize-qp "nameStartsWith" $nameStartsWith "scalar") (serialize-qp "nameLessThan" $nameLessThan "scalar") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/MusicGenres" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a music genre, by name.
#
# GET /MusicGenres/{genreName}
# operationId: GetMusicGenre
export def "music-genres GetMusicGenre" [
  genreName: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/MusicGenres/($genreName)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available packages.
#
# GET /Packages
# operationId: GetPackages
export def "packages GetPackages" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<name: string, description: string, overview: string, owner: string, category: string, guid: string, versions: list<record>, imageUrl: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Packages")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a package by name or assembly GUID.
#
# GET /Packages/{name}
# operationId: GetPackageInfo
export def "packages GetPackageInfo" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --assemblyGuid: string # The GUID of the associated assembly. (format: uuid)
]: nothing -> record<name: string, description: string, overview: string, owner: string, category: string, guid: string, versions: table<version: string, VersionNumber: string, changelog: string, targetAbi: string, sourceUrl: string, checksum: string, timestamp: string, repositoryName: string, repositoryUrl: string>, imageUrl: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "assemblyGuid" $assemblyGuid "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Packages/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Installs a package.
#
# POST /Packages/Installed/{name}
# operationId: InstallPackage
export def "packages-installed InstallPackage" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --assemblyGuid: string # GUID of the associated assembly. (format: uuid)
  --version: string # Optional version. Defaults to latest version.
  --repositoryUrl: string # Optional. Specify the repository to install from.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "assemblyGuid" $assemblyGuid "scalar") (serialize-qp "version" $version "scalar") (serialize-qp "repositoryUrl" $repositoryUrl "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Packages/Installed/($name)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Cancels a package installation.
#
# DELETE /Packages/Installing/{packageId}
# operationId: CancelPackageInstallation
export def "packages-installing CancelPackageInstallation" [
  packageId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Packages/Installing/($packageId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all package repositories.
#
# GET /Repositories
# operationId: GetRepositories
export def "repositories GetRepositories" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Url: string, Enabled: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Repositories")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Sets the enabled and existing package repositories.
#
# POST /Repositories
# operationId: SetRepositories
export def "repositories SetRepositories" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Repositories")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets all persons.
#
# GET /Persons
# operationId: GetPersons
export def "persons GetPersons" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --searchTerm: string # The search term.
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --filters: list # Optional. Specify additional filters to apply.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not. userId is required.
  --enableUserData: oneof<nothing, bool> # Optional, include user data.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --excludePersonTypes: list # Optional. If specified results will be filtered to exclude those containing the specified PersonType. Allows multiple, comma-delimited.
  --personTypes: list # Optional. If specified results will be filtered to include only those containing the specified PersonType. Allows multiple, comma-delimited.
  --appearsInItemId: string # Optional. If specified, person results will be filtered on items related to said persons. (format: uuid)
  --userId: string # User id. (format: uuid)
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filters" $filters "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "excludePersonTypes" $excludePersonTypes "multi") (serialize-qp "personTypes" $personTypes "multi") (serialize-qp "appearsInItemId" $appearsInItemId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "enableImages" $enableImages "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Persons" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get person by name.
#
# GET /Persons/{name}
# operationId: GetPerson
export def "persons GetPerson" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Persons/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates a new playlist.
#
# POST /Playlists
# operationId: CreatePlaylist
# --Users item shape: {UserId?: string, CanEdit?: bool}
@deprecated --flag name
@deprecated --flag ids
@deprecated --flag userId
@deprecated --flag mediaType
export def "playlists CreatePlaylist" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --name: string # The playlist name. (DEPRECATED)
  --ids: list # The item ids. (DEPRECATED)
  --userId: string # The user id. (DEPRECATED, format: uuid)
  --mediaType: string@mediaType-completer # The media type. (DEPRECATED)
  --Name: string # Gets or sets the name of the new playlist.
  --Ids: list # Gets or sets item ids to add to the playlist.
  --UserId: string # Gets or sets the user id. (nullable, format: uuid)
  --MediaType: any@MediaType-completer # Gets or sets the media type. (nullable)
  --Users: list # Gets or sets the playlist users. — item shape: {UserId?: string, CanEdit?: bool}
  --IsPublic: oneof<nothing, bool> # Gets or sets a value indicating whether the playlist is public.
]: any -> record<Id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "ids" $ids "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "mediaType" $mediaType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Playlists" $qp)
  let body = {Name: $Name, Ids: $Ids, UserId: $UserId, MediaType: $MediaType, Users: $Users, IsPublic: $IsPublic} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Updates a playlist.
#
# POST /Playlists/{playlistId}
# operationId: UpdatePlaylist
# --Users item shape: {UserId?: string, CanEdit?: bool}
export def "playlists UpdatePlaylist" [
  playlistId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Name: string # Gets or sets the name of the new playlist. (nullable)
  --Ids: list # Gets or sets item ids of the playlist. (nullable)
  --Users: list # Gets or sets the playlist users. (nullable) — item shape: {UserId?: string, CanEdit?: bool}
  --IsPublic: oneof<nothing, bool> # Gets or sets a value indicating whether the playlist is public. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Playlists/($playlistId)")
  let body = {Name: $Name, Ids: $Ids, Users: $Users, IsPublic: $IsPublic} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a playlist.
#
# GET /Playlists/{playlistId}
# operationId: GetPlaylist
export def "playlists GetPlaylist" [
  playlistId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<OpenAccess: bool, Shares: table<UserId: string, CanEdit: bool>, ItemIds: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Playlists/($playlistId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Adds items to a playlist.
#
# POST /Playlists/{playlistId}/Items
# operationId: AddItemToPlaylist
export def "playlists-items AddItemToPlaylist" [
  playlistId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ids: list # Item id, comma delimited.
  --userId: string # The userId. (format: uuid)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ids" $ids "multi") (serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Playlists/($playlistId)/Items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Removes items from a playlist.
#
# DELETE /Playlists/{playlistId}/Items
# operationId: RemoveItemFromPlaylist
export def "playlists-items RemoveItemFromPlaylist" [
  playlistId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --entryIds: list # The item ids, comma delimited.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "entryIds" $entryIds "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Playlists/($playlistId)/Items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the original items of a playlist.
#
# GET /Playlists/{playlistId}/Items
# operationId: GetPlaylistItems
export def "playlists-items GetPlaylistItems" [
  playlistId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/Playlists/($playlistId)/Items" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Moves a playlist item.
#
# POST /Playlists/{playlistId}/Items/{itemId}/Move/{newIndex}
# operationId: MoveItem
export def "playlists-items-move MoveItem" [
  playlistId: string
  itemId: string
  newIndex: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Playlists/($playlistId)/Items/($itemId)/Move/($newIndex)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a playlist's users.
#
# GET /Playlists/{playlistId}/Users
# operationId: GetPlaylistUsers
export def "playlists-users GetPlaylistUsers" [
  playlistId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<UserId: string, CanEdit: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Playlists/($playlistId)/Users")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a playlist user.
#
# GET /Playlists/{playlistId}/Users/{userId}
# operationId: GetPlaylistUser
export def "playlists-users GetPlaylistUser" [
  playlistId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<UserId: string, CanEdit: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Playlists/($playlistId)/Users/($userId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Modify a user of a playlist's users.
#
# POST /Playlists/{playlistId}/Users/{userId}
# operationId: UpdatePlaylistUser
export def "playlists-users UpdatePlaylistUser" [
  playlistId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --CanEdit: oneof<nothing, bool> # Gets or sets a value indicating whether the user can edit the playlist. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Playlists/($playlistId)/Users/($userId)")
  let body = {CanEdit: $CanEdit} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Remove a user from a playlist's users.
#
# DELETE /Playlists/{playlistId}/Users/{userId}
# operationId: RemoveUserFromPlaylist
export def "playlists-users RemoveUserFromPlaylist" [
  playlistId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Playlists/($playlistId)/Users/($userId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that a session has begun playing an item.
#
# POST /PlayingItems/{itemId}
# DEPRECATED
# operationId: OnPlaybackStart
@deprecated
export def "playing-items OnPlaybackStart" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --mediaSourceId: string # The id of the MediaSource.
  --audioStreamIndex: int # The audio stream index. (format: int32)
  --subtitleStreamIndex: int # The subtitle stream index. (format: int32)
  --playMethod: string@playMethod-completer # The play method.
  --liveStreamId: string # The live stream id.
  --playSessionId: string # The play session id.
  --canSeek: oneof<nothing, bool> # Indicates if the client can seek. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "playMethod" $playMethod "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "canSeek" $canSeek "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/PlayingItems/($itemId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that a session has stopped playing an item.
#
# DELETE /PlayingItems/{itemId}
# DEPRECATED
# operationId: OnPlaybackStopped
@deprecated
export def "playing-items OnPlaybackStopped" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --mediaSourceId: string # The id of the MediaSource.
  --nextMediaType: string # The next media type that will play.
  --positionTicks: int # Optional. The position, in ticks, where playback stopped. 1 tick = 10000 ms. (format: int64)
  --liveStreamId: string # The live stream id.
  --playSessionId: string # The play session id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "nextMediaType" $nextMediaType "scalar") (serialize-qp "positionTicks" $positionTicks "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/PlayingItems/($itemId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports a session's playback progress.
#
# POST /PlayingItems/{itemId}/Progress
# DEPRECATED
# operationId: OnPlaybackProgress
@deprecated
export def "playing-items-progress OnPlaybackProgress" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --mediaSourceId: string # The id of the MediaSource.
  --positionTicks: int # Optional. The current position, in ticks. 1 tick = 10000 ms. (format: int64)
  --audioStreamIndex: int # The audio stream index. (format: int32)
  --subtitleStreamIndex: int # The subtitle stream index. (format: int32)
  --volumeLevel: int # Scale of 0-100. (format: int32)
  --playMethod: string@playMethod-completer # The play method.
  --liveStreamId: string # The live stream id.
  --playSessionId: string # The play session id.
  --repeatMode: string@repeatMode-completer # The repeat mode.
  --isPaused: oneof<nothing, bool> # Indicates if the player is paused. (default: false)
  --isMuted: oneof<nothing, bool> # Indicates if the player is muted. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "positionTicks" $positionTicks "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "volumeLevel" $volumeLevel "scalar") (serialize-qp "playMethod" $playMethod "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "repeatMode" $repeatMode "scalar") (serialize-qp "isPaused" $isPaused "scalar") (serialize-qp "isMuted" $isMuted "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/PlayingItems/($itemId)/Progress" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports playback has started within a session.
#
# POST /Sessions/Playing
# operationId: ReportPlaybackStart
# --NowPlayingQueue item shape: {Id?: string, PlaylistItemId?: string}
export def "sessions-playing ReportPlaybackStart" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --CanSeek: oneof<nothing, bool> # Gets or sets a value indicating whether this instance can seek.
  --Item: any # Gets or sets the item. (nullable)
  --ItemId: string # Gets or sets the item identifier. (format: uuid)
  --SessionId: string # Gets or sets the session id. (nullable)
  --MediaSourceId: string # Gets or sets the media version identifier. (nullable)
  --AudioStreamIndex: int # Gets or sets the index of the audio stream. (nullable, format: int32)
  --SubtitleStreamIndex: int # Gets or sets the index of the subtitle stream. (nullable, format: int32)
  --IsPaused: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is paused.
  --IsMuted: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is muted.
  --PositionTicks: int # Gets or sets the position ticks. (nullable, format: int64)
  --PlaybackStartTimeTicks: int # nullable, format: int64
  --VolumeLevel: int # Gets or sets the volume level. (nullable, format: int32)
  --Brightness: int # nullable, format: int32
  --AspectRatio: string # nullable
  --PlayMethod: any@PlayMethod-completer # Gets or sets the play method.
  --LiveStreamId: string # Gets or sets the live stream identifier. (nullable)
  --PlaySessionId: string # Gets or sets the play session identifier. (nullable)
  --RepeatMode: any@RepeatMode-completer # Gets or sets the repeat mode.
  --PlaybackOrder: any@PlaybackOrder-completer # Gets or sets the playback order.
  --NowPlayingQueue: list # nullable — item shape: {Id?: string, PlaylistItemId?: string}
  --PlaylistItemId: string # nullable
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Sessions/Playing")
  let body = {CanSeek: $CanSeek, Item: $Item, ItemId: $ItemId, SessionId: $SessionId, MediaSourceId: $MediaSourceId, AudioStreamIndex: $AudioStreamIndex, SubtitleStreamIndex: $SubtitleStreamIndex, IsPaused: $IsPaused, IsMuted: $IsMuted, PositionTicks: $PositionTicks, PlaybackStartTimeTicks: $PlaybackStartTimeTicks, VolumeLevel: $VolumeLevel, Brightness: $Brightness, AspectRatio: $AspectRatio, PlayMethod: $PlayMethod, LiveStreamId: $LiveStreamId, PlaySessionId: $PlaySessionId, RepeatMode: $RepeatMode, PlaybackOrder: $PlaybackOrder, NowPlayingQueue: $NowPlayingQueue, PlaylistItemId: $PlaylistItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Pings a playback session.
#
# POST /Sessions/Playing/Ping
# operationId: PingPlaybackSession
export def "sessions-playing-ping PingPlaybackSession" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --playSessionId: string # Playback session id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "playSessionId" $playSessionId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Sessions/Playing/Ping" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports playback progress within a session.
#
# POST /Sessions/Playing/Progress
# operationId: ReportPlaybackProgress
# --NowPlayingQueue item shape: {Id?: string, PlaylistItemId?: string}
export def "sessions-playing-progress ReportPlaybackProgress" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --CanSeek: oneof<nothing, bool> # Gets or sets a value indicating whether this instance can seek.
  --Item: any # Gets or sets the item. (nullable)
  --ItemId: string # Gets or sets the item identifier. (format: uuid)
  --SessionId: string # Gets or sets the session id. (nullable)
  --MediaSourceId: string # Gets or sets the media version identifier. (nullable)
  --AudioStreamIndex: int # Gets or sets the index of the audio stream. (nullable, format: int32)
  --SubtitleStreamIndex: int # Gets or sets the index of the subtitle stream. (nullable, format: int32)
  --IsPaused: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is paused.
  --IsMuted: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is muted.
  --PositionTicks: int # Gets or sets the position ticks. (nullable, format: int64)
  --PlaybackStartTimeTicks: int # nullable, format: int64
  --VolumeLevel: int # Gets or sets the volume level. (nullable, format: int32)
  --Brightness: int # nullable, format: int32
  --AspectRatio: string # nullable
  --PlayMethod: any@PlayMethod-completer # Gets or sets the play method.
  --LiveStreamId: string # Gets or sets the live stream identifier. (nullable)
  --PlaySessionId: string # Gets or sets the play session identifier. (nullable)
  --RepeatMode: any@RepeatMode-completer # Gets or sets the repeat mode.
  --PlaybackOrder: any@PlaybackOrder-completer # Gets or sets the playback order.
  --NowPlayingQueue: list # nullable — item shape: {Id?: string, PlaylistItemId?: string}
  --PlaylistItemId: string # nullable
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Sessions/Playing/Progress")
  let body = {CanSeek: $CanSeek, Item: $Item, ItemId: $ItemId, SessionId: $SessionId, MediaSourceId: $MediaSourceId, AudioStreamIndex: $AudioStreamIndex, SubtitleStreamIndex: $SubtitleStreamIndex, IsPaused: $IsPaused, IsMuted: $IsMuted, PositionTicks: $PositionTicks, PlaybackStartTimeTicks: $PlaybackStartTimeTicks, VolumeLevel: $VolumeLevel, Brightness: $Brightness, AspectRatio: $AspectRatio, PlayMethod: $PlayMethod, LiveStreamId: $LiveStreamId, PlaySessionId: $PlaySessionId, RepeatMode: $RepeatMode, PlaybackOrder: $PlaybackOrder, NowPlayingQueue: $NowPlayingQueue, PlaylistItemId: $PlaylistItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Reports playback has stopped within a session.
#
# POST /Sessions/Playing/Stopped
# operationId: ReportPlaybackStopped
# --NowPlayingQueue item shape: {Id?: string, PlaylistItemId?: string}
export def "sessions-playing-stopped ReportPlaybackStopped" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Item: any # Gets or sets the item. (nullable)
  --ItemId: string # Gets or sets the item identifier. (format: uuid)
  --SessionId: string # Gets or sets the session id. (nullable)
  --MediaSourceId: string # Gets or sets the media version identifier. (nullable)
  --PositionTicks: int # Gets or sets the position ticks. (nullable, format: int64)
  --LiveStreamId: string # Gets or sets the live stream identifier. (nullable)
  --PlaySessionId: string # Gets or sets the play session identifier. (nullable)
  --Failed: oneof<nothing, bool> # Gets or sets a value indicating whether this MediaBrowser.Model.Session.PlaybackStopInfo is failed.
  --NextMediaType: string # nullable
  --PlaylistItemId: string # nullable
  --NowPlayingQueue: list # nullable — item shape: {Id?: string, PlaylistItemId?: string}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Sessions/Playing/Stopped")
  let body = {Item: $Item, ItemId: $ItemId, SessionId: $SessionId, MediaSourceId: $MediaSourceId, PositionTicks: $PositionTicks, LiveStreamId: $LiveStreamId, PlaySessionId: $PlaySessionId, Failed: $Failed, NextMediaType: $NextMediaType, PlaylistItemId: $PlaylistItemId, NowPlayingQueue: $NowPlayingQueue} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Marks an item as played for user.
#
# POST /UserPlayedItems/{itemId}
# operationId: MarkPlayedItem
export def "user-played-items MarkPlayedItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
  --datePlayed: string # Optional. The date the item was played. (format: date-time)
]: nothing -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "datePlayed" $datePlayed "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserPlayedItems/($itemId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Marks an item as unplayed for user.
#
# DELETE /UserPlayedItems/{itemId}
# operationId: MarkUnplayedItem
export def "user-played-items MarkUnplayedItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserPlayedItems/($itemId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of currently installed plugins.
#
# GET /Plugins
# operationId: GetPlugins
export def "plugins GetPlugins" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Version: string, ConfigurationFileName: string, Description: string, Id: string, CanUninstall: bool, HasImage: bool, Status: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Plugins")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Uninstalls a plugin.
#
# DELETE /Plugins/{pluginId}
# DEPRECATED
# operationId: UninstallPlugin
@deprecated
export def "plugins UninstallPlugin" [
  pluginId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Uninstalls a plugin by version.
#
# DELETE /Plugins/{pluginId}/{version}
# operationId: UninstallPluginByVersion
export def "plugins UninstallPluginByVersion" [
  pluginId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)/($version)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Disable a plugin.
#
# POST /Plugins/{pluginId}/{version}/Disable
# operationId: DisablePlugin
export def "plugins-disable DisablePlugin" [
  pluginId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)/($version)/Disable")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Enables a disabled plugin.
#
# POST /Plugins/{pluginId}/{version}/Enable
# operationId: EnablePlugin
export def "plugins-enable EnablePlugin" [
  pluginId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)/($version)/Enable")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a plugin's image.
#
# GET /Plugins/{pluginId}/{version}/Image
# operationId: GetPluginImage
export def "plugins-image GetPluginImage" [
  pluginId: string
  version: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)/($version)/Image")
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets plugin configuration.
#
# GET /Plugins/{pluginId}/Configuration
# operationId: GetPluginConfiguration
export def "plugins-configuration GetPluginConfiguration" [
  pluginId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)/Configuration")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates plugin configuration.
#
# POST /Plugins/{pluginId}/Configuration
# operationId: UpdatePluginConfiguration
export def "plugins-configuration UpdatePluginConfiguration" [
  pluginId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)/Configuration")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a plugin's manifest.
#
# POST /Plugins/{pluginId}/Manifest
# operationId: GetPluginManifest
export def "plugins-manifest GetPluginManifest" [
  pluginId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Plugins/($pluginId)/Manifest")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Authorizes a pending quick connect request.
#
# POST /QuickConnect/Authorize
# operationId: AuthorizeQuickConnect
export def "quick-connect-authorize AuthorizeQuickConnect" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --code: string # Quick connect code to authorize.
  --userId: string # The user the authorize. Access to the requested user is required. (format: uuid)
]: nothing -> bool {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "code" $code "scalar") (serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/QuickConnect/Authorize" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Attempts to retrieve authentication information.
#
# GET /QuickConnect/Connect
# operationId: GetQuickConnectState
export def "quick-connect-connect GetQuickConnectState" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --secret: string # Secret previously returned from the Initiate endpoint.
]: nothing -> record<Authenticated: bool, Secret: string, Code: string, DeviceId: string, DeviceName: string, AppName: string, AppVersion: string, DateAdded: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "secret" $secret "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/QuickConnect/Connect" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the current quick connect state.
#
# GET /QuickConnect/Enabled
# operationId: GetQuickConnectEnabled
export def "quick-connect-enabled GetQuickConnectEnabled" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> bool {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/QuickConnect/Enabled")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Initiate a new quick connect request.
#
# POST /QuickConnect/Initiate
# operationId: InitiateQuickConnect
export def "quick-connect-initiate InitiateQuickConnect" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Authenticated: bool, Secret: string, Code: string, DeviceId: string, DeviceName: string, AppName: string, AppVersion: string, DateAdded: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/QuickConnect/Initiate")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available remote images for an item.
#
# GET /Items/{itemId}/RemoteImages
# operationId: GetRemoteImages
export def "items-remote-images GetRemoteImages" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --type: string@type-completer-1 # The image type.
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --providerName: string # Optional. The image provider to use.
  --includeAllLanguages: oneof<nothing, bool> # Optional. Include all languages. (default: false)
]: nothing -> record<Images: table<ProviderName: string, Url: string, ThumbnailUrl: string, Height: int, Width: int, CommunityRating: float, VoteCount: int, Language: string, Type: record, RatingType: record>, TotalRecordCount: int, Providers: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "type" $type "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "providerName" $providerName "scalar") (serialize-qp "includeAllLanguages" $includeAllLanguages "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/RemoteImages" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Downloads a remote image for an item.
#
# POST /Items/{itemId}/RemoteImages/Download
# operationId: DownloadRemoteImage
export def "items-remote-images-download DownloadRemoteImage" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --type: string@type-completer-1 # The image type.
  --imageUrl: string # The image url.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "type" $type "scalar") (serialize-qp "imageUrl" $imageUrl "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/RemoteImages/Download" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets available remote image providers for an item.
#
# GET /Items/{itemId}/RemoteImages/Providers
# operationId: GetRemoteImageProviders
export def "items-remote-images-providers GetRemoteImageProviders" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, SupportedImages: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/RemoteImages/Providers")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get tasks.
#
# GET /ScheduledTasks
# operationId: GetTasks
export def "scheduled-tasks GetTasks" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --isHidden: oneof<nothing, bool> # Optional filter tasks that are hidden, or not.
  --isEnabled: oneof<nothing, bool> # Optional filter tasks that are enabled, or not.
]: nothing -> table<Name: string, State: record, CurrentProgressPercentage: float, Id: string, LastExecutionResult: record<StartTimeUtc: string, EndTimeUtc: string, Status: record, Name: string, Key: string, Id: string, ErrorMessage: string, LongErrorMessage: string>, Triggers: list<record>, Description: string, Category: string, IsHidden: bool, Key: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "isHidden" $isHidden "scalar") (serialize-qp "isEnabled" $isEnabled "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ScheduledTasks" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get task by id.
#
# GET /ScheduledTasks/{taskId}
# operationId: GetTask
export def "scheduled-tasks GetTask" [
  taskId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Name: string, State: record, CurrentProgressPercentage: float, Id: string, LastExecutionResult: record<StartTimeUtc: string, EndTimeUtc: string, Status: record, Name: string, Key: string, Id: string, ErrorMessage: string, LongErrorMessage: string>, Triggers: table<Type: record, TimeOfDayTicks: int, IntervalTicks: int, DayOfWeek: record, MaxRuntimeTicks: int>, Description: string, Category: string, IsHidden: bool, Key: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ScheduledTasks/($taskId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update specified task triggers.
#
# POST /ScheduledTasks/{taskId}/Triggers
# operationId: UpdateTask
export def "scheduled-tasks-triggers UpdateTask" [
  taskId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ScheduledTasks/($taskId)/Triggers")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Start specified task.
#
# POST /ScheduledTasks/Running/{taskId}
# operationId: StartTask
export def "scheduled-tasks-running StartTask" [
  taskId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ScheduledTasks/Running/($taskId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Stop specified task.
#
# DELETE /ScheduledTasks/Running/{taskId}
# operationId: StopTask
export def "scheduled-tasks-running StopTask" [
  taskId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ScheduledTasks/Running/($taskId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the search hint result.
#
# GET /Search/Hints
# operationId: GetSearchHints
export def "search-hints GetSearchHints" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --userId: string # Optional. Supply a user id to search within a user's library or omit to search all. (format: uuid)
  --searchTerm: string # The search term to filter on.
  --includeItemTypes: list # If specified, only results with the specified item types are returned. This allows multiple, comma delimited.
  --excludeItemTypes: list # If specified, results with these item types are filtered out. This allows multiple, comma delimited.
  --mediaTypes: list # If specified, only results with the specified media types are returned. This allows multiple, comma delimited.
  --parentId: string # If specified, only children of the parent are returned. (format: uuid)
  --isMovie: oneof<nothing, bool> # Optional filter for movies.
  --isSeries: oneof<nothing, bool> # Optional filter for series.
  --isNews: oneof<nothing, bool> # Optional filter for news.
  --isKids: oneof<nothing, bool> # Optional filter for kids.
  --isSports: oneof<nothing, bool> # Optional filter for sports.
  --includePeople: oneof<nothing, bool> # Optional filter whether to include people. (default: true)
  --includeMedia: oneof<nothing, bool> # Optional filter whether to include media. (default: true)
  --includeGenres: oneof<nothing, bool> # Optional filter whether to include genres. (default: true)
  --includeStudios: oneof<nothing, bool> # Optional filter whether to include studios. (default: true)
  --includeArtists: oneof<nothing, bool> # Optional filter whether to include artists. (default: true)
]: nothing -> record<SearchHints: table<ItemId: string, Id: string, Name: string, MatchedTerm: string, IndexNumber: int, ProductionYear: int, ParentIndexNumber: int, PrimaryImageTag: string, ThumbImageTag: string, ThumbImageItemId: string, BackdropImageTag: string, BackdropImageItemId: string, Type: record, IsFolder: bool, RunTimeTicks: int, MediaType: record, StartDate: string, EndDate: string, Series: string, Status: string, Album: string, AlbumId: string, AlbumArtist: string, Artists: list, SongCount: int, EpisodeCount: int, ChannelId: string, ChannelName: string, PrimaryImageAspectRatio: float>, TotalRecordCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "mediaTypes" $mediaTypes "multi") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "includePeople" $includePeople "scalar") (serialize-qp "includeMedia" $includeMedia "scalar") (serialize-qp "includeGenres" $includeGenres "scalar") (serialize-qp "includeStudios" $includeStudios "scalar") (serialize-qp "includeArtists" $includeArtists "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Search/Hints" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all password reset providers.
#
# GET /Auth/PasswordResetProviders
# operationId: GetPasswordResetProviders
export def "auth-password-reset-providers GetPasswordResetProviders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Auth/PasswordResetProviders")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all auth providers.
#
# GET /Auth/Providers
# operationId: GetAuthProviders
export def "auth-providers GetAuthProviders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Auth/Providers")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of sessions.
#
# GET /Sessions
# operationId: GetSessions
export def "sessions GetSessions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --controllableByUserId: string # Filter by sessions that a given user is allowed to remote control. (format: uuid)
  --deviceId: string # Filter by device Id.
  --activeWithinSeconds: int # Optional. Filter by sessions that were active in the last n seconds. (format: int32)
]: nothing -> table<PlayState: record<PositionTicks: int, CanSeek: bool, IsPaused: bool, IsMuted: bool, VolumeLevel: int, AudioStreamIndex: int, SubtitleStreamIndex: int, MediaSourceId: string, PlayMethod: record, RepeatMode: record, PlaybackOrder: record, LiveStreamId: string>, AdditionalUsers: list<record>, Capabilities: record<PlayableMediaTypes: list, SupportedCommands: list, SupportsMediaControl: bool, SupportsPersistentIdentifier: bool, DeviceProfile: record, AppStoreUrl: string, IconUrl: string>, RemoteEndPoint: string, PlayableMediaTypes: list<string>, Id: string, UserId: string, UserName: string, Client: string, LastActivityDate: string, LastPlaybackCheckIn: string, LastPausedDate: string, DeviceName: string, DeviceType: string, NowPlayingItem: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, NowViewingItem: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, DeviceId: string, ApplicationVersion: string, TranscodingInfo: record<AudioCodec: string, VideoCodec: string, Container: string, IsVideoDirect: bool, IsAudioDirect: bool, Bitrate: int, Framerate: float, CompletionPercentage: float, Width: int, Height: int, AudioChannels: int, HardwareAccelerationType: record, TranscodeReasons: list>, IsActive: bool, SupportsMediaControl: bool, SupportsRemoteControl: bool, NowPlayingQueue: list<record>, NowPlayingQueueFullItems: list<record>, HasCustomDeviceName: bool, PlaylistItemId: string, ServerId: string, UserPrimaryImageTag: string, SupportedCommands: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "controllableByUserId" $controllableByUserId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "activeWithinSeconds" $activeWithinSeconds "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Sessions" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Issues a full general command to a client.
#
# POST /Sessions/{sessionId}/Command
# operationId: SendFullGeneralCommand
export def "sessions-command SendFullGeneralCommand" [
  sessionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Name: any@Name-completer # This exists simply to identify a set of known commands.
  --ControllingUserId: string # format: uuid
  --Arguments: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Sessions/($sessionId)/Command")
  let body = {Name: $Name, ControllingUserId: $ControllingUserId, Arguments: $Arguments} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Issues a general command to a client.
#
# POST /Sessions/{sessionId}/Command/{command}
# operationId: SendGeneralCommand
export def "sessions-command SendGeneralCommand" [
  sessionId: string
  command: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Sessions/($sessionId)/Command/($command)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Issues a command to a client to display a message to the user.
#
# POST /Sessions/{sessionId}/Message
# operationId: SendMessageCommand
export def "sessions-message SendMessageCommand" [
  sessionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Header: string # nullable
  Text: string
  --TimeoutMs: int # nullable, format: int64
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Sessions/($sessionId)/Message")
  let body = {Header: $Header, Text: $Text, TimeoutMs: $TimeoutMs} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Instructs a session to play an item.
#
# POST /Sessions/{sessionId}/Playing
# operationId: Play
export def "sessions-playing Play" [
  sessionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --playCommand: string@playCommand-completer # The type of play command to issue (PlayNow, PlayNext, PlayLast). Clients who have not yet implemented play next and play last may play now.
  --itemIds: list # The ids of the items to play, comma delimited.
  --startPositionTicks: int # The starting position of the first item. (format: int64)
  --mediaSourceId: string # Optional. The media source id.
  --audioStreamIndex: int # Optional. The index of the audio stream to play. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to play. (format: int32)
  --startIndex: int # Optional. The start index. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "playCommand" $playCommand "scalar") (serialize-qp "itemIds" $itemIds "multi") (serialize-qp "startPositionTicks" $startPositionTicks "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "startIndex" $startIndex "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Sessions/($sessionId)/Playing" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Issues a playstate command to a client.
#
# POST /Sessions/{sessionId}/Playing/{command}
# operationId: SendPlaystateCommand
export def "sessions-playing SendPlaystateCommand" [
  sessionId: string
  command: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --seekPositionTicks: int # The optional position ticks. (format: int64)
  --controllingUserId: string # The optional controlling user id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "seekPositionTicks" $seekPositionTicks "scalar") (serialize-qp "controllingUserId" $controllingUserId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Sessions/($sessionId)/Playing/($command)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Issues a system command to a client.
#
# POST /Sessions/{sessionId}/System/{command}
# operationId: SendSystemCommand
export def "sessions-system SendSystemCommand" [
  sessionId: string
  command: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Sessions/($sessionId)/System/($command)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Adds an additional user to a session.
#
# POST /Sessions/{sessionId}/User/{userId}
# operationId: AddUserToSession
export def "sessions-user AddUserToSession" [
  sessionId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Sessions/($sessionId)/User/($userId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Removes an additional user from a session.
#
# DELETE /Sessions/{sessionId}/User/{userId}
# operationId: RemoveUserFromSession
export def "sessions-user RemoveUserFromSession" [
  sessionId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Sessions/($sessionId)/User/($userId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Instructs a session to browse to an item or view.
#
# POST /Sessions/{sessionId}/Viewing
# operationId: DisplayContent
export def "sessions-viewing DisplayContent" [
  sessionId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemType: string@itemType-completer # The type of item to browse to.
  --itemId: string # The Id of the item.
  --itemName: string # The name of the item.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemType" $itemType "scalar") (serialize-qp "itemId" $itemId "scalar") (serialize-qp "itemName" $itemName "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Sessions/($sessionId)/Viewing" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates capabilities for a device.
#
# POST /Sessions/Capabilities
# operationId: PostCapabilities
export def "sessions-capabilities PostCapabilities" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string # The session id.
  --playableMediaTypes: list # A list of playable media types, comma delimited. Audio, Video, Book, Photo.
  --supportedCommands: list # A list of supported remote control commands, comma delimited.
  --supportsMediaControl: oneof<nothing, bool> # Determines whether media can be played remotely.. (default: false)
  --supportsPersistentIdentifier: oneof<nothing, bool> # Determines whether the device supports a unique identifier. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "playableMediaTypes" $playableMediaTypes "multi") (serialize-qp "supportedCommands" $supportedCommands "multi") (serialize-qp "supportsMediaControl" $supportsMediaControl "scalar") (serialize-qp "supportsPersistentIdentifier" $supportsPersistentIdentifier "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Sessions/Capabilities" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates capabilities for a device.
#
# POST /Sessions/Capabilities/Full
# operationId: PostFullCapabilities
export def "sessions-capabilities-full PostFullCapabilities" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string # The session id.
  --PlayableMediaTypes: list # Gets or sets the list of playable media types.
  --SupportedCommands: list # Gets or sets the list of supported commands.
  --SupportsMediaControl: oneof<nothing, bool> # Gets or sets a value indicating whether session supports media control.
  --SupportsPersistentIdentifier: oneof<nothing, bool> # Gets or sets a value indicating whether session supports a persistent identifier.
  --DeviceProfile: any # Gets or sets the device profile. (nullable)
  --AppStoreUrl: string # Gets or sets the app store url. (nullable)
  --IconUrl: string # Gets or sets the icon url. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Sessions/Capabilities/Full" $qp)
  let body = {PlayableMediaTypes: $PlayableMediaTypes, SupportedCommands: $SupportedCommands, SupportsMediaControl: $SupportsMediaControl, SupportsPersistentIdentifier: $SupportsPersistentIdentifier, DeviceProfile: $DeviceProfile, AppStoreUrl: $AppStoreUrl, IconUrl: $IconUrl} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Reports that a session has ended.
#
# POST /Sessions/Logout
# operationId: ReportSessionEnded
export def "sessions-logout ReportSessionEnded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Sessions/Logout")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Reports that a session is viewing an item.
#
# POST /Sessions/Viewing
# operationId: ReportViewing
export def "sessions-viewing ReportViewing" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --sessionId: string # The session id.
  --itemId: string # The item id.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sessionId" $sessionId "scalar") (serialize-qp "itemId" $itemId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Sessions/Viewing" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Completes the startup wizard.
#
# POST /Startup/Complete
# operationId: CompleteWizard
export def "startup-complete CompleteWizard" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Startup/Complete")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the initial startup wizard configuration.
#
# GET /Startup/Configuration
# operationId: GetStartupConfiguration
export def "startup-configuration GetStartupConfiguration" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<ServerName: string, UICulture: string, MetadataCountryCode: string, PreferredMetadataLanguage: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Startup/Configuration")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Sets the initial startup wizard configuration.
#
# POST /Startup/Configuration
# operationId: UpdateInitialConfiguration
export def "startup-configuration UpdateInitialConfiguration" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ServerName: string # Gets or sets the server name. (nullable)
  --UICulture: string # Gets or sets UI language culture. (nullable)
  --MetadataCountryCode: string # Gets or sets the metadata country code. (nullable)
  --PreferredMetadataLanguage: string # Gets or sets the preferred language for the metadata. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Startup/Configuration")
  let body = {ServerName: $ServerName, UICulture: $UICulture, MetadataCountryCode: $MetadataCountryCode, PreferredMetadataLanguage: $PreferredMetadataLanguage} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets the first user.
#
# GET /Startup/FirstUser
# operationId: GetFirstUser_2
export def "startup-first-user GetFirstUser-by-" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Name: string, Password: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Startup/FirstUser")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Sets remote access and UPnP.
#
# POST /Startup/RemoteAccess
# operationId: SetRemoteAccess
@deprecated --flag EnableAutomaticPortMapping
export def "startup-remote-access SetRemoteAccess" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --EnableRemoteAccess: oneof<nothing, bool> # Gets or sets a value indicating whether enable remote access.
  --EnableAutomaticPortMapping: oneof<nothing, bool> # Gets or sets a value indicating whether enable automatic port mapping. (DEPRECATED)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Startup/RemoteAccess")
  let body = {EnableRemoteAccess: $EnableRemoteAccess, EnableAutomaticPortMapping: $EnableAutomaticPortMapping} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets the first user.
#
# GET /Startup/User
# operationId: GetFirstUser
export def "startup-user GetFirstUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Name: string, Password: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Startup/User")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Sets the user name and password.
#
# POST /Startup/User
# operationId: UpdateStartupUser
export def "startup-user UpdateStartupUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Name: string # Gets or sets the username. (nullable)
  --Password: string # Gets or sets the user's password. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Startup/User")
  let body = {Name: $Name, Password: $Password} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets all studios from a given item, folder, or the entire library.
#
# GET /Studios
# operationId: GetStudios
export def "studios GetStudios" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --searchTerm: string # Optional. Search term.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --excludeItemTypes: list # Optional. If specified, results will be filtered out based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not.
  --enableUserData: oneof<nothing, bool> # Optional, include user data.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --userId: string # User id. (format: uuid)
  --nameStartsWithOrGreater: string # Optional filter by items whose name is sorted equally or greater than a given input string.
  --nameStartsWith: string # Optional filter by items whose name is sorted equally than a given input string.
  --nameLessThan: string # Optional filter by items whose name is equally or lesser than a given input string.
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
  --enableTotalRecordCount: oneof<nothing, bool> # Total record count. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "nameStartsWithOrGreater" $nameStartsWithOrGreater "scalar") (serialize-qp "nameStartsWith" $nameStartsWith "scalar") (serialize-qp "nameLessThan" $nameLessThan "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Studios" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a studio by name.
#
# GET /Studios/{name}
# operationId: GetStudio
export def "studios GetStudio" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Studios/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of available fallback font files.
#
# GET /FallbackFont/Fonts
# operationId: GetFallbackFontList
export def "fallback-font-fonts GetFallbackFontList" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, Size: int, DateCreated: string, DateModified: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/FallbackFont/Fonts")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a fallback font file.
#
# GET /FallbackFont/Fonts/{name}
# operationId: GetFallbackFont
export def "fallback-font-fonts GetFallbackFont" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/FallbackFont/Fonts/($name)")
  let accept_val = "font/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Search remote subtitles.
#
# GET /Items/{itemId}/RemoteSearch/Subtitles/{language}
# operationId: SearchRemoteSubtitles
export def "items-remote-search-subtitles SearchRemoteSubtitles" [
  itemId: string
  language: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --isPerfectMatch: oneof<nothing, bool> # Optional. Only show subtitles which are a perfect match.
]: nothing -> table<ThreeLetterISOLanguageName: string, Id: string, ProviderName: string, Name: string, Format: string, Author: string, Comment: string, DateCreated: string, CommunityRating: float, FrameRate: float, DownloadCount: int, IsHashMatch: bool, AiTranslated: bool, MachineTranslated: bool, Forced: bool, HearingImpaired: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "isPerfectMatch" $isPerfectMatch "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/RemoteSearch/Subtitles/($language)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Downloads a remote subtitle.
#
# POST /Items/{itemId}/RemoteSearch/Subtitles/{subtitleId}
# operationId: DownloadRemoteSubtitles
export def "items-remote-search-subtitles DownloadRemoteSubtitles" [
  itemId: string
  subtitleId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Items/($itemId)/RemoteSearch/Subtitles/($subtitleId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the remote subtitles.
#
# GET /Providers/Subtitles/Subtitles/{subtitleId}
# operationId: GetRemoteSubtitles
export def "providers-subtitles-subtitles GetRemoteSubtitles" [
  subtitleId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Providers/Subtitles/Subtitles/($subtitleId)")
  let accept_val = "text/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an HLS subtitle playlist.
#
# GET /Videos/{itemId}/{mediaSourceId}/Subtitles/{index}/subtitles.m3u8
# operationId: GetSubtitlePlaylist
export def "videos-subtitles-subtitlesm3u8 GetSubtitlePlaylist" [
  itemId: string
  index: int
  mediaSourceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --segmentLength: int # The subtitle segment length. (format: int32)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "segmentLength" $segmentLength "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/($mediaSourceId)/Subtitles/($index)/subtitles.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Upload an external subtitle file.
#
# POST /Videos/{itemId}/Subtitles
# operationId: UploadSubtitle
export def "videos-subtitles UploadSubtitle" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  Language: string # Gets or sets the subtitle language.
  Format: string # Gets or sets the subtitle format.
  --IsForced: oneof<nothing, bool> # Gets or sets a value indicating whether the subtitle is forced.
  --IsHearingImpaired: oneof<nothing, bool> # Gets or sets a value indicating whether the subtitle is for hearing impaired.
  Data: string # Gets or sets the subtitle data.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Videos/($itemId)/Subtitles")
  let body = {Language: $Language, Format: $Format, IsForced: $IsForced, IsHearingImpaired: $IsHearingImpaired, Data: $Data} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Deletes an external subtitle file.
#
# DELETE /Videos/{itemId}/Subtitles/{index}
# operationId: DeleteSubtitle
export def "videos-subtitles DeleteSubtitle" [
  itemId: string
  index: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Videos/($itemId)/Subtitles/($index)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets subtitles in a specified format.
#
# GET /Videos/{routeItemId}/{routeMediaSourceId}/Subtitles/{routeIndex}/{routeStartPositionTicks}/Stream.{routeFormat}
# operationId: GetSubtitleWithTicks
@deprecated --flag itemId
@deprecated --flag mediaSourceId
@deprecated --flag index
@deprecated --flag startPositionTicks
@deprecated --flag format
export def "videos-subtitles-stream-route-format GetSubtitleWithTicks" [
  routeItemId: string
  routeMediaSourceId: string
  routeIndex: int
  routeStartPositionTicks: int
  routeFormat: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemId: string # The item id. (DEPRECATED, format: uuid)
  --mediaSourceId: string # The media source id. (DEPRECATED)
  --index: int # The subtitle stream index. (DEPRECATED, format: int32)
  --startPositionTicks: int # The start position of the subtitle in ticks. (DEPRECATED, format: int64)
  --format: string # The format of the returned subtitle. (DEPRECATED)
  --endPositionTicks: int # Optional. The end position of the subtitle in ticks. (format: int64)
  --copyTimestamps: oneof<nothing, bool> # Optional. Whether to copy the timestamps. (default: false)
  --addVttTimeMap: oneof<nothing, bool> # Optional. Whether to add a VTT time map. (default: false)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemId" $itemId "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "index" $index "scalar") (serialize-qp "startPositionTicks" $startPositionTicks "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "endPositionTicks" $endPositionTicks "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "addVttTimeMap" $addVttTimeMap "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($routeItemId)/($routeMediaSourceId)/Subtitles/($routeIndex)/($routeStartPositionTicks)/Stream.($routeFormat)" $qp)
  let accept_val = "text/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets subtitles in a specified format.
#
# GET /Videos/{routeItemId}/{routeMediaSourceId}/Subtitles/{routeIndex}/Stream.{routeFormat}
# operationId: GetSubtitle
@deprecated --flag itemId
@deprecated --flag mediaSourceId
@deprecated --flag index
@deprecated --flag format
export def "videos-subtitles-stream-route-format GetSubtitle" [
  routeItemId: string
  routeMediaSourceId: string
  routeIndex: int
  routeFormat: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --itemId: string # The item id. (DEPRECATED, format: uuid)
  --mediaSourceId: string # The media source id. (DEPRECATED)
  --index: int # The subtitle stream index. (DEPRECATED, format: int32)
  --format: string # The format of the returned subtitle. (DEPRECATED)
  --endPositionTicks: int # Optional. The end position of the subtitle in ticks. (format: int64)
  --copyTimestamps: oneof<nothing, bool> # Optional. Whether to copy the timestamps. (default: false)
  --addVttTimeMap: oneof<nothing, bool> # Optional. Whether to add a VTT time map. (default: false)
  --startPositionTicks: int # The start position of the subtitle in ticks. (format: int64, default: 0)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "itemId" $itemId "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "index" $index "scalar") (serialize-qp "format" $format "scalar") (serialize-qp "endPositionTicks" $endPositionTicks "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "addVttTimeMap" $addVttTimeMap "scalar") (serialize-qp "startPositionTicks" $startPositionTicks "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($routeItemId)/($routeMediaSourceId)/Subtitles/($routeIndex)/Stream.($routeFormat)" $qp)
  let accept_val = "text/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets suggestions.
#
# GET /Items/Suggestions
# operationId: GetSuggestions
export def "items-suggestions GetSuggestions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
  --mediaType: list # The media types.
  --type: list # The type.
  --startIndex: int # Optional. The start index. (format: int32)
  --limit: int # Optional. The limit. (format: int32)
  --enableTotalRecordCount: oneof<nothing, bool> # Whether to enable the total record count. (default: false)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "mediaType" $mediaType "multi") (serialize-qp "type" $type "multi") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Items/Suggestions" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a SyncPlay group by id.
#
# GET /SyncPlay/{id}
# operationId: SyncPlayGetGroup
export def "sync-play SyncPlayGetGroup" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<GroupId: string, GroupName: string, State: record, Participants: list<string>, LastUpdatedAt: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/SyncPlay/($id)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Notify SyncPlay group that member is buffering.
#
# POST /SyncPlay/Buffering
# operationId: SyncPlayBuffering
export def "sync-play-buffering SyncPlayBuffering" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --When: string # Gets or sets when the request has been made by the client. (format: date-time)
  --PositionTicks: int # Gets or sets the position ticks. (format: int64)
  --IsPlaying: oneof<nothing, bool> # Gets or sets a value indicating whether the client playback is unpaused.
  --PlaylistItemId: string # Gets or sets the playlist item identifier of the playing item. (format: uuid)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Buffering")
  let body = {When: $When, PositionTicks: $PositionTicks, IsPlaying: $IsPlaying, PlaylistItemId: $PlaylistItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Join an existing SyncPlay group.
#
# POST /SyncPlay/Join
# operationId: SyncPlayJoinGroup
export def "sync-play-join SyncPlayJoinGroup" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --GroupId: string # Gets or sets the group identifier. (format: uuid)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Join")
  let body = {GroupId: $GroupId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Leave the joined SyncPlay group.
#
# POST /SyncPlay/Leave
# operationId: SyncPlayLeaveGroup
export def "sync-play-leave SyncPlayLeaveGroup" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Leave")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets all SyncPlay groups.
#
# GET /SyncPlay/List
# operationId: SyncPlayGetGroups
export def "sync-play-list SyncPlayGetGroups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<GroupId: string, GroupName: string, State: record, Participants: list<string>, LastUpdatedAt: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/List")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Request to move an item in the playlist in SyncPlay group.
#
# POST /SyncPlay/MovePlaylistItem
# operationId: SyncPlayMovePlaylistItem
export def "sync-play-move-playlist-item SyncPlayMovePlaylistItem" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --PlaylistItemId: string # Gets or sets the playlist identifier of the item. (format: uuid)
  --NewIndex: int # Gets or sets the new position. (format: int32)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/MovePlaylistItem")
  let body = {PlaylistItemId: $PlaylistItemId, NewIndex: $NewIndex} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a new SyncPlay group.
#
# POST /SyncPlay/New
# operationId: SyncPlayCreateGroup
export def "sync-play-new SyncPlayCreateGroup" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --GroupName: string # Gets or sets the group name.
]: any -> record<GroupId: string, GroupName: string, State: record, Participants: list<string>, LastUpdatedAt: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/New")
  let body = {GroupName: $GroupName} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request next item in SyncPlay group.
#
# POST /SyncPlay/NextItem
# operationId: SyncPlayNextItem
export def "sync-play-next-item SyncPlayNextItem" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --PlaylistItemId: string # Gets or sets the playing item identifier. (format: uuid)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/NextItem")
  let body = {PlaylistItemId: $PlaylistItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request pause in SyncPlay group.
#
# POST /SyncPlay/Pause
# operationId: SyncPlayPause
export def "sync-play-pause SyncPlayPause" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Pause")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update session ping.
#
# POST /SyncPlay/Ping
# operationId: SyncPlayPing
export def "sync-play-ping SyncPlayPing" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Ping: int # Gets or sets the ping time. (format: int64)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Ping")
  let body = {Ping: $Ping} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request previous item in SyncPlay group.
#
# POST /SyncPlay/PreviousItem
# operationId: SyncPlayPreviousItem
export def "sync-play-previous-item SyncPlayPreviousItem" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --PlaylistItemId: string # Gets or sets the playing item identifier. (format: uuid)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/PreviousItem")
  let body = {PlaylistItemId: $PlaylistItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request to queue items to the playlist of a SyncPlay group.
#
# POST /SyncPlay/Queue
# operationId: SyncPlayQueue
export def "sync-play-queue SyncPlayQueue" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ItemIds: list # Gets or sets the items to enqueue.
  --Mode: any@Mode-completer # Enum GroupQueueMode.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Queue")
  let body = {ItemIds: $ItemIds, Mode: $Mode} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Notify SyncPlay group that member is ready for playback.
#
# POST /SyncPlay/Ready
# operationId: SyncPlayReady
export def "sync-play-ready SyncPlayReady" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --When: string # Gets or sets when the request has been made by the client. (format: date-time)
  --PositionTicks: int # Gets or sets the position ticks. (format: int64)
  --IsPlaying: oneof<nothing, bool> # Gets or sets a value indicating whether the client playback is unpaused.
  --PlaylistItemId: string # Gets or sets the playlist item identifier of the playing item. (format: uuid)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Ready")
  let body = {When: $When, PositionTicks: $PositionTicks, IsPlaying: $IsPlaying, PlaylistItemId: $PlaylistItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request to remove items from the playlist in SyncPlay group.
#
# POST /SyncPlay/RemoveFromPlaylist
# operationId: SyncPlayRemoveFromPlaylist
export def "sync-play-remove-from-playlist SyncPlayRemoveFromPlaylist" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --PlaylistItemIds: list # Gets or sets the playlist identifiers of the items. Ignored when clearing the playlist.
  --ClearPlaylist: oneof<nothing, bool> # Gets or sets a value indicating whether the entire playlist should be cleared.
  --ClearPlayingItem: oneof<nothing, bool> # Gets or sets a value indicating whether the playing item should be removed as well. Used only when clearing the playlist.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/RemoveFromPlaylist")
  let body = {PlaylistItemIds: $PlaylistItemIds, ClearPlaylist: $ClearPlaylist, ClearPlayingItem: $ClearPlayingItem} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request seek in SyncPlay group.
#
# POST /SyncPlay/Seek
# operationId: SyncPlaySeek
export def "sync-play-seek SyncPlaySeek" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --PositionTicks: int # Gets or sets the position ticks. (format: int64)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Seek")
  let body = {PositionTicks: $PositionTicks} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request SyncPlay group to ignore member during group-wait.
#
# POST /SyncPlay/SetIgnoreWait
# operationId: SyncPlaySetIgnoreWait
export def "sync-play-set-ignore-wait SyncPlaySetIgnoreWait" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --IgnoreWait: oneof<nothing, bool> # Gets or sets a value indicating whether the client should be ignored.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/SetIgnoreWait")
  let body = {IgnoreWait: $IgnoreWait} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request to set new playlist in SyncPlay group.
#
# POST /SyncPlay/SetNewQueue
# operationId: SyncPlaySetNewQueue
export def "sync-play-set-new-queue SyncPlaySetNewQueue" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --PlayingQueue: list # Gets or sets the playing queue.
  --PlayingItemPosition: int # Gets or sets the position of the playing item in the queue. (format: int32)
  --StartPositionTicks: int # Gets or sets the start position ticks. (format: int64)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/SetNewQueue")
  let body = {PlayingQueue: $PlayingQueue, PlayingItemPosition: $PlayingItemPosition, StartPositionTicks: $StartPositionTicks} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request to change playlist item in SyncPlay group.
#
# POST /SyncPlay/SetPlaylistItem
# operationId: SyncPlaySetPlaylistItem
export def "sync-play-set-playlist-item SyncPlaySetPlaylistItem" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --PlaylistItemId: string # Gets or sets the playlist identifier of the playing item. (format: uuid)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/SetPlaylistItem")
  let body = {PlaylistItemId: $PlaylistItemId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request to set repeat mode in SyncPlay group.
#
# POST /SyncPlay/SetRepeatMode
# operationId: SyncPlaySetRepeatMode
export def "sync-play-set-repeat-mode SyncPlaySetRepeatMode" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Mode: any@Mode-completer-1 # Enum GroupRepeatMode.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/SetRepeatMode")
  let body = {Mode: $Mode} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request to set shuffle mode in SyncPlay group.
#
# POST /SyncPlay/SetShuffleMode
# operationId: SyncPlaySetShuffleMode
export def "sync-play-set-shuffle-mode SyncPlaySetShuffleMode" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --Mode: any@Mode-completer-2 # Enum GroupShuffleMode.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/SetShuffleMode")
  let body = {Mode: $Mode} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request stop in SyncPlay group.
#
# POST /SyncPlay/Stop
# operationId: SyncPlayStop
export def "sync-play-stop SyncPlayStop" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Stop")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Request unpause in SyncPlay group.
#
# POST /SyncPlay/Unpause
# operationId: SyncPlayUnpause
export def "sync-play-unpause SyncPlayUnpause" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/SyncPlay/Unpause")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets information about the request endpoint.
#
# GET /System/Endpoint
# operationId: GetEndpointInfo
export def "system-endpoint GetEndpointInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<IsLocal: bool, IsInNetwork: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Endpoint")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets information about the server.
#
# GET /System/Info
# operationId: GetSystemInfo
export def "system-info GetSystemInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<LocalAddress: string, ServerName: string, Version: string, ProductName: string, OperatingSystem: string, Id: string, StartupWizardCompleted: bool, OperatingSystemDisplayName: string, PackageName: string, HasPendingRestart: bool, IsShuttingDown: bool, SupportsLibraryMonitor: bool, WebSocketPortNumber: int, CompletedInstallations: table<Guid: string, Name: string, Version: string, Changelog: string, SourceUrl: string, Checksum: string, PackageInfo: record>, CanSelfRestart: bool, CanLaunchWebBrowser: bool, ProgramDataPath: string, WebPath: string, ItemsByNamePath: string, CachePath: string, LogPath: string, InternalMetadataPath: string, TranscodingTempPath: string, CastReceiverApplications: table<Id: string, Name: string>, HasUpdateAvailable: bool, EncoderLocation: string, SystemArchitecture: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Info")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets public information about the server.
#
# GET /System/Info/Public
# operationId: GetPublicSystemInfo
export def "system-info-public GetPublicSystemInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<LocalAddress: string, ServerName: string, Version: string, ProductName: string, OperatingSystem: string, Id: string, StartupWizardCompleted: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Info/Public")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets information about the server.
#
# GET /System/Info/Storage
# operationId: GetSystemStorage
export def "system-info-storage GetSystemStorage" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<ProgramDataFolder: record<Path: string, FreeSpace: int, UsedSpace: int, StorageType: string, DeviceId: string>, WebFolder: record<Path: string, FreeSpace: int, UsedSpace: int, StorageType: string, DeviceId: string>, ImageCacheFolder: record<Path: string, FreeSpace: int, UsedSpace: int, StorageType: string, DeviceId: string>, CacheFolder: record<Path: string, FreeSpace: int, UsedSpace: int, StorageType: string, DeviceId: string>, LogFolder: record<Path: string, FreeSpace: int, UsedSpace: int, StorageType: string, DeviceId: string>, InternalMetadataFolder: record<Path: string, FreeSpace: int, UsedSpace: int, StorageType: string, DeviceId: string>, TranscodingTempFolder: record<Path: string, FreeSpace: int, UsedSpace: int, StorageType: string, DeviceId: string>, Libraries: table<Id: string, Name: string, Folders: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Info/Storage")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of available server log files.
#
# GET /System/Logs
# operationId: GetServerLogs
export def "system-logs GetServerLogs" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<DateCreated: string, DateModified: string, Size: int, Name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Logs")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a log file.
#
# GET /System/Logs/Log
# operationId: GetLogFile
export def "system-logs-log GetLogFile" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --name: string # The name of the log file to get.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/System/Logs/Log" $qp)
  let accept_val = "text/plain"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Pings the system.
#
# GET /System/Ping
# operationId: GetPingSystem
export def "system-ping GetPingSystem" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Ping")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Pings the system.
#
# POST /System/Ping
# operationId: PostPingSystem
export def "system-ping PostPingSystem" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Ping")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Restarts the application.
#
# POST /System/Restart
# operationId: RestartApplication
export def "system-restart RestartApplication" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Restart")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Shuts down the application.
#
# POST /System/Shutdown
# operationId: ShutdownApplication
export def "system-shutdown ShutdownApplication" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/System/Shutdown")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the current UTC time.
#
# GET /GetUtcTime
# operationId: GetUtcTime
export def "get-utc-time GetUtcTime" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<RequestReceptionTime: string, ResponseTransmissionTime: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/GetUtcTime")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the TMDb image configuration options.
#
# GET /Tmdb/ClientConfiguration
# operationId: TmdbClientConfiguration
export def "tmdb-client-configuration TmdbClientConfiguration" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<BackdropSizes: list<string>, BaseUrl: string, LogoSizes: list<string>, PosterSizes: list<string>, ProfileSizes: list<string>, SecureBaseUrl: string, StillSizes: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Tmdb/ClientConfiguration")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Finds movies and trailers similar to a given trailer.
#
# GET /Trailers
# operationId: GetTrailers
export def "trailers GetTrailers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id supplied as query parameter; this is required when not using an API key. (format: uuid)
  --maxOfficialRating: string # Optional filter by maximum official rating (PG, PG-13, TV-MA, etc).
  --hasThemeSong: oneof<nothing, bool> # Optional filter by items with theme songs.
  --hasThemeVideo: oneof<nothing, bool> # Optional filter by items with theme videos.
  --hasSubtitles: oneof<nothing, bool> # Optional filter by items with subtitles.
  --hasSpecialFeature: oneof<nothing, bool> # Optional filter by items with special features.
  --hasTrailer: oneof<nothing, bool> # Optional filter by items with trailers.
  --adjacentTo: string # Optional. Return items that are siblings of a supplied item. (format: uuid)
  --parentIndexNumber: int # Optional filter by parent index number. (format: int32)
  --hasParentalRating: oneof<nothing, bool> # Optional filter by items that have or do not have a parental rating.
  --isHd: oneof<nothing, bool> # Optional filter by items that are HD or not.
  --is4K: oneof<nothing, bool> # Optional filter by items that are 4K or not.
  --locationTypes: list # Optional. If specified, results will be filtered based on LocationType. This allows multiple, comma delimited.
  --excludeLocationTypes: list # Optional. If specified, results will be filtered based on the LocationType. This allows multiple, comma delimited.
  --isMissing: oneof<nothing, bool> # Optional filter by items that are missing episodes or not.
  --isUnaired: oneof<nothing, bool> # Optional filter by items that are unaired episodes or not.
  --minCommunityRating: float # Optional filter by minimum community rating. (format: double)
  --minCriticRating: float # Optional filter by minimum critic rating. (format: double)
  --minPremiereDate: string # Optional. The minimum premiere date. Format = ISO. (format: date-time)
  --minDateLastSaved: string # Optional. The minimum last saved date. Format = ISO. (format: date-time)
  --minDateLastSavedForUser: string # Optional. The minimum last saved date for the current user. Format = ISO. (format: date-time)
  --maxPremiereDate: string # Optional. The maximum premiere date. Format = ISO. (format: date-time)
  --hasOverview: oneof<nothing, bool> # Optional filter by items that have an overview or not.
  --hasImdbId: oneof<nothing, bool> # Optional filter by items that have an IMDb id or not.
  --hasTmdbId: oneof<nothing, bool> # Optional filter by items that have a TMDb id or not.
  --hasTvdbId: oneof<nothing, bool> # Optional filter by items that have a TVDb id or not.
  --isMovie: oneof<nothing, bool> # Optional filter for live tv movies.
  --isSeries: oneof<nothing, bool> # Optional filter for live tv series.
  --isNews: oneof<nothing, bool> # Optional filter for live tv news.
  --isKids: oneof<nothing, bool> # Optional filter for live tv kids.
  --isSports: oneof<nothing, bool> # Optional filter for live tv sports.
  --excludeItemIds: list # Optional. If specified, results will be filtered by excluding item ids. This allows multiple, comma delimited.
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --recursive: oneof<nothing, bool> # When searching within folders, this determines whether or not the search will be recursive. true/false.
  --searchTerm: string # Optional. Filter based on a search term.
  --sortOrder: list # Sort Order - Ascending, Descending.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines.
  --excludeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --filters: list # Optional. Specify additional filters to apply. This allows multiple, comma delimited. Options: IsFolder, IsNotFolder, IsUnplayed, IsPlayed, IsFavorite, IsResumable, Likes, Dislikes.
  --isFavorite: oneof<nothing, bool> # Optional filter by items that are marked as favorite, or not.
  --mediaTypes: list # Optional filter by MediaType. Allows multiple, comma delimited.
  --imageTypes: list # Optional. If specified, results will be filtered based on those containing image types. This allows multiple, comma delimited.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
  --isPlayed: oneof<nothing, bool> # Optional filter by items that are played, or not.
  --genres: list # Optional. If specified, results will be filtered based on genre. This allows multiple, pipe delimited.
  --officialRatings: list # Optional. If specified, results will be filtered based on OfficialRating. This allows multiple, pipe delimited.
  --tags: list # Optional. If specified, results will be filtered based on tag. This allows multiple, pipe delimited.
  --years: list # Optional. If specified, results will be filtered based on production year. This allows multiple, comma delimited.
  --enableUserData: oneof<nothing, bool> # Optional, include user data.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --person: string # Optional. If specified, results will be filtered to include only those containing the specified person.
  --personIds: list # Optional. If specified, results will be filtered to include only those containing the specified person id.
  --personTypes: list # Optional. If specified, along with Person, results will be filtered to include only those containing the specified person and PersonType. Allows multiple, comma-delimited.
  --studios: list # Optional. If specified, results will be filtered based on studio. This allows multiple, pipe delimited.
  --artists: list # Optional. If specified, results will be filtered based on artists. This allows multiple, pipe delimited.
  --excludeArtistIds: list # Optional. If specified, results will be filtered based on artist id. This allows multiple, pipe delimited.
  --artistIds: list # Optional. If specified, results will be filtered to include only those containing the specified artist id.
  --albumArtistIds: list # Optional. If specified, results will be filtered to include only those containing the specified album artist id.
  --contributingArtistIds: list # Optional. If specified, results will be filtered to include only those containing the specified contributing artist id.
  --albums: list # Optional. If specified, results will be filtered based on album. This allows multiple, pipe delimited.
  --albumIds: list # Optional. If specified, results will be filtered based on album id. This allows multiple, pipe delimited.
  --ids: list # Optional. If specific items are needed, specify a list of item id's to retrieve. This allows multiple, comma delimited.
  --videoTypes: list # Optional filter by VideoType (videofile, dvd, bluray, iso). Allows multiple, comma delimited.
  --minOfficialRating: string # Optional filter by minimum official rating (PG, PG-13, TV-MA, etc).
  --isLocked: oneof<nothing, bool> # Optional filter by items that are locked.
  --isPlaceHolder: oneof<nothing, bool> # Optional filter by items that are placeholders.
  --hasOfficialRating: oneof<nothing, bool> # Optional filter by items that have official ratings.
  --collapseBoxSetItems: oneof<nothing, bool> # Whether or not to hide items behind their boxsets.
  --minWidth: int # Optional. Filter by the minimum width of the item. (format: int32)
  --minHeight: int # Optional. Filter by the minimum height of the item. (format: int32)
  --maxWidth: int # Optional. Filter by the maximum width of the item. (format: int32)
  --maxHeight: int # Optional. Filter by the maximum height of the item. (format: int32)
  --is3D: oneof<nothing, bool> # Optional filter by items that are 3D, or not.
  --seriesStatus: list # Optional filter by Series Status. Allows multiple, comma delimited.
  --nameStartsWithOrGreater: string # Optional filter by items whose name is sorted equally or greater than a given input string.
  --nameStartsWith: string # Optional filter by items whose name is sorted equally than a given input string.
  --nameLessThan: string # Optional filter by items whose name is equally or lesser than a given input string.
  --studioIds: list # Optional. If specified, results will be filtered based on studio id. This allows multiple, pipe delimited.
  --genreIds: list # Optional. If specified, results will be filtered based on genre id. This allows multiple, pipe delimited.
  --enableTotalRecordCount: oneof<nothing, bool> # Optional. Enable the total record count. (default: true)
  --enableImages: oneof<nothing, bool> # Optional, include image information in output. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "maxOfficialRating" $maxOfficialRating "scalar") (serialize-qp "hasThemeSong" $hasThemeSong "scalar") (serialize-qp "hasThemeVideo" $hasThemeVideo "scalar") (serialize-qp "hasSubtitles" $hasSubtitles "scalar") (serialize-qp "hasSpecialFeature" $hasSpecialFeature "scalar") (serialize-qp "hasTrailer" $hasTrailer "scalar") (serialize-qp "adjacentTo" $adjacentTo "scalar") (serialize-qp "parentIndexNumber" $parentIndexNumber "scalar") (serialize-qp "hasParentalRating" $hasParentalRating "scalar") (serialize-qp "isHd" $isHd "scalar") (serialize-qp "is4K" $is4K "scalar") (serialize-qp "locationTypes" $locationTypes "multi") (serialize-qp "excludeLocationTypes" $excludeLocationTypes "multi") (serialize-qp "isMissing" $isMissing "scalar") (serialize-qp "isUnaired" $isUnaired "scalar") (serialize-qp "minCommunityRating" $minCommunityRating "scalar") (serialize-qp "minCriticRating" $minCriticRating "scalar") (serialize-qp "minPremiereDate" $minPremiereDate "scalar") (serialize-qp "minDateLastSaved" $minDateLastSaved "scalar") (serialize-qp "minDateLastSavedForUser" $minDateLastSavedForUser "scalar") (serialize-qp "maxPremiereDate" $maxPremiereDate "scalar") (serialize-qp "hasOverview" $hasOverview "scalar") (serialize-qp "hasImdbId" $hasImdbId "scalar") (serialize-qp "hasTmdbId" $hasTmdbId "scalar") (serialize-qp "hasTvdbId" $hasTvdbId "scalar") (serialize-qp "isMovie" $isMovie "scalar") (serialize-qp "isSeries" $isSeries "scalar") (serialize-qp "isNews" $isNews "scalar") (serialize-qp "isKids" $isKids "scalar") (serialize-qp "isSports" $isSports "scalar") (serialize-qp "excludeItemIds" $excludeItemIds "multi") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "recursive" $recursive "scalar") (serialize-qp "searchTerm" $searchTerm "scalar") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "filters" $filters "multi") (serialize-qp "isFavorite" $isFavorite "scalar") (serialize-qp "mediaTypes" $mediaTypes "multi") (serialize-qp "imageTypes" $imageTypes "multi") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "isPlayed" $isPlayed "scalar") (serialize-qp "genres" $genres "multi") (serialize-qp "officialRatings" $officialRatings "multi") (serialize-qp "tags" $tags "multi") (serialize-qp "years" $years "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "person" $person "scalar") (serialize-qp "personIds" $personIds "multi") (serialize-qp "personTypes" $personTypes "multi") (serialize-qp "studios" $studios "multi") (serialize-qp "artists" $artists "multi") (serialize-qp "excludeArtistIds" $excludeArtistIds "multi") (serialize-qp "artistIds" $artistIds "multi") (serialize-qp "albumArtistIds" $albumArtistIds "multi") (serialize-qp "contributingArtistIds" $contributingArtistIds "multi") (serialize-qp "albums" $albums "multi") (serialize-qp "albumIds" $albumIds "multi") (serialize-qp "ids" $ids "multi") (serialize-qp "videoTypes" $videoTypes "multi") (serialize-qp "minOfficialRating" $minOfficialRating "scalar") (serialize-qp "isLocked" $isLocked "scalar") (serialize-qp "isPlaceHolder" $isPlaceHolder "scalar") (serialize-qp "hasOfficialRating" $hasOfficialRating "scalar") (serialize-qp "collapseBoxSetItems" $collapseBoxSetItems "scalar") (serialize-qp "minWidth" $minWidth "scalar") (serialize-qp "minHeight" $minHeight "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "is3D" $is3D "scalar") (serialize-qp "seriesStatus" $seriesStatus "multi") (serialize-qp "nameStartsWithOrGreater" $nameStartsWithOrGreater "scalar") (serialize-qp "nameStartsWith" $nameStartsWith "scalar") (serialize-qp "nameLessThan" $nameLessThan "scalar") (serialize-qp "studioIds" $studioIds "multi") (serialize-qp "genreIds" $genreIds "multi") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar") (serialize-qp "enableImages" $enableImages "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Trailers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a trickplay tile image.
#
# GET /Videos/{itemId}/Trickplay/{width}/{index}.jpg
# operationId: GetTrickplayTileImage
export def "videos-trickplay GetTrickplayTileImage" [
  itemId: string
  width: int
  index: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --mediaSourceId: string # The media version id, if using an alternate version. (format: uuid)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mediaSourceId" $mediaSourceId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/Trickplay/($width)/($index).jpg" $qp)
  let accept_val = "image/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an image tiles playlist for trickplay.
#
# GET /Videos/{itemId}/Trickplay/{width}/tiles.m3u8
# operationId: GetTrickplayHlsPlaylist
export def "videos-trickplay-tilesm3u8 GetTrickplayHlsPlaylist" [
  itemId: string
  width: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --mediaSourceId: string # The media version id, if using an alternate version. (format: uuid)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "mediaSourceId" $mediaSourceId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/Trickplay/($width)/tiles.m3u8" $qp)
  let accept_val = "application/x-mpegURL"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets episodes for a tv season.
#
# GET /Shows/{seriesId}/Episodes
# operationId: GetEpisodes
export def "shows-episodes GetEpisodes" [
  seriesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
  --season: int # Optional filter by season number. (format: int32)
  --seasonId: string # Optional. Filter by season id. (format: uuid)
  --isMissing: oneof<nothing, bool> # Optional. Filter by items that are missing episodes or not.
  --adjacentTo: string # Optional. Return items that are siblings of a supplied item. (format: uuid)
  --startItemId: string # Optional. Skip through the list until a given item is found. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --enableImages: oneof<nothing, bool> # Optional, include image information in output.
  --imageTypeLimit: int # Optional, the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --sortBy: string@sortBy-completer # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "season" $season "scalar") (serialize-qp "seasonId" $seasonId "scalar") (serialize-qp "isMissing" $isMissing "scalar") (serialize-qp "adjacentTo" $adjacentTo "scalar") (serialize-qp "startItemId" $startItemId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "sortBy" $sortBy "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Shows/($seriesId)/Episodes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets seasons for a tv series.
#
# GET /Shows/{seriesId}/Seasons
# operationId: GetSeasons
export def "shows-seasons GetSeasons" [
  seriesId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output. This allows multiple, comma delimited. Options: Budget, Chapters, DateCreated, Genres, HomePageUrl, IndexOptions, MediaStreams, Overview, ParentId, Path, People, ProviderIds, PrimaryImageAspectRatio, Revenue, SortName, Studios, Taglines, TrailerUrls.
  --isSpecialSeason: oneof<nothing, bool> # Optional. Filter by special season.
  --isMissing: oneof<nothing, bool> # Optional. Filter by items that are missing episodes or not.
  --adjacentTo: string # Optional. Return items that are siblings of a supplied item. (format: uuid)
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "isSpecialSeason" $isSpecialSeason "scalar") (serialize-qp "isMissing" $isMissing "scalar") (serialize-qp "adjacentTo" $adjacentTo "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "enableUserData" $enableUserData "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Shows/($seriesId)/Seasons" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of next up episodes.
#
# GET /Shows/NextUp
# operationId: GetNextUp
@deprecated --flag disableFirstEpisode
export def "shows-next-up GetNextUp" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id of the user to get the next up episodes for. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --seriesId: string # Optional. Filter by series id. (format: uuid)
  --parentId: string # Optional. Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --nextUpDateCutoff: string # Optional. Starting date of shows to show in Next Up section. (format: date-time)
  --enableTotalRecordCount: oneof<nothing, bool> # Whether to enable the total records count. Defaults to true. (default: true)
  --disableFirstEpisode: oneof<nothing, bool> # Whether to disable sending the first episode in a series as next up. (DEPRECATED, default: false)
  --enableResumable: oneof<nothing, bool> # Whether to include resumable episodes in next up results. (default: true)
  --enableRewatching: oneof<nothing, bool> # Whether to include watched episodes in next up results. (default: false)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "seriesId" $seriesId "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "nextUpDateCutoff" $nextUpDateCutoff "scalar") (serialize-qp "enableTotalRecordCount" $enableTotalRecordCount "scalar") (serialize-qp "disableFirstEpisode" $disableFirstEpisode "scalar") (serialize-qp "enableResumable" $enableResumable "scalar") (serialize-qp "enableRewatching" $enableRewatching "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Shows/NextUp" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of upcoming episodes.
#
# GET /Shows/Upcoming
# operationId: GetUpcomingEpisodes
export def "shows-upcoming GetUpcomingEpisodes" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # The user id of the user to get the upcoming episodes for. (format: uuid)
  --startIndex: int # Optional. The record index to start at. All items with a lower index will be dropped from the results. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --parentId: string # Optional. Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "enableUserData" $enableUserData "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Shows/Upcoming" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio stream.
#
# GET /Audio/{itemId}/universal
# operationId: GetUniversalAudioStream
export def "audio-universal GetUniversalAudioStream" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --container: list # Optional. The audio container.
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --userId: string # Optional. The user id. (format: uuid)
  --audioCodec: string # Optional. The audio codec to transcode to.
  --maxAudioChannels: int # Optional. The maximum number of audio channels. (format: int32)
  --transcodingAudioChannels: int # Optional. The number of how many audio channels to transcode to. (format: int32)
  --maxStreamingBitrate: int # Optional. The maximum streaming bitrate. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --transcodingContainer: string # Optional. The container to transcode to.
  --transcodingProtocol: string@transcodingProtocol-completer # Optional. The transcoding protocol.
  --maxAudioSampleRate: int # Optional. The maximum audio sample rate. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --enableRemoteMedia: oneof<nothing, bool> # Optional. Whether to enable remote media.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames. (default: false)
  --enableRedirection: oneof<nothing, bool> # Whether to enable redirection. Defaults to true. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "container" $container "multi") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "transcodingAudioChannels" $transcodingAudioChannels "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "transcodingContainer" $transcodingContainer "scalar") (serialize-qp "transcodingProtocol" $transcodingProtocol "scalar") (serialize-qp "maxAudioSampleRate" $maxAudioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "enableRemoteMedia" $enableRemoteMedia "scalar") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "enableRedirection" $enableRedirection "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/universal" $qp)
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets an audio stream.
#
# HEAD /Audio/{itemId}/universal
# operationId: HeadUniversalAudioStream
export def "audio-universal HeadUniversalAudioStream" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --container: list # Optional. The audio container.
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --userId: string # Optional. The user id. (format: uuid)
  --audioCodec: string # Optional. The audio codec to transcode to.
  --maxAudioChannels: int # Optional. The maximum number of audio channels. (format: int32)
  --transcodingAudioChannels: int # Optional. The number of how many audio channels to transcode to. (format: int32)
  --maxStreamingBitrate: int # Optional. The maximum streaming bitrate. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --transcodingContainer: string # Optional. The container to transcode to.
  --transcodingProtocol: string@transcodingProtocol-completer # Optional. The transcoding protocol.
  --maxAudioSampleRate: int # Optional. The maximum audio sample rate. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --enableRemoteMedia: oneof<nothing, bool> # Optional. Whether to enable remote media.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames. (default: false)
  --enableRedirection: oneof<nothing, bool> # Whether to enable redirection. Defaults to true. (default: true)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "container" $container "multi") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "userId" $userId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "transcodingAudioChannels" $transcodingAudioChannels "scalar") (serialize-qp "maxStreamingBitrate" $maxStreamingBitrate "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "transcodingContainer" $transcodingContainer "scalar") (serialize-qp "transcodingProtocol" $transcodingProtocol "scalar") (serialize-qp "maxAudioSampleRate" $maxAudioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "enableRemoteMedia" $enableRemoteMedia "scalar") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "enableRedirection" $enableRedirection "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Audio/($itemId)/universal" $qp)
  let accept_val = "audio/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a list of users.
#
# GET /Users
# operationId: GetUsers
export def "users GetUsers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --isHidden: oneof<nothing, bool> # Optional filter by IsHidden=true or false.
  --isDisabled: oneof<nothing, bool> # Optional filter by IsDisabled=true or false.
]: nothing -> table<Name: string, ServerId: string, ServerName: string, Id: string, PrimaryImageTag: string, HasPassword: bool, HasConfiguredPassword: bool, HasConfiguredEasyPassword: bool, EnableAutoLogin: bool, LastLoginDate: string, LastActivityDate: string, Configuration: record<AudioLanguagePreference: string, PlayDefaultAudioTrack: bool, SubtitleLanguagePreference: string, DisplayMissingEpisodes: bool, GroupedFolders: list, SubtitleMode: record, DisplayCollectionsView: bool, EnableLocalPassword: bool, OrderedViews: list, LatestItemsExcludes: list, MyMediaExcludes: list, HidePlayedInLatest: bool, RememberAudioSelections: bool, RememberSubtitleSelections: bool, EnableNextEpisodeAutoPlay: bool, CastReceiverId: string>, Policy: record<IsAdministrator: bool, IsHidden: bool, EnableCollectionManagement: bool, EnableSubtitleManagement: bool, EnableLyricManagement: bool, IsDisabled: bool, MaxParentalRating: int, MaxParentalSubRating: int, BlockedTags: list, AllowedTags: list, EnableUserPreferenceAccess: bool, AccessSchedules: list, BlockUnratedItems: list, EnableRemoteControlOfOtherUsers: bool, EnableSharedDeviceControl: bool, EnableRemoteAccess: bool, EnableLiveTvManagement: bool, EnableLiveTvAccess: bool, EnableMediaPlayback: bool, EnableAudioPlaybackTranscoding: bool, EnableVideoPlaybackTranscoding: bool, EnablePlaybackRemuxing: bool, ForceRemoteSourceTranscoding: bool, EnableContentDeletion: bool, EnableContentDeletionFromFolders: list, EnableContentDownloading: bool, EnableSyncTranscoding: bool, EnableMediaConversion: bool, EnabledDevices: list, EnableAllDevices: bool, EnabledChannels: list, EnableAllChannels: bool, EnabledFolders: list, EnableAllFolders: bool, InvalidLoginAttemptCount: int, LoginAttemptsBeforeLockout: int, MaxActiveSessions: int, EnablePublicSharing: bool, BlockedMediaFolders: list, BlockedChannels: list, RemoteClientBitrateLimit: int, AuthenticationProviderId: string, PasswordResetProviderId: string, SyncPlayAccess: record>, PrimaryImageAspectRatio: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "isHidden" $isHidden "scalar") (serialize-qp "isDisabled" $isDisabled "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Users" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a user.
#
# POST /Users
# operationId: UpdateUser
@deprecated --flag HasConfiguredEasyPassword
export def "users UpdateUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # The user id. (format: uuid)
  --Name: string # Gets or sets the name. (nullable)
  --ServerId: string # Gets or sets the server identifier. (nullable)
  --ServerName: string # Gets or sets the name of the server. This is not used by the server and is for client-side usage only. (nullable)
  --Id: string # Gets or sets the id. (format: uuid)
  --PrimaryImageTag: string # Gets or sets the primary image tag. (nullable)
  --HasPassword: oneof<nothing, bool> # Gets or sets a value indicating whether this instance has password.
  --HasConfiguredPassword: oneof<nothing, bool> # Gets or sets a value indicating whether this instance has configured password.
  --HasConfiguredEasyPassword: oneof<nothing, bool> # Gets or sets a value indicating whether this instance has configured easy password. (DEPRECATED)
  --EnableAutoLogin: oneof<nothing, bool> # Gets or sets whether async login is enabled or not. (nullable)
  --LastLoginDate: string # Gets or sets the last login date. (nullable, format: date-time)
  --LastActivityDate: string # Gets or sets the last activity date. (nullable, format: date-time)
  --Configuration: any # Gets or sets the configuration. (nullable)
  --Policy: any # Gets or sets the policy. (nullable)
  --PrimaryImageAspectRatio: float # Gets or sets the primary image aspect ratio. (nullable, format: double)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Users" $qp)
  let body = {Name: $Name, ServerId: $ServerId, ServerName: $ServerName, Id: $Id, PrimaryImageTag: $PrimaryImageTag, HasPassword: $HasPassword, HasConfiguredPassword: $HasConfiguredPassword, HasConfiguredEasyPassword: $HasConfiguredEasyPassword, EnableAutoLogin: $EnableAutoLogin, LastLoginDate: $LastLoginDate, LastActivityDate: $LastActivityDate, Configuration: $Configuration, Policy: $Policy, PrimaryImageAspectRatio: $PrimaryImageAspectRatio} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a user by Id.
#
# GET /Users/{userId}
# operationId: GetUserById
export def "users GetUserById" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Name: string, ServerId: string, ServerName: string, Id: string, PrimaryImageTag: string, HasPassword: bool, HasConfiguredPassword: bool, HasConfiguredEasyPassword: bool, EnableAutoLogin: bool, LastLoginDate: string, LastActivityDate: string, Configuration: record<AudioLanguagePreference: string, PlayDefaultAudioTrack: bool, SubtitleLanguagePreference: string, DisplayMissingEpisodes: bool, GroupedFolders: list<string>, SubtitleMode: record, DisplayCollectionsView: bool, EnableLocalPassword: bool, OrderedViews: list<string>, LatestItemsExcludes: list<string>, MyMediaExcludes: list<string>, HidePlayedInLatest: bool, RememberAudioSelections: bool, RememberSubtitleSelections: bool, EnableNextEpisodeAutoPlay: bool, CastReceiverId: string>, Policy: record<IsAdministrator: bool, IsHidden: bool, EnableCollectionManagement: bool, EnableSubtitleManagement: bool, EnableLyricManagement: bool, IsDisabled: bool, MaxParentalRating: int, MaxParentalSubRating: int, BlockedTags: list<string>, AllowedTags: list<string>, EnableUserPreferenceAccess: bool, AccessSchedules: list<record>, BlockUnratedItems: list<string>, EnableRemoteControlOfOtherUsers: bool, EnableSharedDeviceControl: bool, EnableRemoteAccess: bool, EnableLiveTvManagement: bool, EnableLiveTvAccess: bool, EnableMediaPlayback: bool, EnableAudioPlaybackTranscoding: bool, EnableVideoPlaybackTranscoding: bool, EnablePlaybackRemuxing: bool, ForceRemoteSourceTranscoding: bool, EnableContentDeletion: bool, EnableContentDeletionFromFolders: list<string>, EnableContentDownloading: bool, EnableSyncTranscoding: bool, EnableMediaConversion: bool, EnabledDevices: list<string>, EnableAllDevices: bool, EnabledChannels: list<string>, EnableAllChannels: bool, EnabledFolders: list<string>, EnableAllFolders: bool, InvalidLoginAttemptCount: int, LoginAttemptsBeforeLockout: int, MaxActiveSessions: int, EnablePublicSharing: bool, BlockedMediaFolders: list<string>, BlockedChannels: list<string>, RemoteClientBitrateLimit: int, AuthenticationProviderId: string, PasswordResetProviderId: string, SyncPlayAccess: record>, PrimaryImageAspectRatio: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Users/($userId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Deletes a user.
#
# DELETE /Users/{userId}
# operationId: DeleteUser
export def "users DeleteUser" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Users/($userId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a user policy.
#
# POST /Users/{userId}/Policy
# operationId: UpdateUserPolicy
# --AccessSchedules item shape: {UserId?: string, DayOfWeek?: "Sunday"|"Monday"|"Tuesday"|"Wednesday"|"Thursday"|"Friday"|"Saturday"|"Everyday"|"Weekday"|"Weekend", StartHour?: float, EndHour?: float}
export def "users-policy UpdateUserPolicy" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --IsAdministrator: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is administrator.
  --IsHidden: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is hidden.
  --EnableCollectionManagement: oneof<nothing, bool> # Gets or sets a value indicating whether this instance can manage collections. (default: false)
  --EnableSubtitleManagement: oneof<nothing, bool> # Gets or sets a value indicating whether this instance can manage subtitles. (default: false)
  --EnableLyricManagement: oneof<nothing, bool> # Gets or sets a value indicating whether this user can manage lyrics. (default: false)
  --IsDisabled: oneof<nothing, bool> # Gets or sets a value indicating whether this instance is disabled.
  --MaxParentalRating: int # Gets or sets the max parental rating. (nullable, format: int32)
  --MaxParentalSubRating: int # nullable, format: int32
  --BlockedTags: list # nullable
  --AllowedTags: list # nullable
  --EnableUserPreferenceAccess: oneof<nothing, bool>
  --AccessSchedules: list # nullable — item shape: {UserId?: string, DayOfWeek?: "Sunday"|"Monday"|"Tuesday"|"Wednesday"|"Thursday"|"Friday"|"Saturday"|"Everyday"|"Weekday"|"Weekend", StartHour?: float, EndHour?: float}
  --BlockUnratedItems: list # nullable
  --EnableRemoteControlOfOtherUsers: oneof<nothing, bool>
  --EnableSharedDeviceControl: oneof<nothing, bool>
  --EnableRemoteAccess: oneof<nothing, bool>
  --EnableLiveTvManagement: oneof<nothing, bool>
  --EnableLiveTvAccess: oneof<nothing, bool>
  --EnableMediaPlayback: oneof<nothing, bool>
  --EnableAudioPlaybackTranscoding: oneof<nothing, bool>
  --EnableVideoPlaybackTranscoding: oneof<nothing, bool>
  --EnablePlaybackRemuxing: oneof<nothing, bool>
  --ForceRemoteSourceTranscoding: oneof<nothing, bool>
  --EnableContentDeletion: oneof<nothing, bool>
  --EnableContentDeletionFromFolders: list # nullable
  --EnableContentDownloading: oneof<nothing, bool>
  --EnableSyncTranscoding: oneof<nothing, bool> # Gets or sets a value indicating whether [enable synchronize].
  --EnableMediaConversion: oneof<nothing, bool>
  --EnabledDevices: list # nullable
  --EnableAllDevices: oneof<nothing, bool>
  --EnabledChannels: list # nullable
  --EnableAllChannels: oneof<nothing, bool>
  --EnabledFolders: list # nullable
  --EnableAllFolders: oneof<nothing, bool>
  --InvalidLoginAttemptCount: int # format: int32
  --LoginAttemptsBeforeLockout: int # format: int32
  --MaxActiveSessions: int # format: int32
  --EnablePublicSharing: oneof<nothing, bool>
  --BlockedMediaFolders: list # nullable
  --BlockedChannels: list # nullable
  --RemoteClientBitrateLimit: int # format: int32
  AuthenticationProviderId: string
  PasswordResetProviderId: string
  --SyncPlayAccess: any@SyncPlayAccess-completer # Enum SyncPlayUserAccessType.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Users/($userId)/Policy")
  let body = {IsAdministrator: $IsAdministrator, IsHidden: $IsHidden, EnableCollectionManagement: $EnableCollectionManagement, EnableSubtitleManagement: $EnableSubtitleManagement, EnableLyricManagement: $EnableLyricManagement, IsDisabled: $IsDisabled, MaxParentalRating: $MaxParentalRating, MaxParentalSubRating: $MaxParentalSubRating, BlockedTags: $BlockedTags, AllowedTags: $AllowedTags, EnableUserPreferenceAccess: $EnableUserPreferenceAccess, AccessSchedules: $AccessSchedules, BlockUnratedItems: $BlockUnratedItems, EnableRemoteControlOfOtherUsers: $EnableRemoteControlOfOtherUsers, EnableSharedDeviceControl: $EnableSharedDeviceControl, EnableRemoteAccess: $EnableRemoteAccess, EnableLiveTvManagement: $EnableLiveTvManagement, EnableLiveTvAccess: $EnableLiveTvAccess, EnableMediaPlayback: $EnableMediaPlayback, EnableAudioPlaybackTranscoding: $EnableAudioPlaybackTranscoding, EnableVideoPlaybackTranscoding: $EnableVideoPlaybackTranscoding, EnablePlaybackRemuxing: $EnablePlaybackRemuxing, ForceRemoteSourceTranscoding: $ForceRemoteSourceTranscoding, EnableContentDeletion: $EnableContentDeletion, EnableContentDeletionFromFolders: $EnableContentDeletionFromFolders, EnableContentDownloading: $EnableContentDownloading, EnableSyncTranscoding: $EnableSyncTranscoding, EnableMediaConversion: $EnableMediaConversion, EnabledDevices: $EnabledDevices, EnableAllDevices: $EnableAllDevices, EnabledChannels: $EnabledChannels, EnableAllChannels: $EnableAllChannels, EnabledFolders: $EnabledFolders, EnableAllFolders: $EnableAllFolders, InvalidLoginAttemptCount: $InvalidLoginAttemptCount, LoginAttemptsBeforeLockout: $LoginAttemptsBeforeLockout, MaxActiveSessions: $MaxActiveSessions, EnablePublicSharing: $EnablePublicSharing, BlockedMediaFolders: $BlockedMediaFolders, BlockedChannels: $BlockedChannels, RemoteClientBitrateLimit: $RemoteClientBitrateLimit, AuthenticationProviderId: $AuthenticationProviderId, PasswordResetProviderId: $PasswordResetProviderId, SyncPlayAccess: $SyncPlayAccess} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Authenticates a user by name.
#
# POST /Users/AuthenticateByName
# operationId: AuthenticateUserByName
export def "users-authenticate-by-name AuthenticateUserByName" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Username: string # Gets or sets the username. (nullable)
  --Pw: string # Gets or sets the plain text password. (nullable)
]: any -> record<User: record<Name: string, ServerId: string, ServerName: string, Id: string, PrimaryImageTag: string, HasPassword: bool, HasConfiguredPassword: bool, HasConfiguredEasyPassword: bool, EnableAutoLogin: bool, LastLoginDate: string, LastActivityDate: string, Configuration: record<AudioLanguagePreference: string, PlayDefaultAudioTrack: bool, SubtitleLanguagePreference: string, DisplayMissingEpisodes: bool, GroupedFolders: list, SubtitleMode: record, DisplayCollectionsView: bool, EnableLocalPassword: bool, OrderedViews: list, LatestItemsExcludes: list, MyMediaExcludes: list, HidePlayedInLatest: bool, RememberAudioSelections: bool, RememberSubtitleSelections: bool, EnableNextEpisodeAutoPlay: bool, CastReceiverId: string>, Policy: record<IsAdministrator: bool, IsHidden: bool, EnableCollectionManagement: bool, EnableSubtitleManagement: bool, EnableLyricManagement: bool, IsDisabled: bool, MaxParentalRating: int, MaxParentalSubRating: int, BlockedTags: list, AllowedTags: list, EnableUserPreferenceAccess: bool, AccessSchedules: list, BlockUnratedItems: list, EnableRemoteControlOfOtherUsers: bool, EnableSharedDeviceControl: bool, EnableRemoteAccess: bool, EnableLiveTvManagement: bool, EnableLiveTvAccess: bool, EnableMediaPlayback: bool, EnableAudioPlaybackTranscoding: bool, EnableVideoPlaybackTranscoding: bool, EnablePlaybackRemuxing: bool, ForceRemoteSourceTranscoding: bool, EnableContentDeletion: bool, EnableContentDeletionFromFolders: list, EnableContentDownloading: bool, EnableSyncTranscoding: bool, EnableMediaConversion: bool, EnabledDevices: list, EnableAllDevices: bool, EnabledChannels: list, EnableAllChannels: bool, EnabledFolders: list, EnableAllFolders: bool, InvalidLoginAttemptCount: int, LoginAttemptsBeforeLockout: int, MaxActiveSessions: int, EnablePublicSharing: bool, BlockedMediaFolders: list, BlockedChannels: list, RemoteClientBitrateLimit: int, AuthenticationProviderId: string, PasswordResetProviderId: string, SyncPlayAccess: record>, PrimaryImageAspectRatio: float>, SessionInfo: record<PlayState: record<PositionTicks: int, CanSeek: bool, IsPaused: bool, IsMuted: bool, VolumeLevel: int, AudioStreamIndex: int, SubtitleStreamIndex: int, MediaSourceId: string, PlayMethod: record, RepeatMode: record, PlaybackOrder: record, LiveStreamId: string>, AdditionalUsers: list<record>, Capabilities: record<PlayableMediaTypes: list, SupportedCommands: list, SupportsMediaControl: bool, SupportsPersistentIdentifier: bool, DeviceProfile: record, AppStoreUrl: string, IconUrl: string>, RemoteEndPoint: string, PlayableMediaTypes: list<string>, Id: string, UserId: string, UserName: string, Client: string, LastActivityDate: string, LastPlaybackCheckIn: string, LastPausedDate: string, DeviceName: string, DeviceType: string, NowPlayingItem: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, NowViewingItem: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, DeviceId: string, ApplicationVersion: string, TranscodingInfo: record<AudioCodec: string, VideoCodec: string, Container: string, IsVideoDirect: bool, IsAudioDirect: bool, Bitrate: int, Framerate: float, CompletionPercentage: float, Width: int, Height: int, AudioChannels: int, HardwareAccelerationType: record, TranscodeReasons: list>, IsActive: bool, SupportsMediaControl: bool, SupportsRemoteControl: bool, NowPlayingQueue: list<record>, NowPlayingQueueFullItems: list<record>, HasCustomDeviceName: bool, PlaylistItemId: string, ServerId: string, UserPrimaryImageTag: string, SupportedCommands: list<string>>, AccessToken: string, ServerId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Users/AuthenticateByName")
  let body = {Username: $Username, Pw: $Pw} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Authenticates a user with quick connect.
#
# POST /Users/AuthenticateWithQuickConnect
# operationId: AuthenticateWithQuickConnect
export def "users-authenticate-with-quick-connect AuthenticateWithQuickConnect" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  Secret: string # Gets or sets the quick connect secret.
]: any -> record<User: record<Name: string, ServerId: string, ServerName: string, Id: string, PrimaryImageTag: string, HasPassword: bool, HasConfiguredPassword: bool, HasConfiguredEasyPassword: bool, EnableAutoLogin: bool, LastLoginDate: string, LastActivityDate: string, Configuration: record<AudioLanguagePreference: string, PlayDefaultAudioTrack: bool, SubtitleLanguagePreference: string, DisplayMissingEpisodes: bool, GroupedFolders: list, SubtitleMode: record, DisplayCollectionsView: bool, EnableLocalPassword: bool, OrderedViews: list, LatestItemsExcludes: list, MyMediaExcludes: list, HidePlayedInLatest: bool, RememberAudioSelections: bool, RememberSubtitleSelections: bool, EnableNextEpisodeAutoPlay: bool, CastReceiverId: string>, Policy: record<IsAdministrator: bool, IsHidden: bool, EnableCollectionManagement: bool, EnableSubtitleManagement: bool, EnableLyricManagement: bool, IsDisabled: bool, MaxParentalRating: int, MaxParentalSubRating: int, BlockedTags: list, AllowedTags: list, EnableUserPreferenceAccess: bool, AccessSchedules: list, BlockUnratedItems: list, EnableRemoteControlOfOtherUsers: bool, EnableSharedDeviceControl: bool, EnableRemoteAccess: bool, EnableLiveTvManagement: bool, EnableLiveTvAccess: bool, EnableMediaPlayback: bool, EnableAudioPlaybackTranscoding: bool, EnableVideoPlaybackTranscoding: bool, EnablePlaybackRemuxing: bool, ForceRemoteSourceTranscoding: bool, EnableContentDeletion: bool, EnableContentDeletionFromFolders: list, EnableContentDownloading: bool, EnableSyncTranscoding: bool, EnableMediaConversion: bool, EnabledDevices: list, EnableAllDevices: bool, EnabledChannels: list, EnableAllChannels: bool, EnabledFolders: list, EnableAllFolders: bool, InvalidLoginAttemptCount: int, LoginAttemptsBeforeLockout: int, MaxActiveSessions: int, EnablePublicSharing: bool, BlockedMediaFolders: list, BlockedChannels: list, RemoteClientBitrateLimit: int, AuthenticationProviderId: string, PasswordResetProviderId: string, SyncPlayAccess: record>, PrimaryImageAspectRatio: float>, SessionInfo: record<PlayState: record<PositionTicks: int, CanSeek: bool, IsPaused: bool, IsMuted: bool, VolumeLevel: int, AudioStreamIndex: int, SubtitleStreamIndex: int, MediaSourceId: string, PlayMethod: record, RepeatMode: record, PlaybackOrder: record, LiveStreamId: string>, AdditionalUsers: list<record>, Capabilities: record<PlayableMediaTypes: list, SupportedCommands: list, SupportsMediaControl: bool, SupportsPersistentIdentifier: bool, DeviceProfile: record, AppStoreUrl: string, IconUrl: string>, RemoteEndPoint: string, PlayableMediaTypes: list<string>, Id: string, UserId: string, UserName: string, Client: string, LastActivityDate: string, LastPlaybackCheckIn: string, LastPausedDate: string, DeviceName: string, DeviceType: string, NowPlayingItem: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, NowViewingItem: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, DeviceId: string, ApplicationVersion: string, TranscodingInfo: record<AudioCodec: string, VideoCodec: string, Container: string, IsVideoDirect: bool, IsAudioDirect: bool, Bitrate: int, Framerate: float, CompletionPercentage: float, Width: int, Height: int, AudioChannels: int, HardwareAccelerationType: record, TranscodeReasons: list>, IsActive: bool, SupportsMediaControl: bool, SupportsRemoteControl: bool, NowPlayingQueue: list<record>, NowPlayingQueueFullItems: list<record>, HasCustomDeviceName: bool, PlaylistItemId: string, ServerId: string, UserPrimaryImageTag: string, SupportedCommands: list<string>>, AccessToken: string, ServerId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Users/AuthenticateWithQuickConnect")
  let body = {Secret: $Secret} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Updates a user configuration.
#
# POST /Users/Configuration
# operationId: UpdateUserConfiguration
export def "users-configuration UpdateUserConfiguration" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # The user id. (format: uuid)
  --AudioLanguagePreference: string # Gets or sets the audio language preference. (nullable)
  --PlayDefaultAudioTrack: oneof<nothing, bool> # Gets or sets a value indicating whether [play default audio track].
  --SubtitleLanguagePreference: string # Gets or sets the subtitle language preference. (nullable)
  --DisplayMissingEpisodes: oneof<nothing, bool>
  --GroupedFolders: list
  --SubtitleMode: any@SubtitleMode-completer # An enum representing a subtitle playback mode.
  --DisplayCollectionsView: oneof<nothing, bool>
  --EnableLocalPassword: oneof<nothing, bool>
  --OrderedViews: list
  --LatestItemsExcludes: list
  --MyMediaExcludes: list
  --HidePlayedInLatest: oneof<nothing, bool>
  --RememberAudioSelections: oneof<nothing, bool>
  --RememberSubtitleSelections: oneof<nothing, bool>
  --EnableNextEpisodeAutoPlay: oneof<nothing, bool>
  --CastReceiverId: string # Gets or sets the id of the selected cast receiver. (nullable)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Users/Configuration" $qp)
  let body = {AudioLanguagePreference: $AudioLanguagePreference, PlayDefaultAudioTrack: $PlayDefaultAudioTrack, SubtitleLanguagePreference: $SubtitleLanguagePreference, DisplayMissingEpisodes: $DisplayMissingEpisodes, GroupedFolders: $GroupedFolders, SubtitleMode: $SubtitleMode, DisplayCollectionsView: $DisplayCollectionsView, EnableLocalPassword: $EnableLocalPassword, OrderedViews: $OrderedViews, LatestItemsExcludes: $LatestItemsExcludes, MyMediaExcludes: $MyMediaExcludes, HidePlayedInLatest: $HidePlayedInLatest, RememberAudioSelections: $RememberAudioSelections, RememberSubtitleSelections: $RememberSubtitleSelections, EnableNextEpisodeAutoPlay: $EnableNextEpisodeAutoPlay, CastReceiverId: $CastReceiverId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Initiates the forgot password process for a local user.
#
# POST /Users/ForgotPassword
# operationId: ForgotPassword
export def "users-forgot-password ForgotPassword" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  EnteredUsername: string # Gets or sets the entered username to have its password reset.
]: any -> record<Action: record, PinFile: string, PinExpirationDate: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Users/ForgotPassword")
  let body = {EnteredUsername: $EnteredUsername} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Redeems a forgot password pin.
#
# POST /Users/ForgotPassword/Pin
# operationId: ForgotPasswordPin
export def "users-forgot-password-pin ForgotPasswordPin" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  Pin: string # Gets or sets the entered pin to have the password reset.
]: any -> record<Success: bool, UsersReset: list<string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Users/ForgotPassword/Pin")
  let body = {Pin: $Pin} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets the user based on auth token.
#
# GET /Users/Me
# operationId: GetCurrentUser
export def "users-me GetCurrentUser" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Name: string, ServerId: string, ServerName: string, Id: string, PrimaryImageTag: string, HasPassword: bool, HasConfiguredPassword: bool, HasConfiguredEasyPassword: bool, EnableAutoLogin: bool, LastLoginDate: string, LastActivityDate: string, Configuration: record<AudioLanguagePreference: string, PlayDefaultAudioTrack: bool, SubtitleLanguagePreference: string, DisplayMissingEpisodes: bool, GroupedFolders: list<string>, SubtitleMode: record, DisplayCollectionsView: bool, EnableLocalPassword: bool, OrderedViews: list<string>, LatestItemsExcludes: list<string>, MyMediaExcludes: list<string>, HidePlayedInLatest: bool, RememberAudioSelections: bool, RememberSubtitleSelections: bool, EnableNextEpisodeAutoPlay: bool, CastReceiverId: string>, Policy: record<IsAdministrator: bool, IsHidden: bool, EnableCollectionManagement: bool, EnableSubtitleManagement: bool, EnableLyricManagement: bool, IsDisabled: bool, MaxParentalRating: int, MaxParentalSubRating: int, BlockedTags: list<string>, AllowedTags: list<string>, EnableUserPreferenceAccess: bool, AccessSchedules: list<record>, BlockUnratedItems: list<string>, EnableRemoteControlOfOtherUsers: bool, EnableSharedDeviceControl: bool, EnableRemoteAccess: bool, EnableLiveTvManagement: bool, EnableLiveTvAccess: bool, EnableMediaPlayback: bool, EnableAudioPlaybackTranscoding: bool, EnableVideoPlaybackTranscoding: bool, EnablePlaybackRemuxing: bool, ForceRemoteSourceTranscoding: bool, EnableContentDeletion: bool, EnableContentDeletionFromFolders: list<string>, EnableContentDownloading: bool, EnableSyncTranscoding: bool, EnableMediaConversion: bool, EnabledDevices: list<string>, EnableAllDevices: bool, EnabledChannels: list<string>, EnableAllChannels: bool, EnabledFolders: list<string>, EnableAllFolders: bool, InvalidLoginAttemptCount: int, LoginAttemptsBeforeLockout: int, MaxActiveSessions: int, EnablePublicSharing: bool, BlockedMediaFolders: list<string>, BlockedChannels: list<string>, RemoteClientBitrateLimit: int, AuthenticationProviderId: string, PasswordResetProviderId: string, SyncPlayAccess: record>, PrimaryImageAspectRatio: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Users/Me")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Creates a user.
#
# POST /Users/New
# operationId: CreateUserByName
export def "users-new CreateUserByName" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  Name: string # Gets or sets the username.
  --Password: string # Gets or sets the password. (nullable)
]: any -> record<Name: string, ServerId: string, ServerName: string, Id: string, PrimaryImageTag: string, HasPassword: bool, HasConfiguredPassword: bool, HasConfiguredEasyPassword: bool, EnableAutoLogin: bool, LastLoginDate: string, LastActivityDate: string, Configuration: record<AudioLanguagePreference: string, PlayDefaultAudioTrack: bool, SubtitleLanguagePreference: string, DisplayMissingEpisodes: bool, GroupedFolders: list<string>, SubtitleMode: record, DisplayCollectionsView: bool, EnableLocalPassword: bool, OrderedViews: list<string>, LatestItemsExcludes: list<string>, MyMediaExcludes: list<string>, HidePlayedInLatest: bool, RememberAudioSelections: bool, RememberSubtitleSelections: bool, EnableNextEpisodeAutoPlay: bool, CastReceiverId: string>, Policy: record<IsAdministrator: bool, IsHidden: bool, EnableCollectionManagement: bool, EnableSubtitleManagement: bool, EnableLyricManagement: bool, IsDisabled: bool, MaxParentalRating: int, MaxParentalSubRating: int, BlockedTags: list<string>, AllowedTags: list<string>, EnableUserPreferenceAccess: bool, AccessSchedules: list<record>, BlockUnratedItems: list<string>, EnableRemoteControlOfOtherUsers: bool, EnableSharedDeviceControl: bool, EnableRemoteAccess: bool, EnableLiveTvManagement: bool, EnableLiveTvAccess: bool, EnableMediaPlayback: bool, EnableAudioPlaybackTranscoding: bool, EnableVideoPlaybackTranscoding: bool, EnablePlaybackRemuxing: bool, ForceRemoteSourceTranscoding: bool, EnableContentDeletion: bool, EnableContentDeletionFromFolders: list<string>, EnableContentDownloading: bool, EnableSyncTranscoding: bool, EnableMediaConversion: bool, EnabledDevices: list<string>, EnableAllDevices: bool, EnabledChannels: list<string>, EnableAllChannels: bool, EnabledFolders: list<string>, EnableAllFolders: bool, InvalidLoginAttemptCount: int, LoginAttemptsBeforeLockout: int, MaxActiveSessions: int, EnablePublicSharing: bool, BlockedMediaFolders: list<string>, BlockedChannels: list<string>, RemoteClientBitrateLimit: int, AuthenticationProviderId: string, PasswordResetProviderId: string, SyncPlayAccess: record>, PrimaryImageAspectRatio: float> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Users/New")
  let body = {Name: $Name, Password: $Password} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Updates a user's password.
#
# POST /Users/Password
# operationId: UpdateUserPassword
export def "users-password UpdateUserPassword" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --userId: string # The user id. (format: uuid)
  --CurrentPassword: string # Gets or sets the current sha1-hashed password. (nullable)
  --CurrentPw: string # Gets or sets the current plain text password. (nullable)
  --NewPw: string # Gets or sets the new plain text password. (nullable)
  --ResetPassword: oneof<nothing, bool> # Gets or sets a value indicating whether to reset the password.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Users/Password" $qp)
  let body = {CurrentPassword: $CurrentPassword, CurrentPw: $CurrentPw, NewPw: $NewPw, ResetPassword: $ResetPassword} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a list of publicly visible users for display on a login screen.
#
# GET /Users/Public
# operationId: GetPublicUsers
export def "users-public GetPublicUsers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> table<Name: string, ServerId: string, ServerName: string, Id: string, PrimaryImageTag: string, HasPassword: bool, HasConfiguredPassword: bool, HasConfiguredEasyPassword: bool, EnableAutoLogin: bool, LastLoginDate: string, LastActivityDate: string, Configuration: record<AudioLanguagePreference: string, PlayDefaultAudioTrack: bool, SubtitleLanguagePreference: string, DisplayMissingEpisodes: bool, GroupedFolders: list, SubtitleMode: record, DisplayCollectionsView: bool, EnableLocalPassword: bool, OrderedViews: list, LatestItemsExcludes: list, MyMediaExcludes: list, HidePlayedInLatest: bool, RememberAudioSelections: bool, RememberSubtitleSelections: bool, EnableNextEpisodeAutoPlay: bool, CastReceiverId: string>, Policy: record<IsAdministrator: bool, IsHidden: bool, EnableCollectionManagement: bool, EnableSubtitleManagement: bool, EnableLyricManagement: bool, IsDisabled: bool, MaxParentalRating: int, MaxParentalSubRating: int, BlockedTags: list, AllowedTags: list, EnableUserPreferenceAccess: bool, AccessSchedules: list, BlockUnratedItems: list, EnableRemoteControlOfOtherUsers: bool, EnableSharedDeviceControl: bool, EnableRemoteAccess: bool, EnableLiveTvManagement: bool, EnableLiveTvAccess: bool, EnableMediaPlayback: bool, EnableAudioPlaybackTranscoding: bool, EnableVideoPlaybackTranscoding: bool, EnablePlaybackRemuxing: bool, ForceRemoteSourceTranscoding: bool, EnableContentDeletion: bool, EnableContentDeletionFromFolders: list, EnableContentDownloading: bool, EnableSyncTranscoding: bool, EnableMediaConversion: bool, EnabledDevices: list, EnableAllDevices: bool, EnabledChannels: list, EnableAllChannels: bool, EnabledFolders: list, EnableAllFolders: bool, InvalidLoginAttemptCount: int, LoginAttemptsBeforeLockout: int, MaxActiveSessions: int, EnablePublicSharing: bool, BlockedMediaFolders: list, BlockedChannels: list, RemoteClientBitrateLimit: int, AuthenticationProviderId: string, PasswordResetProviderId: string, SyncPlayAccess: record>, PrimaryImageAspectRatio: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/Users/Public")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets intros to play before the main media item plays.
#
# GET /Items/{itemId}/Intros
# operationId: GetIntros
export def "items-intros GetIntros" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/Intros" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets local trailers for an item.
#
# GET /Items/{itemId}/LocalTrailers
# operationId: GetLocalTrailers
export def "items-local-trailers GetLocalTrailers" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/LocalTrailers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets special features for an item.
#
# GET /Items/{itemId}/SpecialFeatures
# operationId: GetSpecialFeatures
export def "items-special-features GetSpecialFeatures" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Items/($itemId)/SpecialFeatures" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets latest media.
#
# GET /Items/Latest
# operationId: GetLatestMedia
export def "items-latest GetLatestMedia" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --includeItemTypes: list # Optional. If specified, results will be filtered based on item type. This allows multiple, comma delimited.
  --isPlayed: oneof<nothing, bool> # Filter by items that are played, or not.
  --enableImages: oneof<nothing, bool> # Optional. include image information in output.
  --imageTypeLimit: int # Optional. the max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --enableUserData: oneof<nothing, bool> # Optional. include user data.
  --limit: int # Return item limit. (format: int32, default: 20)
  --groupItems: oneof<nothing, bool> # Whether or not to group items into a parent container. (default: true)
]: nothing -> table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "isPlayed" $isPlayed "scalar") (serialize-qp "enableImages" $enableImages "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "groupItems" $groupItems "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Items/Latest" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the root folder from a user's library.
#
# GET /Items/Root
# operationId: GetRootFolder
export def "items-root GetRootFolder" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Items/Root" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Marks an item as a favorite.
#
# POST /UserFavoriteItems/{itemId}
# operationId: MarkFavoriteItem
export def "user-favorite-items MarkFavoriteItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserFavoriteItems/($itemId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Unmarks item as a favorite.
#
# DELETE /UserFavoriteItems/{itemId}
# operationId: UnmarkFavoriteItem
export def "user-favorite-items UnmarkFavoriteItem" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserFavoriteItems/($itemId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Deletes a user's saved personal rating for an item.
#
# DELETE /UserItems/{itemId}/Rating
# operationId: DeleteUserItemRating
export def "user-items-rating DeleteUserItemRating" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserItems/($itemId)/Rating" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a user's rating for an item.
#
# POST /UserItems/{itemId}/Rating
# operationId: UpdateUserItemRating
export def "user-items-rating UpdateUserItemRating" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
  --likes: oneof<nothing, bool> # Whether this M:Jellyfin.Api.Controllers.UserLibraryController.UpdateUserItemRating(System.Nullable{System.Guid},System.Guid,System.Nullable{System.Boolean}) is likes.
]: nothing -> record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "likes" $likes "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/UserItems/($itemId)/Rating" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get user views.
#
# GET /UserViews
# operationId: GetUserViews
export def "user-views GetUserViews" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
  --includeExternalContent: oneof<nothing, bool> # Whether or not to include external views such as channels or live tv.
  --presetViews: list # Preset views.
  --includeHidden: oneof<nothing, bool> # Whether or not to include hidden content. (default: false)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar") (serialize-qp "includeExternalContent" $includeExternalContent "scalar") (serialize-qp "presetViews" $presetViews "multi") (serialize-qp "includeHidden" $includeHidden "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/UserViews" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get user view grouping options.
#
# GET /UserViews/GroupingOptions
# operationId: GetGroupingOptions
export def "user-views-grouping-options GetGroupingOptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # User id. (format: uuid)
]: nothing -> table<Name: string, Id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/UserViews/GroupingOptions" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get video attachment.
#
# GET /Videos/{videoId}/{mediaSourceId}/Attachments/{index}
# operationId: GetAttachment
export def "videos-attachments GetAttachment" [
  videoId: string
  mediaSourceId: string
  index: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Videos/($videoId)/($mediaSourceId)/Attachments/($index)")
  let accept_val = "application/octet-stream"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets additional parts for a video.
#
# GET /Videos/{itemId}/AdditionalParts
# operationId: GetAdditionalPart
export def "videos-additional-parts GetAdditionalPart" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/AdditionalParts" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Removes alternate video sources.
#
# DELETE /Videos/{itemId}/AlternateSources
# operationId: DeleteAlternateSources
export def "videos-alternate-sources DeleteAlternateSources" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/Videos/($itemId)/AlternateSources")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video stream.
#
# GET /Videos/{itemId}/stream
# operationId: GetVideoStream
@deprecated --flag deviceProfileId
export def "videos-stream GetVideoStream" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --container: string # The video container. Possible values are: ts, webm, asf, wmv, ogv, mp4, m4v, mkv, mpeg, mpg, avi, 3gp, wmv, wtv, m2ts, mov, iso, flv.
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "container" $container "scalar") (serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/stream" $qp)
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video stream.
#
# HEAD /Videos/{itemId}/stream
# operationId: HeadVideoStream
@deprecated --flag deviceProfileId
export def "videos-stream HeadVideoStream" [
  itemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --container: string # The video container. Possible values are: ts, webm, asf, wmv, ogv, mp4, m4v, mkv, mpeg, mpg, avi, 3gp, wmv, wtv, m2ts, mov, iso, flv.
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize. (DEPRECATED)
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "container" $container "scalar") (serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/stream" $qp)
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video stream.
#
# GET /Videos/{itemId}/stream.{container}
# operationId: GetVideoStreamByContainer
export def "videos-stream-container GetVideoStreamByContainer" [
  itemId: string
  container: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize.
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/stream.($container)" $qp)
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a video stream.
#
# HEAD /Videos/{itemId}/stream.{container}
# operationId: HeadVideoStreamByContainer
export def "videos-stream-container HeadVideoStreamByContainer" [
  itemId: string
  container: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --static: oneof<nothing, bool> # Optional. If true, the original file will be streamed statically without any encoding. Use either no url extension or the original file extension. true/false.
  --params: string # The streaming parameters.
  --tag: string # The tag.
  --deviceProfileId: string # Optional. The dlna device profile id to utilize.
  --playSessionId: string # The play session id.
  --segmentContainer: string # The segment container.
  --segmentLength: int # The segment length. (format: int32)
  --minSegments: int # The minimum number of segments. (format: int32)
  --mediaSourceId: string # The media version id, if playing an alternate version.
  --deviceId: string # The device id of the client requesting. Used to stop encoding processes when needed.
  --audioCodec: string # Optional. Specify an audio codec to encode to, e.g. mp3. If omitted the server will auto-select using the url's extension.
  --enableAutoStreamCopy: oneof<nothing, bool> # Whether or not to allow automatic stream copy if requested values match the original source. Defaults to true.
  --allowVideoStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the video stream url.
  --allowAudioStreamCopy: oneof<nothing, bool> # Whether or not to allow copying of the audio stream url.
  --breakOnNonKeyFrames: oneof<nothing, bool> # Optional. Whether to break on non key frames.
  --audioSampleRate: int # Optional. Specify a specific audio sample rate, e.g. 44100. (format: int32)
  --maxAudioBitDepth: int # Optional. The maximum audio bit depth. (format: int32)
  --audioBitRate: int # Optional. Specify an audio bitrate to encode to, e.g. 128000. If omitted this will be left to encoder defaults. (format: int32)
  --audioChannels: int # Optional. Specify a specific number of audio channels to encode to, e.g. 2. (format: int32)
  --maxAudioChannels: int # Optional. Specify a maximum number of audio channels to encode to, e.g. 2. (format: int32)
  --profile: string # Optional. Specify a specific an encoder profile (varies by encoder), e.g. main, baseline, high.
  --level: string # Optional. Specify a level for the encoder profile (varies by encoder), e.g. 3, 3.1.
  --framerate: float # Optional. A specific video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --maxFramerate: float # Optional. A specific maximum video framerate to encode to, e.g. 23.976. Generally this should be omitted unless the device has specific requirements. (format: float)
  --copyTimestamps: oneof<nothing, bool> # Whether or not to copy timestamps when transcoding with an offset. Defaults to false.
  --startTimeTicks: int # Optional. Specify a starting offset, in ticks. 1 tick = 10000 ms. (format: int64)
  --width: int # Optional. The fixed horizontal resolution of the encoded video. (format: int32)
  --height: int # Optional. The fixed vertical resolution of the encoded video. (format: int32)
  --maxWidth: int # Optional. The maximum horizontal resolution of the encoded video. (format: int32)
  --maxHeight: int # Optional. The maximum vertical resolution of the encoded video. (format: int32)
  --videoBitRate: int # Optional. Specify a video bitrate to encode to, e.g. 500000. If omitted this will be left to encoder defaults. (format: int32)
  --subtitleStreamIndex: int # Optional. The index of the subtitle stream to use. If omitted no subtitles will be used. (format: int32)
  --subtitleMethod: string@subtitleMethod-completer # Optional. Specify the subtitle delivery method.
  --maxRefFrames: int # Optional. (format: int32)
  --maxVideoBitDepth: int # Optional. The maximum video bit depth. (format: int32)
  --requireAvc: oneof<nothing, bool> # Optional. Whether to require avc.
  --deInterlace: oneof<nothing, bool> # Optional. Whether to deinterlace the video.
  --requireNonAnamorphic: oneof<nothing, bool> # Optional. Whether to require a non anamorphic stream.
  --transcodingMaxAudioChannels: int # Optional. The maximum number of audio channels to transcode. (format: int32)
  --cpuCoreLimit: int # Optional. The limit of how many cpu cores to use. (format: int32)
  --liveStreamId: string # The live stream id.
  --enableMpegtsM2TsMode: oneof<nothing, bool> # Optional. Whether to enable the MpegtsM2Ts mode.
  --videoCodec: string # Optional. Specify a video codec to encode to, e.g. h264. If omitted the server will auto-select using the url's extension.
  --subtitleCodec: string # Optional. Specify a subtitle codec to encode to.
  --transcodeReasons: string # Optional. The transcoding reason.
  --audioStreamIndex: int # Optional. The index of the audio stream to use. If omitted the first audio stream will be used. (format: int32)
  --videoStreamIndex: int # Optional. The index of the video stream to use. If omitted the first video stream will be used. (format: int32)
  --context: string@context-completer # Optional. The MediaBrowser.Model.Dlna.EncodingContext.
  --streamOptions: record # Optional. The streaming options.
  --enableAudioVbrEncoding: oneof<nothing, bool> # Optional. Whether to enable Audio Encoding. (default: true)
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "static" $static "scalar") (serialize-qp "params" $params "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "deviceProfileId" $deviceProfileId "scalar") (serialize-qp "playSessionId" $playSessionId "scalar") (serialize-qp "segmentContainer" $segmentContainer "scalar") (serialize-qp "segmentLength" $segmentLength "scalar") (serialize-qp "minSegments" $minSegments "scalar") (serialize-qp "mediaSourceId" $mediaSourceId "scalar") (serialize-qp "deviceId" $deviceId "scalar") (serialize-qp "audioCodec" $audioCodec "scalar") (serialize-qp "enableAutoStreamCopy" $enableAutoStreamCopy "scalar") (serialize-qp "allowVideoStreamCopy" $allowVideoStreamCopy "scalar") (serialize-qp "allowAudioStreamCopy" $allowAudioStreamCopy "scalar") (serialize-qp "breakOnNonKeyFrames" $breakOnNonKeyFrames "scalar") (serialize-qp "audioSampleRate" $audioSampleRate "scalar") (serialize-qp "maxAudioBitDepth" $maxAudioBitDepth "scalar") (serialize-qp "audioBitRate" $audioBitRate "scalar") (serialize-qp "audioChannels" $audioChannels "scalar") (serialize-qp "maxAudioChannels" $maxAudioChannels "scalar") (serialize-qp "profile" $profile "scalar") (serialize-qp "level" $level "scalar") (serialize-qp "framerate" $framerate "scalar") (serialize-qp "maxFramerate" $maxFramerate "scalar") (serialize-qp "copyTimestamps" $copyTimestamps "scalar") (serialize-qp "startTimeTicks" $startTimeTicks "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "height" $height "scalar") (serialize-qp "maxWidth" $maxWidth "scalar") (serialize-qp "maxHeight" $maxHeight "scalar") (serialize-qp "videoBitRate" $videoBitRate "scalar") (serialize-qp "subtitleStreamIndex" $subtitleStreamIndex "scalar") (serialize-qp "subtitleMethod" $subtitleMethod "scalar") (serialize-qp "maxRefFrames" $maxRefFrames "scalar") (serialize-qp "maxVideoBitDepth" $maxVideoBitDepth "scalar") (serialize-qp "requireAvc" $requireAvc "scalar") (serialize-qp "deInterlace" $deInterlace "scalar") (serialize-qp "requireNonAnamorphic" $requireNonAnamorphic "scalar") (serialize-qp "transcodingMaxAudioChannels" $transcodingMaxAudioChannels "scalar") (serialize-qp "cpuCoreLimit" $cpuCoreLimit "scalar") (serialize-qp "liveStreamId" $liveStreamId "scalar") (serialize-qp "enableMpegtsM2TsMode" $enableMpegtsM2TsMode "scalar") (serialize-qp "videoCodec" $videoCodec "scalar") (serialize-qp "subtitleCodec" $subtitleCodec "scalar") (serialize-qp "transcodeReasons" $transcodeReasons "scalar") (serialize-qp "audioStreamIndex" $audioStreamIndex "scalar") (serialize-qp "videoStreamIndex" $videoStreamIndex "scalar") (serialize-qp "context" $context "scalar") (serialize-qp "streamOptions" $streamOptions "multi") (serialize-qp "enableAudioVbrEncoding" $enableAudioVbrEncoding "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Videos/($itemId)/stream.($container)" $qp)
  let accept_val = "video/*"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "head" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Merges videos into a single record.
#
# POST /Videos/MergeVersions
# operationId: MergeVersions
export def "videos-merge-versions MergeVersions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --ids: list # Item id list. This allows multiple, comma delimited.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "ids" $ids "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/Videos/MergeVersions" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get years.
#
# GET /Years
# operationId: GetYears
export def "years GetYears" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --startIndex: int # Skips over a given number of items within the results. Use for paging. (format: int32)
  --limit: int # Optional. The maximum number of records to return. (format: int32)
  --sortOrder: list # Sort Order - Ascending,Descending.
  --parentId: string # Specify this to localize the search to a specific item or folder. Omit to use the root. (format: uuid)
  --qp-fields: list # Optional. Specify additional fields of information to return in the output.
  --excludeItemTypes: list # Optional. If specified, results will be excluded based on item type. This allows multiple, comma delimited.
  --includeItemTypes: list # Optional. If specified, results will be included based on item type. This allows multiple, comma delimited.
  --mediaTypes: list # Optional. Filter by MediaType. Allows multiple, comma delimited.
  --sortBy: list # Optional. Specify one or more sort orders, comma delimited. Options: Album, AlbumArtist, Artist, Budget, CommunityRating, CriticRating, DateCreated, DatePlayed, PlayCount, PremiereDate, ProductionYear, SortName, Random, Revenue, Runtime.
  --enableUserData: oneof<nothing, bool> # Optional. Include user data.
  --imageTypeLimit: int # Optional. The max number of images to return, per image type. (format: int32)
  --enableImageTypes: list # Optional. The image types to include in the output.
  --userId: string # User Id. (format: uuid)
  --recursive: oneof<nothing, bool> # Search recursively. (default: true)
  --enableImages: oneof<nothing, bool> # Optional. Include image information in output. (default: true)
]: nothing -> record<Items: table<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>, TotalRecordCount: int, StartIndex: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sortOrder" $sortOrder "multi") (serialize-qp "parentId" $parentId "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "excludeItemTypes" $excludeItemTypes "multi") (serialize-qp "includeItemTypes" $includeItemTypes "multi") (serialize-qp "mediaTypes" $mediaTypes "multi") (serialize-qp "sortBy" $sortBy "multi") (serialize-qp "enableUserData" $enableUserData "scalar") (serialize-qp "imageTypeLimit" $imageTypeLimit "scalar") (serialize-qp "enableImageTypes" $enableImageTypes "multi") (serialize-qp "userId" $userId "scalar") (serialize-qp "recursive" $recursive "scalar") (serialize-qp "enableImages" $enableImages "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/Years" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a year.
#
# GET /Years/{year}
# operationId: GetYear
export def "years GetYear" [
  year: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userId: string # Optional. Filter by user id, and attach user data. (format: uuid)
]: nothing -> record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: table<Name: string, Url: string>, MediaSources: table<Protocol: record, Id: string, Path: string, EncoderPath: string, EncoderProtocol: record, Type: record, Container: string, Size: int, Name: string, IsRemote: bool, ETag: string, RunTimeTicks: int, ReadAtNativeFramerate: bool, IgnoreDts: bool, IgnoreIndex: bool, GenPtsInput: bool, SupportsTranscoding: bool, SupportsDirectStream: bool, SupportsDirectPlay: bool, IsInfiniteStream: bool, UseMostCompatibleTranscodingProfile: bool, RequiresOpening: bool, OpenToken: string, RequiresClosing: bool, LiveStreamId: string, BufferMs: int, RequiresLooping: bool, SupportsProbing: bool, VideoType: record, IsoType: record, Video3DFormat: record, MediaStreams: list, MediaAttachments: list, Formats: list, Bitrate: int, FallbackMaxStreamingBitrate: int, Timestamp: record, RequiredHttpHeaders: record, TranscodingUrl: string, TranscodingSubProtocol: record, TranscodingContainer: string, AnalyzeDurationMs: int, DefaultAudioStreamIndex: int, DefaultSubtitleStreamIndex: int, HasSegments: bool>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: table<Url: string, Name: string>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: table<Name: string, Id: string, Role: string, Type: record, PrimaryImageTag: string, ImageBlurHashes: record>, Studios: table<Name: string, Id: string>, GenreItems: table<Name: string, Id: string>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: table<Name: string, Id: string>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: table<Name: string, Id: string>, SeasonName: string, MediaStreams: table<Codec: string, CodecTag: string, Language: string, ColorRange: string, ColorSpace: string, ColorTransfer: string, ColorPrimaries: string, DvVersionMajor: int, DvVersionMinor: int, DvProfile: int, DvLevel: int, RpuPresentFlag: int, ElPresentFlag: int, BlPresentFlag: int, DvBlSignalCompatibilityId: int, Rotation: int, Comment: string, TimeBase: string, CodecTimeBase: string, Title: string, Hdr10PlusPresentFlag: bool, VideoRange: record, VideoRangeType: record, VideoDoViTitle: string, AudioSpatialFormat: record, LocalizedUndefined: string, LocalizedDefault: string, LocalizedForced: string, LocalizedExternal: string, LocalizedHearingImpaired: string, DisplayTitle: string, NalLengthSize: string, IsInterlaced: bool, IsAVC: bool, ChannelLayout: string, BitRate: int, BitDepth: int, RefFrames: int, PacketLength: int, Channels: int, SampleRate: int, IsDefault: bool, IsForced: bool, IsHearingImpaired: bool, Height: int, Width: int, AverageFrameRate: float, RealFrameRate: float, ReferenceFrameRate: float, Profile: string, Type: record, AspectRatio: string, Index: int, Score: int, IsExternal: bool, DeliveryMethod: record, DeliveryUrl: string, IsExternalUrl: bool, IsTextSubtitleStream: bool, SupportsExternalStream: bool, Path: string, PixelFormat: string, Level: float, IsAnamorphic: bool>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: table<StartPositionTicks: int, Name: string, ImagePath: string, ImageDateModified: string, ImageTag: string>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list<record>, MediaSources: list<record>, CriticRating: float, ProductionLocations: list<string>, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list<string>, Genres: list<string>, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list<record>, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list<record>, Studios: list<record>, GenreItems: list<record>, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list<string>, LocalTrailerCount: int, UserData: record<Rating: float, PlayedPercentage: float, UnplayedItemCount: int, PlaybackPositionTicks: int, PlayCount: int, IsFavorite: bool, Likes: bool, LastPlayedDate: string, Played: bool, Key: string, ItemId: string>, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list<string>, Tags: list<string>, PrimaryImageAspectRatio: float, Artists: list<string>, ArtistItems: list<record>, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list<record>, SeasonName: string, MediaStreams: list<record>, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list<string>, ScreenshotImageTags: list<string>, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record<Primary: record, Art: record, Backdrop: record, Banner: record, Logo: record, Thumb: record, Disc: record, Box: record, Screenshot: record, Menu: record, Chapter: record, BoxRear: record, Profile: record>, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list<record>, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list<string>, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record<Name: string, OriginalTitle: string, ServerId: string, Id: string, Etag: string, SourceType: string, PlaylistItemId: string, DateCreated: string, DateLastMediaAdded: string, ExtraType: record, AirsBeforeSeasonNumber: int, AirsAfterSeasonNumber: int, AirsBeforeEpisodeNumber: int, CanDelete: bool, CanDownload: bool, HasLyrics: bool, HasSubtitles: bool, PreferredMetadataLanguage: string, PreferredMetadataCountryCode: string, Container: string, SortName: string, ForcedSortName: string, Video3DFormat: record, PremiereDate: string, ExternalUrls: list, MediaSources: list, CriticRating: float, ProductionLocations: list, Path: string, EnableMediaSourceDisplay: bool, OfficialRating: string, CustomRating: string, ChannelId: string, ChannelName: string, Overview: string, Taglines: list, Genres: list, CommunityRating: float, CumulativeRunTimeTicks: int, RunTimeTicks: int, PlayAccess: record, AspectRatio: string, ProductionYear: int, IsPlaceHolder: bool, Number: string, ChannelNumber: string, IndexNumber: int, IndexNumberEnd: int, ParentIndexNumber: int, RemoteTrailers: list, ProviderIds: record, IsHD: bool, IsFolder: bool, ParentId: string, Type: record, People: list, Studios: list, GenreItems: list, ParentLogoItemId: string, ParentBackdropItemId: string, ParentBackdropImageTags: list, LocalTrailerCount: int, UserData: record, RecursiveItemCount: int, ChildCount: int, SeriesName: string, SeriesId: string, SeasonId: string, SpecialFeatureCount: int, DisplayPreferencesId: string, Status: string, AirTime: string, AirDays: list, Tags: list, PrimaryImageAspectRatio: float, Artists: list, ArtistItems: list, Album: string, CollectionType: record, DisplayOrder: string, AlbumId: string, AlbumPrimaryImageTag: string, SeriesPrimaryImageTag: string, AlbumArtist: string, AlbumArtists: list, SeasonName: string, MediaStreams: list, VideoType: record, PartCount: int, MediaSourceCount: int, ImageTags: record, BackdropImageTags: list, ScreenshotImageTags: list, ParentLogoImageTag: string, ParentArtItemId: string, ParentArtImageTag: string, SeriesThumbImageTag: string, ImageBlurHashes: record, SeriesStudio: string, ParentThumbItemId: string, ParentThumbImageTag: string, ParentPrimaryImageItemId: string, ParentPrimaryImageTag: string, Chapters: list, Trickplay: record, LocationType: record, IsoType: record, MediaType: record, EndDate: string, LockedFields: list, TrailerCount: int, MovieCount: int, SeriesCount: int, ProgramCount: int, EpisodeCount: int, SongCount: int, AlbumCount: int, ArtistCount: int, MusicVideoCount: int, LockData: bool, Width: int, Height: int, CameraMake: string, CameraModel: string, Software: string, ExposureTime: float, FocalLength: float, ImageOrientation: record, Aperture: float, ShutterSpeed: float, Latitude: float, Longitude: float, Altitude: float, IsoSpeedRating: int, SeriesTimerId: string, ProgramId: string, ChannelPrimaryImageTag: string, StartDate: string, CompletionPercentage: float, IsRepeat: bool, EpisodeTitle: string, ChannelType: record, Audio: record, IsMovie: bool, IsSports: bool, IsSeries: bool, IsLive: bool, IsNews: bool, IsKids: bool, IsPremiere: bool, TimerId: string, NormalizationGain: float, CurrentProgram: record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userId" $userId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/Years/($year)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}
