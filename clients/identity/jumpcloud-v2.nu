# Auto-generated client for JumpCloud API v2.0
# Source: https://docs.jumpcloud.com/api/2.0/index.yaml
# Auth: --token flag or $env.JUMPCLOUD_API_TOKEN

const BASE_URL = "https://console.jumpcloud.com/api/v2"
const DEFAULT_AUTH = "x-api-key"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o JUMPCLOUD_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "x-api-key" => { {headers: {x-api-key: $token_val}, query: ""} }
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def bool-completer [] { ["'true'" "'false'"] }
def base-url-completer [] { ["https://console.jumpcloud.com/api/v2" "https://console.eu.jumpcloud.com/api/v2" "https://console.in.jumpcloud.com/api/v2"] }
def auth-scheme-completer [] { ["x-api-key"] }

# Completers for enum parameters
def useCase-completer [] { ["ADASAUTHORITY" "JCASAUTHORITY" "TWOWAYSYNC" "UNSET"] }
def delegationState-completer [] { ["DISABLED" "ENABLED" "PENDING"] }
def agentType-completer [] { ["IMPORT" "SYNC"] }
def op-completer [] { ["add" "remove" "update"] }
def type-completer [] { ["user" "user_group"] }
def direction-completer [] { ["EXPORT" "IMPORT" "UNSPECIFIED"] }
def sourceType-completer [] { ["CONSTANT" "EXPR" "GOLANG_TEMPLATE" "PATH"] }
def sort-completer [] { ["" "email" "firstname" "lastname"] }
def sortOrder-completer [] { ["asc" "desc"] }
def creation-source-completer [] { ["jumpcloud:bulk" "jumpcloud:custom_integration" "jumpcloud:gapps" "jumpcloud:o365" "jumpcloud:scim" "jumpcloud:workday"] }
def state-completer [] { ["ACTIVATED" "SUSPENDED"] }
def type-completer-1 [] { ["password_manager_item" "system" "system_group"] }
def builtIn-completer [] { ["user_alternate_email" "user_home_addresses" "user_home_phone_numbers" "user_manager" "user_mobile_phone_numbers" "user_other_addresses" "user_other_phone_numbers" "user_password" "user_primary_organization_cost_center" "user_primary_organization_department" "user_primary_organization_description" "user_primary_organization_employee_id" "user_primary_organization_title" "user_status" "user_work_addresses" "user_work_fax_phone_numbers" "user_work_mobile_phone_numbers" "user_work_phone_numbers"] }
def direction-completer-1 [] { ["export" "import"] }
def userLockoutAction-completer [] { ["maintain" "suspend"] }
def userPasswordExpirationAction-completer [] { ["maintain" "remove_access" "suspend"] }
def userLockoutAction-completer-1 [] { ["disable" "remove"] }
def userPasswordExpirationAction-completer-1 [] { ["disable" "remove"] }
def type-completer-2 [] { ["command" "user" "user_group"] }
def builtIn-completer-1 [] { ["user_alternate_email" "user_business_phones" "user_city" "user_country" "user_department" "user_job_title" "user_manager" "user_mobile_phone" "user_office_location" "user_postal_code" "user_principal_name_from_alternate_email" "user_state" "user_street_address"] }
def type-completer-3 [] { ["system" "system_group" "user" "user_group"] }
def op-completer-1 [] { ["add" "remove"] }
def destination-completer [] { ["queue" "resource"] }
def type-completer-4 [] { ["system" "system_group"] }
def membershipMethod-completer [] { ["DYNAMIC_AUTOMATED" "DYNAMIC_REVIEW_REQUIRED" "NOTSET" "STATIC"] }
def type-completer-5 [] { ["command" "policy" "policy_group" "user" "user_group"] }
def type-completer-6 [] { ["active_directory" "application" "g_suite" "idp_routing_policy" "ldap_server" "office_365" "password_manager_item" "policy" "policy_group" "radius_server" "system" "system_group"] }
def state-completer-1 [] { ["active" "inactive"] }
def type-completer-7 [] { ["admin_portal" "application" "ldap" "user_portal"] }
def type-completer-8 [] { ["activate_gapps_user" "activate_o365_user" "activate_user_custom" "lockout_notice_user" "password_expiration" "password_expiration_warning" "password_reset_confirmation" "user_change_password"] }
def accept-completer [] { ["image/gif" "image/jpeg" "image/png"] }
def install-action-completer [] { ["DOWNLOAD_ONLY" "INSTALL_ASAP" "INSTALL_FORCE_RESTART" "INSTALL_LATER"] }
def iframeFeature-completer [] { ["SOFTWARE_MANAGEMENT" "ZERO_TOUCH_CUSTOMER_MANAGEMENT"] }
def status-completer [] { ["APPROVED" "CANCELLED" "REJECTED" "REQUESTED" "RESTORED" "STATUS_UNSPECIFIED"] }
def shareFolders-completer [] { ["AllExcept" "AllUsers" "Specific" "UNSPECIFIED"] }
def allowPersonalUsage-completer [] { ["PERSONAL_USAGE_ALLOWED" "PERSONAL_USAGE_DISALLOWED" "PERSONAL_USAGE_DISALLOWED_USERLESS"] }
def createdWhere-completer [] { ["ADMIN_PORTAL" "API" "USER_PORTAL"] }
def enrollmentType-completer [] { ["BYOD" "COPE" "DEDICATED" "FULLY_MANAGED" "WORK_PROFILE"] }
def labels-completer [] { ["RULE_LABEL_ADMIN" "RULE_LABEL_UNSPECIFIED" "RULE_LABEL_WORKFLOW"] }
def status-completer-1 [] { ["ALERT_STATUS_ACKNOWLEDGED" "ALERT_STATUS_AUTO_RESOLVED" "ALERT_STATUS_DELETED" "ALERT_STATUS_OPEN" "ALERT_STATUS_RESOLVED" "ALERT_STATUS_UNSPECIFIED"] }
def exportType-completer [] { ["csv" "json"] }
def ttlConfig-completer [] { ["TTL_CONFIG_FIXED_DURATION" "TTL_CONFIG_MULTI_SELECT_DURATIONS" "TTL_CONFIG_UNSPECIFIED"] }
def status-completer-2 [] { ["APPROVED" "IGNORED" "NEWLY_DISCOVERED" "UNAPPROVED"] }
def access-restriction-completer [] { ["BLOCK" "DEFAULT_ACTION" "DISMISSIBLE_WARNING" "NO_ACTION" "WARNING"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "activedirectories list" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# List Active Directories
#
# GET /activedirectories
# operationId: activedirectories_list
export def "activedirectories list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<domain: string, id: string, useCase: string, groupsEnabled: bool, delegationState: string, primaryAgent: string, primaryImportAgent: string, permission: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/activedirectories" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new Active Directory
#
# POST /activedirectories
# operationId: activedirectories_post
export def "activedirectories post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --domain: string # Domain name for this Active Directory instance.
  --useCase: string@useCase-completer
  --groupsEnabled: string@bool-completer
  --delegationState: string@delegationState-completer # Delegation state of the Active Directory instance
]: any -> record<domain: string, id: string, useCase: string, groupsEnabled: bool, delegationState: string, primaryAgent: string, primaryImportAgent: string, permission: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/activedirectories")
  let body = {domain: $domain, useCase: $useCase, groupsEnabled: $groupsEnabled, delegationState: $delegationState} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Active Directory Agents
#
# GET /activedirectories/{activedirectory_id}/agents
# operationId: activedirectories_agentsList
export def "activedirectories-agents agentsList" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, state: string, agentType: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/agents" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new Active Directory Agent
#
# POST /activedirectories/{activedirectory_id}/agents
# operationId: activedirectories_agentsPost
export def "activedirectories-agents agentsPost" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --agentType: string@agentType-completer
]: any -> record<connectKey: string, id: string, state: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/agents")
  let body = {agentType: $agentType} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Active Directory Agent
#
# GET /activedirectories/{activedirectory_id}/agents/{agent_id}
# operationId: activedirectories_agentsGet
export def "activedirectories-agents agentsGet" [
  activedirectory_id: string
  agent_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, state: string, agentType: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/agents/($agent_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Active Directory Agent
#
# DELETE /activedirectories/{activedirectory_id}/agents/{agent_id}
# operationId: activedirectories_agentsDelete
export def "activedirectories-agents agentsDelete" [
  activedirectory_id: string
  agent_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/agents/($agent_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of an Active Directory instance
#
# GET /activedirectories/{activedirectory_id}/associations
# operationId: graph_activeDirectoryAssociationsList
export def "activedirectories-associations activeDirectoryAssociationsList" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "active_directory" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of an Active Directory instance
#
# POST /activedirectories/{activedirectory_id}/associations
# operationId: graph_activeDirectoryAssociationsPost
export def "activedirectories-associations activeDirectoryAssociationsPost" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer # Targets which a "active_directory" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the User Groups bound to an Active Directory instance
#
# GET /activedirectories/{activedirectory_id}/usergroups
# operationId: graph_activeDirectoryTraverseUserGroup
export def "activedirectories-usergroups activeDirectoryTraverseUserGroup" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to an Active Directory instance
#
# GET /activedirectories/{activedirectory_id}/users
# operationId: graph_activeDirectoryTraverseUser
export def "activedirectories-users activeDirectoryTraverseUser" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --syncStatus: string@bool-completer # Include sync status for users
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "syncStatus" $syncStatus "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get an Active Directory
#
# GET /activedirectories/{id}
# operationId: activedirectories_get
export def "activedirectories get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<domain: string, id: string, useCase: string, groupsEnabled: bool, delegationState: string, primaryAgent: string, primaryImportAgent: string, permission: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete an Active Directory
#
# DELETE /activedirectories/{id}
# operationId: activedirectories_delete
export def "activedirectories delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<domain: string, id: string, useCase: string, groupsEnabled: bool, delegationState: string, primaryAgent: string, primaryImportAgent: string, permission: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Active Directory
#
# PATCH /activedirectories/{id}
# operationId: activedirectories_patch
export def "activedirectories patch" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --domain: string # Domain name for this Active Directory instance.
  --useCase: string@useCase-completer
  --groupsEnabled: string@bool-completer
  --delegationState: string@delegationState-completer # Delegation state of the Active Directory instance
]: any -> record<domain: string, id: string, useCase: string, groupsEnabled: bool, delegationState: string, primaryAgent: string, primaryImportAgent: string, permission: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($id)")
  let body = {domain: $domain, useCase: $useCase, groupsEnabled: $groupsEnabled, delegationState: $delegationState} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Bulk Translation Rules Operations
#
# POST /activedirectories/{activedirectory_id}/translation-rules/bulk
# operationId: activedirectoriesTranslationRules_bulk
# --insertTranslationRules item shape: {appliedOn?: list, destination?: string, direction?: "UNSPECIFIED"|"EXPORT"|"IMPORT", source?: string, sourceType?: "PATH"|"GOLANG_TEMPLATE"|"CONSTANT"|"EXPR"}
# --updateTranslationRules item shape: {objectId: string, source?: string, destination?: string, direction?: "UNSPECIFIED"|"EXPORT"|"IMPORT", sourceType?: "PATH"|"GOLANG_TEMPLATE"|"CONSTANT"|"EXPR", appliedOn?: list}
export def "activedirectories-translation-rules-bulk bulk" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --insertTranslationRules: list # item shape: {appliedOn?: list, destination?: string, direction?: "UNSPECIFIED"|"EXPORT"|"IMPORT", source?: string, sourceType?: "PATH"|"GOLANG_TEMPLATE"|"CONSTANT"|"EXPR"}
  --updateTranslationRules: list # item shape: {objectId: string, source?: string, destination?: string, direction?: "UNSPECIFIED"|"EXPORT"|"IMPORT", sourceType?: "PATH"|"GOLANG_TEMPLATE"|"CONSTANT"|"EXPR", appliedOn?: list}
  --deleteTranslationRuleObjectIds: list
]: any -> record<deletedTranslationRuleObjectIds: list<string>, insertedTranslationRules: table<appliedOn: list, default: bool, destination: string, direction: string, editable: bool, id: string, required: bool, source: string, sourceType: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/translation-rules/bulk")
  let body = {insertTranslationRules: $insertTranslationRules, updateTranslationRules: $updateTranslationRules, deleteTranslationRuleObjectIds: $deleteTranslationRuleObjectIds} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Translation Rules of the given active directory
#
# GET /activedirectories/{activedirectory_id}/translation-rules
# operationId: activedirectoriesTranslationRules_list
export def "activedirectories-translation-rules list" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<rules: table<appliedOn: list, default: bool, destination: string, direction: string, editable: bool, id: string, required: bool, source: string, sourceType: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/translation-rules" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Translation Rule
#
# POST /activedirectories/{activedirectory_id}/translation-rules
# operationId: activedirectoriesTranslationRules_post
export def "activedirectories-translation-rules post" [
  activedirectory_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --appliedOn: list # Operations when this translation rule is applied (CREATE, UPDATE)
  --destination: string # Destination field path where translated value will be stored
  --direction: string@direction-completer # Direction defines the direction of the attribute mapping. (default: UNSPECIFIED)
  --body-source: string # Source field path or expression to be translated
  --sourceType: string@sourceType-completer # SourceType defines the type of the source field in attribute mapping. (default: PATH)
]: any -> record<translation_rule: record<appliedOn: list<string>, default: bool, destination: string, direction: string, editable: bool, id: string, required: bool, source: string, sourceType: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/translation-rules")
  let body = {appliedOn: $appliedOn, destination: $destination, direction: $direction, source: $body_source, sourceType: $sourceType} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Recommendation Translation Rules of the given active directory
#
# GET /activedirectories/{provider_name}/translation-rules/recommendation
# operationId: activedirectoriesTranslationRules_recommendations
export def "activedirectories-translation-rules-recommendation recommendations" [
  provider_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<rules: table<appliedOn: list, default: bool, destination: string, direction: string, editable: bool, id: string, required: bool, source: string, sourceType: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/activedirectories/($provider_name)/translation-rules/recommendation" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Translation Rule
#
# PUT /activedirectories/{activedirectory_id}/translation-rules/{translation_rule_object_id}
# operationId: activedirectoriesTranslationRules_update
export def "activedirectories-translation-rules update" [
  activedirectory_id: string
  translation_rule_object_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --appliedOn: list # Updated operations when this translation rule is applied
  --destination: string # Updated destination field path
  --body-source: string # Updated source field path or expression
  --sourceType: string@sourceType-completer # SourceType defines the type of the source field in attribute mapping. (default: PATH)
]: any -> record<object_id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/translation-rules/($translation_rule_object_id)")
  let body = {appliedOn: $appliedOn, destination: $destination, source: $body_source, sourceType: $sourceType} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete Translation Rule
#
# DELETE /activedirectories/{activedirectory_id}/translation-rules/{translation_rule_object_id}
# operationId: activedirectoriesTranslationRules_delete
export def "activedirectories-translation-rules delete" [
  activedirectory_id: string
  translation_rule_object_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> bool {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/activedirectories/($activedirectory_id)/translation-rules/($translation_rule_object_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Preview of Translation Rules
#
# POST /activedirectories/translation-rules/preview
# operationId: activedirectoriesTranslationRules_preview
# --translationRules item shape: {appliedOn?: list, default?: bool, destination?: string, direction?: "UNSPECIFIED"|"EXPORT"|"IMPORT", editable?: bool, id?: string, required?: bool, source?: string, sourceType?: "PATH"|"GOLANG_TEMPLATE"|"CONSTANT"|"EXPR"}
export def "activedirectories-translation-rules-preview preview" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  translationRules: list # A list of translation rules to apply for the preview. — item shape: {appliedOn?: list, default?: bool, destination?: string, direction?: "UNSPECIFIED"|"EXPORT"|"IMPORT", editable?: bool, id?: string, required?: bool, source?: string, sourceType?: "PATH"|"GOLANG_TEMPLATE"|"CONSTANT"|"EXPR"}
  userObjectId: string # The unique identifier (Object ID) of the user whose data will be translated. (format: hex)
  --activeDirectoryId: string # The ObjectID of the Active Directory instance. Optional parameter to specify which Active Directory instance to use for the preview. (format: hex)
]: any -> record<destinationUser: string, sourceUser: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/activedirectories/translation-rules/preview")
  let body = {translationRules: $translationRules, userObjectId: $userObjectId, activeDirectoryId: $activeDirectoryId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the association links between an Administrator and Organizations.
#
# GET /administrators/{id}/organizationlinks
# operationId: administratorOrganizations_listByAdministrator
export def "administrators-organizationlinks listByAdministrator" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
]: nothing -> table<administrator: string, organization: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/administrators/($id)/organizationlinks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Allow Adminstrator access to an Organization.
#
# POST /administrators/{id}/organizationlinks
# operationId: administratorOrganizations_createByAdministrator
export def "administrators-organizationlinks createByAdministrator" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organization: string # The identifier for an organization to link this administrator to.
]: any -> record<administrator: string, organization: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/administrators/($id)/organizationlinks")
  let body = {organization: $organization} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Remove association between an Administrator and an Organization.
#
# DELETE /administrators/{administrator_id}/organizationlinks/{id}
# operationId: administratorOrganizations_removeByAdministrator
export def "administrators-organizationlinks removeByAdministrator" [
  administrator_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/administrators/($administrator_id)/organizationlinks/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Apple MDMs
#
# GET /applemdms
# operationId: applemdms_list
export def "applemdms list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # default: 1
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<apnsPushTopic: string, depServerTokenState: string, id: string, name: string, organization: string, dep: record<welcomeScreen: record, setupAssistantOptions: list, enableZeroTouchEnrollment: bool>, ades: record<macos: record, ios: record>, appleCertCreatorAppleID: string, appleCertSerialNumber: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/applemdms" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update an Apple MDM
#
# PUT /applemdms/{id}
# operationId: applemdms_put
# --dep shape: {welcomeScreen?: record, setupAssistantOptions?: list, enableZeroTouchEnrollment?: bool}
# --ades shape: {macos?: record, ios?: record}
export def "applemdms put" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --appleSignedCert: string # A signed certificate obtained from Apple after providing Apple with the plist file provided on POST.
  --encryptedDepServerToken: string # The S/MIME encoded DEP Server Token returned by Apple Business Manager when creating an MDM instance.
  --name: string # A new name for the Apple MDM configuration.
  --defaultSystemGroupID: string # ObjectId uniquely identifying the MDM default System Group.
  --defaultIosUserEnrollmentDeviceGroupID: string # ObjectId uniquely identifying the MDM default iOS user enrollment device group.
  --allowMobileUserEnrollment: string@bool-completer # A toggle to allow mobile device enrollment for an organization.
  --dep: record # shape: {welcomeScreen?: record, setupAssistantOptions?: list, enableZeroTouchEnrollment?: bool}
  --ades: record # shape: {macos?: record, ios?: record}
  --appleCertCreatorAppleID: string # The Apple ID of the admin who created the Apple signed certificate.
]: any -> record<apnsPushTopic: string, depServerTokenState: string, id: string, name: string, organization: string, dep: record<welcomeScreen: record, setupAssistantOptions: list<record>, enableZeroTouchEnrollment: bool>, ades: record<macos: record<welcomeScreen: record, setupAssistantOptions: list, setupOptions: list, enableZeroTouchEnrollment: bool>, ios: record<welcomeScreen: record, setupAssistantOptions: list, setupOptions: list, enableZeroTouchEnrollment: bool>>, appleCertCreatorAppleID: string, appleCertSerialNumber: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($id)")
  let body = {appleSignedCert: $appleSignedCert, encryptedDepServerToken: $encryptedDepServerToken, name: $name, defaultSystemGroupID: $defaultSystemGroupID, defaultIosUserEnrollmentDeviceGroupID: $defaultIosUserEnrollmentDeviceGroupID, allowMobileUserEnrollment: $allowMobileUserEnrollment, dep: $dep, ades: $ades, appleCertCreatorAppleID: $appleCertCreatorAppleID} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete an Apple MDM
#
# DELETE /applemdms/{id}
# operationId: applemdms_delete
export def "applemdms delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<apnsPushTopic: string, depServerTokenState: string, id: string, name: string, organization: string, dep: record<welcomeScreen: record, setupAssistantOptions: list<record>, enableZeroTouchEnrollment: bool>, ades: record<macos: record<welcomeScreen: record, setupAssistantOptions: list, setupOptions: list, enableZeroTouchEnrollment: bool>, ios: record<welcomeScreen: record, setupAssistantOptions: list, setupOptions: list, enableZeroTouchEnrollment: bool>>, appleCertCreatorAppleID: string, appleCertSerialNumber: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Apple MDM CSR Plist
#
# GET /applemdms/{apple_mdm_id}/csr
# operationId: applemdms_csrget
export def "applemdms-csr csrget" [
  apple_mdm_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/csr")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/octet-stream"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Apple MDM DEP Public Key
#
# GET /applemdms/{apple_mdm_id}/depkey
# operationId: applemdms_depkeyget
export def "applemdms-depkey depkeyget" [
  apple_mdm_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/depkey")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/x-pem-file"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Apple MDM Enrollment Profiles
#
# GET /applemdms/{apple_mdm_id}/enrollmentprofiles
# operationId: applemdms_enrollmentprofileslist
export def "applemdms-enrollmentprofiles enrollmentprofileslist" [
  apple_mdm_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<apnsPushTopic: string, depServerTokenState: string, id: string, name: string, organization: string, dep: record<welcomeScreen: record, setupAssistantOptions: list, enableZeroTouchEnrollment: bool>, ades: record<macos: record, ios: record>, appleCertCreatorAppleID: string, appleCertSerialNumber: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/enrollmentprofiles")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get an Apple MDM Enrollment Profile
#
# GET /applemdms/{apple_mdm_id}/enrollmentprofiles/{id}
# operationId: applemdms_enrollmentprofilesget
export def "applemdms-enrollmentprofiles enrollmentprofilesget" [
  apple_mdm_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/enrollmentprofiles/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/x-apple-aspen-config"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Refresh DEP Devices
#
# POST /applemdms/{apple_mdm_id}/refreshdepdevices
# operationId: applemdms_refreshdepdevices
export def "applemdms-refreshdepdevices refreshdepdevices" [
  apple_mdm_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/refreshdepdevices")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Erase Device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/erase
# operationId: applemdms_deviceserase
export def "applemdms-devices-erase deviceserase" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --pin: string # 6-digit PIN, required for MacOS, to erase the device
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/erase")
  let body = {pin: $pin} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Clear Passcode
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/clearPasscode
# operationId: applemdms_deviceClearPasscode
export def "applemdms-devices-clear-passcode deviceClearPasscode" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/clearPasscode")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lock Device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/lock
# operationId: applemdms_deviceslock
export def "applemdms-devices-lock deviceslock" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --pin: string # 6-digit PIN, required for MacOS, to lock the device
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/lock")
  let body = {pin: $pin} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Restart Device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/restart
# operationId: applemdms_devicesrestart
export def "applemdms-devices-restart devicesrestart" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --kextPaths: list # The string to pass when doing a restart and performing a RebuildKernelCache.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/restart")
  let body = {kextPaths: $kextPaths} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Shut Down Device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/shutdown
# operationId: applemdms_devicesshutdown
export def "applemdms-devices-shutdown devicesshutdown" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/shutdown")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Clear Restrictions Password
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/clearRestrictionsPassword
# operationId: applemdms_devicesClearRestrictionsPassword
export def "applemdms-devices-clear-restrictions-password devicesClearRestrictionsPassword" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/clearRestrictionsPassword")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Clears the Activation Lock for a Device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/clearActivationLock
# operationId: applemdms_devicesClearActivationLock
export def "applemdms-devices-clear-activation-lock devicesClearActivationLock" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/clearActivationLock")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get an Application
#
# GET /applications/{application_id}
# operationId: applications_get
export def "applications get" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applications/($application_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of an Application
#
# GET /applications/{application_id}/associations
# operationId: graph_applicationAssociationsList
export def "applications-associations applicationAssociationsList" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "application" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/applications/($application_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of an Application
#
# POST /applications/{application_id}/associations
# operationId: graph_applicationAssociationsPost
export def "applications-associations applicationAssociationsPost" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer # Targets which a "application" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applications/($application_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Create an import job
#
# POST /applications/{application_id}/import/jobs
# operationId: import_create
export def "applications-import-jobs create" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --queryString: string # Query string to filter and sort the user list returned from the application.  The supported filtering and sorting varies by application.  If no value is sent, all users are returned. **Example:** "location=Chicago&department=IT"Query string used to retrieve users from service (default: )
  --allowUserReactivation: string@bool-completer # A boolean value to allow the reactivation of suspended users (default: true)
  --operations: list # Operations to be performed on the user list returned from the application
]: any -> record<jobId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applications/($application_id)/import/jobs")
  let body = {queryString: $queryString, allowUserReactivation: $allowUserReactivation, operations: $operations} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Save application logo
#
# POST /applications/{application_id}/logo
# operationId: applications_postLogo
export def "applications-logo post" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --image: string # The file to upload. (format: binary)
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applications/($application_id)/logo")
  let body = {image: $image} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "multipart/form-data" $body
}

# Delete application image
#
# DELETE /applications/{application_id}/logo
# operationId: applications_deleteLogo
export def "applications-logo delete" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applications/($application_id)/logo")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to an Application
#
# GET /applications/{application_id}/usergroups
# operationId: graph_applicationTraverseUserGroup
export def "applications-usergroups applicationTraverseUserGroup" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/applications/($application_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to an Application
#
# GET /applications/{application_id}/users
# operationId: graph_applicationTraverseUser
export def "applications-users applicationTraverseUser" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/applications/($application_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a list of users to import from an Application IdM service provider
#
# GET /applications/{application_id}/import/users
# operationId: import_users
export def "applications-import-users users" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: string # Filter users by a search term
  --qp-query: string # URL query to merge with the service provider request
  --qp-sort: string@sort-completer # Sort users by supported fields
  --sortOrder: string@sortOrder-completer # default: asc
  --sessionId: string # Import sessionId for manual select and view user
  --isCount: string@bool-completer # IsCount indicates if the request is only for getting the total count of users. (default: false)
  --isCursor: string@bool-completer # Enable cursor-based pagination (default: false)
  --cursor: string # Cursor token for pagination
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<total_count: float, session_id: string, users: table<id: string, email: string, username: string, firstname: string, lastname: string, middlename: string, displayname: string, department: string, jobTitle: string, costCenter: string, employeeType: string, employeeIdentifier: string, company: string, addresses: list, phoneNumbers: list, manager: string, location: string, alternateEmail: string, attributes: list>, cursor: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "scalar") (serialize-qp "query" $qp_query "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "sessionId" $sessionId "scalar") (serialize-qp "isCount" $isCount "scalar") (serialize-qp "isCursor" $isCursor "scalar") (serialize-qp "cursor" $cursor "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/applications/($application_id)/import/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Bulk Users Create
#
# POST /bulk/users
# operationId: bulk_usersCreate
export def "bulk-users usersCreate" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --suppressEmail: string@bool-completer # An option indicating whether to suppress the job results email that will otherwise be sent to the Administrator who created the job. If true, the email won't be sent. If omitted or false, the email will be sent.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --creation-source: string@creation-source-completer # Defines the creation-source header for gapps, o365 and workdays requests. If the header isn't sent, the default value is `jumpcloud:bulk`, if you send the header with a malformed value you receive a 400 error.
  --body: record
]: any -> record<jobId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "suppressEmail" $suppressEmail "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/bulk/users" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id, "creation-source": $creation_source} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Bulk Users Update
#
# PATCH /bulk/users
# operationId: bulk_usersUpdate
export def "bulk-users usersUpdate" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --suppressEmail: string@bool-completer # An option indicating whether to suppress the job results email that will otherwise be sent to the Administrator who created the job. If true, the email won't be sent. If omitted or false, the email will be sent.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body: record
]: any -> record<jobId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "suppressEmail" $suppressEmail "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/bulk/users" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Bulk Expire Users
#
# POST /bulk/user/expires
# operationId: bulk_userExpires
export def "bulk-user-expires userExpires" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body: record
]: any -> record<jobId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/bulk/user/expires")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Bulk Unlock Users
#
# POST /bulk/user/unlocks
# operationId: bulk_userUnlocks
export def "bulk-user-unlocks userUnlocks" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body: record
]: any -> record<jobId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/bulk/user/unlocks")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Bulk Users Results
#
# GET /bulk/users/{job_id}/results
# operationId: bulk_usersCreateResults
export def "bulk-users-results usersCreateResults" [
  job_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<meta: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/bulk/users/($job_id)/results" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Scheduled Userstate Change Jobs
#
# GET /bulk/userstates
# operationId: bulk_userStatesList
export def "bulk-userstates userStatesList" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --skip: int # The offset into the records to return. (default: 0)
  --userid: string # The systemuser id to filter by.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<scheduledJobId: string, scheduledDate: string, state: string, systemUserId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "skip" $skip "scalar") (serialize-qp "userid" $userid "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/bulk/userstates" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Scheduled Userstate Job
#
# POST /bulk/userstates
# operationId: bulk_userStatesCreate
export def "bulk-userstates userStatesCreate" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  user_ids: list # Array of system user ids to schedule for a state change
  state: string@state-completer # The state to move the user(s) to
  start_date: string # Date and time that scheduled action should occur (format: date-time)
  --send-activation-emails: string@bool-completer # Set to true to send activation or welcome email(s) to each user_id upon activation. Set to false to suppress emails. Can only be used with scheduled activation(s).
  --activation-email-override: string # Send the activation or welcome email to the specified email address upon activation. Can only be used with a single user_id and scheduled activation. This field will be ignored if `send_activation_emails` is explicitly set to false.
]: any -> table<scheduledJobId: string, scheduledDate: string, state: string, systemUserId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/bulk/userstates")
  let body = {user_ids: $user_ids, state: $state, start_date: $start_date, send_activation_emails: $send_activation_emails, activation_email_override: $activation_email_override} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete Scheduled Userstate Job
#
# DELETE /bulk/userstates/{id}
# operationId: bulk_userStatesDelete
export def "bulk-userstates userStatesDelete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/bulk/userstates/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get the next scheduled state change for a list of users
#
# GET /bulk/userstates/eventlist/next
# operationId: bulk_userStatesGetNextScheduled
export def "bulk-userstates-eventlist-next userStatesGetNextScheduled" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --users: list # A list of system user IDs, limited to 100 items.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
]: nothing -> record<events_count: int, results: table<scheduledJobId: string, scheduledDate: string, state: string, systemUserId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "users" $users "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/bulk/userstates/eventlist/next" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a Command
#
# GET /commands/{command_id}/associations
# operationId: graph_commandAssociationsList
export def "commands-associations commandAssociationsList" [
  command_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "command" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/commands/($command_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a Command
#
# POST /commands/{command_id}/associations
# operationId: graph_commandAssociationsPost
export def "commands-associations commandAssociationsPost" [
  command_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-1 # Targets which a "command" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/commands/($command_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the System Groups bound to a Command
#
# GET /commands/{command_id}/systemgroups
# operationId: graph_commandTraverseSystemGroup
export def "commands-systemgroups commandTraverseSystemGroup" [
  command_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/commands/($command_id)/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Systems bound to a Command
#
# GET /commands/{command_id}/systems
# operationId: graph_commandTraverseSystem
export def "commands-systems commandTraverseSystem" [
  command_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/commands/($command_id)/systems" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Fetch the queued Commands for an Organization
#
# GET /queuedcommand/workflows
# operationId: commands_getQueuedCommandsByWorkflow
export def "queuedcommand-workflows get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # default: 10
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --search: string # The search string to query records
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<totalCount: int, results: table<id: string, command: string, system: string, pendingCount: int>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "search" $search "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/queuedcommand/workflows" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Cancel all queued commands for an organization by workflow instance Id
#
# DELETE /commandqueue/{workflow_instance_id}
# operationId: commands_cancelQueuedCommandsByWorkflowInstanceId
export def "commandqueue cancelQueuedCommandsByWorkflowInstanceId" [
  workflow_instance_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/commandqueue/($workflow_instance_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List All Directories
#
# GET /directories
# operationId: directories_list
export def "directories list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, name: string, type: string, defaultDomain: record<id: string, domain: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/directories" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Duo Accounts
#
# GET /duo/accounts
# operationId: duo_accountList
export def "duo-accounts accountList" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/duo/accounts")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Duo Account
#
# POST /duo/accounts
# operationId: duo_accountPost
export def "duo-accounts accountPost" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/duo/accounts")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Duo Applications
#
# GET /duo/accounts/{account_id}/applications
# operationId: duo_applicationList
export def "duo-accounts-applications applicationList" [
  account_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<apiHost: string, id: string, integrationKey: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/duo/accounts/($account_id)/applications")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Duo Application
#
# POST /duo/accounts/{account_id}/applications
# operationId: duo_applicationPost
export def "duo-accounts-applications applicationPost" [
  account_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  apiHost: string
  integrationKey: string
  name: string
  secretKey: string
]: any -> record<apiHost: string, id: string, integrationKey: string, name: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/duo/accounts/($account_id)/applications")
  let body = {apiHost: $apiHost, integrationKey: $integrationKey, name: $name, secretKey: $secretKey} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a Duo application
#
# GET /duo/accounts/{account_id}/applications/{application_id}
# operationId: duo_applicationGet
export def "duo-accounts-applications applicationGet" [
  account_id: string
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<apiHost: string, id: string, integrationKey: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/duo/accounts/($account_id)/applications/($application_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Duo Application
#
# PUT /duo/accounts/{account_id}/applications/{application_id}
# operationId: duo_applicationUpdate
export def "duo-accounts-applications applicationUpdate" [
  account_id: string
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  apiHost: string
  integrationKey: string
  name: string
  --secretKey: string
]: any -> record<apiHost: string, id: string, integrationKey: string, name: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/duo/accounts/($account_id)/applications/($application_id)")
  let body = {apiHost: $apiHost, integrationKey: $integrationKey, name: $name, secretKey: $secretKey} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a Duo Application
#
# DELETE /duo/accounts/{account_id}/applications/{application_id}
# operationId: duo_applicationDelete
export def "duo-accounts-applications applicationDelete" [
  account_id: string
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<apiHost: string, id: string, integrationKey: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/duo/accounts/($account_id)/applications/($application_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a Duo Acount
#
# GET /duo/accounts/{id}
# operationId: duo_accountGet
export def "duo-accounts accountGet" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/duo/accounts/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete a Duo Account
#
# DELETE /duo/accounts/{id}
# operationId: duo_accountDelete
export def "duo-accounts accountDelete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/duo/accounts/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List All Groups
#
# GET /groups
# operationId: groups_list
export def "groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-unfiltered-total-count: int # If provided in the request with any non-empty value, this header will be returned on the response populated with the total count of objects without filters taken into account
]: nothing -> table<attributes: record, description: string, email: string, id: string, name: string, type: string, memberQueryErrorFlags: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/groups" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "x-unfiltered-total-count": $x_unfiltered_total_count} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a G Suite instance
#
# GET /gsuites/{gsuite_id}/associations
# operationId: graph_gSuiteAssociationsList
export def "gsuites-associations gSuiteAssociationsList" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "g_suite" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a G Suite instance
#
# POST /gsuites/{gsuite_id}/associations
# operationId: graph_gSuiteAssociationsPost
export def "gsuites-associations gSuiteAssociationsPost" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer # Targets which a "g_suite" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List all the G Suite Translation Rules
#
# GET /gsuites/{gsuite_id}/translationrules
# operationId: translationRules_gSuiteList
export def "gsuites-translationrules gSuiteList" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> table<builtIn: string, direction: string, id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/translationrules" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new G Suite Translation Rule
#
# POST /gsuites/{gsuite_id}/translationrules
# operationId: translationRules_gSuitePost
export def "gsuites-translationrules gSuitePost" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --builtIn: string@builtIn-completer # Built-in translations for G Suite export: * `user_home_addresses` - Translate all JumpCloud user addresses of type `home` to G Suite Directory user addresses of type `home` * `user_work_addresses` - Translate all JumpCloud user addresses of type `work` to G Suite Directory user addresses of type `work` * `user_other_addresses` - Translate all JumpCloud user addresses of type `other` to G Suite Directory user addresses of type `other` * `user_home_phone_numbers` - Translate all JumpCloud user phoneNumbers of type `home` to G Suite Directory user phones of type `home` * `user_mobile_phone_numbers` - Translate all JumpCloud user phoneNumbers of type `mobile` to G Suite Directory user phones of type `mobile` * `user_other_phone_numbers` - Translate all JumpCloud user phoneNumbers of type `other` to G Suite Directory user phones of type `other` * `user_work_phone_numbers` - Translate all JumpCloud user phoneNumbers of type `work` to G Suite Directory user phones of type `work` * `user_work_fax_phone_numbers` - Translate all JumpCloud user phoneNumbers of type `work_fax` to G Suite Directory user phones of type `work_fax` * `user_work_mobile_phone_numbers` - Translate all JumpCloud user phoneNumbers of type `work_mobile` to G Suite Directory user phones of type `work_mobile` * `user_manager` - Translate JumpCloud user `manager` to G Suite Directory user `relations-manager` * `user_primary_organization_cost_center` - Translate JumpCloud user  `costCenter` to G Suite Directory user `costCenter` for `primary` organization * `user_primary_organization_department` - Translate JumpCloud user  `department` to G Suite Directory user `department` for `primary` organization * `user_primary_organization_description` - Translate JumpCloud user  `employeeType` to G Suite Directory user `description` for `primary` organization * `user_primary_organization_employee_id` - Translate JumpCloud user  `employeeIdentifier` to G Suite Directory user `externalIds` element of type `organization` * `user_primary_organization_title` - Translate JumpCloud user  `jobTitle` to G Suite Directory user `title` for `primary` organization * `user_alternate_email` - Translate JumpCloud user  `alternateEmail` to G Suite Directory user `emails` * `user_status` - Translate JumpCloud user `status` to G Suite Directory user `status` * `user_password` - Translate JumpCloud user `password` to G Suite Directory user `password`
  --direction: string@direction-completer-1 # Direction identify if an attribute is going to be exported or imported from GSuite * `Import`- The data will be imported from GSuite into the user modal * `Export`- The data will be exported from the user modal to GSuite  (default: export)
]: any -> record<builtIn: string, direction: string, id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/translationrules")
  let body = {builtIn: $builtIn, direction: $direction} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Gets a specific G Suite translation rule
#
# GET /gsuites/{gsuite_id}/translationrules/{id}
# operationId: translationRules_gSuiteGet
export def "gsuites-translationrules gSuiteGet" [
  gsuite_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<builtIn: string, direction: string, id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/translationrules/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a G Suite translation rule
#
# DELETE /gsuites/{gsuite_id}/translationrules/{id}
# operationId: translationRules_gSuiteDelete
export def "gsuites-translationrules gSuiteDelete" [
  gsuite_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/translationrules/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to a G Suite instance
#
# GET /gsuites/{gsuite_id}/usergroups
# operationId: graph_gSuiteTraverseUserGroup
export def "gsuites-usergroups gSuiteTraverseUserGroup" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a G Suite instance
#
# GET /gsuites/{gsuite_id}/users
# operationId: graph_gSuiteTraverseUser
export def "gsuites-users gSuiteTraverseUser" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --syncStatus: string@bool-completer # Include sync status for users
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "syncStatus" $syncStatus "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get G Suite
#
# GET /gsuites/{id}
# operationId: gsuites_get
export def "gsuites get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, userLockoutAction: string, userPasswordExpirationAction: string, name: string, groupsEnabled: bool, importFilter: string, defaultDomain: record<id: string, domain: string>, organizationObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update existing G Suite
#
# PATCH /gsuites/{id}
# operationId: gsuites_patch
# --defaultDomain shape: {id?: string}
export def "gsuites patch" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --userLockoutAction: string@userLockoutAction-completer
  --userPasswordExpirationAction: string@userPasswordExpirationAction-completer
  --name: string
  --groupsEnabled: string@bool-completer
  --importFilter: string
  --defaultDomain: record # shape: {id?: string}
  --organizationObjectId: string # OrganizationObjectId is the object id of the organization that the account belongs to. (format: byte)
]: any -> record<id: string, userLockoutAction: string, userPasswordExpirationAction: string, name: string, groupsEnabled: bool, importFilter: string, defaultDomain: record<id: string, domain: string>, organizationObjectId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($id)")
  let body = {userLockoutAction: $userLockoutAction, userPasswordExpirationAction: $userPasswordExpirationAction, name: $name, groupsEnabled: $groupsEnabled, importFilter: $importFilter, defaultDomain: $defaultDomain, organizationObjectId: $organizationObjectId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List IP Lists
#
# GET /iplists
# operationId: iplists_list
export def "iplists list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-total-count: int
]: nothing -> table<id: string, name: string, description: string, ips: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/iplists" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "x-total-count": $x_total_count} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create IP List
#
# POST /iplists
# operationId: iplists_post
export def "iplists post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --description: string
  --ips: list
]: any -> record<id: string, name: string, description: string, ips: list<string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/iplists")
  let body = {name: $name, description: $description, ips: $ips} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List LDAP Servers
#
# GET /ldapservers
# operationId: ldapservers_list
export def "ldapservers list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, name: string, userLockoutAction: string, userPasswordExpirationAction: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/ldapservers" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get LDAP Server
#
# GET /ldapservers/{id}
# operationId: ldapservers_get
export def "ldapservers get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string, userLockoutAction: string, userPasswordExpirationAction: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ldapservers/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update existing LDAP server
#
# PATCH /ldapservers/{id}
# operationId: ldapservers_patch
export def "ldapservers patch" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body-id: string
  --userLockoutAction: string@userLockoutAction-completer-1
  --userPasswordExpirationAction: string@userPasswordExpirationAction-completer-1
]: any -> record<id: string, name: string, userLockoutAction: string, userPasswordExpirationAction: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ldapservers/($id)")
  let body = {id: $body_id, userLockoutAction: $userLockoutAction, userPasswordExpirationAction: $userPasswordExpirationAction} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the associations of a LDAP Server
#
# GET /ldapservers/{ldapserver_id}/associations
# operationId: graph_ldapServerAssociationsList
export def "ldapservers-associations ldapServerAssociationsList" [
  ldapserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "ldap_server" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a LDAP Server
#
# POST /ldapservers/{ldapserver_id}/associations
# operationId: graph_ldapServerAssociationsPost
export def "ldapservers-associations ldapServerAssociationsPost" [
  ldapserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer # Targets which a "ldap_server" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Samba Domains
#
# GET /ldapservers/{ldapserver_id}/sambadomains
# operationId: ldapservers_sambaDomainsList
export def "ldapservers-sambadomains sambaDomainsList" [
  ldapserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, name: string, sid: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/sambadomains" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Samba Domain
#
# POST /ldapservers/{ldapserver_id}/sambadomains
# operationId: ldapservers_sambaDomainsPost
export def "ldapservers-sambadomains sambaDomainsPost" [
  ldapserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  name: string # Name of this domain's WorkGroup
  sid: string # Security identifier of this domain
]: any -> record<id: string, name: string, sid: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/sambadomains")
  let body = {name: $name, sid: $sid} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Samba Domain
#
# GET /ldapservers/{ldapserver_id}/sambadomains/{id}
# operationId: ldapservers_sambaDomainsGet
export def "ldapservers-sambadomains sambaDomainsGet" [
  ldapserver_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string, sid: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/sambadomains/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Samba Domain
#
# PUT /ldapservers/{ldapserver_id}/sambadomains/{id}
# operationId: ldapservers_sambaDomainsPut
export def "ldapservers-sambadomains sambaDomainsPut" [
  ldapserver_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  name: string # Name of this domain's WorkGroup
  sid: string # Security identifier of this domain
]: any -> record<id: string, name: string, sid: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/sambadomains/($id)")
  let body = {name: $name, sid: $sid} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete Samba Domain
#
# DELETE /ldapservers/{ldapserver_id}/sambadomains/{id}
# operationId: ldapservers_sambaDomainsDelete
export def "ldapservers-sambadomains sambaDomainsDelete" [
  ldapserver_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> string {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/sambadomains/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to a LDAP Server
#
# GET /ldapservers/{ldapserver_id}/usergroups
# operationId: graph_ldapServerTraverseUserGroup
export def "ldapservers-usergroups ldapServerTraverseUserGroup" [
  ldapserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a LDAP Server
#
# GET /ldapservers/{ldapserver_id}/users
# operationId: graph_ldapServerTraverseUser
export def "ldapservers-users ldapServerTraverseUser" [
  ldapserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/ldapservers/($ldapserver_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of an Password Manager item
#
# GET /passwordmanageritems/{passwordmanageritem_id}/associations
# operationId: graph_passwordManagerItemAssociationsList
export def "passwordmanageritems-associations passwordManagerItemAssociationsList" [
  passwordmanageritem_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "password_manager_item" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanageritems/($passwordmanageritem_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a Password Manager Item
#
# POST /passwordmanageritems/{passwordmanageritem_id}/associations
# operationId: graph_passwordManagerItemAssociationsPost
export def "passwordmanageritems-associations passwordManagerItemAssociationsPost" [
  passwordmanageritem_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-2 # Targets which an "password_manager_item" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanageritems/($passwordmanageritem_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Users bound to a Password Manager Item
#
# GET /passwordmanageritems/{passwordmanageritem_id}/users
# operationId: graph_passwordManagerItemTraverseUser
export def "passwordmanageritems-users passwordManagerItemTraverseUser" [
  passwordmanageritem_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanageritems/($passwordmanageritem_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to a Password Manager Item
#
# GET /passwordmanageritems/{passwordmanageritem_id}/usergroups
# operationId: graph_passwordManagerItemTraverseUserGroup
export def "passwordmanageritems-usergroups passwordManagerItemTraverseUserGroup" [
  passwordmanageritem_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanageritems/($passwordmanageritem_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Office 365 instance
#
# GET /office365s/{office365_id}
# operationId: office365s_get
export def "office365s get" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, userLockoutAction: string, userPasswordExpirationAction: string, name: string, groupsEnabled: bool, defaultDomain: record<id: string, domain: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update existing Office 365 instance.
#
# PATCH /office365s/{office365_id}
# operationId: office365s_patch
# --defaultDomain shape: {id?: string}
export def "office365s patch" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --groupsEnabled: string@bool-completer
  --defaultDomain: record # shape: {id?: string}
]: any -> record<id: string, userLockoutAction: string, userPasswordExpirationAction: string, name: string, groupsEnabled: bool, defaultDomain: record<id: string, domain: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)")
  let body = {name: $name, groupsEnabled: $groupsEnabled, defaultDomain: $defaultDomain} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the associations of an Office 365 instance
#
# GET /office365s/{office365_id}/associations
# operationId: graph_office365AssociationsList
export def "office365s-associations office365AssociationsList" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "office_365" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/office365s/($office365_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of an Office 365 instance
#
# POST /office365s/{office365_id}/associations
# operationId: graph_office365AssociationsPost
export def "office365s-associations office365AssociationsPost" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer # Targets which a "office_365" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List all the Office 365 Translation Rules
#
# GET /office365s/{office365_id}/translationrules
# operationId: translationRules_office365List
export def "office365s-translationrules office365List" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> table<builtIn: string, direction: string, id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/office365s/($office365_id)/translationrules" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new Office 365 Translation Rule
#
# POST /office365s/{office365_id}/translationrules
# operationId: translationRules_office365Post
export def "office365s-translationrules office365Post" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --builtIn: string@builtIn-completer-1 # Built-in translations for Office 365 (Microsoft Graph) export: * `user_alternate_email` - Translate `alternateEmail` field of JumpCloud user to `otherMails` field of Microsoft Graph `user` * `user_business_phones` - Translate `number` field of first JumpCloud user `phoneNumber` of type `work` to `businessPhones` field of Microsoft Graph `user` * `user_city` - Translate `locality` field of JumpCloud user address of type `work` to `city` field of Microsoft Graph user * `user_country` - Translate `country` field of JumpCloud user address of type `work` to `country` field of Microsoft Graph `user` * `user_department` - Translate `department` field of JumpCloud user to `department` field of Microsoft Graph `user` * `user_job_title` - Translate `jobTitle` field of JumpCloud user to `jobTitle` field of Microsoft Graph `user` * `user_manager` - Translate `manager` field of JumpCloud user to `manager` field of Microsoft Graph `user` * `user_mobile_phone` - Translate `number` field of first JumpCloud user `phoneNumber` of type `mobile` to `mobilePhone` field of Microsoft Graph `user` * `user_office_location` - Translate `location` field of JumpCloud user to `officeLocation` field of Microsoft Graph `user` * `user_postal_code` - Translate `postalCode` field of JumpCloud user address of type `work` to `postalCode` field of Microsoft Graph `user` * `user_principal_name_from_alternate_email` - Translate user `alternateEmail` field of Jumpcloud user to `userPrincipalName` field of Microsoft Graph `user` * `user_state` - Translate `region` field of JumpCloud user address of type `work` to `state` field of Microsoft Graph `user` * `user_street_address` - Translate `streetAddress` field of JumpCloud user address of type `work` to `streetAddress` field of Microsoft Graph user
  --direction: string # Direction identify if a attribute is going to be exported or imported from Office365 * `Export`- The data will exported from the user modal to Office365  (default: export)
]: any -> record<builtIn: string, direction: string, id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)/translationrules")
  let body = {builtIn: $builtIn, direction: $direction} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Gets a specific Office 365 translation rule
#
# GET /office365s/{office365_id}/translationrules/{id}
# operationId: translationRules_office365Get
export def "office365s-translationrules office365Get" [
  office365_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<builtIn: string, direction: string, id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)/translationrules/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes a Office 365 translation rule
#
# DELETE /office365s/{office365_id}/translationrules/{id}
# operationId: translationRules_office365Delete
export def "office365s-translationrules office365Delete" [
  office365_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)/translationrules/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to an Office 365 instance
#
# GET /office365s/{office365_id}/usergroups
# operationId: graph_office365TraverseUserGroup
export def "office365s-usergroups office365TraverseUserGroup" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/office365s/($office365_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to an Office 365 instance
#
# GET /office365s/{office365_id}/users
# operationId: graph_office365TraverseUser
export def "office365s-users office365TraverseUser" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --syncStatus: string@bool-completer # Include sync status for users
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "syncStatus" $syncStatus "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/office365s/($office365_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the association links between an Organization and Administrators.
#
# GET /organizations/{id}/administratorlinks
# operationId: administratorOrganizations_listByOrganization
export def "organizations-administratorlinks listByOrganization" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
]: nothing -> table<administrator: string, organization: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/organizations/($id)/administratorlinks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get all cases (Support/Feature requests) for organization
#
# GET /organizations/cases
# operationId: organizations_orgListCases
export def "organizations-cases orgListCases" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<totalCount: int, results: table<caseNumber: string, label: string, status: string, date: string, subject: string, description: string, reporter: string, reporterEmail: string, organization: string, os: string, caseSubmissionProductType: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/organizations/cases" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint creates sample data for trial organizations as part of the Sample data Opt-In functionality.
#
# POST /sample-data
# operationId: sampleData_post
export def "sample-data post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  resourceTypes: list # Array of resource types to create. Each resource type is processed independently.
]: any -> record<message: string, results: record<created: list<record>, skipped: list<record>, failed: list<record>>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/sample-data")
  let body = {resourceTypes: $resourceTypes} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all cases (Support/Feature requests) for provider
#
# GET /providers/{provider_id}/cases
# operationId: providers_providerListCase
export def "providers-cases providerListCase" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<totalCount: int, results: table<caseNumber: string, label: string, status: string, date: string, subject: string, description: string, reporter: string, reporterEmail: string, organization: string, os: string, caseSubmissionProductType: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/cases" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get the metadata for cases
#
# GET /cases/metadata
# operationId: organizations_casesMetadata
export def "cases-metadata casesMetadata" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<results: record<caseTypes: list<string>, caseSubmissionProductTypes: list<string>, statuses: list<string>, productServiceGroups: list<string>, products: list<string>, severities: list<string>, preferredResponseTimes: list<string>, os: list<string>>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/cases/metadata")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all the Pricing & Packaging Subscriptions
#
# GET /subscriptions
# operationId: subscriptions_get
export def "subscriptions get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> table<annualPrice: float, features: list<record>, displayName: string, listPrice: float, productCode: string, upgradeRecommendation: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/subscriptions")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all the Policies
#
# GET /policies
# operationId: policies_list
export def "policies list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, name: string, template: record<activation: string, behavior: string, deliveryTypes: list, description: string, displayName: string, id: string, name: string, osMetaFamily: string, state: string, osRestrictions: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/policies" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new Policy
#
# POST /policies
# operationId: policies_post
# --template shape: {id: string}
# --values item shape: {configFieldID?: string, sensitive?: bool}
export def "policies post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  name: string # The description for this specific Policy.
  --notes: string # The notes for this specific Policy.
  template: record # shape: {id: string}
  --values: list # item shape: {configFieldID?: string, sensitive?: bool}
]: any -> record<configFields: table<displayType: string, id: string, label: string, name: string, position: float, readOnly: bool, required: bool, sensitive: bool, tooltip: record>, id: string, name: string, notes: string, template: record<activation: string, behavior: string, deliveryTypes: list<string>, description: string, displayName: string, id: string, name: string, osMetaFamily: string, state: string, osRestrictions: list<record>>, values: table<configFieldID: string, sensitive: bool>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/policies")
  let body = {name: $name, notes: $notes, template: $template, values: $values} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Gets a specific Policy.
#
# GET /policies/{id}
# operationId: policies_get
export def "policies get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<configFields: table<displayType: string, id: string, label: string, name: string, position: float, readOnly: bool, required: bool, sensitive: bool, tooltip: record>, id: string, name: string, notes: string, template: record<activation: string, behavior: string, deliveryTypes: list<string>, description: string, displayName: string, id: string, name: string, osMetaFamily: string, state: string, osRestrictions: list<record>>, values: table<configFieldID: string, sensitive: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policies/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update an existing Policy
#
# PUT /policies/{id}
# operationId: policies_put
# --values item shape: {configFieldID?: string, sensitive?: bool}
export def "policies put" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  name: string # The description for this specific Policy.
  --notes: string # The notes for this specific Policy.
  --values: list # item shape: {configFieldID?: string, sensitive?: bool}
]: any -> record<id: string, name: string, template: record<activation: string, behavior: string, deliveryTypes: list<string>, description: string, displayName: string, id: string, name: string, osMetaFamily: string, state: string, osRestrictions: list<record>>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policies/($id)")
  let body = {name: $name, notes: $notes, values: $values} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deletes a Policy
#
# DELETE /policies/{id}
# operationId: policies_delete
export def "policies delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policies/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a Policy
#
# GET /policies/{policy_id}/associations
# operationId: graph_policyAssociationsList
export def "policies-associations policyAssociationsList" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "policy" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a Policy
#
# POST /policies/{policy_id}/associations
# operationId: graph_policyAssociationsPost
export def "policies-associations policyAssociationsPost" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-3 # Targets which a "policy" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policies/($policy_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the parent Groups of a Policy
#
# GET /policies/{policy_id}/memberof
# operationId: graph_policyMemberOf
export def "policies-memberof policyMemberOf" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/memberof" $qp)
  let extra_headers = {"Date": $Date, "Authorization": $Authorization, "x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all the policy results of a policy.
#
# GET /policies/{policy_id}/policyresults
# operationId: policyresults_list
export def "policies-policyresults list" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<detail: string, endedAt: string, exitStatus: int, id: string, policyID: string, startedAt: string, state: string, stdErr: string, stdOut: string, success: bool, systemID: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/policyresults" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists the latest policy results of a policy.
#
# GET /policies/{policy_id}/policystatuses
# operationId: policystatuses_policiesList
export def "policies-policystatuses policiesList" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<detail: string, endedAt: string, exitStatus: int, id: string, policyID: string, startedAt: string, state: string, stdErr: string, stdOut: string, success: bool, systemID: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/policystatuses" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the System Groups bound to a Policy
#
# GET /policies/{policy_id}/systemgroups
# operationId: graph_policyTraverseSystemGroup
export def "policies-systemgroups policyTraverseSystemGroup" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a Policy
#
# GET /policies/{policy_id}/users
# operationId: graph_policyTraverseUser
export def "policies-users policyTraverseUser" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to a Policy
#
# GET /policies/{policy_id}/usergroups
# operationId: graph_policyTraverseUserGroup
export def "policies-usergroups policyTraverseUserGroup" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Systems bound to a Policy
#
# GET /policies/{policy_id}/systems
# operationId: graph_policyTraverseSystem
export def "policies-systems policyTraverseSystem" [
  policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policies/($policy_id)/systems" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List all Policy Groups
#
# GET /policygroups
# operationId: groups_policy_list
export def "policygroups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, description: string, email: string, id: string, name: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/policygroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new Policy Group
#
# POST /policygroups
# operationId: groups_policy_post
export def "policygroups post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  name: string # Display name of a Policy Group.
]: any -> record<attributes: record, description: string, email: string, id: string, name: string, type: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/policygroups")
  let body = {name: $name} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the associations of a Policy Group.
#
# GET /policygroups/{group_id}/associations
# operationId: graph_policyGroupAssociationsList
export def "policygroups-associations policyGroupAssociationsList" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "policy_group" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/policygroups/($group_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a Policy Group
#
# POST /policygroups/{group_id}/associations
# operationId: graph_policyGroupAssociationsPost
export def "policygroups-associations policyGroupAssociationsPost" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-3 # Targets which a "policy_group" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policygroups/($group_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the members of a Policy Group
#
# GET /policygroups/{group_id}/members
# operationId: graph_policyGroupMembersList
export def "policygroups-members policyGroupMembersList" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/policygroups/($group_id)/members" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the members of a Policy Group
#
# POST /policygroups/{group_id}/members
# operationId: graph_policyGroupMembersPost
export def "policygroups-members policyGroupMembersPost" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # The member type.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policygroups/($group_id)/members")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Policy Group's membership
#
# GET /policygroups/{group_id}/membership
# operationId: graph_policyGroupMembership
export def "policygroups-membership policyGroupMembership" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policygroups/($group_id)/membership" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the System Groups bound to Policy Groups
#
# GET /policygroups/{group_id}/systemgroups
# operationId: graph_policyGroupTraverseSystemGroup
export def "policygroups-systemgroups policyGroupTraverseSystemGroup" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policygroups/($group_id)/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Systems bound to a Policy Group
#
# GET /policygroups/{group_id}/systems
# operationId: graph_policyGroupTraverseSystem
export def "policygroups-systems policyGroupTraverseSystem" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policygroups/($group_id)/systems" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a Policy Group
#
# GET /policygroups/{group_id}/users
# operationId: graph_policyGroupTraverseUser
export def "policygroups-users policyGroupTraverseUser" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policygroups/($group_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to a Policy Group
#
# GET /policygroups/{group_id}/usergroups
# operationId: graph_policyGroupTraverseUserGroup
export def "policygroups-usergroups policyGroupTraverseUserGroup" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/policygroups/($group_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# View an individual Policy Group details
#
# GET /policygroups/{id}
# operationId: groups_policy_get
export def "policygroups get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<attributes: record, description: string, email: string, id: string, name: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policygroups/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a Policy Group
#
# PUT /policygroups/{id}
# operationId: groups_policy_put
export def "policygroups put" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  name: string # Display name of a Policy Group.
]: any -> record<attributes: record, description: string, email: string, id: string, name: string, type: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policygroups/($id)")
  let body = {name: $name} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a Policy Group
#
# DELETE /policygroups/{id}
# operationId: groups_policy_delete
export def "policygroups delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<attributes: record, description: string, email: string, id: string, name: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policygroups/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all of the policy results for an organization.
#
# GET /policyresults
# operationId: policyresults_org_list
export def "policyresults list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<detail: string, endedAt: string, exitStatus: int, id: string, policyID: string, startedAt: string, state: string, stdErr: string, stdOut: string, success: bool, systemID: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/policyresults" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a specific Policy Result.
#
# GET /policyresults/{id}
# operationId: policyresults_get
export def "policyresults get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<detail: string, endedAt: string, exitStatus: int, id: string, policyID: string, startedAt: string, state: string, stdErr: string, stdOut: string, success: bool, systemID: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policyresults/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lists all of the Policy Templates
#
# GET /policytemplates
# operationId: policytemplates_list
export def "policytemplates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<activation: string, behavior: string, deliveryTypes: list<string>, description: string, displayName: string, id: string, name: string, osMetaFamily: string, state: string, osRestrictions: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/policytemplates" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a specific Policy Template
#
# GET /policytemplates/{id}
# operationId: policytemplates_get
export def "policytemplates get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<activation: string, behavior: string, configFields: table<displayType: string, id: string, label: string, name: string, position: float, readOnly: bool, required: bool, sensitive: bool, tooltip: record>, description: string, displayName: string, id: string, name: string, osMetaFamily: string, osRestrictions: table<osName: string, earliestVersion: string, isMdmRequired: bool, deprecatedVersion: string, supportedEnrollmentTypes: list, appleRestrictions: record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/policytemplates/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Providers
#
# GET /providers
# operationId: providers_getProviders
export def "providers list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-total-count: int
]: nothing -> table<id: string, name: string, disallowOrgCreation: bool, settings: record<requireAdminMFA: bool>, type: string, contact: record<name: string, email: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/providers")
  let extra_headers = {"x-total-count": $x_total_count} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Provider Applications
#
# GET /providers/{provider_id}/applications
# operationId: applications_listProviderApplications
export def "providers-applications listProviderApplications" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --search: string # The search string to query records
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> table<_id: string, displayLabel: string, displayName: string, logo: record<color: string, url: string>, name: string, provision: record<_id: string, type: string, beta: bool>, sso: record<_id: string, active: bool, type: string, jit: record, beta: bool, certExpired: bool, idpCertExpirationAt: string, idpCertificateUpdatedAt: string, idpPrivateKeyUpdatedAt: string, spCertificateUpdatedAt: string, url: string, hidden: bool>, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "search" $search "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/applications" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Associations for an Active Directory Integration
#
# GET /providers/{provider_id}/activedirectories/{ad_id}/associations
# operationId: providers_listAssociations
export def "providers-activedirectories-associations listAssociations" [
  provider_id: string
  ad_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/activedirectories/($ad_id)/associations")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage Active Directory Integration Associations for Provider
#
# POST /providers/{provider_id}/activedirectories/{ad_id}/associations
# operationId: providers_adbridgeManageAssociations
export def "providers-activedirectories-associations adbridgeManageAssociations" [
  provider_id: string
  ad_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # Targets which a "organization" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/activedirectories/($ad_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Associations for a G Suites Account
#
# GET /providers/{provider_id}/g_suites/{gsuite_id}/associations
# operationId: providers_gSuitesListAssociations
export def "providers-g-suites-associations gSuitesListAssociations" [
  provider_id: string
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/g_suites/($gsuite_id)/associations")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage GSuite account Associations for Provider
#
# POST /providers/{provider_id}/g_suites/{gsuite_id}/associations
# operationId: providers_gSuitesManageAssociations
export def "providers-g-suites-associations gSuitesManageAssociations" [
  provider_id: string
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # Targets which a "organization" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/g_suites/($gsuite_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Manage User Group Associations for Provider
#
# POST /providers/{provider_id}/usergroups/{group_id}/associations
# operationId: providers_userGroupsManageAssociations
export def "providers-usergroups-associations userGroupsManageAssociations" [
  provider_id: string
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # Targets which a "organization" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/usergroups/($group_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Manage System Group Associations for Provider
#
# POST /providers/{provider_id}/systemgroups/{group_id}/associations
# operationId: providers_systemGroupsManageAssociations
export def "providers-systemgroups-associations systemGroupsManageAssociations" [
  provider_id: string
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # Targets which a "organization" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/systemgroups/($group_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Manage Policy Group Associations for Provider
#
# POST /providers/{provider_id}/policygroups/{group_id}/associations
# operationId: providers_policyGroupsManageAssociations
export def "providers-policygroups-associations policyGroupsManageAssociations" [
  provider_id: string
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # Targets which a "organization" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/policygroups/($group_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Active Directories
#
# GET /providers/{provider_id}/activedirectories
# operationId: providers_listProvidersPrimaryOrganizationDomains
export def "providers-activedirectories listProvidersPrimaryOrganizationDomains" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<domain: string, id: string, useCase: string, groupsEnabled: bool, delegationState: string, primaryAgent: string, primaryImportAgent: string, permission: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/activedirectories" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List GSuite Accounts
#
# GET /providers/{provider_id}/g_suites
# operationId: providers_gSuitesListProvidersPrimaryOrganizationAccounts
export def "providers-g-suites gSuitesListProvidersPrimaryOrganizationAccounts" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, userLockoutAction: string, userPasswordExpirationAction: string, name: string, groupsEnabled: bool, importFilter: string, defaultDomain: record<id: string, domain: string>, organizationObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/g_suites" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Provider Application Associations
#
# GET /providers/{provider_id}/applications/{id}/associations
# operationId: providerApplications_getAssociations
export def "providers-applications-associations get" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<totalCount: int, results: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/applications/($id)/associations")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Edit associations of a Provider Application
#
# POST /providers/{provider_id}/applications/{id}/associations
# operationId: providerApplications_editAssociations
export def "providers-applications-associations editAssociations" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-id: string # The ObjectID of Organization being added or removed as an association.
  op: string@op-completer-1 # How to modify the Association.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/applications/($id)/associations")
  let body = {id: $body_id, op: $op} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Provider Administrators
#
# GET /providers/{provider_id}/administrators
# operationId: providers_listAdministrators
export def "providers-administrators listAdministrators" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --sortIgnoreCase: list # The comma separated fields used to sort the collection, ignoring case. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<results: table<apiKeyAllowed: bool, apiKeyHash: record, apiKeySet: bool, email: string, enableMultiFactor: bool, firstname: string, id: string, lastname: string, registered: bool, role: string, roleName: string, roles: list, roleNames: list, suspended: bool, organizationAccessTotal: float>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "sortIgnoreCase" $sortIgnoreCase "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/administrators" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new Provider Administrator
#
# POST /providers/{provider_id}/administrators
# operationId: providers_postAdmins
export def "providers-administrators post" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --apiKeyAllowed: string@bool-completer
  --apiKeyHash: record
  --bindNoOrgs: string@bool-completer # default: false
  email: string
  --enableMultiFactor: string@bool-completer
  --firstname: string
  --lastname: string
  --role: string
  --roleName: string
  --roles: list # Array of role ObjectIds for multi-role assignment (optional, requires feature flag)
  --administratorOrganizations: list # Array of organization ObjectIds for multi-org admin assignment (optional, requires multi-org admin workflow feature flag)
]: any -> record<apiKeyAllowed: bool, apiKeyHash: record, apiKeySet: bool, email: string, enableMultiFactor: bool, firstname: string, id: string, lastname: string, registered: bool, role: string, roleName: string, roles: list<string>, roleNames: list<string>, suspended: bool, organizationAccessTotal: float> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/administrators")
  let body = {apiKeyAllowed: $apiKeyAllowed, apiKeyHash: $apiKeyHash, bindNoOrgs: $bindNoOrgs, email: $email, enableMultiFactor: $enableMultiFactor, firstname: $firstname, lastname: $lastname, role: $role, roleName: $roleName, roles: $roles, administratorOrganizations: $administratorOrganizations} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete Provider Administrator
#
# DELETE /providers/{provider_id}/administrators/{id}
# operationId: providers_removeAdministrator
export def "providers-administrators removeAdministrator" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/administrators/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Integrations for Provider
#
# GET /providers/{provider_id}/integrations
# operationId: providers_retrieveIntegrations
export def "providers-integrations retrieveIntegrations" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<records: table<integrationId: string, type: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/integrations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new ConnectWise integration for the provider
#
# POST /providers/{provider_id}/integrations/connectwise
# operationId: connectwise_createConfiguration
export def "providers-integrations-connectwise createConfiguration" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-url: string # The base url for connecting to ConnectWise.
  companyId: string # The ConnectWise company identifier.
  publicKey: string # The ConnectWise public key for authentication.
  privateKey: string # The ConnectWise private key for authentication
]: any -> record<integrationId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/connectwise")
  let body = {url: $body_url, companyId: $companyId, publicKey: $publicKey, privateKey: $privateKey} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List a provider's invoices.
#
# GET /providers/{provider_id}/invoices
# operationId: providers_retrieveInvoices
export def "providers-invoices retrieveInvoices" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<totalCount: int, records: table<id: string, dueDate: string, status: string, amountBilled: string, amountPaid: string, amountRemaining: string, currency: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/invoices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Download a provider's invoice.
#
# GET /providers/{provider_id}/invoices/{ID}
# operationId: providers_retrieveInvoice
export def "providers-invoices retrieveInvoice" [
  provider_id: string
  ID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/invoices/($ID)")
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve a configured policy template by id.
#
# GET /providers/{provider_id}/configuredpolicytemplates/{id}
# operationId: policyGroupTemplates_getConfiguredPolicyTemplate
export def "providers-configuredpolicytemplates get" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, name: string, policyTemplateId: string, values: table<configFieldId: string, name: string, value: record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/configuredpolicytemplates/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List a provider's configured policy templates.
#
# GET /providers/{provider_id}/configuredpolicytemplates
# operationId: policyGroupTemplates_listConfiguredPolicyTemplates
export def "providers-configuredpolicytemplates listConfiguredPolicyTemplates" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<totalCount: int, records: record<id: string, name: string, policyTemplateId: string, values: list<record>>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/configuredpolicytemplates" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List a provider's policy group templates.
#
# GET /providers/{provider_id}/policygrouptemplates
# operationId: policyGroupTemplates_list
export def "providers-policygrouptemplates list" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<totalCount: int, results: table<id: string, name: string, description: string, members: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/policygrouptemplates" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Gets a provider's policy group template.
#
# GET /providers/{provider_id}/policygrouptemplates/{id}
# operationId: policyGroupTemplates_get
export def "providers-policygrouptemplates get" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, name: string, description: string, members: table<id: string, name: string, policyTemplateId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/policygrouptemplates/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes policy group template.
#
# DELETE /providers/{provider_id}/policygrouptemplates/{id}
# operationId: policyGroupTemplates_delete
export def "providers-policygrouptemplates delete" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/policygrouptemplates/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Gets the list of members from a policy group template.
#
# GET /providers/{provider_id}/policygrouptemplates/{id}/members
# operationId: policyGroupTemplates_listMembers
export def "providers-policygrouptemplates-members listMembers" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<totalCount: int, results: table<id: string, name: string, policyTemplateId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/policygrouptemplates/($id)/members" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Recent Integration Sync Errors
#
# GET /integrations/{integration_type}/{UUID}/errors
# operationId: mtpIntegration_retrieveSyncErrors
export def "integrations-errors retrieveSyncErrors" [
  UUID: string
  integration_type: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<totalCount: int, records: table<orgId: string, timestamp: string, errorType: string, message: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/($integration_type)/($UUID)/errors")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve ConnectWise Integration Configuration
#
# GET /integrations/connectwise/{UUID}
# operationId: connectwise_getConfiguration
export def "integrations-connectwise get" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, url: string, companyId: string, isMspAuthConfigured: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete ConnectWise Integration
#
# DELETE /integrations/connectwise/{UUID}
# operationId: connectwise_deleteConfiguration
export def "integrations-connectwise delete" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update ConnectWise Integration configuration
#
# PATCH /integrations/connectwise/{UUID}
# operationId: connectwise_updateConfiguration
export def "integrations-connectwise updateConfiguration" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-url: string # The base url for connecting to ConnectWise.
  --companyId: string # The ConnectWise company identifier.
  --publicKey: string # The ConnectWise public key for authentication.
  --privateKey: string # The ConnectWise private key for authentication
]: any -> record<id: string, url: string, companyId: string, isMspAuthConfigured: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)")
  let body = {url: $body_url, companyId: $companyId, publicKey: $publicKey, privateKey: $privateKey} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve ConnectWise Agreements
#
# GET /integrations/connectwise/{UUID}/agreements
# operationId: connectwise_retrieveAgreements
export def "integrations-connectwise-agreements retrieveAgreements" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<results: table<id: string, name: string, companyId: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/agreements" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve ConnectWise Additions
#
# GET /integrations/connectwise/{UUID}/agreements/{agreement_ID}/additions
# operationId: connectwise_retrieveAdditions
export def "integrations-connectwise-agreements-additions retrieveAdditions" [
  UUID: string
  agreement_ID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<results: table<id: string, name: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/agreements/($agreement_ID)/additions" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve ConnectWise Companies
#
# GET /integrations/connectwise/{UUID}/companies
# operationId: connectwise_retrieveCompanies
export def "integrations-connectwise-companies retrieveCompanies" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<totalCount: int, records: table<id: string, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/companies" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve ConnectWise Company Types
#
# GET /integrations/connectwise/{UUID}/companytypes
# operationId: connectwise_retrieveCompanyTypes
export def "integrations-connectwise-companytypes retrieveCompanyTypes" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<totalCount: int, records: table<id: float, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/companytypes")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve ConnectWise mappings
#
# GET /integrations/connectwise/{UUID}/mappings
# operationId: connectwise_retrieveMappings
export def "integrations-connectwise-mappings retrieveMappings" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<totalCount: float, records: table<organization: record, company: record, agreement: record, addition: record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/mappings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create, edit, and/or delete ConnectWise Mappings
#
# PATCH /integrations/connectwise/{UUID}/mappings
# operationId: connectwise_patchMappings
# --data item shape: {organization: record, delete?: bool, company: record, agreement?: record, addition?: record}
export def "integrations-connectwise-mappings patch" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --data: list # item shape: {organization: record, delete?: bool, company: record, agreement?: record, addition?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/mappings")
  let body = {data: $data} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve ConnectWise Integration settings
#
# GET /integrations/connectwise/{UUID}/settings
# operationId: connectwise_retrieveSettings
export def "integrations-connectwise-settings retrieveSettings" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<companyTypeIds: list<int>, automaticTicketing: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/settings")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create, edit, and/or delete ConnectWise Integration settings
#
# PATCH /integrations/connectwise/{UUID}/settings
# operationId: connectwise_patchSettings
export def "integrations-connectwise-settings patch" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --companyTypeIds: list # The array of ConnectWise companyType IDs applicable to the Provider.
  --automaticTicketing: string@bool-completer # Determine whether ConnectWise uses automatic ticketing
]: any -> record<companyTypeIds: list<int>, automaticTicketing: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/connectwise/($UUID)/settings")
  let body = {companyTypeIds: $companyTypeIds, automaticTicketing: $automaticTicketing} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Update a ConnectWise ticketing alert's configuration
#
# PUT /providers/{provider_id}/integrations/connectwise/alerts/{alert_UUID}/configuration
# operationId: connectwise_updateAlertConfiguration
# --priority shape: {id?: int, name?: string}
# --source shape: {id?: int, name?: string}
export def "providers-integrations-connectwise-alerts-configuration updateAlertConfiguration" [
  provider_id: string
  alert_UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --shouldCreateTickets: string@bool-completer
  --priority: record # shape: {id?: int, name?: string}
  --body-source: record # shape: {id?: int, name?: string}
]: any -> record<id: int, category: string, description: string, displayName: string, shouldCreateTickets: bool, priority: record<id: int, name: string>, source: record<id: int, name: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/connectwise/alerts/($alert_UUID)/configuration")
  let body = {shouldCreateTickets: $shouldCreateTickets, priority: $priority, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all ConnectWise ticketing alert configurations for a provider
#
# GET /providers/{provider_id}/integrations/connectwise/alerts/configuration
# operationId: connectwise_retrieveAllAlertConfigurations
export def "providers-integrations-connectwise-alerts-configuration retrieveAllAlertConfigurations" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<records: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/connectwise/alerts/configuration")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get all ConnectWise ticketing alert configuration options for a provider
#
# GET /providers/{provider_id}/integrations/connectwise/alerts/configuration/options
# operationId: connectwise_retrieveAllAlertConfigurationOptions
export def "providers-integrations-connectwise-alerts-configuration-options retrieveAllAlertConfigurationOptions" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<records: table<name: string, values: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/connectwise/alerts/configuration/options")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new Autotask integration for the provider
#
# POST /providers/{provider_id}/integrations/autotask
# operationId: autotask_createConfiguration
export def "providers-integrations-autotask createConfiguration" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  username: string # The username for connecting to Autotask.
  secret: string # The secret for connecting to Autotask.
]: any -> record<integrationId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/autotask")
  let body = {username: $username, secret: $secret} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve Autotask Integration Configuration
#
# GET /integrations/autotask/{UUID}
# operationId: autotask_getConfiguration
export def "integrations-autotask get" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, username: string, isMspAuthConfigured: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Autotask Integration
#
# DELETE /integrations/autotask/{UUID}
# operationId: autotask_deleteConfiguration
export def "integrations-autotask delete" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Autotask Integration configuration
#
# PATCH /integrations/autotask/{UUID}
# operationId: autotask_updateConfiguration
export def "integrations-autotask updateConfiguration" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --username: string # The username for connecting to Autotask.
  --secret: string # The secret for connecting to Autotask.
]: any -> record<id: string, username: string, isMspAuthConfigured: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)")
  let body = {username: $username, secret: $secret} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve Autotask Company Types
#
# GET /integrations/autotask/{UUID}/companytypes
# operationId: autotask_retrieveCompanyTypes
export def "integrations-autotask-companytypes retrieveCompanyTypes" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<totalCount: int, records: table<id: float, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/companytypes")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Autotask Integration settings
#
# GET /integrations/autotask/{UUID}/settings
# operationId: autotask_retrieveSettings
export def "integrations-autotask-settings retrieveSettings" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<companyTypeIds: list<int>, automaticTicketing: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/settings")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create, edit, and/or delete Autotask Integration settings
#
# PATCH /integrations/autotask/{UUID}/settings
# operationId: autotask_patchSettings
export def "integrations-autotask-settings patch" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --companyTypeIds: list # The array of Autotask companyType IDs applicable to the Provider.
  --automaticTicketing: string@bool-completer # Determine whether Autotask uses automatic ticketing
]: any -> record<companyTypeIds: list<int>, automaticTicketing: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/settings")
  let body = {companyTypeIds: $companyTypeIds, automaticTicketing: $automaticTicketing} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve Autotask Contracts
#
# GET /integrations/autotask/{UUID}/contracts
# operationId: autotask_retrieveContracts
export def "integrations-autotask-contracts retrieveContracts" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<results: table<id: string, name: string, companyId: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/contracts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Autotask Contract Services
#
# GET /integrations/autotask/{UUID}/contracts/services
# operationId: autotask_retrieveServices
export def "integrations-autotask-contracts-services retrieveServices" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<results: table<id: string, name: string, contractId: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/contracts/services" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Autotask Contract Fields
#
# GET /integrations/autotask/{UUID}/contracts/fields
# operationId: autotask_retrieveContractsFields
export def "integrations-autotask-contracts-fields retrieveContractsFields" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<results: table<name: string, values: list>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/contracts/fields")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Autotask Companies
#
# GET /integrations/autotask/{UUID}/companies
# operationId: autotask_retrieveCompanies
export def "integrations-autotask-companies retrieveCompanies" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<totalCount: int, records: table<id: string, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/companies" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Autotask mappings
#
# GET /integrations/autotask/{UUID}/mappings
# operationId: autotask_retrieveMappings
export def "integrations-autotask-mappings retrieveMappings" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<totalCount: float, records: table<organization: record, company: record, contract: record, service: record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/mappings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create, edit, and/or delete Autotask Mappings
#
# PATCH /integrations/autotask/{UUID}/mappings
# operationId: autotask_patchMappings
# --data item shape: {organization: record, delete?: bool, company: record, contract?: record, service?: record}
export def "integrations-autotask-mappings patch" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --data: list # item shape: {organization: record, delete?: bool, company: record, contract?: record, service?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/autotask/($UUID)/mappings")
  let body = {data: $data} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all ticketing alerts available for a provider's ticketing integration.
#
# GET /providers/{provider_id}/integrations/ticketing/alerts
# operationId: mtpIntegration_retrieveAlerts
export def "providers-integrations-ticketing-alerts retrieveAlerts" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<records: table<id: int, name: string, category: string, description: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/ticketing/alerts")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update an Autotask ticketing alert's configuration
#
# PUT /providers/{provider_id}/integrations/autotask/alerts/{alert_UUID}/configuration
# operationId: autotask_updateAlertConfiguration
# --priority shape: {id?: int, name?: string}
# --status shape: {id?: int, name?: string}
# --source shape: {id?: int, name?: string}
# --resource shape: {id?: int, name?: string, role?: record}
# --queue shape: {id?: int, name?: string}
export def "providers-integrations-autotask-alerts-configuration updateAlertConfiguration" [
  provider_id: string
  alert_UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --shouldCreateTickets: string@bool-completer
  priority: record # shape: {id?: int, name?: string}
  status: record # shape: {id?: int, name?: string}
  --body-source: record # shape: {id?: int, name?: string}
  dueDays: int
  destination: string@destination-completer
  --resource: record # shape: {id?: int, name?: string, role?: record}
  --queue: record # shape: {id?: int, name?: string}
]: any -> record<id: int, category: string, description: string, displayName: string, shouldCreateTickets: bool, priority: record<id: int, name: string>, status: record<id: int, name: string>, source: record<id: int, name: string>, dueDays: int, destination: string, resource: record<name: string, role: record<id: int, name: string>>, queue: record<id: int, name: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/autotask/alerts/($alert_UUID)/configuration")
  let body = {shouldCreateTickets: $shouldCreateTickets, priority: $priority, status: $status, source: $body_source, dueDays: $dueDays, destination: $destination, resource: $resource, queue: $queue} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all Autotask ticketing alert configurations for a provider
#
# GET /providers/{provider_id}/integrations/autotask/alerts/configuration
# operationId: autotask_retrieveAllAlertConfigurations
export def "providers-integrations-autotask-alerts-configuration retrieveAllAlertConfigurations" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<records: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/autotask/alerts/configuration")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get all Autotask ticketing alert configuration options for a provider
#
# GET /providers/{provider_id}/integrations/autotask/alerts/configuration/options
# operationId: autotask_retrieveAllAlertConfigurationOptions
export def "providers-integrations-autotask-alerts-configuration-options retrieveAllAlertConfigurationOptions" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<options: table<name: string, values: list>, resources: table<id: int, name: string, roles: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/autotask/alerts/configuration/options")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a new Syncro integration for the provider
#
# POST /providers/{provider_id}/integrations/syncro
# operationId: syncro_createConfiguration
export def "providers-integrations-syncro createConfiguration" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  subdomain: string # The subdomain for the URL to connect to Syncro.
  apiToken: string # The Syncro API token for authentication
]: any -> record<integrationId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/syncro")
  let body = {subdomain: $subdomain, apiToken: $apiToken} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve Syncro Integration Configuration
#
# GET /integrations/syncro/{UUID}
# operationId: syncro_getConfiguration
export def "integrations-syncro get" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, subdomain: string, isMspAuthConfigured: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/syncro/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Syncro Integration
#
# DELETE /integrations/syncro/{UUID}
# operationId: syncro_deleteConfiguration
export def "integrations-syncro delete" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/syncro/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Syncro Integration configuration
#
# PATCH /integrations/syncro/{UUID}
# operationId: syncro_updateConfiguration
export def "integrations-syncro updateConfiguration" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --subdomain: string # The subdomain for the URL to connect to Syncro.
  --apiToken: string # The Syncro API token for authentication
]: any -> record<id: string, subdomain: string, isMspAuthConfigured: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/syncro/($UUID)")
  let body = {subdomain: $subdomain, apiToken: $apiToken} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve Syncro Integration settings
#
# GET /integrations/syncro/{UUID}/settings
# operationId: syncro_retrieveSettings
export def "integrations-syncro-settings retrieveSettings" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<automaticTicketing: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/syncro/($UUID)/settings")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create, edit, and/or delete Syncro Integration settings
#
# PATCH /integrations/syncro/{UUID}/settings
# operationId: syncro_patchSettings
export def "integrations-syncro-settings patch" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --automaticTicketing: string@bool-completer # Determine whether Syncro uses automatic ticketing
]: any -> record<automaticTicketing: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/syncro/($UUID)/settings")
  let body = {automaticTicketing: $automaticTicketing} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Update a Syncro ticketing alert's configuration
#
# PUT /providers/{provider_id}/integrations/syncro/alerts/{alert_UUID}/configuration
# operationId: syncro_updateAlertConfiguration
export def "providers-integrations-syncro-alerts-configuration updateAlertConfiguration" [
  provider_id: string
  alert_UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --shouldCreateTickets: string@bool-completer
  problemType: string
  --dueDays: int
]: any -> record<id: int, category: string, description: string, displayName: string, shouldCreateTickets: bool, priority: string, problemType: string, status: string, dueDays: int, userId: int, username: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/syncro/alerts/($alert_UUID)/configuration")
  let body = {shouldCreateTickets: $shouldCreateTickets, problemType: $problemType, dueDays: $dueDays} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all Syncro ticketing alert configurations for a provider
#
# GET /providers/{provider_id}/integrations/syncro/alerts/configuration
# operationId: syncro_retrieveAllAlertConfigurations
export def "providers-integrations-syncro-alerts-configuration retrieveAllAlertConfigurations" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<records: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/syncro/alerts/configuration")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get all Syncro ticketing alert configuration options for a provider
#
# GET /providers/{provider_id}/integrations/syncro/alerts/configuration/options
# operationId: syncro_retrieveAllAlertConfigurationOptions
export def "providers-integrations-syncro-alerts-configuration-options retrieveAllAlertConfigurationOptions" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<records: table<name: string, values: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/integrations/syncro/alerts/configuration/options")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Syncro Companies
#
# GET /integrations/syncro/{UUID}/companies
# operationId: syncro_retrieveCompanies
export def "integrations-syncro-companies retrieveCompanies" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<totalCount: int, records: table<id: string, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/syncro/($UUID)/companies" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Syncro mappings
#
# GET /integrations/syncro/{UUID}/mappings
# operationId: syncro_retrieveMappings
export def "integrations-syncro-mappings retrieveMappings" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<totalCount: float, records: table<organization: record, company: record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/syncro/($UUID)/mappings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create, edit, and/or delete Syncro Mappings
#
# PATCH /integrations/syncro/{UUID}/mappings
# operationId: syncro_patchMappings
# --data item shape: {organization: record, delete?: bool, company: record, billingConfigurations?: record}
export def "integrations-syncro-mappings patch" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --data: list # item shape: {organization: record, delete?: bool, company: record, billingConfigurations?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/integrations/syncro/($UUID)/mappings")
  let body = {data: $data} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve Syncro billing mappings dependencies
#
# GET /integrations/syncro/{UUID}/billing_mapping_configuration_options
# operationId: syncro_retrieveBillingMappingConfigurationOptions
export def "integrations-syncro-billing-mapping-configuration-options retrieveBillingMappingConfigurationOptions" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<options: table<name: string, values: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/integrations/syncro/($UUID)/billing_mapping_configuration_options" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve Provider
#
# GET /providers/{provider_id}
# operationId: providers_getProvider
export def "providers get" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
]: nothing -> record<id: string, name: string, disallowOrgCreation: bool, settings: record<requireAdminMFA: bool>, type: string, contact: record<name: string, email: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update provider name, settings, and contact
#
# PUT /providers/{provider_id}
# operationId: providers_putProvider
# --settings shape: {requireAdminMFA?: bool}
# --contact shape: {name?: string, email?: string}
export def "providers put" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --name: string # MSP display name
  --settings: record # shape: {requireAdminMFA?: bool}
  --contact: record # MSP contact; omit a property to leave it unchanged (including contact.email) — shape: {name?: string, email?: string}
]: any -> record<id: string, name: string, disallowOrgCreation: bool, settings: record<requireAdminMFA: bool>, type: string, contact: record<name: string, email: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)")
  let body = {name: $name, settings: $settings, contact: $contact} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Provider Organizations
#
# GET /providers/{provider_id}/organizations
# operationId: providers_listOrganizations
export def "providers-organizations listOrganizations" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --sortIgnoreCase: list # The comma separated fields used to sort the collection, ignoring case. Default sort is ascending, prefix with `-` to sort descending.
]: nothing -> record<results: table<id: string, name: string, maxSystemUsers: int>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "sortIgnoreCase" $sortIgnoreCase "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/providers/($provider_id)/organizations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Provider Organization
#
# POST /providers/{provider_id}/organizations
# operationId: providerOrganizations_createOrg
export def "providers-organizations createOrg" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --name: string
  --maxSystemUsers: int # The maximum number of users allowed in this organization. Requires organizations.billing scope to modify.
]: any -> record<id: string, name: string, maxSystemUsers: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/organizations")
  let body = {name: $name, maxSystemUsers: $maxSystemUsers} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Update Provider Organization
#
# PUT /providers/{provider_id}/organizations/{id}
# operationId: providerOrganizations_updateOrg
export def "providers-organizations updateOrg" [
  provider_id: string
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-id: string
  --name: string
  --maxSystemUsers: int # The maximum number of users allowed in this organization. Requires organizations.billing scope to modify.
]: any -> record<id: string, name: string, maxSystemUsers: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/organizations/($id)")
  let body = {id: $body_id, name: $name, maxSystemUsers: $maxSystemUsers} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# POST provider Directory Insights events query (converged audit)
#
# POST /providers/{provider_id}/directoryinsights/events
# operationId: providers_directoryInsightsEventsPost
export def "providers-directoryinsights-events directoryInsightsEventsPost" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --exclude-provider-activity: string@bool-completer # When true, excludes provider-level (master MSP) activity and limits results to child-organization activity.
]: any -> list<record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/directoryinsights/events")
  let body = {exclude_provider_activity: $exclude_provider_activity} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# POST provider Directory Insights distinct event field values (converged audit)
#
# POST /providers/{provider_id}/directoryinsights/events/distinct
# operationId: providers_directoryInsightsEventsDistinctPost
export def "providers-directoryinsights-events-distinct directoryInsightsEventsDistinctPost" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --exclude-provider-activity: string@bool-completer # When true, excludes provider-level (master MSP) activity and limits results to child-organization activity.
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/directoryinsights/events/distinct")
  let body = {exclude_provider_activity: $exclude_provider_activity} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# POST provider Directory Insights events count (converged audit)
#
# POST /providers/{provider_id}/directoryinsights/events/count
# operationId: providers_directoryInsightsEventsCountPost
export def "providers-directoryinsights-events-count directoryInsightsEventsCountPost" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --exclude-provider-activity: string@bool-completer # When true, excludes provider-level (master MSP) activity and limits results to child-organization activity.
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/directoryinsights/events/count")
  let body = {exclude_provider_activity: $exclude_provider_activity} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# POST provider Directory Insights events interval (converged audit)
#
# POST /providers/{provider_id}/directoryinsights/events/interval
# operationId: providers_directoryInsightsEventsIntervalPost
export def "providers-directoryinsights-events-interval directoryInsightsEventsIntervalPost" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --exclude-provider-activity: string@bool-completer # When true, excludes provider-level (master MSP) activity and limits results to child-organization activity.
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/directoryinsights/events/interval")
  let body = {exclude_provider_activity: $exclude_provider_activity} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the associations of a RADIUS  Server
#
# GET /radiusservers/{radiusserver_id}/associations
# operationId: graph_radiusServerAssociationsList
export def "radiusservers-associations radiusServerAssociationsList" [
  radiusserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "radius_server" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/radiusservers/($radiusserver_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a RADIUS Server
#
# POST /radiusservers/{radiusserver_id}/associations
# operationId: graph_radiusServerAssociationsPost
export def "radiusservers-associations radiusServerAssociationsPost" [
  radiusserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer # Targets which a "radius_server" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/radiusservers/($radiusserver_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the User Groups bound to a RADIUS  Server
#
# GET /radiusservers/{radiusserver_id}/usergroups
# operationId: graph_radiusServerTraverseUserGroup
export def "radiusservers-usergroups radiusServerTraverseUserGroup" [
  radiusserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/radiusservers/($radiusserver_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a RADIUS  Server
#
# GET /radiusservers/{radiusserver_id}/users
# operationId: graph_radiusServerTraverseUser
export def "radiusservers-users radiusServerTraverseUser" [
  radiusserver_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/radiusservers/($radiusserver_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a Rule
#
# GET /healthmonitoring/rules/{rule_id}/associations
# operationId: graph_ruleAssociationsList
export def "healthmonitoring-rules-associations ruleAssociationsList" [
  rule_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "rule" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/healthmonitoring/rules/($rule_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a Rules
#
# POST /healthmonitoring/rules/{rule_id}/associations
# operationId: graph_ruleAssociationsPost
export def "healthmonitoring-rules-associations ruleAssociationsPost" [
  rule_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-4 # Targets which a "rule" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/healthmonitoring/rules/($rule_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the System Groups bound to a Rule
#
# GET /healthmonitoring/rules/{rule_id}/systemgroups
# operationId: graph_ruleTraverseSystemGroup
export def "healthmonitoring-rules-systemgroups ruleTraverseSystemGroup" [
  rule_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/healthmonitoring/rules/($rule_id)/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get all configured Software Applications.
#
# GET /softwareapps
# operationId: softwareApps_list
export def "softwareapps list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, organization: string, displayName: string, settings: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/softwareapps" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a Software Application that will be managed by JumpCloud.
#
# POST /softwareapps
# operationId: softwareApps_post
# --settings item shape: {packageId?: string, autoUpdate?: bool, allowUpdateDelay?: bool, packageManager?: string, locationObjectId?: string, location?: string, desiredState?: string, adminIntent?: "INTENT_UNSPECIFIED"|"INTENT_FORCE_MANAGED"|"INTENT_FORCE_UNMANAGED", managedInstallEligibilityStatus?: "MANAGED_INSTALL_ELIGIBILITY_NOT_EVALUATED"|"MANAGED_INSTALL_ELIGIBLE"|"MANAGED_INSTALL_INELIGIBLE"|"MANAGED_INSTALL_ELIGIBILITY_UNKNOWN", detectionRules?: record, appleVpp?: record, packageSubtitle?: string, packageVersion?: string, packageKind?: string, assetKind?: string, assetSha256Size?: int, assetSha256Strings?: list, description?: string, googleAndroid?: record, enterpriseObjectId?: string, commandLineArguments?: string, storedPackage?: record, storedPackageObjectId?: string, microsoftStore?: record, scope?: string, appCatalogInstallableObjectId?: string, iconUrl?: string, installerEngineVersion?: string, versionlessDownloadUrl?: bool, bundleId?: string, teamId?: string, format?: string, appFileName?: string, shortVersion?: string, signatureLabel?: string, silentInstallFlags?: list, downloadUrl?: string, architectures?: list, updateTool?: string, updateToolArguments?: string, uninstallOnUnbind?: bool, unsafeUninstall?: bool, appDeliveryMethod?: string}
export def "softwareapps post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --id: string
  --organization: string # The owning organization ID.
  --displayName: string
  --settings: list # item shape: {packageId?: string, autoUpdate?: bool, allowUpdateDelay?: bool, packageManager?: string, locationObjectId?: string, location?: string, desiredState?: string, adminIntent?: "INTENT_UNSPECIFIED"|"INTENT_FORCE_MANAGED"|"INTENT_FORCE_UNMANAGED", managedInstallEligibilityStatus?: "MANAGED_INSTALL_ELIGIBILITY_NOT_EVALUATED"|"MANAGED_INSTALL_ELIGIBLE"|"MANAGED_INSTALL_INELIGIBLE"|"MANAGED_INSTALL_ELIGIBILITY_UNKNOWN", detectionRules?: record, appleVpp?: record, packageSubtitle?: string, packageVersion?: string, packageKind?: string, assetKind?: string, assetSha256Size?: int, assetSha256Strings?: list, description?: string, googleAndroid?: record, enterpriseObjectId?: string, commandLineArguments?: string, storedPackage?: record, storedPackageObjectId?: string, microsoftStore?: record, scope?: string, appCatalogInstallableObjectId?: string, iconUrl?: string, installerEngineVersion?: string, versionlessDownloadUrl?: bool, bundleId?: string, teamId?: string, format?: string, appFileName?: string, shortVersion?: string, signatureLabel?: string, silentInstallFlags?: list, downloadUrl?: string, architectures?: list, updateTool?: string, updateToolArguments?: string, uninstallOnUnbind?: bool, unsafeUninstall?: bool, appDeliveryMethod?: string}
]: any -> record<id: string, organization: string, displayName: string, settings: table<packageId: string, autoUpdate: bool, allowUpdateDelay: bool, packageManager: string, locationObjectId: string, location: string, desiredState: string, adminIntent: string, managedInstallEligibilityStatus: string, detectionRules: record, appleVpp: record, packageSubtitle: string, packageVersion: string, packageKind: string, assetKind: string, assetSha256Size: int, assetSha256Strings: list, description: string, googleAndroid: record, enterpriseObjectId: string, commandLineArguments: string, storedPackage: record, storedPackageObjectId: string, microsoftStore: record, scope: string, appCatalogInstallableObjectId: string, iconUrl: string, installerEngineVersion: string, versionlessDownloadUrl: bool, bundleId: string, teamId: string, format: string, appFileName: string, shortVersion: string, signatureLabel: string, silentInstallFlags: list, downloadUrl: string, architectures: list, updateTool: string, updateToolArguments: string, uninstallOnUnbind: bool, unsafeUninstall: bool, appDeliveryMethod: string>, uploadUrl: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/softwareapps")
  let body = {id: $id, organization: $organization, displayName: $displayName, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve a configured Software Application.
#
# GET /softwareapps/{id}
# operationId: softwareApps_get
export def "softwareapps get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, organization: string, displayName: string, settings: table<packageId: string, autoUpdate: bool, allowUpdateDelay: bool, packageManager: string, locationObjectId: string, location: string, desiredState: string, adminIntent: string, managedInstallEligibilityStatus: string, detectionRules: record, appleVpp: record, packageSubtitle: string, packageVersion: string, packageKind: string, assetKind: string, assetSha256Size: int, assetSha256Strings: list, description: string, googleAndroid: record, enterpriseObjectId: string, commandLineArguments: string, storedPackage: record, storedPackageObjectId: string, microsoftStore: record, scope: string, appCatalogInstallableObjectId: string, iconUrl: string, installerEngineVersion: string, versionlessDownloadUrl: bool, bundleId: string, teamId: string, format: string, appFileName: string, shortVersion: string, signatureLabel: string, silentInstallFlags: list, downloadUrl: string, architectures: list, updateTool: string, updateToolArguments: string, uninstallOnUnbind: bool, unsafeUninstall: bool, appDeliveryMethod: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/softwareapps/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a Software Application Configuration.
#
# PUT /softwareapps/{id}
# operationId: softwareApps_update
# --settings item shape: {packageId?: string, autoUpdate?: bool, allowUpdateDelay?: bool, packageManager?: string, locationObjectId?: string, location?: string, desiredState?: string, adminIntent?: "INTENT_UNSPECIFIED"|"INTENT_FORCE_MANAGED"|"INTENT_FORCE_UNMANAGED", managedInstallEligibilityStatus?: "MANAGED_INSTALL_ELIGIBILITY_NOT_EVALUATED"|"MANAGED_INSTALL_ELIGIBLE"|"MANAGED_INSTALL_INELIGIBLE"|"MANAGED_INSTALL_ELIGIBILITY_UNKNOWN", detectionRules?: record, appleVpp?: record, packageSubtitle?: string, packageVersion?: string, packageKind?: string, assetKind?: string, assetSha256Size?: int, assetSha256Strings?: list, description?: string, googleAndroid?: record, enterpriseObjectId?: string, commandLineArguments?: string, storedPackage?: record, storedPackageObjectId?: string, microsoftStore?: record, scope?: string, appCatalogInstallableObjectId?: string, iconUrl?: string, installerEngineVersion?: string, versionlessDownloadUrl?: bool, bundleId?: string, teamId?: string, format?: string, appFileName?: string, shortVersion?: string, signatureLabel?: string, silentInstallFlags?: list, downloadUrl?: string, architectures?: list, updateTool?: string, updateToolArguments?: string, uninstallOnUnbind?: bool, unsafeUninstall?: bool, appDeliveryMethod?: string}
export def "softwareapps update" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body-id: string
  --organization: string # The owning organization ID.
  --displayName: string
  --settings: list # item shape: {packageId?: string, autoUpdate?: bool, allowUpdateDelay?: bool, packageManager?: string, locationObjectId?: string, location?: string, desiredState?: string, adminIntent?: "INTENT_UNSPECIFIED"|"INTENT_FORCE_MANAGED"|"INTENT_FORCE_UNMANAGED", managedInstallEligibilityStatus?: "MANAGED_INSTALL_ELIGIBILITY_NOT_EVALUATED"|"MANAGED_INSTALL_ELIGIBLE"|"MANAGED_INSTALL_INELIGIBLE"|"MANAGED_INSTALL_ELIGIBILITY_UNKNOWN", detectionRules?: record, appleVpp?: record, packageSubtitle?: string, packageVersion?: string, packageKind?: string, assetKind?: string, assetSha256Size?: int, assetSha256Strings?: list, description?: string, googleAndroid?: record, enterpriseObjectId?: string, commandLineArguments?: string, storedPackage?: record, storedPackageObjectId?: string, microsoftStore?: record, scope?: string, appCatalogInstallableObjectId?: string, iconUrl?: string, installerEngineVersion?: string, versionlessDownloadUrl?: bool, bundleId?: string, teamId?: string, format?: string, appFileName?: string, shortVersion?: string, signatureLabel?: string, silentInstallFlags?: list, downloadUrl?: string, architectures?: list, updateTool?: string, updateToolArguments?: string, uninstallOnUnbind?: bool, unsafeUninstall?: bool, appDeliveryMethod?: string}
]: any -> record<id: string, organization: string, displayName: string, settings: table<packageId: string, autoUpdate: bool, allowUpdateDelay: bool, packageManager: string, locationObjectId: string, location: string, desiredState: string, adminIntent: string, managedInstallEligibilityStatus: string, detectionRules: record, appleVpp: record, packageSubtitle: string, packageVersion: string, packageKind: string, assetKind: string, assetSha256Size: int, assetSha256Strings: list, description: string, googleAndroid: record, enterpriseObjectId: string, commandLineArguments: string, storedPackage: record, storedPackageObjectId: string, microsoftStore: record, scope: string, appCatalogInstallableObjectId: string, iconUrl: string, installerEngineVersion: string, versionlessDownloadUrl: bool, bundleId: string, teamId: string, format: string, appFileName: string, shortVersion: string, signatureLabel: string, silentInstallFlags: list, downloadUrl: string, architectures: list, updateTool: string, updateToolArguments: string, uninstallOnUnbind: bool, unsafeUninstall: bool, appDeliveryMethod: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/softwareapps/($id)")
  let body = {id: $body_id, organization: $organization, displayName: $displayName, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a configured Software Application
#
# DELETE /softwareapps/{id}
# operationId: softwareApps_delete
export def "softwareapps delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/softwareapps/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a Software Application
#
# GET /softwareapps/{software_app_id}/associations
# operationId: graph_softwareappsAssociationsList
export def "softwareapps-associations softwareappsAssociationsList" [
  software_app_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "software_app" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/softwareapps/($software_app_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a software application.
#
# POST /softwareapps/{software_app_id}/associations
# operationId: graph_softwareappsAssociationsPost
export def "softwareapps-associations softwareappsAssociationsPost" [
  software_app_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-4 # Targets which a "software_app" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/softwareapps/($software_app_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Reclaim Licenses for a Software Application.
#
# POST /softwareapps/{software_app_id}/reclaim-licenses
# operationId: softwareApps_reclaimLicenses
export def "softwareapps-reclaim-licenses reclaimLicenses" [
  software_app_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<assignedLicenses: int, availableLicenses: int, reclaimedLicenses: int, totalLicenses: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/softwareapps/($software_app_id)/reclaim-licenses")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retry Installation for a Software Application
#
# POST /softwareapps/{software_app_id}/retry-installation
# operationId: softwareApps_retryInstallation
export def "softwareapps-retry-installation retryInstallation" [
  software_app_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --system-ids: list # An array of system IDs to retry the software application installation.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/softwareapps/($software_app_id)/retry-installation")
  let body = {system_ids: $system_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get the status of the provided Software Application
#
# GET /softwareapps/{software_app_id}/statuses
# operationId: softwareAppStatuses_list
export def "softwareapps-statuses list" [
  software_app_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, softwareAppId: string, systemId: string, state: string, details: string, code: int, timestamp: string, version: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/softwareapps/($software_app_id)/statuses" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the System Groups bound to a Software App.
#
# GET /softwareapps/{software_app_id}/systemgroups
# operationId: graph_softwareappsTraverseSystemGroup
export def "softwareapps-systemgroups softwareappsTraverseSystemGroup" [
  software_app_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/softwareapps/($software_app_id)/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Systems bound to a Software App.
#
# GET /softwareapps/{software_app_id}/systems
# operationId: graph_softwareappsTraverseSystem
export def "softwareapps-systems softwareappsTraverseSystem" [
  software_app_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/softwareapps/($software_app_id)/systems" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List all System Groups
#
# GET /systemgroups
# operationId: groups_system_list
export def "systemgroups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, description: string, email: string, id: string, memberQuery: record<queryType: string, filters: list>, memberQueryErrorFlags: list<string>, memberQueryExemptions: list<record>, membershipMethod: string, memberSuggestionsNotify: bool, name: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new System Group
#
# POST /systemgroups
# operationId: groups_system_post
# --memberQueryExemptions item shape: {attributes?: record, id: string, type: string}
export def "systemgroups post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --attributes: record # The graph attributes.
  --email: string # Email address of a System Group
  --description: string # Description of a System Group
  name: string # Display name of a System Group.
  --memberQuery: any # Query using a sequence of field filters.
  --membershipMethod: string@membershipMethod-completer # The type of membership method for this group. Valid values include NOTSET, STATIC, DYNAMIC_REVIEW_REQUIRED, and DYNAMIC_AUTOMATED.  Note DYNAMIC_AUTOMATED and DYNAMIC_REVIEW_REQUIRED group rules will supersede any group enrollment for [group-associated MDM-enrolled devices](https://jumpcloud.com/support/change-a-default-device-group-for-apple-devices).  Use caution when creating dynamic device groups with MDM-enrolled devices to avoid creating conflicting rule sets.
  --memberSuggestionsNotify: string@bool-completer # True if notification emails are to be sent for membership suggestions.
  --memberQueryExemptions: list # Array of GraphObjects exempted from the query — item shape: {attributes?: record, id: string, type: string}
]: any -> record<attributes: record, description: string, email: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryErrorFlags: list<string>, memberQueryExemptions: table<attributes: record, id: string, type: string>, membershipMethod: string, memberSuggestionsNotify: bool, name: string, type: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/systemgroups")
  let body = {attributes: $attributes, email: $email, description: $description, name: $name, memberQuery: $memberQuery, membershipMethod: $membershipMethod, memberSuggestionsNotify: $memberSuggestionsNotify, memberQueryExemptions: $memberQueryExemptions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the associations of a System Group
#
# GET /systemgroups/{group_id}/associations
# operationId: graph_systemGroupAssociationsList
export def "systemgroups-associations systemGroupAssociationsList" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "system_group" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a System Group
#
# POST /systemgroups/{group_id}/associations
# operationId: graph_systemGroupAssociationsPost
export def "systemgroups-associations systemGroupAssociationsPost" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-5 # Targets which a "system_group" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systemgroups/($group_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Commands bound to a System Group
#
# GET /systemgroups/{group_id}/commands
# operationId: graph_systemGroupTraverseCommand
export def "systemgroups-commands systemGroupTraverseCommand" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --details: string # This will provide detail descriptive response for the request.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, commandType: string, name: string, timeout: string, timeToLiveSeconds: int, organization: string, command: string, launchType: string, schedule: string, scheduleRepeatType: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "details" $details "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/commands" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the members of a System Group
#
# GET /systemgroups/{group_id}/members
# operationId: graph_systemGroupMembersList
export def "systemgroups-members systemGroupMembersList" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/members" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the members of a System Group
#
# POST /systemgroups/{group_id}/members
# operationId: graph_systemGroupMembersPost
export def "systemgroups-members systemGroupMembersPost" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # The member type.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systemgroups/($group_id)/members")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Date": $Date, "Authorization": $Authorization, "x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the System Group's membership
#
# GET /systemgroups/{group_id}/membership
# operationId: graph_systemGroupMembership
export def "systemgroups-membership systemGroupMembership" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/membership" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policy Groups bound to a System Group
#
# GET /systemgroups/{group_id}/policygroups
# operationId: graph_systemGroupTraversePolicyGroup
export def "systemgroups-policygroups systemGroupTraversePolicyGroup" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/policygroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policies bound to a System Group
#
# GET /systemgroups/{group_id}/policies
# operationId: graph_systemGroupTraversePolicy
export def "systemgroups-policies systemGroupTraversePolicy" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/policies" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to a System Group
#
# GET /systemgroups/{group_id}/usergroups
# operationId: graph_systemGroupTraverseUserGroup
export def "systemgroups-usergroups systemGroupTraverseUserGroup" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a System Group
#
# GET /systemgroups/{group_id}/users
# operationId: graph_systemGroupTraverseUser
export def "systemgroups-users systemGroupTraverseUser" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# View an individual System Group details
#
# GET /systemgroups/{id}
# operationId: groups_system_get
export def "systemgroups get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<attributes: record, description: string, email: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryErrorFlags: list<string>, memberQueryExemptions: table<attributes: record, id: string, type: string>, membershipMethod: string, memberSuggestionsNotify: bool, name: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systemgroups/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a System Group
#
# PUT /systemgroups/{id}
# operationId: groups_system_put
# --memberQueryExemptions item shape: {attributes?: record, id: string, type: string}
export def "systemgroups put" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --attributes: record # The graph attributes.
  --email: string # Email address of a System Group
  --description: string # Description of a System Group
  name: string # Display name of a System Group.
  --memberQuery: any # Query using a sequence of field filters.
  --membershipMethod: string@membershipMethod-completer # The type of membership method for this group. Valid values include NOTSET, STATIC, DYNAMIC_REVIEW_REQUIRED, and DYNAMIC_AUTOMATED.  Note DYNAMIC_AUTOMATED and DYNAMIC_REVIEW_REQUIRED group rules will supersede any group enrollment for [group-associated MDM-enrolled devices](https://jumpcloud.com/support/change-a-default-device-group-for-apple-devices).  Use caution when creating dynamic device groups with MDM-enrolled devices to avoid creating conflicting rule sets.
  --memberSuggestionsNotify: string@bool-completer # True if notification emails are to be sent for membership suggestions.
  --memberQueryExemptions: list # Array of GraphObjects exempted from the query — item shape: {attributes?: record, id: string, type: string}
]: any -> record<attributes: record, description: string, email: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryErrorFlags: list<string>, memberQueryExemptions: table<attributes: record, id: string, type: string>, membershipMethod: string, memberSuggestionsNotify: bool, name: string, type: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systemgroups/($id)")
  let body = {attributes: $attributes, email: $email, description: $description, name: $name, memberQuery: $memberQuery, membershipMethod: $membershipMethod, memberSuggestionsNotify: $memberSuggestionsNotify, memberQueryExemptions: $memberQueryExemptions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a System Group
#
# DELETE /systemgroups/{id}
# operationId: groups_system_delete
export def "systemgroups delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<attributes: record, description: string, email: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryErrorFlags: list<string>, memberQueryExemptions: table<attributes: record, id: string, type: string>, membershipMethod: string, memberSuggestionsNotify: bool, name: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systemgroups/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights ALF
#
# GET /systeminsights/alf
# operationId: systeminsights_list_alf
export def "systeminsights-alf alf" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/alf" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights ALF Exceptions
#
# GET /systeminsights/alf_exceptions
# operationId: systeminsights_list_alf_exceptions
export def "systeminsights-alf-exceptions exceptions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/alf_exceptions" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights ALF Explicit Authentications
#
# GET /systeminsights/alf_explicit_auths
# operationId: systeminsights_list_alf_explicit_auths
export def "systeminsights-alf-explicit-auths auths" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/alf_explicit_auths" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Application Compatibility Shims
#
# GET /systeminsights/appcompat_shims
# operationId: systeminsights_list_appcompat_shims
export def "systeminsights-appcompat-shims shims" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/appcompat_shims" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Apps
#
# GET /systeminsights/apps
# operationId: systeminsights_list_apps
export def "systeminsights-apps apps" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/apps" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Authorized Keys
#
# GET /systeminsights/authorized_keys
# operationId: systeminsights_list_authorized_keys
export def "systeminsights-authorized-keys keys" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/authorized_keys" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Battery
#
# GET /systeminsights/battery
# operationId: systeminsights_list_battery
export def "systeminsights-battery battery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/battery" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Bitlocker Info
#
# GET /systeminsights/bitlocker_info
# operationId: systeminsights_list_bitlocker_info
export def "systeminsights-bitlocker-info info" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/bitlocker_info" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Browser Plugins
#
# GET /systeminsights/browser_plugins
# operationId: systeminsights_list_browser_plugins
export def "systeminsights-browser-plugins plugins" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/browser_plugins" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Certificates
#
# GET /systeminsights/certificates
# operationId: systeminsights_list_certificates
export def "systeminsights-certificates certificates" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3` Note: You can only filter by `system_id` and `common_name`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/certificates" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Chrome Extensions
#
# GET /systeminsights/chrome_extensions
# operationId: systeminsights_list_chrome_extensions
export def "systeminsights-chrome-extensions extensions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/chrome_extensions" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Connectivity
#
# GET /systeminsights/connectivity
# operationId: systeminsights_list_connectivity
export def "systeminsights-connectivity connectivity" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/connectivity" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Crashes
#
# GET /systeminsights/crashes
# operationId: systeminsights_list_crashes
export def "systeminsights-crashes crashes" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/crashes" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights CUPS Destinations
#
# GET /systeminsights/cups_destinations
# operationId: systeminsights_list_cups_destinations
export def "systeminsights-cups-destinations destinations" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/cups_destinations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Disk Encryption
#
# GET /systeminsights/disk_encryption
# operationId: systeminsights_list_disk_encryption
export def "systeminsights-disk-encryption encryption" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/disk_encryption" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Disk Info
#
# GET /systeminsights/disk_info
# operationId: systeminsights_list_disk_info
export def "systeminsights-disk-info info" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/disk_info" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights DNS Resolvers
#
# GET /systeminsights/dns_resolvers
# operationId: systeminsights_list_dns_resolvers
export def "systeminsights-dns-resolvers resolvers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/dns_resolvers" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Etc Hosts
#
# GET /systeminsights/etc_hosts
# operationId: systeminsights_list_etc_hosts
export def "systeminsights-etc-hosts hosts" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/etc_hosts" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Firefox Addons
#
# GET /systeminsights/firefox_addons
# operationId: systeminsights_list_firefox_addons
export def "systeminsights-firefox-addons addons" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/firefox_addons" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Groups
#
# GET /systeminsights/groups
# operationId: systeminsights_list_groups
export def "systeminsights-groups groups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/groups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights IE Extensions
#
# GET /systeminsights/ie_extensions
# operationId: systeminsights_list_ie_extensions
export def "systeminsights-ie-extensions extensions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/ie_extensions" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Interface Addresses
#
# GET /systeminsights/interface_addresses
# operationId: systeminsights_list_interface_addresses
export def "systeminsights-interface-addresses addresses" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/interface_addresses" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Interface Details
#
# GET /systeminsights/interface_details
# operationId: systeminsights_list_interface_details
export def "systeminsights-interface-details details" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/interface_details" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Kernel Info
#
# GET /systeminsights/kernel_info
# operationId: systeminsights_list_kernel_info
export def "systeminsights-kernel-info info" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/kernel_info" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Launchd
#
# GET /systeminsights/launchd
# operationId: systeminsights_list_launchd
export def "systeminsights-launchd launchd" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/launchd" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Linux Packages
#
# GET /systeminsights/linux_packages
# operationId: systeminsights_list_linux_packages
export def "systeminsights-linux-packages packages" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/linux_packages" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Logged-In Users
#
# GET /systeminsights/logged_in_users
# operationId: systeminsights_list_logged_in_users
export def "systeminsights-logged-in-users users" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/logged_in_users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Logical Drives
#
# GET /systeminsights/logical_drives
# operationId: systeminsights_list_logical_drives
export def "systeminsights-logical-drives drives" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/logical_drives" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Managed Policies
#
# GET /systeminsights/managed_policies
# operationId: systeminsights_list_managed_policies
export def "systeminsights-managed-policies policies" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/managed_policies" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Mounts
#
# GET /systeminsights/mounts
# operationId: systeminsights_list_mounts
export def "systeminsights-mounts mounts" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/mounts" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights OS Version
#
# GET /systeminsights/os_version
# operationId: systeminsights_list_os_version
export def "systeminsights-os-version version" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/os_version" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Patches
#
# GET /systeminsights/patches
# operationId: systeminsights_list_patches
export def "systeminsights-patches patches" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/patches" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Programs
#
# GET /systeminsights/programs
# operationId: systeminsights_list_programs
export def "systeminsights-programs programs" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/programs" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Python Packages
#
# GET /systeminsights/python_packages
# operationId: systeminsights_list_python_packages
export def "systeminsights-python-packages packages" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/python_packages" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Safari Extensions
#
# GET /systeminsights/safari_extensions
# operationId: systeminsights_list_safari_extensions
export def "systeminsights-safari-extensions extensions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/safari_extensions" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Scheduled Tasks
#
# GET /systeminsights/scheduled_tasks
# operationId: systeminsights_list_scheduled_tasks
export def "systeminsights-scheduled-tasks tasks" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/scheduled_tasks" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Services
#
# GET /systeminsights/services
# operationId: systeminsights_list_services
export def "systeminsights-services services" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/services" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# LIst System Insights Shadow
#
# GET /systeminsights/shadow
# operationId: systeminsights_list_shadow
export def "systeminsights-shadow shadow" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/shadow" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Shared Folders
#
# GET /systeminsights/shared_folders
# operationId: systeminsights_list_shared_folders
export def "systeminsights-shared-folders folders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/shared_folders" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Shared Resources
#
# GET /systeminsights/shared_resources
# operationId: systeminsights_list_shared_resources
export def "systeminsights-shared-resources resources" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/shared_resources" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Sharing Preferences
#
# GET /systeminsights/sharing_preferences
# operationId: systeminsights_list_sharing_preferences
export def "systeminsights-sharing-preferences preferences" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/sharing_preferences" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights SIP Config
#
# GET /systeminsights/sip_config
# operationId: systeminsights_list_sip_config
export def "systeminsights-sip-config config" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/sip_config" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Startup Items
#
# GET /systeminsights/startup_items
# operationId: systeminsights_list_startup_items
export def "systeminsights-startup-items items" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/startup_items" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights System Control
#
# GET /systeminsights/system_controls
# operationId: systeminsights_list_system_controls
export def "systeminsights-system-controls controls" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3` Note: You can only filter by `system_id` and `name`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/system_controls" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights System Info
#
# GET /systeminsights/system_info
# operationId: systeminsights_list_system_info
export def "systeminsights-system-info info" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/system_info" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Uptime
#
# GET /systeminsights/uptime
# operationId: systeminsights_list_uptime
export def "systeminsights-uptime uptime" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, gte, in. e.g: Filter for single value: `filter=field:gte:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/uptime" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights USB Devices
#
# GET /systeminsights/usb_devices
# operationId: systeminsights_list_usb_devices
export def "systeminsights-usb-devices devices" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/usb_devices" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights User Groups
#
# GET /systeminsights/user_groups
# operationId: systeminsights_list_user_groups
export def "systeminsights-user-groups groups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/user_groups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights User SSH Keys
#
# GET /systeminsights/user_ssh_keys
# operationId: systeminsights_list_user_ssh_keys
export def "systeminsights-user-ssh-keys keys" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/user_ssh_keys" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Users
#
# GET /systeminsights/users
# operationId: systeminsights_list_users
export def "systeminsights-users users" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<collection_time: string, system_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights WiFi Networks
#
# GET /systeminsights/wifi_networks
# operationId: systeminsights_list_wifi_networks
export def "systeminsights-wifi-networks networks" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/wifi_networks" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights WiFi Status
#
# GET /systeminsights/wifi_status
# operationId: systeminsights_list_wifi_status
export def "systeminsights-wifi-status status" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/wifi_status" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Windows Security Products
#
# GET /systeminsights/windows_security_products
# operationId: systeminsights_list_windows_security_products
export def "systeminsights-windows-security-products products" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/windows_security_products" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Windows Security Center
#
# GET /systeminsights/windows_security_center
# operationId: systeminsights_list_windows_security_center
export def "systeminsights-windows-security-center center" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/windows_security_center" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights User Assist
#
# GET /systeminsights/userassist
# operationId: systeminsights_list_userassist
export def "systeminsights-userassist userassist" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/userassist" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Chassis Info
#
# GET /systeminsights/chassis_info
# operationId: systeminsights_list_chassis_info
export def "systeminsights-chassis-info info" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/chassis_info" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Secure Boot
#
# GET /systeminsights/secureboot
# operationId: systeminsights_list_secureboot
export def "systeminsights-secureboot secureboot" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/secureboot" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights TPM Info
#
# GET /systeminsights/tpm_info
# operationId: systeminsights_list_tpm_info
export def "systeminsights-tpm-info info" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/tpm_info" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "text/html"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Azure Instance Metadata
#
# GET /systeminsights/azure_instance_metadata
# operationId: systeminsights_list_azure_instance_metadata
export def "systeminsights-azure-instance-metadata metadata" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/azure_instance_metadata" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List System Insights Azure Instance Tags
#
# GET /systeminsights/azure_instance_tags
# operationId: systeminsights_list_azure_instance_tags
export def "systeminsights-azure-instance-tags tags" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending. e.g: Sort by single field: `sort=field` Sort descending by single field: `sort=-field` Sort by multiple fields: `sort=field1,-field2,field3`
  --filter: list # Supported operators are: eq, in. e.g: Filter for single value: `filter=field:eq:value` Filter for any value in a list: (note "pipe" character: `|` separating values) `filter=field:in:value1|value2|value3`
  --limit: int # default: 10
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<system_id: string, collection_time: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systeminsights/azure_instance_tags" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a System
#
# GET /systems/{system_id}/associations
# operationId: graph_systemAssociationsList
export def "systems-associations systemAssociationsList" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "system" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/associations" $qp)
  let extra_headers = {"Date": $Date, "Authorization": $Authorization, "x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage associations of a System
#
# POST /systems/{system_id}/associations
# operationId: graph_systemAssociationsPost
# --attributes shape: {sudo?: record}
export def "systems-associations systemAssociationsPost" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # shape: {sudo?: record}
  type: string@type-completer-5 # Targets which a "system" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systems/($system_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Date": $Date, "Authorization": $Authorization, "x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Commands bound to a System
#
# GET /systems/{system_id}/commands
# operationId: graph_systemTraverseCommand
export def "systems-commands systemTraverseCommand" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --details: string # This will provide detail descriptive response for the request.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, commandType: string, name: string, timeout: string, timeToLiveSeconds: int, organization: string, command: string, launchType: string, schedule: string, scheduleRepeatType: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "details" $details "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/commands" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get System FDE Key
#
# GET /systems/{system_id}/fdekey
# operationId: systems_getFDEKey
export def "systems-fdekey get" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<key: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systems/($system_id)/fdekey")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the parent Groups of a System
#
# GET /systems/{system_id}/memberof
# operationId: graph_systemMemberOf
export def "systems-memberof systemMemberOf" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/memberof" $qp)
  let extra_headers = {"Date": $Date, "Authorization": $Authorization, "x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associated Software Application Statuses of a System
#
# GET /systems/{system_id}/softwareappstatuses
# operationId: systems_ListSoftwareAppsWithStatuses
export def "systems-softwareappstatuses ListSoftwareAppsWithStatuses" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<app: record<id: string, organization: string, displayName: string, settings: list>, status: record<id: string, softwareAppId: string, systemId: string, state: string, details: string, code: int, timestamp: string, version: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/softwareappstatuses" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policy Groups bound to a System
#
# GET /systems/{system_id}/policygroups
# operationId: graph_systemTraversePolicyGroup
export def "systems-policygroups systemTraversePolicyGroup" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/policygroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "Date": $Date, "Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policies bound to a System
#
# GET /systems/{system_id}/policies
# operationId: graph_systemTraversePolicy
export def "systems-policies systemTraversePolicy" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/policies" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the policy statuses for a system
#
# GET /systems/{system_id}/policystatuses
# operationId: policystatuses_systemsList
export def "systems-policystatuses systemsList" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<detail: string, endedAt: string, exitStatus: int, id: string, policyID: string, startedAt: string, state: string, stdErr: string, stdOut: string, success: bool, systemID: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/policystatuses" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the User Groups bound to a System
#
# GET /systems/{system_id}/usergroups
# operationId: graph_systemTraverseUserGroup
export def "systems-usergroups systemTraverseUserGroup" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "Date": $Date, "Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a System
#
# GET /systems/{system_id}/users
# operationId: graph_systemTraverseUser
export def "systems-users systemTraverseUser" [
  system_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --Date: string # Current date header for the System Context API
  --Authorization: string # Authorization header for the System Context API
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($system_id)/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "Date": $Date, "Authorization": $Authorization} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List all User Groups
#
# GET /usergroups
# operationId: groups_user_list
export def "usergroups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record<sudo: record, ldapGroups: list, posixGroups: list, radius: record, sambaEnabled: bool>, email: string, description: string, id: string, memberQuery: record<queryType: string, filters: list>, memberQueryExemptions: list<record>, memberSuggestionsNotify: bool, memberQueryErrorFlags: list<string>, membershipMethod: string, name: string, suggestionCounts: record<add: int, remove: int, total: int>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new User Group
#
# POST /usergroups
# operationId: groups_user_post
# --memberQueryExemptions item shape: {attributes?: record, id: string, type: string}
export def "usergroups post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --attributes: any # The graph attributes for a UserGroup.
  --email: string # Email address of a User Group
  --description: string # Description of a User Group
  --memberQuery: any # Query using a sequence of field filters.
  --memberQueryExemptions: list # Array of GraphObjects exempted from the query — item shape: {attributes?: record, id: string, type: string}
  --memberSuggestionsNotify: string@bool-completer # True if notification emails are to be sent for membership suggestions.
  --membershipMethod: string@membershipMethod-completer # The type of membership method for this group. Valid values include NOTSET, STATIC, DYNAMIC_REVIEW_REQUIRED, and DYNAMIC_AUTOMATED.  Note DYNAMIC_AUTOMATED and DYNAMIC_REVIEW_REQUIRED group rules will supersede any group enrollment for [group-associated MDM-enrolled devices](https://jumpcloud.com/support/change-a-default-device-group-for-apple-devices).  Use caution when creating dynamic device groups with MDM-enrolled devices to avoid creating conflicting rule sets.
  name: string # Display name of a User Group.
]: any -> record<attributes: record<sudo: record<enabled: bool, withoutPassword: bool>, ldapGroups: list<record>, posixGroups: list<record>, radius: record<reply: list>, sambaEnabled: bool>, email: string, description: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryExemptions: table<attributes: record, id: string, type: string>, memberSuggestionsNotify: bool, memberQueryErrorFlags: list<string>, membershipMethod: string, name: string, suggestionCounts: record<add: int, remove: int, total: int>, type: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/usergroups")
  let body = {attributes: $attributes, email: $email, description: $description, memberQuery: $memberQuery, memberQueryExemptions: $memberQueryExemptions, memberSuggestionsNotify: $memberSuggestionsNotify, membershipMethod: $membershipMethod, name: $name} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Active Directories bound to a User Group
#
# GET /usergroups/{group_id}/activedirectories
# operationId: graph_userGroupTraverseActiveDirectory
export def "usergroups-activedirectories userGroupTraverseActiveDirectory" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/activedirectories" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Applications bound to a User Group
#
# GET /usergroups/{group_id}/applications
# operationId: graph_userGroupTraverseApplication
export def "usergroups-applications userGroupTraverseApplication" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/applications" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a User Group.
#
# GET /usergroups/{group_id}/associations
# operationId: graph_userGroupAssociationsList
export def "usergroups-associations userGroupAssociationsList" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "user_group" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a User Group
#
# POST /usergroups/{group_id}/associations
# operationId: graph_userGroupAssociationsPost
export def "usergroups-associations userGroupAssociationsPost" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer-6 # Targets which a "user_group" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/usergroups/($group_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Directories bound to a User Group
#
# GET /usergroups/{group_id}/directories
# operationId: graph_userGroupTraverseDirectory
export def "usergroups-directories userGroupTraverseDirectory" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/directories" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the G Suite instances bound to a User Group
#
# GET /usergroups/{group_id}/gsuites
# operationId: graph_userGroupTraverseGSuite
export def "usergroups-gsuites userGroupTraverseGSuite" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/gsuites" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the LDAP Servers bound to a User Group
#
# GET /usergroups/{group_id}/ldapservers
# operationId: graph_userGroupTraverseLdapServer
export def "usergroups-ldapservers userGroupTraverseLdapServer" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/ldapservers" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the members of a User Group
#
# GET /usergroups/{group_id}/members
# operationId: graph_userGroupMembersList
export def "usergroups-members userGroupMembersList" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/members" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the members of a User Group
#
# POST /usergroups/{group_id}/members
# operationId: graph_userGroupMembersPost
export def "usergroups-members userGroupMembersPost" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string # The member type.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/usergroups/($group_id)/members")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the User Group's membership
#
# GET /usergroups/{group_id}/membership
# operationId: graph_userGroupMembership
export def "usergroups-membership userGroupMembership" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **Valid filter fields**: `object_id` and `type`.  **EX:** `GET /api/v2/usergroups/{GroupID}/membership?filter=type:eq:user_group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/membership" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Office 365 instances bound to a User Group
#
# GET /usergroups/{group_id}/office365s
# operationId: graph_userGroupTraverseOffice365
export def "usergroups-office365s userGroupTraverseOffice365" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/office365s" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policies bound to a User Group
#
# GET /usergroups/{group_id}/policies
# operationId: graph_userGroupTraversePolicy
export def "usergroups-policies userGroupTraversePolicy" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/policies" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policy Groups bound to a User Group
#
# GET /usergroups/{group_id}/policygroups
# operationId: graph_userGroupTraversePolicyGroup
export def "usergroups-policygroups userGroupTraversePolicyGroup" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/policygroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the RADIUS Servers bound to a User Group
#
# GET /usergroups/{group_id}/radiusservers
# operationId: graph_userGroupTraverseRadiusServer
export def "usergroups-radiusservers userGroupTraverseRadiusServer" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/radiusservers" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the System Groups bound to User Groups
#
# GET /usergroups/{group_id}/systemgroups
# operationId: graph_userGroupTraverseSystemGroup
export def "usergroups-systemgroups userGroupTraverseSystemGroup" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Systems bound to a User Group
#
# GET /usergroups/{group_id}/systems
# operationId: graph_userGroupTraverseSystem
export def "usergroups-systems userGroupTraverseSystem" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/systems" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Suggestions for a User Group
#
# GET /usergroups/{group_id}/suggestions
# operationId: groups_user_suggestions_get
export def "usergroups-suggestions get" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<object: record<attributes: record, id: string, type: string>, op: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/usergroups/($group_id)/suggestions" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Apply Suggestions for a User Group
#
# POST /usergroups/{group_id}/suggestions
# operationId: groups_user_suggestions_post
export def "usergroups-suggestions post" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --user-ids: list
]: any -> table<suggestions_found: list<string>, suggestions_not_found: list<string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/usergroups/($group_id)/suggestions")
  let body = {user_ids: $user_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Suggestions for a System Group
#
# GET /systemgroups/{group_id}/suggestions
# operationId: groups_system_suggestions_get
export def "systemgroups-suggestions get" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<object: record<attributes: record, id: string, type: string>, op: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/systemgroups/($group_id)/suggestions" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Apply Suggestions for a System Group
#
# POST /systemgroups/{group_id}/suggestions
# operationId: groups_system_suggestions_post
export def "systemgroups-suggestions post" [
  group_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --object-ids: list
]: any -> table<suggestions_found: list<string>, suggestions_not_found: list<string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systemgroups/($group_id)/suggestions")
  let body = {object_ids: $object_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# View an individual User Group details
#
# GET /usergroups/{id}
# operationId: groups_user_get
export def "usergroups get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<attributes: record<sudo: record<enabled: bool, withoutPassword: bool>, ldapGroups: list<record>, posixGroups: list<record>, radius: record<reply: list>, sambaEnabled: bool>, email: string, description: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryExemptions: table<attributes: record, id: string, type: string>, memberSuggestionsNotify: bool, memberQueryErrorFlags: list<string>, membershipMethod: string, name: string, suggestionCounts: record<add: int, remove: int, total: int>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/usergroups/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a User Group
#
# PUT /usergroups/{id}
# operationId: groups_user_put
# --memberQueryExemptions item shape: {attributes?: record, id: string, type: string}
export def "usergroups put" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --attributes: any # The graph attributes for a UserGroup.
  --email: string # Email address of a User Group
  --description: string # Description of a User Group
  --memberQuery: any # Query using a sequence of field filters.
  --memberQueryExemptions: list # Array of GraphObjects exempted from the query — item shape: {attributes?: record, id: string, type: string}
  --memberSuggestionsNotify: string@bool-completer # True if notification emails are to be sent for membership suggestions.
  --membershipMethod: string@membershipMethod-completer # The type of membership method for this group. Valid values include NOTSET, STATIC, DYNAMIC_REVIEW_REQUIRED, and DYNAMIC_AUTOMATED.  Note DYNAMIC_AUTOMATED and DYNAMIC_REVIEW_REQUIRED group rules will supersede any group enrollment for [group-associated MDM-enrolled devices](https://jumpcloud.com/support/change-a-default-device-group-for-apple-devices).  Use caution when creating dynamic device groups with MDM-enrolled devices to avoid creating conflicting rule sets.
  name: string # Display name of a User Group.
]: any -> record<attributes: record<sudo: record<enabled: bool, withoutPassword: bool>, ldapGroups: list<record>, posixGroups: list<record>, radius: record<reply: list>, sambaEnabled: bool>, email: string, description: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryExemptions: table<attributes: record, id: string, type: string>, memberSuggestionsNotify: bool, memberQueryErrorFlags: list<string>, membershipMethod: string, name: string, suggestionCounts: record<add: int, remove: int, total: int>, type: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/usergroups/($id)")
  let body = {attributes: $attributes, email: $email, description: $description, memberQuery: $memberQuery, memberQueryExemptions: $memberQueryExemptions, memberSuggestionsNotify: $memberSuggestionsNotify, membershipMethod: $membershipMethod, name: $name} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a User Group
#
# DELETE /usergroups/{id}
# operationId: groups_user_delete
export def "usergroups delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<attributes: record<sudo: record<enabled: bool, withoutPassword: bool>, ldapGroups: list<record>, posixGroups: list<record>, radius: record<reply: list>, sambaEnabled: bool>, email: string, description: string, id: string, memberQuery: record<queryType: string, filters: list<record>>, memberQueryExemptions: table<attributes: record, id: string, type: string>, memberSuggestionsNotify: bool, memberQueryErrorFlags: list<string>, membershipMethod: string, name: string, suggestionCounts: record<add: int, remove: int, total: int>, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/usergroups/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Active Directory instances bound to a User
#
# GET /users/{user_id}/activedirectories
# operationId: graph_userTraverseActiveDirectory
export def "users-activedirectories userTraverseActiveDirectory" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/activedirectories" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Applications bound to a User
#
# GET /users/{user_id}/applications
# operationId: graph_userTraverseApplication
export def "users-applications userTraverseApplication" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/applications" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the associations of a User
#
# GET /users/{user_id}/associations
# operationId: graph_userAssociationsList
export def "users-associations userAssociationsList" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "user" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a User
#
# POST /users/{user_id}/associations
# operationId: graph_userAssociationsPost
# --attributes shape: {sudo?: record}
export def "users-associations userAssociationsPost" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # shape: {sudo?: record}
  type: string@type-completer-6 # Targets which a "user" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($user_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Directories bound to a User
#
# GET /users/{user_id}/directories
# operationId: graph_userTraverseDirectory
export def "users-directories userTraverseDirectory" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/directories" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the G Suite instances bound to a User
#
# GET /users/{user_id}/gsuites
# operationId: graph_userTraverseGSuite
export def "users-gsuites userTraverseGSuite" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/gsuites" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the LDAP servers bound to a User
#
# GET /users/{user_id}/ldapservers
# operationId: graph_userTraverseLdapServer
export def "users-ldapservers userTraverseLdapServer" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/ldapservers" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the parent Groups of a User
#
# GET /users/{user_id}/memberof
# operationId: graph_userMemberOf
export def "users-memberof userMemberOf" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **Valid filter fields**: `object_id` and `type`.  **EX:** `GET /api/v2/users/{UserID}/memberof?filter=type:eq:user_group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/memberof" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Office 365 instances bound to a User
#
# GET /users/{user_id}/office365s
# operationId: graph_userTraverseOffice365
export def "users-office365s userTraverseOffice365" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/office365s" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policies bound to a User
#
# GET /users/{user_id}/policies
# operationId: graph_userTraversePolicy
export def "users-policies userTraversePolicy" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/policies" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Policy Groups bound to a User
#
# GET /users/{user_id}/policygroups
# operationId: graph_userTraversePolicyGroup
export def "users-policygroups userTraversePolicyGroup" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/policygroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the policy statuses for a user
#
# GET /users/{user_id}/policystatuses
# operationId: policystatuses_usersList
export def "users-policystatuses usersList" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<detail: string, endedAt: string, exitStatus: int, id: string, policyID: string, startedAt: string, state: string, stdErr: string, stdOut: string, success: bool, systemID: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/policystatuses" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Push Endpoints associated with a User
#
# GET /users/{user_id}/pushendpoints
# operationId: pushEndpoints_list
export def "users-pushendpoints list" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<id: string, state: string, name: string, device: record<make: string, model: string, os: string, appVersion: string, osVersion: string, uvEnabled: bool>, enrollmentDate: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($user_id)/pushendpoints")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a push endpoint associated with a User
#
# GET /users/{user_id}/pushendpoints/{push_endpoint_id}
# operationId: pushEndpoints_get
export def "users-pushendpoints get" [
  user_id: string
  push_endpoint_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, state: string, name: string, device: record<make: string, model: string, os: string, appVersion: string, osVersion: string, uvEnabled: bool>, enrollmentDate: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($user_id)/pushendpoints/($push_endpoint_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete a Push Endpoint associated with a User
#
# DELETE /users/{user_id}/pushendpoints/{push_endpoint_id}
# operationId: pushEndpoints_delete
export def "users-pushendpoints delete" [
  user_id: string
  push_endpoint_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, state: string, name: string, device: record<make: string, model: string, os: string, appVersion: string, osVersion: string, uvEnabled: bool>, enrollmentDate: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($user_id)/pushendpoints/($push_endpoint_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a push endpoint associated with a User
#
# PATCH /users/{user_id}/pushendpoints/{push_endpoint_id}
# operationId: pushEndpoints_patch
export def "users-pushendpoints patch" [
  user_id: string
  push_endpoint_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --state: string@state-completer-1
]: any -> record<id: string, state: string, name: string, device: record<make: string, model: string, os: string, appVersion: string, osVersion: string, uvEnabled: bool>, enrollmentDate: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($user_id)/pushendpoints/($push_endpoint_id)")
  let body = {name: $name, state: $state} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the RADIUS Servers bound to a User
#
# GET /users/{user_id}/radiusservers
# operationId: graph_userTraverseRadiusServer
export def "users-radiusservers userTraverseRadiusServer" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/radiusservers" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the System Groups bound to a User
#
# GET /users/{user_id}/systemgroups
# operationId: graph_userTraverseSystemGroup
export def "users-systemgroups userTraverseSystemGroup" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/systemgroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Systems bound to a User
#
# GET /users/{user_id}/systems
# operationId: graph_userTraverseSystem
export def "users-systems userTraverseSystem" [
  user_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/users/($user_id)/systems" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Workdays
#
# GET /workdays
# operationId: workdays_list
export def "workdays list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-fields: list # The comma separated fields included in the returned records. If omitted, the default list of fields will be returned.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<auth: record<basic: record, oauth: record>, id: string, lastImport: string, name: string, reportUrl: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "fields" $qp_fields "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/workdays" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create new Workday
#
# POST /workdays
# operationId: workdays_post
# --auth shape: {basic?: record, oauth?: record}
export def "workdays post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body-auth: record # shape: {basic?: record, oauth?: record}
  --name: string
  --reportUrl: string
]: any -> record<auth: record<basic: record<expiry: string, isValid: bool, message: string>, oauth: record<expiry: string, isValid: bool, message: string>>, id: string, lastImport: string, name: string, reportUrl: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/workdays")
  let body = {auth: $body_auth, name: $name, reportUrl: $reportUrl} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Workday
#
# GET /workdays/{id}
# operationId: workdays_get
export def "workdays get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<auth: record<basic: record<expiry: string, isValid: bool, message: string>, oauth: record<expiry: string, isValid: bool, message: string>>, id: string, lastImport: string, name: string, reportUrl: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/workdays/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Workday
#
# PUT /workdays/{id}
# operationId: workdays_put
export def "workdays put" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --reportUrl: string
]: any -> record<auth: record<basic: record<expiry: string, isValid: bool, message: string>, oauth: record<expiry: string, isValid: bool, message: string>>, id: string, lastImport: string, name: string, reportUrl: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/workdays/($id)")
  let body = {name: $name, reportUrl: $reportUrl} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Import Results
#
# GET /workdays/{id}/import/{job_id}/results
# operationId: workdays_importresults
export def "workdays-import-results importresults" [
  id: string
  job_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<meta: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/workdays/($id)/import/($job_id)/results" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Authorize Workday
#
# POST /workdays/{workday_id}/auth
# operationId: workdays_authorize
# --auth shape: {basic?: record, oauth?: record}
export def "workdays-auth authorize" [
  workday_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body-auth: record # shape: {basic?: record, oauth?: record}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/workdays/($workday_id)/auth")
  let body = {auth: $body_auth} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Deauthorize Workday
#
# DELETE /workdays/{workday_id}/auth
# operationId: workdays_deauthorize
export def "workdays-auth deauthorize" [
  workday_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/workdays/($workday_id)/auth")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Workday Import
#
# POST /workdays/{workday_id}/import
# operationId: workdays_import
export def "workdays-import import" [
  workday_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --body: record
]: any -> record<jobId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/workdays/($workday_id)/import")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Workday Workers
#
# GET /workdays/{workday_id}/workers
# operationId: workdays_workers
export def "workdays-workers workers" [
  workday_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, email: string, firstName: string, lastName: string, username: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/workdays/($workday_id)/workers" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List AppleMDM Devices
#
# GET /applemdms/{apple_mdm_id}/devices
# operationId: applemdms_deviceslist
export def "applemdms-devices deviceslist" [
  apple_mdm_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-total-count: int
]: nothing -> table<id: string, serialNumber: string, udid: string, osVersion: string, depRegistered: bool, enrolled: bool, createdAt: string, hasActivationLockBypassCodes: bool, modelFamily: string, mdmOsFamily: string, securityInfo: record<enrolledViaDep: bool, isActivationLockManageable: bool, isUserEnrollment: bool, userApprovedEnrollment: bool, passcodePresent: bool, passcodeCompliant: bool, filevaultEnabled: bool, recoveryLockEnabled: bool>, deviceInformation: record<isSupervised: bool, activationLockAllowedWhileSupervised: bool, availableDeviceCapacity: float, deviceCapacity: float, deviceName: string, iccid: string, imei: string, subscriberCarrierNetwork: string, secondIccid: string, secondImei: string, secondSubscriberCarrierNetwork: string, wifiMac: string, modelName: string, osMarketingName: string, osSupplementalBuildVersion: string, osBuildVersion: string, osExtraVersion: string, sofwareUpdateId: string, modelMarketingName: string, modelNumber: string, isAppleSilicon: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "x-total-count": $x_total_count} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Details of an AppleMDM Device
#
# GET /applemdms/{apple_mdm_id}/devices/{device_id}
# operationId: applemdms_getdevice
export def "applemdms-devices getdevice" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, serialNumber: string, udid: string, osVersion: string, depRegistered: bool, enrolled: bool, createdAt: string, hasActivationLockBypassCodes: bool, modelFamily: string, mdmOsFamily: string, securityInfo: record<enrolledViaDep: bool, isActivationLockManageable: bool, isUserEnrollment: bool, userApprovedEnrollment: bool, passcodePresent: bool, passcodeCompliant: bool, filevaultEnabled: bool, recoveryLockEnabled: bool>, deviceInformation: record<isSupervised: bool, activationLockAllowedWhileSupervised: bool, availableDeviceCapacity: float, deviceCapacity: float, deviceName: string, iccid: string, imei: string, subscriberCarrierNetwork: string, secondIccid: string, secondImei: string, secondSubscriberCarrierNetwork: string, wifiMac: string, modelName: string, osMarketingName: string, osSupplementalBuildVersion: string, osBuildVersion: string, osExtraVersion: string, sofwareUpdateId: string, modelMarketingName: string, modelNumber: string, isAppleSilicon: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Remove an Apple MDM Device's Enrollment
#
# DELETE /applemdms/{apple_mdm_id}/devices/{device_id}
# operationId: applemdms_deletedevice
export def "applemdms-devices deletedevice" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, serialNumber: string, udid: string, osVersion: string, depRegistered: bool, enrolled: bool, createdAt: string, hasActivationLockBypassCodes: bool, modelFamily: string, mdmOsFamily: string, securityInfo: record<enrolledViaDep: bool, isActivationLockManageable: bool, isUserEnrollment: bool, userApprovedEnrollment: bool, passcodePresent: bool, passcodeCompliant: bool, filevaultEnabled: bool, recoveryLockEnabled: bool>, deviceInformation: record<isSupervised: bool, activationLockAllowedWhileSupervised: bool, availableDeviceCapacity: float, deviceCapacity: float, deviceName: string, iccid: string, imei: string, subscriberCarrierNetwork: string, secondIccid: string, secondImei: string, secondSubscriberCarrierNetwork: string, wifiMac: string, modelName: string, osMarketingName: string, osSupplementalBuildVersion: string, osBuildVersion: string, osExtraVersion: string, sofwareUpdateId: string, modelMarketingName: string, modelNumber: string, isAppleSilicon: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Authentication Policies
#
# GET /authn/policies
# operationId: authnpolicies_list
export def "authn-policies list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-total-count: int
]: nothing -> table<id: string, name: string, description: string, disabled: bool, monitorOnly: bool, effect: record<action: string, obligations: record, custom_error_message: record>, targets: record<excludedApplications: list, resources: list, userAttributes: record, userGroups: record, users: record>, type: string, conditions: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/authn/policies" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "x-total-count": $x_total_count} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create an Authentication Policy
#
# POST /authn/policies
# operationId: authnpolicies_post
# --effect shape: {action: "allow"|"deny"|"unknown", obligations?: record, custom_error_message?: record}
# --targets shape: {excludedApplications?: list, resources?: list, userAttributes?: record, userGroups?: record, users?: record}
export def "authn-policies post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --description: string
  --disabled: string@bool-completer
  --monitorOnly: string@bool-completer
  --effect: record # shape: {action: "allow"|"deny"|"unknown", obligations?: record, custom_error_message?: record}
  --targets: record # shape: {excludedApplications?: list, resources?: list, userAttributes?: record, userGroups?: record, users?: record}
  --type: string@type-completer-7 # default: user_portal
  --conditions: record # Conditions may be added to an authentication policy using the following conditional language:  ``` <conditions> ::= <expression> <expression> ::= <deviceEncrypted> | <deviceManaged> | <ipAddressIn> |                  <locationIn> | <notExpression> | <allExpression> |                  <anyExpression> <deviceEncrypted> ::= { "deviceEncrypted": <boolean> } <deviceManaged> ::= { "deviceManaged": <boolean> } <ipAddressIn> ::= { "ipAddressIn": [ <objectId>, ... ] } <locationIn> ::= { "locationIn": {                      "countries": [                        <iso_3166_country_code>, ...                      ]                    }                  } <notExpression> ::= { "not": <expression> } <allExpression> ::= { "all": [ <expression>, ... ] } <anyExpression> ::= { "any": [ <expression>, ... ] } ```  For example, to add a condition that applies to IP addresses in a given list, the following condition can be added:  ``` {"ipAddressIn": [ <ip_list_object_id> ]} ```  If you would rather exclude IP addresses in the given lists, the following condition could be added:  ``` {   "not": {     "ipAddressIn": [ <ip_list_object_id_1>, <ip_list_object_id_2> ]   } } ```  You may also include more than one condition and choose whether "all" or "any" of them must be met for the policy to apply:  ``` {   "all": [     {       "ipAddressIn": [ <ip_list_object_id>, ... ]     },     {       "deviceManaged": true     },     {       "locationIn": {         countries: [ <iso_3166_country_code>, ... ]       }     }   ] } ```
]: any -> record<id: string, name: string, description: string, disabled: bool, monitorOnly: bool, effect: record<action: string, obligations: record<mfa: record, userVerification: record, mfaFactors: list>, custom_error_message: record<message: string, enabled: bool, primary_help: record, secondary_help: record>>, targets: record<excludedApplications: list<string>, resources: list<record>, userAttributes: record<exclusions: list, inclusions: list>, userGroups: record<inclusions: list, exclusions: list>, users: record<inclusions: list>>, type: string, conditions: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/authn/policies")
  let body = {name: $name, description: $description, disabled: $disabled, monitorOnly: $monitorOnly, effect: $effect, targets: $targets, type: $type, conditions: $conditions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get an authentication policy
#
# GET /authn/policies/{id}
# operationId: authnpolicies_get
export def "authn-policies get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string, description: string, disabled: bool, monitorOnly: bool, effect: record<action: string, obligations: record<mfa: record, userVerification: record, mfaFactors: list>, custom_error_message: record<message: string, enabled: bool, primary_help: record, secondary_help: record>>, targets: record<excludedApplications: list<string>, resources: list<record>, userAttributes: record<exclusions: list, inclusions: list>, userGroups: record<inclusions: list, exclusions: list>, users: record<inclusions: list>>, type: string, conditions: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/authn/policies/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Authentication Policy
#
# DELETE /authn/policies/{id}
# operationId: authnpolicies_delete
export def "authn-policies delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string, description: string, disabled: bool, monitorOnly: bool, effect: record<action: string, obligations: record<mfa: record, userVerification: record, mfaFactors: list>, custom_error_message: record<message: string, enabled: bool, primary_help: record, secondary_help: record>>, targets: record<excludedApplications: list<string>, resources: list<record>, userAttributes: record<exclusions: list, inclusions: list>, userGroups: record<inclusions: list, exclusions: list>, users: record<inclusions: list>>, type: string, conditions: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/authn/policies/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Patch Authentication Policy
#
# PATCH /authn/policies/{id}
# operationId: authnpolicies_patch
# --effect shape: {action: "allow"|"deny"|"unknown", obligations?: record, custom_error_message?: record}
# --targets shape: {excludedApplications?: list, resources?: list, userAttributes?: record, userGroups?: record, users?: record}
export def "authn-policies patch" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --description: string
  --disabled: string@bool-completer
  --monitorOnly: string@bool-completer
  --effect: record # shape: {action: "allow"|"deny"|"unknown", obligations?: record, custom_error_message?: record}
  --targets: record # shape: {excludedApplications?: list, resources?: list, userAttributes?: record, userGroups?: record, users?: record}
  --type: string@type-completer-7 # default: user_portal
  --conditions: record # Conditions may be added to an authentication policy using the following conditional language:  ``` <conditions> ::= <expression> <expression> ::= <deviceEncrypted> | <deviceManaged> | <ipAddressIn> |                  <locationIn> | <notExpression> | <allExpression> |                  <anyExpression> <deviceEncrypted> ::= { "deviceEncrypted": <boolean> } <deviceManaged> ::= { "deviceManaged": <boolean> } <ipAddressIn> ::= { "ipAddressIn": [ <objectId>, ... ] } <locationIn> ::= { "locationIn": {                      "countries": [                        <iso_3166_country_code>, ...                      ]                    }                  } <notExpression> ::= { "not": <expression> } <allExpression> ::= { "all": [ <expression>, ... ] } <anyExpression> ::= { "any": [ <expression>, ... ] } ```  For example, to add a condition that applies to IP addresses in a given list, the following condition can be added:  ``` {"ipAddressIn": [ <ip_list_object_id> ]} ```  If you would rather exclude IP addresses in the given lists, the following condition could be added:  ``` {   "not": {     "ipAddressIn": [ <ip_list_object_id_1>, <ip_list_object_id_2> ]   } } ```  You may also include more than one condition and choose whether "all" or "any" of them must be met for the policy to apply:  ``` {   "all": [     {       "ipAddressIn": [ <ip_list_object_id>, ... ]     },     {       "deviceManaged": true     },     {       "locationIn": {         countries: [ <iso_3166_country_code>, ... ]       }     }   ] } ```
]: any -> record<id: string, name: string, description: string, disabled: bool, monitorOnly: bool, effect: record<action: string, obligations: record<mfa: record, userVerification: record, mfaFactors: list>, custom_error_message: record<message: string, enabled: bool, primary_help: record, secondary_help: record>>, targets: record<excludedApplications: list<string>, resources: list<record>, userAttributes: record<exclusions: list, inclusions: list>, userGroups: record<inclusions: list, exclusions: list>, users: record<inclusions: list>>, type: string, conditions: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/authn/policies/($id)")
  let body = {name: $name, description: $description, disabled: $disabled, monitorOnly: $monitorOnly, effect: $effect, targets: $targets, type: $type, conditions: $conditions} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get an IP list
#
# GET /iplists/{id}
# operationId: iplists_get
export def "iplists get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string, description: string, ips: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/iplists/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Replace an IP list
#
# PUT /iplists/{id}
# operationId: iplists_put
export def "iplists put" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --description: string
  --ips: list
]: any -> record<id: string, name: string, description: string, ips: list<string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/iplists/($id)")
  let body = {name: $name, description: $description, ips: $ips} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete an IP list
#
# DELETE /iplists/{id}
# operationId: iplists_delete
export def "iplists delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string, description: string, ips: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/iplists/($id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update an IP list
#
# PATCH /iplists/{id}
# operationId: iplists_patch
export def "iplists patch" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --name: string
  --description: string
  --ips: list
]: any -> record<id: string, name: string, description: string, ips: list<string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/iplists/($id)")
  let body = {name: $name, description: $description, ips: $ips} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a list of users to import from a G Suite instance
#
# GET /gsuites/{gsuite_id}/import/users
# operationId: gsuites_listImportUsers
export def "gsuites-import-users listImportUsers" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --maxResults: int # Google Directory API maximum number of results per page. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
  --orderBy: string # Google Directory API sort field parameter. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
  --pageToken: string # Google Directory API token used to access the next page of results. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
  --qp-query: string # Google Directory API search parameter. See https://developers.google.com/admin-sdk/directory/v1/guides/search-users.
  --sortOrder: string # Google Directory API sort direction parameter. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
]: nothing -> record<nextPageToken: string, users: table<id: string, primaryEmail: string, givenName: string, familyName: string, thumbnailPhotoUrl: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "maxResults" $maxResults "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageToken" $pageToken "scalar") (serialize-qp "query" $qp_query "scalar") (serialize-qp "sortOrder" $sortOrder "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/import/users" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a list of users in Jumpcloud format to import from a Google Workspace account.
#
# GET /gsuites/{gsuite_id}/import/jumpcloudusers
# operationId: gsuites_listImportJumpcloudUsers
export def "gsuites-import-jumpcloudusers listImportJumpcloudUsers" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allowEmptyFields: string@bool-completer # [EXPERIMENTAL] Allow empty fields of the user object to be sent in the response (default: false)
  --maxResults: int # Google Directory API maximum number of results per page. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
  --orderBy: string # Google Directory API sort field parameter. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
  --pageToken: string # Google Directory API token used to access the next page of results. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
  --qp-query: string # Google Directory API search parameter. See https://developers.google.com/admin-sdk/directory/v1/guides/search-users.
  --sortOrder: string # Google Directory API sort direction parameter. See https://developers.google.com/admin-sdk/directory/reference/rest/v1/users/list.
]: nothing -> record<nextPageToken: string, users: table<addresses: list, alternateEmail: string, company: string, costCenter: string, department: string, email: string, employeeIdentifier: string, employeeType: string, firstname: string, jobTitle: string, lastname: string, location: string, phoneNumbers: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "allowEmptyFields" $allowEmptyFields "scalar") (serialize-qp "maxResults" $maxResults "scalar") (serialize-qp "orderBy" $orderBy "scalar") (serialize-qp "pageToken" $pageToken "scalar") (serialize-qp "query" $qp_query "scalar") (serialize-qp "sortOrder" $sortOrder "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/import/jumpcloudusers" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a list of users to import from an Office 365 instance
#
# GET /office365s/{office365_id}/import/users
# operationId: office365s_listImportUsers
export def "office365s-import-users listImportUsers" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --top: int # Office 365 API maximum number of results per page. See https://docs.microsoft.com/en-us/graph/paging.
  --skipToken: string # Office 365 API token used to access the next page of results. See https://docs.microsoft.com/en-us/graph/paging.
  --filter: string # Office 365 API filter parameter. See https://docs.microsoft.com/en-us/graph/api/user-list?view=graph-rest-1.0&tabs=http#optional-query-parameters.
  --search: string # Office 365 API search parameter. See https://docs.microsoft.com/en-us/graph/api/user-list?view=graph-rest-1.0&tabs=http#optional-query-parameters.
  --orderby: string # Office 365 API orderby parameter. See https://docs.microsoft.com/en-us/graph/api/user-list?view=graph-rest-1.0&tabs=http#optional-query-parameters.
  --count: string@bool-completer # Office 365 API count parameter. See https://docs.microsoft.com/en-us/graph/api/user-list?view=graph-rest-1.0&tabs=http#optional-query-parameters.
  --ConsistencyLevel: string # Defines the consistency header for O365 requests. See https://docs.microsoft.com/en-us/graph/api/user-list?view=graph-rest-1.0&tabs=http#request-headers
]: nothing -> record<top: int, skipToken: string, users: table<id: string, userPrincipalName: string, givenName: string, surname: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "top" $top "scalar") (serialize-qp "skipToken" $skipToken "scalar") (serialize-qp "filter" $filter "scalar") (serialize-qp "search" $search "scalar") (serialize-qp "orderby" $orderby "scalar") (serialize-qp "count" $count "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/office365s/($office365_id)/import/users" $qp)
  let extra_headers = {"ConsistencyLevel": $ConsistencyLevel} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create custom email configuration
#
# POST /customemails
# operationId: customEmails_create
export def "customemails create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  type: string@type-completer-8
  subject: string
  --title: string
  --header: string
  --body-body: string
  --button: string
  --nextStepContactInfo: string
]: any -> record<id: string, type: string, subject: string, title: string, header: string, body: string, button: string, nextStepContactInfo: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/customemails")
  let body = {type: $type, subject: $subject, title: $title, header: $header, body: $body_body, button: $button, nextStepContactInfo: $nextStepContactInfo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get custom email configuration
#
# GET /customemails/{custom_email_type}
# operationId: customEmails_read
export def "customemails read" [
  custom_email_type: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<id: string, type: string, subject: string, title: string, header: string, body: string, button: string, nextStepContactInfo: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/customemails/($custom_email_type)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update custom email configuration
#
# PUT /customemails/{custom_email_type}
# operationId: customEmails_update
export def "customemails update" [
  custom_email_type: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  type: string@type-completer-8
  subject: string
  --title: string
  --header: string
  --body-body: string
  --button: string
  --nextStepContactInfo: string
]: any -> record<id: string, type: string, subject: string, title: string, header: string, body: string, button: string, nextStepContactInfo: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/customemails/($custom_email_type)")
  let body = {type: $type, subject: $subject, title: $title, header: $header, body: $body_body, button: $button, nextStepContactInfo: $nextStepContactInfo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete custom email configuration
#
# DELETE /customemails/{custom_email_type}
# operationId: customEmails_destroy
export def "customemails delete" [
  custom_email_type: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/customemails/($custom_email_type)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List custom email templates
#
# GET /customemail/templates
# operationId: customEmails_getTemplates
export def "customemail-templates get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> table<type: string, displayName: string, description: string, messageType: string, fields: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/customemail/templates")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get the logo associated with the specified id
#
# GET /logos/{id}
# operationId: logos_get
export def "logos get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/logos/($id)")
  let accept_val = ($accept | default "image/gif")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Refresh activation lock information for a device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/refreshActivationLockInformation
# operationId: applemdms_devicesRefreshActivationLockInformation
export def "applemdms-devices-refresh-activation-lock-information devicesRefreshActivationLockInformation" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/refreshActivationLockInformation")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Schedule an OS update for a device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/scheduleOSUpdate
# operationId: applemdms_devicesScheduleOSUpdate
export def "applemdms-devices-schedule-os-update devicesScheduleOSUpdate" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  install_action: string@install-action-completer
  product_key: string
  --max-user-deferrals: int # format: int32
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/scheduleOSUpdate")
  let body = {install_action: $install_action, product_key: $product_key, max_user_deferrals: $max_user_deferrals} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Request the status of an OS update for a device
#
# POST /applemdms/{apple_mdm_id}/devices/{device_id}/osUpdateStatus
# operationId: applemdms_devicesOSUpdateStatus
export def "applemdms-devices-os-update-status devicesOSUpdateStatus" [
  apple_mdm_id: string
  device_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($apple_mdm_id)/devices/($device_id)/osUpdateStatus")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Check current feature trial usage for a specific feature
#
# GET /featureTrials/{feature_code}
# operationId: featureTrials_getFeatureTrials
export def "feature-trials get" [
  feature_code: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<startDate: string, endDate: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/featureTrials/($feature_code)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deactivate Password Manager
#
# DELETE /passwordmanager
# operationId: CompanyService_DeleteCompany
export def "passwordmanager DeleteCompany" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordmanager")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List App Versions
#
# GET /passwordmanager/appversions
# operationId: OverviewService_GetAppVersionsList
export def "passwordmanager-appversions GetAppVersionsList" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: int # format: int32
  --limit: int # format: int32
  --filter: list
]: nothing -> record<results: table<usersCount: int, version: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/appversions" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Items Metadata
#
# GET /passwordmanager/items
# operationId: ItemMetadataService_GetMetadata
export def "passwordmanager-items GetMetadata" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --search: string
  --skip: int # format: int32
  --limit: int # format: int32
  --qp-sort: string
  --filter: list
]: nothing -> record<items: table<count: int, type: int>, results: table<dateCreated: string, dateUpdated: string, field1: string, field2: string, folderUuid: string, id: string, itemUuid: string, nickname: string, type: int>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "search" $search "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Overview Stats
#
# GET /passwordmanager/overview
# operationId: OverviewService_MainOverview
export def "passwordmanager-overview MainOverview" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<compromisedPasswords: int, devices: table<count: int, id: int, name: string>, enrolledGroups: int, oldPasswords: int, passwordsCount: int, passwordsScore: float, pendingInvites: int, sharedFolders: int, totalUsers: int, weakPasswords: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordmanager/overview")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Shared Folders
#
# GET /passwordmanager/sharedfolders
# operationId: SharedFolderService_GetSharedFolders
export def "passwordmanager-sharedfolders GetSharedFolders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --search: string
  --skip: int # format: int32
  --limit: int # format: int32
  --qp-sort: string
]: nothing -> record<results: table<createdAt: string, itemsInFolder: int, name: string, passwordsCount: int, passwordsScore: float, scoreUpdatedAt: string, usersWithAccess: int, uuid: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "search" $search "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/sharedfolders" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Shared Folder
#
# POST /passwordmanager/sharedfolders
# operationId: SharedFolderService_CreateSharedFolder
export def "passwordmanager-sharedfolders CreateSharedFolder" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --name: string
  --description: string
]: nothing -> record<folderId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "description" $description "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/sharedfolders" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Shared Folder
#
# DELETE /passwordmanager/sharedfolders
# operationId: SharedFolderService_BatchDeleteSharedFolders
export def "passwordmanager-sharedfolders BatchDeleteSharedFolders" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --folderIds: list
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordmanager/sharedfolders")
  let body = {folderIds: $folderIds} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Shared Folder Details
#
# GET /passwordmanager/sharedfolders/{folderId}
# operationId: SharedFolderService_GetSharedFolder
export def "passwordmanager-sharedfolders GetSharedFolder" [
  folderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<compromisedPasswords: int, createdAt: string, description: string, itemsInFolder: int, maxLastSync: string, name: string, oldPasswords: int, passwordsCount: int, passwordsScore: float, reusedPasswords: int, scoreUpdatedAt: string, usersWithAccess: int, uuid: string, weakPasswords: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Shared Folder
#
# PUT /passwordmanager/sharedfolders/{folderId}
# operationId: SharedFolderService_UpdateSharedFolder
export def "passwordmanager-sharedfolders UpdateSharedFolder" [
  folderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --name: string
  --description: string
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "description" $description "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Shared Folder Groups
#
# GET /passwordmanager/sharedfolders/{folderId}/groups
# operationId: SharedFolderService_GetSharedFolderGroups
export def "passwordmanager-sharedfolders-groups GetSharedFolderGroups" [
  folderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --search: string
  --skip: int # format: int32
  --limit: int # format: int32
  --qp-sort: string
]: nothing -> record<results: table<accessLevelId: string, accessLevelName: string, description: string, externalId: string, id: string, name: string, usersCount: int>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "search" $search "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)/groups" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Shared Folder Group Access Level
#
# PUT /passwordmanager/sharedfolders/{folderId}/groups/{groupId}
# operationId: SharedFolderService_UpdateGroupPermission
export def "passwordmanager-sharedfolders-groups UpdateGroupPermission" [
  folderId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --accessLevel: string
]: nothing -> record<accessLevelId: string, accessLevelName: string, description: string, externalId: string, id: string, name: string, usersCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "accessLevel" $accessLevel "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)/groups/($groupId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Shared Folder Group
#
# DELETE /passwordmanager/sharedfolders/{folderId}/groups/{groupId}
# operationId: SharedFolderService_RevokeGroupAccess
export def "passwordmanager-sharedfolders-groups RevokeGroupAccess" [
  folderId: string
  groupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)/groups/($groupId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Shared Folder Users
#
# GET /passwordmanager/sharedfolders/{folderId}/users
# operationId: SharedFolderService_GetSharedFolderUsers
export def "passwordmanager-sharedfolders-users GetSharedFolderUsers" [
  folderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --search: string
  --skip: int # format: int32
  --limit: int # format: int32
  --qp-sort: string
]: nothing -> record<results: table<accessLevelId: string, accessLevelName: string, apps: list, email: string, employeeUuid: string, externalId: string, id: string, name: string, source: record, status: string, username: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "search" $search "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)/users" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Shared Folder User Access Level
#
# PUT /passwordmanager/sharedfolders/{folderId}/users/{userId}
# operationId: SharedFolderService_UpdateUserPermission
export def "passwordmanager-sharedfolders-users UpdateUserPermission" [
  folderId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --accessLevel: string
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "accessLevel" $accessLevel "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)/users/($userId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Shared Folder User
#
# DELETE /passwordmanager/sharedfolders/{folderId}/users/{userId}
# operationId: SharedFolderService_RevokeUserAccess
export def "passwordmanager-sharedfolders-users RevokeUserAccess" [
  folderId: string
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)/users/($userId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Shared Folder Access Levels
#
# GET /passwordmanager/sharedfolders/accessLevels
# operationId: SharedFolderService_GetRoles
export def "passwordmanager-sharedfolders-access-levels GetRoles" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<results: table<description: string, id: string, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordmanager/sharedfolders/accessLevels")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get users
#
# GET /passwordmanager/users
# operationId: UserService_GetAllUsers
export def "passwordmanager-users GetAllUsers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --search: string
  --skip: int # format: int32
  --limit: int # format: int32
  --qp-sort: string
  --filter: list
]: nothing -> record<results: table<apps: list, cloudBackupRestores: record, email: string, employeeUuid: string, externalId: string, groups: list, id: string, itemsCount: int, name: string, oldPasswords: int, passwordsCount: int, passwordsScore: float, scoreUpdatedAt: string, status: string, username: string, weakPasswords: int>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "search" $search "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/users" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get User details
#
# GET /passwordmanager/users/{userId}
# operationId: UserService_GetUserDetailsById
export def "passwordmanager-users GetUserDetailsById" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<apps: table<appVersion: string, osId: int>, cloudBackupRestores: record<approved: int, requested: int>, compromisedPasswords: int, createdAt: string, email: string, employeeUuid: string, externalId: string, groups: table<createdAt: string, displayName: string, externalId: string, id: string, name: string, updatedAt: string, uuid: string>, id: string, itemsCount: int, name: string, oldPasswords: int, passwordsCount: int, passwordsScore: float, reusedPasswords: int, scoreUpdatedAt: string, status: string, username: string, weakPasswords: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/users/($userId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get User Items
#
# GET /passwordmanager/users/{userId}/items
# operationId: ItemMetadataService_GetMetadataPerUser
export def "passwordmanager-users-items GetMetadataPerUser" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --search: string
  --skip: int # format: int32
  --limit: int # format: int32
  --qp-sort: string
  --filter: list
]: nothing -> record<items: table<count: int, type: int>, results: table<dateCreated: string, dateUpdated: string, field1: string, field2: string, folderUuid: string, id: string, itemUuid: string, nickname: string, type: int>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "search" $search "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/users/($userId)/items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get User Shared Folders
#
# GET /passwordmanager/users/{userId}/sharedFolders
# operationId: SharedFolderService_GetSharedFoldersPerUser
export def "passwordmanager-users-shared-folders GetSharedFoldersPerUser" [
  userId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --search: string
  --skip: int # format: int32
  --limit: int # format: int32
  --qp-sort: string
]: nothing -> record<results: table<accessLevelId: string, accessLevelName: string, createdAt: string, id: string, itemsInFolder: int, name: string, passwordsScore: float, scoreUpdatedAt: string, source: record, usersWithAccess: int>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "search" $search "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/users/($userId)/sharedFolders" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Resend User Invite
#
# POST /passwordmanager/users/reinvite
# operationId: UserService_ResendUserInvite
export def "passwordmanager-users-reinvite ResendUserInvite" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --userIds: list
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIds" $userIds "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/users/reinvite" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get device
#
# GET /google-emm/devices/{deviceId}
# operationId: Devices_GetDevice
export def "google-emm-devices GetDevice" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<deviceId: string, deviceInformation: record<deviceStateInfo: record<appliedDeviceState: string, commonCriteriaModeInfo: record, deviceSettings: record, deviceState: string, disabledReason: string, lastPolicySyncTime: string, lastStatusReportTime: string, policyCompliant: bool, securityPosture: record>, emmEnrollmentInfo: record<appliedPolicyName: string, appliedPolicyVersion: int, enrollmentTime: string, enrollmentType: string, managementMode: string, ownership: string, policyName: string>, hardwareInfo: record<brand: string, deviceBaseBandVersion: string, hardware: string, manufacturer: string, model: string, serialNumber: string>, memoryInfo: record<totalInternalStorage: int, totalRam: int>, networkInfo: record<imei: string, meid: string, telephonyInfo: list, wifiMacAddress: string>, softwareInfo: record<androidBuildNumber: string, androidBuildTime: string, androidDevicePolicyVersionCode: int, androidVersion: string, bootloaderVersion: string, deviceBuildSignature: string, deviceKernelVersion: string, primaryLanguageCode: string, securityPatchLevel: string, systemUpdateInfo: record>>, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lock device
#
# POST /google-emm/devices/{deviceId}/lock
# operationId: Devices_LockDevice
export def "google-emm-devices-lock LockDevice" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body: record
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)/lock")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Reset Password of a device
#
# POST /google-emm/devices/{deviceId}/resetpassword
# operationId: Devices_ResetPassword
export def "google-emm-devices-resetpassword ResetPassword" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --flags: list
  --newPassword: string # Not logging as it contains sensitive information.
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)/resetpassword")
  let body = {flags: $flags, newPassword: $newPassword} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Google Enterprises
#
# GET /google-emm/enterprises
# operationId: Enterprises_ListEnterprises
export def "google-emm-enterprises ListEnterprises" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
]: nothing -> record<count: int, enterprises: table<allowDeviceEnrollment: bool, contactEmail: string, createdAt: string, deviceGroupId: string, displayName: string, enterpriseType: string, googleAuthenticationRequired: string, name: string, objectId: string, organizationObjectId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/google-emm/enterprises" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a Google Enterprise
#
# POST /google-emm/enterprises
# operationId: Enterprises_CreateEnterprise
export def "google-emm-enterprises CreateEnterprise" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --enrollmentToken: string
  --signupUrlName: string
]: any -> record<allowDeviceEnrollment: bool, contactEmail: string, createdAt: string, deviceGroupId: string, displayName: string, enterpriseType: string, googleAuthenticationRequired: string, name: string, objectId: string, organizationObjectId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/google-emm/enterprises")
  let body = {enrollmentToken: $enrollmentToken, signupUrlName: $signupUrlName} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a Google Enterprise
#
# DELETE /google-emm/enterprises/{enterpriseId}
# operationId: Enterprises_DeleteEnterprise
export def "google-emm-enterprises DeleteEnterprise" [
  enterpriseId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a Google Enterprise
#
# PATCH /google-emm/enterprises/{enterpriseId}
# operationId: Enterprises_PatchEnterprise
export def "google-emm-enterprises PatchEnterprise" [
  enterpriseId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allowDeviceEnrollment: string@bool-completer
  --deviceGroupId: string # format: byte
]: any -> record<allowDeviceEnrollment: bool, contactEmail: string, createdAt: string, deviceGroupId: string, displayName: string, enterpriseType: string, googleAuthenticationRequired: string, name: string, objectId: string, organizationObjectId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseId)")
  let body = {allowDeviceEnrollment: $allowDeviceEnrollment, deviceGroupId: $deviceGroupId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a Signup URL to enroll Google enterprise
#
# POST /google-emm/signup-urls
# operationId: SignupURLs_Create
export def "google-emm-signup-urls Create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body: record
]: any -> record<name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/google-emm/signup-urls")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a web token to render Google Play iFrame
#
# POST /google-emm/web-tokens
# operationId: WebTokens_CreateWebToken
export def "google-emm-web-tokens CreateWebToken" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --enterpriseObjectId: string # format: byte
  --iframeFeature: string@iframeFeature-completer # default: SOFTWARE_MANAGEMENT
  --parentFrameUrl: string
]: any -> record<name: string, value: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/google-emm/web-tokens")
  let body = {enterpriseObjectId: $enterpriseObjectId, iframeFeature: $iframeFeature, parentFrameUrl: $parentFrameUrl} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get the policy JSON of a device
#
# GET /google-emm/devices/{deviceId}/policy_results
# operationId: Devices_GetDeviceAndroidPolicy
export def "google-emm-devices-policy-results GetDeviceAndroidPolicy" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<policy: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)/policy_results")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Test connection with Google
#
# GET /google-emm/enterprises/{enterpriseId}/connection-status
# operationId: Enterprises_GetConnectionStatus
export def "google-emm-enterprises-connection-status GetConnectionStatus" [
  enterpriseId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<enterpriseId: string, isConnected: bool, organizationObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseId)/connection-status")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Retrieve billing details for a Provider
#
# GET /providers/{provider_id}/billing/details
# operationId: Billing_GetDetails
export def "providers-billing-details GetDetails" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<contractType: string, assignedLicenses: int, products: table<name: string, capabilities: list, includedLicenses: int, purchasedLicenses: int>, hasContract: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/billing/details")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Erase the Android Device
#
# POST /google-emm/devices/{deviceId}/erase-device
# operationId: Devices_EraseDevice
export def "google-emm-devices-erase-device EraseDevice" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body: record
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)/erase-device")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Cloud Backup Keys
#
# GET /passwordmanager/backup/keys
# operationId: BackupKeyService_ListBackupKeys
export def "passwordmanager-backup-keys ListBackupKeys" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # format: int32
  --skip: int # format: int32
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<active: bool, companyId: string, createdAt: string, createdBy: string, id: string, name: string, publicKey: string, updatedAt: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/backup/keys" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Cloud Backup Keys
#
# POST /passwordmanager/backup/keys
# operationId: BackupKeyService_CreateBackupKey
export def "passwordmanager-backup-keys CreateBackupKey" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active: string@bool-completer
  --companyId: string
  --createdAt: string
  --createdBy: string
  --id: string
  --name: string
  --publicKey: string
  --updatedAt: string
]: any -> record<active: bool, companyId: string, createdAt: string, createdBy: string, id: string, name: string, publicKey: string, updatedAt: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordmanager/backup/keys")
  let body = {active: $active, companyId: $companyId, createdAt: $createdAt, createdBy: $createdBy, id: $id, name: $name, publicKey: $publicKey, updatedAt: $updatedAt} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Cloud Backup Key
#
# GET /passwordmanager/backup/keys/{UUID}
# operationId: BackupKeyService_GetBackupKey
export def "passwordmanager-backup-keys GetBackupKey" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<active: bool, companyId: string, createdAt: string, createdBy: string, id: string, name: string, publicKey: string, updatedAt: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/backup/keys/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Cloud Backup Key
#
# PUT /passwordmanager/backup/keys/{UUID}
# operationId: BackupKeyService_UpdateBackupKey
export def "passwordmanager-backup-keys UpdateBackupKey" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --active: string@bool-completer
]: nothing -> record<active: bool, companyId: string, createdAt: string, createdBy: string, id: string, name: string, publicKey: string, updatedAt: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "active" $active "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/backup/keys/($UUID)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete Cloud Backup Key
#
# DELETE /passwordmanager/backup/keys/{UUID}
# operationId: BackupKeyService_DeleteBackupKey
export def "passwordmanager-backup-keys DeleteBackupKey" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/backup/keys/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Cloud Backup Restore Requests
#
# GET /passwordmanager/backup/restores
# operationId: BackupRestoreService_ListBackupRestores
export def "passwordmanager-backup-restores ListBackupRestores" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # format: int32
  --skip: int # format: int32
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<backupUuid: string, createdAt: string, deviceUuid: string, id: string, status: string, updatedAt: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/backup/restores" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Cloud Backup Restore Request
#
# PUT /passwordmanager/backup/restores/{UUID}
# operationId: BackupRestoreService_UpdateBackupRestore
export def "passwordmanager-backup-restores UpdateBackupRestore" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --status: string@status-completer # default: STATUS_UNSPECIFIED
  --encryptionKey: string # Backup's encryption key encrypted using the Device's publicKey
]: nothing -> record<backupUuid: string, createdAt: string, deviceUuid: string, id: string, status: string, updatedAt: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "status" $status "scalar") (serialize-qp "encryptionKey" $encryptionKey "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/backup/restores/($UUID)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Cloud Backups
#
# GET /passwordmanager/backups
# operationId: BackupService_ListBackups
export def "passwordmanager-backups ListBackups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # format: int32
  --skip: int # format: int32
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<backupKeyUuid: string, createdAt: string, encryptionKey: string, formatVersion: string, id: string, keyHash: string, totalCount: int, updatedAt: string, userUuid: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/backups" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Cloud Backup
#
# GET /passwordmanager/backups/{UUID}
# operationId: BackupService_GetBackup
export def "passwordmanager-backups GetBackup" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<backupKeyUuid: string, createdAt: string, encryptionKey: string, formatVersion: string, id: string, keyHash: string, totalCount: int, updatedAt: string, userUuid: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/backups/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Devices
#
# GET /passwordmanager/devices
# operationId: DeviceService_ListDevices
export def "passwordmanager-devices ListDevices" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # format: int32
  --skip: int # format: int32
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<appVersion: string, createdAt: string, id: string, name: string, osName: string, publicKey: string, updatedAt: string, userUuid: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/devices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Device
#
# GET /passwordmanager/devices/{UUID}
# operationId: DeviceService_GetDevice
export def "passwordmanager-devices GetDevice" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<appVersion: string, createdAt: string, id: string, name: string, osName: string, publicKey: string, updatedAt: string, userUuid: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/devices/($UUID)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Shared Folders Policy
#
# GET /passwordmanager/policy/sharedfolders
# operationId: FoldersPolicyService_GetFoldersPolicy
export def "passwordmanager-policy-sharedfolders GetFoldersPolicy" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, shareFolders: string, updatedAt: string, updatedBy: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordmanager/policy/sharedfolders")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Shared Folders Policy
#
# PUT /passwordmanager/policy/sharedfolders/{UUID}
# operationId: FoldersPolicyService_UpdateFoldersPolicy
export def "passwordmanager-policy-sharedfolders UpdateFoldersPolicy" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --shareFolders: string@shareFolders-completer # default: UNSPECIFIED
]: nothing -> record<id: string, shareFolders: string, updatedAt: string, updatedBy: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "shareFolders" $shareFolders "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/policy/sharedfolders/($UUID)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Groups with Permission to Share Folders
#
# GET /passwordmanager/policy/sharedfolders/groups
# operationId: FoldersPolicyService_ListSelectedGroups
export def "passwordmanager-policy-sharedfolders-groups ListSelectedGroups" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # format: int32
  --skip: int # format: int32
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<id: string, name: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/policy/sharedfolders/groups" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Users with Permission to Share Folders
#
# GET /passwordmanager/policy/sharedfolders/users
# operationId: FoldersPolicyService_ListSelectedUsers
export def "passwordmanager-policy-sharedfolders-users ListSelectedUsers" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # format: int32
  --skip: int # format: int32
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<id: string, name: string, username: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordmanager/policy/sharedfolders/users" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List all domains configured for an Office 365 instance
#
# GET /office365s/{office365_id}/domains
# operationId: Domains_List
export def "office365s-domains List" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
]: nothing -> record<totalCount: int, domains: table<objectId: string, resourceObjectId: string, domain: string, default: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/office365s/($office365_id)/domains" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Add a domain to an Office 365 instance
#
# POST /office365s/{office365_id}/domains
# operationId: Domains_Insert
export def "office365s-domains Insert" [
  office365_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --domain: string
]: any -> record<domain: record<objectId: string, resourceObjectId: string, domain: string, default: bool>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)/domains")
  let body = {domain: $domain} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a domain from an Office 365 instance
#
# DELETE /office365s/{office365_id}/domains/{domain_id}
# operationId: Domains_Delete
export def "office365s-domains Delete" [
  office365_id: string
  domain_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<domain: record<objectId: string, resourceObjectId: string, domain: string, default: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/office365s/($office365_id)/domains/($domain_id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List all domains configured for the Google Workspace integration instance
#
# GET /gsuites/{gsuite_id}/domains
# operationId: Gapps_Domains_List
export def "gsuites-domains List" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
]: nothing -> record<domains: table<accountObjectId: string, default: bool, domain: string, objectId: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/domains" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Add a domain to a Google Workspace integration instance
#
# POST /gsuites/{gsuite_id}/domains
# operationId: Gapps_Domains_Insert
export def "gsuites-domains Insert" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --domain: string
]: nothing -> record<domain: record<accountObjectId: string, default: bool, domain: string, objectId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "domain" $domain "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/domains" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete a domain from a Google Workspace integration instance
#
# DELETE /gsuites/{gsuite_id}/domains/{domainId}
# operationId: Gapps_Domains_Delete
export def "gsuites-domains Delete" [
  gsuite_id: string
  domainId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<domain: record<accountObjectId: string, default: bool, domain: string, objectId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/domains/($domainId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Reboot device
#
# POST /google-emm/devices/{deviceId}/reboot
# operationId: Devices_RebootDevice
export def "google-emm-devices-reboot RebootDevice" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body: record
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)/reboot")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Retrieve contract for a Provider
#
# GET /providers/{provider_id}/billing/contract
# operationId: Billing_GetContract
export def "providers-billing-contract GetContract" [
  provider_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/providers/($provider_id)/billing/contract")
  let accept_val = "application/pdf"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get the Sign In with JumpCloud Settings
#
# GET /devices/settings/signinwithjumpcloud
# operationId: SystemsOrgSettings_GetSignInWithJumpCloudSettings
export def "devices-settings-signinwithjumpcloud GetSignInWithJumpCloudSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<organizationObjectId: string, settings: table<defaultPermission: string, enabled: bool, osFamily: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/devices/settings/signinwithjumpcloud" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Set the Sign In with JumpCloud Settings
#
# PUT /devices/settings/signinwithjumpcloud
# operationId: SystemsOrgSettings_SetSignInWithJumpCloudSettings
# --settings item shape: {defaultPermission?: "STANDARD"|"ADMIN", enabled?: bool, osFamily?: "UNKNOWN"|"WINDOWS"|"MACOS"}
export def "devices-settings-signinwithjumpcloud SetSignInWithJumpCloudSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --settings: list # item shape: {defaultPermission?: "STANDARD"|"ADMIN", enabled?: bool, osFamily?: "UNKNOWN"|"WINDOWS"|"MACOS"}
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/devices/settings/signinwithjumpcloud")
  let body = {organizationObjectId: $organizationObjectId, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List devices
#
# GET /google-emm/enterprises/{enterpriseObjectId}/devices
# operationId: Devices_ListDevices
export def "google-emm-enterprises-devices ListDevices" [
  enterpriseObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --filter: list
]: nothing -> record<count: int, devices: table<deviceId: string, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseObjectId)/devices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List enrollment tokens
#
# GET /google-emm/enterprises/{enterpriseObjectId}/enrollment-tokens
# operationId: EnrollmentTokens_ListEnrollmentTokens
export def "google-emm-enterprises-enrollment-tokens ListEnrollmentTokens" [
  enterpriseObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --filter: list
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
]: nothing -> record<results: table<createdBy: string, createdWhere: string, displayName: string, enrollmentLink: string, enrollmentType: string, expirationTime: string, id: string, metadata: string, name: string, oneTimeOnly: bool, qrCodeImage: string, value: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "multi") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseObjectId)/enrollment-tokens" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create an enrollment token for a given enterprise
#
# POST /google-emm/enterprises/{enterpriseObjectId}/enrollment-tokens
# operationId: EnrollmentTokens_CreateEnterprisesEnrollmentToken
# --provisioningExtras shape: {allowOfflineProvisioning?: bool, enableSystemApps?: bool, keepScreenOn?: bool, language?: string, skipEducationScreens?: bool, timeZone?: string, useMobileNetworkForProvisioning?: bool, wifiHiddenNetwork?: bool, wifiPassword?: string, wifiSecurityType?: "NONE"|"WPA"|"WEP", wifiSsid?: string}
export def "google-emm-enterprises-enrollment-tokens CreateEnterprisesEnrollmentToken" [
  enterpriseObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --allowPersonalUsage: string@allowPersonalUsage-completer # default: PERSONAL_USAGE_ALLOWED
  --createdWhere: string@createdWhere-completer # default: API
  --displayName: string
  --duration: string
  --enrollmentType: string@enrollmentType-completer # default: WORK_PROFILE
  --oneTimeOnly: string@bool-completer
  --provisioningExtras: record # shape: {allowOfflineProvisioning?: bool, enableSystemApps?: bool, keepScreenOn?: bool, language?: string, skipEducationScreens?: bool, timeZone?: string, useMobileNetworkForProvisioning?: bool, wifiHiddenNetwork?: bool, wifiPassword?: string, wifiSecurityType?: "NONE"|"WPA"|"WEP", wifiSsid?: string}
  --userObjectId: string # format: byte
  --zeroTouch: string@bool-completer
]: any -> record<createdBy: string, createdWhere: string, displayName: string, enrollmentLink: string, enrollmentType: string, expirationTime: string, id: string, metadata: string, name: string, oneTimeOnly: bool, qrCodeImage: string, value: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseObjectId)/enrollment-tokens")
  let body = {allowPersonalUsage: $allowPersonalUsage, createdWhere: $createdWhere, displayName: $displayName, duration: $duration, enrollmentType: $enrollmentType, oneTimeOnly: $oneTimeOnly, provisioningExtras: $provisioningExtras, userObjectId: $userObjectId, zeroTouch: $zeroTouch} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get an enrollment token by ID
#
# GET /google-emm/enterprises/{enterpriseId}/enrollment-tokens/{tokenId}
# operationId: EnrollmentTokens_GetEnrollmentToken
export def "google-emm-enterprises-enrollment-tokens GetEnrollmentToken" [
  enterpriseId: string
  tokenId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<createdBy: string, createdWhere: string, displayName: string, enrollmentLink: string, enrollmentType: string, expirationTime: string, id: string, metadata: string, name: string, oneTimeOnly: bool, qrCodeImage: string, value: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseId)/enrollment-tokens/($tokenId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Deletes an enrollment token for a given enterprise and token id
#
# DELETE /google-emm/enterprises/{enterpriseId}/enrollment-tokens/{tokenId}
# operationId: EnrollmentTokens_DeleteEnrollmentToken
export def "google-emm-enterprises-enrollment-tokens DeleteEnrollmentToken" [
  enterpriseId: string
  tokenId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<name: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseId)/enrollment-tokens/($tokenId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get the Aggregated Policy Stats for a System
#
# GET /systems/{systemObjectId}/aggregated-policy-stats
# operationId: AggregatedPolicyResults_Get
export def "systems-aggregated-policy-stats Get" [
  systemObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<failedPolicies: list<string>, pendingPolicies: list<string>, policyCountData: record<duplicate: int, failed: int, pending: int, success: int, total: int, unsupportedOs: int>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/systems/($systemObjectId)/aggregated-policy-stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint provides a zipped configuration file intended for use with Microsoft Configuration Designer to create a Provisioning Package (PPKG) for bulk MDM enrollment.
#
# POST /microsoft-mdm/configuration-files
# operationId: Enrollments_DownloadConfigFiles
export def "microsoft-mdm-configuration-files DownloadConfigFiles" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/microsoft-mdm/configuration-files")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/zip"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get the Default Password Sync Setting
#
# GET /devices/settings/defaultpasswordsync
# operationId: SystemsOrgSettings_GetDefaultPasswordSyncSettings
export def "devices-settings-defaultpasswordsync GetDefaultPasswordSyncSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<enabled: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/devices/settings/defaultpasswordsync" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Set the Default Password Sync Setting
#
# PUT /devices/settings/defaultpasswordsync
# operationId: SystemsOrgSettings_SetDefaultPasswordSyncSettings
export def "devices-settings-defaultpasswordsync SetDefaultPasswordSyncSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --enabled: string@bool-completer
  --organizationObjectId: string # format: byte
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/devices/settings/defaultpasswordsync")
  let body = {enabled: $enabled, organizationObjectId: $organizationObjectId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Push Verification status
#
# GET /pushendpoints/verifications/{verificationId}
# operationId: PushVerifications_Get
export def "pushendpoints-verifications Get" [
  verificationId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<confirmationCode: string, expiresAt: string, failureStatus: record<code: int, details: list<record>, message: string>, id: string, initiatedAt: string, pushEndpointId: string, status: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/pushendpoints/verifications/($verificationId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Send Push Verification message
#
# POST /users/{userId}/pushendpoints/{pushEndpointId}/verify
# operationId: PushVerifications_Start
export def "users-pushendpoints-verify Start" [
  userId: string
  pushEndpointId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --message: string
]: any -> record<confirmationCode: string, expiresAt: string, failureStatus: record<code: int, details: list<record>, message: string>, id: string, initiatedAt: string, pushEndpointId: string, status: string, userObjectId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/users/($userId)/pushendpoints/($pushEndpointId)/verify")
  let body = {message: $message} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the associations of a Routing Policy
#
# GET /identity-provider/policies/{idp_routing_policy_id}/associations
# operationId: graph_idpRoutingPolicyAssociationsList
export def "identity-provider-policies-associations idpRoutingPolicyAssociationsList" [
  idp_routing_policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --targets: list # Targets which a "idp_routing_policy" can be associated to.
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<attributes: record, from: record<attributes: record, id: string, type: string>, to: record<attributes: record, id: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "targets" $targets "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/identity-provider/policies/($idp_routing_policy_id)/associations" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Manage the associations of a routing policy
#
# POST /identity-provider/policies/{idp_routing_policy_id}/associations
# operationId: graph_idpRoutingPolicyAssociationsPost
export def "identity-provider-policies-associations idpRoutingPolicyAssociationsPost" [
  idp_routing_policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  id: string # The ObjectID of graph object being added or removed as an association.
  op: string@op-completer # How to modify the graph connection.
  --attributes: record # The graph attributes.
  type: string@type-completer # Targets which an "idp_routing_policy" can be associated to.
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/identity-provider/policies/($idp_routing_policy_id)/associations")
  let body = {id: $id, op: $op, attributes: $attributes, type: $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the User Groups bound to a Routing Policy
#
# GET /identity-provider/policies/{idp_routing_policy_id}/associations/usergroups
# operationId: graph_idpRoutingPolicyTraverseUserGroup
export def "identity-provider-policies-associations-usergroups idpRoutingPolicyTraverseUserGroup" [
  idp_routing_policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/identity-provider/policies/($idp_routing_policy_id)/associations/usergroups" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Users bound to a Routing Policy
#
# GET /identity-provider/policies/{idp_routing_policy_id}/associations/users
# operationId: graph_idpRoutingPolicyTraverseUser
export def "identity-provider-policies-associations-users idpRoutingPolicyTraverseUser" [
  idp_routing_policy_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> table<compiledAttributes: record, id: string, paths: list<list>, type: string, externalId: string, syncStatus: record<syncedAt: string, code: int, message: string, details: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/identity-provider/policies/($idp_routing_policy_id)/associations/users" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Access Request
#
# POST /accessrequests
# operationId: AccessRequestApi_CreateAccessRequest
export def "accessrequests CreateAccessRequest" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --additionalAttributes: record
  --applicationIntId: string
  --expiry: string # format: date-time
  --operationId: string
  --organizationObjectId: string # format: byte
  --remarks: string
  --requestorId: string
  --resourceId: string
  --resourceType: string
]: any -> record<id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/accessrequests")
  let body = {additionalAttributes: $additionalAttributes, applicationIntId: $applicationIntId, expiry: $expiry, operationId: $operationId, organizationObjectId: $organizationObjectId, remarks: $remarks, requestorId: $requestorId, resourceId: $resourceId, resourceType: $resourceType} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all Access Requests by Access Id
#
# GET /accessrequests/{accessId}
# operationId: AccessRequestApi_GetAccessRequest
export def "accessrequests GetAccessRequest" [
  accessId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<accessId: string, accessState: string, additionalAttributes: string, applicationIntId: string, companyId: string, createdBy: string, duration: int, expiry: string, id: int, jobId: string, metadata: string, onBehalfOfUserId: string, operationId: string, remarks: string, requestorId: string, resourceId: string, resourceType: string, tempGroupId: string, updatedBy: string, version: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/accessrequests/($accessId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update access request by id
#
# PUT /accessrequests/{accessId}
# operationId: AccessRequestApi_UpdateAccessRequest
export def "accessrequests UpdateAccessRequest" [
  accessId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --additionalAttributes: record
  --expiry: string # format: date-time
  --organizationObjectId: string # format: byte
  --remarks: string
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/accessrequests/($accessId)")
  let body = {additionalAttributes: $additionalAttributes, expiry: $expiry, organizationObjectId: $organizationObjectId, remarks: $remarks} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Revoke access request by id
#
# POST /accessrequests/{accessId}/revoke
# operationId: AccessRequestApi_RevokeAccessRequest
export def "accessrequests-revoke RevokeAccessRequest" [
  accessId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/accessrequests/($accessId)/revoke")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Scheduled Jobs
#
# GET /gsuites/{gsuite_id}/import/schedules
# operationId: Gapps_Scheduled_Import_List
export def "gsuites-import-schedules List" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<schedules: table<scheduledJobId: string, sendEmail: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/import/schedules")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a Scheduled Job
#
# POST /gsuites/{gsuite_id}/import/schedules
# operationId: Gapps_Scheduled_Import_Create
export def "gsuites-import-schedules Create" [
  gsuite_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --sendEmail: string@bool-completer # SendEmail is a flag to opt-in for an email with results of a scheduled import job
]: nothing -> record<scheduledJobId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sendEmail" $sendEmail "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/import/schedules" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete a Scheduled Job
#
# DELETE /gsuites/{gsuite_id}/import/schedules/{scheduledJobId}
# operationId: Gapps_Scheduled_Import_Delete
export def "gsuites-import-schedules Delete" [
  gsuite_id: string
  scheduledJobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/import/schedules/($scheduledJobId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a Scheduled Job
#
# PATCH /gsuites/{gsuite_id}/import/schedules/{scheduledJobId}
# operationId: Gapps_Scheduled_Import_Update
export def "gsuites-import-schedules Update" [
  gsuite_id: string
  scheduledJobId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --sendEmail: string@bool-completer # SendEmail is a flag to opt-in for an email with results of a scheduled import job
]: nothing -> record<scheduledJob: record<scheduledJobId: string, sendEmail: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sendEmail" $sendEmail "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/gsuites/($gsuite_id)/import/schedules/($scheduledJobId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Items stored in Object Storage
#
# GET /object-storage/items
# operationId: ObjectStorage_ListObjectStorageItemsPublic
export def "object-storage-items ListObjectStorageItemsPublic" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of resources to return at once. If the number of resources returned is less than `limit` there are no more resources to fetch. If the number of resources returned is equal to limit there may be more resources to fetch. Increment `skip` by `limit` to get the next set of resources. (format: int64)
]: nothing -> table<objectId: string, versions: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/object-storage/items" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get the current Object Storage status
#
# GET /object-storage/status
# operationId: ObjectStorage_GetStatus
export def "object-storage-status GetStatus" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<spaceTotal: int, spaceUsed: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/object-storage/status")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for listing the rules of health monitoring.
#
# GET /healthmonitoring/rules
# operationId: RulesService_ListRules
export def "healthmonitoring-rules ListRules" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.    **Filter structure**: `<field>:<operator>:<value>`.    **field** = Populate with a valid field from an endpoint response.    **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.    **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.    **Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of rules to return at once. If the number of rules returned is less than `limit` there are no more rules to fetch. If the number of rules returned is equal to limit there may be more rules to fetch. Increment `skip` by `limit` to get the next set of rules. (format: int64, default: 100)
  --labels: string@labels-completer #  - RULE_LABEL_UNSPECIFIED: No label specified; defaults to admin behaviour.  - RULE_LABEL_ADMIN: Rule was created through the Admin UI.  - RULE_LABEL_WORKFLOW: Rule was created by the Workflow service. (default: RULE_LABEL_UNSPECIFIED)
]: nothing -> record<count: int, rules: table<alertsEnabled: bool, category: string, conditions: list, conditionsRelation: string, createdAt: string, createdBy: string, createdByObjectId: string, description: string, detectPreExistingAnamolies: bool, eventFilters: list, eventType: string, excludeSuspendedUsers: bool, groupObjectIds: list, labels: string, lastUpdatedAt: string, lastUpdatedBy: string, name: string, notificationChannelObjectIds: list, notifyOnAcknowledgement: bool, notifyOnResolution: bool, notifyOnViolation: bool, notifyPrimaryUserOfDevice: bool, objectId: string, organizationObjectId: string, osFamily: string, pollConditions: list, postExpiryEnabled: bool, ruleType: string, scopeType: string, selectedScopeValues: list, severity: string, status: string, templateObjectId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar") (serialize-qp "labels" $labels "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/healthmonitoring/rules" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for creating the rules.
#
# POST /healthmonitoring/rules
# operationId: RulesService_CreateRule
# --rule shape: {alertsEnabled?: bool, category?: "RULE_CATEGORY_UNSPECIFIED"|"RULE_CATEGORY_DIRECTORY"|"RULE_CATEGORY_SYSTEM"|"RULE_CATEGORY_ADMIN"|"RULE_CATEGORY_ACCESS"|"RULE_CATEGORY_GENERAL"|"RULE_CATEGORY_CUSTOM", conditions?: list, conditionsRelation?: "LOGICAL_OPERATOR_UNSPECIFIED"|"LOGICAL_OPERATOR_AND"|"LOGICAL_OPERATOR_OR", description?: string, detectPreExistingAnamolies?: bool, eventFilters?: list, eventType?: string, excludeSuspendedUsers?: bool, groupObjectIds?: list, labels?: "RULE_LABEL_UNSPECIFIED"|"RULE_LABEL_ADMIN"|"RULE_LABEL_WORKFLOW", name?: string, notificationChannelObjectIds?: list, notifyOnAcknowledgement?: bool, notifyOnResolution?: bool, notifyOnViolation?: bool, notifyPrimaryUserOfDevice?: bool, osFamily?: "RULE_OS_FAMILY_UNSPECIFIED"|"RULE_OS_FAMILY_ALL"|"RULE_OS_FAMILY_WINDOWS"|"RULE_OS_FAMILY_DARWIN"|"RULE_OS_FAMILY_LINUX", pollConditions?: list, postExpiryEnabled?: bool, ruleType?: "RULE_TYPE_UNSPECIFIED"|"RULE_TYPE_TEMPLATE_BASED"|"RULE_TYPE_EVENT_FILTER_BASED"|"RULE_TYPE_POLL_BASED", scopeType?: string, selectedScopeValues?: list, severity?: "RULE_SEVERITY_UNSPECIFIED"|"RULE_SEVERITY_LOW"|"RULE_SEVERITY_MEDIUM"|"RULE_SEVERITY_HIGH"|"RULE_SEVERITY_CRITICAL", status?: "RULE_STATUS_UNSPECIFIED"|"RULE_STATUS_ENABLED"|"RULE_STATUS_DISABLED", templateObjectId?: string}
export def "healthmonitoring-rules CreateRule" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --rule: record # Rule represents a rule resource. — shape: {alertsEnabled?: bool, category?: "RULE_CATEGORY_UNSPECIFIED"|"RULE_CATEGORY_DIRECTORY"|"RULE_CATEGORY_SYSTEM"|"RULE_CATEGORY_ADMIN"|"RULE_CATEGORY_ACCESS"|"RULE_CATEGORY_GENERAL"|"RULE_CATEGORY_CUSTOM", conditions?: list, conditionsRelation?: "LOGICAL_OPERATOR_UNSPECIFIED"|"LOGICAL_OPERATOR_AND"|"LOGICAL_OPERATOR_OR", description?: string, detectPreExistingAnamolies?: bool, eventFilters?: list, eventType?: string, excludeSuspendedUsers?: bool, groupObjectIds?: list, labels?: "RULE_LABEL_UNSPECIFIED"|"RULE_LABEL_ADMIN"|"RULE_LABEL_WORKFLOW", name?: string, notificationChannelObjectIds?: list, notifyOnAcknowledgement?: bool, notifyOnResolution?: bool, notifyOnViolation?: bool, notifyPrimaryUserOfDevice?: bool, osFamily?: "RULE_OS_FAMILY_UNSPECIFIED"|"RULE_OS_FAMILY_ALL"|"RULE_OS_FAMILY_WINDOWS"|"RULE_OS_FAMILY_DARWIN"|"RULE_OS_FAMILY_LINUX", pollConditions?: list, postExpiryEnabled?: bool, ruleType?: "RULE_TYPE_UNSPECIFIED"|"RULE_TYPE_TEMPLATE_BASED"|"RULE_TYPE_EVENT_FILTER_BASED"|"RULE_TYPE_POLL_BASED", scopeType?: string, selectedScopeValues?: list, severity?: "RULE_SEVERITY_UNSPECIFIED"|"RULE_SEVERITY_LOW"|"RULE_SEVERITY_MEDIUM"|"RULE_SEVERITY_HIGH"|"RULE_SEVERITY_CRITICAL", status?: "RULE_STATUS_UNSPECIFIED"|"RULE_STATUS_ENABLED"|"RULE_STATUS_DISABLED", templateObjectId?: string}
]: any -> record<rule: record<alertsEnabled: bool, category: string, conditions: list<record>, conditionsRelation: string, createdAt: string, createdBy: string, createdByObjectId: string, description: string, detectPreExistingAnamolies: bool, eventFilters: list<record>, eventType: string, excludeSuspendedUsers: bool, groupObjectIds: list<string>, labels: string, lastUpdatedAt: string, lastUpdatedBy: string, name: string, notificationChannelObjectIds: list<string>, notifyOnAcknowledgement: bool, notifyOnResolution: bool, notifyOnViolation: bool, notifyPrimaryUserOfDevice: bool, objectId: string, organizationObjectId: string, osFamily: string, pollConditions: list<record>, postExpiryEnabled: bool, ruleType: string, scopeType: string, selectedScopeValues: list<string>, severity: string, status: string, templateObjectId: string>, warning: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/healthmonitoring/rules")
  let body = {rule: $rule} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# This endpoint is used for getting a health monitoring rule.
#
# GET /healthmonitoring/rules/{id}
# operationId: RulesService_GetRule
export def "healthmonitoring-rules GetRule" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<rule: record<alertsEnabled: bool, category: string, conditions: list<record>, conditionsRelation: string, createdAt: string, createdBy: string, createdByObjectId: string, description: string, detectPreExistingAnamolies: bool, eventFilters: list<record>, eventType: string, excludeSuspendedUsers: bool, groupObjectIds: list<string>, labels: string, lastUpdatedAt: string, lastUpdatedBy: string, name: string, notificationChannelObjectIds: list<string>, notifyOnAcknowledgement: bool, notifyOnResolution: bool, notifyOnViolation: bool, notifyPrimaryUserOfDevice: bool, objectId: string, organizationObjectId: string, osFamily: string, pollConditions: list<record>, postExpiryEnabled: bool, ruleType: string, scopeType: string, selectedScopeValues: list<string>, severity: string, status: string, templateObjectId: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/healthmonitoring/rules/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used to delete health monitoring rules.
#
# DELETE /healthmonitoring/rules/{id}
# operationId: RulesService_DeleteRule
export def "healthmonitoring-rules DeleteRule" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/healthmonitoring/rules/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for updating the rules.
#
# PATCH /healthmonitoring/rules/{id}
# operationId: RulesService_UpdateRule
# --rule shape: {alertsEnabled?: bool, category?: "RULE_CATEGORY_UNSPECIFIED"|"RULE_CATEGORY_DIRECTORY"|"RULE_CATEGORY_SYSTEM"|"RULE_CATEGORY_ADMIN"|"RULE_CATEGORY_ACCESS"|"RULE_CATEGORY_GENERAL"|"RULE_CATEGORY_CUSTOM", conditions?: list, conditionsRelation?: "LOGICAL_OPERATOR_UNSPECIFIED"|"LOGICAL_OPERATOR_AND"|"LOGICAL_OPERATOR_OR", description?: string, detectPreExistingAnamolies?: bool, eventFilters?: list, eventType?: string, excludeSuspendedUsers?: bool, groupObjectIds?: list, labels?: "RULE_LABEL_UNSPECIFIED"|"RULE_LABEL_ADMIN"|"RULE_LABEL_WORKFLOW", name?: string, notificationChannelObjectIds?: list, notifyOnAcknowledgement?: bool, notifyOnResolution?: bool, notifyOnViolation?: bool, notifyPrimaryUserOfDevice?: bool, osFamily?: "RULE_OS_FAMILY_UNSPECIFIED"|"RULE_OS_FAMILY_ALL"|"RULE_OS_FAMILY_WINDOWS"|"RULE_OS_FAMILY_DARWIN"|"RULE_OS_FAMILY_LINUX", pollConditions?: list, postExpiryEnabled?: bool, ruleType?: "RULE_TYPE_UNSPECIFIED"|"RULE_TYPE_TEMPLATE_BASED"|"RULE_TYPE_EVENT_FILTER_BASED"|"RULE_TYPE_POLL_BASED", scopeType?: string, selectedScopeValues?: list, severity?: "RULE_SEVERITY_UNSPECIFIED"|"RULE_SEVERITY_LOW"|"RULE_SEVERITY_MEDIUM"|"RULE_SEVERITY_HIGH"|"RULE_SEVERITY_CRITICAL", status?: "RULE_STATUS_UNSPECIFIED"|"RULE_STATUS_ENABLED"|"RULE_STATUS_DISABLED", templateObjectId?: string}
export def "healthmonitoring-rules UpdateRule" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --rule: record # Rule represents a rule resource. — shape: {alertsEnabled?: bool, category?: "RULE_CATEGORY_UNSPECIFIED"|"RULE_CATEGORY_DIRECTORY"|"RULE_CATEGORY_SYSTEM"|"RULE_CATEGORY_ADMIN"|"RULE_CATEGORY_ACCESS"|"RULE_CATEGORY_GENERAL"|"RULE_CATEGORY_CUSTOM", conditions?: list, conditionsRelation?: "LOGICAL_OPERATOR_UNSPECIFIED"|"LOGICAL_OPERATOR_AND"|"LOGICAL_OPERATOR_OR", description?: string, detectPreExistingAnamolies?: bool, eventFilters?: list, eventType?: string, excludeSuspendedUsers?: bool, groupObjectIds?: list, labels?: "RULE_LABEL_UNSPECIFIED"|"RULE_LABEL_ADMIN"|"RULE_LABEL_WORKFLOW", name?: string, notificationChannelObjectIds?: list, notifyOnAcknowledgement?: bool, notifyOnResolution?: bool, notifyOnViolation?: bool, notifyPrimaryUserOfDevice?: bool, osFamily?: "RULE_OS_FAMILY_UNSPECIFIED"|"RULE_OS_FAMILY_ALL"|"RULE_OS_FAMILY_WINDOWS"|"RULE_OS_FAMILY_DARWIN"|"RULE_OS_FAMILY_LINUX", pollConditions?: list, postExpiryEnabled?: bool, ruleType?: "RULE_TYPE_UNSPECIFIED"|"RULE_TYPE_TEMPLATE_BASED"|"RULE_TYPE_EVENT_FILTER_BASED"|"RULE_TYPE_POLL_BASED", scopeType?: string, selectedScopeValues?: list, severity?: "RULE_SEVERITY_UNSPECIFIED"|"RULE_SEVERITY_LOW"|"RULE_SEVERITY_MEDIUM"|"RULE_SEVERITY_HIGH"|"RULE_SEVERITY_CRITICAL", status?: "RULE_STATUS_UNSPECIFIED"|"RULE_STATUS_ENABLED"|"RULE_STATUS_DISABLED", templateObjectId?: string}
]: any -> record<rule: record<alertsEnabled: bool, category: string, conditions: list<record>, conditionsRelation: string, createdAt: string, createdBy: string, createdByObjectId: string, description: string, detectPreExistingAnamolies: bool, eventFilters: list<record>, eventType: string, excludeSuspendedUsers: bool, groupObjectIds: list<string>, labels: string, lastUpdatedAt: string, lastUpdatedBy: string, name: string, notificationChannelObjectIds: list<string>, notifyOnAcknowledgement: bool, notifyOnResolution: bool, notifyOnViolation: bool, notifyPrimaryUserOfDevice: bool, objectId: string, organizationObjectId: string, osFamily: string, pollConditions: list<record>, postExpiryEnabled: bool, ruleType: string, scopeType: string, selectedScopeValues: list<string>, severity: string, status: string, templateObjectId: string>, warning: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/healthmonitoring/rules/($id)")
  let body = {rule: $rule} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# This endpoint is used for listing the rules of health monitoring.
#
# GET /healthmonitoring/ruletemplates
# operationId: RuleTemplatesService_ListRuleTemplates
export def "healthmonitoring-ruletemplates ListRuleTemplates" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of rule templates to return at once. If the number of rule templates returned is less than `limit` there are no more rule templates to fetch. If the number of rule templates returned is equal to limit there may be more rule templates to fetch. Increment `skip` by `limit` to get the next set of rule templates. (format: int64, default: 100)
]: nothing -> record<count: int, templates: table<category: string, configurations: list, createdAt: string, description: string, frequency: int, name: string, objectId: string, osFamily: string, outputSchema: record, siRequired: bool, status: string, systemBindingRequired: bool, type: string, unrestricted: bool, updatedAt: string, version: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/healthmonitoring/ruletemplates" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for getting a health monitoring rule template.
#
# GET /healthmonitoring/ruletemplates/{id}
# operationId: RuleTemplatesService_GetRuleTemplate
export def "healthmonitoring-ruletemplates GetRuleTemplate" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<template: record<category: string, configurations: list<record>, createdAt: string, description: string, frequency: int, name: string, objectId: string, osFamily: string, outputSchema: record, siRequired: bool, status: string, systemBindingRequired: bool, type: string, unrestricted: bool, updatedAt: string, version: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/healthmonitoring/ruletemplates/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint provides details of all alerts.
#
# GET /alerts
# operationId: AlertsService_ListAlerts
export def "alerts ListAlerts" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of alerts to return at once. If the number of alerts returned is less than `limit` there are no more alerts to fetch. If the number of alerts returned is equal to limit there may be more alerts to fetch. Increment `skip` by `limit` to get the next set of alerts. (format: int64, default: 100)
]: nothing -> record<alerts: table<acknowledgedAt: string, category: string, createdAt: string, description: string, firstOccurredAt: string, frequency: int, lastOccurredAt: string, lastOccurrenceContext: record, objectId: string, occurrencesCount: int, organizationObjectId: string, referenceId: string, remark: string, resolvedAt: string, severity: string, sourceId: string, sourceName: string, sourceType: string, status: string, title: string, updatedAt: string>, count: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/alerts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint provides details of an alert.
#
# GET /alerts/{id}
# operationId: AlertsService_GetAlert
export def "alerts GetAlert" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<alert: record<acknowledgedAt: string, category: string, createdAt: string, description: string, firstOccurredAt: string, frequency: int, lastOccurredAt: string, lastOccurrenceContext: record, objectId: string, occurrencesCount: int, organizationObjectId: string, referenceId: string, remark: string, resolvedAt: string, severity: string, sourceId: string, sourceName: string, sourceType: string, status: string, title: string, updatedAt: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/alerts/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used to delete an alert.
#
# DELETE /alerts/{id}
# operationId: AlertsService_DeleteAlert
export def "alerts DeleteAlert" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/alerts/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used to list one or more occurrences for an Alert.
#
# GET /alerts/{id}/occurrences
# operationId: AlertOccurrencesService_ListAlertOccurrences
export def "alerts-occurrences ListAlertOccurrences" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of alert occurrences to return at once. If the number of alert occurrences returned is less than `limit` there are no more alert occurrences to fetch. If the number of alert occurrences returned is equal to limit there may be more alert occurrences to fetch. Increment `skip` by `limit` to get the next set of alert occurrences. (format: int64, default: 100)
]: nothing -> record<alertOccurrences: table<alertObjectId: string, context: record, createdAt: string, occurredAt: string, updatedAt: string>, count: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/alerts/($id)/occurrences" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for updating the status of an alert.
#
# POST /alerts/{id}/status
# operationId: AlertsService_UpdateAlertStatus
export def "alerts-status UpdateAlertStatus" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --remark: string # A remark associated with status update.
  --status: string@status-completer-1 # AlertStatus specifies status of an alert. (default: ALERT_STATUS_UNSPECIFIED)
]: any -> record<alert: record<acknowledgedAt: string, category: string, createdAt: string, description: string, firstOccurredAt: string, frequency: int, lastOccurredAt: string, lastOccurrenceContext: record, objectId: string, occurrencesCount: int, organizationObjectId: string, referenceId: string, remark: string, resolvedAt: string, severity: string, sourceId: string, sourceName: string, sourceType: string, status: string, title: string, updatedAt: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/alerts/($id)/status")
  let body = {remark: $remark, status: $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# This endpoint provides alert statistics based on status and severity
#
# GET /alerts-stats
# operationId: AlertsService_GetAlertStats
export def "alerts-stats GetAlertStats" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --groupBy: list # Provide a list of group by strings as input. Possible values are 'GROUP_BY_STATUS', 'GROUP_BY_SEVERITY'.For combined grouping with 'GROUP_BY_STATUS' and 'GROUP_BY_SEVERITY' , the input should look like this: ['GROUP_BY_STATUS:GROUP_BY_SEVERITY'].Example : ["GROUP_BY_STATUS","GROUP_BY_SEVERITY","GROUP_BY_STATUS:GROUP_BY_SEVERITY"]
]: nothing -> record<context: record, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "groupBy" $groupBy "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/alerts-stats" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Organization Policies
#
# GET /passwordmanager/company/policies
# operationId: CompanyService_GetPolicies
export def "passwordmanager-company-policies GetPolicies" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<disableExport: bool, id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordmanager/company/policies")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Organization Policies
#
# PUT /passwordmanager/company/policies/{UUID}
# operationId: CompanyService_UpdatePolicies
export def "passwordmanager-company-policies UpdatePolicies" [
  UUID: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --disableExport: string@bool-completer
]: nothing -> record<disableExport: bool, id: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "disableExport" $disableExport "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordmanager/company/policies/($UUID)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Lost Mode
#
# POST /applemdms/{deviceManagerObjectId}/devices/{deviceObjectId}/lostmode
# operationId: Devices_LostMode
# --enableLostMode shape: {footnote?: string, message?: string, phoneNumber?: string}
export def "applemdms-devices-lostmode LostMode" [
  deviceManagerObjectId: string
  deviceObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --disableLostMode: record
  --enableLostMode: record # shape: {footnote?: string, message?: string, phoneNumber?: string}
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($deviceManagerObjectId)/devices/($deviceObjectId)/lostmode")
  let body = {disableLostMode: $disableLostMode, enableLostMode: $enableLostMode} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get the SSAO Devices Settings for an Organization
#
# GET /devices/settings/ssao
# operationId: SystemsOrgSettings_GetSsaoDevicesSettings
export def "devices-settings-ssao GetSsaoDevicesSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<organizationObjectId: string, settings: table<defaultPermission: string, enabled: bool, osFamily: string, passwordSyncEnabled: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/devices/settings/ssao" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Set the SSAO Devices Settings for an Organization
#
# PUT /devices/settings/ssao
# operationId: SystemsOrgSettings_SetSsaoDevicesSettings
# --settings item shape: {defaultPermission?: "STANDARD"|"ADMIN", enabled?: bool, osFamily?: "UNKNOWN"|"WINDOWS"|"MACOS", passwordSyncEnabled?: bool}
export def "devices-settings-ssao SetSsaoDevicesSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --settings: list # item shape: {defaultPermission?: "STANDARD"|"ADMIN", enabled?: bool, osFamily?: "UNKNOWN"|"WINDOWS"|"MACOS", passwordSyncEnabled?: bool}
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/devices/settings/ssao")
  let body = {organizationObjectId: $organizationObjectId, settings: $settings} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the App Catalog
#
# GET /software/catalog/apps
# operationId: AppCatalog_ListAppCatalogItems
export def "software-catalog-apps ListAppCatalogItems" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of resources to return at once. If the number of resources returned is less than `limit` there are no more resources to fetch. If the number of resources returned is equal to limit there may be more resources to fetch. Increment `skip` by `limit` to get the next set of resources. (format: int64)
]: nothing -> table<iconSha256: string, iconUrl: string, installableItems: list<record>, name: string, objectId: string, version: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/software/catalog/apps" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get an App Catalog Item
#
# GET /software/catalog/apps/{objectId}
# operationId: AppCatalog_GetAppCatalogItem
export def "software-catalog-apps GetAppCatalogItem" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<iconSha256: string, iconUrl: string, installableItems: table<architectures: list, installerType: string, objectId: string, operatingSystem: string, softwareApp: record, version: string>, name: string, objectId: string, version: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/software/catalog/apps/($objectId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Start lost mode
#
# POST /google-emm/devices/{deviceId}/lostmode/start
# operationId: Devices_StartLostMode
export def "google-emm-devices-lostmode-start StartLostMode" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --emailAddress: string
  --lostMessage: string
  --organizationName: string
  --phoneNumber: string
  --streetAddress: string
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)/lostmode/start")
  let body = {emailAddress: $emailAddress, lostMessage: $lostMessage, organizationName: $organizationName, phoneNumber: $phoneNumber, streetAddress: $streetAddress} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Stop lost mode
#
# POST /google-emm/devices/{deviceId}/lostmode/stop
# operationId: Devices_StopLostMode
export def "google-emm-devices-lostmode-stop StopLostMode" [
  deviceId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body: record
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/devices/($deviceId)/lostmode/stop")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# ADUE Configuration
#
# GET /applemdms/{organizationObjectId}/account-driven-service-discovery
# operationId: EnrollmentProfiles_GetAdueRedirect
export def "applemdms-account-driven-service-discovery GetAdueRedirect" [
  organizationObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<redirectUrl: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($organizationObjectId)/account-driven-service-discovery")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# ADUE Configuration
#
# GET /applemdms/{organizationObjectId}/account-driven-service-discovery/config
# operationId: EnrollmentProfiles_GetAdueConfig
export def "applemdms-account-driven-service-discovery-config GetAdueConfig" [
  organizationObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<jsonConfig: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($organizationObjectId)/account-driven-service-discovery/config")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List the Items stored in SavedViews
#
# GET /systems/views
# operationId: SavedViews_List
export def "systems-views List" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --userObjectId: string # format: byte
]: nothing -> table<columns: list<string>, filters: list<record>, isDefault: bool, name: string, objectId: string, shared: bool, sortConfig: string, userObjectId: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar") (serialize-qp "userObjectId" $userObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/systems/views" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a saved view for an organization
#
# POST /systems/views
# operationId: SavedViews_Create
# --filters item shape: {field?: string, values?: list}
export def "systems-views Create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --columns: list
  --filters: list # item shape: {field?: string, values?: list}
  --isDefault: string@bool-completer
  --name: string
  --objectId: string # format: byte
  --shared: string@bool-completer
  --sortConfig: string
  --userObjectId: string # format: byte
]: any -> record<columns: list<string>, filters: table<field: string, values: list>, isDefault: bool, name: string, objectId: string, shared: bool, sortConfig: string, userObjectId: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/systems/views")
  let body = {columns: $columns, filters: $filters, isDefault: $isDefault, name: $name, objectId: $objectId, shared: $shared, sortConfig: $sortConfig, userObjectId: $userObjectId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Custom Reports
#
# GET /reports/custom
# operationId: Reports_ListReportViews
export def "reports-custom ListReportViews" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --qp-fields: list # The fields on the items listed to return.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<reportViews: table<configuration: record, createdAt: string, createdBy: string, description: string, displayName: string, id: string, resources: list, searchRequest: record, updatedAt: string, updatedBy: string, updatedBySource: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/reports/custom" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a Custom Report
#
# POST /reports/custom
# operationId: Reports_CreateReportView
# --reportView shape: {configuration?: record, description?: string, displayName?: string, id?: string, resources?: list, searchRequest?: record, updatedBySource?: "unspecified"|"api"|"ui"|"ai"}
export def "reports-custom CreateReportView" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --reportView: record # shape: {configuration?: record, description?: string, displayName?: string, id?: string, resources?: list, searchRequest?: record, updatedBySource?: "unspecified"|"api"|"ui"|"ai"}
]: any -> record<configuration: record<columnSettings: record<fixed: list>>, createdAt: string, createdBy: string, description: string, displayName: string, id: string, resources: list<string>, searchRequest: record<aggregations: list<record>, cacheToken: string, fields: record<exclude: list, include: list>, filters: list<record>, limit: int, metadata: record<context: list, searchPurpose: record, source: string>, pagination: record<offset: int, pageSize: int>, requestCache: bool, sort: list<record>>, updatedAt: string, updatedBy: string, updatedBySource: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/reports/custom")
  let body = {reportView: $reportView} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a Custom Report
#
# GET /reports/custom/{objectId}
# operationId: Reports_GetReportView
export def "reports-custom GetReportView" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<reportView: record<configuration: record<columnSettings: record>, createdAt: string, createdBy: string, description: string, displayName: string, id: string, resources: list<string>, searchRequest: record<aggregations: list, cacheToken: string, fields: record, filters: list, limit: int, metadata: record, pagination: record, requestCache: bool, sort: list>, updatedAt: string, updatedBy: string, updatedBySource: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/custom/($objectId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Custom Report
#
# PUT /reports/custom/{objectId}
# operationId: Reports_UpdateReportView
# --reportView shape: {configuration?: record, description?: string, displayName?: string, id?: string, resources?: list, searchRequest?: record, updatedBySource?: "unspecified"|"api"|"ui"|"ai"}
export def "reports-custom UpdateReportView" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --reportView: record # shape: {configuration?: record, description?: string, displayName?: string, id?: string, resources?: list, searchRequest?: record, updatedBySource?: "unspecified"|"api"|"ui"|"ai"}
]: any -> record<configuration: record<columnSettings: record<fixed: list>>, createdAt: string, createdBy: string, description: string, displayName: string, id: string, resources: list<string>, searchRequest: record<aggregations: list<record>, cacheToken: string, fields: record<exclude: list, include: list>, filters: list<record>, limit: int, metadata: record<context: list, searchPurpose: record, source: string>, pagination: record<offset: int, pageSize: int>, requestCache: bool, sort: list<record>>, updatedAt: string, updatedBy: string, updatedBySource: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/custom/($objectId)")
  let body = {reportView: $reportView} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a Custom report
#
# DELETE /reports/custom/{objectId}
# operationId: Reports_DeleteReportView
export def "reports-custom DeleteReportView" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/custom/($objectId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Jumpcloud Reports
#
# GET /reports/jumpcloud
# operationId: Reports_ListReportTemplates
export def "reports-jumpcloud ListReportTemplates" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --qp-fields: list # The fields on the items listed to return.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<reportTemplates: table<configuration: record, description: string, displayName: string, id: string, resources: list, searchRequest: record>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/reports/jumpcloud" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a Jumpcloud Report
#
# GET /reports/jumpcloud/{objectId}
# operationId: Reports_GetReportTemplate
export def "reports-jumpcloud GetReportTemplate" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<reportTemplate: record<configuration: record<columnSettings: record>, description: string, displayName: string, id: string, resources: list<string>, searchRequest: record<aggregations: list, cacheToken: string, fields: record, filters: list, limit: int, metadata: record, pagination: record, requestCache: bool, sort: list>>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/jumpcloud/($objectId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is getting rules stats.
#
# GET /healthmonitoring/rules-stats
# operationId: RulesService_GetRulesStats
export def "healthmonitoring-rules-stats GetRulesStats" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<systemInsightsRequiredCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/healthmonitoring/rules-stats")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Export a report
#
# POST /reports/export
# operationId: Reports_ExportReport
# --searchRequest shape: {aggregations?: list, cacheToken?: string, fields?: record, filters?: list, limit?: int, metadata?: any, pagination?: any, requestCache?: bool, sort?: list}
export def "reports-export ExportReport" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --exportType: string@exportType-completer # default: csv
  --notifyByEmail: string@bool-completer
  --reportName: string
  --searchRequest: record # shape: {aggregations?: list, cacheToken?: string, fields?: record, filters?: list, limit?: int, metadata?: any, pagination?: any, requestCache?: bool, sort?: list}
]: any -> record<downloadUrl: string, status: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/reports/export")
  let body = {exportType: $exportType, notifyByEmail: $notifyByEmail, reportName: $reportName, searchRequest: $searchRequest} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# This endpoint is used for updating the status of a rule.
#
# PATCH /healthmonitoring/rules/{id}/status
# operationId: RulesService_UpdateRuleStatus
export def "healthmonitoring-rules-status UpdateRuleStatus" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --remark: string # A remark associated with status update.
  --status: string@status-completer-1 # AlertStatus specifies status of an alert. (default: ALERT_STATUS_UNSPECIFIED)
]: any -> record<rule: record<alertsEnabled: bool, category: string, conditions: list<record>, conditionsRelation: string, createdAt: string, createdBy: string, createdByObjectId: string, description: string, detectPreExistingAnamolies: bool, eventFilters: list<record>, eventType: string, excludeSuspendedUsers: bool, groupObjectIds: list<string>, labels: string, lastUpdatedAt: string, lastUpdatedBy: string, name: string, notificationChannelObjectIds: list<string>, notifyOnAcknowledgement: bool, notifyOnResolution: bool, notifyOnViolation: bool, notifyPrimaryUserOfDevice: bool, objectId: string, organizationObjectId: string, osFamily: string, pollConditions: list<record>, postExpiryEnabled: bool, ruleType: string, scopeType: string, selectedScopeValues: list<string>, severity: string, status: string, templateObjectId: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/healthmonitoring/rules/($id)/status")
  let body = {remark: $remark, status: $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# ABM Devices List
#
# GET /applemdms/{deviceManagerObjectId}/abm_devices
# operationId: DeviceManagers_ListABMDevices
export def "applemdms-abm-devices ListABMDevices" [
  deviceManagerObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --skip: string # The offset into the records to return. (format: int64)
  --limit: string # The number of resources to return at once. If the number of resources returned is less than `limit` there are no more resources to fetch. If the number of resources returned is equal to limit there may be more resources to fetch. Increment `skip` by `limit` to get the next set of resources. (format: int64)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "skip" $skip "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/applemdms/($deviceManagerObjectId)/abm_devices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for listing the notification channels.
#
# GET /notifications/channels
# operationId: NotificationsChannelsService_ListChannels
export def "notifications-channels ListChannels" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.    **Filter structure**: `<field>:<operator>:<value>`.    **field** = Populate with a valid field from an endpoint response.    **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.    **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.    **Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of channels to return at once. If the number of channels returned is less than `limit` there are no more channels to fetch. If the number of channels returned is equal to limit there may be more channels to fetch. Increment `skip` by `limit` to get the next set of channels. (format: int64, default: 100)
]: nothing -> record<channels: table<config: record, createdAt: string, createdBy: string, description: string, enabled: bool, name: string, objectId: string, organizationObjectId: string, type: string, updatedAt: string, updatedBy: string>, count: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notifications/channels" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for creating the channels.
#
# POST /notifications/channels
# operationId: NotificationsChannelsService_CreateChannel
# --channel shape: {config?: record, description?: string, enabled?: bool, name?: string, type?: "CHANNEL_TYPE_UNSPECIFIED"|"CHANNEL_TYPE_EMAIL"|"CHANNEL_TYPE_WEBHOOK"|"CHANNEL_TYPE_SLACK"}
export def "notifications-channels CreateChannel" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --channel: record # Channel represents the notification channel. — shape: {config?: record, description?: string, enabled?: bool, name?: string, type?: "CHANNEL_TYPE_UNSPECIFIED"|"CHANNEL_TYPE_EMAIL"|"CHANNEL_TYPE_WEBHOOK"|"CHANNEL_TYPE_SLACK"}
]: any -> record<channel: record<config: record<email: record, slack: record, webhook: record>, createdAt: string, createdBy: string, description: string, enabled: bool, name: string, objectId: string, organizationObjectId: string, type: string, updatedAt: string, updatedBy: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/notifications/channels")
  let body = {channel: $channel} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# This endpoint is used for getting a notification channel.
#
# GET /notifications/channels/{id}
# operationId: NotificationsChannelsService_GetChannel
export def "notifications-channels GetChannel" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<channel: record<config: record<email: record, slack: record, webhook: record>, createdAt: string, createdBy: string, description: string, enabled: bool, name: string, objectId: string, organizationObjectId: string, type: string, updatedAt: string, updatedBy: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notifications/channels/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used to delete notification channels.
#
# DELETE /notifications/channels/{id}
# operationId: NotificationsChannelsService_DeleteChannel
export def "notifications-channels DeleteChannel" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notifications/channels/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for updating the channels.
#
# PATCH /notifications/channels/{id}
# operationId: NotificationsChannelsService_UpdateChannel
# --channel shape: {config?: record, description?: string, enabled?: bool, name?: string, type?: "CHANNEL_TYPE_UNSPECIFIED"|"CHANNEL_TYPE_EMAIL"|"CHANNEL_TYPE_WEBHOOK"|"CHANNEL_TYPE_SLACK"}
export def "notifications-channels UpdateChannel" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --channel: record # Channel represents the notification channel. — shape: {config?: record, description?: string, enabled?: bool, name?: string, type?: "CHANNEL_TYPE_UNSPECIFIED"|"CHANNEL_TYPE_EMAIL"|"CHANNEL_TYPE_WEBHOOK"|"CHANNEL_TYPE_SLACK"}
]: any -> record<channel: record<config: record<email: record, slack: record, webhook: record>, createdAt: string, createdBy: string, description: string, enabled: bool, name: string, objectId: string, organizationObjectId: string, type: string, updatedAt: string, updatedBy: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notifications/channels/($id)")
  let body = {channel: $channel} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Manage Shared Folder Permission
#
# POST /passwordmanager/sharedfolders/{folderId}/manage
# operationId: SharedFolderService_ManageSharedFolderPermission
# --userGroups shape: {addition?: list, deletion?: list, update?: list}
# --users shape: {addition?: list, deletion?: list, update?: list}
export def "passwordmanager-sharedfolders-manage ManageSharedFolderPermission" [
  folderId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --userGroups: record # shape: {addition?: list, deletion?: list, update?: list}
  --users: record # shape: {addition?: list, deletion?: list, update?: list}
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordmanager/sharedfolders/($folderId)/manage")
  let body = {userGroups: $userGroups, users: $users} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List the Items stored in SavedViews
#
# GET /saved-views
# operationId: SavedViewsService_List
export def "saved-views List" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/saved-views?filter=source:eq:users`
]: nothing -> record<totalCount: int, views: table<adminId: string, columns: list, configuration: record, id: string, isDefault: bool, name: string, shared: bool, source: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "sort" $qp_sort "scalar") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/saved-views" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Creates a saved view for an organization belonging to a specific source
#
# POST /saved-views
# operationId: SavedViewsService_Create
# --configuration shape: {filters?: list, sort?: string}
export def "saved-views Create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --columns: list
  --configuration: record # shape: {filters?: list, sort?: string}
  --id: string # format: byte
  --isDefault: string@bool-completer
  --name: string
  --shared: string@bool-completer
  --body-source: string
]: any -> record<adminId: string, columns: list<string>, configuration: record<filters: list<record>, sort: string>, id: string, isDefault: bool, name: string, shared: bool, source: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/saved-views")
  let body = {columns: $columns, configuration: $configuration, id: $id, isDefault: $isDefault, name: $name, shared: $shared, source: $body_source} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Update Saved View
#
# PUT /saved-views/{id}
# operationId: SavedViewsService_Update
# --savedView shape: {columns?: list, configuration?: record, id?: string, isDefault?: bool, name?: string, shared?: bool, source?: string}
export def "saved-views Update" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --savedView: record # shape: {columns?: list, configuration?: record, id?: string, isDefault?: bool, name?: string, shared?: bool, source?: string}
]: any -> record<adminId: string, columns: list<string>, configuration: record<filters: list<record>, sort: string>, id: string, isDefault: bool, name: string, shared: bool, source: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saved-views/($id)")
  let body = {savedView: $savedView} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete Saved View
#
# DELETE /saved-views/{id}
# operationId: SavedViewsService_Delete
export def "saved-views Delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saved-views/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Password Policy
#
# POST /passwordpolicies
# operationId: Policy_Insert
# --policy shape: {allowUnenrolledMFAPasswordReset?: bool, allowUsernameSubstring?: bool, daysAfterExpirationToSelfRecover?: int, daysBeforeExpirationToForceReset?: int, default?: bool, description?: string, disallowCommonlyUsedPasswords?: bool, disallowSequentialOrRepetitiveChars?: bool, displayComplexityOnResetScreen?: bool, effectiveDate?: string, enableDaysAfterExpirationToSelfRecover?: bool, enableDaysBeforeExpirationToForceReset?: bool, enableLockoutTimeInSeconds?: bool, enableMaxHistory?: bool, enableMaxLoginAttempts?: bool, enableMinChangePeriodInDays?: bool, enableMinLength?: bool, enablePasswordExpirationInDays?: bool, enableRecoveryEmail?: bool, enableResetLockoutCounter?: bool, lockoutTimeInSeconds?: int, maxHistory?: int, maxLoginAttempts?: int, minChangePeriodInDays?: int, minLength?: int, name?: string, needsLowercase?: bool, needsNumeric?: bool, needsSymbolic?: bool, needsUppercase?: bool, passwordExpirationInDays?: int, precedence?: int, resetLockoutCounterMinutes?: int}
export def "passwordpolicies Insert" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --groupIds: list
  --organizationObjectId: string # format: byte
  --policy: record # shape: {allowUnenrolledMFAPasswordReset?: bool, allowUsernameSubstring?: bool, daysAfterExpirationToSelfRecover?: int, daysBeforeExpirationToForceReset?: int, default?: bool, description?: string, disallowCommonlyUsedPasswords?: bool, disallowSequentialOrRepetitiveChars?: bool, displayComplexityOnResetScreen?: bool, effectiveDate?: string, enableDaysAfterExpirationToSelfRecover?: bool, enableDaysBeforeExpirationToForceReset?: bool, enableLockoutTimeInSeconds?: bool, enableMaxHistory?: bool, enableMaxLoginAttempts?: bool, enableMinChangePeriodInDays?: bool, enableMinLength?: bool, enablePasswordExpirationInDays?: bool, enableRecoveryEmail?: bool, enableResetLockoutCounter?: bool, lockoutTimeInSeconds?: int, maxHistory?: int, maxLoginAttempts?: int, minChangePeriodInDays?: int, minLength?: int, name?: string, needsLowercase?: bool, needsNumeric?: bool, needsSymbolic?: bool, needsUppercase?: bool, passwordExpirationInDays?: int, precedence?: int, resetLockoutCounterMinutes?: int}
]: any -> record<cached: bool, groups: table<groupId: string, name: string>, objectId: string, policy: record<allowUnenrolledMFAPasswordReset: bool, allowUsernameSubstring: bool, daysAfterExpirationToSelfRecover: int, daysBeforeExpirationToForceReset: int, default: bool, description: string, disallowCommonlyUsedPasswords: bool, disallowSequentialOrRepetitiveChars: bool, displayComplexityOnResetScreen: bool, effectiveDate: string, enableDaysAfterExpirationToSelfRecover: bool, enableDaysBeforeExpirationToForceReset: bool, enableLockoutTimeInSeconds: bool, enableMaxHistory: bool, enableMaxLoginAttempts: bool, enableMinChangePeriodInDays: bool, enableMinLength: bool, enablePasswordExpirationInDays: bool, enableRecoveryEmail: bool, enableResetLockoutCounter: bool, lockoutTimeInSeconds: int, maxHistory: int, maxLoginAttempts: int, minChangePeriodInDays: int, minLength: int, name: string, needsLowercase: bool, needsNumeric: bool, needsSymbolic: bool, needsUppercase: bool, passwordExpirationInDays: int, precedence: int, resetLockoutCounterMinutes: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordpolicies")
  let body = {groupIds: $groupIds, organizationObjectId: $organizationObjectId, policy: $policy} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Batch Delete Password Policy
#
# DELETE /passwordpolicies
# operationId: Policy_BatchDelete
export def "passwordpolicies BatchDelete" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --objectIds: list
  --organizationObjectId: string # format: byte
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/passwordpolicies")
  let body = {objectIds: $objectIds, organizationObjectId: $organizationObjectId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Password Policy by ID
#
# GET /passwordpolicies/{objectId}
# operationId: Policy_GetByID
export def "passwordpolicies GetByID" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<cached: bool, groups: table<groupId: string, name: string>, objectId: string, policy: record<allowUnenrolledMFAPasswordReset: bool, allowUsernameSubstring: bool, daysAfterExpirationToSelfRecover: int, daysBeforeExpirationToForceReset: int, default: bool, description: string, disallowCommonlyUsedPasswords: bool, disallowSequentialOrRepetitiveChars: bool, displayComplexityOnResetScreen: bool, effectiveDate: string, enableDaysAfterExpirationToSelfRecover: bool, enableDaysBeforeExpirationToForceReset: bool, enableLockoutTimeInSeconds: bool, enableMaxHistory: bool, enableMaxLoginAttempts: bool, enableMinChangePeriodInDays: bool, enableMinLength: bool, enablePasswordExpirationInDays: bool, enableRecoveryEmail: bool, enableResetLockoutCounter: bool, lockoutTimeInSeconds: int, maxHistory: int, maxLoginAttempts: int, minChangePeriodInDays: int, minLength: int, name: string, needsLowercase: bool, needsNumeric: bool, needsSymbolic: bool, needsUppercase: bool, passwordExpirationInDays: int, precedence: int, resetLockoutCounterMinutes: int>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordpolicies/($objectId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Password Policy
#
# PUT /passwordpolicies/{objectId}
# operationId: Policy_Update
# --policy shape: {allowUnenrolledMFAPasswordReset?: bool, allowUsernameSubstring?: bool, daysAfterExpirationToSelfRecover?: int, daysBeforeExpirationToForceReset?: int, default?: bool, description?: string, disallowCommonlyUsedPasswords?: bool, disallowSequentialOrRepetitiveChars?: bool, displayComplexityOnResetScreen?: bool, effectiveDate?: string, enableDaysAfterExpirationToSelfRecover?: bool, enableDaysBeforeExpirationToForceReset?: bool, enableLockoutTimeInSeconds?: bool, enableMaxHistory?: bool, enableMaxLoginAttempts?: bool, enableMinChangePeriodInDays?: bool, enableMinLength?: bool, enablePasswordExpirationInDays?: bool, enableRecoveryEmail?: bool, enableResetLockoutCounter?: bool, lockoutTimeInSeconds?: int, maxHistory?: int, maxLoginAttempts?: int, minChangePeriodInDays?: int, minLength?: int, name?: string, needsLowercase?: bool, needsNumeric?: bool, needsSymbolic?: bool, needsUppercase?: bool, passwordExpirationInDays?: int, precedence?: int, resetLockoutCounterMinutes?: int}
export def "passwordpolicies Update" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --groupIds: list
  --organizationObjectId: string # format: byte
  --policy: record # shape: {allowUnenrolledMFAPasswordReset?: bool, allowUsernameSubstring?: bool, daysAfterExpirationToSelfRecover?: int, daysBeforeExpirationToForceReset?: int, default?: bool, description?: string, disallowCommonlyUsedPasswords?: bool, disallowSequentialOrRepetitiveChars?: bool, displayComplexityOnResetScreen?: bool, effectiveDate?: string, enableDaysAfterExpirationToSelfRecover?: bool, enableDaysBeforeExpirationToForceReset?: bool, enableLockoutTimeInSeconds?: bool, enableMaxHistory?: bool, enableMaxLoginAttempts?: bool, enableMinChangePeriodInDays?: bool, enableMinLength?: bool, enablePasswordExpirationInDays?: bool, enableRecoveryEmail?: bool, enableResetLockoutCounter?: bool, lockoutTimeInSeconds?: int, maxHistory?: int, maxLoginAttempts?: int, minChangePeriodInDays?: int, minLength?: int, name?: string, needsLowercase?: bool, needsNumeric?: bool, needsSymbolic?: bool, needsUppercase?: bool, passwordExpirationInDays?: int, precedence?: int, resetLockoutCounterMinutes?: int}
]: any -> record<cached: bool, groups: table<groupId: string, name: string>, objectId: string, policy: record<allowUnenrolledMFAPasswordReset: bool, allowUsernameSubstring: bool, daysAfterExpirationToSelfRecover: int, daysBeforeExpirationToForceReset: int, default: bool, description: string, disallowCommonlyUsedPasswords: bool, disallowSequentialOrRepetitiveChars: bool, displayComplexityOnResetScreen: bool, effectiveDate: string, enableDaysAfterExpirationToSelfRecover: bool, enableDaysBeforeExpirationToForceReset: bool, enableLockoutTimeInSeconds: bool, enableMaxHistory: bool, enableMaxLoginAttempts: bool, enableMinChangePeriodInDays: bool, enableMinLength: bool, enablePasswordExpirationInDays: bool, enableRecoveryEmail: bool, enableResetLockoutCounter: bool, lockoutTimeInSeconds: int, maxHistory: int, maxLoginAttempts: int, minChangePeriodInDays: int, minLength: int, name: string, needsLowercase: bool, needsNumeric: bool, needsSymbolic: bool, needsUppercase: bool, passwordExpirationInDays: int, precedence: int, resetLockoutCounterMinutes: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/passwordpolicies/($objectId)")
  let body = {groupIds: $groupIds, organizationObjectId: $organizationObjectId, policy: $policy} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete Password Policy
#
# DELETE /passwordpolicies/{objectId}
# operationId: Policy_Delete
export def "passwordpolicies Delete" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordpolicies/($objectId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Password Policy Precedence
#
# PUT /passwordpolicies/precedence/set
# operationId: Policy_UpdatePolicyPrecedence
export def "passwordpolicies-precedence-set UpdatePolicyPrecedence" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --body: record
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/passwordpolicies/precedence/set" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Password Policy by User
#
# GET /passwordpolicies/user/{systemUserId}
# operationId: Policy_Get
export def "passwordpolicies-user Get" [
  systemUserId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<cached: bool, groups: table<groupId: string, name: string>, objectId: string, policy: record<allowUnenrolledMFAPasswordReset: bool, allowUsernameSubstring: bool, daysAfterExpirationToSelfRecover: int, daysBeforeExpirationToForceReset: int, default: bool, description: string, disallowCommonlyUsedPasswords: bool, disallowSequentialOrRepetitiveChars: bool, displayComplexityOnResetScreen: bool, effectiveDate: string, enableDaysAfterExpirationToSelfRecover: bool, enableDaysBeforeExpirationToForceReset: bool, enableLockoutTimeInSeconds: bool, enableMaxHistory: bool, enableMaxLoginAttempts: bool, enableMinChangePeriodInDays: bool, enableMinLength: bool, enablePasswordExpirationInDays: bool, enableRecoveryEmail: bool, enableResetLockoutCounter: bool, lockoutTimeInSeconds: int, maxHistory: int, maxLoginAttempts: int, minChangePeriodInDays: int, minLength: int, name: string, needsLowercase: bool, needsNumeric: bool, needsSymbolic: bool, needsUppercase: bool, passwordExpirationInDays: int, precedence: int, resetLockoutCounterMinutes: int>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordpolicies/user/($systemUserId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Background Tasks List
#
# GET /applemdms/{deviceObjectId}/background_tasks
# operationId: BackgroundTasks_List
export def "applemdms-background-tasks List" [
  deviceObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --queryParameterslimit: string # format: int64
  --queryParametersskip: string # format: int64
  --queryParametersfields: list
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "queryParameters.limit" $queryParameterslimit "scalar") (serialize-qp "queryParameters.skip" $queryParametersskip "scalar") (serialize-qp "queryParameters.fields" $queryParametersfields "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/applemdms/($deviceObjectId)/background_tasks" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for listing the webhook notification channels.
#
# GET /notifications/webhook/channels
# operationId: NotificationsChannelsService_ListWebhookChannels
export def "notifications-webhook-channels ListWebhookChannels" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.    **Filter structure**: `<field>:<operator>:<value>`.    **field** = Populate with a valid field from an endpoint response.    **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in.    **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.    **Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-fields: list # The fields on the items listed to return.
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: list # An optional comma separated field names used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --limit: string # The number of channels to return at once. If the number of channels returned is less than `limit` there are no more channels to fetch. If the number of channels returned is equal to limit there may be more channels to fetch. Increment `skip` by `limit` to get the next set of channels. (format: int64, default: 100)
]: nothing -> record<channels: table<enabled: bool, lastResponseStatusCode: int, lastResponseTimeStamp: string, name: string, objectId: string, organizationObjectId: string, successPercentage: int, totalRequests: int>, count: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "multi") (serialize-qp "limit" $limit "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notifications/webhook/channels" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used to test a webhook.
#
# POST /notifications/webhook/test
# operationId: NotificationsChannelsService_TestWebhook
# --postSave shape: {authPassword?: string, authToken?: string, authType?: "WEBHOOK_AUTH_TYPE_UNSPECIFIED"|"WEBHOOK_AUTH_TYPE_BASIC"|"WEBHOOK_AUTH_TYPE_BEARER", authUsername?: string, customHeaders?: list, maskedUrlPortion?: list, objectId?: string, sslVerification?: bool, url?: string, urlMasked?: bool}
# --preSave shape: {authPassword?: string, authToken?: string, authType?: "WEBHOOK_AUTH_TYPE_UNSPECIFIED"|"WEBHOOK_AUTH_TYPE_BASIC"|"WEBHOOK_AUTH_TYPE_BEARER", authUsername?: string, customHeaders?: list, sslVerification?: bool, url?: string, urlMasked?: bool}
export def "notifications-webhook-test TestWebhook" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --postSave: record # PostSaveWebhookRequest represents the request to test a webhook after saving. — shape: {authPassword?: string, authToken?: string, authType?: "WEBHOOK_AUTH_TYPE_UNSPECIFIED"|"WEBHOOK_AUTH_TYPE_BASIC"|"WEBHOOK_AUTH_TYPE_BEARER", authUsername?: string, customHeaders?: list, maskedUrlPortion?: list, objectId?: string, sslVerification?: bool, url?: string, urlMasked?: bool}
  --preSave: record # PreSaveWebhookRequest represents the request to test a webhook before saving. — shape: {authPassword?: string, authToken?: string, authType?: "WEBHOOK_AUTH_TYPE_UNSPECIFIED"|"WEBHOOK_AUTH_TYPE_BASIC"|"WEBHOOK_AUTH_TYPE_BEARER", authUsername?: string, customHeaders?: list, sslVerification?: bool, url?: string, urlMasked?: bool}
]: any -> record<responseMessage: string, statusCode: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/notifications/webhook/test")
  let body = {organizationObjectId: $organizationObjectId, postSave: $postSave, preSave: $preSave} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Scope Groups
#
# GET /scope-groups
# operationId: scopegroups_list
export def "scope-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-total-count: int
]: nothing -> record<totalCount: int, results: table<id: string, groupName: string, groupKey: string, fullAccessScopes: list, displayToUsers: bool, groupDescription: string, scopes: list, createdAt: string, updatedAt: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "filter" $filter "csv") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/scope-groups" $qp)
  let extra_headers = {"x-total-count": $x_total_count} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List all roles.
#
# GET /roles
# operationId: roles_list
export def "roles list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  **Filter structure**: `<field>:<operator>:<value>`.  **field** = Populate with a valid field from an endpoint response.  **operator** =  Supported operators are: eq, ne, gt, ge, lt, le, between, search, in. _Note: v1 operators differ from v2 operators._  **value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  **EX:** `GET /api/v2/groups?filter=name:eq:Test+Group`
  --limit: int # The number of records to return at once. Limited to 100. (default: 10)
  --skip: int # The offset into the records to return. (default: 0)
  --qp-sort: list # The comma separated fields used to sort the collection. Default sort is ascending, prefix with `-` to sort descending.
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-provider-id: string # Multi-tenant Provider identifier that can be obtained from console settings.
  --x-total-count: int
]: nothing -> record<totalCount: int, results: table<id: string, name: string, scopes: list, providerId: string, organizationIds: list, description: string, createdBy: string, lastUpdatedBy: string, isCustom: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "csv") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/roles" $qp)
  let extra_headers = {"x-org-id": $x_org_id, "x-provider-id": $x_provider_id, "x-total-count": $x_total_count} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new role.
#
# POST /roles
# operationId: roles_create
export def "roles create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  name: string # Name of the role
  scopes: list # List of permission scopes to assign to this role
  --providerId: string # ID of the provider to associate with this role
  --organizationIds: list # List of organization IDs to associate with this role
  --description: string # Description of the role and its purpose
]: any -> record<id: string, name: string, scopes: list<string>, providerId: string, organizationIds: list<string>, description: string, createdBy: string, lastUpdatedBy: string, isCustom: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/roles")
  let body = {name: $name, scopes: $scopes, providerId: $providerId, organizationIds: $organizationIds, description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a specific role.
#
# GET /roles/{role_id}
# operationId: roles_get
export def "roles get" [
  role_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-provider-id: string # Multi-tenant Provider identifier that can be obtained from console settings.
]: nothing -> record<id: string, name: string, scopes: list<string>, providerId: string, organizationIds: list<string>, description: string, createdBy: string, lastUpdatedBy: string, isCustom: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/roles/($role_id)")
  let extra_headers = {"x-org-id": $x_org_id, "x-provider-id": $x_provider_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a role.
#
# PUT /roles/{role_id}
# operationId: roles_update
export def "roles update" [
  role_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-provider-id: string # Multi-tenant Provider identifier that can be obtained from console settings.
  name: string # Updated name of the role
  scopes: list # Updated list of permission scopes for this role
  --providerId: string # ID of the provider to associate with this role
  --organizationIds: list # List of organization IDs to associate with this role
  --description: string # Description of the role and its purpose
]: any -> record<id: string, name: string, scopes: list<string>, providerId: string, organizationIds: list<string>, description: string, createdBy: string, lastUpdatedBy: string, isCustom: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/roles/($role_id)")
  let body = {name: $name, scopes: $scopes, providerId: $providerId, organizationIds: $organizationIds, description: $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id, "x-provider-id": $x_provider_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a role.
#
# DELETE /roles/{role_id}
# operationId: roles_delete
export def "roles delete" [
  role_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  --x-provider-id: string # Multi-tenant Provider identifier that can be obtained from console settings.
]: nothing -> record<code: int, message: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/roles/($role_id)")
  let extra_headers = {"x-org-id": $x_org_id, "x-provider-id": $x_provider_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Password Policy by User Group
#
# GET /passwordpolicies/usergroup/{userGroupId}
# operationId: Policy_GetByUserGroup
export def "passwordpolicies-usergroup GetByUserGroup" [
  userGroupId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<cached: bool, groups: table<groupId: string, name: string>, objectId: string, policy: record<allowUnenrolledMFAPasswordReset: bool, allowUsernameSubstring: bool, daysAfterExpirationToSelfRecover: int, daysBeforeExpirationToForceReset: int, default: bool, description: string, disallowCommonlyUsedPasswords: bool, disallowSequentialOrRepetitiveChars: bool, displayComplexityOnResetScreen: bool, effectiveDate: string, enableDaysAfterExpirationToSelfRecover: bool, enableDaysBeforeExpirationToForceReset: bool, enableLockoutTimeInSeconds: bool, enableMaxHistory: bool, enableMaxLoginAttempts: bool, enableMinChangePeriodInDays: bool, enableMinLength: bool, enablePasswordExpirationInDays: bool, enableRecoveryEmail: bool, enableResetLockoutCounter: bool, lockoutTimeInSeconds: int, maxHistory: int, maxLoginAttempts: int, minChangePeriodInDays: int, minLength: int, name: string, needsLowercase: bool, needsNumeric: bool, needsSymbolic: bool, needsUppercase: bool, passwordExpirationInDays: int, precedence: int, resetLockoutCounterMinutes: int>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/passwordpolicies/usergroup/($userGroupId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Fetch all saved service accounts
#
# GET /service-accounts
# operationId: ServiceAccounts_ListServiceAccounts
export def "service-accounts ListServiceAccounts" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # pagination - page size (maximum number of results to return) (format: int32)
  --skip: int # pagination - page offset (number of results to skip) (format: int32)
  --searchfields: list # search field (currently only supports "name")
  --searchsearchTerm: string # search term/value (e.g., "John")
  --qp-sort: string # sort configuration (allowed values: "name", "-name", "expiresAt", "-expiresAt") "-" prefix indicates descending order if search config is provided, it will override the sort field to "name" and sort order to ascending
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<results: table<authConfigList: list, authType: string, createdAt: string, expiresAt: string, name: string, objectId: string, orgCount: int, orgId: string, providerId: string, roleId: string, roleName: string, status: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "search.fields" $searchfields "multi") (serialize-qp "search.searchTerm" $searchsearchTerm "scalar") (serialize-qp "sort" $qp_sort "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/service-accounts" $qp)
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new service account
#
# POST /service-accounts
# operationId: ServiceAccounts_CreateServiceAccount
# --authConfig shape: {authType: "AUTH_TYPE_UNSPECIFIED"|"CLIENT_SECRET", clientSecretConfig: record}
export def "service-accounts CreateServiceAccount" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  authConfig: record # shape: {authType: "AUTH_TYPE_UNSPECIFIED"|"CLIENT_SECRET", clientSecretConfig: record}
  name: string
  roleId: string # format: byte
  --roleName: string # DEPRECATED: the role name is ignored. Use role_id instead.
]: any -> record<serviceAccount: record<authConfigList: list<record>, authType: string, createdAt: string, expiresAt: string, name: string, objectId: string, orgCount: int, orgId: string, providerId: string, roleId: string, roleName: string, status: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/service-accounts")
  let body = {authConfig: $authConfig, name: $name, roleId: $roleId, roleName: $roleName} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Fetch a service account by ID
#
# GET /service-accounts/{service_account_id}
# operationId: ServiceAccounts_GetServiceAccount
export def "service-accounts GetServiceAccount" [
  service_account_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record<authConfigList: table<authType: string, clientSecretConfig: record>, authType: string, createdAt: string, expiresAt: string, name: string, objectId: string, orgCount: int, orgId: string, providerId: string, roleId: string, roleName: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/service-accounts/($service_account_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete a service account
#
# DELETE /service-accounts/{service_account_id}
# operationId: ServiceAccounts_DeleteServiceAccount
export def "service-accounts DeleteServiceAccount" [
  service_account_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/service-accounts/($service_account_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a new service account authentication config
#
# POST /service-accounts/{service_account_id}/auth-config
# operationId: ServiceAccounts_CreateServiceAccountAuthConfig
# --authConfig shape: {authType: "AUTH_TYPE_UNSPECIFIED"|"CLIENT_SECRET", clientSecretConfig: record}
export def "service-accounts-auth-config CreateServiceAccountAuthConfig" [
  service_account_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
  authConfig: record # shape: {authType: "AUTH_TYPE_UNSPECIFIED"|"CLIENT_SECRET", clientSecretConfig: record}
]: any -> record<authConfig: record<authType: string, clientSecretConfig: record<clientId: string, clientSecret: string, createdAt: string, createdBy: record, expiresAt: string, lifetime: string, objectId: string, status: string>>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/service-accounts/($service_account_id)/auth-config")
  let body = {authConfig: $authConfig} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a specific auth config for a service account
#
# DELETE /service-accounts/{service_account_id}/auth-config/{auth_config_id}
# operationId: ServiceAccounts_DeleteServiceAccountAuthConfig
export def "service-accounts-auth-config DeleteServiceAccountAuthConfig" [
  service_account_id: string
  auth_config_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Organization identifier that can be obtained from console settings.
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/service-accounts/($service_account_id)/auth-config/($auth_config_id)")
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# This endpoint is used for performing bulk delete operations on alerts.
#
# POST /alerts/bulk-delete
# operationId: AlertsService_BulkDelete
# --filter shape: {category?: list, existingCondition?: string, lastOccurredAtAfter?: string, lastOccurredAtBefore?: string, severity?: list, sourceId?: list, sourceType?: list, status?: list, title?: string}
export def "alerts-bulk-delete BulkDelete" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --excludeIds: list
  --filter: record # shape: {category?: list, existingCondition?: string, lastOccurredAtAfter?: string, lastOccurredAtBefore?: string, severity?: list, sourceId?: list, sourceType?: list, status?: list, title?: string}
]: any -> record<affectedCount: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/alerts/bulk-delete")
  let body = {excludeIds: $excludeIds, filter: $filter} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# This endpoint is used for performing bulk update operations on alerts.
#
# POST /alerts/bulk-update
# operationId: AlertsService_BulkUpdate
# --filter shape: {category?: list, existingCondition?: string, lastOccurredAtAfter?: string, lastOccurredAtBefore?: string, severity?: list, sourceId?: list, sourceType?: list, status?: list, title?: string}
# --updateField shape: {status?: "ALERT_STATUS_UNSPECIFIED"|"ALERT_STATUS_OPEN"|"ALERT_STATUS_ACKNOWLEDGED"|"ALERT_STATUS_RESOLVED"|"ALERT_STATUS_AUTO_RESOLVED"|"ALERT_STATUS_DELETED"}
export def "alerts-bulk-update BulkUpdate" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --excludeIds: list
  --filter: record # shape: {category?: list, existingCondition?: string, lastOccurredAtAfter?: string, lastOccurredAtBefore?: string, severity?: list, sourceId?: list, sourceType?: list, status?: list, title?: string}
  --remark: string
  --updateField: record # shape: {status?: "ALERT_STATUS_UNSPECIFIED"|"ALERT_STATUS_OPEN"|"ALERT_STATUS_ACKNOWLEDGED"|"ALERT_STATUS_RESOLVED"|"ALERT_STATUS_AUTO_RESOLVED"|"ALERT_STATUS_DELETED"}
]: any -> record<affectedCount: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/alerts/bulk-update")
  let body = {excludeIds: $excludeIds, filter: $filter, remark: $remark, updateField: $updateField} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# User Approval Access Request
#
# POST /userapprovalrequest
# operationId: WorkflowAccessRequestApi_UserApprovalRequest
export def "userapprovalrequest UserApprovalRequest" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --approvedDuration: string
  --id: string
  --organizationObjectId: string # format: byte
  --remarks: string
  --status: string
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/userapprovalrequest")
  let body = {approvedDuration: $approvedDuration, id: $id, organizationObjectId: $organizationObjectId, remarks: $remarks, status: $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Organization Feature Settings
#
# GET /feature-settings
# operationId: OrganizationFeatureSettingsService_List
export def "feature-settings List" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/feature-settings?filter=feature.name:eq:mcp&filter=setting.name:eq:enabled`
]: nothing -> record<features: table<category: string, name: string, settings: list>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/feature-settings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Organization Feature Settings
#
# PUT /feature-settings/{feature_name}/{setting_name}
# operationId: OrganizationFeatureSettingsService_Update
export def "feature-settings Update" [
  feature_name: string
  setting_name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --value: record
]: any -> record<name: string, value: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/feature-settings/($feature_name)/($setting_name)")
  let body = {value: $value} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Approval flow progress by id for admin portal
#
# GET /approversprogress/{id}
# operationId: WorkflowAccessRequestApi_GetAccessRequestApprovalProgressAdminPortal
export def "approversprogress GetAccessRequestApprovalProgressAdminPortal" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<approvers: table<approvalSequence: int, approvalStatus: string, approvedCount: int, approverId: string, approverType: string, createdAt: string, overridenApproval: bool, requireAll: bool, resourceId: string, resourceName: string, resourceType: string, totalCount: int>, approversRequirement: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/approversprogress/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# ADUE
#
# POST /applemdms/{deviceManagerObjectId}/service-discovery-url
# operationId: EnrollmentProfiles_UpdateServiceDiscoveryURL
export def "applemdms-service-discovery-url UpdateServiceDiscoveryURL" [
  deviceManagerObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body: record
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($deviceManagerObjectId)/service-discovery-url")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# ADUE
#
# DELETE /applemdms/{deviceManagerObjectId}/service-discovery-url
# operationId: EnrollmentProfiles_DeleteServiceDiscoveryURL
export def "applemdms-service-discovery-url DeleteServiceDiscoveryURL" [
  deviceManagerObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($deviceManagerObjectId)/service-discovery-url")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# ADUE
#
# GET /applemdms/{deviceManagerObjectId}/validate-service-discovery-url
# operationId: EnrollmentProfiles_ValidateServiceDiscoveryURL
export def "applemdms-validate-service-discovery-url ValidateServiceDiscoveryURL" [
  deviceManagerObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/applemdms/($deviceManagerObjectId)/validate-service-discovery-url")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get all Workflow Access Requests
#
# GET /approvalflowrequest
# operationId: WorkflowAccessRequestApi_GetAllWorkflowAccessRequests
export def "approvalflowrequest GetAllWorkflowAccessRequests" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --limit: string # format: int64
  --skip: string # format: int64
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<approvalFlowDescription: string, approvalFlowId: string, approvalFlowName: string, approvalType: string, approvedDuration: string, createdAt: string, department: string, devicePermissionLevel: string, expirationDate: string, group: string, iconColor: string, iconUrl: string, id: string, internalNote: string, manager: string, nonAdminApproval: bool, remarks: string, requestedDuration: string, requestor: string, resourceId: string, resourceType: string, runId: string, sessionStartDate: string, status: string, timeBasedAccess: bool, updatedAt: string, updatedBy: string, userName: string, wfMetadata: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/approvalflowrequest" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Workflow Access Request
#
# POST /approvalflowrequest
# operationId: WorkflowAccessRequestApi_CreateWorkflowAccessRequest
export def "approvalflowrequest CreateWorkflowAccessRequest" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --deviceId: string
  --devicePermissionLevel: string
  --id: string
  --organizationObjectId: string # format: byte
  --reason: string
  --requestedDuration: string
  --sessionStartDate: string
]: any -> record<id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/approvalflowrequest")
  let body = {deviceId: $deviceId, devicePermissionLevel: $devicePermissionLevel, id: $id, organizationObjectId: $organizationObjectId, reason: $reason, requestedDuration: $requestedDuration, sessionStartDate: $sessionStartDate} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Workflow Access Requests
#
# GET /approvalflowrequest/{id}
# operationId: WorkflowAccessRequestApi_GetWorkflowAccessRequests
export def "approvalflowrequest GetWorkflowAccessRequests" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<approvalFlowDescription: string, approvalFlowId: string, approvalFlowName: string, approvalType: string, approvedDuration: string, approvers: table<approvalSequence: string, approvalStatus: string, approverId: string, approverType: string, createdAt: string, requireAll: bool, resourceId: string, resourceName: string, resourceType: string>, approversRequirement: string, category: string, createdAt: string, department: string, devicePermissionLevel: string, fixedDuration: string, group: string, iconColor: string, iconUrl: string, id: string, internalNote: string, manager: string, metadata: string, multiSelectDuration: list<string>, nonAdminApproval: bool, remarks: string, requestedDuration: string, requestor: string, resourceId: string, resourceType: string, runId: string, sessionStartDate: string, status: string, timeBasedAccess: bool, ttlConfig: string, updatedAt: string, updatedBy: string, userName: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/approvalflowrequest/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Workflow Access Requests
#
# PUT /approvalflowrequest/{id}
# operationId: WorkflowAccessRequestApi_UpdateWorkflowAccessRequests
export def "approvalflowrequest UpdateWorkflowAccessRequests" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --admin: string
  --approvedDuration: string
  --internalNote: string
  --organizationObjectId: string # format: byte
  --remarks: string
  --status: string
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/approvalflowrequest/($id)")
  let body = {admin: $admin, approvedDuration: $approvedDuration, internalNote: $internalNote, organizationObjectId: $organizationObjectId, remarks: $remarks, status: $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get all Access workflows
#
# GET /approvalflows
# operationId: AccessWorkflowApi_GetAllAccessWorkFlows
export def "approvalflows GetAllAccessWorkFlows" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --limit: string # format: int64
  --skip: string # format: int64
  --qp-sort: string
  --qp-fields: list
  --filter: list
]: nothing -> record<results: table<approvalType: string, approverRequirement: string, approverResources: list, category: string, createdBy: string, description: string, deviceLevelPermission: list, fixedDuration: string, iconColor: string, iconUrl: string, id: string, multiSelectDuration: list, name: string, nonAdminApproval: bool, organizationId: string, resources: list, slackConfig: list, slackDmEnabled: bool, slackEnabled: bool, status: string, timeBasedAccess: bool, ttlConfig: string, updatedBy: string, visibleTo: list>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/approvalflows" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create Access Workflow
#
# POST /approvalflows
# operationId: AccessWorkflowApi_CreateAccessWorkflow
# --approverResources item shape: {requireAll?: bool, resourceId?: string, resourceName?: string, resourceOrder?: int, resourceType?: string}
# --slackConfig item shape: {slackResourceId?: string}
export def "approvalflows CreateAccessWorkflow" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --approvalType: string
  --approverRequirement: string
  --approverResources: list # item shape: {requireAll?: bool, resourceId?: string, resourceName?: string, resourceOrder?: int, resourceType?: string}
  --category: string
  --description: string
  --deviceLevelPermission: list
  --fixedDuration: string
  --iconColor: string
  --iconUrl: string
  --multiSelectDuration: list
  --name: string
  --nonAdminApproval: string@bool-completer
  --organizationObjectId: string # format: byte
  --resourceId: string
  --resourceType: string
  --slackConfig: list # item shape: {slackResourceId?: string}
  --slackDmEnabled: string@bool-completer
  --slackEnabled: string@bool-completer
  --status: string
  --timeBasedAccess: string@bool-completer
  --ttlConfig: string@ttlConfig-completer # default: TTL_CONFIG_UNSPECIFIED
  --visibleTo: list
]: any -> record<id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/approvalflows")
  let body = {approvalType: $approvalType, approverRequirement: $approverRequirement, approverResources: $approverResources, category: $category, description: $description, deviceLevelPermission: $deviceLevelPermission, fixedDuration: $fixedDuration, iconColor: $iconColor, iconUrl: $iconUrl, multiSelectDuration: $multiSelectDuration, name: $name, nonAdminApproval: $nonAdminApproval, organizationObjectId: $organizationObjectId, resourceId: $resourceId, resourceType: $resourceType, slackConfig: $slackConfig, slackDmEnabled: $slackDmEnabled, slackEnabled: $slackEnabled, status: $status, timeBasedAccess: $timeBasedAccess, ttlConfig: $ttlConfig, visibleTo: $visibleTo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get AccessWorkflow by id
#
# GET /approvalflows/{approvalFlowId}
# operationId: AccessWorkflowApi_GetAccessWorkflow
export def "approvalflows GetAccessWorkflow" [
  approvalFlowId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<approvalType: string, approverRequirement: string, approverResources: table<requireAll: bool, resourceId: string, resourceName: string, resourceOrder: int, resourceType: string>, category: string, createdBy: string, description: string, deviceLevelPermission: list<string>, fixedDuration: string, iconColor: string, iconUrl: string, id: string, multiSelectDuration: list<string>, name: string, nonAdminApproval: bool, organizationId: string, resources: table<resourceId: string, resourceName: string, resourceType: string>, slackConfig: table<slackResourceId: string>, slackDmEnabled: bool, slackEnabled: bool, status: string, timeBasedAccess: bool, ttlConfig: string, updatedBy: string, visibleTo: table<id: string, name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/approvalflows/($approvalFlowId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Access Workflow
#
# PUT /approvalflows/{approvalFlowId}
# operationId: AccessWorkflowApi_UpdateAccessWorkflow
# --approverResources item shape: {requireAll?: bool, resourceId?: string, resourceName?: string, resourceOrder?: int, resourceType?: string}
# --slackConfig item shape: {slackResourceId?: string}
export def "approvalflows UpdateAccessWorkflow" [
  approvalFlowId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --approvalType: string
  --approverRequirement: string
  --approverResources: list # item shape: {requireAll?: bool, resourceId?: string, resourceName?: string, resourceOrder?: int, resourceType?: string}
  --description: string
  --deviceLevelPermission: list
  --fixedDuration: string
  --iconColor: string
  --iconUrl: string
  --multiSelectDuration: list
  --name: string
  --nonAdminApproval: string@bool-completer
  --organizationObjectId: string # format: byte
  --resourceId: string
  --slackConfig: list # item shape: {slackResourceId?: string}
  --slackDmEnabled: string@bool-completer
  --slackEnabled: string@bool-completer
  --status: string
  --timeBasedAccess: string@bool-completer
  --ttlConfig: string@ttlConfig-completer # default: TTL_CONFIG_UNSPECIFIED
  --visibleTo: list
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/approvalflows/($approvalFlowId)")
  let body = {approvalType: $approvalType, approverRequirement: $approverRequirement, approverResources: $approverResources, description: $description, deviceLevelPermission: $deviceLevelPermission, fixedDuration: $fixedDuration, iconColor: $iconColor, iconUrl: $iconUrl, multiSelectDuration: $multiSelectDuration, name: $name, nonAdminApproval: $nonAdminApproval, organizationObjectId: $organizationObjectId, resourceId: $resourceId, slackConfig: $slackConfig, slackDmEnabled: $slackDmEnabled, slackEnabled: $slackEnabled, status: $status, timeBasedAccess: $timeBasedAccess, ttlConfig: $ttlConfig, visibleTo: $visibleTo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete access workflow
#
# DELETE /approvalflows/{approvalFlowId}
# operationId: AccessWorkflowApi_DeleteAccessWorkflow
export def "approvalflows DeleteAccessWorkflow" [
  approvalFlowId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/approvalflows/($approvalFlowId)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get AccessWorkflow settings for an organisation
#
# GET /approvalflowsettings
# operationId: AccessWorkflowSettingsApi_GetAccessWorkflowSettings
export def "approvalflowsettings GetAccessWorkflowSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<adminApprovalEmail: bool, channelEvents: table<channelObjectId: string, eventTypes: list>, createdBy: string, exposeApprovalProgress: bool, id: int, resourceRequest: bool, updatedBy: string, userApprovalEmail: bool, userRequestEmail: bool> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/approvalflowsettings" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update Access Workflow Settings
#
# PUT /approvalflowsettings
# operationId: AccessWorkflowSettingsApi_UpdateAccessWorkflowSettings
# --channelEvents item shape: {channelObjectId?: string, eventTypes?: list}
export def "approvalflowsettings UpdateAccessWorkflowSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --adminApprovalEmail: string@bool-completer
  --channelEvents: list # item shape: {channelObjectId?: string, eventTypes?: list}
  --exposeApprovalProgress: string@bool-completer
  --organizationObjectId: string # format: byte
  --resourceRequest: string@bool-completer
  --userApprovalEmail: string@bool-completer
  --userRequestEmail: string@bool-completer
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/approvalflowsettings")
  let body = {adminApprovalEmail: $adminApprovalEmail, channelEvents: $channelEvents, exposeApprovalProgress: $exposeApprovalProgress, organizationObjectId: $organizationObjectId, resourceRequest: $resourceRequest, userApprovalEmail: $userApprovalEmail, userRequestEmail: $userRequestEmail} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Create Access Workflow Settings for an organization
#
# POST /approvalflowsettings
# operationId: AccessWorkflowSettingsApi_CreateAccessWorkflowSettings
# --channelEvents item shape: {channelObjectId?: string, eventTypes?: list}
export def "approvalflowsettings CreateAccessWorkflowSettings" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --adminApprovalEmail: string@bool-completer
  --channelEvents: list # item shape: {channelObjectId?: string, eventTypes?: list}
  --exposeApprovalProgress: string@bool-completer
  --organizationObjectId: string # format: byte
  --resourceRequest: string@bool-completer
  --userApprovalEmail: string@bool-completer
  --userRequestEmail: string@bool-completer
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/approvalflowsettings")
  let body = {adminApprovalEmail: $adminApprovalEmail, channelEvents: $channelEvents, exposeApprovalProgress: $exposeApprovalProgress, organizationObjectId: $organizationObjectId, resourceRequest: $resourceRequest, userApprovalEmail: $userApprovalEmail, userRequestEmail: $userRequestEmail} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# This endpoint is used to test a slack notification.
#
# POST /notifications/slack/test
# operationId: NotificationsChannelsService_TestSlack
export def "notifications-slack-test TestSlack" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --slackChannelId: string
  --slackTeamId: string
]: any -> record<message: string, success: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/notifications/slack/test")
  let body = {organizationObjectId: $organizationObjectId, slackChannelId: $slackChannelId, slackTeamId: $slackTeamId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Recovery Lock Configuration
#
# GET /applemdms/{deviceManagerObjectId}/devices/{deviceObjectId}/getrecoverylockconfig
# operationId: Devices_GetRecoveryLockConfig
export def "applemdms-devices-getrecoverylockconfig GetRecoveryLockConfig" [
  deviceManagerObjectId: string
  deviceObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/applemdms/($deviceManagerObjectId)/devices/($deviceObjectId)/getrecoverylockconfig" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get Recovery Lock Password
#
# GET /applemdms/{deviceManagerObjectId}/devices/{deviceObjectId}/getrecoverylockpassword
# operationId: Devices_GetRecoveryLockPassword
export def "applemdms-devices-getrecoverylockpassword GetRecoveryLockPassword" [
  deviceManagerObjectId: string
  deviceObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/applemdms/($deviceManagerObjectId)/devices/($deviceObjectId)/getrecoverylockpassword" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Generates an upgrade URL for an enterprise
#
# POST /google-emm/enterprises/{enterpriseId}/generate-upgrade-url
# operationId: Enterprises_GenerateEnterpriseUpgradeURL
export def "google-emm-enterprises-generate-upgrade-url GenerateEnterpriseUpgradeURL" [
  enterpriseId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --domainsAllowed: list
]: any -> record<upgradeUrl: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/google-emm/enterprises/($enterpriseId)/generate-upgrade-url")
  let body = {domainsAllowed: $domainsAllowed} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Basic Transfer Device Between Organizations
#
# POST /systems/{systemObjectId}/transfer
# operationId: SystemsInternal_BasicTransferDevice
export def "systems-transfer BasicTransferDevice" [
  systemObjectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --x-org-id: string # Required header. Set to '-' to scope the request to all organizations under your account.
  --org-id: string # format: byte
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/systems/($systemObjectId)/transfer")
  let body = {org_id: $org_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"x-org-id": $x_org_id} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List Scheduled Reports
#
# GET /reports/scheduled
# operationId: Reports_ListScheduledReports
export def "reports-scheduled ListScheduledReports" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --qp-fields: list # The fields on the items listed to return.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Use camelCase only; allowed values: displayName, description, scheduleFrequency, isActive, createdAt, updatedAt. Do not use snake_case (e.g. display_name is invalid).  	**operator** = Supported operators: eq, ne, gt, ge, lt, le, search, in.  	**value** = Value to search for (case sensitive; supports wild cards).  	**Example** `GET /api/v2/reports/scheduled?filter=displayName:eq:Daily+Report`
]: nothing -> record<scheduledReports: table<createdAt: string, cronExpression: string, days: list, description: string, displayName: string, endDate: string, exportType: string, id: string, isActive: bool, lastRunAt: string, lastRunStatus: string, nextRunAt: string, recipients: list, reportId: string, reportType: string, scheduleFrequency: string, scheduleTime: string, startDate: string, timeZone: string, updatedAt: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/reports/scheduled" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a Scheduled Report
#
# POST /reports/scheduled
# operationId: Reports_CreateScheduledReport
# --scheduledReport shape: {days?: list, description?: string, displayName: string, endDate?: string, exportType: string, id?: string, isActive: bool, recipients: list, reportId?: string, reportType?: string, scheduleFrequency: string, scheduleTime: string, startDate?: string, timeZone: string}
export def "reports-scheduled CreateScheduledReport" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  scheduledReport: record # shape: {days?: list, description?: string, displayName: string, endDate?: string, exportType: string, id?: string, isActive: bool, recipients: list, reportId?: string, reportType?: string, scheduleFrequency: string, scheduleTime: string, startDate?: string, timeZone: string}
]: any -> record<createdAt: string, cronExpression: string, days: list<string>, description: string, displayName: string, endDate: string, exportType: string, id: string, isActive: bool, lastRunAt: string, lastRunStatus: string, nextRunAt: string, recipients: table<channelType: string, value: list>, reportId: string, reportType: string, scheduleFrequency: string, scheduleTime: string, startDate: string, timeZone: string, updatedAt: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/reports/scheduled")
  let body = {scheduledReport: $scheduledReport} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a Scheduled Report
#
# GET /reports/scheduled/{objectId}
# operationId: Reports_GetScheduledReport
export def "reports-scheduled GetScheduledReport" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<scheduledReport: record<createdAt: string, cronExpression: string, days: list<string>, description: string, displayName: string, endDate: string, exportType: string, id: string, isActive: bool, lastRunAt: string, lastRunStatus: string, nextRunAt: string, recipients: list<record>, reportId: string, reportType: string, scheduleFrequency: string, scheduleTime: string, startDate: string, timeZone: string, updatedAt: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/scheduled/($objectId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a Scheduled Report
#
# PUT /reports/scheduled/{objectId}
# operationId: Reports_UpdateScheduledReport
# --scheduledReport shape: {days?: list, description?: string, displayName: string, endDate?: string, exportType: string, id?: string, isActive: bool, recipients: list, reportId?: string, reportType?: string, scheduleFrequency: string, scheduleTime: string, startDate?: string, timeZone: string}
export def "reports-scheduled UpdateScheduledReport" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  scheduledReport: record # shape: {days?: list, description?: string, displayName: string, endDate?: string, exportType: string, id?: string, isActive: bool, recipients: list, reportId?: string, reportType?: string, scheduleFrequency: string, scheduleTime: string, startDate?: string, timeZone: string}
]: any -> record<scheduledReport: record<createdAt: string, cronExpression: string, days: list<string>, description: string, displayName: string, endDate: string, exportType: string, id: string, isActive: bool, lastRunAt: string, lastRunStatus: string, nextRunAt: string, recipients: list<record>, reportId: string, reportType: string, scheduleFrequency: string, scheduleTime: string, startDate: string, timeZone: string, updatedAt: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/scheduled/($objectId)")
  let body = {scheduledReport: $scheduledReport} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a Scheduled Report
#
# DELETE /reports/scheduled/{objectId}
# operationId: Reports_DeleteScheduledReport
export def "reports-scheduled DeleteScheduledReport" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<message: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/scheduled/($objectId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Scheduled Report Runs by Report
#
# GET /reports/scheduled/{objectId}/runs
# operationId: Reports_ListScheduledReportRunsByReport
export def "reports-scheduled-runs ListScheduledReportRunsByReport" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/reports/scheduled/{id}/runs?filter=status:eq:SUCCESS`
  --startDate: string # Inclusive start of completed_at range for history filtering (RFC3339). (format: date-time)
  --endDate: string # Inclusive end of completed_at range for history filtering (RFC3339). (format: date-time)
]: nothing -> record<runs: table<artifactUrl: string, completedAt: string, displayName: string, fileSize: string, id: string, rowCount: string, scheduleFrequency: string, scheduleTime: string, scheduledReportId: string, startedAt: string, status: string, timeZone: string, timeoutAt: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "filter" $filter "multi") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/reports/scheduled/($objectId)/runs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Trigger a Scheduled Report (Run Now)
#
# POST /reports/scheduled/{objectId}/trigger
# operationId: Reports_TriggerScheduledReport
export def "reports-scheduled-trigger TriggerScheduledReport" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<runId: string, startedAt: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/scheduled/($objectId)/trigger")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List all Scheduled Report Runs
#
# GET /reports/scheduled/runs
# operationId: Reports_ListScheduledReportRuns
export def "reports-scheduled-runs ListScheduledReportRuns" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/reports/scheduled/runs?filter=status:eq:SUCCESS`
  --startDate: string # Inclusive start of completed_at range for history filtering (RFC3339). (format: date-time)
  --endDate: string # Inclusive end of completed_at range for history filtering (RFC3339). (format: date-time)
]: nothing -> record<runs: table<artifactUrl: string, completedAt: string, displayName: string, fileSize: string, id: string, rowCount: string, scheduleFrequency: string, scheduleTime: string, scheduledReportId: string, startedAt: string, status: string, timeZone: string, timeoutAt: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "filter" $filter "multi") (serialize-qp "startDate" $startDate "scalar") (serialize-qp "endDate" $endDate "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/reports/scheduled/runs" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a Scheduled Report Run
#
# POST /reports/scheduled/runs
# operationId: Reports_CreateScheduledReportRun
# --scheduledReportRun shape: {id?: string, status?: string}
export def "reports-scheduled-runs CreateScheduledReportRun" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  scheduledReportRun: record # shape: {id?: string, status?: string}
]: any -> record<artifactUrl: string, completedAt: string, displayName: string, fileSize: string, id: string, rowCount: string, scheduleFrequency: string, scheduleTime: string, scheduledReportId: string, startedAt: string, status: string, timeZone: string, timeoutAt: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/reports/scheduled/runs")
  let body = {scheduledReportRun: $scheduledReportRun} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a Scheduled Report Run
#
# GET /reports/scheduled/runs/{objectId}
# operationId: Reports_GetScheduledReportRun
export def "reports-scheduled-runs GetScheduledReportRun" [
  objectId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<artifactUrl: string, completedAt: string, displayName: string, fileSize: string, id: string, rowCount: string, scheduleFrequency: string, scheduleTime: string, scheduledReportId: string, startedAt: string, status: string, timeZone: string, timeoutAt: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/scheduled/runs/($objectId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Cancel Workflow Access Request
#
# POST /approvalflowrequest/{id}/cancel
# operationId: WorkflowAccessRequestApi_CancelWorkflowAccessRequest
export def "approvalflowrequest-cancel CancelWorkflowAccessRequest" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/approvalflowrequest/($id)/cancel")
  let body = {organizationObjectId: $organizationObjectId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# End Session (device)
#
# POST /approvalflowrequest/{id}/endsession
# operationId: WorkflowAccessRequestApi_EndSession
export def "approvalflowrequest-endsession EndSession" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/approvalflowrequest/($id)/endsession")
  let body = {organizationObjectId: $organizationObjectId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Start Session (device)
#
# POST /approvalflowrequest/{id}/startsession
# operationId: WorkflowAccessRequestApi_StartSession
export def "approvalflowrequest-startsession StartSession" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/approvalflowrequest/($id)/startsession")
  let body = {organizationObjectId: $organizationObjectId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Revoke TTL Active Session
#
# POST /approvalflowrequest/revoke-ttl-active-session
# operationId: WorkflowAccessRequestApi_RevokeTTLActiveSession
export def "approvalflowrequest-revoke-ttl-active-session RevokeTTLActiveSession" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --requestIds: list
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/approvalflowrequest/revoke-ttl-active-session")
  let body = {organizationObjectId: $organizationObjectId, requestIds: $requestIds} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Revoke Elevated Permissions (Active Sessions)
#
# POST /sudoactivesessions/revoke
# operationId: WorkflowAccessRequestApi_RevokeElevatedPermissions
export def "sudoactivesessions-revoke RevokeElevatedPermissions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
  --requestIds: list
]: any -> record {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/sudoactivesessions/revoke")
  let body = {organizationObjectId: $organizationObjectId, requestIds: $requestIds} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get Password Manager Organization
#
# GET /password-manager
# operationId: Config_Get
export def "password-manager Get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --organizationObjectId: string # format: byte
]: nothing -> record<activated: bool, orgPublicKey: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "organizationObjectId" $organizationObjectId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/password-manager" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List Custom Report Builder reports
#
# GET /reports/custom-reports
# operationId: Reports_ListCustomReports
export def "reports-custom-reports ListCustomReports" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --qp-fields: list # The fields on the items listed to return.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<customReports: table<columns: list, createdAt: string, createdBy: string, description: string, displayName: string, filters: list, id: string, primaryObject: string, searchRequest: record, secondaryObjects: list, updatedAt: string, updatedBy: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/reports/custom-reports" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a Custom Report Builder report
#
# POST /reports/custom-reports
# operationId: Reports_CreateCustomReport
# --customReport shape: {columns?: list, description?: string, displayName: string, filters?: list, id?: string, primaryObject: string, searchRequest: record, secondaryObjects?: list}
export def "reports-custom-reports CreateCustomReport" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --customReport: record # shape: {columns?: list, description?: string, displayName: string, filters?: list, id?: string, primaryObject: string, searchRequest: record, secondaryObjects?: list}
]: any -> record<columns: list<string>, createdAt: string, createdBy: string, description: string, displayName: string, filters: table<field: string, operation: string, value: string>, id: string, primaryObject: string, searchRequest: record<aggregations: list<record>, cacheToken: string, fields: record<exclude: list, include: list>, filters: list<record>, limit: int, metadata: record<context: list, searchPurpose: record, source: string>, pagination: record<offset: int, pageSize: int>, requestCache: bool, sort: list<record>>, secondaryObjects: list<string>, updatedAt: string, updatedBy: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/reports/custom-reports")
  let body = {customReport: $customReport} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a Custom Report Builder report
#
# GET /reports/custom-reports/{customReportId}
# operationId: Reports_GetCustomReport
export def "reports-custom-reports GetCustomReport" [
  customReportId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<customReport: record<columns: list<string>, createdAt: string, createdBy: string, description: string, displayName: string, filters: list<record>, id: string, primaryObject: string, searchRequest: record<aggregations: list, cacheToken: string, fields: record, filters: list, limit: int, metadata: record, pagination: record, requestCache: bool, sort: list>, secondaryObjects: list<string>, updatedAt: string, updatedBy: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/custom-reports/($customReportId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a Custom Report Builder report
#
# PUT /reports/custom-reports/{customReportId}
# operationId: Reports_UpdateCustomReport
# --customReport shape: {columns?: list, description?: string, displayName: string, filters?: list, id?: string, primaryObject: string, searchRequest: record, secondaryObjects?: list}
export def "reports-custom-reports UpdateCustomReport" [
  customReportId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --customReport: record # shape: {columns?: list, description?: string, displayName: string, filters?: list, id?: string, primaryObject: string, searchRequest: record, secondaryObjects?: list}
]: any -> record<customReport: record<columns: list<string>, createdAt: string, createdBy: string, description: string, displayName: string, filters: list<record>, id: string, primaryObject: string, searchRequest: record<aggregations: list, cacheToken: string, fields: record, filters: list, limit: int, metadata: record, pagination: record, requestCache: bool, sort: list>, secondaryObjects: list<string>, updatedAt: string, updatedBy: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/custom-reports/($customReportId)")
  let body = {customReport: $customReport} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a Custom Report Builder report
#
# DELETE /reports/custom-reports/{customReportId}
# operationId: Reports_DeleteCustomReport
export def "reports-custom-reports DeleteCustomReport" [
  customReportId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<message: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/reports/custom-reports/($customReportId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List saved reports
#
# GET /reports/saved-reports
# operationId: Reports_ListSavedReports
export def "reports-saved-reports ListSavedReports" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: string # The number of records to return at once. Limited to 100. (format: int64, default: 100)
  --skip: string # The offset into the records to return. (format: int64, default: 0)
  --qp-sort: string # Use space separated sort parameters to sort the collection. Default sort is ascending. Prefix with - to sort descending.
  --qp-fields: list # The fields on the items listed to return.
  --filter: list # A filter to apply to the query.  	**Filter structure**: `<field>:<operator>:<value>`.  	**field** = Populate with a valid field from an endpoint response.  	**operator** =  Supported operators are: eq, ne, gt, ge, lt, le, search, in.  	**value** = Populate with the value you want to search for. Is case sensitive. Supports wild cards.  	**Example** `GET /api/v2/groups?filter=name:eq:Test+Group`
]: nothing -> record<savedReports: table<customReport: record, reportType: string, reportView: record>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "sort" $qp_sort "scalar") (serialize-qp "fields" $qp_fields "multi") (serialize-qp "filter" $filter "multi")] | flatten | str join "&"
  let full_url = (build-url $base "/reports/saved-reports" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a saved report
#
# GET /reports/saved-reports/{id}
# operationId: Reports_GetSavedReport
export def "reports-saved-reports GetSavedReport" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --reportType: string # report_view or custom_report
]: nothing -> record<savedReport: record<customReport: record<columns: list, createdAt: string, createdBy: string, description: string, displayName: string, filters: list, id: string, primaryObject: string, searchRequest: record, secondaryObjects: list, updatedAt: string, updatedBy: string>, reportType: string, reportView: record<configuration: record, createdAt: string, createdBy: string, description: string, displayName: string, id: string, resources: list, searchRequest: record, updatedAt: string, updatedBy: string, updatedBySource: string>>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "reportType" $reportType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/reports/saved-reports/($id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List field configurations
#
# GET /assets/field-configurations
# operationId: listFieldConfigurations
export def "assets-field-configurations listFieldConfigurations" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<results: table<id: string, label: string, default: bool, mandatory: bool, scope: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/assets/field-configurations")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List select options for a field configuration
#
# GET /assets/field-configurations/{id}/select-options
# operationId: getFieldConfigSelectOptions
export def "assets-field-configurations-select-options get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<results: table<id: string, value: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/field-configurations/($id)/select-options")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get a device asset by ID
#
# GET /assets/devices/{id}
# operationId: getDeviceAsset
export def "assets-devices get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, jcSystemId: string, fields: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/devices/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a device asset
#
# PUT /assets/devices/{id}
# operationId: updateDeviceAsset
export def "assets-devices updateDeviceAsset" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-fields: record # Asset field values organized by field label to update
]: any -> record<id: string, jcSystemId: string, fields: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/devices/($id)")
  let body = {fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a device asset
#
# DELETE /assets/devices/{id}
# operationId: deleteDeviceAsset
export def "assets-devices delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<message: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/devices/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List device assets
#
# GET /assets/devices
# operationId: listDeviceAssets
export def "assets-devices listDeviceAssets" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of results to return (format: int64, default: 10)
  --skip: int # Number of results to skip for pagination (format: int64, default: 0)
  --qp-fields: list # Field configuration labels to include in each asset's `fields` map (comma-separated). Each name must match the `label` from `GET /assets/field-configurations` for devices.
]: nothing -> record<results: table<id: string, jcSystemId: string, fields: record>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "fields" $qp_fields "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/assets/devices" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a device asset
#
# POST /assets/devices
# operationId: createDeviceAsset
export def "assets-devices createDeviceAsset" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-fields: record # Map of field label to raw JSON value for each field to set (not `FieldValue` objects). Formats match the API overview (dates, RFC3339 datetimes, `{"id"}` for references, etc.).
]: any -> record<id: string, jcSystemId: string, fields: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/assets/devices")
  let body = {fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Batch update device assets
#
# PATCH /assets/devices/batch
# operationId: batchUpdateDeviceAssets
# --fieldValue shape: {label: string, value: record}
export def "assets-devices-batch batchUpdateDeviceAssets" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  assetIds: list # Array of device asset IDs to update
  --fieldValue: record # Field value for batch updating assets — shape: {label: string, value: record}
  --body-fields: record # Asset field values organized by field label to update
]: any -> record<updatedAssetCount: int, notUpdatedAssetCount: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/assets/devices/batch")
  let body = {assetIds: $assetIds, fieldValue: $fieldValue, fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a device asset by JC System ID
#
# GET /assets/devices/jc-system/{jcSystemId}
# operationId: getDeviceAssetByJCSystemID
export def "assets-devices-jc-system get" [
  jcSystemId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, assetStatus: record<id: string, name: string, type: string>, warrantyExpiredDate: string, purchaseCost: record<amount: float, currency: string>, deployedDate: string, ownershipType: record<id: string, name: string, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/devices/jc-system/($jcSystemId)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get an accessory asset by ID
#
# GET /assets/accessories/{id}
# operationId: getAccessoryAsset
export def "assets-accessories get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, jcSystemId: string, fields: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/accessories/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update an accessory asset
#
# PUT /assets/accessories/{id}
# operationId: updateAccessoryAsset
export def "assets-accessories updateAccessoryAsset" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-fields: record # Asset field values organized by field label to update
]: any -> record<id: string, jcSystemId: string, fields: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/accessories/($id)")
  let body = {fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete an accessory asset
#
# DELETE /assets/accessories/{id}
# operationId: deleteAccessoryAsset
export def "assets-accessories delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<message: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/accessories/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List accessory assets
#
# GET /assets/accessories
# operationId: listAccessoryAssets
export def "assets-accessories listAccessoryAssets" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of results to return (format: int64, default: 100)
  --skip: int # Number of results to skip for pagination (format: int64, default: 0)
  --qp-fields: list # Field configuration labels to include in each asset's `fields` map. Each name must match the `label` from `GET /assets/field-configurations` for accessories.
]: nothing -> record<results: table<id: string, jcSystemId: string, fields: record>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "fields" $qp_fields "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/assets/accessories" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create an accessory asset
#
# POST /assets/accessories
# operationId: createAccessoryAsset
export def "assets-accessories createAccessoryAsset" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-fields: record # Map of field label to raw JSON value for each field to set (not `FieldValue` objects). Formats match the API overview (dates, RFC3339 datetimes, `{"id"}` for references, etc.).
]: any -> record<id: string, jcSystemId: string, fields: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/assets/accessories")
  let body = {fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Batch update accessory assets
#
# PATCH /assets/accessories/batch
# operationId: batchUpdateAccessoryAssets
# --fieldValue shape: {label: string, value: record}
export def "assets-accessories-batch batchUpdateAccessoryAssets" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  assetIds: list # Array of accessory asset IDs to update
  --fieldValue: record # Field value for batch updating assets — shape: {label: string, value: record}
  --body-fields: record # Asset field values organized by field label to update
]: any -> record<updatedAssetCount: int, notUpdatedAssetCount: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/assets/accessories/batch")
  let body = {assetIds: $assetIds, fieldValue: $fieldValue, fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get a location asset
#
# GET /assets/locations/{id}
# operationId: getLocationAsset
export def "assets-locations get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, jcSystemId: string, fields: record> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/locations/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update a location asset
#
# PUT /assets/locations/{id}
# operationId: updateLocationAsset
export def "assets-locations updateLocationAsset" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-fields: record # Asset field values organized by field label to update
]: any -> record<id: string, jcSystemId: string, fields: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/locations/($id)")
  let body = {fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete a location asset
#
# DELETE /assets/locations/{id}
# operationId: deleteLocationAsset
export def "assets-locations delete" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<message: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/locations/($id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List location assets
#
# GET /assets/locations
# operationId: listLocationAssets
export def "assets-locations listLocationAssets" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of results to return (format: int64, default: 100)
  --skip: int # Number of results to skip for pagination (format: int64, default: 0)
  --qp-fields: list # Field configuration labels to include in each asset's `fields` map (comma-separated). Each name must match the `label` from `GET /assets/field-configurations` for locations.
]: nothing -> record<results: table<id: string, jcSystemId: string, fields: record>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar") (serialize-qp "fields" $qp_fields "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/assets/locations" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create a location asset
#
# POST /assets/locations
# operationId: createLocationAsset
export def "assets-locations createLocationAsset" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --body-fields: record # Map of field label to raw JSON value for each field to set (not `FieldValue` objects). Formats match the API overview (dates, RFC3339 datetimes, `{"id"}` for references, etc.).
]: any -> record<id: string, jcSystemId: string, fields: record> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/assets/locations")
  let body = {fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Batch update location assets
#
# PATCH /assets/locations/batch
# operationId: batchUpdateLocationAssets
# --fieldValue shape: {label: string, value: record}
export def "assets-locations-batch batchUpdateLocationAssets" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  assetIds: list # Array of location asset IDs to update
  --fieldValue: record # Field value for batch updating assets — shape: {label: string, value: record}
  --body-fields: record # Asset field values organized by field label to update
]: any -> record<updatedAssetCount: int, notUpdatedAssetCount: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/assets/locations/batch")
  let body = {assetIds: $assetIds, fieldValue: $fieldValue, fields: $body_fields} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List device asset history
#
# GET /assets/devices/{id}/history
# operationId: listDeviceAssetHistory
export def "assets-devices-history listDeviceAssetHistory" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of results to return (format: int64, default: 100)
  --skip: int # Number of results to skip for pagination (format: int64, default: 0)
]: nothing -> record<results: table<eventType: string, note: string, changedFields: list, occurredAt: string, actor: record, actionSource: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assets/devices/($id)/history" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create device asset history note
#
# POST /assets/devices/{id}/history
# operationId: createDeviceAssetHistoryNote
export def "assets-devices-history createDeviceAssetHistoryNote" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  note: string # The content of the note to add to asset history
]: any -> record<eventType: string, note: string, changedFields: table<label: string, oldValue: record, newValue: record>, occurredAt: string, actor: record<id: string, name: string, type: string>, actionSource: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/devices/($id)/history")
  let body = {note: $note} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List accessory asset history
#
# GET /assets/accessories/{id}/history
# operationId: listAccessoryAssetHistory
export def "assets-accessories-history listAccessoryAssetHistory" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of results to return (format: int64, default: 100)
  --skip: int # Number of results to skip for pagination (format: int64, default: 0)
]: nothing -> record<results: table<eventType: string, note: string, changedFields: list, occurredAt: string, actor: record, actionSource: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assets/accessories/($id)/history" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create accessory asset history note
#
# POST /assets/accessories/{id}/history
# operationId: createAccessoryAssetHistoryNote
export def "assets-accessories-history createAccessoryAssetHistoryNote" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  note: string # The content of the note to add to asset history
]: any -> record<eventType: string, note: string, changedFields: table<label: string, oldValue: record, newValue: record>, occurredAt: string, actor: record<id: string, name: string, type: string>, actionSource: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/accessories/($id)/history")
  let body = {note: $note} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List location asset history
#
# GET /assets/locations/{id}/history
# operationId: listLocationAssetHistory
export def "assets-locations-history listLocationAssetHistory" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of results to return (format: int64, default: 100)
  --skip: int # Number of results to skip for pagination (format: int64, default: 0)
]: nothing -> record<results: table<eventType: string, note: string, changedFields: list, occurredAt: string, actor: record, actionSource: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "skip" $skip "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assets/locations/($id)/history" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create location asset history note
#
# POST /assets/locations/{id}/history
# operationId: createLocationAssetHistoryNote
export def "assets-locations-history createLocationAssetHistoryNote" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  note: string # The content of the note to add to asset history
]: any -> record<eventType: string, note: string, changedFields: table<label: string, oldValue: record, newValue: record>, occurredAt: string, actor: record<id: string, name: string, type: string>, actionSource: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assets/locations/($id)/history")
  let body = {note: $note} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# List catalog applications
#
# GET /saas-management/application-catalog
# operationId: listCatalogApplications
export def "saas-management-application-catalog listCatalogApplications" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of catalog applications to return (default: 50)
  --offset: int # Number of catalog applications to skip (default: 0)
  --name: string # Filter catalog applications by name
  --ids: list # List of catalog application IDs to retrieve (batch-get). When provided, pagination and name filter are ignored.
]: nothing -> record<results: table<id: string, name: string, logo_url: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "ids" $ids "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/saas-management/application-catalog" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get catalog application
#
# GET /saas-management/application-catalog/{catalog_application_id}
# operationId: getCatalogApplication
export def "saas-management-application-catalog get" [
  catalog_application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, name: string, description: string, domains: list<string>, logo_url: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saas-management/application-catalog/($catalog_application_id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List applications
#
# GET /saas-management/applications
# operationId: listApplications
export def "saas-management-applications listApplications" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of applications to return (default: 50)
  --offset: int # Number of applications to skip (default: 0)
  --expand: list # Optional list of related resources to include in each list item. Supported values: `discovery_source_types` (derived from account discovery records linked to the application).
]: nothing -> record<results: table<id: string, catalog_app_id: string, owner_user_id: string, status: string, access_restriction: string, custom_restriction_message: string, discovered_at: string, restriction_excluded_group_ids: list, discovery_source_types: list>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar") (serialize-qp "expand" $expand "csv")] | flatten | str join "&"
  let full_url = (build-url $base "/saas-management/applications" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Create application
#
# POST /saas-management/applications
# operationId: createApplication
export def "saas-management-applications createApplication" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --app-name: string # Application name if it is a custom application. Either provide app_name or catalog_app_id parameter.
  --app-domains: list # List of app URLs if it is a custom application.
  --description: string # Description of the application if it is a custom application.
  --catalog-app-id: string # Catalog application ID if it is already in our app catalog. Either provide app_name or catalog_app_id parameter.
  --owner-user-id: string # Owner User ID
  status: string@status-completer-2 # Application status
  --access-restriction: string@access-restriction-completer # Access restriction type if the status is set to UNAPPROVED.
  --alternative-button: string # Alternative button text
  --alternative-button-url: string # Alternative button URL
  --custom-restriction-message: string # Custom restriction message
  --restriction-excluded-group-ids: list # List of excluded group IDs
]: any -> record<id: string, name: string, catalog_app_id: string, owner_user_id: string, status: string, access_restriction: string, alternative_button: string, alternative_button_url: string, custom_restriction_message: string, discovered_at: string, restriction_excluded_group_ids: list<string>, sso_apps: table<id: string, app_name: string, display_label: string, template_name: string, status: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/saas-management/applications")
  let body = {app_name: $app_name, app_domains: $app_domains, description: $description, catalog_app_id: $catalog_app_id, owner_user_id: $owner_user_id, status: $status, access_restriction: $access_restriction, alternative_button: $alternative_button, alternative_button_url: $alternative_button_url, custom_restriction_message: $custom_restriction_message, restriction_excluded_group_ids: $restriction_excluded_group_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Get application
#
# GET /saas-management/applications/{application_id}
# operationId: getApplication
export def "saas-management-applications get" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --expand: list # Optional list of related resources to include in the response. Supported values: `sso_apps`.
]: nothing -> record<id: string, name: string, catalog_app_id: string, owner_user_id: string, status: string, access_restriction: string, alternative_button: string, alternative_button_url: string, custom_restriction_message: string, discovered_at: string, restriction_excluded_group_ids: list<string>, sso_apps: table<id: string, app_name: string, display_label: string, template_name: string, status: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expand" $expand "csv")] | flatten | str join "&"
  let full_url = (build-url $base $"/saas-management/applications/($application_id)" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Update application
#
# PUT /saas-management/applications/{application_id}
# operationId: updateApplication
export def "saas-management-applications updateApplication" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --owner-user-id: string # Owner User ID
  --app-name: string # Application name if it is a custom application.
  --app-domains: list # List of app URLs if it is a custom application.
  --description: string # Description of the application if it is a custom application.
  --status: string@status-completer-2 # Application status
  --access-restriction: string@access-restriction-completer # Access restriction type
  --alternative-button: string # Alternative button text
  --alternative-button-url: string # Alternative button URL
  --custom-restriction-message: string # Custom restriction message
  --restriction-excluded-group-ids: list # List of excluded group IDs
]: any -> record<id: string, name: string, catalog_app_id: string, owner_user_id: string, status: string, access_restriction: string, alternative_button: string, alternative_button_url: string, custom_restriction_message: string, discovered_at: string, restriction_excluded_group_ids: list<string>, sso_apps: table<id: string, app_name: string, display_label: string, template_name: string, status: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saas-management/applications/($application_id)")
  let body = {owner_user_id: $owner_user_id, app_name: $app_name, app_domains: $app_domains, description: $description, status: $status, access_restriction: $access_restriction, alternative_button: $alternative_button, alternative_button_url: $alternative_button_url, custom_restriction_message: $custom_restriction_message, restriction_excluded_group_ids: $restriction_excluded_group_ids} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $max_time $allow_errors "application/json" $body
}

# Delete application
#
# DELETE /saas-management/applications/{application_id}
# operationId: deleteApplication
export def "saas-management-applications delete" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<message: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saas-management/applications/($application_id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get application usage
#
# GET /saas-management/applications/{application_id}/usage
# operationId: getApplicationUsage
export def "saas-management-applications-usage get" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --day-count: int # Day count to filter (min:1 max:90)
]: nothing -> record<results: table<account_id: string, latest_used_at: string, account_email: string, account_username: string, user_id: string, user_email: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "day_count" $day_count "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/saas-management/applications/($application_id)/usage" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List licenses
#
# GET /saas-management/application-licenses
# operationId: listLicenses
export def "saas-management-application-licenses listLicenses" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of licenses to return (default: 50)
  --offset: int # Number of licenses to skip (default: 0)
]: nothing -> record<results: table<id: string, currency: string, licenses: record, costs: record, renewal_date: string, contract_term: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/saas-management/application-licenses" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List application licenses
#
# GET /saas-management/application-licenses/{application_id}
# operationId: listApplicationLicenses
export def "saas-management-application-licenses listApplicationLicenses" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<results: table<id: string, name: string, count: int, cost_per_license: float, assigned: int, unassigned: int, is_unlimited: bool>, totalCount: int, contract: record<cost: float, currency: string, term: string, renewal_date: string, notes: string>> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saas-management/application-licenses/($application_id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# List application accounts
#
# GET /saas-management/applications/{application_id}/accounts
# operationId: listApplicationAccounts
export def "saas-management-applications-accounts listApplicationAccounts" [
  application_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --limit: int # Maximum number of accounts to return (default: 50)
  --offset: int # Number of accounts to skip (default: 0)
]: nothing -> record<results: table<id: string, user_id: string, parent_app_id: string, email: string, username: string>, totalCount: int> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/saas-management/applications/($application_id)/accounts" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Get account
#
# GET /saas-management/applications/{application_id}/accounts/{account_id}
# operationId: getAccount
export def "saas-management-applications-accounts get" [
  application_id: string
  account_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<id: string, user_id: string, parent_app_id: string, email: string, username: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saas-management/applications/($application_id)/accounts/($account_id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}

# Delete account
#
# DELETE /saas-management/applications/{application_id}/accounts/{account_id}
# operationId: deleteAccount
export def "saas-management-applications-accounts delete" [
  application_id: string
  account_id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
]: nothing -> record<message: string> {
  let auth = (build-auth $token ($auth_scheme | default "x-api-key"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/saas-management/applications/($application_id)/accounts/($account_id)")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $max_time $allow_errors "application/json"
}
