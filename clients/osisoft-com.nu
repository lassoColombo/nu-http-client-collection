# Auto-generated client for PI Web API 2018 SP1 Swagger Spec v1.11.1.5383
# Source: https://api.apis.guru/v2/specs/osisoft.com/1.11.1.5383/swagger.json
# Auth: --token flag or $env.PI_WEB_API_2018_SP1_SWAGGER_SPEC_TOKEN

const BASE_URL = "https://devdata.osisoft.com/piwebapi"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o PI_WEB_API_2018_SP1_SWAGGER_SPEC_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, dry_run: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  if $dry_run { return {method: $method, url: $req_url, headers: $auth.headers, query_string: $auth.query, content_type: $ct, timeout: $timeout, body: $body} }
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def base-url-completer [] { ["https://devdata.osisoft.com/piwebapi"] }
def auth-scheme-completer [] { ["bearer"] }

# Completers for enum parameters
def accept-completer [] { ["application/json" "application/x-ms-application" "text/html" "text/json"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "dry-run" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "home Get" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# Get top level links for this PI System Web API instance.
#
# GET /
# operationId: Home_Get
export def "home Get" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Links: record<AssetServers: string, DataServers: string, Search: string, Self: string, System: string>, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Analysis by path.
#
# GET /analyses
# operationId: Analysis_GetByPath
export def "analyses GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the Analysis.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AnalysisRulePlugInName: string, AutoCreated: bool, CategoryNames: list<string>, Description: string, GroupId: int, HasNotification: bool, HasTarget: bool, HasTemplate: bool, Id: string, IsConfigured: bool, IsTimeRuleDefinedByTemplate: bool, Links: record<AnalysisRule: string, AnalysisRulePlugIn: string, Categories: string, Database: string, Security: string, SecurityEntries: string, Self: string, Target: string, Template: string, TimeRule: string, TimeRulePlugIn: string>, MaximumQueueSize: int, Name: string, OutputTime: string, Path: string, Priority: string, PublishResults: bool, Status: string, TargetWebId: string, TemplateName: string, TimeRulePlugInName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analyses" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve analyses based on the specified conditions. By default, returns all analyses.
#
# GET /analyses/search
# operationId: Analysis_GetAnalysesQuery
export def "analyses-search GetAnalysesQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --databaseWebId: string # The ID of the asset database to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the analyses in the asset database. An example would be: "query= Name:=MyAnalysis1* Template:=AnalysisTemplate".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AnalysisRulePlugInName: string, AutoCreated: bool, CategoryNames: list, Description: string, GroupId: int, HasNotification: bool, HasTarget: bool, HasTemplate: bool, Id: string, IsConfigured: bool, IsTimeRuleDefinedByTemplate: bool, Links: record, MaximumQueueSize: int, Name: string, OutputTime: string, Path: string, Priority: string, PublishResults: bool, Status: string, TargetWebId: string, TemplateName: string, TimeRulePlugInName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "databaseWebId" $databaseWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analyses/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an Analysis.
#
# DELETE /analyses/{webId}
# operationId: Analysis_Delete
export def "analyses Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analyses/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Analysis.
#
# GET /analyses/{webId}
# operationId: Analysis_Get
export def "analyses Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AnalysisRulePlugInName: string, AutoCreated: bool, CategoryNames: list<string>, Description: string, GroupId: int, HasNotification: bool, HasTarget: bool, HasTemplate: bool, Id: string, IsConfigured: bool, IsTimeRuleDefinedByTemplate: bool, Links: record<AnalysisRule: string, AnalysisRulePlugIn: string, Categories: string, Database: string, Security: string, SecurityEntries: string, Self: string, Target: string, Template: string, TimeRule: string, TimeRulePlugIn: string>, MaximumQueueSize: int, Name: string, OutputTime: string, Path: string, Priority: string, PublishResults: bool, Status: string, TargetWebId: string, TemplateName: string, TimeRulePlugInName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an Analysis.
#
# PATCH /analyses/{webId}
# operationId: Analysis_Update
# --Links shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, Template?: string, TimeRule?: string, TimeRulePlugIn?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analyses Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --AnalysisRulePlugInName: string # e.g. PerformanceEquation
  --AutoCreated: oneof<nothing, bool> # e.g. false
  --CategoryNames: list # e.g. [MyAnalysisCategory]
  --Description: string # e.g. 
  --GroupId: int # format: int32, e.g. 0
  --HasNotification: oneof<nothing, bool> # e.g. false
  --HasTarget: oneof<nothing, bool> # e.g. true
  --HasTemplate: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 11853358-94bf-11e7-8443-00155d84c200
  --IsConfigured: oneof<nothing, bool> # e.g. false
  --IsTimeRuleDefinedByTemplate: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, Template?: string, TimeRule?: string, TimeRulePlugIn?: string}
  --MaximumQueueSize: int # format: int32, e.g. 0
  --Name: string # e.g. MyAnalysis
  --OutputTime: string # e.g. 
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\MyElement\Analyses[MyAnalysis]
  --Priority: string # e.g. High
  --PublishResults: oneof<nothing, bool> # e.g. false
  --Status: string # e.g. Disabled
  --TargetWebId: string # format: webid, e.g. I1ETDqD5loBNH0erqeqJodtALAjFPVfUpY-02A8uioGDSgIg
  --TemplateName: string # e.g. 
  --TimeRulePlugInName: string # e.g. Periodic
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XsDqD5loBNH0erqeqJodtALAWDOFEb-U5xGEQwAVXYTCAA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analyses/($webId)")
  let body = {AnalysisRulePlugInName: $AnalysisRulePlugInName, AutoCreated: $AutoCreated, CategoryNames: $CategoryNames, Description: $Description, GroupId: $GroupId, HasNotification: $HasNotification, HasTarget: $HasTarget, HasTemplate: $HasTemplate, Id: $Id, IsConfigured: $IsConfigured, IsTimeRuleDefinedByTemplate: $IsTimeRuleDefinedByTemplate, Links: $Links, MaximumQueueSize: $MaximumQueueSize, Name: $Name, OutputTime: $OutputTime, Path: $Path, Priority: $Priority, PublishResults: $PublishResults, Status: $Status, TargetWebId: $TargetWebId, TemplateName: $TemplateName, TimeRulePlugInName: $TimeRulePlugInName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get an Analysis' categories.
#
# GET /analyses/{webId}/categories
# operationId: Analysis_GetCategories
export def "analyses-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the security information of the specified security item associated with the Analysis for a specified user.
#
# GET /analyses/{webId}/security
# operationId: Analysis_GetSecurity
export def "analyses-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the analysis based on the specified criteria. By default, all security entries for this analysis are returned.
#
# GET /analyses/{webId}/securityentries
# operationId: Analysis_GetSecurityEntries
export def "analyses-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the analysis.
#
# POST /analyses/{webId}/securityentries
# operationId: Analysis_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analyses-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the analysis.
#
# DELETE /analyses/{webId}/securityentries/{name}
# operationId: Analysis_DeleteSecurityEntry
export def "analyses-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the analysis with the specified name.
#
# GET /analyses/{webId}/securityentries/{name}
# operationId: Analysis_GetSecurityEntryByName
export def "analyses-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the analysis.
#
# PUT /analyses/{webId}/securityentries/{name}
# operationId: Analysis_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analyses-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analyses/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an analysis category by path.
#
# GET /analysiscategories
# operationId: AnalysisCategory_GetByPath
export def "analysiscategories GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the target analysis category.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analysiscategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an analysis category.
#
# DELETE /analysiscategories/{webId}
# operationId: AnalysisCategory_Delete
export def "analysiscategories Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analysiscategories/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an analysis category.
#
# GET /analysiscategories/{webId}
# operationId: AnalysisCategory_Get
export def "analysiscategories Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysiscategories/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an analysis category by replacing items in its definition.
#
# PATCH /analysiscategories/{webId}
# operationId: AnalysisCategory_Update
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysiscategories Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. f9364aa2-0e4a-4854-9b85-35900a4d4c0f
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesAnalysis[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XCDqD5loBNH0erqeqJodtALAoko2-UoOVEibhTWQCk1MDw
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analysiscategories/($webId)")
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the analysis category for a specified user.
#
# GET /analysiscategories/{webId}/security
# operationId: AnalysisCategory_GetSecurity
export def "analysiscategories-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysiscategories/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the analysis category based on the specified criteria. By default, all security entries for this analysis category are returned.
#
# GET /analysiscategories/{webId}/securityentries
# operationId: AnalysisCategory_GetSecurityEntries
export def "analysiscategories-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysiscategories/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the analysis category.
#
# POST /analysiscategories/{webId}/securityentries
# operationId: AnalysisCategory_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysiscategories-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysiscategories/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the analysis category.
#
# DELETE /analysiscategories/{webId}/securityentries/{name}
# operationId: AnalysisCategory_DeleteSecurityEntry
export def "analysiscategories-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysiscategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the analysis category with the specified name.
#
# GET /analysiscategories/{webId}/securityentries/{name}
# operationId: AnalysisCategory_GetSecurityEntryByName
export def "analysiscategories-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysiscategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the analysis category.
#
# PUT /analysiscategories/{webId}/securityentries/{name}
# operationId: AnalysisCategory_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysiscategories-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysiscategories/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an Analysis Rule Plug-in by path.
#
# GET /analysisruleplugins
# operationId: AnalysisRulePlugIn_GetByPath
export def "analysisruleplugins GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the Analysis Rule Plug-in.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list<string>, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record<AssetServer: string, Self: string>, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analysisruleplugins" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Analysis Rule Plug-in.
#
# GET /analysisruleplugins/{webId}
# operationId: AnalysisRulePlugIn_Get
export def "analysisruleplugins Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list<string>, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record<AssetServer: string, Self: string>, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysisruleplugins/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Analysis Rule by path.
#
# GET /analysisrules
# operationId: AnalysisRule_GetByPath
export def "analysisrules GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the Analysis Rule.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<ConfigString: string, Description: string, DisplayString: string, EditorType: string, HasChildren: bool, Id: string, IsConfigured: bool, IsInitializing: bool, Links: record<Analysis: string, AnalysisRules: string, AnalysisTemplate: string, Parent: string, PlugIn: string, Self: string>, Name: string, Path: string, PlugInName: string, SupportedBehaviors: list<string>, VariableMapping: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analysisrules" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an Analysis Rule.
#
# DELETE /analysisrules/{webId}
# operationId: AnalysisRule_Delete
export def "analysisrules Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analysisrules/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Analysis Rule.
#
# GET /analysisrules/{webId}
# operationId: AnalysisRule_Get
export def "analysisrules Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<ConfigString: string, Description: string, DisplayString: string, EditorType: string, HasChildren: bool, Id: string, IsConfigured: bool, IsInitializing: bool, Links: record<Analysis: string, AnalysisRules: string, AnalysisTemplate: string, Parent: string, PlugIn: string, Self: string>, Name: string, Path: string, PlugInName: string, SupportedBehaviors: list<string>, VariableMapping: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysisrules/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an Analysis Rule by replacing items in its definition.
#
# PATCH /analysisrules/{webId}
# operationId: AnalysisRule_Update
# --Links shape: {Analysis?: string, AnalysisRules?: string, AnalysisTemplate?: string, Parent?: string, PlugIn?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysisrules Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --ConfigString: string # e.g. a := TagVal('sinusoid'); b := Pow(a, 2); c := a + b;
  --Description: string # e.g. Runs a performance equation.
  --DisplayString: string # e.g. a := TagVal('sinusoid'); b := Pow(a, 2); c := a + b;
  --EditorType: string # e.g. OSIsoft.AF.Analysis.AnalysisRule.PEAnalysisRuleEditor
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. ca9d257f-94c2-11e7-8443-00155d84c200
  --IsConfigured: oneof<nothing, bool> # e.g. false
  --IsInitializing: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Analysis?: string, AnalysisRules?: string, AnalysisTemplate?: string, Parent?: string, PlugIn?: string, Self?: string}
  --Name: string # e.g. PerformanceEquation
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\MyElement\Analyses[MyAnalysis]\AnalysisRule
  --PlugInName: string # e.g. PerformanceEquation
  --SupportedBehaviors: list # e.g. [SupportsRunningCase, SupportStatePassing, OutputCorrectAfterSkipping]
  --VariableMapping: string # e.g. b||Attribute1;c||Attribute2
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XRXDqD5loBNH0erqeqJodtALAfyWdysKU5xGEQwAVXYTCAAfyWdysKU5xGEQwAVXYTCAA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analysisrules/($webId)")
  let body = {ConfigString: $ConfigString, Description: $Description, DisplayString: $DisplayString, EditorType: $EditorType, HasChildren: $HasChildren, Id: $Id, IsConfigured: $IsConfigured, IsInitializing: $IsInitializing, Links: $Links, Name: $Name, Path: $Path, PlugInName: $PlugInName, SupportedBehaviors: $SupportedBehaviors, VariableMapping: $VariableMapping, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the child Analysis Rules of the Analysis Rule.
#
# GET /analysisrules/{webId}/analysisrules
# operationId: AnalysisRule_GetAnalysisRules
export def "analysisrules-analysisrules GetAnalysisRules" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding Analysis Rules. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include Analysis Rules nested further than the immediate children of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<ConfigString: string, Description: string, DisplayString: string, EditorType: string, HasChildren: bool, Id: string, IsConfigured: bool, IsInitializing: bool, Links: record, Name: string, Path: string, PlugInName: string, SupportedBehaviors: list, VariableMapping: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysisrules/($webId)/analysisrules" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new Analysis Rule as a child of an existing Analysis Rule.
#
# POST /analysisrules/{webId}/analysisrules
# operationId: AnalysisRule_CreateAnalysisRule
# --Links shape: {Analysis?: string, AnalysisRules?: string, AnalysisTemplate?: string, Parent?: string, PlugIn?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysisrules-analysisrules CreateAnalysisRule" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --ConfigString: string # e.g. a := TagVal('sinusoid'); b := Pow(a, 2); c := a + b;
  --Description: string # e.g. Runs a performance equation.
  --DisplayString: string # e.g. a := TagVal('sinusoid'); b := Pow(a, 2); c := a + b;
  --EditorType: string # e.g. OSIsoft.AF.Analysis.AnalysisRule.PEAnalysisRuleEditor
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. ca9d257f-94c2-11e7-8443-00155d84c200
  --IsConfigured: oneof<nothing, bool> # e.g. false
  --IsInitializing: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Analysis?: string, AnalysisRules?: string, AnalysisTemplate?: string, Parent?: string, PlugIn?: string, Self?: string}
  --Name: string # e.g. PerformanceEquation
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\MyElement\Analyses[MyAnalysis]\AnalysisRule
  --PlugInName: string # e.g. PerformanceEquation
  --SupportedBehaviors: list # e.g. [SupportsRunningCase, SupportStatePassing, OutputCorrectAfterSkipping]
  --VariableMapping: string # e.g. b||Attribute1;c||Attribute2
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XRXDqD5loBNH0erqeqJodtALAfyWdysKU5xGEQwAVXYTCAAfyWdysKU5xGEQwAVXYTCAA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysisrules/($webId)/analysisrules" $qp)
  let body = {ConfigString: $ConfigString, Description: $Description, DisplayString: $DisplayString, EditorType: $EditorType, HasChildren: $HasChildren, Id: $Id, IsConfigured: $IsConfigured, IsInitializing: $IsInitializing, Links: $Links, Name: $Name, Path: $Path, PlugInName: $PlugInName, SupportedBehaviors: $SupportedBehaviors, VariableMapping: $VariableMapping, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an analysis template by path.
#
# GET /analysistemplates
# operationId: AnalysisTemplate_GetByPath
export def "analysistemplates GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the analysis template.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AnalysisRulePlugInName: string, CategoryNames: list<string>, CreateEnabled: bool, Description: string, GroupId: int, HasNotificationTemplate: bool, HasTarget: bool, Id: string, Links: record<AnalysisRule: string, AnalysisRulePlugIn: string, Categories: string, Database: string, Security: string, SecurityEntries: string, Self: string, Target: string, TimeRule: string, TimeRulePlugIn: string>, Name: string, OutputTime: string, Path: string, TargetName: string, TimeRulePlugInName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analysistemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an Analysis template based upon a specified Analysis.
#
# POST /analysistemplates
# operationId: AnalysisTemplate_CreateFromAnalysis
export def "analysistemplates CreateFromAnalysis" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --analysisWebId: string # The ID of the Analysis, on which the template is created.
  --name: string # The name for the created template, which must be unique within the database's AnalysisTemplate collection. If the name ends with an asterisk (*), then a unique name will be generated based on the supplied name. The default is the specified Analysis' name suffixed with an asterisk (*).
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "analysisWebId" $analysisWebId "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analysistemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve analysis templates based on the specified conditions. By default, returns all analysis templates.
#
# GET /analysistemplates/search
# operationId: AnalysisTemplate_GetAnalysisTemplatesQuery
export def "analysistemplates-search GetAnalysisTemplatesQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --databaseWebId: string # The ID of the asset database to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the analyses in the asset database. An example would be: "query= Name:=MyAnalysisTemplate1*".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AnalysisRulePlugInName: string, CategoryNames: list, CreateEnabled: bool, Description: string, GroupId: int, HasNotificationTemplate: bool, HasTarget: bool, Id: string, Links: record, Name: string, OutputTime: string, Path: string, TargetName: string, TimeRulePlugInName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "databaseWebId" $databaseWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/analysistemplates/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an analysis template.
#
# DELETE /analysistemplates/{webId}
# operationId: AnalysisTemplate_Delete
export def "analysistemplates Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analysistemplates/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an analysis template.
#
# GET /analysistemplates/{webId}
# operationId: AnalysisTemplate_Get
export def "analysistemplates Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AnalysisRulePlugInName: string, CategoryNames: list<string>, CreateEnabled: bool, Description: string, GroupId: int, HasNotificationTemplate: bool, HasTarget: bool, Id: string, Links: record<AnalysisRule: string, AnalysisRulePlugIn: string, Categories: string, Database: string, Security: string, SecurityEntries: string, Self: string, Target: string, TimeRule: string, TimeRulePlugIn: string>, Name: string, OutputTime: string, Path: string, TargetName: string, TimeRulePlugInName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an analysis template by replacing items in its definition.
#
# PATCH /analysistemplates/{webId}
# operationId: AnalysisTemplate_Update
# --Links shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, TimeRule?: string, TimeRulePlugIn?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysistemplates Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --AnalysisRulePlugInName: string # e.g. PerformanceEquation
  --CategoryNames: list # e.g. [MyAnalysisCategory]
  --CreateEnabled: oneof<nothing, bool> # e.g. true
  --Description: string # e.g. 
  --GroupId: int # format: int32, e.g. 0
  --HasNotificationTemplate: oneof<nothing, bool> # e.g. false
  --HasTarget: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 612d9811-3f06-4e27-989c-42e5e5c00b88
  --Links: record # shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, TimeRule?: string, TimeRulePlugIn?: string}
  --Name: string # e.g. MyAnalysisTemplate
  --OutputTime: string # e.g. 
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\AnalysisTemplates[MyAnalysisTemplate]
  --TargetName: string # e.g. MyElementTemplate
  --TimeRulePlugInName: string # e.g. Periodic
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XTG_auSSsvuECG8ad_p8b25QEZgtYQY_J06YnELl5cALiA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/analysistemplates/($webId)")
  let body = {AnalysisRulePlugInName: $AnalysisRulePlugInName, CategoryNames: $CategoryNames, CreateEnabled: $CreateEnabled, Description: $Description, GroupId: $GroupId, HasNotificationTemplate: $HasNotificationTemplate, HasTarget: $HasTarget, Id: $Id, Links: $Links, Name: $Name, OutputTime: $OutputTime, Path: $Path, TargetName: $TargetName, TimeRulePlugInName: $TimeRulePlugInName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get an analysis template's categories.
#
# GET /analysistemplates/{webId}/categories
# operationId: AnalysisTemplate_GetCategories
export def "analysistemplates-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the security information of the specified security item associated with the analysis template for a specified user.
#
# GET /analysistemplates/{webId}/security
# operationId: AnalysisTemplate_GetSecurity
export def "analysistemplates-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the analysis template based on the specified criteria. By default, all security entries for this analysis template are returned.
#
# GET /analysistemplates/{webId}/securityentries
# operationId: AnalysisTemplate_GetSecurityEntries
export def "analysistemplates-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the analysis template.
#
# POST /analysistemplates/{webId}/securityentries
# operationId: AnalysisTemplate_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysistemplates-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the analysis template.
#
# DELETE /analysistemplates/{webId}/securityentries/{name}
# operationId: AnalysisTemplate_DeleteSecurityEntry
export def "analysistemplates-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the analysis template with the specified name.
#
# GET /analysistemplates/{webId}/securityentries/{name}
# operationId: AnalysisTemplate_GetSecurityEntryByName
export def "analysistemplates-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the analysis template.
#
# PUT /analysistemplates/{webId}/securityentries/{name}
# operationId: AnalysisTemplate_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "analysistemplates-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/analysistemplates/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an Asset Database by path.
#
# GET /assetdatabases
# operationId: AssetDatabase_GetByPath
export def "assetdatabases GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the database.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, ExtendedProperties: record, Id: string, Links: record<AnalysisCategories: string, AnalysisTemplates: string, AssetServer: string, AttributeCategories: string, ElementCategories: string, ElementTemplates: string, Elements: string, EnumerationSets: string, EventFrames: string, Security: string, SecurityEntries: string, Self: string, TableCategories: string, Tables: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/assetdatabases" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an asset database.
#
# DELETE /assetdatabases/{webId}
# operationId: AssetDatabase_Delete
export def "assetdatabases Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assetdatabases/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Asset Database.
#
# GET /assetdatabases/{webId}
# operationId: AssetDatabase_Get
export def "assetdatabases Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, ExtendedProperties: record, Id: string, Links: record<AnalysisCategories: string, AnalysisTemplates: string, AssetServer: string, AttributeCategories: string, ElementCategories: string, ElementTemplates: string, Elements: string, EnumerationSets: string, EventFrames: string, Security: string, SecurityEntries: string, Self: string, TableCategories: string, Tables: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an asset database by replacing items in its definition.
#
# PATCH /assetdatabases/{webId}
# operationId: AssetDatabase_Update
# --Links shape: {AnalysisCategories?: string, AnalysisTemplates?: string, AssetServer?: string, AttributeCategories?: string, ElementCategories?: string, ElementTemplates?: string, Elements?: string, EnumerationSets?: string, EventFrames?: string, Security?: string, SecurityEntries?: string, Self?: string, TableCategories?: string, Tables?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. PI BI Project Asset Model
  --ExtendedProperties: record
  --Id: string # format: uuid, e.g. a3a5ebaa-37ae-42dc-9d1e-b6bb7ec98bd2
  --Links: record # shape: {AnalysisCategories?: string, AnalysisTemplates?: string, AssetServer?: string, AttributeCategories?: string, ElementCategories?: string, ElementTemplates?: string, Elements?: string, EnumerationSets?: string, EventFrames?: string, Security?: string, SecurityEntries?: string, Self?: string, TableCategories?: string, Tables?: string}
  --Name: string # e.g. MyDatabase
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1RDDqD5loBNH0erqeqJodtALAquulo6433EKdHra7fsmL0g
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/assetdatabases/($webId)")
  let body = {Description: $Description, ExtendedProperties: $ExtendedProperties, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve analyses based on the specified conditions.
#
# GET /assetdatabases/{webId}/analyses
# operationId: AssetDatabase_FindAnalyses
export def "assetdatabases-analyses FindAnalyses" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --field: list # Specifies which of the object's properties are searched. Multiple search fields may be specified with multiple instances of the parameter. The default is 'Name'.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string used for finding analyses. The default is null.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AnalysisRulePlugInName: string, AutoCreated: bool, CategoryNames: list, Description: string, GroupId: int, HasNotification: bool, HasTarget: bool, HasTemplate: bool, Id: string, IsConfigured: bool, IsTimeRuleDefinedByTemplate: bool, Links: record, MaximumQueueSize: int, Name: string, OutputTime: string, Path: string, Priority: string, PublishResults: bool, Status: string, TargetWebId: string, TemplateName: string, TimeRulePlugInName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field" $field "multi") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/analyses" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve analysis categories for a given Asset Database.
#
# GET /assetdatabases/{webId}/analysiscategories
# operationId: AssetDatabase_GetAnalysisCategories
export def "assetdatabases-analysiscategories GetAnalysisCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/analysiscategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an analysis category at the Asset Database root.
#
# POST /assetdatabases/{webId}/analysiscategories
# operationId: AssetDatabase_CreateAnalysisCategory
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-analysiscategories CreateAnalysisCategory" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. f9364aa2-0e4a-4854-9b85-35900a4d4c0f
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesAnalysis[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XCDqD5loBNH0erqeqJodtALAoko2-UoOVEibhTWQCk1MDw
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/analysiscategories" $qp)
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve analysis templates based on the specified criteria. By default, all analysis templates in the specified Asset Database are returned.
#
# GET /assetdatabases/{webId}/analysistemplates
# operationId: AssetDatabase_GetAnalysisTemplates
export def "assetdatabases-analysistemplates GetAnalysisTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --field: list # Specifies which of the object's properties are searched. Multiple search fields may be specified with multiple instances of the parameter. The default is 'Name'.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string used for finding objects. The default is no query string.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AnalysisRulePlugInName: string, CategoryNames: list, CreateEnabled: bool, Description: string, GroupId: int, HasNotificationTemplate: bool, HasTarget: bool, Id: string, Links: record, Name: string, OutputTime: string, Path: string, TargetName: string, TimeRulePlugInName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field" $field "multi") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/analysistemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an analysis template at the Asset Database root.
#
# POST /assetdatabases/{webId}/analysistemplates
# operationId: AssetDatabase_CreateAnalysisTemplate
# --Links shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, TimeRule?: string, TimeRulePlugIn?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-analysistemplates CreateAnalysisTemplate" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AnalysisRulePlugInName: string # e.g. PerformanceEquation
  --CategoryNames: list # e.g. [MyAnalysisCategory]
  --CreateEnabled: oneof<nothing, bool> # e.g. true
  --Description: string # e.g. 
  --GroupId: int # format: int32, e.g. 0
  --HasNotificationTemplate: oneof<nothing, bool> # e.g. false
  --HasTarget: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 612d9811-3f06-4e27-989c-42e5e5c00b88
  --Links: record # shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, TimeRule?: string, TimeRulePlugIn?: string}
  --Name: string # e.g. MyAnalysisTemplate
  --OutputTime: string # e.g. 
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\AnalysisTemplates[MyAnalysisTemplate]
  --TargetName: string # e.g. MyElementTemplate
  --TimeRulePlugInName: string # e.g. Periodic
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XTG_auSSsvuECG8ad_p8b25QEZgtYQY_J06YnELl5cALiA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/analysistemplates" $qp)
  let body = {AnalysisRulePlugInName: $AnalysisRulePlugInName, CategoryNames: $CategoryNames, CreateEnabled: $CreateEnabled, Description: $Description, GroupId: $GroupId, HasNotificationTemplate: $HasNotificationTemplate, HasTarget: $HasTarget, Id: $Id, Links: $Links, Name: $Name, OutputTime: $OutputTime, Path: $Path, TargetName: $TargetName, TimeRulePlugInName: $TimeRulePlugInName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve attribute categories for a given Asset Database.
#
# GET /assetdatabases/{webId}/attributecategories
# operationId: AssetDatabase_GetAttributeCategories
export def "assetdatabases-attributecategories GetAttributeCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/attributecategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an attribute category at the Asset Database root.
#
# POST /assetdatabases/{webId}/attributecategories
# operationId: AssetDatabase_CreateAttributeCategory
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-attributecategories CreateAttributeCategory" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. 0520f4a1-4454-4fdc-b476-4d371ea6b59e
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesAttribute[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ACDqD5loBNH0erqeqJodtALAofQgBVRE3E-0dk03Hqa1ng
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/attributecategories" $qp)
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieves a list of element attributes matching the specified filters from the specified asset database.
#
# GET /assetdatabases/{webId}/elementattributes
# operationId: AssetDatabase_FindElementAttributes
export def "assetdatabases-elementattributes FindElementAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --attributeCategory: string # Specify that returned attributes must have this category. The default is no filter.
  --attributeDescriptionFilter: string # The attribute description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --attributeNameFilter: string # The attribute name filter string used for finding objects. The default is no filter.
  --attributeType: string # Specify that returned attributes' value type must be this value type. The default is no filter.
  --elementCategory: string # Specify that the owner of the returned attributes must have this category. The default is no filter.
  --elementDescriptionFilter: string # The element description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --elementNameFilter: string # The element name filter string used for finding objects. The default is no filter.
  --elementTemplate: string # Specify that the owner of the returned attributes must have this template or a template derived from this template. The default is no filter.
  --elementType: string # Specify that the element of the returned attributes must have this AFElementType. The default is no filter.
  --maxCount: int # The maximum number of objects to be returned (the page size). The default is 1000.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include objects nested further than immediate children of the given resource. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "attributeCategory" $attributeCategory "scalar") (serialize-qp "attributeDescriptionFilter" $attributeDescriptionFilter "scalar") (serialize-qp "attributeNameFilter" $attributeNameFilter "scalar") (serialize-qp "attributeType" $attributeType "scalar") (serialize-qp "elementCategory" $elementCategory "scalar") (serialize-qp "elementDescriptionFilter" $elementDescriptionFilter "scalar") (serialize-qp "elementNameFilter" $elementNameFilter "scalar") (serialize-qp "elementTemplate" $elementTemplate "scalar") (serialize-qp "elementType" $elementType "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/elementattributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve element categories for a given Asset Database.
#
# GET /assetdatabases/{webId}/elementcategories
# operationId: AssetDatabase_GetElementCategories
export def "assetdatabases-elementcategories GetElementCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/elementcategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an element category at the Asset Database root.
#
# POST /assetdatabases/{webId}/elementcategories
# operationId: AssetDatabase_CreateElementCategory
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-elementcategories CreateElementCategory" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. 3051f943-7e4d-4241-ab9c-450a84c80450
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesElement[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ECDqD5loBNH0erqeqJodtALAQ_lRME1-QUKrnEUKhMgEUA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/elementcategories" $qp)
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve elements based on the specified conditions. By default, this method selects immediate children of the specified asset database.
#
# GET /assetdatabases/{webId}/elements
# operationId: AssetDatabase_GetElements
export def "assetdatabases-elements GetElements" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --categoryName: string # Specify that returned elements must have this category. The default is no category filter.
  --descriptionFilter: string # Specify that returned elements must have this description. The default is no description filter.
  --elementType: string # Specify that returned elements must have this type. The default type is 'Any'.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding objects. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include objects nested further than the immediate children of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --templateName: string # Specify that returned elements must have this template or a template derived from this template. The default is no template filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "descriptionFilter" $descriptionFilter "scalar") (serialize-qp "elementType" $elementType "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/elements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a child element.
#
# POST /assetdatabases/{webId}/elements
# operationId: AssetDatabase_CreateElement
# --Errors item shape: {FieldName?: string, Message?: list}
# --Links shape: {Analyses?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, Elements?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, NotificationRules?: string, Parent?: string, PlotData?: string, RecordedData?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-elements CreateElement" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Equipment Assets]
  --Description: string # e.g. Manufacturing Equipment MachineName
  --Errors: list # item shape: {FieldName?: string, Message?: list}
  --ExtendedProperties: record
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. cbb28260-853d-11e4-80c5-00155d844304
  --Links: record # shape: {Analyses?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, Elements?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, NotificationRules?: string, Parent?: string, PlotData?: string, RecordedData?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
  --Name: string # e.g. MachineName
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName
  --Paths: list
  --TemplateName: string # e.g. MachineName
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1EmDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/elements" $qp)
  let body = {CategoryNames: $CategoryNames, Description: $Description, Errors: $Errors, ExtendedProperties: $ExtendedProperties, HasChildren: $HasChildren, Id: $Id, Links: $Links, Name: $Name, Path: $Path, Paths: $Paths, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve element templates based on the specified criteria. Only templates of instance type "Element" and "EventFrame" are returned. By default, all element and event frame templates in the specified Asset Database are returned.
#
# GET /assetdatabases/{webId}/elementtemplates
# operationId: AssetDatabase_GetElementTemplates
export def "assetdatabases-elementtemplates GetElementTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --field: list # Specifies which of the object's properties are searched. Multiple search fields may be specified with multiple instances of the parameter. The default is 'Name'.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string used for finding objects. The default is no query string.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowElementToExtend: bool, BaseTemplate: string, CanBeAcknowledged: bool, CategoryNames: list, Description: string, ExtendedProperties: record, Id: string, InstanceType: string, Links: record, Name: string, NamingPattern: string, Path: string, Severity: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field" $field "multi") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/elementtemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a template at the Asset Database root. Specify InstanceType of "Element" or "EventFrame" to create element or event frame template respectively. Only these two types of templates can be created.
#
# POST /assetdatabases/{webId}/elementtemplates
# operationId: AssetDatabase_CreateElementTemplate
# --Links shape: {AnalysisTemplates?: string, AttributeTemplates?: string, BaseTemplate?: string, BaseTemplates?: string, Categories?: string, Database?: string, DefaultAttribute?: string, DerivedTemplates?: string, NotificationRuleTemplates?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-elementtemplates CreateElementTemplate" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowElementToExtend: oneof<nothing, bool> # e.g. false
  --BaseTemplate: string # e.g. Equipment
  --CanBeAcknowledged: oneof<nothing, bool> # e.g. false
  --CategoryNames: list # e.g. [Equipment Assets]
  --Description: string # e.g. Manufacturing Machine MachineName
  --ExtendedProperties: record
  --Id: string # format: uuid, e.g. a7989492-b2ab-4d90-8abb-4c8284353dd6
  --InstanceType: string # e.g. Element
  --Links: record # shape: {AnalysisTemplates?: string, AttributeTemplates?: string, BaseTemplate?: string, BaseTemplates?: string, Categories?: string, Database?: string, DefaultAttribute?: string, DerivedTemplates?: string, NotificationRuleTemplates?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. MachineName
  --NamingPattern: string # e.g. %TEMPLATE%
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\ElementTemplates[Boiler]
  --Severity: string # e.g. None
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ETDqD5loBNH0erqeqJodtALAkpSYp6uykE2Ku0yChDU91g
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/elementtemplates" $qp)
  let body = {AllowElementToExtend: $AllowElementToExtend, BaseTemplate: $BaseTemplate, CanBeAcknowledged: $CanBeAcknowledged, CategoryNames: $CategoryNames, Description: $Description, ExtendedProperties: $ExtendedProperties, Id: $Id, InstanceType: $InstanceType, Links: $Links, Name: $Name, NamingPattern: $NamingPattern, Path: $Path, Severity: $Severity, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve enumeration sets for given asset database.
#
# GET /assetdatabases/{webId}/enumerationsets
# operationId: AssetDatabase_GetEnumerationSets
export def "assetdatabases-enumerationsets GetEnumerationSets" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, SerializeDescription: bool, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/enumerationsets" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an enumeration set at the Asset Database.
#
# POST /assetdatabases/{webId}/enumerationsets
# operationId: AssetDatabase_CreateEnumerationSet
# --Links shape: {DataServer?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Values?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-enumerationsets CreateEnumerationSet" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. Model numbers by brand of vehicle
  --Id: string # format: uuid, e.g. 8e77fc4f-ac91-4029-81d6-fb66bd01d431
  --Links: record # shape: {DataServer?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Values?: string}
  --Name: string # e.g. Model Number
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EnumerationSets[Model Number]
  --SerializeDescription: oneof<nothing, bool>
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1MSRDqD5loBNH0erqeqJodtALAT_x3jpGsKUCB1vtmvQHUMQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/enumerationsets" $qp)
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, SerializeDescription: $SerializeDescription, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieves a list of event frame attributes matching the specified filters from the specified asset database.
#
# GET /assetdatabases/{webId}/eventframeattributes
# operationId: AssetDatabase_FindEventFrameAttributes
export def "assetdatabases-eventframeattributes FindEventFrameAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --attributeCategory: string # Specify that returned attributes must have this category. The default is no filter.
  --attributeDescriptionFilter: string # The attribute description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --attributeNameFilter: string # The attribute name filter string used for finding objects. The default is no filter.
  --attributeType: string # Specify that returned attributes' value type must be this value type. The default is no filter.
  --endTime: string # A string representing the latest ending time for the event frames to be matched. The endTime must be greater than or equal to the startTime. The default is '*'.
  --eventFrameCategory: string # Specify that the owner of the returned attributes must have this category. The default is no filter.
  --eventFrameDescriptionFilter: string # The event frame description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --eventFrameNameFilter: string # The event frame name filter string used for finding objects. The default is no filter.
  --eventFrameTemplate: string # Specify that the owner of the returned attributes must have this template or a template derived from this template. The default is no filter.
  --maxCount: int # The maximum number of objects to be returned (the page size). The default is 1000.
  --referencedElementNameFilter: string # The name query string which must match the name of a referenced element. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include objects nested further than immediate children of the given resource. The default is 'false'.
  --searchMode: string # Determines how the startTime and endTime parameters are treated when searching for event frames. The default is 'Overlapped'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --startTime: string # A string representing the earliest starting time for the event frames to be matched. startTime must be less than or equal to the endTime. The default is '*-8h'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "attributeCategory" $attributeCategory "scalar") (serialize-qp "attributeDescriptionFilter" $attributeDescriptionFilter "scalar") (serialize-qp "attributeNameFilter" $attributeNameFilter "scalar") (serialize-qp "attributeType" $attributeType "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "eventFrameCategory" $eventFrameCategory "scalar") (serialize-qp "eventFrameDescriptionFilter" $eventFrameDescriptionFilter "scalar") (serialize-qp "eventFrameNameFilter" $eventFrameNameFilter "scalar") (serialize-qp "eventFrameTemplate" $eventFrameTemplate "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "referencedElementNameFilter" $referencedElementNameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "searchMode" $searchMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/eventframeattributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve event frames based on the specified conditions. By default, returns all children of the specified root resource that have been active in the past 8 hours.
#
# GET /assetdatabases/{webId}/eventframes
# operationId: AssetDatabase_GetEventFrames
export def "assetdatabases-eventframes GetEventFrames" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --canBeAcknowledged: oneof<nothing, bool> # Specify the returned event frames' canBeAcknowledged property. The default is no canBeAcknowledged filter.
  --categoryName: string # Specify that returned event frames must have this category. The default is no category filter.
  --endTime: string # The ending time for the search. The endTime must be greater than or equal to the startTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --isAcknowledged: oneof<nothing, bool> # Specify the returned event frames' isAcknowledged property. The default no isAcknowledged filter.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding event frames. The default is no filter.
  --referencedElementNameFilter: string # The name query string which must match the name of a referenced element. The default is no filter.
  --referencedElementTemplateName: string # Specify that returned event frames must have an element in the event frame's referenced elements collection that derives from the template. Specify this parameter by name.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies whether the search should include objects nested further than the immediate children of the search root. The default is 'false'.
  --searchMode: string # Determines how the startTime and endTime parameters are treated when searching for event frame objects to be included in the returned collection. If this parameter is one of the 'Backward*' or 'Forward*' values, none of endTime, sortField, or sortOrder may be specified. The default is 'Overlapped'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --severity: list # Specify that returned event frames must have this severity. Multiple severity values may be specified with multiple instances of the parameter. The default is no severity filter.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --startTime: string # The starting time for the search. startTime must be less than or equal to the endTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*-8h'.
  --templateName: string # Specify that returned event frames must have this template or a template derived from this template. The default is no template filter. Specify this parameter by name.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record, Name: string, Path: string, RefElementWebIds: list, Security: record, Severity: string, StartTime: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "canBeAcknowledged" $canBeAcknowledged "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "isAcknowledged" $isAcknowledged "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "referencedElementNameFilter" $referencedElementNameFilter "scalar") (serialize-qp "referencedElementTemplateName" $referencedElementTemplateName "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "searchMode" $searchMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "severity" $severity "multi") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/eventframes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an event frame.
#
# POST /assetdatabases/{webId}/eventframes
# operationId: AssetDatabase_CreateEventFrame
# --Links shape: {Annotations?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, PrimaryReferencedElement?: string, RecordedData?: string, ReferencedElements?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
# --Security shape: {CanAnnotate?: bool, CanDelete?: bool, CanExecute?: bool, CanRead?: bool, CanReadData?: bool, CanSubscribe?: bool, CanSubscribeOthers?: bool, CanWrite?: bool, CanWriteData?: bool, HasAdmin?: bool, Rights?: list, WebException?: record}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-eventframes CreateEventFrame" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AcknowledgedBy: string # e.g. MyDomain\UserA
  --AcknowledgedDate: string # format: date-time, e.g. 2014-07-30T11:04:23Z
  --AreValuesCaptured: oneof<nothing, bool> # e.g. false
  --CanBeAcknowledged: oneof<nothing, bool> # e.g. true
  --CategoryNames: list # e.g. [Processing Plant]
  --Description: string # e.g. Event Frame of Past Week
  --EndTime: string # format: date-time, e.g. 2014-07-25T14:45:29Z
  --ExtendedProperties: record
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 96f9a00e-4d80-471f-0000-00000004b002
  --IsAcknowledged: oneof<nothing, bool> # e.g. true
  --IsAnnotated: oneof<nothing, bool> # e.g. false
  --IsLocked: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Annotations?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, PrimaryReferencedElement?: string, RecordedData?: string, ReferencedElements?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
  --Name: string # e.g. EF20140725-001
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EventFrames[EF20140725-001]
  --RefElementWebIds: list # e.g. [I1EmDqD5loBNH0erqeqJodtALAaqQoQHk26BGgMQAVXYR0Ag]
  --Security: record # shape: {CanAnnotate?: bool, CanDelete?: bool, CanExecute?: bool, CanRead?: bool, CanReadData?: bool, CanSubscribe?: bool, CanSubscribeOthers?: bool, CanWrite?: bool, CanWriteData?: bool, HasAdmin?: bool, Rights?: list, WebException?: record}
  --Severity: string # e.g. None
  --StartTime: string # format: date-time, e.g. 2014-07-18T14:45:29Z
  --TemplateName: string # e.g. Template
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1FmDqD5loBNH0erqeqJodtALADqD5loBNH0cAAAAAAASwAg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/eventframes" $qp)
  let body = {AcknowledgedBy: $AcknowledgedBy, AcknowledgedDate: $AcknowledgedDate, AreValuesCaptured: $AreValuesCaptured, CanBeAcknowledged: $CanBeAcknowledged, CategoryNames: $CategoryNames, Description: $Description, EndTime: $EndTime, ExtendedProperties: $ExtendedProperties, HasChildren: $HasChildren, Id: $Id, IsAcknowledged: $IsAcknowledged, IsAnnotated: $IsAnnotated, IsLocked: $IsLocked, Links: $Links, Name: $Name, Path: $Path, RefElementWebIds: $RefElementWebIds, Security: $Security, Severity: $Severity, StartTime: $StartTime, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Export the asset database.
#
# GET /assetdatabases/{webId}/export
# operationId: AssetDatabase_Export
export def "assetdatabases-export Export" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --endTime: string # The latest ending time for AFEventFrame, AFTransfer, and AFCase objects that may be part of the export. The default is '*'.
  --exportMode: list # Indicates the type of export to perform. The default is 'StrongReferences'. Multiple export modes may be specified by using multiple instances of exportMode.
  --startTime: string # The earliest starting time for AFEventFrame, AFTransfer, and AFCase objects that may be part of the export. The default is '*-30d'.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "endTime" $endTime "scalar") (serialize-qp "exportMode" $exportMode "multi") (serialize-qp "startTime" $startTime "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/export" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Import an asset database.
#
# POST /assetdatabases/{webId}/import
# operationId: AssetDatabase_Import
export def "assetdatabases-import Import" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --importMode: list # Indicates the type of import to perform. The default is 'AllowCreate | AllowUpdate | AutoCheckIn'. Multiple import modes may be specified by using multiple instances of importMode.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "importMode" $importMode "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/import" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a reference to an existing element from the specified database.
#
# DELETE /assetdatabases/{webId}/referencedelements
# operationId: AssetDatabase_RemoveReferencedElement
export def "assetdatabases-referencedelements RemoveReferencedElement" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --referencedElementWebId: list # The ID of the referenced element. Multiple referenced elements may be specified with multiple instances of the parameter.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referencedElementWebId" $referencedElementWebId "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/referencedelements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve referenced elements based on the specified conditions. By default, this method selects all referenced elements at the root level of the asset database.
#
# GET /assetdatabases/{webId}/referencedelements
# operationId: AssetDatabase_GetReferencedElements
export def "assetdatabases-referencedelements GetReferencedElements" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --categoryName: string # Specify that returned elements must have this category. The default is no category filter.
  --descriptionFilter: string # Specify that returned elements must have this description. The default is no description filter.
  --elementType: string # Specify that returned elements must have this type. The default type is 'Any'.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding objects. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --templateName: string # Specify that returned elements must have this template or a template derived from this template. The default is no template filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "descriptionFilter" $descriptionFilter "scalar") (serialize-qp "elementType" $elementType "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/referencedelements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a reference to an existing element to the specified database.
#
# POST /assetdatabases/{webId}/referencedelements
# operationId: AssetDatabase_AddReferencedElement
export def "assetdatabases-referencedelements AddReferencedElement" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --referencedElementWebId: list # The ID of the referenced element. Multiple referenced elements may be specified with multiple instances of the parameter.
  --referenceType: string # The name of the reference type between the parent and the referenced element. This must be a "strong" reference type. The default is "parent-child".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referencedElementWebId" $referencedElementWebId "multi") (serialize-qp "referenceType" $referenceType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/referencedelements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the security information of the specified security item associated with the asset database for a specified user.
#
# GET /assetdatabases/{webId}/security
# operationId: AssetDatabase_GetSecurity
export def "assetdatabases-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --securityItem: list # The security item of the desired security information to be returned. Multiple security items may be specified with multiple instances of the parameter. If the parameter is not specified, only 'Default' security item of the security information will be returned.
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "securityItem" $securityItem "multi") (serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries of the specified security item associated with the asset database based on the specified criteria. By default, all security entries for this asset database are returned.
#
# GET /assetdatabases/{webId}/securityentries
# operationId: AssetDatabase_GetSecurityEntries
export def "assetdatabases-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --securityItem: string # The security item of the desired security entries information to be returned. If the parameter is not specified, security entries of the 'Default' security item will be returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "securityItem" $securityItem "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the asset database.
#
# POST /assetdatabases/{webId}/securityentries
# operationId: AssetDatabase_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --securityItem: string # The security item of the desired security entries to be created. If the parameter is not specified, security entries of the 'Default' security item will be created.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "securityItem" $securityItem "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the asset database.
#
# DELETE /assetdatabases/{webId}/securityentries/{name}
# operationId: AssetDatabase_DeleteSecurityEntry
export def "assetdatabases-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --securityItem: string # The security item of the desired security entries to be deleted. If the parameter is not specified, security entries of the 'Default' security item will be deleted.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "securityItem" $securityItem "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry of the specified security item associated with the asset database with the specified name.
#
# GET /assetdatabases/{webId}/securityentries/{name}
# operationId: AssetDatabase_GetSecurityEntryByName
export def "assetdatabases-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --securityItem: string # The security item of the desired security entries information to be returned. If the parameter is not specified, security entries of the 'Default' security item will be returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "securityItem" $securityItem "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the asset database.
#
# PUT /assetdatabases/{webId}/securityentries/{name}
# operationId: AssetDatabase_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --securityItem: string # The security item of the desired security entries to be updated. If the parameter is not specified, security entries of the 'Default' security item will be updated.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "securityItem" $securityItem "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve table categories for a given Asset Database.
#
# GET /assetdatabases/{webId}/tablecategories
# operationId: AssetDatabase_GetTableCategories
export def "assetdatabases-tablecategories GetTableCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/tablecategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a table category on the Asset Database.
#
# POST /assetdatabases/{webId}/tablecategories
# operationId: AssetDatabase_CreateTableCategory
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-tablecategories CreateTableCategory" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. 89c70cc2-5221-4e77-873f-894a3ea61e7d
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesTable[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1BCDqD5loBNH0erqeqJodtALAwgzHiSFSd06HP4lKPqYefQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/tablecategories" $qp)
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve tables for given Asset Database.
#
# GET /assetdatabases/{webId}/tables
# operationId: AssetDatabase_GetTables
export def "assetdatabases-tables GetTables" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConvertToLocalTime: bool, Description: string, Id: string, Links: record, Name: string, Path: string, TimeZone: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/tables" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a table on the Asset Database.
#
# POST /assetdatabases/{webId}/tables
# operationId: AssetDatabase_CreateTable
# --Links shape: {Categories?: string, Data?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetdatabases-tables CreateTable" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Table Category]
  --ConvertToLocalTime: oneof<nothing, bool> # e.g. false
  --Description: string # e.g. Table of car info.
  --Id: string # format: uuid, e.g. 5ff8ba98-89ce-479a-b29e-ddd057096a5f
  --Links: record # shape: {Categories?: string, Data?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CarInfo
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\Tables[CarInfo]
  --TimeZone: string # e.g. Eastern Standard Time
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1BlDqD5loBNH0erqeqJodtALAmLr4X86Jmkeynt3QVwlqXw
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetdatabases/($webId)/tables" $qp)
  let body = {CategoryNames: $CategoryNames, ConvertToLocalTime: $ConvertToLocalTime, Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, TimeZone: $TimeZone, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a list of all Asset Servers known to this service.
#
# GET /assetservers
# operationId: AssetServer_List
export def "assetservers List" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, ExtendedProperties: record, Id: string, IsConnected: bool, Links: record, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/assetservers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Asset Server by name.
#
# GET /assetservers#name
# operationId: AssetServer_GetByName
export def "assetserversname GetByName" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --name: string # The name of the server.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, ExtendedProperties: record, Id: string, IsConnected: bool, Links: record<AnalysisRulePlugIns: string, Databases: string, NotificationContactTemplates: string, NotificationPlugIns: string, Security: string, SecurityEntries: string, SecurityIdentities: string, SecurityMappings: string, Self: string, TimeRulePlugIns: string, UnitClasses: string>, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/assetservers#name" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Asset Server by path.
#
# GET /assetservers#path
# operationId: AssetServer_GetByPath
export def "assetserverspath GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the server.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, ExtendedProperties: record, Id: string, IsConnected: bool, Links: record<AnalysisRulePlugIns: string, Databases: string, NotificationContactTemplates: string, NotificationPlugIns: string, Security: string, SecurityEntries: string, SecurityIdentities: string, SecurityMappings: string, Self: string, TimeRulePlugIns: string, UnitClasses: string>, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/assetservers#path" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an Asset Server.
#
# GET /assetservers/{webId}
# operationId: AssetServer_Get
export def "assetservers Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, ExtendedProperties: record, Id: string, IsConnected: bool, Links: record<AnalysisRulePlugIns: string, Databases: string, NotificationContactTemplates: string, NotificationPlugIns: string, Security: string, SecurityEntries: string, SecurityIdentities: string, SecurityMappings: string, Self: string, TimeRulePlugIns: string, UnitClasses: string>, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a list of all Analysis Rule Plug-in's.
#
# GET /assetservers/{webId}/analysisruleplugins
# operationId: AssetServer_GetAnalysisRulePlugIns
export def "assetservers-analysisruleplugins GetAnalysisRulePlugIns" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/analysisruleplugins" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a list of all Asset Databases on the specified Asset Server.
#
# GET /assetservers/{webId}/assetdatabases
# operationId: AssetServer_GetDatabases
export def "assetservers-assetdatabases GetDatabases" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, ExtendedProperties: record, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/assetdatabases" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an asset database.
#
# POST /assetservers/{webId}/assetdatabases
# operationId: AssetServer_CreateAssetDatabase
# --Links shape: {AnalysisCategories?: string, AnalysisTemplates?: string, AssetServer?: string, AttributeCategories?: string, ElementCategories?: string, ElementTemplates?: string, Elements?: string, EnumerationSets?: string, EventFrames?: string, Security?: string, SecurityEntries?: string, Self?: string, TableCategories?: string, Tables?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetservers-assetdatabases CreateAssetDatabase" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. PI BI Project Asset Model
  --ExtendedProperties: record
  --Id: string # format: uuid, e.g. a3a5ebaa-37ae-42dc-9d1e-b6bb7ec98bd2
  --Links: record # shape: {AnalysisCategories?: string, AnalysisTemplates?: string, AssetServer?: string, AttributeCategories?: string, ElementCategories?: string, ElementTemplates?: string, Elements?: string, EnumerationSets?: string, EventFrames?: string, Security?: string, SecurityEntries?: string, Self?: string, TableCategories?: string, Tables?: string}
  --Name: string # e.g. MyDatabase
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1RDDqD5loBNH0erqeqJodtALAquulo6433EKdHra7fsmL0g
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/assetdatabases" $qp)
  let body = {Description: $Description, ExtendedProperties: $ExtendedProperties, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a list of all notification contact templates on the specified Asset Server.
#
# GET /assetservers/{webId}/notificationcontacttemplates
# operationId: AssetServer_GetNotificationContactTemplates
export def "assetservers-notificationcontacttemplates GetNotificationContactTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Available: bool, ConfigString: string, ContactType: string, Description: string, EscalationTimeout: string, HasChildren: bool, Id: string, Links: record, MaximumRetries: int, MinimumAcknowledgements: int, Name: string, NotifyWhenInstanceEnded: bool, Path: string, PlugInName: string, RetryInterval: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/notificationcontacttemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a notification contact template.
#
# POST /assetservers/{webId}/notificationcontacttemplates
# operationId: AssetServer_CreateNotificationContactTemplate
# --Links shape: {AssetServer?: string, NotificationContactTemplates?: string, NotificationPlugIn?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetservers-notificationcontacttemplates CreateNotificationContactTemplate" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Available: oneof<nothing, bool> # e.g. true
  --ConfigString: string # e.g. 
  --ContactType: string # e.g. Individual
  --Description: string # e.g. 
  --EscalationTimeout: string # format: Duration, e.g. PT3S
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. cbb28260-853d-11e4-80c5-00155d844304
  --Links: record # shape: {AssetServer?: string, NotificationContactTemplates?: string, NotificationPlugIn?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --MaximumRetries: int # e.g. 5
  --MinimumAcknowledgements: int # e.g. 2
  --Name: string # e.g. Plant Manager
  --NotifyWhenInstanceEnded: oneof<nothing, bool> # e.g. true
  --Path: string # format: path, e.g. \\MyAssetServer\NotificationContactTemplates[Plant Manager]
  --PlugInName: string # e.g. 
  --RetryInterval: string # format: Duration, e.g. PT5S
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NCEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/notificationcontacttemplates" $qp)
  let body = {Available: $Available, ConfigString: $ConfigString, ContactType: $ContactType, Description: $Description, EscalationTimeout: $EscalationTimeout, HasChildren: $HasChildren, Id: $Id, Links: $Links, MaximumRetries: $MaximumRetries, MinimumAcknowledgements: $MinimumAcknowledgements, Name: $Name, NotifyWhenInstanceEnded: $NotifyWhenInstanceEnded, Path: $Path, PlugInName: $PlugInName, RetryInterval: $RetryInterval, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a list of all notification plugins on the specified Asset Server.
#
# GET /assetservers/{webId}/notificationplugins
# operationId: AssetServer_GetNotificationPlugIns
export def "assetservers-notificationplugins GetNotificationPlugIns" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/notificationplugins" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the security information of the specified security item associated with the asset server for a specified user.
#
# GET /assetservers/{webId}/security
# operationId: AssetServer_GetSecurity
export def "assetservers-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --securityItem: list # The security item of the desired security information to be returned. Multiple security items may be specified with multiple instances of the parameter. If the parameter is not specified, only 'Default' security item of the security information will be returned.
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "securityItem" $securityItem "multi") (serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries of the specified security item associated with the asset server based on the specified criteria. By default, all security entries for this asset server are returned.
#
# GET /assetservers/{webId}/securityentries
# operationId: AssetServer_GetSecurityEntries
export def "assetservers-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --securityItem: string # The security item of the desired security entries information to be returned. If the parameter is not specified, security entries of the 'Default' security item will be returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "securityItem" $securityItem "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the asset server.
#
# POST /assetservers/{webId}/securityentries
# operationId: AssetServer_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetservers-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --securityItem: string # The security item of the desired security entries to be created. If the parameter is not specified, security entries of the 'Default' security item will be created.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "securityItem" $securityItem "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the asset server.
#
# DELETE /assetservers/{webId}/securityentries/{name}
# operationId: AssetServer_DeleteSecurityEntry
export def "assetservers-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --securityItem: string # The security item of the desired security entries to be deleted. If the parameter is not specified, security entries of the 'Default' security item will be deleted.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "securityItem" $securityItem "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry of the specified security item associated with the asset server with the specified name.
#
# GET /assetservers/{webId}/securityentries/{name}
# operationId: AssetServer_GetSecurityEntryByName
export def "assetservers-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --securityItem: string # The security item of the desired security entries information to be returned. If the parameter is not specified, security entries of the 'Default' security item will be returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "securityItem" $securityItem "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the asset server.
#
# PUT /assetservers/{webId}/securityentries/{name}
# operationId: AssetServer_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetservers-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --securityItem: string # The security item of the desired security entries to be updated. If the parameter is not specified, security entries of the 'Default' security item will be updated.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "securityItem" $securityItem "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve security identities based on the specified criteria. By default, all security identities in the specified Asset Server are returned.
#
# GET /assetservers/{webId}/securityidentities
# operationId: AssetServer_GetSecurityIdentities
export def "assetservers-securityidentities GetSecurityIdentities" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --field: string # Specifies which of the object's properties are searched. The default is 'Name'.
  --maxCount: int # The maximum number of objects to be returned. The default is 1000.
  --query: string # The query string used for finding objects. The default is no query string.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, IsEnabled: bool, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field" $field "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityidentities" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security identity.
#
# POST /assetservers/{webId}/securityidentities
# operationId: AssetServer_CreateSecurityIdentity
# --Links shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityMappings?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetservers-securityidentities CreateSecurityIdentity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. 
  --Id: string # format: uuid, e.g. f3a5ee49-1838-4aa2-9d79-e16294523db7
  --IsEnabled: oneof<nothing, bool> # e.g. true
  --Links: record # shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityMappings?: string, Self?: string}
  --Name: string # e.g. MySecurityIdentity
  --Path: string # format: path, e.g. \\MyAssetServer\SecurityIdentities[MySecurityIdentity]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1SIDqD5loBNH0erqeqJodtALASe6l8zgYokqdeeFilFI9tw
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityidentities" $qp)
  let body = {Description: $Description, Id: $Id, IsEnabled: $IsEnabled, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve security identities for a specific user.
#
# GET /assetservers/{webId}/securityidentities#userIdentity
# operationId: AssetServer_GetSecurityIdentitiesForUser
export def "assetservers-securityidentitiesuser-identity GetSecurityIdentitiesForUser" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: string # The user identity to search for.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, IsEnabled: bool, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securityidentities#userIdentity" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve security mappings based on the specified criteria. By default, all security mappings in the specified Asset Server are returned.
#
# GET /assetservers/{webId}/securitymappings
# operationId: AssetServer_GetSecurityMappings
export def "assetservers-securitymappings GetSecurityMappings" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --field: string # Specifies which of the object's properties are searched. The default is 'Name'.
  --maxCount: int # The maximum number of objects to be returned. The default is 1000.
  --query: string # The query string used for finding objects. The default is no query string.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Account: string, Description: string, Id: string, Links: record, Name: string, Path: string, SecurityIdentityWebId: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "field" $field "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securitymappings" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security mapping.
#
# POST /assetservers/{webId}/securitymappings
# operationId: AssetServer_CreateSecurityMapping
# --Links shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetservers-securitymappings CreateSecurityMapping" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Account: string # e.g. domain\user
  --Description: string # e.g. 
  --Id: string # format: uuid, e.g. ac14ef82-19c0-4a07-96a7-d1fb0b84c05d
  --Links: record # shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. MySecurityMapping
  --Path: string # format: path, e.g. \\MyAssetServer\SecurityMappings[MySecurityMapping]
  --SecurityIdentityWebId: string # e.g. I1SIEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1SMDqD5loBNH0erqeqJodtALAgu8UrMAZB0qWp9H7C4TAXQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/securitymappings" $qp)
  let body = {Account: $Account, Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, SecurityIdentityWebId: $SecurityIdentityWebId, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a list of all Time Rule Plug-in's.
#
# GET /assetservers/{webId}/timeruleplugins
# operationId: AssetServer_GetTimeRulePlugIns
export def "assetservers-timeruleplugins GetTimeRulePlugIns" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/timeruleplugins" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a list of all unit classes on the specified Asset Server.
#
# GET /assetservers/{webId}/unitclasses
# operationId: AssetServer_GetUnitClasses
export def "assetservers-unitclasses GetUnitClasses" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanonicalUnitAbbreviation: string, CanonicalUnitName: string, Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/unitclasses" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a unit class in the specified Asset Server.
#
# POST /assetservers/{webId}/unitclasses
# operationId: AssetServer_CreateUnitClass
# --Links shape: {AssetServer?: string, CanonicalUnit?: string, Self?: string, Units?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "assetservers-unitclasses CreateUnitClass" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CanonicalUnitAbbreviation: string # e.g. W
  --CanonicalUnitName: string # e.g. watt
  --Description: string # e.g. Power Unit Class
  --Id: string # format: uuid, e.g. f925b94d-6efc-4b94-a50d-0015c391f249
  --Links: record # shape: {AssetServer?: string, CanonicalUnit?: string, Self?: string, Units?: string}
  --Name: string # e.g. Power
  --Path: string # format: path, e.g. \\MyAssetServer\UOMDatabase\UOMClasses[Power]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1UCDqD5loBNH0erqeqJodtALATbkl-fxulEulDQAVw5HySQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/assetservers/($webId)/unitclasses" $qp)
  let body = {CanonicalUnitAbbreviation: $CanonicalUnitAbbreviation, CanonicalUnitName: $CanonicalUnitName, Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an attribute category by path.
#
# GET /attributecategories
# operationId: AttributeCategory_GetByPath
export def "attributecategories GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the target attribute category.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/attributecategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an attribute category.
#
# DELETE /attributecategories/{webId}
# operationId: AttributeCategory_Delete
export def "attributecategories Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/attributecategories/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an attribute category.
#
# GET /attributecategories/{webId}
# operationId: AttributeCategory_Get
export def "attributecategories Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributecategories/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an attribute category by replacing items in its definition.
#
# PATCH /attributecategories/{webId}
# operationId: AttributeCategory_Update
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributecategories Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. 0520f4a1-4454-4fdc-b476-4d371ea6b59e
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesAttribute[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ACDqD5loBNH0erqeqJodtALAofQgBVRE3E-0dk03Hqa1ng
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/attributecategories/($webId)")
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the attribute category for a specified user.
#
# GET /attributecategories/{webId}/security
# operationId: AttributeCategory_GetSecurity
export def "attributecategories-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributecategories/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the attribute category based on the specified criteria. By default, all security entries for this attribute category are returned.
#
# GET /attributecategories/{webId}/securityentries
# operationId: AttributeCategory_GetSecurityEntries
export def "attributecategories-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributecategories/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the attribute category.
#
# POST /attributecategories/{webId}/securityentries
# operationId: AttributeCategory_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributecategories-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributecategories/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the attribute category.
#
# DELETE /attributecategories/{webId}/securityentries/{name}
# operationId: AttributeCategory_DeleteSecurityEntry
export def "attributecategories-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributecategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the attribute category with the specified name.
#
# GET /attributecategories/{webId}/securityentries/{name}
# operationId: AttributeCategory_GetSecurityEntryByName
export def "attributecategories-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributecategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the attribute category.
#
# PUT /attributecategories/{webId}/securityentries/{name}
# operationId: AttributeCategory_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributecategories-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributecategories/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an attribute by path.
#
# GET /attributes
# operationId: Attribute_GetByPath
export def "attributes GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the attribute.
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, ConfigString: string, DataReference: record<PIPoint: record<Descriptor: string, DigitalSetName: string, DisplayDigits: int, EngineeringUnits: string, Future: bool, Id: int, Name: string, Path: string, PointClass: string, PointType: string, Span: float, Step: bool, WebId: string, Zero: float>, Type: string, WebException: record<Errors: list, StatusCode: int>>, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record<Attributes: string, Categories: string, Element: string, EndValue: string, EnumerationSet: string, EnumerationValues: string, EventFrame: string, InterpolatedData: string, Parent: string, PlotData: string, Point: string, RecordedData: string, Self: string, SummaryData: string, Template: string, Trait: string, Value: string>, Name: string, Path: string, Paths: list<string>, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string, Zero: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "associations" $associations "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/attributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve multiple attributes by web id or path.
#
# GET /attributes/multiple
# operationId: Attribute_GetMultiple
export def "attributes-multiple GetMultiple" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --asParallel: oneof<nothing, bool> # Specifies if the retrieval processes should be run in parallel on the server. This may improve the response time for large amounts of requested attributes. The default is 'false'.
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --includeMode: string # The include mode for the return list. The default is 'All'.
  --path: list # The path of an attribute. Multiple attributes may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webId: list # The ID of an attribute. Multiple attributes may be specified with multiple instances of the parameter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Exception: record, Identifier: string, IdentifierType: string, Object: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "asParallel" $asParallel "scalar") (serialize-qp "associations" $associations "scalar") (serialize-qp "includeMode" $includeMode "scalar") (serialize-qp "path" $path "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webId" $webId "multi") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/attributes/multiple" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve attributes based on the specified conditions. Returns attributes using the specified search query string.
#
# GET /attributes/search
# operationId: Attribute_GetAttributesQuery
export def "attributes-search GetAttributesQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --databaseWebId: string # The ID of the asset database to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the attributes in the asset database. An example would be: "query=Element:{ Name:='MyElement' } Type:=Int32".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "databaseWebId" $databaseWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/attributes/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an attribute.
#
# DELETE /attributes/{webId}
# operationId: Attribute_Delete
export def "attributes Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/attributes/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an attribute.
#
# GET /attributes/{webId}
# operationId: Attribute_Get
export def "attributes Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, ConfigString: string, DataReference: record<PIPoint: record<Descriptor: string, DigitalSetName: string, DisplayDigits: int, EngineeringUnits: string, Future: bool, Id: int, Name: string, Path: string, PointClass: string, PointType: string, Span: float, Step: bool, WebId: string, Zero: float>, Type: string, WebException: record<Errors: list, StatusCode: int>>, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record<Attributes: string, Categories: string, Element: string, EndValue: string, EnumerationSet: string, EnumerationValues: string, EventFrame: string, InterpolatedData: string, Parent: string, PlotData: string, Point: string, RecordedData: string, Self: string, SummaryData: string, Template: string, Trait: string, Value: string>, Name: string, Path: string, Paths: list<string>, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string, Zero: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributes/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an attribute by replacing items in its definition.
#
# PATCH /attributes/{webId}
# operationId: Attribute_Update
# --DataReference shape: {PIPoint?: record, Type?: string, WebException?: record}
# --Links shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributes Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --CategoryNames: list # e.g. [Energy Savings Targets]
  --ConfigString: string # e.g. SELECT [Water Use] FROM [Energy Use 2008] WHERE [Asset ID] = '%Element%'
  --DataReference: record # shape: {PIPoint?: record, Type?: string, WebException?: record}
  --DataReferencePlugIn: string # e.g. Table Lookup
  --DefaultUnitsName: string # e.g. liter
  --DefaultUnitsNameAbbreviation: string # e.g. L
  --Description: string # e.g. 2008 Water Use
  --DisplayDigits: int # e.g. -5
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 75de231b-b591-49b1-b138-e725a77165c2
  --IsConfigurationItem: oneof<nothing, bool> # e.g. false
  --IsExcluded: oneof<nothing, bool> # e.g. false
  --IsHidden: oneof<nothing, bool> # e.g. false
  --IsManualDataEntry: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
  --Name: string # e.g. Water
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName|Water(2008)
  --Paths: list # e.g. [\\MyAssetServer\MyDatabase\MyElement|MyAttribute, \\MyAssetServer\MyDatabase\ReferencingElement\MyElement|MyAttribute]
  --Span: float # e.g. 100
  --Step: oneof<nothing, bool> # e.g. false
  --TraitName: string # e.g. LimitLoLo
  --Type: string # e.g. Int32
  --TypeQualifier: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1AbEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
  --Zero: float # e.g. 0
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/attributes/($webId)")
  let body = {CategoryNames: $CategoryNames, ConfigString: $ConfigString, DataReference: $DataReference, DataReferencePlugIn: $DataReferencePlugIn, DefaultUnitsName: $DefaultUnitsName, DefaultUnitsNameAbbreviation: $DefaultUnitsNameAbbreviation, Description: $Description, DisplayDigits: $DisplayDigits, HasChildren: $HasChildren, Id: $Id, IsConfigurationItem: $IsConfigurationItem, IsExcluded: $IsExcluded, IsHidden: $IsHidden, IsManualDataEntry: $IsManualDataEntry, Links: $Links, Name: $Name, Path: $Path, Paths: $Paths, Span: $Span, Step: $Step, TraitName: $TraitName, Type: $Type, TypeQualifier: $TypeQualifier, WebException: $WebException, WebId: $WebId, Zero: $Zero} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the child attributes of the specified attribute.
#
# GET /attributes/{webId}/attributes
# operationId: Attribute_GetAttributes
export def "attributes-attributes GetAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --categoryName: string # Specify that returned attributes must have this category. The default is no category filter.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --templateName: string # Specify that returned attributes must be members of this template. The default is no template filter.
  --trait: list # The name of the attribute trait. Multiple traits may be specified with multiple instances of the parameter.
  --traitCategory: list # The category of the attribute traits. Multiple categories may be specified with multiple instances of the parameter. If the parameter is not specified, or if its value is "all", then all attribute traits of all categories will be returned.
  --valueType: string # Specify that returned attributes' value type must be the given value type. The default is no value type filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "trait" $trait "multi") (serialize-qp "traitCategory" $traitCategory "multi") (serialize-qp "valueType" $valueType "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributes/($webId)/attributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new attribute as a child of the specified attribute.
#
# POST /attributes/{webId}/attributes
# operationId: Attribute_CreateAttribute
# --DataReference shape: {PIPoint?: record, Type?: string, WebException?: record}
# --Links shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributes-attributes CreateAttribute" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Energy Savings Targets]
  --ConfigString: string # e.g. SELECT [Water Use] FROM [Energy Use 2008] WHERE [Asset ID] = '%Element%'
  --DataReference: record # shape: {PIPoint?: record, Type?: string, WebException?: record}
  --DataReferencePlugIn: string # e.g. Table Lookup
  --DefaultUnitsName: string # e.g. liter
  --DefaultUnitsNameAbbreviation: string # e.g. L
  --Description: string # e.g. 2008 Water Use
  --DisplayDigits: int # e.g. -5
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 75de231b-b591-49b1-b138-e725a77165c2
  --IsConfigurationItem: oneof<nothing, bool> # e.g. false
  --IsExcluded: oneof<nothing, bool> # e.g. false
  --IsHidden: oneof<nothing, bool> # e.g. false
  --IsManualDataEntry: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
  --Name: string # e.g. Water
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName|Water(2008)
  --Paths: list # e.g. [\\MyAssetServer\MyDatabase\MyElement|MyAttribute, \\MyAssetServer\MyDatabase\ReferencingElement\MyElement|MyAttribute]
  --Span: float # e.g. 100
  --Step: oneof<nothing, bool> # e.g. false
  --TraitName: string # e.g. LimitLoLo
  --Type: string # e.g. Int32
  --TypeQualifier: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1AbEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
  --Zero: float # e.g. 0
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributes/($webId)/attributes" $qp)
  let body = {CategoryNames: $CategoryNames, ConfigString: $ConfigString, DataReference: $DataReference, DataReferencePlugIn: $DataReferencePlugIn, DefaultUnitsName: $DefaultUnitsName, DefaultUnitsNameAbbreviation: $DefaultUnitsNameAbbreviation, Description: $Description, DisplayDigits: $DisplayDigits, HasChildren: $HasChildren, Id: $Id, IsConfigurationItem: $IsConfigurationItem, IsExcluded: $IsExcluded, IsHidden: $IsHidden, IsManualDataEntry: $IsManualDataEntry, Links: $Links, Name: $Name, Path: $Path, Paths: $Paths, Span: $Span, Step: $Step, TraitName: $TraitName, Type: $Type, TypeQualifier: $TypeQualifier, WebException: $WebException, WebId: $WebId, Zero: $Zero} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get an attribute's categories.
#
# GET /attributes/{webId}/categories
# operationId: Attribute_GetCategories
export def "attributes-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributes/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create or update an attribute's DataReference configuration (Create/Update PI point for PI Point DataReference).
#
# POST /attributes/{webId}/config
# operationId: Attribute_CreateConfig
export def "attributes-config CreateConfig" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributes/($webId)/config" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the attribute's value. This call is intended for use with attributes that have no data reference only. For attributes with a data reference, consult the documentation for Streams.
#
# GET /attributes/{webId}/value
# operationId: Attribute_GetValue
export def "attributes-value GetValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
]: nothing -> record<Annotated: bool, Errors: table<FieldName: string, Message: list>, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributes/($webId)/value" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Set the value of a configuration item attribute. For attributes with a data reference or non-configuration item attributes, consult the documentation for streams.
#
# PUT /attributes/{webId}/value
# operationId: Attribute_SetValue
# --Errors item shape: {FieldName?: string, Message?: list}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributes-value SetValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Annotated: oneof<nothing, bool> # e.g. false
  --Errors: list # item shape: {FieldName?: string, Message?: list}
  --Good: oneof<nothing, bool> # e.g. true
  --Questionable: oneof<nothing, bool> # e.g. false
  --Substituted: oneof<nothing, bool> # e.g. false
  --Timestamp: string # format: date-time, e.g. 2014-07-22T14:00:00Z
  --UnitsAbbreviation: string # e.g. m
  --Value: record # e.g. 12.3
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/attributes/($webId)/value")
  let body = {Annotated: $Annotated, Errors: $Errors, Good: $Good, Questionable: $Questionable, Substituted: $Substituted, Timestamp: $Timestamp, UnitsAbbreviation: $UnitsAbbreviation, Value: $Value, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an attribute template by path.
#
# GET /attributetemplates
# operationId: AttributeTemplate_GetByPath
export def "attributetemplates GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the attribute template.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, ConfigString: string, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, DefaultValue: record, Description: string, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record<AttributeTemplates: string, Categories: string, ElementTemplate: string, Parent: string, Self: string, Trait: string>, Name: string, Path: string, TraitName: string, Type: string, TypeQualifier: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/attributetemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an attribute template.
#
# DELETE /attributetemplates/{webId}
# operationId: AttributeTemplate_Delete
export def "attributetemplates Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/attributetemplates/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an attribute template.
#
# GET /attributetemplates/{webId}
# operationId: AttributeTemplate_Get
export def "attributetemplates Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, ConfigString: string, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, DefaultValue: record, Description: string, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record<AttributeTemplates: string, Categories: string, ElementTemplate: string, Parent: string, Self: string, Trait: string>, Name: string, Path: string, TraitName: string, Type: string, TypeQualifier: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributetemplates/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an existing attribute template by replacing items in its definition.
#
# PATCH /attributetemplates/{webId}
# operationId: AttributeTemplate_Update
# --Links shape: {AttributeTemplates?: string, Categories?: string, ElementTemplate?: string, Parent?: string, Self?: string, Trait?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributetemplates Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --CategoryNames: list # e.g. [Energy Savings Targets]
  --ConfigString: string # e.g. SELECT [Water Use] FROM [Energy Use 2008] WHERE [Asset ID] = '%Element%'
  --DataReferencePlugIn: string # e.g. Table Lookup
  --DefaultUnitsName: string # e.g. liter
  --DefaultUnitsNameAbbreviation: string # e.g. L
  --DefaultValue: record # e.g. 0
  --Description: string # e.g. 2008 Water Use
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 23d027b5-5dd5-41c5-80f1-f8b5c9ff4356
  --IsConfigurationItem: oneof<nothing, bool> # e.g. false
  --IsExcluded: oneof<nothing, bool> # e.g. false
  --IsHidden: oneof<nothing, bool> # e.g. false
  --IsManualDataEntry: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {AttributeTemplates?: string, Categories?: string, ElementTemplate?: string, Parent?: string, Self?: string, Trait?: string}
  --Name: string # e.g. Water(2008)
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\ElementTemplates[MachineName]|Water(2008)
  --TraitName: string # e.g. LimitLoLo
  --Type: string # e.g. Int32
  --TypeQualifier: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ATEG_auSSsvuECG8ad_p8b25QQkxqWDwIWU6zC4vmgpd4kgtSfQI9VdxUGA8fi1yf9DVg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/attributetemplates/($webId)")
  let body = {CategoryNames: $CategoryNames, ConfigString: $ConfigString, DataReferencePlugIn: $DataReferencePlugIn, DefaultUnitsName: $DefaultUnitsName, DefaultUnitsNameAbbreviation: $DefaultUnitsNameAbbreviation, DefaultValue: $DefaultValue, Description: $Description, HasChildren: $HasChildren, Id: $Id, IsConfigurationItem: $IsConfigurationItem, IsExcluded: $IsExcluded, IsHidden: $IsHidden, IsManualDataEntry: $IsManualDataEntry, Links: $Links, Name: $Name, Path: $Path, TraitName: $TraitName, Type: $Type, TypeQualifier: $TypeQualifier, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an attribute template's child attribute templates.
#
# GET /attributetemplates/{webId}/attributetemplates
# operationId: AttributeTemplate_GetAttributeTemplates
export def "attributetemplates-attributetemplates GetAttributeTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, DefaultValue: record, Description: string, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributetemplates/($webId)/attributetemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an attribute template as a child of another attribute template.
#
# POST /attributetemplates/{webId}/attributetemplates
# operationId: AttributeTemplate_CreateAttributeTemplate
# --Links shape: {AttributeTemplates?: string, Categories?: string, ElementTemplate?: string, Parent?: string, Self?: string, Trait?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "attributetemplates-attributetemplates CreateAttributeTemplate" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Energy Savings Targets]
  --ConfigString: string # e.g. SELECT [Water Use] FROM [Energy Use 2008] WHERE [Asset ID] = '%Element%'
  --DataReferencePlugIn: string # e.g. Table Lookup
  --DefaultUnitsName: string # e.g. liter
  --DefaultUnitsNameAbbreviation: string # e.g. L
  --DefaultValue: record # e.g. 0
  --Description: string # e.g. 2008 Water Use
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 23d027b5-5dd5-41c5-80f1-f8b5c9ff4356
  --IsConfigurationItem: oneof<nothing, bool> # e.g. false
  --IsExcluded: oneof<nothing, bool> # e.g. false
  --IsHidden: oneof<nothing, bool> # e.g. false
  --IsManualDataEntry: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {AttributeTemplates?: string, Categories?: string, ElementTemplate?: string, Parent?: string, Self?: string, Trait?: string}
  --Name: string # e.g. Water(2008)
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\ElementTemplates[MachineName]|Water(2008)
  --TraitName: string # e.g. LimitLoLo
  --Type: string # e.g. Int32
  --TypeQualifier: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ATEG_auSSsvuECG8ad_p8b25QQkxqWDwIWU6zC4vmgpd4kgtSfQI9VdxUGA8fi1yf9DVg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributetemplates/($webId)/attributetemplates" $qp)
  let body = {CategoryNames: $CategoryNames, ConfigString: $ConfigString, DataReferencePlugIn: $DataReferencePlugIn, DefaultUnitsName: $DefaultUnitsName, DefaultUnitsNameAbbreviation: $DefaultUnitsNameAbbreviation, DefaultValue: $DefaultValue, Description: $Description, HasChildren: $HasChildren, Id: $Id, IsConfigurationItem: $IsConfigurationItem, IsExcluded: $IsExcluded, IsHidden: $IsHidden, IsManualDataEntry: $IsManualDataEntry, Links: $Links, Name: $Name, Path: $Path, TraitName: $TraitName, Type: $Type, TypeQualifier: $TypeQualifier, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get an attribute template's categories.
#
# GET /attributetemplates/{webId}/categories
# operationId: AttributeTemplate_GetCategories
export def "attributetemplates-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributetemplates/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve all attribute traits of the specified category/categories.
#
# GET /attributetraits
# operationId: AttributeTrait_GetByCategory
export def "attributetraits GetByCategory" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --category: list # The category of the attribute traits. Multiple categories may be specified with multiple instances of the parameter. If the parameter is not specified, or if its value is "all", then all attribute traits of all categories will be returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
]: nothing -> record<Items: table<Abbreviation: string, AllowChildAttributes: bool, AllowDuplicates: bool, IsAllowedOnRootAttribute: bool, IsTypeInherited: bool, IsUOMInherited: bool, Links: record, Name: string, RequireNumeric: bool, RequireString: bool, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "category" $category "multi") (serialize-qp "selectedFields" $selectedFields "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/attributetraits" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an attribute trait.
#
# GET /attributetraits/{name}
# operationId: AttributeTrait_Get
export def "attributetraits Get" [
  name: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
]: nothing -> record<Abbreviation: string, AllowChildAttributes: bool, AllowDuplicates: bool, IsAllowedOnRootAttribute: bool, IsTypeInherited: bool, IsUOMInherited: bool, Links: record<Self: string>, Name: string, RequireNumeric: bool, RequireString: bool, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/attributetraits/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Execute a batch of requests against the service. As shown in the Sample Request, the input is a dictionary with IDs as keys and request objects as values. Each request object specifies the HTTP method and the resource and, optionally, the content and a list of parent IDs. The list of parent IDs specifies which other requests must complete before the given request will be executed. The example first creates an element, then gets the element by the response's Location header, then creates an attribute for the element. Note that the resource can be an absolute URL or a JsonPath that references the response to the parent request. The batch's response is a dictionary uses keys corresponding those provided in the request, with response objects containing a status code, response headers, and the response body. A request can alternatively specify a request template in place of a resource. In this case, a single JsonPath may select multiple tokens, and a separate subrequest will be made from the template for each token. The responses of these subrequests will returned as the content of a single outer response.
#
# POST /batch
# operationId: Batch_Execute
export def "batch Execute" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/batch")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Returns results of evaluating the expression over the time range from the start time to the end time at a defined interval.
#
# GET /calculation/intervals
# operationId: Calculation_GetAtIntervals
export def "calculation-intervals GetAtIntervals" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --expression: string # A string containing the expression to be evaluated. The syntax for the expression generally follows the Performance Equation syntax as described in the PI Server documentation, with the exception that expressions which target AF objects use attribute names in place of tag names in the equation.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --sampleInterval: string # A time span specifies how often the filter expression is evaluated when computing the summary for an interval.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --webId: string # The ID of the target object of the expression. A target object can be a Data Server, a database, an element, an event frame or an attribute. References to attributes or points are based on the target. If this parameter is not provided, the target object is set to null.
]: nothing -> record<Items: table<Annotated: bool, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expression" $expression "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "sampleInterval" $sampleInterval "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "webId" $webId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/calculation/intervals" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns the result of evaluating the expression at each point in time over the time range from the start time to the end time where a recorded value exists for a member of the expression.
#
# GET /calculation/recorded
# operationId: Calculation_GetAtRecorded
export def "calculation-recorded GetAtRecorded" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --expression: string # A string containing the expression to be evaluated. The syntax for the expression generally follows the Performance Equation syntax as described in the PI Server documentation, with the exception that expressions which target AF objects use attribute names in place of tag names in the equation.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --webId: string # The ID of the target object of the expression. A target object can be a Data Server, a database, an element, an event frame or an attribute. References to attributes or points are based on the target. If this parameter is not provided, the target object is set to null.
]: nothing -> record<Items: table<Annotated: bool, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expression" $expression "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "webId" $webId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/calculation/recorded" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns the result of evaluating the expression over the time range from the start time to the end time. The time range is first divided into a number of summary intervals. Then the calculation is performed for the specified summaries over each interval.
#
# GET /calculation/summary
# operationId: Calculation_GetSummary
export def "calculation-summary GetSummary" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --expression: string # A string containing the expression to be evaluated. The syntax for the expression generally follows the Performance Equation syntax as described in the PI Server documentation, with the exception that expressions which target AF objects use attribute names in place of tag names in the equation.
  --calculationBasis: string # Specifies the method of evaluating the data over the time range. The default is 'TimeWeighted'.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --sampleInterval: string # A time span specifies how often the filter expression is evaluated when computing the summary for an interval, if the sampleType is 'Interval'.
  --sampleType: string # A flag which specifies one or more summaries to compute for each interval over the time range. The default is 'ExpressionRecordedValues'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --summaryDuration: string # The duration of each summary interval.
  --summaryType: list # Specifies the kinds of summaries to produce over the range. The default is 'Total'. Multiple summary types may be specified by using multiple instances of summaryType.
  --timeType: string # Specifies how to calculate the timestamp for each interval. The default is 'Auto'.
  --webId: string # The ID of the target object of the expression. A target object can be a Data Server, a database, an element, an event frame or an attribute. References to attributes or points are based on the target. If this parameter is not provided, the target object is set to null.
]: nothing -> record<Items: table<Type: string, Value: record, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expression" $expression "scalar") (serialize-qp "calculationBasis" $calculationBasis "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "sampleInterval" $sampleInterval "scalar") (serialize-qp "sampleType" $sampleType "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "summaryDuration" $summaryDuration "scalar") (serialize-qp "summaryType" $summaryType "multi") (serialize-qp "timeType" $timeType "scalar") (serialize-qp "webId" $webId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/calculation/summary" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns the result of evaluating the expression at the specified timestamps.
#
# GET /calculation/times
# operationId: Calculation_GetAtTimes
export def "calculation-times GetAtTimes" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --expression: string # A string containing the expression to be evaluated. The syntax for the expression generally follows the Performance Equation syntax as described in the PI Server documentation, with the exception that expressions which target AF objects use attribute names in place of tag names in the equation.
  --time: list # A list of timestamps at which to calculate the expression.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --webId: string # The ID of the target object of the expression. A target object can be a Data Server, a database, an element, an event frame or an attribute. References to attributes or points are based on the target. If this parameter is not provided, the target object is set to null.
]: nothing -> record<Items: table<Annotated: bool, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "expression" $expression "scalar") (serialize-qp "time" $time "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "webId" $webId "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/calculation/times" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieves a list of currently running channel instances.
#
# GET /channels/instances
# operationId: Channel_Instances
export def "channels-instances Instances" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Items: table<Id: string, LastMessageSentTime: string, SentMessageCount: int, StartTime: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/channels/instances")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a list of Data Servers known to this service.
#
# GET /dataservers
# operationId: DataServer_List
export def "dataservers List" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Id: string, IsConnected: bool, Links: record, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dataservers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a Data Server by name.
#
# GET /dataservers#name
# operationId: DataServer_GetByName
export def "dataserversname GetByName" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --name: string # The name of the server.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Id: string, IsConnected: bool, Links: record<EnumerationSets: string, Points: string, Self: string>, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dataservers#name" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a Data Server by path.
#
# GET /dataservers#path
# operationId: DataServer_GetByPath
export def "dataserverspath GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the server. Note that the path supplied to this method must be of the form '\\servername'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Id: string, IsConnected: bool, Links: record<EnumerationSets: string, Points: string, Self: string>, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dataservers#path" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a Data Server.
#
# GET /dataservers/{webId}
# operationId: DataServer_Get
export def "dataservers Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Id: string, IsConnected: bool, Links: record<EnumerationSets: string, Points: string, Self: string>, Name: string, Path: string, ServerTime: string, ServerVersion: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/dataservers/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve enumeration sets for given Data Server.
#
# GET /dataservers/{webId}/enumerationsets
# operationId: DataServer_GetEnumerationSets
export def "dataservers-enumerationsets GetEnumerationSets" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, SerializeDescription: bool, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/dataservers/($webId)/enumerationsets" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an enumeration set on the Data Server.
#
# POST /dataservers/{webId}/enumerationsets
# operationId: DataServer_CreateEnumerationSet
# --Links shape: {DataServer?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Values?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "dataservers-enumerationsets CreateEnumerationSet" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. Model numbers by brand of vehicle
  --Id: string # format: uuid, e.g. 8e77fc4f-ac91-4029-81d6-fb66bd01d431
  --Links: record # shape: {DataServer?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Values?: string}
  --Name: string # e.g. Model Number
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EnumerationSets[Model Number]
  --SerializeDescription: oneof<nothing, bool>
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1MSRDqD5loBNH0erqeqJodtALAT_x3jpGsKUCB1vtmvQHUMQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/dataservers/($webId)/enumerationsets" $qp)
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, SerializeDescription: $SerializeDescription, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieves the specified license for the given Data Server. The fields of the response object are string representations of the numerical values reported by the Data Server, with "Infinity" representing a license field with no limit.
#
# GET /dataservers/{webId}/license
# operationId: DataServer_GetLicense
export def "dataservers-license GetLicense" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --qp-module: string # The case-sensitive name of the module.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AmountLeft: string, AmountUsed: string, Links: record<Parent: string, Self: string>, Name: string, TotalAmount: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "module" $qp_module "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/dataservers/($webId)/license" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a list of points on a specified Data Server.
#
# GET /dataservers/{webId}/points
# operationId: DataServer_GetPoints
export def "dataservers-points GetPoints" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # A query string for filtering by point name. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is '0'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Descriptor: string, DigitalSetName: string, DisplayDigits: int, EngineeringUnits: string, Future: bool, Id: int, Links: record, Name: string, Path: string, PointClass: string, PointType: string, Span: float, Step: bool, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/dataservers/($webId)/points" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a point in the specified Data Server.
#
# POST /dataservers/{webId}/points
# operationId: DataServer_CreatePoint
# --Links shape: {Attributes?: string, DataServer?: string, EndValue?: string, InterpolatedData?: string, PlotData?: string, RecordedData?: string, Self?: string, SummaryData?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "dataservers-points CreatePoint" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Descriptor: string # e.g. 12 Hour Sine Wave
  --DigitalSetName: string # e.g. 
  --DisplayDigits: int # e.g. -5
  --EngineeringUnits: string # e.g. 
  --Future: oneof<nothing, bool> # e.g. false
  --Id: int # format: int32, e.g. 82
  --Links: record # shape: {Attributes?: string, DataServer?: string, EndValue?: string, InterpolatedData?: string, PlotData?: string, RecordedData?: string, Self?: string, SummaryData?: string, Value?: string}
  --Name: string # e.g. PointName
  --Path: string # format: path, e.g. \\MyPIServer\PointName
  --PointClass: string # e.g. classic
  --PointType: string # e.g. Float32
  --Span: float # e.g. 100
  --Step: oneof<nothing, bool> # e.g. false
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1DPa70Wf0zBA06CLkV9ovNQgQCAAAAA
  --Zero: float # e.g. 0
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/dataservers/($webId)/points" $qp)
  let body = {Descriptor: $Descriptor, DigitalSetName: $DigitalSetName, DisplayDigits: $DisplayDigits, EngineeringUnits: $EngineeringUnits, Future: $Future, Id: $Id, Links: $Links, Name: $Name, Path: $Path, PointClass: $PointClass, PointType: $PointType, Span: $Span, Step: $Step, WebException: $WebException, WebId: $WebId, Zero: $Zero} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an element category by path.
#
# GET /elementcategories
# operationId: ElementCategory_GetByPath
export def "elementcategories GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the target element category.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/elementcategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an element category.
#
# DELETE /elementcategories/{webId}
# operationId: ElementCategory_Delete
export def "elementcategories Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/elementcategories/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an element category.
#
# GET /elementcategories/{webId}
# operationId: ElementCategory_Get
export def "elementcategories Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementcategories/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an element category by replacing items in its definition.
#
# PATCH /elementcategories/{webId}
# operationId: ElementCategory_Update
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementcategories Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. 3051f943-7e4d-4241-ab9c-450a84c80450
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesElement[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ECDqD5loBNH0erqeqJodtALAQ_lRME1-QUKrnEUKhMgEUA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/elementcategories/($webId)")
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the element category for a specified user.
#
# GET /elementcategories/{webId}/security
# operationId: ElementCategory_GetSecurity
export def "elementcategories-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementcategories/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the element category based on the specified criteria. By default, all security entries for this element category are returned.
#
# GET /elementcategories/{webId}/securityentries
# operationId: ElementCategory_GetSecurityEntries
export def "elementcategories-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementcategories/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the element category.
#
# POST /elementcategories/{webId}/securityentries
# operationId: ElementCategory_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementcategories-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementcategories/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the element category.
#
# DELETE /elementcategories/{webId}/securityentries/{name}
# operationId: ElementCategory_DeleteSecurityEntry
export def "elementcategories-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementcategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the element category with the specified name.
#
# GET /elementcategories/{webId}/securityentries/{name}
# operationId: ElementCategory_GetSecurityEntryByName
export def "elementcategories-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementcategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the element category.
#
# PUT /elementcategories/{webId}/securityentries/{name}
# operationId: ElementCategory_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementcategories-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementcategories/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an element by path.
#
# GET /elements
# operationId: Element_GetByPath
export def "elements GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the element.
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, Description: string, Errors: table<FieldName: string, Message: list>, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record<Analyses: string, Attributes: string, Categories: string, Database: string, DefaultAttribute: string, Elements: string, EndValue: string, EventFrames: string, InterpolatedData: string, NotificationRules: string, Parent: string, PlotData: string, RecordedData: string, Security: string, SecurityEntries: string, Self: string, SummaryData: string, Template: string, Value: string>, Name: string, Path: string, Paths: list<string>, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "associations" $associations "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/elements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve multiple elements by web id or path.
#
# GET /elements/multiple
# operationId: Element_GetMultiple
export def "elements-multiple GetMultiple" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --asParallel: oneof<nothing, bool> # Specifies if the retrieval processes should be run in parallel on the server. This may improve the response time for large amounts of requested attributes. The default is 'false'.
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --includeMode: string # The include mode for the return list. The default is 'All'.
  --path: list # The path of an element. Multiple elements may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webId: list # The ID of an element. Multiple elements may be specified with multiple instances of the parameter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Exception: record, Identifier: string, IdentifierType: string, Object: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "asParallel" $asParallel "scalar") (serialize-qp "associations" $associations "scalar") (serialize-qp "includeMode" $includeMode "scalar") (serialize-qp "path" $path "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webId" $webId "multi") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/elements/multiple" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve elements based on the specified conditions. By default, returns all the elements.
#
# GET /elements/search
# operationId: Element_GetElementsQuery
export def "elements-search GetElementsQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --databaseWebId: string # The ID of the asset database to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the elements in the asset database. An example would be: "query=Name:=MyElement* Template:=ElementTemplate".
  --queryDate: string # Optional parameter. Used to retrieve the relative the version of an object. A value of null or AFTime.MaxValue initializes the query date so the latest versions of sub-objects are retrieved. The value may be an AFTime, DateTime, PITime, String, or numeric. An integer numeric represents the number of ticks (100-nanosecond intervals) since January 1, 0001. A floating point numeric represents the number of seconds since January 1, 1970 UTC. A String is interpreted as local time, unless it contains a time zone indicator such as a trailing "Z" or "GMT".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "databaseWebId" $databaseWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "queryDate" $queryDate "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/elements/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a link for a "Search Elements By Attribute Value" operation, whose queries are specified in the request content. The SearchRoot is specified by the Web Id of the root Element. If the SearchRoot is not specified, then the search starts at the Asset Database. ElementTemplate must be provided as the Web ID of the ElementTemplate, which are used to create the Elements. All the attributes in the queries must be defined as AttributeTemplates on the ElementTemplate. An array of attribute value queries are ANDed together to find the desired Element objects. At least one value query must be specified. There are limitations on SearchOperators.
#
# POST /elements/searchbyattribute
# operationId: Element_CreateSearchByAttribute
# --ValueQueries item shape: {AttributeName?: string, AttributeUOM?: string, AttributeValue?: record, SearchOperator?: string, WebException?: record}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements-searchbyattribute CreateSearchByAttribute" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --noResults: oneof<nothing, bool> # If false, the response content will contain the first page of the search results. If true, the response content will be empty. The default is false.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --ElementTemplate: string # format: webid, e.g. I1ETDqD5loBNH0erqeqJodtALAjFPVfUpY-02A8uioGDSgIg
  --SearchRoot: string # format: webid, e.g. I1RDDqD5loBNH0erqeqJodtALA8fbgUnyQW02v-gLGIxumSg
  --ValueQueries: list # item shape: {AttributeName?: string, AttributeUOM?: string, AttributeValue?: record, SearchOperator?: string, WebException?: record}
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "noResults" $noResults "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/elements/searchbyattribute" $qp)
  let body = {ElementTemplate: $ElementTemplate, SearchRoot: $SearchRoot, ValueQueries: $ValueQueries, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Execute a "Search Elements By Attribute Value" operation.
#
# GET /elements/searchbyattribute/{searchId}
# operationId: Element_ExecuteSearchByAttribute
export def "elements-searchbyattribute ExecuteSearchByAttribute" [
  searchId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --categoryName: string # Specify that the owner of the returned attributes must have this category. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --descriptionFilter: string # The element description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --maxCount: int # The maximum number of objects to be returned. The default is 1000.
  --nameFilter: string # The name query string used for finding objects. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include objects nested further than the immediate children of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "descriptionFilter" $descriptionFilter "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/searchbyattribute/($searchId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an element.
#
# DELETE /elements/{webId}
# operationId: Element_Delete
export def "elements Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/elements/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an element.
#
# GET /elements/{webId}
# operationId: Element_Get
export def "elements Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, Description: string, Errors: table<FieldName: string, Message: list>, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record<Analyses: string, Attributes: string, Categories: string, Database: string, DefaultAttribute: string, Elements: string, EndValue: string, EventFrames: string, InterpolatedData: string, NotificationRules: string, Parent: string, PlotData: string, RecordedData: string, Security: string, SecurityEntries: string, Self: string, SummaryData: string, Template: string, Value: string>, Name: string, Path: string, Paths: list<string>, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an element by replacing items in its definition.
#
# PATCH /elements/{webId}
# operationId: Element_Update
# --Errors item shape: {FieldName?: string, Message?: list}
# --Links shape: {Analyses?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, Elements?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, NotificationRules?: string, Parent?: string, PlotData?: string, RecordedData?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --CategoryNames: list # e.g. [Equipment Assets]
  --Description: string # e.g. Manufacturing Equipment MachineName
  --Errors: list # item shape: {FieldName?: string, Message?: list}
  --ExtendedProperties: record
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. cbb28260-853d-11e4-80c5-00155d844304
  --Links: record # shape: {Analyses?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, Elements?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, NotificationRules?: string, Parent?: string, PlotData?: string, RecordedData?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
  --Name: string # e.g. MachineName
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName
  --Paths: list
  --TemplateName: string # e.g. MachineName
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1EmDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/elements/($webId)")
  let body = {CategoryNames: $CategoryNames, Description: $Description, Errors: $Errors, ExtendedProperties: $ExtendedProperties, HasChildren: $HasChildren, Id: $Id, Links: $Links, Name: $Name, Path: $Path, Paths: $Paths, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve analyses based on the specified conditions.
#
# GET /elements/{webId}/analyses
# operationId: Element_GetAnalyses
export def "elements-analyses GetAnalyses" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AnalysisRulePlugInName: string, AutoCreated: bool, CategoryNames: list, Description: string, GroupId: int, HasNotification: bool, HasTarget: bool, HasTemplate: bool, Id: string, IsConfigured: bool, IsTimeRuleDefinedByTemplate: bool, Links: record, MaximumQueueSize: int, Name: string, OutputTime: string, Path: string, Priority: string, PublishResults: bool, Status: string, TargetWebId: string, TemplateName: string, TimeRulePlugInName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/analyses" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an Analysis.
#
# POST /elements/{webId}/analyses
# operationId: Element_CreateAnalysis
# --Links shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, Template?: string, TimeRule?: string, TimeRulePlugIn?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements-analyses CreateAnalysis" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AnalysisRulePlugInName: string # e.g. PerformanceEquation
  --AutoCreated: oneof<nothing, bool> # e.g. false
  --CategoryNames: list # e.g. [MyAnalysisCategory]
  --Description: string # e.g. 
  --GroupId: int # format: int32, e.g. 0
  --HasNotification: oneof<nothing, bool> # e.g. false
  --HasTarget: oneof<nothing, bool> # e.g. true
  --HasTemplate: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 11853358-94bf-11e7-8443-00155d84c200
  --IsConfigured: oneof<nothing, bool> # e.g. false
  --IsTimeRuleDefinedByTemplate: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {AnalysisRule?: string, AnalysisRulePlugIn?: string, Categories?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Target?: string, Template?: string, TimeRule?: string, TimeRulePlugIn?: string}
  --MaximumQueueSize: int # format: int32, e.g. 0
  --Name: string # e.g. MyAnalysis
  --OutputTime: string # e.g. 
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\MyElement\Analyses[MyAnalysis]
  --Priority: string # e.g. High
  --PublishResults: oneof<nothing, bool> # e.g. false
  --Status: string # e.g. Disabled
  --TargetWebId: string # format: webid, e.g. I1ETDqD5loBNH0erqeqJodtALAjFPVfUpY-02A8uioGDSgIg
  --TemplateName: string # e.g. 
  --TimeRulePlugInName: string # e.g. Periodic
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1XsDqD5loBNH0erqeqJodtALAWDOFEb-U5xGEQwAVXYTCAA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/analyses" $qp)
  let body = {AnalysisRulePlugInName: $AnalysisRulePlugInName, AutoCreated: $AutoCreated, CategoryNames: $CategoryNames, Description: $Description, GroupId: $GroupId, HasNotification: $HasNotification, HasTarget: $HasTarget, HasTemplate: $HasTemplate, Id: $Id, IsConfigured: $IsConfigured, IsTimeRuleDefinedByTemplate: $IsTimeRuleDefinedByTemplate, Links: $Links, MaximumQueueSize: $MaximumQueueSize, Name: $Name, OutputTime: $OutputTime, Path: $Path, Priority: $Priority, PublishResults: $PublishResults, Status: $Status, TargetWebId: $TargetWebId, TemplateName: $TemplateName, TimeRulePlugInName: $TimeRulePlugInName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the attributes of the specified element.
#
# GET /elements/{webId}/attributes
# operationId: Element_GetAttributes
export def "elements-attributes GetAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --categoryName: string # Specify that returned attributes must have this category. The default is no category filter.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --templateName: string # Specify that returned attributes must be members of this template. The default is no template filter.
  --trait: list # The name of the attribute trait. Multiple traits may be specified with multiple instances of the parameter.
  --traitCategory: list # The category of the attribute traits. Multiple categories may be specified with multiple instances of the parameter. If the parameter is not specified, or if its value is "all", then all attribute traits of all categories will be returned.
  --valueType: string # Specify that returned attributes' value type must be the given value type. The default is no value type filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "trait" $trait "multi") (serialize-qp "traitCategory" $traitCategory "multi") (serialize-qp "valueType" $valueType "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/attributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new attribute of the specified element.
#
# POST /elements/{webId}/attributes
# operationId: Element_CreateAttribute
# --DataReference shape: {PIPoint?: record, Type?: string, WebException?: record}
# --Links shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements-attributes CreateAttribute" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Energy Savings Targets]
  --ConfigString: string # e.g. SELECT [Water Use] FROM [Energy Use 2008] WHERE [Asset ID] = '%Element%'
  --DataReference: record # shape: {PIPoint?: record, Type?: string, WebException?: record}
  --DataReferencePlugIn: string # e.g. Table Lookup
  --DefaultUnitsName: string # e.g. liter
  --DefaultUnitsNameAbbreviation: string # e.g. L
  --Description: string # e.g. 2008 Water Use
  --DisplayDigits: int # e.g. -5
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 75de231b-b591-49b1-b138-e725a77165c2
  --IsConfigurationItem: oneof<nothing, bool> # e.g. false
  --IsExcluded: oneof<nothing, bool> # e.g. false
  --IsHidden: oneof<nothing, bool> # e.g. false
  --IsManualDataEntry: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
  --Name: string # e.g. Water
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName|Water(2008)
  --Paths: list # e.g. [\\MyAssetServer\MyDatabase\MyElement|MyAttribute, \\MyAssetServer\MyDatabase\ReferencingElement\MyElement|MyAttribute]
  --Span: float # e.g. 100
  --Step: oneof<nothing, bool> # e.g. false
  --TraitName: string # e.g. LimitLoLo
  --Type: string # e.g. Int32
  --TypeQualifier: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1AbEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
  --Zero: float # e.g. 0
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/attributes" $qp)
  let body = {CategoryNames: $CategoryNames, ConfigString: $ConfigString, DataReference: $DataReference, DataReferencePlugIn: $DataReferencePlugIn, DefaultUnitsName: $DefaultUnitsName, DefaultUnitsNameAbbreviation: $DefaultUnitsNameAbbreviation, Description: $Description, DisplayDigits: $DisplayDigits, HasChildren: $HasChildren, Id: $Id, IsConfigurationItem: $IsConfigurationItem, IsExcluded: $IsExcluded, IsHidden: $IsHidden, IsManualDataEntry: $IsManualDataEntry, Links: $Links, Name: $Name, Path: $Path, Paths: $Paths, Span: $Span, Step: $Step, TraitName: $TraitName, Type: $Type, TypeQualifier: $TypeQualifier, WebException: $WebException, WebId: $WebId, Zero: $Zero} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get an element's categories.
#
# GET /elements/{webId}/categories
# operationId: Element_GetCategories
export def "elements-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Executes the create configuration function of the data references found within the attributes of the element, and optionally, its children.
#
# POST /elements/{webId}/config
# operationId: Element_CreateConfig
export def "elements-config CreateConfig" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --includeChildElements: oneof<nothing, bool> # If true, includes the child elements of the specified element.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "includeChildElements" $includeChildElements "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/config" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieves a list of element attributes matching the specified filters from the specified element.
#
# GET /elements/{webId}/elementattributes
# operationId: Element_FindElementAttributes
export def "elements-elementattributes FindElementAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --attributeCategory: string # Specify that returned attributes must have this category. The default is no filter.
  --attributeDescriptionFilter: string # The attribute description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --attributeNameFilter: string # The attribute name filter string used for finding objects. The default is no filter.
  --attributeType: string # Specify that returned attributes' value type must be this value type. The default is no filter.
  --elementCategory: string # Specify that the owner of the returned attributes must have this category. The default is no filter.
  --elementDescriptionFilter: string # The element description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --elementNameFilter: string # The element name filter string used for finding objects. The default is no filter.
  --elementTemplate: string # Specify that the owner of the returned attributes must have this template or a template derived from this template. The default is no filter.
  --elementType: string # Specify that the element of the returned attributes must have this AFElementType. The default is no filter.
  --maxCount: int # The maximum number of objects to be returned (the page size). The default is 1000.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include objects nested further than immediate children of the given resource. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "attributeCategory" $attributeCategory "scalar") (serialize-qp "attributeDescriptionFilter" $attributeDescriptionFilter "scalar") (serialize-qp "attributeNameFilter" $attributeNameFilter "scalar") (serialize-qp "attributeType" $attributeType "scalar") (serialize-qp "elementCategory" $elementCategory "scalar") (serialize-qp "elementDescriptionFilter" $elementDescriptionFilter "scalar") (serialize-qp "elementNameFilter" $elementNameFilter "scalar") (serialize-qp "elementTemplate" $elementTemplate "scalar") (serialize-qp "elementType" $elementType "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/elementattributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve elements based on the specified conditions. By default, this method selects immediate children of the specified element.
#
# GET /elements/{webId}/elements
# operationId: Element_GetElements
export def "elements-elements GetElements" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --categoryName: string # Specify that returned elements must have this category. The default is no category filter.
  --descriptionFilter: string # Specify that returned elements must have this description. The default is no description filter.
  --elementType: string # Specify that returned elements must have this type. The default type is 'Any'.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding objects. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include objects nested further than the immediate children of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --templateName: string # Specify that returned elements must have this template or a template derived from this template. The default is no template filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "descriptionFilter" $descriptionFilter "scalar") (serialize-qp "elementType" $elementType "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/elements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a child element.
#
# POST /elements/{webId}/elements
# operationId: Element_CreateElement
# --Errors item shape: {FieldName?: string, Message?: list}
# --Links shape: {Analyses?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, Elements?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, NotificationRules?: string, Parent?: string, PlotData?: string, RecordedData?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements-elements CreateElement" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Equipment Assets]
  --Description: string # e.g. Manufacturing Equipment MachineName
  --Errors: list # item shape: {FieldName?: string, Message?: list}
  --ExtendedProperties: record
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. cbb28260-853d-11e4-80c5-00155d844304
  --Links: record # shape: {Analyses?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, Elements?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, NotificationRules?: string, Parent?: string, PlotData?: string, RecordedData?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
  --Name: string # e.g. MachineName
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName
  --Paths: list
  --TemplateName: string # e.g. MachineName
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1EmDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/elements" $qp)
  let body = {CategoryNames: $CategoryNames, Description: $Description, Errors: $Errors, ExtendedProperties: $ExtendedProperties, HasChildren: $HasChildren, Id: $Id, Links: $Links, Name: $Name, Path: $Path, Paths: $Paths, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve event frames that reference this element based on the specified conditions. By default, returns all event frames that reference this element that have been active in the past 8 hours.
#
# GET /elements/{webId}/eventframes
# operationId: Element_GetEventFrames
export def "elements-eventframes GetEventFrames" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --canBeAcknowledged: oneof<nothing, bool> # Specify the returned event frames' canBeAcknowledged property. The default is no canBeAcknowledged filter.
  --categoryName: string # Specify that returned event frames must have this category. The default is no category filter.
  --endTime: string # The ending time for the search. The endTime must be greater than or equal to the startTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --isAcknowledged: oneof<nothing, bool> # Specify the returned event frames' isAcknowledged property. The default no isAcknowledged filter.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding event frames. The default is no filter.
  --searchMode: string # Determines how the startTime and endTime parameters are treated when searching for event frame objects to be included in the returned collection. If this parameter is one of the 'Backward*' or 'Forward*' values, none of endTime, sortField, or sortOrder may be specified. The default is 'Overlapped'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --severity: list # Specify that returned event frames must have this severity. Multiple severity values may be specified with multiple instances of the parameter. The default is no severity filter.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --startTime: string # The starting time for the search. startTime must be less than or equal to the endTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*-8h'.
  --templateName: string # Specify that returned event frames must have this template or a template derived from this template. The default is no template filter. Specify this parameter by name.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record, Name: string, Path: string, RefElementWebIds: list, Security: record, Severity: string, StartTime: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "canBeAcknowledged" $canBeAcknowledged "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "isAcknowledged" $isAcknowledged "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchMode" $searchMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "severity" $severity "multi") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/eventframes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve notification rules for an element
#
# GET /elements/{webId}/notificationrules
# operationId: Element_GetNotificationRules
export def "elements-notificationrules GetNotificationRules" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AutoCreated: bool, CategoryNames: list, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/notificationrules" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a notification rule.
#
# POST /elements/{webId}/notificationrules
# operationId: Element_CreateNotificationRule
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements-notificationrules CreateNotificationRule" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AutoCreated: oneof<nothing, bool> # e.g. true
  --CategoryNames: list # e.g. [Equipment Assets]
  --Criteria: string # e.g. Name: EventFrameCriteriaName
  --Description: string # e.g. Manufacturing Equipment MachineName
  --Id: string # format: uuid, e.g. e9a984d0-f47c-11e7-8454-00155d029708
  --MultiTriggerEventOption: string # e.g. HighestSeverity
  --Name: string # e.g. MachineName
  --NonrepetitionInterval: string # format: Duration, e.g. PT3S
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName\NotificationRules[RuleName]
  --ResendInterval: string # format: Duration, e.g. PT5S
  --Status: string # e.g. Disabled
  --TemplateName: string # e.g. MachineName Notification Rule
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NRDqD5loBNH0erqeqJodtALA5bYfWno26BGgMQAVXYR0Ag
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/notificationrules" $qp)
  let body = {AutoCreated: $AutoCreated, CategoryNames: $CategoryNames, Criteria: $Criteria, Description: $Description, Id: $Id, MultiTriggerEventOption: $MultiTriggerEventOption, Name: $Name, NonrepetitionInterval: $NonrepetitionInterval, Path: $Path, ResendInterval: $ResendInterval, Status: $Status, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a list of the full or relative paths to this element.
#
# GET /elements/{webId}/paths
# operationId: Element_GetPaths
export def "elements-paths GetPaths" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --relativePath: string # The full path in ShortName format to the parent object that the returned paths should be relative. For example, "\\Server1\Database2" would return all the paths to the element relative to the database. A path of "\\Server1\Database2\RootElement" would return all paths to the element relative to "RootElement". If null, then all the full paths to the element will be returned.
]: nothing -> record<Items: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "relativePath" $relativePath "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/paths" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Remove a reference to an existing element from the child elements collection.
#
# DELETE /elements/{webId}/referencedelements
# operationId: Element_RemoveReferencedElement
export def "elements-referencedelements RemoveReferencedElement" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --referencedElementWebId: list # The ID of the referenced element. Multiple referenced elements may be specified with multiple instances of the parameter.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referencedElementWebId" $referencedElementWebId "multi")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/referencedelements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve referenced elements based on the specified conditions. By default, this method selects all referenced elements of the current resource.
#
# GET /elements/{webId}/referencedelements
# operationId: Element_GetReferencedElements
export def "elements-referencedelements GetReferencedElements" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --categoryName: string # Specify that returned elements must have this category. The default is no category filter.
  --descriptionFilter: string # Specify that returned elements must have this description. The default is no description filter.
  --elementType: string # Specify that returned elements must have this type. The default type is 'Any'.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding objects. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --templateName: string # Specify that returned elements must have this template or a template derived from this template. The default is no template filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "descriptionFilter" $descriptionFilter "scalar") (serialize-qp "elementType" $elementType "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/referencedelements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Add a reference to an existing element to the child elements collection.
#
# POST /elements/{webId}/referencedelements
# operationId: Element_AddReferencedElement
export def "elements-referencedelements AddReferencedElement" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --referencedElementWebId: list # The ID of the referenced element. Multiple referenced elements may be specified with multiple instances of the parameter.
  --referenceType: string # The name of the reference type between the parent and the referenced element. The default is "parent-child".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "referencedElementWebId" $referencedElementWebId "multi") (serialize-qp "referenceType" $referenceType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/referencedelements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the security information of the specified security item associated with the element for a specified user.
#
# GET /elements/{webId}/security
# operationId: Element_GetSecurity
export def "elements-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the element based on the specified criteria. By default, all security entries for this element are returned.
#
# GET /elements/{webId}/securityentries
# operationId: Element_GetSecurityEntries
export def "elements-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the element.
#
# POST /elements/{webId}/securityentries
# operationId: Element_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the element.
#
# DELETE /elements/{webId}/securityentries/{name}
# operationId: Element_DeleteSecurityEntry
export def "elements-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the element with the specified name.
#
# GET /elements/{webId}/securityentries/{name}
# operationId: Element_GetSecurityEntryByName
export def "elements-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the element.
#
# PUT /elements/{webId}/securityentries/{name}
# operationId: Element_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elements-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elements/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an element template by path.
#
# GET /elementtemplates
# operationId: ElementTemplate_GetByPath
export def "elementtemplates GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the element template.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowElementToExtend: bool, BaseTemplate: string, CanBeAcknowledged: bool, CategoryNames: list<string>, Description: string, ExtendedProperties: record, Id: string, InstanceType: string, Links: record<AnalysisTemplates: string, AttributeTemplates: string, BaseTemplate: string, BaseTemplates: string, Categories: string, Database: string, DefaultAttribute: string, DerivedTemplates: string, NotificationRuleTemplates: string, Security: string, SecurityEntries: string, Self: string>, Name: string, NamingPattern: string, Path: string, Severity: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/elementtemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an element template.
#
# DELETE /elementtemplates/{webId}
# operationId: ElementTemplate_Delete
export def "elementtemplates Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/elementtemplates/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an element template.
#
# GET /elementtemplates/{webId}
# operationId: ElementTemplate_Get
export def "elementtemplates Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowElementToExtend: bool, BaseTemplate: string, CanBeAcknowledged: bool, CategoryNames: list<string>, Description: string, ExtendedProperties: record, Id: string, InstanceType: string, Links: record<AnalysisTemplates: string, AttributeTemplates: string, BaseTemplate: string, BaseTemplates: string, Categories: string, Database: string, DefaultAttribute: string, DerivedTemplates: string, NotificationRuleTemplates: string, Security: string, SecurityEntries: string, Self: string>, Name: string, NamingPattern: string, Path: string, Severity: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an element template by replacing items in its definition.
#
# PATCH /elementtemplates/{webId}
# operationId: ElementTemplate_Update
# --Links shape: {AnalysisTemplates?: string, AttributeTemplates?: string, BaseTemplate?: string, BaseTemplates?: string, Categories?: string, Database?: string, DefaultAttribute?: string, DerivedTemplates?: string, NotificationRuleTemplates?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementtemplates Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --AllowElementToExtend: oneof<nothing, bool> # e.g. false
  --BaseTemplate: string # e.g. Equipment
  --CanBeAcknowledged: oneof<nothing, bool> # e.g. false
  --CategoryNames: list # e.g. [Equipment Assets]
  --Description: string # e.g. Manufacturing Machine MachineName
  --ExtendedProperties: record
  --Id: string # format: uuid, e.g. a7989492-b2ab-4d90-8abb-4c8284353dd6
  --InstanceType: string # e.g. Element
  --Links: record # shape: {AnalysisTemplates?: string, AttributeTemplates?: string, BaseTemplate?: string, BaseTemplates?: string, Categories?: string, Database?: string, DefaultAttribute?: string, DerivedTemplates?: string, NotificationRuleTemplates?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. MachineName
  --NamingPattern: string # e.g. %TEMPLATE%
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\ElementTemplates[Boiler]
  --Severity: string # e.g. None
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ETDqD5loBNH0erqeqJodtALAkpSYp6uykE2Ku0yChDU91g
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/elementtemplates/($webId)")
  let body = {AllowElementToExtend: $AllowElementToExtend, BaseTemplate: $BaseTemplate, CanBeAcknowledged: $CanBeAcknowledged, CategoryNames: $CategoryNames, Description: $Description, ExtendedProperties: $ExtendedProperties, Id: $Id, InstanceType: $InstanceType, Links: $Links, Name: $Name, NamingPattern: $NamingPattern, Path: $Path, Severity: $Severity, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get analysis templates for an element template.
#
# GET /elementtemplates/{webId}/analysistemplates
# operationId: ElementTemplate_GetAnalysisTemplates
export def "elementtemplates-analysistemplates GetAnalysisTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AnalysisRulePlugInName: string, CategoryNames: list, CreateEnabled: bool, Description: string, GroupId: int, HasNotificationTemplate: bool, HasTarget: bool, Id: string, Links: record, Name: string, OutputTime: string, Path: string, TargetName: string, TimeRulePlugInName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/analysistemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get child attribute templates for an element template.
#
# GET /elementtemplates/{webId}/attributetemplates
# operationId: ElementTemplate_GetAttributeTemplates
export def "elementtemplates-attributetemplates GetAttributeTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --depthFirstTraverse: oneof<nothing, bool> # When 'true', a Depth First traversal will be performed; this starts at the root and explores as far as possible along each branch before backtracking. When 'false', a Breadth First traversal will be performed; this starts at the tree root and explores the neighbor nodes first, then moves onto the next level of neighbors. The default is 'false' (Breadth First).
  --maxCount: int # The maximum number of objects to be returned. The default is 1000.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showDescendants: oneof<nothing, bool> # Specifies if the result should include all descendant attribute templates from the current element template, even indirect ones. The default is 'false'.
  --showInherited: oneof<nothing, bool> # Specifies if the result should include attribute templates inherited from base element templates. The default is 'false'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, DefaultValue: record, Description: string, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "depthFirstTraverse" $depthFirstTraverse "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showDescendants" $showDescendants "scalar") (serialize-qp "showInherited" $showInherited "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/attributetemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an attribute template.
#
# POST /elementtemplates/{webId}/attributetemplates
# operationId: ElementTemplate_CreateAttributeTemplate
# --Links shape: {AttributeTemplates?: string, Categories?: string, ElementTemplate?: string, Parent?: string, Self?: string, Trait?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementtemplates-attributetemplates CreateAttributeTemplate" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Energy Savings Targets]
  --ConfigString: string # e.g. SELECT [Water Use] FROM [Energy Use 2008] WHERE [Asset ID] = '%Element%'
  --DataReferencePlugIn: string # e.g. Table Lookup
  --DefaultUnitsName: string # e.g. liter
  --DefaultUnitsNameAbbreviation: string # e.g. L
  --DefaultValue: record # e.g. 0
  --Description: string # e.g. 2008 Water Use
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 23d027b5-5dd5-41c5-80f1-f8b5c9ff4356
  --IsConfigurationItem: oneof<nothing, bool> # e.g. false
  --IsExcluded: oneof<nothing, bool> # e.g. false
  --IsHidden: oneof<nothing, bool> # e.g. false
  --IsManualDataEntry: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {AttributeTemplates?: string, Categories?: string, ElementTemplate?: string, Parent?: string, Self?: string, Trait?: string}
  --Name: string # e.g. Water(2008)
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\ElementTemplates[MachineName]|Water(2008)
  --TraitName: string # e.g. LimitLoLo
  --Type: string # e.g. Int32
  --TypeQualifier: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1ATEG_auSSsvuECG8ad_p8b25QQkxqWDwIWU6zC4vmgpd4kgtSfQI9VdxUGA8fi1yf9DVg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/attributetemplates" $qp)
  let body = {CategoryNames: $CategoryNames, ConfigString: $ConfigString, DataReferencePlugIn: $DataReferencePlugIn, DefaultUnitsName: $DefaultUnitsName, DefaultUnitsNameAbbreviation: $DefaultUnitsNameAbbreviation, DefaultValue: $DefaultValue, Description: $Description, HasChildren: $HasChildren, Id: $Id, IsConfigurationItem: $IsConfigurationItem, IsExcluded: $IsExcluded, IsHidden: $IsHidden, IsManualDataEntry: $IsManualDataEntry, Links: $Links, Name: $Name, Path: $Path, TraitName: $TraitName, Type: $Type, TypeQualifier: $TypeQualifier, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get base element templates for an element template.
#
# GET /elementtemplates/{webId}/baseelementtemplates
# operationId: ElementTemplate_GetBaseElementTemplates
export def "elementtemplates-baseelementtemplates GetBaseElementTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --maxCount: int # The maximum number of objects to be returned. The default is 1000.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowElementToExtend: bool, BaseTemplate: string, CanBeAcknowledged: bool, CategoryNames: list, Description: string, ExtendedProperties: record, Id: string, InstanceType: string, Links: record, Name: string, NamingPattern: string, Path: string, Severity: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/baseelementtemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an element template's categories.
#
# GET /elementtemplates/{webId}/categories
# operationId: ElementTemplate_GetCategories
export def "elementtemplates-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showInherited: oneof<nothing, bool> # Specifies if the result should include categories inherited from base element templates. The default is 'false'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showInherited" $showInherited "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get derived element templates for an element template.
#
# GET /elementtemplates/{webId}/derivedelementtemplates
# operationId: ElementTemplate_GetDerivedElementTemplates
export def "elementtemplates-derivedelementtemplates GetDerivedElementTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --maxCount: int # The maximum number of objects to be returned. The default is 1000.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showDescendants: oneof<nothing, bool> # Specifies if the result should include all descendant element templates from the current element template, even indirect ones. The default is 'false'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowElementToExtend: bool, BaseTemplate: string, CanBeAcknowledged: bool, CategoryNames: list, Description: string, ExtendedProperties: record, Id: string, InstanceType: string, Links: record, Name: string, NamingPattern: string, Path: string, Severity: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showDescendants" $showDescendants "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/derivedelementtemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get notification rule templates for an element template
#
# GET /elementtemplates/{webId}/notificationruletemplates
# operationId: ElementTemplate_GetNotificationRuleTemplates
export def "elementtemplates-notificationruletemplates GetNotificationRuleTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/notificationruletemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a notification rule template.
#
# POST /elementtemplates/{webId}/notificationruletemplates
# operationId: ElementTemplate_CreateNotificationRuleTemplate
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementtemplates-notificationruletemplates CreateNotificationRuleTemplate" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [MachineNameAlerts]
  --Criteria: string # e.g. Name: EventFrameCriteriaName
  --Description: string # e.g. Description entered by the user
  --Id: string # format: uuid, e.g. e9a984d0-f47c-11e7-8454-00155d029708
  --MultiTriggerEventOption: string # e.g. HighestSeverity
  --Name: string # e.g. MachineName
  --NonrepetitionInterval: string # format: Duration, e.g. PT3S
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\ElementTemplates[Template3]\NotificationRuleTemplates[Notification Rule Template]
  --ResendInterval: string # format: Duration, e.g. PT5S
  --Status: string # e.g. Functioning
  --TemplateName: string # e.g. MachineName Notification Rule Template
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NTDqD5loBNH0erqeqJodtALAtdcX5JH_5xGEKAAVXTSaAg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/notificationruletemplates" $qp)
  let body = {CategoryNames: $CategoryNames, Criteria: $Criteria, Description: $Description, Id: $Id, MultiTriggerEventOption: $MultiTriggerEventOption, Name: $Name, NonrepetitionInterval: $NonrepetitionInterval, Path: $Path, ResendInterval: $ResendInterval, Status: $Status, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the element template for a specified user.
#
# GET /elementtemplates/{webId}/security
# operationId: ElementTemplate_GetSecurity
export def "elementtemplates-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the element template based on the specified criteria. By default, all security entries for this element template are returned.
#
# GET /elementtemplates/{webId}/securityentries
# operationId: ElementTemplate_GetSecurityEntries
export def "elementtemplates-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the element template.
#
# POST /elementtemplates/{webId}/securityentries
# operationId: ElementTemplate_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementtemplates-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the element template.
#
# DELETE /elementtemplates/{webId}/securityentries/{name}
# operationId: ElementTemplate_DeleteSecurityEntry
export def "elementtemplates-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the element template with the specified name.
#
# GET /elementtemplates/{webId}/securityentries/{name}
# operationId: ElementTemplate_GetSecurityEntryByName
export def "elementtemplates-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the element template.
#
# PUT /elementtemplates/{webId}/securityentries/{name}
# operationId: ElementTemplate_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "elementtemplates-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/elementtemplates/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an enumeration set by path.
#
# GET /enumerationsets
# operationId: EnumerationSet_GetByPath
export def "enumerationsets GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the target enumeration set.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<DataServer: string, Database: string, Security: string, SecurityEntries: string, Self: string, Values: string>, Name: string, Path: string, SerializeDescription: bool, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/enumerationsets" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an enumeration set.
#
# DELETE /enumerationsets/{webId}
# operationId: EnumerationSet_Delete
export def "enumerationsets Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/enumerationsets/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an enumeration set.
#
# GET /enumerationsets/{webId}
# operationId: EnumerationSet_Get
export def "enumerationsets Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<DataServer: string, Database: string, Security: string, SecurityEntries: string, Self: string, Values: string>, Name: string, Path: string, SerializeDescription: bool, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an enumeration set by replacing items in its definition.
#
# PATCH /enumerationsets/{webId}
# operationId: EnumerationSet_Update
# --Links shape: {DataServer?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Values?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "enumerationsets Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. Model numbers by brand of vehicle
  --Id: string # format: uuid, e.g. 8e77fc4f-ac91-4029-81d6-fb66bd01d431
  --Links: record # shape: {DataServer?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string, Values?: string}
  --Name: string # e.g. Model Number
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EnumerationSets[Model Number]
  --SerializeDescription: oneof<nothing, bool>
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1MSRDqD5loBNH0erqeqJodtALAT_x3jpGsKUCB1vtmvQHUMQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/enumerationsets/($webId)")
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, SerializeDescription: $SerializeDescription, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an enumeration set's values.
#
# GET /enumerationsets/{webId}/enumerationvalues
# operationId: EnumerationSet_GetValues
export def "enumerationsets-enumerationvalues GetValues" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Parent: string, Path: string, SerializeDescription: bool, SerializeId: bool, SerializeLinks: bool, SerializePath: bool, SerializeWebId: bool, Value: int, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/enumerationvalues" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an enumeration value for a enumeration set.
#
# POST /enumerationsets/{webId}/enumerationvalues
# operationId: EnumerationSet_CreateValue
# --Links shape: {EnumerationSet?: string, Parent?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "enumerationsets-enumerationvalues CreateValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Description: string # e.g. Model Number of CarBrand
  --Id: string # format: uuid, e.g. 982a8694-6fe9-4cb3-a011-09d278649eef
  --Links: record # shape: {EnumerationSet?: string, Parent?: string, Self?: string}
  --Name: string # e.g. CarBrand|Model3
  --Parent: string # e.g. CarBrand
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EnumerationSets[Model Number]\CarBrand|Model3
  --SerializeDescription: oneof<nothing, bool>
  --SerializeId: oneof<nothing, bool>
  --SerializeLinks: oneof<nothing, bool>
  --SerializePath: oneof<nothing, bool>
  --SerializeWebId: oneof<nothing, bool>
  --Value: int # format: int32, e.g. 2005
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1MVRDqD5loBNH0erqeqJodtALAT_x3jpGsKUCB1vtmvQHUMQlIYqmOlvs0ygEQnSeGSe7w
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/enumerationvalues" $qp)
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Parent: $Parent, Path: $Path, SerializeDescription: $SerializeDescription, SerializeId: $SerializeId, SerializeLinks: $SerializeLinks, SerializePath: $SerializePath, SerializeWebId: $SerializeWebId, Value: $Value, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the enumeration set for a specified user.
#
# GET /enumerationsets/{webId}/security
# operationId: EnumerationSet_GetSecurity
export def "enumerationsets-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the enumeration set based on the specified criteria. By default, all security entries for this enumeration set are returned.
#
# GET /enumerationsets/{webId}/securityentries
# operationId: EnumerationSet_GetSecurityEntries
export def "enumerationsets-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the enumeration set.
#
# POST /enumerationsets/{webId}/securityentries
# operationId: EnumerationSet_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "enumerationsets-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the enumeration set.
#
# DELETE /enumerationsets/{webId}/securityentries/{name}
# operationId: EnumerationSet_DeleteSecurityEntry
export def "enumerationsets-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the enumeration set with the specified name.
#
# GET /enumerationsets/{webId}/securityentries/{name}
# operationId: EnumerationSet_GetSecurityEntryByName
export def "enumerationsets-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the enumeration set.
#
# PUT /enumerationsets/{webId}/securityentries/{name}
# operationId: EnumerationSet_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "enumerationsets-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationsets/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an enumeration value by path.
#
# GET /enumerationvalues
# operationId: EnumerationValue_GetByPath
export def "enumerationvalues GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the target enumeration value.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<EnumerationSet: string, Parent: string, Self: string>, Name: string, Parent: string, Path: string, SerializeDescription: bool, SerializeId: bool, SerializeLinks: bool, SerializePath: bool, SerializeWebId: bool, Value: int, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/enumerationvalues" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an enumeration value from an enumeration set.
#
# DELETE /enumerationvalues/{webId}
# operationId: EnumerationValue_DeleteEnumerationValue
export def "enumerationvalues DeleteEnumerationValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/enumerationvalues/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an enumeration value mapping
#
# GET /enumerationvalues/{webId}
# operationId: EnumerationValue_Get
export def "enumerationvalues Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<EnumerationSet: string, Parent: string, Self: string>, Name: string, Parent: string, Path: string, SerializeDescription: bool, SerializeId: bool, SerializeLinks: bool, SerializePath: bool, SerializeWebId: bool, Value: int, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/enumerationvalues/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an enumeration value by replacing items in its definition.
#
# PATCH /enumerationvalues/{webId}
# operationId: EnumerationValue_UpdateEnumerationValue
# --Links shape: {EnumerationSet?: string, Parent?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "enumerationvalues UpdateEnumerationValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. Model Number of CarBrand
  --Id: string # format: uuid, e.g. 982a8694-6fe9-4cb3-a011-09d278649eef
  --Links: record # shape: {EnumerationSet?: string, Parent?: string, Self?: string}
  --Name: string # e.g. CarBrand|Model3
  --Parent: string # e.g. CarBrand
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EnumerationSets[Model Number]\CarBrand|Model3
  --SerializeDescription: oneof<nothing, bool>
  --SerializeId: oneof<nothing, bool>
  --SerializeLinks: oneof<nothing, bool>
  --SerializePath: oneof<nothing, bool>
  --SerializeWebId: oneof<nothing, bool>
  --Value: int # format: int32, e.g. 2005
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1MVRDqD5loBNH0erqeqJodtALAT_x3jpGsKUCB1vtmvQHUMQlIYqmOlvs0ygEQnSeGSe7w
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/enumerationvalues/($webId)")
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Parent: $Parent, Path: $Path, SerializeDescription: $SerializeDescription, SerializeId: $SerializeId, SerializeLinks: $SerializeLinks, SerializePath: $SerializePath, SerializeWebId: $SerializeWebId, Value: $Value, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve an event frame by path.
#
# GET /eventframes
# operationId: EventFrame_GetByPath
export def "eventframes GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the event frame.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list<string>, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record<Annotations: string, Attributes: string, Categories: string, Database: string, DefaultAttribute: string, EndValue: string, EventFrames: string, InterpolatedData: string, Parent: string, PlotData: string, PrimaryReferencedElement: string, RecordedData: string, ReferencedElements: string, Security: string, SecurityEntries: string, Self: string, SummaryData: string, Template: string, Value: string>, Name: string, Path: string, RefElementWebIds: list<string>, Security: record<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Rights: list<string>, WebException: record<Errors: list, StatusCode: int>>, Severity: string, StartTime: string, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/eventframes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve multiple event frames by web ids or paths.
#
# GET /eventframes/multiple
# operationId: EventFrame_GetMultiple
export def "eventframes-multiple GetMultiple" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --asParallel: oneof<nothing, bool> # Specifies if the retrieval processes should be run in parallel on the server. This may improve the response time for large amounts of requested attributes. The default is 'false'.
  --includeMode: string # The include mode for the return list. The default is 'All'.
  --path: list # The path of an event frame. Multiple event frames may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webId: list # The ID of an event frame. Multiple event frames may be specified with multiple instances of the parameter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Exception: record, Identifier: string, IdentifierType: string, Object: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "asParallel" $asParallel "scalar") (serialize-qp "includeMode" $includeMode "scalar") (serialize-qp "path" $path "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webId" $webId "multi") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/eventframes/multiple" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve event frames based on the specified conditions. Returns event frames using the specified search query string.
#
# GET /eventframes/search
# operationId: EventFrame_GetEventFramesQuery
export def "eventframes-search GetEventFramesQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --databaseWebId: string # The ID of the asset database to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the eventframes in the asset database. An example would be: "query=Name:=MyEventFrame* Category:=MyCategory Template:=EFTemplate".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record, Name: string, Path: string, RefElementWebIds: list, Security: record, Severity: string, StartTime: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "databaseWebId" $databaseWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/eventframes/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a link for a "Search EventFrames By Attribute Value" operation, whose queries are specified in the request content. The SearchRoot is specified by the Web Id of the root EventFrame. If the SearchRoot is not specified, then the search starts at the Asset Database. ElementTemplate must be provided as the Web ID of the ElementTemplate, which are used to create the EventFrames. All the attributes in the queries must be defined as AttributeTemplates on the ElementTemplate. An array of attribute value queries are ANDed together to find the desired Element objects. At least one value query must be specified. There are limitations on SearchOperators.
#
# POST /eventframes/searchbyattribute
# operationId: EventFrame_CreateSearchByAttribute
# --ValueQueries item shape: {AttributeName?: string, AttributeUOM?: string, AttributeValue?: record, SearchOperator?: string, WebException?: record}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes-searchbyattribute CreateSearchByAttribute" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --noResults: oneof<nothing, bool> # If false, the response content will contain the first page of the search results. If true, the response content will be empty. The default is false.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --ElementTemplate: string # format: webid, e.g. I1ETDqD5loBNH0erqeqJodtALAjFPVfUpY-02A8uioGDSgIg
  --SearchRoot: string # format: webid, e.g. I1RDDqD5loBNH0erqeqJodtALA8fbgUnyQW02v-gLGIxumSg
  --ValueQueries: list # item shape: {AttributeName?: string, AttributeUOM?: string, AttributeValue?: record, SearchOperator?: string, WebException?: record}
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> record<Items: table<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record, Name: string, Path: string, RefElementWebIds: list, Security: record, Severity: string, StartTime: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "noResults" $noResults "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/eventframes/searchbyattribute" $qp)
  let body = {ElementTemplate: $ElementTemplate, SearchRoot: $SearchRoot, ValueQueries: $ValueQueries, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Execute a "Search EventFrames By Attribute Value" operation.
#
# GET /eventframes/searchbyattribute/{searchId}
# operationId: EventFrame_ExecuteSearchByAttribute
export def "eventframes-searchbyattribute ExecuteSearchByAttribute" [
  searchId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --canBeAcknowledged: oneof<nothing, bool> # Specify the returned event frames' canBeAcknowledged property. The default is no canBeAcknowledged filter.
  --endTime: string # The ending time for the search. endTime must be greater than or equal to the startTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*'.
  --isAcknowledged: oneof<nothing, bool> # Specify the returned event frames' isAcknowledged property. The default no isAcknowledged filter.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding event frames. The default is no filter.
  --referencedElementNameFilter: string # The name query string which must match the name of a referenced element. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies whether the search should include objects nested further than the immediate children of the search root. The default is 'false'.
  --searchMode: string # Determines how the startTime and endTime parameters are treated when searching for event frame objects to be included in the returned collection. The default is 'Overlapped'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --severity: list # Specify that returned event frames must have this severity. Multiple severity values may be specified with multiple instances of the parameter. The default is no severity filter.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --startTime: string # The starting time for the search. startTime must be less than or equal to the endTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*-8h'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record, Name: string, Path: string, RefElementWebIds: list, Security: record, Severity: string, StartTime: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "canBeAcknowledged" $canBeAcknowledged "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "isAcknowledged" $isAcknowledged "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "referencedElementNameFilter" $referencedElementNameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "searchMode" $searchMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "severity" $severity "multi") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/searchbyattribute/($searchId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete an event frame.
#
# DELETE /eventframes/{webId}
# operationId: EventFrame_Delete
export def "eventframes Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/eventframes/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve an event frame.
#
# GET /eventframes/{webId}
# operationId: EventFrame_Get
export def "eventframes Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list<string>, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record<Annotations: string, Attributes: string, Categories: string, Database: string, DefaultAttribute: string, EndValue: string, EventFrames: string, InterpolatedData: string, Parent: string, PlotData: string, PrimaryReferencedElement: string, RecordedData: string, ReferencedElements: string, Security: string, SecurityEntries: string, Self: string, SummaryData: string, Template: string, Value: string>, Name: string, Path: string, RefElementWebIds: list<string>, Security: record<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Rights: list<string>, WebException: record<Errors: list, StatusCode: int>>, Severity: string, StartTime: string, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an event frame by replacing items in its definition.
#
# PATCH /eventframes/{webId}
# operationId: EventFrame_Update
# --Links shape: {Annotations?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, PrimaryReferencedElement?: string, RecordedData?: string, ReferencedElements?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
# --Security shape: {CanAnnotate?: bool, CanDelete?: bool, CanExecute?: bool, CanRead?: bool, CanReadData?: bool, CanSubscribe?: bool, CanSubscribeOthers?: bool, CanWrite?: bool, CanWriteData?: bool, HasAdmin?: bool, Rights?: list, WebException?: record}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --AcknowledgedBy: string # e.g. MyDomain\UserA
  --AcknowledgedDate: string # format: date-time, e.g. 2014-07-30T11:04:23Z
  --AreValuesCaptured: oneof<nothing, bool> # e.g. false
  --CanBeAcknowledged: oneof<nothing, bool> # e.g. true
  --CategoryNames: list # e.g. [Processing Plant]
  --Description: string # e.g. Event Frame of Past Week
  --EndTime: string # format: date-time, e.g. 2014-07-25T14:45:29Z
  --ExtendedProperties: record
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 96f9a00e-4d80-471f-0000-00000004b002
  --IsAcknowledged: oneof<nothing, bool> # e.g. true
  --IsAnnotated: oneof<nothing, bool> # e.g. false
  --IsLocked: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Annotations?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, PrimaryReferencedElement?: string, RecordedData?: string, ReferencedElements?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
  --Name: string # e.g. EF20140725-001
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EventFrames[EF20140725-001]
  --RefElementWebIds: list # e.g. [I1EmDqD5loBNH0erqeqJodtALAaqQoQHk26BGgMQAVXYR0Ag]
  --Security: record # shape: {CanAnnotate?: bool, CanDelete?: bool, CanExecute?: bool, CanRead?: bool, CanReadData?: bool, CanSubscribe?: bool, CanSubscribeOthers?: bool, CanWrite?: bool, CanWriteData?: bool, HasAdmin?: bool, Rights?: list, WebException?: record}
  --Severity: string # e.g. None
  --StartTime: string # format: date-time, e.g. 2014-07-18T14:45:29Z
  --TemplateName: string # e.g. Template
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1FmDqD5loBNH0erqeqJodtALADqD5loBNH0cAAAAAAASwAg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/eventframes/($webId)")
  let body = {AcknowledgedBy: $AcknowledgedBy, AcknowledgedDate: $AcknowledgedDate, AreValuesCaptured: $AreValuesCaptured, CanBeAcknowledged: $CanBeAcknowledged, CategoryNames: $CategoryNames, Description: $Description, EndTime: $EndTime, ExtendedProperties: $ExtendedProperties, HasChildren: $HasChildren, Id: $Id, IsAcknowledged: $IsAcknowledged, IsAnnotated: $IsAnnotated, IsLocked: $IsLocked, Links: $Links, Name: $Name, Path: $Path, RefElementWebIds: $RefElementWebIds, Security: $Security, Severity: $Severity, StartTime: $StartTime, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Calls the EventFrame's Acknowledge method.
#
# PATCH /eventframes/{webId}/acknowledge
# operationId: EventFrame_Acknowledge
export def "eventframes-acknowledge Acknowledge" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/eventframes/($webId)/acknowledge")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an event frame's annotations.
#
# GET /eventframes/{webId}/annotations
# operationId: EventFrame_GetAnnotations
export def "eventframes-annotations GetAnnotations" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CreationDate: string, Creator: string, Description: string, Errors: list, Id: string, Links: record, Modifier: string, ModifyDate: string, Name: string, Value: record, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/annotations" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an annotation on an event frame.
#
# POST /eventframes/{webId}/annotations
# operationId: EventFrame_CreateAnnotation
# --Errors item shape: {FieldName?: string, Message?: list}
# --Links shape: {MediaData?: string, MediaMetadata?: string, Owner?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes-annotations CreateAnnotation" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CreationDate: string # format: date-time, e.g. 2016-06-21T14:45:50.2988321Z
  --Creator: string # e.g. MyDomain\UserA
  --Description: string # e.g. Signifies a spike in temperature.
  --Errors: list # item shape: {FieldName?: string, Message?: list}
  --Id: string # format: uuid, e.g. 512B6616-CE39-4F70-9048-8C6A025FB592
  --Links: record # shape: {MediaData?: string, MediaMetadata?: string, Owner?: string, Self?: string}
  --Modifier: string # e.g. MyDomain\UserA
  --ModifyDate: string # format: date-time, e.g. 2016-06-21T14:45:50.2988321Z
  --Name: string # e.g. Temperature Annotation
  --Value: record # e.g. The temperature spiked because of a malfunction with a unit in our west plant.
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/annotations" $qp)
  let body = {CreationDate: $CreationDate, Creator: $Creator, Description: $Description, Errors: $Errors, Id: $Id, Links: $Links, Modifier: $Modifier, ModifyDate: $ModifyDate, Name: $Name, Value: $Value, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete an annotation on an event frame. If the annotation has attached media, the attached media will also be deleted.
#
# DELETE /eventframes/{webId}/annotations/{id}
# operationId: EventFrame_DeleteAnnotation
export def "eventframes-annotations DeleteAnnotation" [
  id: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/eventframes/($webId)/annotations/($id)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific annotation on an event frame.
#
# GET /eventframes/{webId}/annotations/{id}
# operationId: EventFrame_GetAnnotationById
export def "eventframes-annotations GetAnnotationById" [
  id: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CreationDate: string, Creator: string, Description: string, Errors: table<FieldName: string, Message: list>, Id: string, Links: record<MediaData: string, MediaMetadata: string, Owner: string, Self: string>, Modifier: string, ModifyDate: string, Name: string, Value: record, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/annotations/($id)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update an annotation on an event frame by replacing items in its definition.
#
# PATCH /eventframes/{webId}/annotations/{id}
# operationId: EventFrame_UpdateAnnotation
# --Errors item shape: {FieldName?: string, Message?: list}
# --Links shape: {MediaData?: string, MediaMetadata?: string, Owner?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes-annotations UpdateAnnotation" [
  id: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --CreationDate: string # format: date-time, e.g. 2016-06-21T14:45:50.2988321Z
  --Creator: string # e.g. MyDomain\UserA
  --Description: string # e.g. Signifies a spike in temperature.
  --Errors: list # item shape: {FieldName?: string, Message?: list}
  --Id: string # format: uuid, e.g. 512B6616-CE39-4F70-9048-8C6A025FB592
  --Links: record # shape: {MediaData?: string, MediaMetadata?: string, Owner?: string, Self?: string}
  --Modifier: string # e.g. MyDomain\UserA
  --ModifyDate: string # format: date-time, e.g. 2016-06-21T14:45:50.2988321Z
  --Name: string # e.g. Temperature Annotation
  --Value: record # e.g. The temperature spiked because of a malfunction with a unit in our west plant.
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/eventframes/($webId)/annotations/($id)")
  let body = {CreationDate: $CreationDate, Creator: $Creator, Description: $Description, Errors: $Errors, Id: $Id, Links: $Links, Modifier: $Modifier, ModifyDate: $ModifyDate, Name: $Name, Value: $Value, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete attached media from an annotation on an event frame.
#
# DELETE /eventframes/{webId}/annotations/{id}/attachment/media
# operationId: EventFrame_DeleteAnnotationAttachmentMediaById
export def "eventframes-annotations-attachment-media DeleteAnnotationAttachmentMediaById" [
  id: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/eventframes/($webId)/annotations/($id)/attachment/media")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets the metadata of the media attached to the specified annotation.
#
# GET /eventframes/{webId}/annotations/{id}/attachment/media/metadata
# operationId: EventFrame_GetAnnotationAttachmentMediaMetadataById
export def "eventframes-annotations-attachment-media-metadata GetAnnotationAttachmentMediaMetadataById" [
  id: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Author: string, ChangeDate: string, Description: string, Links: record<MediaData: string, Owner: string, Self: string>, Name: string, Size: float, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/annotations/($id)/attachment/media/metadata" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the attributes of the specified event frame.
#
# GET /eventframes/{webId}/attributes
# operationId: EventFrame_GetAttributes
export def "eventframes-attributes GetAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --categoryName: string # Specify that returned attributes must have this category. The default is no category filter.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --templateName: string # Specify that returned attributes must be members of this template. The default is no template filter.
  --trait: list # The name of the attribute trait. Multiple traits may be specified with multiple instances of the parameter.
  --traitCategory: list # The category of the attribute traits. Multiple categories may be specified with multiple instances of the parameter. If the parameter is not specified, or if its value is "all", then all attribute traits of all categories will be returned.
  --valueType: string # Specify that returned attributes' value type must be the given value type. The default is no value type filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "trait" $trait "multi") (serialize-qp "traitCategory" $traitCategory "multi") (serialize-qp "valueType" $valueType "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/attributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new attribute of the specified event frame.
#
# POST /eventframes/{webId}/attributes
# operationId: EventFrame_CreateAttribute
# --DataReference shape: {PIPoint?: record, Type?: string, WebException?: record}
# --Links shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes-attributes CreateAttribute" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --CategoryNames: list # e.g. [Energy Savings Targets]
  --ConfigString: string # e.g. SELECT [Water Use] FROM [Energy Use 2008] WHERE [Asset ID] = '%Element%'
  --DataReference: record # shape: {PIPoint?: record, Type?: string, WebException?: record}
  --DataReferencePlugIn: string # e.g. Table Lookup
  --DefaultUnitsName: string # e.g. liter
  --DefaultUnitsNameAbbreviation: string # e.g. L
  --Description: string # e.g. 2008 Water Use
  --DisplayDigits: int # e.g. -5
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 75de231b-b591-49b1-b138-e725a77165c2
  --IsConfigurationItem: oneof<nothing, bool> # e.g. false
  --IsExcluded: oneof<nothing, bool> # e.g. false
  --IsHidden: oneof<nothing, bool> # e.g. false
  --IsManualDataEntry: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Attributes?: string, Categories?: string, Element?: string, EndValue?: string, EnumerationSet?: string, EnumerationValues?: string, EventFrame?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, Point?: string, RecordedData?: string, Self?: string, SummaryData?: string, Template?: string, Trait?: string, Value?: string}
  --Name: string # e.g. Water
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName|Water(2008)
  --Paths: list # e.g. [\\MyAssetServer\MyDatabase\MyElement|MyAttribute, \\MyAssetServer\MyDatabase\ReferencingElement\MyElement|MyAttribute]
  --Span: float # e.g. 100
  --Step: oneof<nothing, bool> # e.g. false
  --TraitName: string # e.g. LimitLoLo
  --Type: string # e.g. Int32
  --TypeQualifier: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1AbEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
  --Zero: float # e.g. 0
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/attributes" $qp)
  let body = {CategoryNames: $CategoryNames, ConfigString: $ConfigString, DataReference: $DataReference, DataReferencePlugIn: $DataReferencePlugIn, DefaultUnitsName: $DefaultUnitsName, DefaultUnitsNameAbbreviation: $DefaultUnitsNameAbbreviation, Description: $Description, DisplayDigits: $DisplayDigits, HasChildren: $HasChildren, Id: $Id, IsConfigurationItem: $IsConfigurationItem, IsExcluded: $IsExcluded, IsHidden: $IsHidden, IsManualDataEntry: $IsManualDataEntry, Links: $Links, Name: $Name, Path: $Path, Paths: $Paths, Span: $Span, Step: $Step, TraitName: $TraitName, Type: $Type, TypeQualifier: $TypeQualifier, WebException: $WebException, WebId: $WebId, Zero: $Zero} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Calls the EventFrame's CaptureValues method.
#
# POST /eventframes/{webId}/attributes/capture
# operationId: EventFrame_CaptureValues
export def "eventframes-attributes-capture CaptureValues" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/eventframes/($webId)/attributes/capture")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an event frame's categories.
#
# GET /eventframes/{webId}/categories
# operationId: EventFrame_GetCategories
export def "eventframes-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Executes the create configuration function of the data references found within the attributes of the event frame, and optionally, its children.
#
# POST /eventframes/{webId}/config
# operationId: EventFrame_CreateConfig
export def "eventframes-config CreateConfig" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --includeChildElements: oneof<nothing, bool> # If true, includes the child event frames of the specified event frame.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "includeChildElements" $includeChildElements "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/config" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieves a list of event frame attributes matching the specified filters from the specified event frame.
#
# GET /eventframes/{webId}/eventframeattributes
# operationId: EventFrame_FindEventFrameAttributes
export def "eventframes-eventframeattributes FindEventFrameAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports DataReference to return attributes with data references. If this parameter is not specified, DataReference values are not returned.
  --attributeCategory: string # Specify that returned attributes must have this category. The default is no filter.
  --attributeDescriptionFilter: string # The attribute description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --attributeNameFilter: string # The attribute name filter string used for finding objects. The default is no filter.
  --attributeType: string # Specify that returned attributes' value type must be this value type. The default is no filter.
  --endTime: string # A string representing the latest ending time for the event frames to be matched. The endTime must be greater than or equal to the startTime. The default is '*'.
  --eventFrameCategory: string # Specify that the owner of the returned attributes must have this category. The default is no filter.
  --eventFrameDescriptionFilter: string # The event frame description filter string used for finding objects. Only the first 440 characters of the description will be searched. For Asset Servers older than 2.7, a 400 status code (Bad Request) will be returned if this parameter is specified. The default is no filter.
  --eventFrameNameFilter: string # The event frame name filter string used for finding objects. The default is no filter.
  --eventFrameTemplate: string # Specify that the owner of the returned attributes must have this template or a template derived from this template. The default is no filter.
  --maxCount: int # The maximum number of objects to be returned (the page size). The default is 1000.
  --referencedElementNameFilter: string # The name query string which must match the name of a referenced element. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include objects nested further than immediate children of the given resource. The default is 'false'.
  --searchMode: string # Determines how the startTime and endTime parameters are treated when searching for event frames. The default is 'Overlapped'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --startTime: string # A string representing the earliest starting time for the event frames to be matched. startTime must be less than or equal to the endTime. The default is '*-8h'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, ConfigString: string, DataReference: record, DataReferencePlugIn: string, DefaultUnitsName: string, DefaultUnitsNameAbbreviation: string, Description: string, DisplayDigits: int, HasChildren: bool, Id: string, IsConfigurationItem: bool, IsExcluded: bool, IsHidden: bool, IsManualDataEntry: bool, Links: record, Name: string, Path: string, Paths: list, Span: float, Step: bool, TraitName: string, Type: string, TypeQualifier: string, WebException: record, WebId: string, Zero: float>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "attributeCategory" $attributeCategory "scalar") (serialize-qp "attributeDescriptionFilter" $attributeDescriptionFilter "scalar") (serialize-qp "attributeNameFilter" $attributeNameFilter "scalar") (serialize-qp "attributeType" $attributeType "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "eventFrameCategory" $eventFrameCategory "scalar") (serialize-qp "eventFrameDescriptionFilter" $eventFrameDescriptionFilter "scalar") (serialize-qp "eventFrameNameFilter" $eventFrameNameFilter "scalar") (serialize-qp "eventFrameTemplate" $eventFrameTemplate "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "referencedElementNameFilter" $referencedElementNameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "searchMode" $searchMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/eventframeattributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve event frames based on the specified conditions. By default, returns all children of the specified root event frame that have been active in the past 8 hours.
#
# GET /eventframes/{webId}/eventframes
# operationId: EventFrame_GetEventFrames
export def "eventframes-eventframes GetEventFrames" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --canBeAcknowledged: oneof<nothing, bool> # Specify the returned event frames' canBeAcknowledged property. The default is no canBeAcknowledged filter.
  --categoryName: string # Specify that returned event frames must have this category. The default is no category filter.
  --endTime: string # The ending time for the search. The endTime must be greater than or equal to the startTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --isAcknowledged: oneof<nothing, bool> # Specify the returned event frames' isAcknowledged property. The default no isAcknowledged filter.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --nameFilter: string # The name query string used for finding event frames. The default is no filter.
  --referencedElementNameFilter: string # The name query string which must match the name of a referenced element. The default is no filter.
  --referencedElementTemplateName: string # Specify that returned event frames must have an element in the event frame's referenced elements collection that derives from the template. Specify this parameter by name.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies whether the search should include objects nested further than the immediate children of the search root. The default is 'false'.
  --searchMode: string # Determines how the startTime and endTime parameters are treated when searching for event frame objects to be included in the returned collection. If this parameter is one of the 'Backward*' or 'Forward*' values, none of endTime, sortField, or sortOrder may be specified. The default is 'Overlapped'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --severity: list # Specify that returned event frames must have this severity. Multiple severity values may be specified with multiple instances of the parameter. The default is no severity filter.
  --sortField: string # The field or property of the object used to sort the returned collection. The default is 'Name' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending' if searchMode is not one of the 'Backward*' or 'Forward*' values.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --startTime: string # The starting time for the search. startTime must be less than or equal to the endTime. The searchMode parameter will control whether the comparison will be performed against the event frame's startTime or endTime. The default is '*-8h'.
  --templateName: string # Specify that returned event frames must have this template or a template derived from this template. The default is no template filter. Specify this parameter by name.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AcknowledgedBy: string, AcknowledgedDate: string, AreValuesCaptured: bool, CanBeAcknowledged: bool, CategoryNames: list, Description: string, EndTime: string, ExtendedProperties: record, HasChildren: bool, Id: string, IsAcknowledged: bool, IsAnnotated: bool, IsLocked: bool, Links: record, Name: string, Path: string, RefElementWebIds: list, Security: record, Severity: string, StartTime: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "canBeAcknowledged" $canBeAcknowledged "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "isAcknowledged" $isAcknowledged "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "referencedElementNameFilter" $referencedElementNameFilter "scalar") (serialize-qp "referencedElementTemplateName" $referencedElementTemplateName "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "searchMode" $searchMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "severity" $severity "multi") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/eventframes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create an event frame as a child of the specified event frame.
#
# POST /eventframes/{webId}/eventframes
# operationId: EventFrame_CreateEventFrame
# --Links shape: {Annotations?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, PrimaryReferencedElement?: string, RecordedData?: string, ReferencedElements?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
# --Security shape: {CanAnnotate?: bool, CanDelete?: bool, CanExecute?: bool, CanRead?: bool, CanReadData?: bool, CanSubscribe?: bool, CanSubscribeOthers?: bool, CanWrite?: bool, CanWriteData?: bool, HasAdmin?: bool, Rights?: list, WebException?: record}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes-eventframes CreateEventFrame" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AcknowledgedBy: string # e.g. MyDomain\UserA
  --AcknowledgedDate: string # format: date-time, e.g. 2014-07-30T11:04:23Z
  --AreValuesCaptured: oneof<nothing, bool> # e.g. false
  --CanBeAcknowledged: oneof<nothing, bool> # e.g. true
  --CategoryNames: list # e.g. [Processing Plant]
  --Description: string # e.g. Event Frame of Past Week
  --EndTime: string # format: date-time, e.g. 2014-07-25T14:45:29Z
  --ExtendedProperties: record
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. 96f9a00e-4d80-471f-0000-00000004b002
  --IsAcknowledged: oneof<nothing, bool> # e.g. true
  --IsAnnotated: oneof<nothing, bool> # e.g. false
  --IsLocked: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Annotations?: string, Attributes?: string, Categories?: string, Database?: string, DefaultAttribute?: string, EndValue?: string, EventFrames?: string, InterpolatedData?: string, Parent?: string, PlotData?: string, PrimaryReferencedElement?: string, RecordedData?: string, ReferencedElements?: string, Security?: string, SecurityEntries?: string, Self?: string, SummaryData?: string, Template?: string, Value?: string}
  --Name: string # e.g. EF20140725-001
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\EventFrames[EF20140725-001]
  --RefElementWebIds: list # e.g. [I1EmDqD5loBNH0erqeqJodtALAaqQoQHk26BGgMQAVXYR0Ag]
  --Security: record # shape: {CanAnnotate?: bool, CanDelete?: bool, CanExecute?: bool, CanRead?: bool, CanReadData?: bool, CanSubscribe?: bool, CanSubscribeOthers?: bool, CanWrite?: bool, CanWriteData?: bool, HasAdmin?: bool, Rights?: list, WebException?: record}
  --Severity: string # e.g. None
  --StartTime: string # format: date-time, e.g. 2014-07-18T14:45:29Z
  --TemplateName: string # e.g. Template
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1FmDqD5loBNH0erqeqJodtALADqD5loBNH0cAAAAAAASwAg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/eventframes" $qp)
  let body = {AcknowledgedBy: $AcknowledgedBy, AcknowledgedDate: $AcknowledgedDate, AreValuesCaptured: $AreValuesCaptured, CanBeAcknowledged: $CanBeAcknowledged, CategoryNames: $CategoryNames, Description: $Description, EndTime: $EndTime, ExtendedProperties: $ExtendedProperties, HasChildren: $HasChildren, Id: $Id, IsAcknowledged: $IsAcknowledged, IsAnnotated: $IsAnnotated, IsLocked: $IsLocked, Links: $Links, Name: $Name, Path: $Path, RefElementWebIds: $RefElementWebIds, Security: $Security, Severity: $Severity, StartTime: $StartTime, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve the event frame's referenced elements.
#
# GET /eventframes/{webId}/referencedelements
# operationId: EventFrame_GetReferencedElements
export def "eventframes-referencedelements GetReferencedElements" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Paths to return all paths to the element. If this parameter is not specified, paths are not returned.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Description: string, Errors: list, ExtendedProperties: record, HasChildren: bool, Id: string, Links: record, Name: string, Path: string, Paths: list, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/referencedelements" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the security information of the specified security item associated with the event frame for a specified user.
#
# GET /eventframes/{webId}/security
# operationId: EventFrame_GetSecurity
export def "eventframes-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the event frame based on the specified criteria. By default, all security entries for this event frame are returned.
#
# GET /eventframes/{webId}/securityentries
# operationId: EventFrame_GetSecurityEntries
export def "eventframes-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the event frame.
#
# POST /eventframes/{webId}/securityentries
# operationId: EventFrame_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the event frame.
#
# DELETE /eventframes/{webId}/securityentries/{name}
# operationId: EventFrame_DeleteSecurityEntry
export def "eventframes-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the event frame with the specified name.
#
# GET /eventframes/{webId}/securityentries/{name}
# operationId: EventFrame_GetSecurityEntryByName
export def "eventframes-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the event frame.
#
# PUT /eventframes/{webId}/securityentries/{name}
# operationId: EventFrame_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "eventframes-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/eventframes/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a notification contact template by path.
#
# GET /notificationcontacttemplates
# operationId: NotificationContactTemplate_GetByPath
export def "notificationcontacttemplates GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the notification contact template.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Available: bool, ConfigString: string, ContactType: string, Description: string, EscalationTimeout: string, HasChildren: bool, Id: string, Links: record<AssetServer: string, NotificationContactTemplates: string, NotificationPlugIn: string, Security: string, SecurityEntries: string, Self: string>, MaximumRetries: int, MinimumAcknowledgements: int, Name: string, NotifyWhenInstanceEnded: bool, Path: string, PlugInName: string, RetryInterval: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationcontacttemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve notification contact templates based on the specified conditions. Returns notification contact templates using the specified search query string.
#
# GET /notificationcontacttemplates/search
# operationId: NotificationContactTemplate_GetNotificationContactTemplatesQuery
export def "notificationcontacttemplates-search GetNotificationContactTemplatesQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --assetServerWebId: string # The ID of the asset server to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the Notification Contact Templates in the asset database. An example would be: "query=Name:='MyNotificationContactTemplate'".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Available: bool, ConfigString: string, ContactType: string, Description: string, EscalationTimeout: string, HasChildren: bool, Id: string, Links: record, MaximumRetries: int, MinimumAcknowledgements: int, Name: string, NotifyWhenInstanceEnded: bool, Path: string, PlugInName: string, RetryInterval: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "assetServerWebId" $assetServerWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationcontacttemplates/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a notification contact template.
#
# DELETE /notificationcontacttemplates/{webId}
# operationId: NotificationContactTemplate_Delete
export def "notificationcontacttemplates Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a notification contact template.
#
# GET /notificationcontacttemplates/{webId}
# operationId: NotificationContactTemplate_Get
export def "notificationcontacttemplates Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Available: bool, ConfigString: string, ContactType: string, Description: string, EscalationTimeout: string, HasChildren: bool, Id: string, Links: record<AssetServer: string, NotificationContactTemplates: string, NotificationPlugIn: string, Security: string, SecurityEntries: string, Self: string>, MaximumRetries: int, MinimumAcknowledgements: int, Name: string, NotifyWhenInstanceEnded: bool, Path: string, PlugInName: string, RetryInterval: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a notification contact template by replacing items in its definition.
#
# PATCH /notificationcontacttemplates/{webId}
# operationId: NotificationContactTemplate_Update
# --Links shape: {AssetServer?: string, NotificationContactTemplates?: string, NotificationPlugIn?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationcontacttemplates Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Available: oneof<nothing, bool> # e.g. true
  --ConfigString: string # e.g. 
  --ContactType: string # e.g. Individual
  --Description: string # e.g. 
  --EscalationTimeout: string # format: Duration, e.g. PT3S
  --HasChildren: oneof<nothing, bool> # e.g. false
  --Id: string # format: uuid, e.g. cbb28260-853d-11e4-80c5-00155d844304
  --Links: record # shape: {AssetServer?: string, NotificationContactTemplates?: string, NotificationPlugIn?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --MaximumRetries: int # e.g. 5
  --MinimumAcknowledgements: int # e.g. 2
  --Name: string # e.g. Plant Manager
  --NotifyWhenInstanceEnded: oneof<nothing, bool> # e.g. true
  --Path: string # format: path, e.g. \\MyAssetServer\NotificationContactTemplates[Plant Manager]
  --PlugInName: string # e.g. 
  --RetryInterval: string # format: Duration, e.g. PT5S
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NCEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)")
  let body = {Available: $Available, ConfigString: $ConfigString, ContactType: $ContactType, Description: $Description, EscalationTimeout: $EscalationTimeout, HasChildren: $HasChildren, Id: $Id, Links: $Links, MaximumRetries: $MaximumRetries, MinimumAcknowledgements: $MinimumAcknowledgements, Name: $Name, NotifyWhenInstanceEnded: $NotifyWhenInstanceEnded, Path: $Path, PlugInName: $PlugInName, RetryInterval: $RetryInterval, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve notification contact template's child templates.
#
# GET /notificationcontacttemplates/{webId}/notificationcontacttemplates
# operationId: NotificationContactTemplate_GetNotificationContactTemplates
export def "notificationcontacttemplates-notificationcontacttemplates GetNotificationContactTemplates" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Available: bool, ConfigString: string, ContactType: string, Description: string, EscalationTimeout: string, HasChildren: bool, Id: string, Links: record<AssetServer: string, NotificationContactTemplates: string, NotificationPlugIn: string, Security: string, SecurityEntries: string, Self: string>, MaximumRetries: int, MinimumAcknowledgements: int, Name: string, NotifyWhenInstanceEnded: bool, Path: string, PlugInName: string, RetryInterval: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)/notificationcontacttemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the security information of the specified security item associated with the notification contact template for a specified user.
#
# GET /notificationcontacttemplates/{webId}/security
# operationId: NotificationContactTemplate_GetSecurity
export def "notificationcontacttemplates-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the notification contact template based on the specified criteria. By default, all security entries for this notification contact template are returned.
#
# GET /notificationcontacttemplates/{webId}/securityentries
# operationId: NotificationContactTemplate_GetSecurityEntries
export def "notificationcontacttemplates-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the notification contact template.
#
# POST /notificationcontacttemplates/{webId}/securityentries
# operationId: NotificationContactTemplate_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationcontacttemplates-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the notification contact template.
#
# DELETE /notificationcontacttemplates/{webId}/securityentries/{name}
# operationId: NotificationContactTemplate_DeleteSecurityEntry
export def "notificationcontacttemplates-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the notification contact template with the specified name.
#
# GET /notificationcontacttemplates/{webId}/securityentries/{name}
# operationId: NotificationContactTemplate_GetSecurityEntryByName
export def "notificationcontacttemplates-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the notification contact template.
#
# PUT /notificationcontacttemplates/{webId}/securityentries/{name}
# operationId: NotificationContactTemplate_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationcontacttemplates-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationcontacttemplates/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a notification plugin by path.
#
# GET /notificationplugIns
# operationId: NotificationPlugIn_GetByPath
export def "notificationplug-ins GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the notification plugin.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list<string>, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record<AssetServer: string, Self: string>, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationplugIns" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a notification plugin.
#
# GET /notificationplugins/{webId}
# operationId: NotificationPlugIn_Get
export def "notificationplugins Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list<string>, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record<AssetServer: string, Self: string>, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationplugins/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a notification rule by path.
#
# GET /notificationrules
# operationId: NotificationRule_GetByPath
export def "notificationrules GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the notification rule.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AutoCreated: bool, CategoryNames: list<string>, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationrules" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve notification rules based on the specified conditions. Returns notification rules using the specified search query string.
#
# GET /notificationrules/search
# operationId: NotificationRule_GetNotificationRulesQuery
export def "notificationrules-search GetNotificationRulesQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --databaseWebId: string # The ID of the asset database to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the Notification rules in the asset database. An example would be: "query=Name:=MyNotificationRule* Template:=NoteRuleTemplate".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AutoCreated: bool, CategoryNames: list, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "databaseWebId" $databaseWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationrules/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a notification rule.
#
# DELETE /notificationrules/{webId}
# operationId: NotificationRule_Delete
export def "notificationrules Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationrules/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a notification rule.
#
# GET /notificationrules/{webId}
# operationId: NotificationRule_Get
export def "notificationrules Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AutoCreated: bool, CategoryNames: list<string>, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a notification rule by replacing items in its definition.
#
# PATCH /notificationrules/{webId}
# operationId: NotificationRule_Update
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationrules Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --AutoCreated: oneof<nothing, bool> # e.g. true
  --CategoryNames: list # e.g. [Equipment Assets]
  --Criteria: string # e.g. Name: EventFrameCriteriaName
  --Description: string # e.g. Manufacturing Equipment MachineName
  --Id: string # format: uuid, e.g. e9a984d0-f47c-11e7-8454-00155d029708
  --MultiTriggerEventOption: string # e.g. HighestSeverity
  --Name: string # e.g. MachineName
  --NonrepetitionInterval: string # format: Duration, e.g. PT3S
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\EngineeringProcess\Equipment\MachineName\NotificationRules[RuleName]
  --ResendInterval: string # format: Duration, e.g. PT5S
  --Status: string # e.g. Disabled
  --TemplateName: string # e.g. MachineName Notification Rule
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NRDqD5loBNH0erqeqJodtALA5bYfWno26BGgMQAVXYR0Ag
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationrules/($webId)")
  let body = {AutoCreated: $AutoCreated, CategoryNames: $CategoryNames, Criteria: $Criteria, Description: $Description, Id: $Id, MultiTriggerEventOption: $MultiTriggerEventOption, Name: $Name, NonrepetitionInterval: $NonrepetitionInterval, Path: $Path, ResendInterval: $ResendInterval, Status: $Status, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve notification rule subscribers.
#
# GET /notificationrules/{webId}/notificationrulesubscribers
# operationId: NotificationRule_GetNotificationRuleSubscribers
export def "notificationrules-notificationrulesubscribers GetNotificationRuleSubscribers" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<ConfigString: string, ContactTemplateName: string, ContactType: string, DeliveryFormatName: string, Description: string, EscalationTimeout: string, Id: string, MaximumRetries: int, Name: string, NotifyOption: string, Path: string, PlugInName: string, RetryInterval: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/notificationrulesubscribers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a notification rule subscriber.
#
# POST /notificationrules/{webId}/notificationrulesubscribers
# operationId: NotificationRule_CreateNotificationRuleSubscriber
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationrules-notificationrulesubscribers CreateNotificationRuleSubscriber" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --ConfigString: string # e.g. ToEmail=mike@testemail.com
  --ContactTemplateName: string # e.g. Plant Manager
  --ContactType: string # e.g. Individual
  --DeliveryFormatName: string # e.g. DeliveryFormat
  --Description: string # e.g. Manufacturing Machine MachineName subscription
  --EscalationTimeout: string # format: Duration, e.g. PT3S
  --Id: string # format: uuid, e.g. e9a984d0-f47c-11e7-8454-00155d029708
  --MaximumRetries: int # e.g. 3
  --Name: string # e.g. NotificationSubscriptionRuleName
  --NotifyOption: string # e.g. EventStart
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\NotificationRules[NotificationSubscriptionRuleName]
  --PlugInName: string # e.g. Email
  --RetryInterval: string # format: Duration, e.g. PT3S
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NSLDqD5loBNH0erqeqJodtALA5bYfWno26BGgMQAVXYR0AgPUJJXNlEW1w78rnCXDmcDA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/notificationrulesubscribers" $qp)
  let body = {ConfigString: $ConfigString, ContactTemplateName: $ContactTemplateName, ContactType: $ContactType, DeliveryFormatName: $DeliveryFormatName, Description: $Description, EscalationTimeout: $EscalationTimeout, Id: $Id, MaximumRetries: $MaximumRetries, Name: $Name, NotifyOption: $NotifyOption, Path: $Path, PlugInName: $PlugInName, RetryInterval: $RetryInterval, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the notification rule for a specified user.
#
# GET /notificationrules/{webId}/security
# operationId: NotificationRule_GetSecurity
export def "notificationrules-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the notification rule based on the specified criteria. By default, all security entries for this notification rule are returned.
#
# GET /notificationrules/{webId}/securityentries
# operationId: NotificationRule_GetSecurityEntries
export def "notificationrules-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the notification rule.
#
# POST /notificationrules/{webId}/securityentries
# operationId: NotificationRule_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationrules-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the notification rule.
#
# DELETE /notificationrules/{webId}/securityentries/{name}
# operationId: NotificationRule_DeleteSecurityEntry
export def "notificationrules-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the notification rule with the specified name.
#
# GET /notificationrules/{webId}/securityentries/{name}
# operationId: NotificationRule_GetSecurityEntryByName
export def "notificationrules-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the notification rule.
#
# PUT /notificationrules/{webId}/securityentries/{name}
# operationId: NotificationRule_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationrules-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrules/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a notification rule subscriber by path.
#
# GET /notificationrulesubscribers
# operationId: NotificationRuleSubscriber_GetByPath
export def "notificationrulesubscribers GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the notification rule subscriber.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<ConfigString: string, ContactTemplateName: string, ContactType: string, DeliveryFormatName: string, Description: string, EscalationTimeout: string, Id: string, MaximumRetries: int, Name: string, NotifyOption: string, Path: string, PlugInName: string, RetryInterval: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationrulesubscribers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a notification rule subscriber.
#
# DELETE /notificationrulesubscribers/{webId}
# operationId: NotificationRuleSubscriber_Delete
export def "notificationrulesubscribers Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationrulesubscribers/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a notification rule subscriber.
#
# GET /notificationrulesubscribers/{webId}
# operationId: NotificationRuleSubscriber_Get
export def "notificationrulesubscribers Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<ConfigString: string, ContactTemplateName: string, ContactType: string, DeliveryFormatName: string, Description: string, EscalationTimeout: string, Id: string, MaximumRetries: int, Name: string, NotifyOption: string, Path: string, PlugInName: string, RetryInterval: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrulesubscribers/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a notification rule subscriber.
#
# PATCH /notificationrulesubscribers/{webId}
# operationId: NotificationRuleSubscriber_Update
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationrulesubscribers Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --ConfigString: string # e.g. ToEmail=mike@testemail.com
  --ContactTemplateName: string # e.g. Plant Manager
  --ContactType: string # e.g. Individual
  --DeliveryFormatName: string # e.g. DeliveryFormat
  --Description: string # e.g. Manufacturing Machine MachineName subscription
  --EscalationTimeout: string # format: Duration, e.g. PT3S
  --Id: string # format: uuid, e.g. e9a984d0-f47c-11e7-8454-00155d029708
  --MaximumRetries: int # e.g. 3
  --Name: string # e.g. NotificationSubscriptionRuleName
  --NotifyOption: string # e.g. EventStart
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\NotificationRules[NotificationSubscriptionRuleName]
  --PlugInName: string # e.g. Email
  --RetryInterval: string # format: Duration, e.g. PT3S
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NSLDqD5loBNH0erqeqJodtALA5bYfWno26BGgMQAVXYR0AgPUJJXNlEW1w78rnCXDmcDA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationrulesubscribers/($webId)")
  let body = {ConfigString: $ConfigString, ContactTemplateName: $ContactTemplateName, ContactType: $ContactType, DeliveryFormatName: $DeliveryFormatName, Description: $Description, EscalationTimeout: $EscalationTimeout, Id: $Id, MaximumRetries: $MaximumRetries, Name: $Name, NotifyOption: $NotifyOption, Path: $Path, PlugInName: $PlugInName, RetryInterval: $RetryInterval, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve notification rule subscriber subscribers.
#
# GET /notificationrulesubscribers/{webId}/notificationrulesubscribers
# operationId: NotificationRuleSubscriber_GetNotificationRuleSubscribers
export def "notificationrulesubscribers-notificationrulesubscribers GetNotificationRuleSubscribers" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<ConfigString: string, ContactTemplateName: string, ContactType: string, DeliveryFormatName: string, Description: string, EscalationTimeout: string, Id: string, MaximumRetries: int, Name: string, NotifyOption: string, Path: string, PlugInName: string, RetryInterval: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationrulesubscribers/($webId)/notificationrulesubscribers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a notification rule template by path.
#
# GET /notificationruletemplates
# operationId: NotificationRuleTemplate_GetByPath
export def "notificationruletemplates GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the notification rule template.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationruletemplates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve Notification rule templates based on the specified conditions. Returns Notification rule templates using the specified search query string.
#
# GET /notificationruletemplates/search
# operationId: NotificationRuleTemplate_GetNotificationRuleTemplatesQuery
export def "notificationruletemplates-search GetNotificationRuleTemplatesQuery" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --databaseWebId: string # The ID of the asset database to use as the root of the query.
  --maxCount: int # The maximum number of objects to be returned per call (page size). The default is 1000.
  --query: string # The query string is a list of filters used to perform an AFSearch for the Notification rule templates in the asset database. An example would be: "query=NotificationRuleTemplate:{ Name:='MyNotificationRuleTemplate' } Type:=Int32".
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startIndex: int # The starting index (zero based) of the items to be returned. The default is 0.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CategoryNames: list, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "databaseWebId" $databaseWebId "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "query" $query "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startIndex" $startIndex "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/notificationruletemplates/search" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a notification rule template.
#
# DELETE /notificationruletemplates/{webId}
# operationId: NotificationRuleTemplate_Delete
export def "notificationruletemplates Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationruletemplates/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the specified notification rule template.
#
# GET /notificationruletemplates/{webId}
# operationId: NotificationRuleTemplate_Get
export def "notificationruletemplates Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, Criteria: string, Description: string, Id: string, MultiTriggerEventOption: string, Name: string, NonrepetitionInterval: string, Path: string, ResendInterval: string, Status: string, TemplateName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a notification rule template by replacing items in its definition.
#
# PATCH /notificationruletemplates/{webId}
# operationId: NotificationRuleTemplate_Update
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationruletemplates Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --CategoryNames: list # e.g. [MachineNameAlerts]
  --Criteria: string # e.g. Name: EventFrameCriteriaName
  --Description: string # e.g. Description entered by the user
  --Id: string # format: uuid, e.g. e9a984d0-f47c-11e7-8454-00155d029708
  --MultiTriggerEventOption: string # e.g. HighestSeverity
  --Name: string # e.g. MachineName
  --NonrepetitionInterval: string # format: Duration, e.g. PT3S
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\CityName\ElementTemplates[Template3]\NotificationRuleTemplates[Notification Rule Template]
  --ResendInterval: string # format: Duration, e.g. PT5S
  --Status: string # e.g. Functioning
  --TemplateName: string # e.g. MachineName Notification Rule Template
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NTDqD5loBNH0erqeqJodtALAtdcX5JH_5xGEKAAVXTSaAg
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/notificationruletemplates/($webId)")
  let body = {CategoryNames: $CategoryNames, Criteria: $Criteria, Description: $Description, Id: $Id, MultiTriggerEventOption: $MultiTriggerEventOption, Name: $Name, NonrepetitionInterval: $NonrepetitionInterval, Path: $Path, ResendInterval: $ResendInterval, Status: $Status, TemplateName: $TemplateName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve notification rule template subscribers.
#
# GET /notificationruletemplates/{webId}/notificationrulesubscribers
# operationId: NotificationRuleTemplate_GetNotificationRuleTemplateSubscribers
export def "notificationruletemplates-notificationrulesubscribers GetNotificationRuleTemplateSubscribers" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<ConfigString: string, ContactTemplateName: string, ContactType: string, DeliveryFormatName: string, Description: string, EscalationTimeout: string, Id: string, MaximumRetries: int, Name: string, NotifyOption: string, Path: string, PlugInName: string, RetryInterval: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/notificationrulesubscribers" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a notification rule subscriber.
#
# POST /notificationruletemplates/{webId}/notificationrulesubscribers
# operationId: NotificationRuleTemplate_CreateNotificationRuleTemplateSubscriber
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationruletemplates-notificationrulesubscribers CreateNotificationRuleTemplateSubscriber" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --ConfigString: string # e.g. ToEmail=mike@testemail.com
  --ContactTemplateName: string # e.g. Plant Manager
  --ContactType: string # e.g. Individual
  --DeliveryFormatName: string # e.g. DeliveryFormat
  --Description: string # e.g. Manufacturing Machine MachineName subscription
  --EscalationTimeout: string # format: Duration, e.g. PT3S
  --Id: string # format: uuid, e.g. e9a984d0-f47c-11e7-8454-00155d029708
  --MaximumRetries: int # e.g. 3
  --Name: string # e.g. NotificationSubscriptionRuleName
  --NotifyOption: string # e.g. EventStart
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\NotificationRules[NotificationSubscriptionRuleName]
  --PlugInName: string # e.g. Email
  --RetryInterval: string # format: Duration, e.g. PT3S
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1NSLDqD5loBNH0erqeqJodtALA5bYfWno26BGgMQAVXYR0AgPUJJXNlEW1w78rnCXDmcDA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/notificationrulesubscribers" $qp)
  let body = {ConfigString: $ConfigString, ContactTemplateName: $ContactTemplateName, ContactType: $ContactType, DeliveryFormatName: $DeliveryFormatName, Description: $Description, EscalationTimeout: $EscalationTimeout, Id: $Id, MaximumRetries: $MaximumRetries, Name: $Name, NotifyOption: $NotifyOption, Path: $Path, PlugInName: $PlugInName, RetryInterval: $RetryInterval, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the notification rule template for a specified user.
#
# GET /notificationruletemplates/{webId}/security
# operationId: NotificationRuleTemplate_GetSecurity
export def "notificationruletemplates-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the notification rule template based on the specified criteria. By default, all security entries for this notification rule template are returned.
#
# GET /notificationruletemplates/{webId}/securityentries
# operationId: NotificationRuleTemplate_GetSecurityEntries
export def "notificationruletemplates-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the notification rule template.
#
# POST /notificationruletemplates/{webId}/securityentries
# operationId: NotificationRuleTemplate_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationruletemplates-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the notification rule template.
#
# DELETE /notificationruletemplates/{webId}/securityentries/{name}
# operationId: NotificationRuleTemplate_DeleteSecurityEntry
export def "notificationruletemplates-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the notification rule template with the specified name.
#
# GET /notificationruletemplates/{webId}/securityentries/{name}
# operationId: NotificationRuleTemplate_GetSecurityEntryByName
export def "notificationruletemplates-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the notification rule template.
#
# PUT /notificationruletemplates/{webId}/securityentries/{name}
# operationId: NotificationRuleTemplate_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "notificationruletemplates-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/notificationruletemplates/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a point by path.
#
# GET /points
# operationId: Point_GetByPath
export def "points GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the point.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Descriptor: string, DigitalSetName: string, DisplayDigits: int, EngineeringUnits: string, Future: bool, Id: int, Links: record<Attributes: string, DataServer: string, EndValue: string, InterpolatedData: string, PlotData: string, RecordedData: string, Self: string, SummaryData: string, Value: string>, Name: string, Path: string, PointClass: string, PointType: string, Span: float, Step: bool, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string, Zero: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/points" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve multiple points by web id or path.
#
# GET /points/multiple
# operationId: Point_GetMultiple
export def "points-multiple GetMultiple" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --asParallel: oneof<nothing, bool> # Specifies if the retrieval processes should be run in parallel on the server. This may improve the response time for large amounts of requested points. The default is 'false'.
  --includeMode: string # The include mode for the return list. The default is 'All'.
  --path: list # The path of a point. Multiple points may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webId: list # The ID of a point. Multiple points may be specified with multiple instances of the parameter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Exception: record, Identifier: string, IdentifierType: string, Object: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "asParallel" $asParallel "scalar") (serialize-qp "includeMode" $includeMode "scalar") (serialize-qp "path" $path "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webId" $webId "multi") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/points/multiple" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a point.
#
# DELETE /points/{webId}
# operationId: Point_Delete
export def "points Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/points/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a point.
#
# GET /points/{webId}
# operationId: Point_Get
export def "points Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Descriptor: string, DigitalSetName: string, DisplayDigits: int, EngineeringUnits: string, Future: bool, Id: int, Links: record<Attributes: string, DataServer: string, EndValue: string, InterpolatedData: string, PlotData: string, RecordedData: string, Self: string, SummaryData: string, Value: string>, Name: string, Path: string, PointClass: string, PointType: string, Span: float, Step: bool, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string, Zero: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/points/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a point. The only PI Point attributes that can be updated include: Name, Descriptor, EngineeringUnits, Step, and DisplayDigits. Other PI Point attributes cannot be updated through PI Web API.
#
# PATCH /points/{webId}
# operationId: Point_Update
# --Links shape: {Attributes?: string, DataServer?: string, EndValue?: string, InterpolatedData?: string, PlotData?: string, RecordedData?: string, Self?: string, SummaryData?: string, Value?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "points Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Descriptor: string # e.g. 12 Hour Sine Wave
  --DigitalSetName: string # e.g. 
  --DisplayDigits: int # e.g. -5
  --EngineeringUnits: string # e.g. 
  --Future: oneof<nothing, bool> # e.g. false
  --Id: int # format: int32, e.g. 82
  --Links: record # shape: {Attributes?: string, DataServer?: string, EndValue?: string, InterpolatedData?: string, PlotData?: string, RecordedData?: string, Self?: string, SummaryData?: string, Value?: string}
  --Name: string # e.g. PointName
  --Path: string # format: path, e.g. \\MyPIServer\PointName
  --PointClass: string # e.g. classic
  --PointType: string # e.g. Float32
  --Span: float # e.g. 100
  --Step: oneof<nothing, bool> # e.g. false
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1DPa70Wf0zBA06CLkV9ovNQgQCAAAAA
  --Zero: float # e.g. 0
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/points/($webId)")
  let body = {Descriptor: $Descriptor, DigitalSetName: $DigitalSetName, DisplayDigits: $DisplayDigits, EngineeringUnits: $EngineeringUnits, Future: $Future, Id: $Id, Links: $Links, Name: $Name, Path: $Path, PointClass: $PointClass, PointType: $PointType, Span: $Span, Step: $Step, WebException: $WebException, WebId: $WebId, Zero: $Zero} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get point attributes.
#
# GET /points/{webId}/attributes
# operationId: Point_GetAttributes
export def "points-attributes GetAttributes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --name: list # The name of a point attribute to be returned. Multiple attributes may be specified with multiple instances of the parameter.
  --nameFilter: string # The filter to the names of the list of point attributes to be returned. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Links: record, Name: string, Value: record, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "name" $name "multi") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/points/($webId)/attributes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a point attribute by name.
#
# GET /points/{webId}/attributes/{name}
# operationId: Point_GetAttributeByName
export def "points-attributes GetAttributeByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Links: record<Point: string, Self: string>, Name: string, Value: record, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/points/($webId)/attributes/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a security identity by path.
#
# GET /securityidentities
# operationId: SecurityIdentity_GetByPath
export def "securityidentities GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the security identity.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, IsEnabled: bool, Links: record<AssetServer: string, Security: string, SecurityEntries: string, SecurityMappings: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/securityidentities" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a security identity.
#
# DELETE /securityidentities/{webId}
# operationId: SecurityIdentity_Delete
export def "securityidentities Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/securityidentities/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a security identity.
#
# GET /securityidentities/{webId}
# operationId: SecurityIdentity_Get
export def "securityidentities Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, IsEnabled: bool, Links: record<AssetServer: string, Security: string, SecurityEntries: string, SecurityMappings: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securityidentities/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security identity by replacing items in its definition.
#
# PATCH /securityidentities/{webId}
# operationId: SecurityIdentity_Update
# --Links shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityMappings?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "securityidentities Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. 
  --Id: string # format: uuid, e.g. f3a5ee49-1838-4aa2-9d79-e16294523db7
  --IsEnabled: oneof<nothing, bool> # e.g. true
  --Links: record # shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityMappings?: string, Self?: string}
  --Name: string # e.g. MySecurityIdentity
  --Path: string # format: path, e.g. \\MyAssetServer\SecurityIdentities[MySecurityIdentity]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1SIDqD5loBNH0erqeqJodtALASe6l8zgYokqdeeFilFI9tw
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/securityidentities/($webId)")
  let body = {Description: $Description, Id: $Id, IsEnabled: $IsEnabled, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the security identity for a specified user.
#
# GET /securityidentities/{webId}/security
# operationId: SecurityIdentity_GetSecurity
export def "securityidentities-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securityidentities/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the security identity based on the specified criteria. By default, all security entries for this security identity are returned.
#
# GET /securityidentities/{webId}/securityentries
# operationId: SecurityIdentity_GetSecurityEntries
export def "securityidentities-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securityidentities/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the security identity with the specified name.
#
# GET /securityidentities/{webId}/securityentries/{name}
# operationId: SecurityIdentity_GetSecurityEntryByName
export def "securityidentities-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securityidentities/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get security mappings for the specified security identity.
#
# GET /securityidentities/{webId}/securitymappings
# operationId: SecurityIdentity_GetSecurityMappings
export def "securityidentities-securitymappings GetSecurityMappings" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Account: string, Description: string, Id: string, Links: record, Name: string, Path: string, SecurityIdentityWebId: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securityidentities/($webId)/securitymappings" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a security mapping by path.
#
# GET /securitymappings
# operationId: SecurityMapping_GetByPath
export def "securitymappings GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the security mapping.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Account: string, Description: string, Id: string, Links: record<AssetServer: string, Security: string, SecurityEntries: string, SecurityIdentity: string, Self: string>, Name: string, Path: string, SecurityIdentityWebId: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/securitymappings" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a security mapping.
#
# DELETE /securitymappings/{webId}
# operationId: SecurityMapping_Delete
export def "securitymappings Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/securitymappings/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a security mapping.
#
# GET /securitymappings/{webId}
# operationId: SecurityMapping_Get
export def "securitymappings Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Account: string, Description: string, Id: string, Links: record<AssetServer: string, Security: string, SecurityEntries: string, SecurityIdentity: string, Self: string>, Name: string, Path: string, SecurityIdentityWebId: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securitymappings/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security mapping by replacing items in its definition.
#
# PATCH /securitymappings/{webId}
# operationId: SecurityMapping_Update
# --Links shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "securitymappings Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Account: string # e.g. domain\user
  --Description: string # e.g. 
  --Id: string # format: uuid, e.g. ac14ef82-19c0-4a07-96a7-d1fb0b84c05d
  --Links: record # shape: {AssetServer?: string, Security?: string, SecurityEntries?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. MySecurityMapping
  --Path: string # format: path, e.g. \\MyAssetServer\SecurityMappings[MySecurityMapping]
  --SecurityIdentityWebId: string # e.g. I1SIEDqD5loBNH0erqeqJodtALAYIKyyz2F5BGAxQAVXYRDBAGyPedZG1sUmxOOclp3Flwg
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1SMDqD5loBNH0erqeqJodtALAgu8UrMAZB0qWp9H7C4TAXQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/securitymappings/($webId)")
  let body = {Account: $Account, Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, SecurityIdentityWebId: $SecurityIdentityWebId, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the security mapping for a specified user.
#
# GET /securitymappings/{webId}/security
# operationId: SecurityMapping_GetSecurity
export def "securitymappings-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securitymappings/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the security mapping based on the specified criteria. By default, all security entries for this security mapping are returned.
#
# GET /securitymappings/{webId}/securityentries
# operationId: SecurityMapping_GetSecurityEntries
export def "securitymappings-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securitymappings/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the security mapping with the specified name.
#
# GET /securitymappings/{webId}/securityentries/{name}
# operationId: SecurityMapping_GetSecurityEntryByName
export def "securitymappings-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/securitymappings/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Receive stream updates
#
# GET /streams/updates/{marker}
# operationId: Stream_RetrieveStreamUpdate
export def "streams-updates RetrieveStreamUpdate" [
  marker: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Events: table<Action: string, Annotated: bool, Errors: list, Good: bool, PreviousEventAction: string, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, Exception: record<Errors: list<string>>, LatestMarker: string, RequestedMarker: string, Source: string, SourceName: string, SourcePath: string, Status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/updates/($marker)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Opens a channel that will send messages about any value changes for the specified stream.
#
# GET /streams/{webId}/channel
# operationId: Stream_GetChannel
export def "streams-channel GetChannel" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --heartbeatRate: int # HeartbeatRate is an integer multiple of the Polling Interval. It specifies the rate at which a client will receive an empty message if there are no data updates. It can be used to check that the connection is still alive. Zero/negative values correspond to no heartbeat. By default, no empty messages will be sent to the user.
  --includeInitialValues: oneof<nothing, bool> # Specified if the channel should send a message with the current value of the stream after the connection is opened. The default is 'false'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "heartbeatRate" $heartbeatRate "scalar") (serialize-qp "includeInitialValues" $includeInitialValues "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/channel" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns the end-of-stream value of the stream.
#
# GET /streams/{webId}/end
# operationId: Stream_GetEnd
export def "streams-end GetEnd" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
]: nothing -> record<Annotated: bool, Errors: table<FieldName: string, Message: list>, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "selectedFields" $selectedFields "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/end" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieves interpolated values over the specified time range at the specified sampling interval.
#
# GET /streams/{webId}/interpolated
# operationId: Stream_GetInterpolated
export def "streams-interpolated GetInterpolated" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. If the attribute does not support filtering, the filter will be ignored. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --interval: string # The sampling interval, in AFTimeSpan format.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --syncTime: string # An optional start time anchor, in AFTime format. When specified, interpolated data retrieval will use the sync time as the origin for calculating the interval times.
  --syncTimeBoundaryType: string # An optional string specifying the boundary type to use when applying a syncTime. The allowed values are 'Inside' and 'Outside'. The default is 'Inside'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Items: table<Annotated: bool, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "syncTime" $syncTime "scalar") (serialize-qp "syncTimeBoundaryType" $syncTimeBoundaryType "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/interpolated" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieves interpolated values over the specified time range at the specified sampling interval.
#
# GET /streams/{webId}/interpolatedattimes
# operationId: Stream_GetInterpolatedAtTimes
export def "streams-interpolatedattimes GetInterpolatedAtTimes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: list # The timestamp at which to retrieve an interpolated value. Multiple timestamps may be specified with multiple instances of the parameter.
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. If the attribute does not support filtering, the filter will be ignored. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Items: table<Annotated: bool, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "multi") (serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/interpolatedattimes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieves values over the specified time range suitable for plotting over the number of intervals (typically represents pixels).
#
# GET /streams/{webId}/plot
# operationId: Stream_GetPlot
export def "streams-plot GetPlot" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --intervals: int # The number of intervals to plot over. Typically, this would be the number of horizontal pixels in the trend. The default is '24'. For each interval, the data available is examined and significant values are returned. Each interval can produce up to 5 values if they are unique, the first value in the interval, the last value, the highest value, the lowest value and at most one exceptional point (bad status or digital state).
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Items: table<Annotated: bool, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "intervals" $intervals "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/plot" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns a list of compressed values for the requested time range from the source provider.
#
# GET /streams/{webId}/recorded
# operationId: Stream_GetRecorded
export def "streams-recorded GetRecorded" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Annotations to return events with annotation values. If this parameter is not specified, annotation values are not returned.
  --boundaryType: string # An optional value that determines how the times and values of the returned end points are determined. The default is 'Inside'.
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --maxCount: int # The maximum number of values to be returned. The default is 1000.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Items: table<Annotated: bool, Annotations: list, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "associations" $associations "scalar") (serialize-qp "boundaryType" $boundaryType "scalar") (serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/recorded" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates multiple values for the specified stream.
#
# POST /streams/{webId}/recorded
# operationId: Stream_UpdateValues
export def "streams-recorded UpdateValues" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --bufferOption: string # The desired AFBufferOption. The default is 'BufferIfPossible'.
  --updateOption: string # The desired AFUpdateOption. The default is 'Replace'.
  --body: record
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bufferOption" $bufferOption "scalar") (serialize-qp "updateOption" $updateOption "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/recorded" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Returns a single recorded value based on the passed time and retrieval mode from the stream.
#
# GET /streams/{webId}/recordedattime
# operationId: Stream_GetRecordedAtTime
export def "streams-recordedattime GetRecordedAtTime" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: string # The timestamp at which the value is desired.
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Annotations to return events with annotation values. If this parameter is not specified, annotation values are not returned.
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --retrievalMode: string # An optional value that determines the value to return when a value doesn't exist at the exact time specified. The default is 'Auto'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Annotated: bool, Annotations: table<CreationDate: string, Creator: string, Description: string, Errors: list, Id: string, Modifier: string, ModifyDate: string, Name: string, Value: record, WebException: record>, Errors: table<FieldName: string, Message: list>, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "scalar") (serialize-qp "associations" $associations "scalar") (serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "retrievalMode" $retrievalMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/recordedattime" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieves recorded values at the specified times.
#
# GET /streams/{webId}/recordedattimes
# operationId: Stream_GetRecordedAtTimes
export def "streams-recordedattimes GetRecordedAtTimes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: list # The timestamp at which to retrieve a recorded value. Multiple timestamps may be specified with multiple instances of the parameter.
  --associations: string # Associated values to return in the response, separated by semicolons (;). This call supports Annotations to return events with annotation values. If this parameter is not specified, annotation values are not returned.
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --retrievalMode: string # An optional value that determines the value to return when a value doesn't exist at the exact time specified. The default is 'Auto'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Items: table<Annotated: bool, Annotations: list, Errors: list, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record>, UnitsAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "multi") (serialize-qp "associations" $associations "scalar") (serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "retrievalMode" $retrievalMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/recordedattimes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns a summary over the specified time range for the stream.
#
# GET /streams/{webId}/summary
# operationId: Stream_GetSummary
export def "streams-summary GetSummary" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --calculationBasis: string # Specifies the method of evaluating the data over the time range. The default is 'TimeWeighted'.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # A string containing a filter expression. Expression variables are relative to the attribute. Use '.' to reference the containing attribute.
  --sampleInterval: string # When the sampleType is Interval, sampleInterval specifies how often the filter expression is evaluated when computing the summary for an interval.
  --sampleType: string # Defines the evaluation of an expression over a time range. The default is 'ExpressionRecordedValues'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --summaryDuration: string # The duration of each summary interval. If specified in hours, minutes, seconds, or milliseconds, the summary durations will be evenly spaced UTC time intervals. Longer interval types are interpreted using wall clock rules and are time zone dependent.
  --summaryType: list # Specifies the kinds of summaries to produce over the range. The default is 'Total'. Multiple summary types may be specified by using multiple instances of summaryType.
  --timeType: string # Specifies how to calculate the timestamp for each interval. The default is 'Auto'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Items: table<Type: string, Value: record, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "calculationBasis" $calculationBasis "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "sampleInterval" $sampleInterval "scalar") (serialize-qp "sampleType" $sampleType "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "summaryDuration" $summaryDuration "scalar") (serialize-qp "summaryType" $summaryType "multi") (serialize-qp "timeType" $timeType "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/summary" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Register for stream updates
#
# POST /streams/{webId}/updates
# operationId: Stream_RegisterStreamUpdate
export def "streams-updates RegisterStreamUpdate" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Exception: record<Errors: list<string>>, LatestMarker: string, Source: string, SourceName: string, SourcePath: string, Status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/updates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns the value of the stream at the specified time. By default, this is usually the current value.
#
# GET /streams/{webId}/value
# operationId: Stream_GetValue
export def "streams-value GetValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --desiredUnits: string # The name or abbreviation of the desired units of measure for the returned value, as found in the UOM database associated with the attribute. If not specified for an attribute, the attribute's default unit of measure is used. If the underlying stream is a point, this value may not be specified, as points are not associated with a unit of measure.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --time: string # An optional time. The default time context is determined from the owning object - for example, the time range of the event frame or transfer which holds this attribute. Otherwise, the implementation of the Data Reference determines the meaning of no context. For Points or simply configured PI Point Data References, this means the snapshot value of the PI Point on the Data Server.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
]: nothing -> record<Annotated: bool, Errors: table<FieldName: string, Message: list>, Good: bool, Questionable: bool, Substituted: bool, Timestamp: string, UnitsAbbreviation: string, Value: record, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "desiredUnits" $desiredUnits "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "timeZone" $timeZone "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/value" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a value for the specified stream.
#
# POST /streams/{webId}/value
# operationId: Stream_UpdateValue
# --Errors item shape: {FieldName?: string, Message?: list}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "streams-value UpdateValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --bufferOption: string # The desired AFBufferOption. The default is 'BufferIfPossible'.
  --updateOption: string # The desired AFUpdateOption. The default is 'Replace'. This parameter is ignored if the attribute is a configuration item.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Annotated: oneof<nothing, bool> # e.g. false
  --Errors: list # item shape: {FieldName?: string, Message?: list}
  --Good: oneof<nothing, bool> # e.g. true
  --Questionable: oneof<nothing, bool> # e.g. false
  --Substituted: oneof<nothing, bool> # e.g. false
  --Timestamp: string # format: date-time, e.g. 2014-07-22T14:00:00Z
  --UnitsAbbreviation: string # e.g. m
  --Value: record # e.g. 12.3
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bufferOption" $bufferOption "scalar") (serialize-qp "updateOption" $updateOption "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streams/($webId)/value" $qp)
  let body = {Annotated: $Annotated, Errors: $Errors, Good: $Good, Questionable: $Questionable, Substituted: $Substituted, Timestamp: $Timestamp, UnitsAbbreviation: $UnitsAbbreviation, Value: $Value, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Opens a channel that will send messages about any value changes for the specified streams.
#
# GET /streamsets/channel
# operationId: StreamSet_GetChannelAdHoc
export def "streamsets-channel GetChannelAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --heartbeatRate: int # Specifies the maximum number of consecutive empty messages that can be elapsed with no new data updates from the PI System, after which the client receives an empty payload. It helps to check if the connection is still alive. Zero/negative values correspond to no heartbeat, and the default value is no heartbeat.
  --includeInitialValues: oneof<nothing, bool> # Specified if the channel should send a message with the current values of all the streams after the connection is opened. The default is 'false'.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "heartbeatRate" $heartbeatRate "scalar") (serialize-qp "includeInitialValues" $includeInitialValues "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/channel" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns End Of Stream values for attributes of the specified streams
#
# GET /streamsets/end
# operationId: StreamSet_GetEndAdHoc
export def "streamsets-end GetEndAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/end" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns interpolated values of the specified streams over the specified time range at the specified sampling interval.
#
# GET /streamsets/interpolated
# operationId: StreamSet_GetInterpolatedAdHoc
export def "streamsets-interpolated GetInterpolatedAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --endTime: string # An optional end time. The default is '*'. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. If the attribute does not support filtering, the filter will be ignored. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --interval: string # The sampling interval, in AFTimeSpan format.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --startTime: string # An optional start time. The default is '*-1d'.
  --syncTime: string # An optional start time anchor, in AFTime format. When specified, interpolated data retrieval will use the sync time as the origin for calculating the interval times.
  --syncTimeBoundaryType: string # An optional string specifying the boundary type to use when applying a syncTime. The allowed values are 'Inside' and 'Outside'. The default is 'Inside'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "syncTime" $syncTime "scalar") (serialize-qp "syncTimeBoundaryType" $syncTimeBoundaryType "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/interpolated" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns interpolated values of the specified streams at the specified times.
#
# GET /streamsets/interpolatedattimes
# operationId: StreamSet_GetInterpolatedAtTimesAdHoc
export def "streamsets-interpolatedattimes GetInterpolatedAtTimesAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: list # The timestamp at which to retrieve a interpolated value. Multiple timestamps may be specified with multiple instances of the parameter.
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. If the attribute does not support filtering, the filter will be ignored. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "multi") (serialize-qp "webId" $webId "multi") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/interpolatedattimes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns the base stream's recorded values and subordinate streams' interpolated values at times matching the recorded values' timestamps.
#
# GET /streamsets/joined
# operationId: StreamSet_GetJoined
export def "streamsets-joined GetJoined" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --baseWebId: string # The ID of the base stream which is used for retrieving the recorded values.
  --subordinateWebId: list # The ID of a stream whose values will be joined on the times with the values of the base stream. Multiple streams may be specified with multiple instances of the parameter.
  --boundaryType: string # An optional value that determines how the times and values of the returned end points are determined. The default is 'Inside'.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --maxCount: int # The maximum number of values to be returned. The default is 1000.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either place, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "baseWebId" $baseWebId "scalar") (serialize-qp "subordinateWebId" $subordinateWebId "multi") (serialize-qp "boundaryType" $boundaryType "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/joined" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns values of attributes for the specified streams over the specified time range suitable for plotting over the number of intervals (typically represents pixels).
#
# GET /streamsets/plot
# operationId: StreamSet_GetPlotAdHoc
export def "streamsets-plot GetPlotAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --endTime: string # An optional end time. The default is '*'. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --intervals: int # The number of intervals to plot over. Typically, this would be the number of horizontal pixels in the trend. The default is '24'. For each interval, the data available is examined and significant values are returned. Each interval can produce up to 5 values if they are unique, the first value in the interval, the last value, the highest value, the lowest value and at most one exceptional point (bad status or digital state).
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --startTime: string # An optional start time. The default is '*-1d'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "intervals" $intervals "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/plot" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns recorded values of the specified streams.
#
# GET /streamsets/recorded
# operationId: StreamSet_GetRecordedAdHoc
export def "streamsets-recorded GetRecordedAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --boundaryType: string # An optional value that determines how the times and values of the returned end points are determined. The default is 'Inside'.
  --endTime: string # An optional end time. The default is '*'. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --maxCount: int # The maximum number of values to be returned. The default is 1000.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --startTime: string # An optional start time. The default is '*-1d'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "boundaryType" $boundaryType "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/recorded" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates multiple values for the specified streams.
#
# POST /streamsets/recorded
# operationId: StreamSet_UpdateValuesAdHoc
export def "streamsets-recorded UpdateValuesAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --bufferOption: string # The desired AFBufferOption. The default is 'BufferIfPossible'.
  --updateOption: string # The desired AFUpdateOption. The default is 'Replace'.
  --body: record
]: any -> record<Items: table<Items: list, Links: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bufferOption" $bufferOption "scalar") (serialize-qp "updateOption" $updateOption "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/recorded" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Returns recorded values based on the passed time and retrieval mode.
#
# GET /streamsets/recordedattime
# operationId: StreamSet_GetRecordedAtTimeAdHoc
export def "streamsets-recordedattime GetRecordedAtTimeAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: string # The timestamp at which the values are desired.
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --retrievalMode: string # An optional value that determines the values to return when values don't exist at the exact time specified. The default is 'Auto'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Links: record, Name: string, Path: string, Value: record, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "scalar") (serialize-qp "webId" $webId "multi") (serialize-qp "retrievalMode" $retrievalMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/recordedattime" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns recorded values of the specified streams at the specified times.
#
# GET /streamsets/recordedattimes
# operationId: StreamSet_GetRecordedAtTimesAdHoc
export def "streamsets-recordedattimes GetRecordedAtTimesAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: list # The timestamp at which to retrieve a recorded value. Multiple timestamps may be specified with multiple instances of the parameter.
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --retrievalMode: string # An optional value that determines the values to return when values don't exist at the exact time specified. The default is 'Auto'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "multi") (serialize-qp "webId" $webId "multi") (serialize-qp "retrievalMode" $retrievalMode "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/recordedattimes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns summary values of the specified streams.
#
# GET /streamsets/summary
# operationId: StreamSet_GetSummariesAdHoc
export def "streamsets-summary GetSummariesAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --calculationBasis: string # Specifies the method of evaluating the data over the time range. The default is 'TimeWeighted'.
  --endTime: string # An optional end time. The default is '*'. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # A string containing a filter expression. Expression variables are relative to the attribute. Use '.' to reference the containing attribute. The default is no filtering.
  --sampleInterval: string # A time span specifies how often the filter expression is evaluated when computing the summary for an interval, if the sampleType is 'Interval'.
  --sampleType: string # A flag which specifies one or more summaries to compute for each interval over the time range. The default is 'ExpressionRecordedValues'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --startTime: string # An optional start time. The default is '*-1d'.
  --summaryDuration: string # The duration of each summary interval.
  --summaryType: list # Specifies the kinds of summaries to produce over the range. The default is 'Total'. Multiple summary types may be specified by using multiple instances of summaryType.
  --timeType: string # Specifies how to calculate the timestamp for each interval. The default is 'Auto'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "calculationBasis" $calculationBasis "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "sampleInterval" $sampleInterval "scalar") (serialize-qp "sampleType" $sampleType "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "summaryDuration" $summaryDuration "scalar") (serialize-qp "summaryType" $summaryType "multi") (serialize-qp "timeType" $timeType "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/summary" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Receive stream updates
#
# GET /streamsets/updates
# operationId: StreamSet_RetrieveStreamSetUpdates
export def "streamsets-updates RetrieveStreamSetUpdates" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --marker: list # Identifier of stream source and current position. Multiple markers may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Events: list, Exception: record, LatestMarker: string, RequestedMarker: string, Source: string, SourceName: string, SourcePath: string, Status: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "marker" $marker "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/updates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Register for stream updates
#
# POST /streamsets/updates
# operationId: StreamSet_RegisterStreamSetUpdates
export def "streamsets-updates RegisterStreamSetUpdates" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Exception: record, LatestMarker: string, Source: string, SourceName: string, SourcePath: string, Status: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/updates" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns values of the specified streams.
#
# GET /streamsets/value
# operationId: StreamSet_GetValuesAdHoc
export def "streamsets-value GetValuesAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webId: list # The ID of a stream. Multiple streams may be specified with multiple instances of the parameter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --time: string # An AF time string, which is used as the time context to get stream values if it is provided. By default, it is not specified, and the default time context of the AF object will be used.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Links: record, Name: string, Path: string, Value: record, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webId" $webId "multi") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/value" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a single value for the specified streams.
#
# POST /streamsets/value
# operationId: StreamSet_UpdateValueAdHoc
export def "streamsets-value UpdateValueAdHoc" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --bufferOption: string # The desired AFBufferOption. The default is 'BufferIfPossible'.
  --updateOption: string # The desired AFUpdateOption. The default is 'Replace'.
  --body: record
]: any -> record<Items: table<Message: string, Substatus: int, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bufferOption" $bufferOption "scalar") (serialize-qp "updateOption" $updateOption "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/streamsets/value" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Opens a channel that will send messages about any value changes for the attributes of an Element, Event Frame, or Attribute.
#
# GET /streamsets/{webId}/channel
# operationId: StreamSet_GetChannel
export def "streamsets-channel GetChannel" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --heartbeatRate: int # Specifies the maximum number of consecutive empty messages that can be elapsed with no new data updates from the PI System, after which the client receives an empty payload. It helps to check if the connection is still alive. Zero/negative values correspond to no heartbeat, and the default value is no heartbeat.
  --includeInitialValues: oneof<nothing, bool> # Specified if the channel should send a message with the current values of all the streams after the connection is opened. The default is 'false'.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "heartbeatRate" $heartbeatRate "scalar") (serialize-qp "includeInitialValues" $includeInitialValues "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/channel" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns End of stream values of the attributes for an Element, Event Frame or Attribute
#
# GET /streamsets/{webId}/end
# operationId: StreamSet_GetEnd
export def "streamsets-end GetEnd" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Links: record, Name: string, Path: string, Value: record, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/end" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns interpolated values of attributes for an element, event frame or attribute over the specified time range at the specified sampling interval.
#
# GET /streamsets/{webId}/interpolated
# operationId: StreamSet_GetInterpolated
export def "streamsets-interpolated GetInterpolated" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. If the attribute does not support filtering, the filter will be ignored. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --interval: string # The sampling interval, in AFTimeSpan format.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --syncTime: string # An optional start time anchor, in AFTime format. When specified, interpolated data retrieval will use the sync time as the origin for calculating the interval times.
  --syncTimeBoundaryType: string # An optional string specifying the boundary type to use when applying a syncTime. The allowed values are 'Inside' and 'Outside'. The default is 'Inside'.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "syncTime" $syncTime "scalar") (serialize-qp "syncTimeBoundaryType" $syncTimeBoundaryType "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/interpolated" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns interpolated values of attributes for an element, event frame or attribute at the specified times.
#
# GET /streamsets/{webId}/interpolatedattimes
# operationId: StreamSet_GetInterpolatedAtTimes
export def "streamsets-interpolatedattimes GetInterpolatedAtTimes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: list # The timestamp at which to retrieve a interpolated value. Multiple timestamps may be specified with multiple instances of the parameter.
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. If the attribute does not support filtering, the filter will be ignored. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "multi") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/interpolatedattimes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns values of attributes for an element, event frame or attribute over the specified time range suitable for plotting over the number of intervals (typically represents pixels).
#
# GET /streamsets/{webId}/plot
# operationId: StreamSet_GetPlot
export def "streamsets-plot GetPlot" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --intervals: int # The number of intervals to plot over. Typically, this would be the number of horizontal pixels in the trend. The default is '24'. For each interval, the data available is examined and significant values are returned. Each interval can produce up to 5 values if they are unique, the first value in the interval, the last value, the highest value, the lowest value and at most one exceptional point (bad status or digital state).
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "intervals" $intervals "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/plot" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns recorded values of the attributes for an element, event frame, or attribute.
#
# GET /streamsets/{webId}/recorded
# operationId: StreamSet_GetRecorded
export def "streamsets-recorded GetRecorded" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --boundaryType: string # An optional value that determines how the times and values of the returned end points are determined. The default is 'Inside'.
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # An optional string containing a filter expression. Expression variables are relative to the data point. Use '.' to reference the containing attribute. The default is no filtering.
  --includeFilteredValues: oneof<nothing, bool> # Specify 'true' to indicate that values which fail the filter criteria are present in the returned data at the times where they occurred with a value set to a 'Filtered' enumeration value with bad status. Repeated consecutive failures are omitted.
  --maxCount: int # The maximum number of values to be returned. The default is 1000.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "boundaryType" $boundaryType "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "includeFilteredValues" $includeFilteredValues "scalar") (serialize-qp "maxCount" $maxCount "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/recorded" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates multiple values for the specified streams.
#
# POST /streamsets/{webId}/recorded
# operationId: StreamSet_UpdateValues
export def "streamsets-recorded UpdateValues" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --bufferOption: string # The desired AFBufferOption. The default is 'BufferIfPossible'.
  --updateOption: string # The desired AFUpdateOption. The default is 'Replace'.
  --body: record
]: any -> record<Items: table<Items: list, Links: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bufferOption" $bufferOption "scalar") (serialize-qp "updateOption" $updateOption "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/recorded" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Returns recorded values of the attributes for an element, event frame, or attribute.
#
# GET /streamsets/{webId}/recordedattime
# operationId: StreamSet_GetRecordedAtTime
export def "streamsets-recordedattime GetRecordedAtTime" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: string # The timestamp at which the values are desired.
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --retrievalMode: string # An optional value that determines the values to return when values don't exist at the exact time specified. The default is 'Auto'.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Links: record, Name: string, Path: string, Value: record, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "retrievalMode" $retrievalMode "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/recordedattime" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns recorded values of attributes for an element, event frame or attribute at the specified times.
#
# GET /streamsets/{webId}/recordedattimes
# operationId: StreamSet_GetRecordedAtTimes
export def "streamsets-recordedattimes GetRecordedAtTimes" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --time: list # The timestamp at which to retrieve a recorded value. Multiple timestamps may be specified with multiple instances of the parameter.
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --retrievalMode: string # An optional value that determines the values to return when values don't exist at the exact time specified. The default is 'Auto'.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, UnitsAbbreviation: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "time" $time "multi") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "retrievalMode" $retrievalMode "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/recordedattimes" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns summary values of the attributes for an element, event frame or attribute.
#
# GET /streamsets/{webId}/summary
# operationId: StreamSet_GetSummaries
export def "streamsets-summary GetSummaries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --calculationBasis: string # Specifies the method of evaluating the data over the time range. The default is 'TimeWeighted'.
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --endTime: string # An optional end time. The default is '*' for element attributes and points. For event frame attributes, the default is the event frame's end time, or '*' if that is not set. Note that if endTime is earlier than startTime, the resulting values will be in time-descending order.
  --filterExpression: string # A string containing a filter expression. Expression variables are relative to the attribute. Use '.' to reference the containing attribute. The default is no filtering.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --sampleInterval: string # A time span specifies how often the filter expression is evaluated when computing the summary for an interval, if the sampleType is 'Interval'.
  --sampleType: string # A flag which specifies one or more summaries to compute for each interval over the time range. The default is 'ExpressionRecordedValues'.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --startTime: string # An optional start time. The default is '*-1d' for element attributes and points. For event frame attributes, the default is the event frame's start time, or '*-1d' if that is not set.
  --summaryDuration: string # The duration of each summary interval.
  --summaryType: list # Specifies the kinds of summaries to produce over the range. The default is 'Total'. Multiple summary types may be specified by using multiple instances of summaryType.
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --timeType: string # Specifies how to calculate the timestamp for each interval. The default is 'Auto'.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Items: list, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "calculationBasis" $calculationBasis "scalar") (serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "endTime" $endTime "scalar") (serialize-qp "filterExpression" $filterExpression "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "sampleInterval" $sampleInterval "scalar") (serialize-qp "sampleType" $sampleType "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "startTime" $startTime "scalar") (serialize-qp "summaryDuration" $summaryDuration "scalar") (serialize-qp "summaryType" $summaryType "multi") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "timeType" $timeType "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/summary" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Returns values of the attributes for an Element, Event Frame or Attribute at the specified time.
#
# GET /streamsets/{webId}/value
# operationId: StreamSet_GetValues
export def "streamsets-value GetValues" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --categoryName: string # Specify that included attributes must have this category. The default is no category filter.
  --nameFilter: string # The name query string used for filtering attributes. The default is no filter.
  --searchFullHierarchy: oneof<nothing, bool> # Specifies if the search should include attributes nested further than the immediate attributes of the searchRoot. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --showExcluded: oneof<nothing, bool> # Specified if the search should include attributes with the Excluded property set. The default is 'false'.
  --showHidden: oneof<nothing, bool> # Specified if the search should include attributes with the Hidden property set. The default is 'false'.
  --sortField: string # The field or property of the object used to sort the returned collection. For better performance, by default no sorting is applied. 'Name' is the only supported field by which to sort.
  --sortOrder: string # The order that the returned collection is sorted. The default is 'Ascending'
  --templateName: string # Specify that included attributes must be members of this template. The default is no template filter.
  --time: string # An AF time string, which is used as the time context to get stream values if it is provided. By default, it is not specified, and the default time context of the AF object will be used.
  --timeZone: string # The time zone in which the time string will be interpreted. This parameter will be ignored if a time zone is specified in the time string. If no time zone is specified in either places, the PI Web API server time zone will be used.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Links: record, Name: string, Path: string, Value: record, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "categoryName" $categoryName "scalar") (serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "searchFullHierarchy" $searchFullHierarchy "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "showExcluded" $showExcluded "scalar") (serialize-qp "showHidden" $showHidden "scalar") (serialize-qp "sortField" $sortField "scalar") (serialize-qp "sortOrder" $sortOrder "scalar") (serialize-qp "templateName" $templateName "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "timeZone" $timeZone "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/value" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Updates a single value for the specified streams.
#
# POST /streamsets/{webId}/value
# operationId: StreamSet_UpdateValue
export def "streamsets-value UpdateValue" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --bufferOption: string # The desired AFBufferOption. The default is 'BufferIfPossible'.
  --updateOption: string # The desired AFUpdateOption. The default is 'Replace'.
  --body: record
]: any -> record<Items: table<Message: string, Substatus: int, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "bufferOption" $bufferOption "scalar") (serialize-qp "updateOption" $updateOption "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/streamsets/($webId)/value" $qp)
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get system links for this PI System Web API instance.
#
# GET /system
# operationId: System_Landing
export def "system Landing" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Links: record<CacheInstances: string, Configuration: string, Self: string, Status: string, UserInfo: string, Versions: string>, ProductTitle: string, ProductVersion: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/system")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get AF cache instances currently in use by the system. These are caches from which user requests are serviced. The number of instances depends on the number of users connected to the service, the service's authentication method, and the cache instance configuration.
#
# GET /system/cacheinstances
# operationId: System_CacheInstances
export def "system-cacheinstances CacheInstances" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<Items: table<Id: string, LastRefreshTime: string, ScheduledExpirationTime: string, User: string, WebException: record, WillRefreshAfter: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/system/cacheinstances")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the current system configuration.
#
# GET /system/configuration
# operationId: Configuration_List
export def "system-configuration List" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/system/configuration")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a configuration item.
#
# DELETE /system/configuration/{key}
# operationId: Configuration_Delete
export def "system-configuration Delete" [
  key: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/system/configuration/($key)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the value of a configuration item.
#
# GET /system/configuration/{key}
# operationId: Configuration_Get
export def "system-configuration Get" [
  key: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/system/configuration/($key)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get information about this PI Web API instance. Examples of information returned include the system uptime, the number of cache instances for this PI System Web API instance, and the system run state.
#
# GET /system/status
# operationId: System_Status
export def "system-status Status" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<CacheInstances: int, ServerTime: string, State: string, UpTimeInMinutes: float, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/system/status")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get information about the Windows identity used to fulfill the request. This depends on the service's authentication method and the credentials passed by the client. The impersonation level of the Windows identity is included.
#
# GET /system/userinfo
# operationId: System_UserInfo
export def "system-userinfo UserInfo" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record<IdentityType: string, ImpersonationLevel: string, IsAuthenticated: bool, Name: string, SID: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/system/userinfo")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the current versions of the PI Web API instance and all external plugins.
#
# GET /system/versions
# operationId: System_Versions
export def "system-versions Versions" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/system/versions")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a table category by path.
#
# GET /tablecategories
# operationId: TableCategory_GetByPath
export def "tablecategories GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the target table category.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tablecategories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a table category.
#
# DELETE /tablecategories/{webId}
# operationId: TableCategory_Delete
export def "tablecategories Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/tablecategories/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a table category.
#
# GET /tablecategories/{webId}
# operationId: TableCategory_Get
export def "tablecategories Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Description: string, Id: string, Links: record<Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tablecategories/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a table category by replacing items in its definition.
#
# PATCH /tablecategories/{webId}
# operationId: TableCategory_Update
# --Links shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "tablecategories Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Description: string # e.g. Relative energy use per ton of process feed.
  --Id: string # format: uuid, e.g. 89c70cc2-5221-4e77-873f-894a3ea61e7d
  --Links: record # shape: {Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CategoryName
  --Path: string # format: path, e.g. \\MyAssetServer\Database\CategoriesTable[CategoryName]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1BCDqD5loBNH0erqeqJodtALAwgzHiSFSd06HP4lKPqYefQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/tablecategories/($webId)")
  let body = {Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the table category for a specified user.
#
# GET /tablecategories/{webId}/security
# operationId: TableCategory_GetSecurity
export def "tablecategories-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tablecategories/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the table category based on the specified criteria. By default, all security entries for this table category are returned.
#
# GET /tablecategories/{webId}/securityentries
# operationId: TableCategory_GetSecurityEntries
export def "tablecategories-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tablecategories/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the table category.
#
# POST /tablecategories/{webId}/securityentries
# operationId: TableCategory_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "tablecategories-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tablecategories/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the table category.
#
# DELETE /tablecategories/{webId}/securityentries/{name}
# operationId: TableCategory_DeleteSecurityEntry
export def "tablecategories-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tablecategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the table category with the specified name.
#
# GET /tablecategories/{webId}/securityentries/{name}
# operationId: TableCategory_GetSecurityEntryByName
export def "tablecategories-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tablecategories/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the table category.
#
# PUT /tablecategories/{webId}/securityentries/{name}
# operationId: TableCategory_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "tablecategories-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tablecategories/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a table by path.
#
# GET /tables
# operationId: Table_GetByPath
export def "tables GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the table.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, ConvertToLocalTime: bool, Description: string, Id: string, Links: record<Categories: string, Data: string, Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, TimeZone: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tables" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a table.
#
# DELETE /tables/{webId}
# operationId: Table_Delete
export def "tables Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/tables/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a table.
#
# GET /tables/{webId}
# operationId: Table_Get
export def "tables Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CategoryNames: list<string>, ConvertToLocalTime: bool, Description: string, Id: string, Links: record<Categories: string, Data: string, Database: string, Security: string, SecurityEntries: string, Self: string>, Name: string, Path: string, TimeZone: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a table by replacing items in its definition.
#
# PATCH /tables/{webId}
# operationId: Table_Update
# --Links shape: {Categories?: string, Data?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "tables Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --CategoryNames: list # e.g. [Table Category]
  --ConvertToLocalTime: oneof<nothing, bool> # e.g. false
  --Description: string # e.g. Table of car info.
  --Id: string # format: uuid, e.g. 5ff8ba98-89ce-479a-b29e-ddd057096a5f
  --Links: record # shape: {Categories?: string, Data?: string, Database?: string, Security?: string, SecurityEntries?: string, Self?: string}
  --Name: string # e.g. CarInfo
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\Tables[CarInfo]
  --TimeZone: string # e.g. Eastern Standard Time
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1BlDqD5loBNH0erqeqJodtALAmLr4X86Jmkeynt3QVwlqXw
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/tables/($webId)")
  let body = {CategoryNames: $CategoryNames, ConvertToLocalTime: $ConvertToLocalTime, Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, TimeZone: $TimeZone, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a table's categories.
#
# GET /tables/{webId}/categories
# operationId: Table_GetCategories
export def "tables-categories GetCategories" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<Description: string, Id: string, Links: record, Name: string, Path: string, WebException: record, WebId: string>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/categories" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the table's data.
#
# GET /tables/{webId}/data
# operationId: Table_GetData
export def "tables-data GetData" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
]: nothing -> record<Columns: record, Rows: list<record>, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/data" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update the table's data.
#
# PUT /tables/{webId}/data
# operationId: Table_UpdateData
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "tables-data UpdateData" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Columns: record
  --Rows: list
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/tables/($webId)/data")
  let body = {Columns: $Columns, Rows: $Rows, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the security information of the specified security item associated with the table for a specified user.
#
# GET /tables/{webId}/security
# operationId: Table_GetSecurity
export def "tables-security GetSecurity" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --userIdentity: list # The user identity for the security information to be checked. Multiple security identities may be specified with multiple instances of the parameter. If the parameter is not specified, only the current user's security rights will be returned.
  --forceRefresh: oneof<nothing, bool> # Indicates if the security cache should be refreshed before getting security information. The default is 'false'.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<CanAnnotate: bool, CanDelete: bool, CanExecute: bool, CanRead: bool, CanReadData: bool, CanSubscribe: bool, CanSubscribeOthers: bool, CanWrite: bool, CanWriteData: bool, HasAdmin: bool, Links: record, OwnerWebId: string, Rights: list, SecurityItem: string, UserIdentity: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "userIdentity" $userIdentity "multi") (serialize-qp "forceRefresh" $forceRefresh "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/security" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entries associated with the table based on the specified criteria. By default, all security entries for this table are returned.
#
# GET /tables/{webId}/securityentries
# operationId: Table_GetSecurityEntries
export def "tables-securityentries GetSecurityEntries" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --nameFilter: string # The name query string used for filtering security entries. The default is no filter.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Items: table<AllowRights: list, DenyRights: list, Links: record, Name: string, SecurityIdentityName: string, WebException: record>, Links: record<First: string, Last: string, Next: string, Previous: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "nameFilter" $nameFilter "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/securityentries" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a security entry owned by the table.
#
# POST /tables/{webId}/securityentries
# operationId: Table_CreateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "tables-securityentries CreateSecurityEntry" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/securityentries" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Delete a security entry owned by the table.
#
# DELETE /tables/{webId}/securityentries/{name}
# operationId: Table_DeleteSecurityEntry
export def "tables-securityentries DeleteSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve the security entry associated with the table with the specified name.
#
# GET /tables/{webId}/securityentries/{name}
# operationId: Table_GetSecurityEntryByName
export def "tables-securityentries GetSecurityEntryByName" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AllowRights: list<string>, DenyRights: list<string>, Links: record<SecurableObject: string, SecurityIdentity: string, Self: string>, Name: string, SecurityIdentityName: string, WebException: record<Errors: list<string>, StatusCode: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/securityentries/($name)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a security entry owned by the table.
#
# PUT /tables/{webId}/securityentries/{name}
# operationId: Table_UpdateSecurityEntry
# --Links shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "tables-securityentries UpdateSecurityEntry" [
  name: string
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --applyToChildren: oneof<nothing, bool> # If false, the new access permissions are only applied to the associated object. If true, the access permissions of children with any parent-child reference types will change when the permissions on the primary parent change.
  --AllowRights: list # e.g. [Read, ReadData]
  --DenyRights: list # e.g. [Write, Execute, Admin]
  --Links: record # shape: {SecurableObject?: string, SecurityIdentity?: string, Self?: string}
  --Name: string # e.g. domain\user1
  --SecurityIdentityName: string # e.g. domain\user1
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "applyToChildren" $applyToChildren "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/tables/($webId)/securityentries/($name)" $qp)
  let body = {AllowRights: $AllowRights, DenyRights: $DenyRights, Links: $Links, Name: $Name, SecurityIdentityName: $SecurityIdentityName, WebException: $WebException} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a Time Rule Plug-in by path.
#
# GET /timeruleplugins
# operationId: TimeRulePlugIn_GetByPath
export def "timeruleplugins GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the Time Rule Plug-in.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list<string>, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record<AssetServer: string, Self: string>, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/timeruleplugins" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a Time Rule Plug-in.
#
# GET /timeruleplugins/{webId}
# operationId: TimeRulePlugIn_Get
export def "timeruleplugins Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<AssemblyFileName: string, AssemblyID: string, AssemblyLoadProperties: list<string>, AssemblyTime: string, CompatibilityVersion: int, Description: string, Id: string, IsBrowsable: bool, IsNonEditableConfig: bool, Links: record<AssetServer: string, Self: string>, LoadedAssemblyTime: string, LoadedVersion: string, Name: string, Path: string, Version: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/timeruleplugins/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a Time Rule by path.
#
# GET /timerules
# operationId: TimeRule_GetByPath
export def "timerules GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the Time Rule.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<ConfigString: string, ConfigStringStored: string, Description: string, DisplayString: string, EditorType: string, Id: string, IsConfigured: bool, IsInitializing: bool, Links: record<Analysis: string, AnalysisTemplate: string, PlugIn: string, Self: string>, MergeDuplicatedItems: bool, Name: string, Path: string, PlugInName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/timerules" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a Time Rule.
#
# DELETE /timerules/{webId}
# operationId: TimeRule_Delete
export def "timerules Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/timerules/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a Time Rule.
#
# GET /timerules/{webId}
# operationId: TimeRule_Get
export def "timerules Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<ConfigString: string, ConfigStringStored: string, Description: string, DisplayString: string, EditorType: string, Id: string, IsConfigured: bool, IsInitializing: bool, Links: record<Analysis: string, AnalysisTemplate: string, PlugIn: string, Self: string>, MergeDuplicatedItems: bool, Name: string, Path: string, PlugInName: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/timerules/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a Time Rule by replacing items in its definition.
#
# PATCH /timerules/{webId}
# operationId: TimeRule_Update
# --Links shape: {Analysis?: string, AnalysisTemplate?: string, PlugIn?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "timerules Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --ConfigString: string # e.g. Frequency=300
  --ConfigStringStored: string # e.g. 
  --Description: string # e.g. Creates regular periodic time periods.
  --DisplayString: string # e.g. Frequency=300
  --EditorType: string # e.g. OSIsoft.AF.Time.TimeRule.PeriodicConfig
  --Id: string # format: uuid, e.g. 1514eb44-94c5-11e7-8443-00155d84c200
  --IsConfigured: oneof<nothing, bool> # e.g. true
  --IsInitializing: oneof<nothing, bool> # e.g. false
  --Links: record # shape: {Analysis?: string, AnalysisTemplate?: string, PlugIn?: string, Self?: string}
  --MergeDuplicatedItems: oneof<nothing, bool> # e.g. false
  --Name: string # e.g. Periodic
  --Path: string # format: path, e.g. \\MyAssetServer\MyDatabase\MyElement\Analyses[MyAnalysis]\TimeRule
  --PlugInName: string # e.g. Periodic
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1TRXDqD5loBNH0erqeqJodtALAROsUFcWU5xGEQwAVXYTCAAROsUFcWU5xGEQwAVXYTCAA
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/timerules/($webId)")
  let body = {ConfigString: $ConfigString, ConfigStringStored: $ConfigStringStored, Description: $Description, DisplayString: $DisplayString, EditorType: $EditorType, Id: $Id, IsConfigured: $IsConfigured, IsInitializing: $IsInitializing, Links: $Links, MergeDuplicatedItems: $MergeDuplicatedItems, Name: $Name, Path: $Path, PlugInName: $PlugInName, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a unit class by path.
#
# GET /unitclasses
# operationId: UnitClass_GetByPath
export def "unitclasses GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the unit class.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CanonicalUnitAbbreviation: string, CanonicalUnitName: string, Description: string, Id: string, Links: record<AssetServer: string, CanonicalUnit: string, Self: string, Units: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/unitclasses" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a unit class.
#
# DELETE /unitclasses/{webId}
# operationId: UnitClass_Delete
export def "unitclasses Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/unitclasses/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a unit class.
#
# GET /unitclasses/{webId}
# operationId: UnitClass_Get
export def "unitclasses Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<CanonicalUnitAbbreviation: string, CanonicalUnitName: string, Description: string, Id: string, Links: record<AssetServer: string, CanonicalUnit: string, Self: string, Units: string>, Name: string, Path: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/unitclasses/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a unit class.
#
# PATCH /unitclasses/{webId}
# operationId: UnitClass_Update
# --Links shape: {AssetServer?: string, CanonicalUnit?: string, Self?: string, Units?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "unitclasses Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --CanonicalUnitAbbreviation: string # e.g. W
  --CanonicalUnitName: string # e.g. watt
  --Description: string # e.g. Power Unit Class
  --Id: string # format: uuid, e.g. f925b94d-6efc-4b94-a50d-0015c391f249
  --Links: record # shape: {AssetServer?: string, CanonicalUnit?: string, Self?: string, Units?: string}
  --Name: string # e.g. Power
  --Path: string # format: path, e.g. \\MyAssetServer\UOMDatabase\UOMClasses[Power]
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1UCDqD5loBNH0erqeqJodtALATbkl-fxulEulDQAVw5HySQ
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/unitclasses/($webId)")
  let body = {CanonicalUnitAbbreviation: $CanonicalUnitAbbreviation, CanonicalUnitName: $CanonicalUnitName, Description: $Description, Id: $Id, Links: $Links, Name: $Name, Path: $Path, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the canonical unit of a unit class.
#
# GET /unitclasses/{webId}/canonicalunit
# operationId: UnitClass_GetCanonicalUnit
export def "unitclasses-canonicalunit GetCanonicalUnit" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Abbreviation: string, Description: string, Factor: float, Id: string, Links: record<Class: string, ReferenceUnit: string, Self: string>, Name: string, Offset: float, Path: string, ReferenceFactor: float, ReferenceOffset: float, ReferenceUnitAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/unitclasses/($webId)/canonicalunit" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a list of all units belonging to the unit class.
#
# GET /unitclasses/{webId}/units
# operationId: UnitClass_GetUnits
export def "unitclasses-units GetUnits" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Abbreviation: string, Description: string, Factor: float, Id: string, Links: record<Class: string, ReferenceUnit: string, Self: string>, Name: string, Offset: float, Path: string, ReferenceFactor: float, ReferenceOffset: float, ReferenceUnitAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/unitclasses/($webId)/units" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a unit in the specified Unit Class.
#
# POST /unitclasses/{webId}/units
# operationId: UnitClass_CreateUnit
# --Links shape: {Class?: string, ReferenceUnit?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "unitclasses-units CreateUnit" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
  --Abbreviation: string # e.g. Hz
  --Description: string # e.g. Hertz Unit
  --Factor: float # format: double, e.g. 1
  --Id: string # format: uuid, e.g. 8121ac8e-3ce2-4794-95e3-1783d5d6f4fc
  --Links: record # shape: {Class?: string, ReferenceUnit?: string, Self?: string}
  --Name: string # e.g. Hertz
  --Offset: float # format: double, e.g. 0
  --Path: string # format: path, e.g. \\MyAssetServer\UOMDatabase\Hertz
  --ReferenceFactor: float # format: double, e.g. 1
  --ReferenceOffset: float # format: double, e.g. 0
  --ReferenceUnitAbbreviation: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1UtDqD5loBNH0erqeqJodtALAjqwhgeI8lEeV4xeD1db0_A
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/unitclasses/($webId)/units" $qp)
  let body = {Abbreviation: $Abbreviation, Description: $Description, Factor: $Factor, Id: $Id, Links: $Links, Name: $Name, Offset: $Offset, Path: $Path, ReferenceFactor: $ReferenceFactor, ReferenceOffset: $ReferenceOffset, ReferenceUnitAbbreviation: $ReferenceUnitAbbreviation, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Retrieve a unit by path.
#
# GET /units
# operationId: Unit_GetByPath
export def "units GetByPath" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --path: string # The path to the unit.
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Abbreviation: string, Description: string, Factor: float, Id: string, Links: record<Class: string, ReferenceUnit: string, Self: string>, Name: string, Offset: float, Path: string, ReferenceFactor: float, ReferenceOffset: float, ReferenceUnitAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "path" $path "scalar") (serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/units" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Delete a unit.
#
# DELETE /units/{webId}
# operationId: Unit_Delete
export def "units Delete" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/units/($webId)")
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Retrieve a unit.
#
# GET /units/{webId}
# operationId: Unit_Get
export def "units Get" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --selectedFields: string # List of fields to be returned in the response, separated by semicolons (;). If this parameter is not specified, all available fields will be returned.
  --webIdType: string # Optional parameter. Used to specify the type of WebID. Useful for URL brevity and other special cases. Default is the value of the configuration item "WebIDType".
]: nothing -> record<Abbreviation: string, Description: string, Factor: float, Id: string, Links: record<Class: string, ReferenceUnit: string, Self: string>, Name: string, Offset: float, Path: string, ReferenceFactor: float, ReferenceOffset: float, ReferenceUnitAbbreviation: string, WebException: record<Errors: list<string>, StatusCode: int>, WebId: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "selectedFields" $selectedFields "scalar") (serialize-qp "webIdType" $webIdType "scalar")] | flatten | str join "&"
  let full_url = (build-url $base $"/units/($webId)" $qp)
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a unit.
#
# PATCH /units/{webId}
# operationId: Unit_Update
# --Links shape: {Class?: string, ReferenceUnit?: string, Self?: string}
# --WebException shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
export def "units Update" [
  webId: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --accept: string@accept-completer # Response content type
  --Abbreviation: string # e.g. Hz
  --Description: string # e.g. Hertz Unit
  --Factor: float # format: double, e.g. 1
  --Id: string # format: uuid, e.g. 8121ac8e-3ce2-4794-95e3-1783d5d6f4fc
  --Links: record # shape: {Class?: string, ReferenceUnit?: string, Self?: string}
  --Name: string # e.g. Hertz
  --Offset: float # format: double, e.g. 0
  --Path: string # format: path, e.g. \\MyAssetServer\UOMDatabase\Hertz
  --ReferenceFactor: float # format: double, e.g. 1
  --ReferenceOffset: float # format: double, e.g. 0
  --ReferenceUnitAbbreviation: string # e.g. 
  --WebException: record # shape: {Errors?: list, StatusCode?: "100"|"101"|"200"|"201"|"202"|"203"|"204"|"205"|"206"|"207"|"300"|"301"|"302"|"303"|"304"|"305"|"306"|"307"|"400"|"401"|"402"|"403"|"404"|"405"|"406"|"407"|"408"|"409"|"410"|"411"|"412"|"413"|"414"|"415"|"416"|"417"|"426"|"500"|"501"|"502"|"503"|"504"|"505"}
  --WebId: string # format: webid, e.g. I1UtDqD5loBNH0erqeqJodtALAjqwhgeI8lEeV4xeD1db0_A
]: any -> any {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base $"/units/($webId)")
  let body = {Abbreviation: $Abbreviation, Description: $Description, Factor: $Factor, Id: $Id, Links: $Links, Name: $Name, Offset: $Offset, Path: $Path, ReferenceFactor: $ReferenceFactor, ReferenceOffset: $ReferenceOffset, ReferenceUnitAbbreviation: $ReferenceUnitAbbreviation, WebException: $WebException, WebId: $WebId} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let accept_val = ($accept | default "application/json")
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}
