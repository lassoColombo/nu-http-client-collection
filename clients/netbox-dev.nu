# Auto-generated client for NetBox API v3.4
# Source: https://api.apis.guru/v2/specs/netbox.dev/3.4/openapi.json
# Auth: --token flag or $env.NETBOX_API_TOKEN

const BASE_URL = "https://demo.netbox.dev/api"

# Build auth: returns {scheme: string, headers: record, query: string, location: string}.
# `location` is "header" | "query" | "cookie" | "none" and tells dry-run callers
# where the token went without inspecting headers/query themselves.
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o NETBOX_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {scheme: $scheme, headers: {}, query: "", location: "none"} }
  match $scheme {
    "bearer" => { {scheme: $scheme, headers: {Authorization: $"Bearer ($token_val)"}, query: "", location: "header"} }
    "none" => { {scheme: $scheme, headers: {}, query: "", location: "none"} }
    _ => { {scheme: $scheme, headers: {Authorization: $"Bearer ($token_val)"}, query: "", location: "header"} }
  }
}

# Serialize a single query parameter based on collection style
# Uses encode-path-segment for keys and values: RFC 3986 unreserved chars
# ([A-Za-z0-9-._~]) stay literal; everything else gets %XX.
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let is_list = ($value | describe | str starts-with "list")
  if $is_list and ($value | is-empty) { return [] }
  let n = (encode-path-segment $name)
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[(encode-path-segment $in.k)]=(encode-path-segment $in.v)" }) }
  if not $is_list { return [$"($n)=(encode-path-segment $value)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=(encode-path-segment $v)" } }
    "csv" => { let joined = ($value | each { encode-path-segment $in } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { encode-path-segment $in } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { encode-path-segment $in } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { encode-path-segment $in } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=(encode-path-segment $v)" } }
    _ => { $value | each {|v| $"($n)=(encode-path-segment $v)" } }
  }
}

# Percent-encode a path-segment value per RFC 3986.
# Unreserved chars ([A-Za-z0-9-._~]) stay literal; everything else gets %XX.
# Trick: `url encode --all` over-encodes, then we decode the four unreserved
# punctuation chars back. Pre-existing %XX sequences in the input survive
# because `url encode --all` first turns their % into %25.
def encode-path-segment [v: any]: nothing -> string {
  $v | into string | url encode --all | str replace --all "%2D" "-" | str replace --all "%2E" "." | str replace --all "%5F" "_" | str replace --all "%7E" "~"
}

# Serialize an array-typed path parameter (issue 49.A). OpenAPI 3 `style: simple`
# (the default for path params) and Swagger 2 `collectionFormat: csv` both join
# the elements with a literal comma WITHIN the single path segment, each element
# RFC-3986-encoded individually (so a comma inside an element stays %2C). Without
# this a `list` positional would render as the Nushell debug form `[a, b]`,
# producing a guaranteed-404 URL. The else-branch keeps scalar values on the
# historical encode-path-segment path (defensive against a bare string).
def encode-path-array [v: any]: nothing -> string {
  if (($v | describe) | str starts-with "list") { $v | each { encode-path-segment $in } | str join "," } else { encode-path-segment $v }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Build the dry-run record returned by --dry-run. Shape:
#   {dry_run: true, method, url, query: <record>, headers, body, content_type, timeout,
#    auth: {scheme, location}}
# `meta` carries logical-form data (the query record by spec name, the pre-serialization
# body) that do-request itself cannot reconstruct from its wire-format args.
def build-dry-run-record [method: string, url: string, auth: record, content_type: string, timeout: duration, meta?: record]: nothing -> record {
  let m = ($meta | default {})
  {
    dry_run: true
    method: $method
    url: $url
    query: ($m | get -o query | default {})
    headers: $auth.headers
    body: ($m | get -o body)
    content_type: $content_type
    timeout: $timeout
    auth: {scheme: $auth.scheme, location: $auth.location}
  }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, dry_run: bool, max_time?: duration, allow_errors?: bool, full?: bool, content_type?: string, body?: any, dry_run_meta?: record]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  if $dry_run { return (build-dry-run-record $method $req_url $auth $ct $timeout $dry_run_meta) }
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure $req_url }
    "post" => { if ($body | is-empty) { http post --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url "" } else { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url $body } }
    "put" => { if ($body | is-empty) { http put --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url "" } else { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url $body } }
    "patch" => { if ($body | is-empty) { http patch --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url "" } else { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url $body } }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method == "head") and (not $full) and (not $allow_errors) and $resp.status < 400 { return $resp.headers }
  if $allow_errors { $resp } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else if $full { {status: $resp.status, headers: $resp.headers, body: $resp.body} } else if $resp.status == 204 { null } else { $resp.body }
}

def base-url-completer [] { ["https://demo.netbox.dev/api"] }
def auth-scheme-completer [] { ["bearer"] }

# Completers for enum parameters
def term-side-completer [] { ["A" "Z"] }
def status-completer [] { ["active" "decommissioned" "deprovisioning" "offline" "planned" "provisioning"] }
def cable-end-completer [] { ["A" "B"] }
def length-unit-completer [] { ["cm" "ft" "in" "km" "m" "mi"] }
def status-completer-1 [] { ["connected" "decommissioning" "planned"] }
def type-completer [] { ["aoc" "cat3" "cat5" "cat5e" "cat6" "cat6a" "cat7" "cat7a" "cat8" "coaxial" "dac-active" "dac-passive" "mmf" "mmf-om1" "mmf-om2" "mmf-om3" "mmf-om4" "mmf-om5" "mrj21-trunk" "power" "smf" "smf-os1" "smf-os2"] }
def type-completer-1 [] { ["db-25" "de-9" "mini-din-8" "other" "rj-11" "rj-12" "rj-45" "usb-a" "usb-b" "usb-c" "usb-micro-a" "usb-micro-ab" "usb-micro-b" "usb-mini-a" "usb-mini-b"] }
def speed-completer [] { ["115200" "1200" "19200" "2400" "38400" "4800" "57600" "9600"] }
def airflow-completer [] { ["front-to-rear" "left-to-right" "mixed" "passive" "rear-to-front" "right-to-left" "side-to-rear"] }
def subdevice-role-completer [] { ["child" "parent"] }
def weight-unit-completer [] { ["g" "kg" "lb" "oz"] }
def face-completer [] { ["front" "rear"] }
def status-completer-2 [] { ["active" "decommissioning" "failed" "inventory" "offline" "planned" "staged"] }
def type-completer-2 [] { ["110-punch" "4p2c" "4p4c" "6p2c" "6p4c" "6p6c" "8p2c" "8p4c" "8p6c" "8p8c" "bnc" "cs" "f" "fc" "gg45" "lc" "lc-apc" "lc-pc" "lc-upc" "lsh" "lsh-apc" "lsh-pc" "lsh-upc" "mpo" "mrj21" "mtrj" "n" "other" "sc" "sc-apc" "sc-pc" "sc-upc" "sma-905" "sma-906" "sn" "splice" "st" "tera-1p" "tera-2p" "tera-4p" "urm-p2" "urm-p4" "urm-p8"] }
def poe-mode-completer [] { ["pd" "pse"] }
def poe-type-completer [] { ["passive-24v-2pair" "passive-24v-4pair" "passive-48v-2pair" "passive-48v-4pair" "type1-ieee802.3af" "type2-ieee802.3at" "type2-ieee802.3az" "type3-ieee802.3bt" "type4-ieee802.3bt"] }
def type-completer-3 [] { ["1000base-kx" "1000base-t" "1000base-x-gbic" "1000base-x-sfp" "100base-fx" "100base-lfx" "100base-t1" "100base-tx" "100gbase-kp4" "100gbase-kr2" "100gbase-kr4" "100gbase-x-cfp" "100gbase-x-cfp2" "100gbase-x-cfp4" "100gbase-x-cpak" "100gbase-x-qsfp28" "10g-epon" "10gbase-cx4" "10gbase-kr" "10gbase-kx4" "10gbase-t" "10gbase-x-sfpp" "10gbase-x-x2" "10gbase-x-xenpak" "10gbase-x-xfp" "128gfc-qsfp28" "16gfc-sfpp" "1gfc-sfp" "2.5gbase-t" "200gbase-x-cfp2" "200gbase-x-qsfp56" "25gbase-kr" "25gbase-x-sfp28" "2gfc-sfp" "32gfc-sfp28" "400gbase-x-osfp" "400gbase-x-qsfpdd" "40gbase-kr4" "40gbase-x-qsfpp" "4gfc-sfp" "50gbase-kr" "50gbase-x-sfp28" "50gbase-x-sfp56" "5gbase-t" "64gfc-qsfpp" "800gbase-x-osfp" "800gbase-x-qsfpdd" "8gfc-sfpp" "bridge" "cdma" "cisco-flexstack" "cisco-flexstack-plus" "cisco-stackwise" "cisco-stackwise-160" "cisco-stackwise-1t" "cisco-stackwise-320" "cisco-stackwise-480" "cisco-stackwise-80" "cisco-stackwise-plus" "docsis" "e1" "e3" "epon" "extreme-summitstack" "extreme-summitstack-128" "extreme-summitstack-256" "extreme-summitstack-512" "gpon" "gsm" "ieee802.11a" "ieee802.11ac" "ieee802.11ad" "ieee802.11ax" "ieee802.11ay" "ieee802.11g" "ieee802.11n" "ieee802.15.1" "infiniband-ddr" "infiniband-edr" "infiniband-fdr" "infiniband-fdr10" "infiniband-hdr" "infiniband-ndr" "infiniband-qdr" "infiniband-sdr" "infiniband-xdr" "juniper-vcp" "lag" "lte" "ng-pon2" "other" "other-wireless" "sonet-oc12" "sonet-oc192" "sonet-oc1920" "sonet-oc3" "sonet-oc3840" "sonet-oc48" "sonet-oc768" "t1" "t3" "virtual" "xdsl" "xg-pon" "xgs-pon"] }
def duplex-completer [] { ["auto" "full" "half"] }
def mode-completer [] { ["access" "tagged" "tagged-all"] }
def rf-channel-completer [] { ["2.4g-1-2412-22" "2.4g-10-2457-22" "2.4g-11-2462-22" "2.4g-12-2467-22" "2.4g-13-2472-22" "2.4g-2-2417-22" "2.4g-3-2422-22" "2.4g-4-2427-22" "2.4g-5-2432-22" "2.4g-6-2437-22" "2.4g-7-2442-22" "2.4g-8-2447-22" "2.4g-9-2452-22" "5g-100-5500-20" "5g-102-5510-40" "5g-104-5520-20" "5g-106-5530-80" "5g-108-5540-20" "5g-110-5550-40" "5g-112-5560-20" "5g-114-5570-160" "5g-116-5580-20" "5g-118-5590-40" "5g-120-5600-20" "5g-122-5610-80" "5g-124-5620-20" "5g-126-5630-40" "5g-128-5640-20" "5g-132-5660-20" "5g-134-5670-40" "5g-136-5680-20" "5g-138-5690-80" "5g-140-5700-20" "5g-142-5710-40" "5g-144-5720-20" "5g-149-5745-20" "5g-151-5755-40" "5g-153-5765-20" "5g-155-5775-80" "5g-157-5785-20" "5g-159-5795-40" "5g-161-5805-20" "5g-163-5815-160" "5g-165-5825-20" "5g-167-5835-40" "5g-169-5845-20" "5g-171-5855-80" "5g-173-5865-20" "5g-175-5875-40" "5g-177-5885-20" "5g-32-5160-20" "5g-34-5170-40" "5g-36-5180-20" "5g-38-5190-40" "5g-40-5200-20" "5g-42-5210-80" "5g-44-5220-20" "5g-46-5230-40" "5g-48-5240-20" "5g-50-5250-160" "5g-52-5260-20" "5g-54-5270-40" "5g-56-5280-20" "5g-58-5290-80" "5g-60-5300-20" "5g-62-5310-40" "5g-64-5320-20" "60g-1-58320-2160" "60g-10-61560-4320" "60g-11-63720-4320" "60g-12-65880-4320" "60g-13-68040-4320" "60g-17-60480-6480" "60g-18-62640-6480" "60g-19-64800-6480" "60g-2-60480-2160" "60g-20-66960-6480" "60g-25-61560-6480" "60g-26-63720-6480" "60g-27-65880-6480" "60g-3-62640-2160" "60g-4-64800-2160" "60g-5-66960-2160" "60g-6-69120-2160" "60g-9-59400-4320" "6g-1-5955-20" "6g-101-6455-20" "6g-103-6465-80" "6g-105-6475-20" "6g-107-6485-40" "6g-109-6495-20" "6g-11-6005-40" "6g-111-6505-160" "6g-113-6515-20" "6g-115-6525-40" "6g-117-6535-20" "6g-119-6545-80" "6g-121-6555-20" "6g-123-6565-40" "6g-125-6575-20" "6g-129-6595-20" "6g-13-6015-20" "6g-131-6605-40" "6g-133-6615-20" "6g-135-6625-80" "6g-137-6635-20" "6g-139-6645-40" "6g-141-6655-20" "6g-143-6665-160" "6g-145-6675-20" "6g-147-6685-40" "6g-149-6695-20" "6g-15-6025-160" "6g-151-6705-80" "6g-153-6715-20" "6g-155-6725-40" "6g-157-6735-20" "6g-159-6745-320" "6g-161-6755-20" "6g-163-6765-40" "6g-165-6775-20" "6g-167-6785-80" "6g-169-6795-20" "6g-17-6035-20" "6g-171-6805-40" "6g-173-6815-20" "6g-175-6825-160" "6g-177-6835-20" "6g-179-6845-40" "6g-181-6855-20" "6g-183-6865-80" "6g-185-6875-20" "6g-187-6885-40" "6g-189-6895-20" "6g-19-6045-40" "6g-193-6915-20" "6g-195-6925-40" "6g-197-6935-20" "6g-199-6945-80" "6g-201-6955-20" "6g-203-6965-40" "6g-205-6975-20" "6g-207-6985-160" "6g-209-6995-20" "6g-21-6055-20" "6g-211-7005-40" "6g-213-7015-20" "6g-215-7025-80" "6g-217-7035-20" "6g-219-7045-40" "6g-221-7055-20" "6g-225-7075-20" "6g-227-7085-40" "6g-229-7095-20" "6g-23-6065-80" "6g-233-7115-20" "6g-25-6075-20" "6g-27-6085-40" "6g-29-6095-20" "6g-3-5965-40" "6g-31-6105-320" "6g-33-6115-20" "6g-35-6125-40" "6g-37-6135-20" "6g-39-6145-80" "6g-41-6155-20" "6g-43-6165-40" "6g-45-6175-20" "6g-47-6185-160" "6g-49-6195-20" "6g-5-5975-20" "6g-51-6205-40" "6g-53-6215-20" "6g-55-6225-80" "6g-57-6235-20" "6g-59-6245-40" "6g-61-6255-20" "6g-65-6275-20" "6g-67-6285-40" "6g-69-6295-20" "6g-7-5985-80" "6g-71-6305-80" "6g-73-6315-20" "6g-75-6325-40" "6g-77-6335-20" "6g-79-6345-160" "6g-81-6355-20" "6g-83-6365-40" "6g-85-6375-20" "6g-87-6385-80" "6g-89-6395-20" "6g-9-5995-20" "6g-91-6405-40" "6g-93-6415-20" "6g-95-6425-320" "6g-97-6435-20" "6g-99-6445-40"] }
def rf-role-completer [] { ["ap" "station"] }
def status-completer-3 [] { ["active" "decommissioning" "planned" "retired" "staging"] }
def status-completer-4 [] { ["active" "decommissioning" "failed" "offline" "planned" "staged"] }
def phase-completer [] { ["single-phase" "three-phase"] }
def status-completer-5 [] { ["active" "failed" "offline" "planned"] }
def supply-completer [] { ["ac" "dc"] }
def type-completer-4 [] { ["primary" "redundant"] }
def feed-leg-completer [] { ["A" "B" "C"] }
def type-completer-5 [] { ["CS6360C" "CS6364C" "CS8164C" "CS8264C" "CS8364C" "CS8464C" "dc-terminal" "hardwired" "hdot-cx" "iec-60309-2p-e-4h" "iec-60309-2p-e-6h" "iec-60309-2p-e-9h" "iec-60309-3p-e-4h" "iec-60309-3p-e-6h" "iec-60309-3p-e-9h" "iec-60309-3p-n-e-4h" "iec-60309-3p-n-e-6h" "iec-60309-3p-n-e-9h" "iec-60309-p-n-e-4h" "iec-60309-p-n-e-6h" "iec-60309-p-n-e-9h" "iec-60320-c13" "iec-60320-c15" "iec-60320-c19" "iec-60320-c21" "iec-60320-c5" "iec-60320-c7" "ita-e" "ita-f" "ita-g" "ita-h" "ita-i" "ita-j" "ita-k" "ita-l" "ita-m" "ita-multistandard" "ita-n" "ita-o" "nema-1-15r" "nema-10-30r" "nema-10-50r" "nema-14-20r" "nema-14-30r" "nema-14-50r" "nema-14-60r" "nema-15-15r" "nema-15-20r" "nema-15-30r" "nema-15-50r" "nema-15-60r" "nema-5-15r" "nema-5-20r" "nema-5-30r" "nema-5-50r" "nema-6-15r" "nema-6-20r" "nema-6-30r" "nema-6-50r" "nema-l1-15r" "nema-l10-30r" "nema-l14-20r" "nema-l14-30r" "nema-l14-50r" "nema-l14-60r" "nema-l15-20r" "nema-l15-30r" "nema-l15-50r" "nema-l15-60r" "nema-l21-20r" "nema-l21-30r" "nema-l22-30r" "nema-l5-15r" "nema-l5-20r" "nema-l5-30r" "nema-l5-50r" "nema-l6-15r" "nema-l6-20r" "nema-l6-30r" "nema-l6-50r" "neutrik-powercon-20a" "neutrik-powercon-32a" "neutrik-powercon-true1" "neutrik-powercon-true1-top" "other" "saf-d-grid" "ubiquiti-smartpower" "usb-a" "usb-c" "usb-micro-b"] }
def type-completer-6 [] { ["cs6361c" "cs6365c" "cs8165c" "cs8265c" "cs8365c" "cs8465c" "dc-terminal" "hardwired" "iec-60309-2p-e-4h" "iec-60309-2p-e-6h" "iec-60309-2p-e-9h" "iec-60309-3p-e-4h" "iec-60309-3p-e-6h" "iec-60309-3p-e-9h" "iec-60309-3p-n-e-4h" "iec-60309-3p-n-e-6h" "iec-60309-3p-n-e-9h" "iec-60309-p-n-e-4h" "iec-60309-p-n-e-6h" "iec-60309-p-n-e-9h" "iec-60320-c14" "iec-60320-c16" "iec-60320-c20" "iec-60320-c22" "iec-60320-c6" "iec-60320-c8" "ita-c" "ita-e" "ita-ef" "ita-f" "ita-g" "ita-h" "ita-i" "ita-j" "ita-k" "ita-l" "ita-m" "ita-n" "ita-o" "nema-1-15p" "nema-10-30p" "nema-10-50p" "nema-14-20p" "nema-14-30p" "nema-14-50p" "nema-14-60p" "nema-15-15p" "nema-15-20p" "nema-15-30p" "nema-15-50p" "nema-15-60p" "nema-5-15p" "nema-5-20p" "nema-5-30p" "nema-5-50p" "nema-6-15p" "nema-6-20p" "nema-6-30p" "nema-6-50p" "nema-l1-15p" "nema-l10-30p" "nema-l14-20p" "nema-l14-30p" "nema-l14-50p" "nema-l14-60p" "nema-l15-20p" "nema-l15-30p" "nema-l15-50p" "nema-l15-60p" "nema-l21-20p" "nema-l21-30p" "nema-l22-30p" "nema-l5-15p" "nema-l5-20p" "nema-l5-30p" "nema-l5-50p" "nema-l6-15p" "nema-l6-20p" "nema-l6-30p" "nema-l6-50p" "neutrik-powercon-20" "neutrik-powercon-32" "neutrik-powercon-true1" "neutrik-powercon-true1-top" "other" "saf-d-grid" "ubiquiti-smartpower" "usb-3-b" "usb-3-micro-b" "usb-a" "usb-b" "usb-c" "usb-micro-a" "usb-micro-ab" "usb-micro-b" "usb-mini-a" "usb-mini-b"] }
def outer-unit-completer [] { ["in" "mm"] }
def status-completer-6 [] { ["active" "available" "deprecated" "planned" "reserved"] }
def type-completer-7 [] { ["2-post-frame" "4-post-cabinet" "4-post-frame" "wall-cabinet" "wall-cabinet-vertical" "wall-frame" "wall-frame-vertical"] }
def width-completer [] { ["10" "19" "21" "23"] }
def render-completer [] { ["json" "svg"] }
def status-completer-7 [] { ["active" "offline" "planned"] }
def filter-logic-completer [] { ["disabled" "exact" "loose"] }
def type-completer-8 [] { ["boolean" "date" "decimal" "integer" "json" "longtext" "multiobject" "multiselect" "object" "select" "text" "url"] }
def ui-visibility-completer [] { ["hidden" "read-only" "read-write"] }
def button-class-completer [] { ["black" "blue" "cyan" "ghost-dark" "gray" "green" "indigo" "orange" "outline-dark" "pink" "purple" "red" "teal" "white" "yellow"] }
def kind-completer [] { ["danger" "info" "success" "warning"] }
def http-method-completer [] { ["DELETE" "GET" "PATCH" "POST" "PUT"] }
def auth-type-completer [] { ["md5" "plaintext"] }
def protocol-completer [] { ["carp" "clusterxl" "glbp" "hsrp" "other" "vrrp2" "vrrp3"] }
def role-completer [] { ["anycast" "carp" "glbp" "hsrp" "loopback" "secondary" "vip" "vrrp"] }
def status-completer-8 [] { ["active" "deprecated" "dhcp" "reserved" "slaac"] }
def status-completer-9 [] { ["active" "deprecated" "reserved"] }
def type-completer-9 [] { ["ep-lan" "ep-tree" "epl" "evp-lan" "evp-tree" "evpl" "mpls-evpn" "pbb-evpn" "vpls" "vpws" "vxlan" "vxlan-evpn"] }
def status-completer-10 [] { ["active" "container" "deprecated" "reserved"] }
def protocol-completer-1 [] { ["sctp" "tcp" "udp"] }
def priority-completer [] { ["inactive" "primary" "secondary" "tertiary"] }
def status-completer-11 [] { ["active" "decommissioning" "offline" "planned" "staging"] }
def auth-cipher-completer [] { ["aes" "auto" "tkip"] }
def auth-type-completer-1 [] { ["open" "wep" "wpa-enterprise" "wpa-personal"] }
def status-completer-12 [] { ["active" "deprecated" "disabled" "reserved"] }

# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "full" "dry-run" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "circuits-circuit-terminations delete-bulk" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# DELETE /circuits/circuit-terminations/
#
# operationId: circuits_circuit-terminations_bulk_delete
export def "circuits-circuit-terminations delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-terminations/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/circuit-terminations/
#
# operationId: circuits_circuit-terminations_list
export def "circuits-circuit-terminations list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --term-side: string
  --port-speed: string
  --upstream-speed: string
  --xconnect-id: string
  --description: string
  --cable-end: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --cabled: string
  --occupied: string
  --circuit-id: string
  --site-id: string
  --site: string
  --provider-network-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --term-side-n: string
  --port-speed-n: string
  --port-speed-lte: string
  --port-speed-lt: string
  --port-speed-gte: string
  --port-speed-gt: string
  --upstream-speed-n: string
  --upstream-speed-lte: string
  --upstream-speed-lt: string
  --upstream-speed-gte: string
  --upstream-speed-gt: string
  --xconnect-id-n: string
  --xconnect-id-ic: string
  --xconnect-id-nic: string
  --xconnect-id-iew: string
  --xconnect-id-niew: string
  --xconnect-id-isw: string
  --xconnect-id-nisw: string
  --xconnect-id-ie: string
  --xconnect-id-nie: string
  --xconnect-id-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --circuit-id-n: string
  --site-id-n: string
  --site-n: string
  --provider-network-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, cable: record, cable_end: string, circuit: record, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record, site: record, tags: list, term_side: string, upstream_speed: int, url: string, xconnect_id: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "term_side" $term_side "scalar") (serialize-qp "port_speed" $port_speed "scalar") (serialize-qp "upstream_speed" $upstream_speed "scalar") (serialize-qp "xconnect_id" $xconnect_id "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "circuit_id" $circuit_id "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "provider_network_id" $provider_network_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "term_side__n" $term_side_n "scalar") (serialize-qp "port_speed__n" $port_speed_n "scalar") (serialize-qp "port_speed__lte" $port_speed_lte "scalar") (serialize-qp "port_speed__lt" $port_speed_lt "scalar") (serialize-qp "port_speed__gte" $port_speed_gte "scalar") (serialize-qp "port_speed__gt" $port_speed_gt "scalar") (serialize-qp "upstream_speed__n" $upstream_speed_n "scalar") (serialize-qp "upstream_speed__lte" $upstream_speed_lte "scalar") (serialize-qp "upstream_speed__lt" $upstream_speed_lt "scalar") (serialize-qp "upstream_speed__gte" $upstream_speed_gte "scalar") (serialize-qp "upstream_speed__gt" $upstream_speed_gt "scalar") (serialize-qp "xconnect_id__n" $xconnect_id_n "scalar") (serialize-qp "xconnect_id__ic" $xconnect_id_ic "scalar") (serialize-qp "xconnect_id__nic" $xconnect_id_nic "scalar") (serialize-qp "xconnect_id__iew" $xconnect_id_iew "scalar") (serialize-qp "xconnect_id__niew" $xconnect_id_niew "scalar") (serialize-qp "xconnect_id__isw" $xconnect_id_isw "scalar") (serialize-qp "xconnect_id__nisw" $xconnect_id_nisw "scalar") (serialize-qp "xconnect_id__ie" $xconnect_id_ie "scalar") (serialize-qp "xconnect_id__nie" $xconnect_id_nie "scalar") (serialize-qp "xconnect_id__empty" $xconnect_id_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "circuit_id__n" $circuit_id_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "provider_network_id__n" $provider_network_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/circuits/circuit-terminations/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "term_side": $term_side, "port_speed": $port_speed, "upstream_speed": $upstream_speed, "xconnect_id": $xconnect_id, "description": $description, "cable_end": $cable_end, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "circuit_id": $circuit_id, "site_id": $site_id, "site": $site, "provider_network_id": $provider_network_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "term_side__n": $term_side_n, "port_speed__n": $port_speed_n, "port_speed__lte": $port_speed_lte, "port_speed__lt": $port_speed_lt, "port_speed__gte": $port_speed_gte, "port_speed__gt": $port_speed_gt, "upstream_speed__n": $upstream_speed_n, "upstream_speed__lte": $upstream_speed_lte, "upstream_speed__lt": $upstream_speed_lt, "upstream_speed__gte": $upstream_speed_gte, "upstream_speed__gt": $upstream_speed_gt, "xconnect_id__n": $xconnect_id_n, "xconnect_id__ic": $xconnect_id_ic, "xconnect_id__nic": $xconnect_id_nic, "xconnect_id__iew": $xconnect_id_iew, "xconnect_id__niew": $xconnect_id_niew, "xconnect_id__isw": $xconnect_id_isw, "xconnect_id__nisw": $xconnect_id_nisw, "xconnect_id__ie": $xconnect_id_ie, "xconnect_id__nie": $xconnect_id_nie, "xconnect_id__empty": $xconnect_id_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "circuit_id__n": $circuit_id_n, "site_id__n": $site_id_n, "site__n": $site_n, "provider_network_id__n": $provider_network_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /circuits/circuit-terminations/
#
# operationId: circuits_circuit-terminations_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-terminations update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  circuit: int
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --port-speed: int # nullable
  --pp-info: string
  --provider-network: int # nullable
  --site: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  term_side: string@term-side-completer
  --upstream-speed: int # Upstream speed, if different from port speed (nullable)
  --xconnect-id: string
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, circuit: record<cid: string, display: string, id: int, url: string>, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, term_side: string, upstream_speed: int, url: string, xconnect_id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-terminations/")
  let req_body = {"cable": $cable, "circuit": $circuit, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "port_speed": $port_speed, "pp_info": $pp_info, "provider_network": $provider_network, "site": $site, "tags": $tags, "term_side": $term_side, "upstream_speed": $upstream_speed, "xconnect_id": $xconnect_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /circuits/circuit-terminations/
#
# operationId: circuits_circuit-terminations_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-terminations create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  circuit: int
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --port-speed: int # nullable
  --pp-info: string
  --provider-network: int # nullable
  --site: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  term_side: string@term-side-completer
  --upstream-speed: int # Upstream speed, if different from port speed (nullable)
  --xconnect-id: string
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, circuit: record<cid: string, display: string, id: int, url: string>, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, term_side: string, upstream_speed: int, url: string, xconnect_id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-terminations/")
  let req_body = {"cable": $cable, "circuit": $circuit, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "port_speed": $port_speed, "pp_info": $pp_info, "provider_network": $provider_network, "site": $site, "tags": $tags, "term_side": $term_side, "upstream_speed": $upstream_speed, "xconnect_id": $xconnect_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/circuit-terminations/
#
# operationId: circuits_circuit-terminations_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-terminations update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  circuit: int
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --port-speed: int # nullable
  --pp-info: string
  --provider-network: int # nullable
  --site: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  term_side: string@term-side-completer
  --upstream-speed: int # Upstream speed, if different from port speed (nullable)
  --xconnect-id: string
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, circuit: record<cid: string, display: string, id: int, url: string>, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, term_side: string, upstream_speed: int, url: string, xconnect_id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-terminations/")
  let req_body = {"cable": $cable, "circuit": $circuit, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "port_speed": $port_speed, "pp_info": $pp_info, "provider_network": $provider_network, "site": $site, "tags": $tags, "term_side": $term_side, "upstream_speed": $upstream_speed, "xconnect_id": $xconnect_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/circuit-terminations/{id}/
#
# operationId: circuits_circuit-terminations_delete
export def "circuits-circuit-terminations delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-terminations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/circuit-terminations/{id}/
#
# operationId: circuits_circuit-terminations_read
export def "circuits-circuit-terminations get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, circuit: record<cid: string, display: string, id: int, url: string>, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, term_side: string, upstream_speed: int, url: string, xconnect_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-terminations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /circuits/circuit-terminations/{id}/
#
# operationId: circuits_circuit-terminations_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-terminations update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  circuit: int
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --port-speed: int # nullable
  --pp-info: string
  --provider-network: int # nullable
  --site: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  term_side: string@term-side-completer
  --upstream-speed: int # Upstream speed, if different from port speed (nullable)
  --xconnect-id: string
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, circuit: record<cid: string, display: string, id: int, url: string>, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, term_side: string, upstream_speed: int, url: string, xconnect_id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-terminations/{id}/"))
  let req_body = {"cable": $cable, "circuit": $circuit, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "port_speed": $port_speed, "pp_info": $pp_info, "provider_network": $provider_network, "site": $site, "tags": $tags, "term_side": $term_side, "upstream_speed": $upstream_speed, "xconnect_id": $xconnect_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/circuit-terminations/{id}/
#
# operationId: circuits_circuit-terminations_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-terminations update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  circuit: int
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --port-speed: int # nullable
  --pp-info: string
  --provider-network: int # nullable
  --site: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  term_side: string@term-side-completer
  --upstream-speed: int # Upstream speed, if different from port speed (nullable)
  --xconnect-id: string
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, circuit: record<cid: string, display: string, id: int, url: string>, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, term_side: string, upstream_speed: int, url: string, xconnect_id: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-terminations/{id}/"))
  let req_body = {"cable": $cable, "circuit": $circuit, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "port_speed": $port_speed, "pp_info": $pp_info, "provider_network": $provider_network, "site": $site, "tags": $tags, "term_side": $term_side, "upstream_speed": $upstream_speed, "xconnect_id": $xconnect_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Return all CablePaths which traverse a given pass-through port.
#
# GET /circuits/circuit-terminations/{id}/paths/
# operationId: circuits_circuit-terminations_paths
export def "circuits-circuit-terminations-paths get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, circuit: record<cid: string, display: string, id: int, url: string>, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, port_speed: int, pp_info: string, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, term_side: string, upstream_speed: int, url: string, xconnect_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-terminations/{id}/paths/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /circuits/circuit-types/
#
# operationId: circuits_circuit-types_bulk_delete
export def "circuits-circuit-types delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-types/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/circuit-types/
#
# operationId: circuits_circuit-types_list
export def "circuits-circuit-types list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<circuit_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/circuits/circuit-types/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /circuits/circuit-types/
#
# operationId: circuits_circuit-types_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-types update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-types/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /circuits/circuit-types/
#
# operationId: circuits_circuit-types_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-types create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-types/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/circuit-types/
#
# operationId: circuits_circuit-types_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-types update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuit-types/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/circuit-types/{id}/
#
# operationId: circuits_circuit-types_delete
export def "circuits-circuit-types delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/circuit-types/{id}/
#
# operationId: circuits_circuit-types_read
export def "circuits-circuit-types get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<circuit_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /circuits/circuit-types/{id}/
#
# operationId: circuits_circuit-types_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-types update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-types/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/circuit-types/{id}/
#
# operationId: circuits_circuit-types_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuit-types update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuit-types/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/circuits/
#
# operationId: circuits_circuits_bulk_delete
export def "circuits-circuits delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuits/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/circuits/
#
# operationId: circuits_circuits_list
export def "circuits-circuits list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --cid: string
  --description: string
  --install-date: string
  --termination-date: string
  --commit-rate: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --provider-id: string
  --provider: string
  --provider-network-id: string
  --type-id: string
  --type: string
  --status: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --cid-n: string
  --cid-ic: string
  --cid-nic: string
  --cid-iew: string
  --cid-niew: string
  --cid-isw: string
  --cid-nisw: string
  --cid-ie: string
  --cid-nie: string
  --cid-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --install-date-n: string
  --install-date-lte: string
  --install-date-lt: string
  --install-date-gte: string
  --install-date-gt: string
  --termination-date-n: string
  --termination-date-lte: string
  --termination-date-lt: string
  --termination-date-gte: string
  --termination-date-gt: string
  --commit-rate-n: string
  --commit-rate-lte: string
  --commit-rate-lt: string
  --commit-rate-gte: string
  --commit-rate-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --provider-id-n: string
  --provider-n: string
  --provider-network-id-n: string
  --type-id-n: string
  --type-n: string
  --status-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<cid: string, comments: string, commit_rate: int, created: string, custom_fields: record, description: string, display: string, id: int, install_date: string, last_updated: string, provider: record, status: record, tags: list, tenant: record, termination_a: record, termination_date: string, termination_z: record, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "cid" $cid "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "install_date" $install_date "scalar") (serialize-qp "termination_date" $termination_date "scalar") (serialize-qp "commit_rate" $commit_rate "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "provider_id" $provider_id "scalar") (serialize-qp "provider" $provider "scalar") (serialize-qp "provider_network_id" $provider_network_id "scalar") (serialize-qp "type_id" $type_id "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "cid__n" $cid_n "scalar") (serialize-qp "cid__ic" $cid_ic "scalar") (serialize-qp "cid__nic" $cid_nic "scalar") (serialize-qp "cid__iew" $cid_iew "scalar") (serialize-qp "cid__niew" $cid_niew "scalar") (serialize-qp "cid__isw" $cid_isw "scalar") (serialize-qp "cid__nisw" $cid_nisw "scalar") (serialize-qp "cid__ie" $cid_ie "scalar") (serialize-qp "cid__nie" $cid_nie "scalar") (serialize-qp "cid__empty" $cid_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "install_date__n" $install_date_n "scalar") (serialize-qp "install_date__lte" $install_date_lte "scalar") (serialize-qp "install_date__lt" $install_date_lt "scalar") (serialize-qp "install_date__gte" $install_date_gte "scalar") (serialize-qp "install_date__gt" $install_date_gt "scalar") (serialize-qp "termination_date__n" $termination_date_n "scalar") (serialize-qp "termination_date__lte" $termination_date_lte "scalar") (serialize-qp "termination_date__lt" $termination_date_lt "scalar") (serialize-qp "termination_date__gte" $termination_date_gte "scalar") (serialize-qp "termination_date__gt" $termination_date_gt "scalar") (serialize-qp "commit_rate__n" $commit_rate_n "scalar") (serialize-qp "commit_rate__lte" $commit_rate_lte "scalar") (serialize-qp "commit_rate__lt" $commit_rate_lt "scalar") (serialize-qp "commit_rate__gte" $commit_rate_gte "scalar") (serialize-qp "commit_rate__gt" $commit_rate_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "provider_id__n" $provider_id_n "scalar") (serialize-qp "provider__n" $provider_n "scalar") (serialize-qp "provider_network_id__n" $provider_network_id_n "scalar") (serialize-qp "type_id__n" $type_id_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/circuits/circuits/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "cid": $cid, "description": $description, "install_date": $install_date, "termination_date": $termination_date, "commit_rate": $commit_rate, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "provider_id": $provider_id, "provider": $provider, "provider_network_id": $provider_network_id, "type_id": $type_id, "type": $type, "status": $status, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "cid__n": $cid_n, "cid__ic": $cid_ic, "cid__nic": $cid_nic, "cid__iew": $cid_iew, "cid__niew": $cid_niew, "cid__isw": $cid_isw, "cid__nisw": $cid_nisw, "cid__ie": $cid_ie, "cid__nie": $cid_nie, "cid__empty": $cid_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "install_date__n": $install_date_n, "install_date__lte": $install_date_lte, "install_date__lt": $install_date_lt, "install_date__gte": $install_date_gte, "install_date__gt": $install_date_gt, "termination_date__n": $termination_date_n, "termination_date__lte": $termination_date_lte, "termination_date__lt": $termination_date_lt, "termination_date__gte": $termination_date_gte, "termination_date__gt": $termination_date_gt, "commit_rate__n": $commit_rate_n, "commit_rate__lte": $commit_rate_lte, "commit_rate__lt": $commit_rate_lt, "commit_rate__gte": $commit_rate_gte, "commit_rate__gt": $commit_rate_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "provider_id__n": $provider_id_n, "provider__n": $provider_n, "provider_network_id__n": $provider_network_id_n, "type_id__n": $type_id_n, "type__n": $type_n, "status__n": $status_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /circuits/circuits/
#
# operationId: circuits_circuits_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuits update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cid: string
  --comments: string
  --commit-rate: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --install-date: string # nullable, format: date
  provider: int
  --status: string@status-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --termination-date: string # nullable, format: date
  type: int
]: any -> record<cid: string, comments: string, commit_rate: int, created: string, custom_fields: record, description: string, display: string, id: int, install_date: string, last_updated: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, termination_a: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, termination_date: string, termination_z: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, type: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuits/")
  let req_body = {"cid": $cid, "comments": $comments, "commit_rate": $commit_rate, "custom_fields": $custom_fields, "description": $description, "install_date": $install_date, "provider": $provider, "status": $status, "tags": $tags, "tenant": $tenant, "termination_date": $termination_date, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /circuits/circuits/
#
# operationId: circuits_circuits_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuits create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cid: string
  --comments: string
  --commit-rate: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --install-date: string # nullable, format: date
  provider: int
  --status: string@status-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --termination-date: string # nullable, format: date
  type: int
]: any -> record<cid: string, comments: string, commit_rate: int, created: string, custom_fields: record, description: string, display: string, id: int, install_date: string, last_updated: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, termination_a: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, termination_date: string, termination_z: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, type: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuits/")
  let req_body = {"cid": $cid, "comments": $comments, "commit_rate": $commit_rate, "custom_fields": $custom_fields, "description": $description, "install_date": $install_date, "provider": $provider, "status": $status, "tags": $tags, "tenant": $tenant, "termination_date": $termination_date, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/circuits/
#
# operationId: circuits_circuits_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuits update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cid: string
  --comments: string
  --commit-rate: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --install-date: string # nullable, format: date
  provider: int
  --status: string@status-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --termination-date: string # nullable, format: date
  type: int
]: any -> record<cid: string, comments: string, commit_rate: int, created: string, custom_fields: record, description: string, display: string, id: int, install_date: string, last_updated: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, termination_a: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, termination_date: string, termination_z: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, type: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/circuits/")
  let req_body = {"cid": $cid, "comments": $comments, "commit_rate": $commit_rate, "custom_fields": $custom_fields, "description": $description, "install_date": $install_date, "provider": $provider, "status": $status, "tags": $tags, "tenant": $tenant, "termination_date": $termination_date, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/circuits/{id}/
#
# operationId: circuits_circuits_delete
export def "circuits-circuits delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuits/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/circuits/{id}/
#
# operationId: circuits_circuits_read
export def "circuits-circuits get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cid: string, comments: string, commit_rate: int, created: string, custom_fields: record, description: string, display: string, id: int, install_date: string, last_updated: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, termination_a: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, termination_date: string, termination_z: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, type: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuits/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /circuits/circuits/{id}/
#
# operationId: circuits_circuits_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuits update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cid: string
  --comments: string
  --commit-rate: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --install-date: string # nullable, format: date
  provider: int
  --status: string@status-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --termination-date: string # nullable, format: date
  type: int
]: any -> record<cid: string, comments: string, commit_rate: int, created: string, custom_fields: record, description: string, display: string, id: int, install_date: string, last_updated: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, termination_a: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, termination_date: string, termination_z: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, type: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuits/{id}/"))
  let req_body = {"cid": $cid, "comments": $comments, "commit_rate": $commit_rate, "custom_fields": $custom_fields, "description": $description, "install_date": $install_date, "provider": $provider, "status": $status, "tags": $tags, "tenant": $tenant, "termination_date": $termination_date, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/circuits/{id}/
#
# operationId: circuits_circuits_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-circuits update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cid: string
  --comments: string
  --commit-rate: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --install-date: string # nullable, format: date
  provider: int
  --status: string@status-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --termination-date: string # nullable, format: date
  type: int
]: any -> record<cid: string, comments: string, commit_rate: int, created: string, custom_fields: record, description: string, display: string, id: int, install_date: string, last_updated: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, termination_a: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, termination_date: string, termination_z: record<description: string, display: string, id: int, port_speed: int, provider_network: record<display: string, id: int, name: string, url: string>, site: record<display: string, id: int, name: string, slug: string, url: string>, upstream_speed: int, url: string, xconnect_id: string>, type: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/circuits/{id}/"))
  let req_body = {"cid": $cid, "comments": $comments, "commit_rate": $commit_rate, "custom_fields": $custom_fields, "description": $description, "install_date": $install_date, "provider": $provider, "status": $status, "tags": $tags, "tenant": $tenant, "termination_date": $termination_date, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/provider-networks/
#
# operationId: circuits_provider-networks_bulk_delete
export def "circuits-provider-networks delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/provider-networks/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/provider-networks/
#
# operationId: circuits_provider-networks_list
export def "circuits-provider-networks list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --service-id: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --provider-id: string
  --provider: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --service-id-n: string
  --service-id-ic: string
  --service-id-nic: string
  --service-id-iew: string
  --service-id-niew: string
  --service-id-isw: string
  --service-id-nisw: string
  --service-id-ie: string
  --service-id-nie: string
  --service-id-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --provider-id-n: string
  --provider-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, provider: record, service_id: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "service_id" $service_id "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "provider_id" $provider_id "scalar") (serialize-qp "provider" $provider "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "service_id__n" $service_id_n "scalar") (serialize-qp "service_id__ic" $service_id_ic "scalar") (serialize-qp "service_id__nic" $service_id_nic "scalar") (serialize-qp "service_id__iew" $service_id_iew "scalar") (serialize-qp "service_id__niew" $service_id_niew "scalar") (serialize-qp "service_id__isw" $service_id_isw "scalar") (serialize-qp "service_id__nisw" $service_id_nisw "scalar") (serialize-qp "service_id__ie" $service_id_ie "scalar") (serialize-qp "service_id__nie" $service_id_nie "scalar") (serialize-qp "service_id__empty" $service_id_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "provider_id__n" $provider_id_n "scalar") (serialize-qp "provider__n" $provider_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/circuits/provider-networks/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "service_id": $service_id, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "provider_id": $provider_id, "provider": $provider, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "service_id__n": $service_id_n, "service_id__ic": $service_id_ic, "service_id__nic": $service_id_nic, "service_id__iew": $service_id_iew, "service_id__niew": $service_id_niew, "service_id__isw": $service_id_isw, "service_id__nisw": $service_id_nisw, "service_id__ie": $service_id_ie, "service_id__nie": $service_id_nie, "service_id__empty": $service_id_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "provider_id__n": $provider_id_n, "provider__n": $provider_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /circuits/provider-networks/
#
# operationId: circuits_provider-networks_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-provider-networks update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  provider: int
  --service-id: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, service_id: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/provider-networks/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "provider": $provider, "service_id": $service_id, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /circuits/provider-networks/
#
# operationId: circuits_provider-networks_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-provider-networks create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  provider: int
  --service-id: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, service_id: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/provider-networks/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "provider": $provider, "service_id": $service_id, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/provider-networks/
#
# operationId: circuits_provider-networks_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-provider-networks update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  provider: int
  --service-id: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, service_id: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/provider-networks/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "provider": $provider, "service_id": $service_id, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/provider-networks/{id}/
#
# operationId: circuits_provider-networks_delete
export def "circuits-provider-networks delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/provider-networks/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/provider-networks/{id}/
#
# operationId: circuits_provider-networks_read
export def "circuits-provider-networks get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, service_id: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/provider-networks/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /circuits/provider-networks/{id}/
#
# operationId: circuits_provider-networks_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-provider-networks update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  provider: int
  --service-id: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, service_id: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/provider-networks/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "provider": $provider, "service_id": $service_id, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/provider-networks/{id}/
#
# operationId: circuits_provider-networks_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-provider-networks update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  provider: int
  --service-id: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, provider: record<circuit_count: int, display: string, id: int, name: string, slug: string, url: string>, service_id: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/provider-networks/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "provider": $provider, "service_id": $service_id, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/providers/
#
# operationId: circuits_providers_bulk_delete
export def "circuits-providers delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/providers/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/providers/
#
# operationId: circuits_providers_list
export def "circuits-providers list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --account: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --asn-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --account-n: string
  --account-ic: string
  --account-nic: string
  --account-iew: string
  --account-niew: string
  --account-isw: string
  --account-nisw: string
  --account-ie: string
  --account-nie: string
  --account-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --asn-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<account: string, asns: list, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "account" $account "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "asn_id" $asn_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "account__n" $account_n "scalar") (serialize-qp "account__ic" $account_ic "scalar") (serialize-qp "account__nic" $account_nic "scalar") (serialize-qp "account__iew" $account_iew "scalar") (serialize-qp "account__niew" $account_niew "scalar") (serialize-qp "account__isw" $account_isw "scalar") (serialize-qp "account__nisw" $account_nisw "scalar") (serialize-qp "account__ie" $account_ie "scalar") (serialize-qp "account__nie" $account_nie "scalar") (serialize-qp "account__empty" $account_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "asn_id__n" $asn_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/circuits/providers/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "account": $account, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "asn_id": $asn_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "account__n": $account_n, "account__ic": $account_ic, "account__nic": $account_nic, "account__iew": $account_iew, "account__niew": $account_niew, "account__isw": $account_isw, "account__nisw": $account_nisw, "account__ie": $account_ie, "account__nie": $account_nie, "account__empty": $account_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "asn_id__n": $asn_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /circuits/providers/
#
# operationId: circuits_providers_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-providers update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --account: string
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<account: string, asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/providers/")
  let req_body = {"account": $account, "asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /circuits/providers/
#
# operationId: circuits_providers_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-providers create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --account: string
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<account: string, asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/providers/")
  let req_body = {"account": $account, "asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/providers/
#
# operationId: circuits_providers_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-providers update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --account: string
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<account: string, asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/circuits/providers/")
  let req_body = {"account": $account, "asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /circuits/providers/{id}/
#
# operationId: circuits_providers_delete
export def "circuits-providers delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/providers/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /circuits/providers/{id}/
#
# operationId: circuits_providers_read
export def "circuits-providers get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<account: string, asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/providers/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /circuits/providers/{id}/
#
# operationId: circuits_providers_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-providers update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --account: string
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<account: string, asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/providers/{id}/"))
  let req_body = {"account": $account, "asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /circuits/providers/{id}/
#
# operationId: circuits_providers_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "circuits-providers update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --account: string
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<account: string, asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/circuits/providers/{id}/"))
  let req_body = {"account": $account, "asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/cable-terminations/
#
# operationId: dcim_cable-terminations_bulk_delete
export def "dcim-cable-terminations delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cable-terminations/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/cable-terminations/
#
# operationId: dcim_cable-terminations_list
export def "dcim-cable-terminations list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --cable: string
  --cable-end: string
  --termination-type: string
  --termination-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --cable-n: string
  --cable-end-n: string
  --termination-type-n: string
  --termination-id-n: string
  --termination-id-lte: string
  --termination-id-lt: string
  --termination-id-gte: string
  --termination-id-gt: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<cable: int, cable_end: string, display: string, id: int, termination: record, termination_id: int, termination_type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "cable" $cable "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "termination_type" $termination_type "scalar") (serialize-qp "termination_id" $termination_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "cable__n" $cable_n "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "termination_type__n" $termination_type_n "scalar") (serialize-qp "termination_id__n" $termination_id_n "scalar") (serialize-qp "termination_id__lte" $termination_id_lte "scalar") (serialize-qp "termination_id__lt" $termination_id_lt "scalar") (serialize-qp "termination_id__gte" $termination_id_gte "scalar") (serialize-qp "termination_id__gt" $termination_id_gt "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/cable-terminations/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "cable": $cable, "cable_end": $cable_end, "termination_type": $termination_type, "termination_id": $termination_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "cable__n": $cable_n, "cable_end__n": $cable_end_n, "termination_type__n": $termination_type_n, "termination_id__n": $termination_id_n, "termination_id__lte": $termination_id_lte, "termination_id__lt": $termination_id_lt, "termination_id__gte": $termination_id_gte, "termination_id__gt": $termination_id_gt, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/cable-terminations/
#
# operationId: dcim_cable-terminations_bulk_partial_update
export def "dcim-cable-terminations update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cable: int
  cable_end: string@cable-end-completer
  termination_id: int
  termination_type: string
]: any -> record<cable: int, cable_end: string, display: string, id: int, termination: record, termination_id: int, termination_type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cable-terminations/")
  let req_body = {"cable": $cable, "cable_end": $cable_end, "termination_id": $termination_id, "termination_type": $termination_type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/cable-terminations/
#
# operationId: dcim_cable-terminations_create
export def "dcim-cable-terminations create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cable: int
  cable_end: string@cable-end-completer
  termination_id: int
  termination_type: string
]: any -> record<cable: int, cable_end: string, display: string, id: int, termination: record, termination_id: int, termination_type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cable-terminations/")
  let req_body = {"cable": $cable, "cable_end": $cable_end, "termination_id": $termination_id, "termination_type": $termination_type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/cable-terminations/
#
# operationId: dcim_cable-terminations_bulk_update
export def "dcim-cable-terminations update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cable: int
  cable_end: string@cable-end-completer
  termination_id: int
  termination_type: string
]: any -> record<cable: int, cable_end: string, display: string, id: int, termination: record, termination_id: int, termination_type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cable-terminations/")
  let req_body = {"cable": $cable, "cable_end": $cable_end, "termination_id": $termination_id, "termination_type": $termination_type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/cable-terminations/{id}/
#
# operationId: dcim_cable-terminations_delete
export def "dcim-cable-terminations delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cable-terminations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/cable-terminations/{id}/
#
# operationId: dcim_cable-terminations_read
export def "dcim-cable-terminations get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cable: int, cable_end: string, display: string, id: int, termination: record, termination_id: int, termination_type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cable-terminations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/cable-terminations/{id}/
#
# operationId: dcim_cable-terminations_partial_update
export def "dcim-cable-terminations update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cable: int
  cable_end: string@cable-end-completer
  termination_id: int
  termination_type: string
]: any -> record<cable: int, cable_end: string, display: string, id: int, termination: record, termination_id: int, termination_type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cable-terminations/{id}/"))
  let req_body = {"cable": $cable, "cable_end": $cable_end, "termination_id": $termination_id, "termination_type": $termination_type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/cable-terminations/{id}/
#
# operationId: dcim_cable-terminations_update
export def "dcim-cable-terminations update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  cable: int
  cable_end: string@cable-end-completer
  termination_id: int
  termination_type: string
]: any -> record<cable: int, cable_end: string, display: string, id: int, termination: record, termination_id: int, termination_type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cable-terminations/{id}/"))
  let req_body = {"cable": $cable, "cable_end": $cable_end, "termination_id": $termination_id, "termination_type": $termination_type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/cables/
#
# operationId: dcim_cables_bulk_delete
export def "dcim-cables delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cables/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/cables/
#
# operationId: dcim_cables_list
export def "dcim-cables list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --label: string
  --length: string
  --length-unit: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --termination-a-type: string
  --termination-a-id: string
  --termination-b-type: string
  --termination-b-id: string
  --type: string
  --status: string
  --color: string
  --device-id: string
  --device: string
  --rack-id: string
  --rack: string
  --location-id: string
  --location: string
  --site-id: string
  --site: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --length-n: string
  --length-lte: string
  --length-lt: string
  --length-gte: string
  --length-gt: string
  --length-unit-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --termination-a-type-n: string
  --termination-a-id-n: string
  --termination-a-id-lte: string
  --termination-a-id-lt: string
  --termination-a-id-gte: string
  --termination-a-id-gt: string
  --termination-b-type-n: string
  --termination-b-id-n: string
  --termination-b-id-lte: string
  --termination-b-id-lt: string
  --termination-b-id-gte: string
  --termination-b-id-gt: string
  --type-n: string
  --status-n: string
  --color-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<a_terminations: list, b_terminations: list, color: string, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, label: string, last_updated: string, length: float, length_unit: record, status: record, tags: list, tenant: record, type: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "length" $length "scalar") (serialize-qp "length_unit" $length_unit "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "termination_a_type" $termination_a_type "scalar") (serialize-qp "termination_a_id" $termination_a_id "scalar") (serialize-qp "termination_b_type" $termination_b_type "scalar") (serialize-qp "termination_b_id" $termination_b_id "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "length__n" $length_n "scalar") (serialize-qp "length__lte" $length_lte "scalar") (serialize-qp "length__lt" $length_lt "scalar") (serialize-qp "length__gte" $length_gte "scalar") (serialize-qp "length__gt" $length_gt "scalar") (serialize-qp "length_unit__n" $length_unit_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "termination_a_type__n" $termination_a_type_n "scalar") (serialize-qp "termination_a_id__n" $termination_a_id_n "scalar") (serialize-qp "termination_a_id__lte" $termination_a_id_lte "scalar") (serialize-qp "termination_a_id__lt" $termination_a_id_lt "scalar") (serialize-qp "termination_a_id__gte" $termination_a_id_gte "scalar") (serialize-qp "termination_a_id__gt" $termination_a_id_gt "scalar") (serialize-qp "termination_b_type__n" $termination_b_type_n "scalar") (serialize-qp "termination_b_id__n" $termination_b_id_n "scalar") (serialize-qp "termination_b_id__lte" $termination_b_id_lte "scalar") (serialize-qp "termination_b_id__lt" $termination_b_id_lt "scalar") (serialize-qp "termination_b_id__gte" $termination_b_id_gte "scalar") (serialize-qp "termination_b_id__gt" $termination_b_id_gt "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/cables/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "label": $label, "length": $length, "length_unit": $length_unit, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "termination_a_type": $termination_a_type, "termination_a_id": $termination_a_id, "termination_b_type": $termination_b_type, "termination_b_id": $termination_b_id, "type": $type, "status": $status, "color": $color, "device_id": $device_id, "device": $device, "rack_id": $rack_id, "rack": $rack, "location_id": $location_id, "location": $location, "site_id": $site_id, "site": $site, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "length__n": $length_n, "length__lte": $length_lte, "length__lt": $length_lt, "length__gte": $length_gte, "length__gt": $length_gt, "length_unit__n": $length_unit_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "termination_a_type__n": $termination_a_type_n, "termination_a_id__n": $termination_a_id_n, "termination_a_id__lte": $termination_a_id_lte, "termination_a_id__lt": $termination_a_id_lt, "termination_a_id__gte": $termination_a_id_gte, "termination_a_id__gt": $termination_a_id_gt, "termination_b_type__n": $termination_b_type_n, "termination_b_id__n": $termination_b_id_n, "termination_b_id__lte": $termination_b_id_lte, "termination_b_id__lt": $termination_b_id_lt, "termination_b_id__gte": $termination_b_id_gte, "termination_b_id__gt": $termination_b_id_gt, "type__n": $type_n, "status__n": $status_n, "color__n": $color_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/cables/
#
# operationId: dcim_cables_bulk_partial_update
# --a_terminations item shape: {object_id: int, object_type: string}
# --b_terminations item shape: {object_id: int, object_type: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-cables update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --a-terminations: list # item shape: {object_id: int, object_type: string}
  --b-terminations: list # item shape: {object_id: int, object_type: string}
  --color: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --label: string
  --length: float # nullable, format: decimal
  --length-unit: string@length-unit-completer
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer
]: any -> record<a_terminations: table<object: record, object_id: int, object_type: string>, b_terminations: table<object: record, object_id: int, object_type: string>, color: string, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, label: string, last_updated: string, length: float, length_unit: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cables/")
  let req_body = {"a_terminations": $a_terminations, "b_terminations": $b_terminations, "color": $color, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "label": $label, "length": $length, "length_unit": $length_unit, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/cables/
#
# operationId: dcim_cables_create
# --a_terminations item shape: {object_id: int, object_type: string}
# --b_terminations item shape: {object_id: int, object_type: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-cables create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --a-terminations: list # item shape: {object_id: int, object_type: string}
  --b-terminations: list # item shape: {object_id: int, object_type: string}
  --color: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --label: string
  --length: float # nullable, format: decimal
  --length-unit: string@length-unit-completer
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer
]: any -> record<a_terminations: table<object: record, object_id: int, object_type: string>, b_terminations: table<object: record, object_id: int, object_type: string>, color: string, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, label: string, last_updated: string, length: float, length_unit: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cables/")
  let req_body = {"a_terminations": $a_terminations, "b_terminations": $b_terminations, "color": $color, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "label": $label, "length": $length, "length_unit": $length_unit, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/cables/
#
# operationId: dcim_cables_bulk_update
# --a_terminations item shape: {object_id: int, object_type: string}
# --b_terminations item shape: {object_id: int, object_type: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-cables update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --a-terminations: list # item shape: {object_id: int, object_type: string}
  --b-terminations: list # item shape: {object_id: int, object_type: string}
  --color: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --label: string
  --length: float # nullable, format: decimal
  --length-unit: string@length-unit-completer
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer
]: any -> record<a_terminations: table<object: record, object_id: int, object_type: string>, b_terminations: table<object: record, object_id: int, object_type: string>, color: string, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, label: string, last_updated: string, length: float, length_unit: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/cables/")
  let req_body = {"a_terminations": $a_terminations, "b_terminations": $b_terminations, "color": $color, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "label": $label, "length": $length, "length_unit": $length_unit, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/cables/{id}/
#
# operationId: dcim_cables_delete
export def "dcim-cables delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cables/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/cables/{id}/
#
# operationId: dcim_cables_read
export def "dcim-cables get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<a_terminations: table<object: record, object_id: int, object_type: string>, b_terminations: table<object: record, object_id: int, object_type: string>, color: string, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, label: string, last_updated: string, length: float, length_unit: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cables/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/cables/{id}/
#
# operationId: dcim_cables_partial_update
# --a_terminations item shape: {object_id: int, object_type: string}
# --b_terminations item shape: {object_id: int, object_type: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-cables update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --a-terminations: list # item shape: {object_id: int, object_type: string}
  --b-terminations: list # item shape: {object_id: int, object_type: string}
  --color: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --label: string
  --length: float # nullable, format: decimal
  --length-unit: string@length-unit-completer
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer
]: any -> record<a_terminations: table<object: record, object_id: int, object_type: string>, b_terminations: table<object: record, object_id: int, object_type: string>, color: string, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, label: string, last_updated: string, length: float, length_unit: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cables/{id}/"))
  let req_body = {"a_terminations": $a_terminations, "b_terminations": $b_terminations, "color": $color, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "label": $label, "length": $length, "length_unit": $length_unit, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/cables/{id}/
#
# operationId: dcim_cables_update
# --a_terminations item shape: {object_id: int, object_type: string}
# --b_terminations item shape: {object_id: int, object_type: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-cables update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --a-terminations: list # item shape: {object_id: int, object_type: string}
  --b-terminations: list # item shape: {object_id: int, object_type: string}
  --color: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --label: string
  --length: float # nullable, format: decimal
  --length-unit: string@length-unit-completer
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer
]: any -> record<a_terminations: table<object: record, object_id: int, object_type: string>, b_terminations: table<object: record, object_id: int, object_type: string>, color: string, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, label: string, last_updated: string, length: float, length_unit: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/cables/{id}/"))
  let req_body = {"a_terminations": $a_terminations, "b_terminations": $b_terminations, "color": $color, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "label": $label, "length": $length, "length_unit": $length_unit, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# This endpoint allows a user to determine what device (if any) is connected to a given peer device and peer interface. This is useful in a situation where a device boots with no configuration, but can detect its neighbors via a protocol such as LLDP. Two query parameters must be included in the request: * `peer_device`: The name of the peer device * `peer_interface`: The name of the peer interface
#
# GET /dcim/connected-device/
# operationId: dcim_connected-device_list
export def "dcim-connected-device list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --peer-device: string # The name of the peer device
  --peer-interface: string # The name of the peer interface
]: nothing -> record<airflow: record<label: string, value: string>, asset_tag: string, cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, created: string, custom_fields: record, description: string, device_role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, face: record<label: string, value: string>, id: int, last_updated: string, local_context_data: record, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, parent_device: record<display: string, id: int, name: string, url: string>, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, position: float, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vc_position: int, vc_priority: int, virtual_chassis: record<display: string, id: int, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "peer_device" $peer_device "scalar") (serialize-qp "peer_interface" $peer_interface "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/connected-device/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"peer_device": $peer_device, "peer_interface": $peer_interface} | compact), body: null}
}

# DELETE /dcim/console-port-templates/
#
# operationId: dcim_console-port-templates_bulk_delete
export def "dcim-console-port-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-port-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-port-templates/
#
# operationId: dcim_console-port-templates_list
export def "dcim-console-port-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --moduletype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --type-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --moduletype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, module_type: record, name: string, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "moduletype_id" $moduletype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "moduletype_id__n" $moduletype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/console-port-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type": $type, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "moduletype_id": $moduletype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "type__n": $type_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "moduletype_id__n": $moduletype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/console-port-templates/
#
# operationId: dcim_console-port-templates_bulk_partial_update
export def "dcim-console-port-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-port-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/console-port-templates/
#
# operationId: dcim_console-port-templates_create
export def "dcim-console-port-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-port-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-port-templates/
#
# operationId: dcim_console-port-templates_bulk_update
export def "dcim-console-port-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-port-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/console-port-templates/{id}/
#
# operationId: dcim_console-port-templates_delete
export def "dcim-console-port-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-port-templates/{id}/
#
# operationId: dcim_console-port-templates_read
export def "dcim-console-port-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/console-port-templates/{id}/
#
# operationId: dcim_console-port-templates_partial_update
export def "dcim-console-port-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-port-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-port-templates/{id}/
#
# operationId: dcim_console-port-templates_update
export def "dcim-console-port-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-port-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/console-ports/
#
# operationId: dcim_console-ports_bulk_delete
export def "dcim-console-ports delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-ports/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-ports/
#
# operationId: dcim_console-ports_list
export def "dcim-console-ports list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --description: string
  --cable-end: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --module-id: string
  --created: string
  --last-updated: string
  --tag: string
  --cabled: string
  --occupied: string
  --connected: string
  --type: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --module-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, cable: record, cable_end: string, connected_endpoints: list, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, label: string, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, module: record, name: string, speed: record, tags: list, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "module_id" $module_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "connected" $connected "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "module_id__n" $module_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/console-ports/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "description": $description, "cable_end": $cable_end, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "module_id": $module_id, "created": $created, "last_updated": $last_updated, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "connected": $connected, "type": $type, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "module_id__n": $module_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "type__n": $type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/console-ports/
#
# operationId: dcim_console-ports_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-ports update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-ports/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/console-ports/
#
# operationId: dcim_console-ports_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-ports create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-ports/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-ports/
#
# operationId: dcim_console-ports_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-ports update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-ports/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/console-ports/{id}/
#
# operationId: dcim_console-ports_delete
export def "dcim-console-ports delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-ports/{id}/
#
# operationId: dcim_console-ports_read
export def "dcim-console-ports get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/console-ports/{id}/
#
# operationId: dcim_console-ports_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-ports update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-ports/{id}/"))
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-ports/{id}/
#
# operationId: dcim_console-ports_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-ports update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-ports/{id}/"))
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Trace a complete cable path and return each segment as a three-tuple of (termination, cable, termination).
#
# GET /dcim/console-ports/{id}/trace/
# operationId: dcim_console-ports_trace
export def "dcim-console-ports-trace get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-ports/{id}/trace/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/console-server-port-templates/
#
# operationId: dcim_console-server-port-templates_bulk_delete
export def "dcim-console-server-port-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-port-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-server-port-templates/
#
# operationId: dcim_console-server-port-templates_list
export def "dcim-console-server-port-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --moduletype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --type-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --moduletype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, module_type: record, name: string, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "moduletype_id" $moduletype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "moduletype_id__n" $moduletype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/console-server-port-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type": $type, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "moduletype_id": $moduletype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "type__n": $type_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "moduletype_id__n": $moduletype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/console-server-port-templates/
#
# operationId: dcim_console-server-port-templates_bulk_partial_update
export def "dcim-console-server-port-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-port-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/console-server-port-templates/
#
# operationId: dcim_console-server-port-templates_create
export def "dcim-console-server-port-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-port-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-server-port-templates/
#
# operationId: dcim_console-server-port-templates_bulk_update
export def "dcim-console-server-port-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-port-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/console-server-port-templates/{id}/
#
# operationId: dcim_console-server-port-templates_delete
export def "dcim-console-server-port-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-server-port-templates/{id}/
#
# operationId: dcim_console-server-port-templates_read
export def "dcim-console-server-port-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/console-server-port-templates/{id}/
#
# operationId: dcim_console-server-port-templates_partial_update
export def "dcim-console-server-port-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-port-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-server-port-templates/{id}/
#
# operationId: dcim_console-server-port-templates_update
export def "dcim-console-server-port-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-1
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-port-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/console-server-ports/
#
# operationId: dcim_console-server-ports_bulk_delete
export def "dcim-console-server-ports delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-ports/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-server-ports/
#
# operationId: dcim_console-server-ports_list
export def "dcim-console-server-ports list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --description: string
  --cable-end: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --module-id: string
  --created: string
  --last-updated: string
  --tag: string
  --cabled: string
  --occupied: string
  --connected: string
  --type: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --module-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, cable: record, cable_end: string, connected_endpoints: list, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, label: string, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, module: record, name: string, speed: record, tags: list, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "module_id" $module_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "connected" $connected "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "module_id__n" $module_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/console-server-ports/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "description": $description, "cable_end": $cable_end, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "module_id": $module_id, "created": $created, "last_updated": $last_updated, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "connected": $connected, "type": $type, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "module_id__n": $module_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "type__n": $type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/console-server-ports/
#
# operationId: dcim_console-server-ports_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-server-ports update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-ports/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/console-server-ports/
#
# operationId: dcim_console-server-ports_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-server-ports create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-ports/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-server-ports/
#
# operationId: dcim_console-server-ports_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-server-ports update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/console-server-ports/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/console-server-ports/{id}/
#
# operationId: dcim_console-server-ports_delete
export def "dcim-console-server-ports delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/console-server-ports/{id}/
#
# operationId: dcim_console-server-ports_read
export def "dcim-console-server-ports get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/console-server-ports/{id}/
#
# operationId: dcim_console-server-ports_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-server-ports update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-ports/{id}/"))
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/console-server-ports/{id}/
#
# operationId: dcim_console-server-ports_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-console-server-ports update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --speed: int@speed-completer # Port speed in bits per second (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-1 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-ports/{id}/"))
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "speed": $speed, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Trace a complete cable path and return each segment as a three-tuple of (termination, cable, termination).
#
# GET /dcim/console-server-ports/{id}/trace/
# operationId: dcim_console-server-ports_trace
export def "dcim-console-server-ports-trace get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, speed: record<label: string, value: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/console-server-ports/{id}/trace/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/device-bay-templates/
#
# operationId: dcim_device-bay-templates_bulk_delete
export def "dcim-device-bay-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bay-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-bay-templates/
#
# operationId: dcim_device-bay-templates_list
export def "dcim-device-bay-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, name: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/device-bay-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/device-bay-templates/
#
# operationId: dcim_device-bay-templates_bulk_partial_update
export def "dcim-device-bay-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bay-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/device-bay-templates/
#
# operationId: dcim_device-bay-templates_create
export def "dcim-device-bay-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bay-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-bay-templates/
#
# operationId: dcim_device-bay-templates_bulk_update
export def "dcim-device-bay-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bay-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/device-bay-templates/{id}/
#
# operationId: dcim_device-bay-templates_delete
export def "dcim-device-bay-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bay-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-bay-templates/{id}/
#
# operationId: dcim_device-bay-templates_read
export def "dcim-device-bay-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bay-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/device-bay-templates/{id}/
#
# operationId: dcim_device-bay-templates_partial_update
export def "dcim-device-bay-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bay-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-bay-templates/{id}/
#
# operationId: dcim_device-bay-templates_update
export def "dcim-device-bay-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bay-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/device-bays/
#
# operationId: dcim_device-bays_bulk_delete
export def "dcim-device-bays delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bays/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-bays/
#
# operationId: dcim_device-bays_list
export def "dcim-device-bays list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --description: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --created: string
  --last-updated: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, custom_fields: record, description: string, device: record, display: string, id: int, installed_device: record, label: string, last_updated: string, name: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/device-bays/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "description": $description, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "created": $created, "last_updated": $last_updated, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/device-bays/
#
# operationId: dcim_device-bays_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-bays update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  --installed-device: int # nullable
  --label: string # Physical label
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_device: record<display: string, id: int, name: string, url: string>, label: string, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bays/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_device": $installed_device, "label": $label, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/device-bays/
#
# operationId: dcim_device-bays_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-bays create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  --installed-device: int # nullable
  --label: string # Physical label
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_device: record<display: string, id: int, name: string, url: string>, label: string, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bays/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_device": $installed_device, "label": $label, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-bays/
#
# operationId: dcim_device-bays_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-bays update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  --installed-device: int # nullable
  --label: string # Physical label
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_device: record<display: string, id: int, name: string, url: string>, label: string, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-bays/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_device": $installed_device, "label": $label, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/device-bays/{id}/
#
# operationId: dcim_device-bays_delete
export def "dcim-device-bays delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bays/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-bays/{id}/
#
# operationId: dcim_device-bays_read
export def "dcim-device-bays get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_device: record<display: string, id: int, name: string, url: string>, label: string, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bays/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/device-bays/{id}/
#
# operationId: dcim_device-bays_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-bays update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  --installed-device: int # nullable
  --label: string # Physical label
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_device: record<display: string, id: int, name: string, url: string>, label: string, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bays/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_device": $installed_device, "label": $label, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-bays/{id}/
#
# operationId: dcim_device-bays_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-bays update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  --installed-device: int # nullable
  --label: string # Physical label
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_device: record<display: string, id: int, name: string, url: string>, label: string, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-bays/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_device": $installed_device, "label": $label, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/device-roles/
#
# operationId: dcim_device-roles_bulk_delete
export def "dcim-device-roles delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-roles/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-roles/
#
# operationId: dcim_device-roles_list
export def "dcim-device-roles list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --color: string
  --vm-role: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<color: string, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, slug: string, tags: list, url: string, virtualmachine_count: int, vm_role: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "vm_role" $vm_role "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/device-roles/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "color": $color, "vm_role": $vm_role, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/device-roles/
#
# operationId: dcim_device-roles_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-roles update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --vm-role: oneof<nothing, bool> # Virtual machines may be assigned to this role
]: any -> record<color: string, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vm_role: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "vm_role": $vm_role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/device-roles/
#
# operationId: dcim_device-roles_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-roles create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --vm-role: oneof<nothing, bool> # Virtual machines may be assigned to this role
]: any -> record<color: string, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vm_role: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "vm_role": $vm_role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-roles/
#
# operationId: dcim_device-roles_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-roles update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --vm-role: oneof<nothing, bool> # Virtual machines may be assigned to this role
]: any -> record<color: string, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vm_role: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "vm_role": $vm_role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/device-roles/{id}/
#
# operationId: dcim_device-roles_delete
export def "dcim-device-roles delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-roles/{id}/
#
# operationId: dcim_device-roles_read
export def "dcim-device-roles get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<color: string, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vm_role: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/device-roles/{id}/
#
# operationId: dcim_device-roles_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-roles update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --vm-role: oneof<nothing, bool> # Virtual machines may be assigned to this role
]: any -> record<color: string, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vm_role: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-roles/{id}/"))
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "vm_role": $vm_role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-roles/{id}/
#
# operationId: dcim_device-roles_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-roles update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --vm-role: oneof<nothing, bool> # Virtual machines may be assigned to this role
]: any -> record<color: string, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vm_role: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-roles/{id}/"))
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "vm_role": $vm_role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/device-types/
#
# operationId: dcim_device-types_bulk_delete
export def "dcim-device-types delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-types/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-types/
#
# operationId: dcim_device-types_list
export def "dcim-device-types list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --model: string
  --slug: string
  --part-number: string
  --u-height: string
  --is-full-depth: string
  --subdevice-role: string
  --airflow: string
  --weight: string
  --weight-unit: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --manufacturer-id: string
  --manufacturer: string
  --has-front-image: string
  --has-rear-image: string
  --console-ports: string
  --console-server-ports: string
  --power-ports: string
  --power-outlets: string
  --interfaces: string
  --pass-through-ports: string
  --module-bays: string
  --device-bays: string
  --inventory-items: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --model-n: string
  --model-ic: string
  --model-nic: string
  --model-iew: string
  --model-niew: string
  --model-isw: string
  --model-nisw: string
  --model-ie: string
  --model-nie: string
  --model-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --part-number-n: string
  --part-number-ic: string
  --part-number-nic: string
  --part-number-iew: string
  --part-number-niew: string
  --part-number-isw: string
  --part-number-nisw: string
  --part-number-ie: string
  --part-number-nie: string
  --part-number-empty: string
  --u-height-n: string
  --u-height-lte: string
  --u-height-lt: string
  --u-height-gte: string
  --u-height-gt: string
  --subdevice-role-n: string
  --airflow-n: string
  --weight-n: string
  --weight-lte: string
  --weight-lt: string
  --weight-gte: string
  --weight-gt: string
  --weight-unit-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --manufacturer-id-n: string
  --manufacturer-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<airflow: record, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, front_image: string, id: int, is_full_depth: bool, last_updated: string, manufacturer: record, model: string, part_number: string, rear_image: string, slug: string, subdevice_role: record, tags: list, u_height: float, url: string, weight: float, weight_unit: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "model" $model "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "part_number" $part_number "scalar") (serialize-qp "u_height" $u_height "scalar") (serialize-qp "is_full_depth" $is_full_depth "scalar") (serialize-qp "subdevice_role" $subdevice_role "scalar") (serialize-qp "airflow" $airflow "scalar") (serialize-qp "weight" $weight "scalar") (serialize-qp "weight_unit" $weight_unit "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "manufacturer_id" $manufacturer_id "scalar") (serialize-qp "manufacturer" $manufacturer "scalar") (serialize-qp "has_front_image" $has_front_image "scalar") (serialize-qp "has_rear_image" $has_rear_image "scalar") (serialize-qp "console_ports" $console_ports "scalar") (serialize-qp "console_server_ports" $console_server_ports "scalar") (serialize-qp "power_ports" $power_ports "scalar") (serialize-qp "power_outlets" $power_outlets "scalar") (serialize-qp "interfaces" $interfaces "scalar") (serialize-qp "pass_through_ports" $pass_through_ports "scalar") (serialize-qp "module_bays" $module_bays "scalar") (serialize-qp "device_bays" $device_bays "scalar") (serialize-qp "inventory_items" $inventory_items "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "model__n" $model_n "scalar") (serialize-qp "model__ic" $model_ic "scalar") (serialize-qp "model__nic" $model_nic "scalar") (serialize-qp "model__iew" $model_iew "scalar") (serialize-qp "model__niew" $model_niew "scalar") (serialize-qp "model__isw" $model_isw "scalar") (serialize-qp "model__nisw" $model_nisw "scalar") (serialize-qp "model__ie" $model_ie "scalar") (serialize-qp "model__nie" $model_nie "scalar") (serialize-qp "model__empty" $model_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "part_number__n" $part_number_n "scalar") (serialize-qp "part_number__ic" $part_number_ic "scalar") (serialize-qp "part_number__nic" $part_number_nic "scalar") (serialize-qp "part_number__iew" $part_number_iew "scalar") (serialize-qp "part_number__niew" $part_number_niew "scalar") (serialize-qp "part_number__isw" $part_number_isw "scalar") (serialize-qp "part_number__nisw" $part_number_nisw "scalar") (serialize-qp "part_number__ie" $part_number_ie "scalar") (serialize-qp "part_number__nie" $part_number_nie "scalar") (serialize-qp "part_number__empty" $part_number_empty "scalar") (serialize-qp "u_height__n" $u_height_n "scalar") (serialize-qp "u_height__lte" $u_height_lte "scalar") (serialize-qp "u_height__lt" $u_height_lt "scalar") (serialize-qp "u_height__gte" $u_height_gte "scalar") (serialize-qp "u_height__gt" $u_height_gt "scalar") (serialize-qp "subdevice_role__n" $subdevice_role_n "scalar") (serialize-qp "airflow__n" $airflow_n "scalar") (serialize-qp "weight__n" $weight_n "scalar") (serialize-qp "weight__lte" $weight_lte "scalar") (serialize-qp "weight__lt" $weight_lt "scalar") (serialize-qp "weight__gte" $weight_gte "scalar") (serialize-qp "weight__gt" $weight_gt "scalar") (serialize-qp "weight_unit__n" $weight_unit_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "manufacturer_id__n" $manufacturer_id_n "scalar") (serialize-qp "manufacturer__n" $manufacturer_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/device-types/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "model": $model, "slug": $slug, "part_number": $part_number, "u_height": $u_height, "is_full_depth": $is_full_depth, "subdevice_role": $subdevice_role, "airflow": $airflow, "weight": $weight, "weight_unit": $weight_unit, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "manufacturer_id": $manufacturer_id, "manufacturer": $manufacturer, "has_front_image": $has_front_image, "has_rear_image": $has_rear_image, "console_ports": $console_ports, "console_server_ports": $console_server_ports, "power_ports": $power_ports, "power_outlets": $power_outlets, "interfaces": $interfaces, "pass_through_ports": $pass_through_ports, "module_bays": $module_bays, "device_bays": $device_bays, "inventory_items": $inventory_items, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "model__n": $model_n, "model__ic": $model_ic, "model__nic": $model_nic, "model__iew": $model_iew, "model__niew": $model_niew, "model__isw": $model_isw, "model__nisw": $model_nisw, "model__ie": $model_ie, "model__nie": $model_nie, "model__empty": $model_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "part_number__n": $part_number_n, "part_number__ic": $part_number_ic, "part_number__nic": $part_number_nic, "part_number__iew": $part_number_iew, "part_number__niew": $part_number_niew, "part_number__isw": $part_number_isw, "part_number__nisw": $part_number_nisw, "part_number__ie": $part_number_ie, "part_number__nie": $part_number_nie, "part_number__empty": $part_number_empty, "u_height__n": $u_height_n, "u_height__lte": $u_height_lte, "u_height__lt": $u_height_lt, "u_height__gte": $u_height_gte, "u_height__gt": $u_height_gt, "subdevice_role__n": $subdevice_role_n, "airflow__n": $airflow_n, "weight__n": $weight_n, "weight__lte": $weight_lte, "weight__lt": $weight_lt, "weight__gte": $weight_gte, "weight__gt": $weight_gt, "weight_unit__n": $weight_unit_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "manufacturer_id__n": $manufacturer_id_n, "manufacturer__n": $manufacturer_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/device-types/
#
# operationId: dcim_device-types_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-types update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-full-depth: oneof<nothing, bool> # Device consumes both front and rear rack faces
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  slug: string # format: slug
  --subdevice-role: string@subdevice-role-completer # Parent devices house child devices in device bays. Leave blank if this device type is neither a parent nor a child.
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --u-height: float # format: decimal, default: 1
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<airflow: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, front_image: string, id: int, is_full_depth: bool, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, rear_image: string, slug: string, subdevice_role: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, u_height: float, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-types/")
  let req_body = {"airflow": $airflow, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_full_depth": $is_full_depth, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "slug": $slug, "subdevice_role": $subdevice_role, "tags": $tags, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/device-types/
#
# operationId: dcim_device-types_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-types create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-full-depth: oneof<nothing, bool> # Device consumes both front and rear rack faces
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  slug: string # format: slug
  --subdevice-role: string@subdevice-role-completer # Parent devices house child devices in device bays. Leave blank if this device type is neither a parent nor a child.
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --u-height: float # format: decimal, default: 1
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<airflow: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, front_image: string, id: int, is_full_depth: bool, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, rear_image: string, slug: string, subdevice_role: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, u_height: float, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-types/")
  let req_body = {"airflow": $airflow, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_full_depth": $is_full_depth, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "slug": $slug, "subdevice_role": $subdevice_role, "tags": $tags, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-types/
#
# operationId: dcim_device-types_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-types update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-full-depth: oneof<nothing, bool> # Device consumes both front and rear rack faces
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  slug: string # format: slug
  --subdevice-role: string@subdevice-role-completer # Parent devices house child devices in device bays. Leave blank if this device type is neither a parent nor a child.
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --u-height: float # format: decimal, default: 1
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<airflow: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, front_image: string, id: int, is_full_depth: bool, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, rear_image: string, slug: string, subdevice_role: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, u_height: float, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/device-types/")
  let req_body = {"airflow": $airflow, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_full_depth": $is_full_depth, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "slug": $slug, "subdevice_role": $subdevice_role, "tags": $tags, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/device-types/{id}/
#
# operationId: dcim_device-types_delete
export def "dcim-device-types delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/device-types/{id}/
#
# operationId: dcim_device-types_read
export def "dcim-device-types get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<airflow: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, front_image: string, id: int, is_full_depth: bool, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, rear_image: string, slug: string, subdevice_role: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, u_height: float, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/device-types/{id}/
#
# operationId: dcim_device-types_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-types update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-full-depth: oneof<nothing, bool> # Device consumes both front and rear rack faces
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  slug: string # format: slug
  --subdevice-role: string@subdevice-role-completer # Parent devices house child devices in device bays. Leave blank if this device type is neither a parent nor a child.
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --u-height: float # format: decimal, default: 1
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<airflow: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, front_image: string, id: int, is_full_depth: bool, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, rear_image: string, slug: string, subdevice_role: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, u_height: float, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-types/{id}/"))
  let req_body = {"airflow": $airflow, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_full_depth": $is_full_depth, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "slug": $slug, "subdevice_role": $subdevice_role, "tags": $tags, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/device-types/{id}/
#
# operationId: dcim_device-types_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-device-types update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-full-depth: oneof<nothing, bool> # Device consumes both front and rear rack faces
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  slug: string # format: slug
  --subdevice-role: string@subdevice-role-completer # Parent devices house child devices in device bays. Leave blank if this device type is neither a parent nor a child.
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --u-height: float # format: decimal, default: 1
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<airflow: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, front_image: string, id: int, is_full_depth: bool, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, rear_image: string, slug: string, subdevice_role: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, u_height: float, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/device-types/{id}/"))
  let req_body = {"airflow": $airflow, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_full_depth": $is_full_depth, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "slug": $slug, "subdevice_role": $subdevice_role, "tags": $tags, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/devices/
#
# operationId: dcim_devices_bulk_delete
export def "dcim-devices delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/devices/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/devices/
#
# operationId: dcim_devices_list
export def "dcim-devices list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --asset-tag: string
  --face: string
  --position: string
  --airflow: string
  --vc-position: string
  --vc-priority: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --local-context-data: string
  --manufacturer-id: string
  --manufacturer: string
  --device-type: string
  --device-type-id: string
  --role-id: string
  --role: string
  --parent-device-id: string
  --platform-id: string
  --platform: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --rack-id: string
  --cluster-id: string
  --model: string
  --name: string
  --status: string
  --is-full-depth: string
  --mac-address: string
  --serial: string
  --has-primary-ip: string
  --virtual-chassis-id: string
  --virtual-chassis-member: string
  --console-ports: string
  --console-server-ports: string
  --power-ports: string
  --power-outlets: string
  --interfaces: string
  --pass-through-ports: string
  --module-bays: string
  --device-bays: string
  --primary-ip4-id: string
  --primary-ip6-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --asset-tag-n: string
  --asset-tag-ic: string
  --asset-tag-nic: string
  --asset-tag-iew: string
  --asset-tag-niew: string
  --asset-tag-isw: string
  --asset-tag-nisw: string
  --asset-tag-ie: string
  --asset-tag-nie: string
  --asset-tag-empty: string
  --face-n: string
  --position-n: string
  --position-lte: string
  --position-lt: string
  --position-gte: string
  --position-gt: string
  --airflow-n: string
  --vc-position-n: string
  --vc-position-lte: string
  --vc-position-lt: string
  --vc-position-gte: string
  --vc-position-gt: string
  --vc-priority-n: string
  --vc-priority-lte: string
  --vc-priority-lt: string
  --vc-priority-gte: string
  --vc-priority-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --manufacturer-id-n: string
  --manufacturer-n: string
  --device-type-n: string
  --device-type-id-n: string
  --role-id-n: string
  --role-n: string
  --parent-device-id-n: string
  --platform-id-n: string
  --platform-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --rack-id-n: string
  --cluster-id-n: string
  --model-n: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --status-n: string
  --mac-address-n: string
  --mac-address-ic: string
  --mac-address-nic: string
  --mac-address-iew: string
  --mac-address-niew: string
  --mac-address-isw: string
  --mac-address-nisw: string
  --mac-address-ie: string
  --mac-address-nie: string
  --serial-n: string
  --serial-ic: string
  --serial-nic: string
  --serial-iew: string
  --serial-niew: string
  --serial-isw: string
  --serial-nisw: string
  --serial-ie: string
  --serial-nie: string
  --serial-empty: string
  --virtual-chassis-id-n: string
  --primary-ip4-id-n: string
  --primary-ip6-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<airflow: record, asset_tag: string, cluster: record, comments: string, config_context: record, created: string, custom_fields: record, description: string, device_role: record, device_type: record, display: string, face: record, id: int, last_updated: string, local_context_data: record, location: record, name: string, parent_device: record, platform: record, position: float, primary_ip: record, primary_ip4: record, primary_ip6: record, rack: record, serial: string, site: record, status: record, tags: list, tenant: record, url: string, vc_position: int, vc_priority: int, virtual_chassis: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "asset_tag" $asset_tag "scalar") (serialize-qp "face" $face "scalar") (serialize-qp "position" $position "scalar") (serialize-qp "airflow" $airflow "scalar") (serialize-qp "vc_position" $vc_position "scalar") (serialize-qp "vc_priority" $vc_priority "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "local_context_data" $local_context_data "scalar") (serialize-qp "manufacturer_id" $manufacturer_id "scalar") (serialize-qp "manufacturer" $manufacturer "scalar") (serialize-qp "device_type" $device_type "scalar") (serialize-qp "device_type_id" $device_type_id "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "parent_device_id" $parent_device_id "scalar") (serialize-qp "platform_id" $platform_id "scalar") (serialize-qp "platform" $platform "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "cluster_id" $cluster_id "scalar") (serialize-qp "model" $model "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "is_full_depth" $is_full_depth "scalar") (serialize-qp "mac_address" $mac_address "scalar") (serialize-qp "serial" $serial "scalar") (serialize-qp "has_primary_ip" $has_primary_ip "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis_member" $virtual_chassis_member "scalar") (serialize-qp "console_ports" $console_ports "scalar") (serialize-qp "console_server_ports" $console_server_ports "scalar") (serialize-qp "power_ports" $power_ports "scalar") (serialize-qp "power_outlets" $power_outlets "scalar") (serialize-qp "interfaces" $interfaces "scalar") (serialize-qp "pass_through_ports" $pass_through_ports "scalar") (serialize-qp "module_bays" $module_bays "scalar") (serialize-qp "device_bays" $device_bays "scalar") (serialize-qp "primary_ip4_id" $primary_ip4_id "scalar") (serialize-qp "primary_ip6_id" $primary_ip6_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "asset_tag__n" $asset_tag_n "scalar") (serialize-qp "asset_tag__ic" $asset_tag_ic "scalar") (serialize-qp "asset_tag__nic" $asset_tag_nic "scalar") (serialize-qp "asset_tag__iew" $asset_tag_iew "scalar") (serialize-qp "asset_tag__niew" $asset_tag_niew "scalar") (serialize-qp "asset_tag__isw" $asset_tag_isw "scalar") (serialize-qp "asset_tag__nisw" $asset_tag_nisw "scalar") (serialize-qp "asset_tag__ie" $asset_tag_ie "scalar") (serialize-qp "asset_tag__nie" $asset_tag_nie "scalar") (serialize-qp "asset_tag__empty" $asset_tag_empty "scalar") (serialize-qp "face__n" $face_n "scalar") (serialize-qp "position__n" $position_n "scalar") (serialize-qp "position__lte" $position_lte "scalar") (serialize-qp "position__lt" $position_lt "scalar") (serialize-qp "position__gte" $position_gte "scalar") (serialize-qp "position__gt" $position_gt "scalar") (serialize-qp "airflow__n" $airflow_n "scalar") (serialize-qp "vc_position__n" $vc_position_n "scalar") (serialize-qp "vc_position__lte" $vc_position_lte "scalar") (serialize-qp "vc_position__lt" $vc_position_lt "scalar") (serialize-qp "vc_position__gte" $vc_position_gte "scalar") (serialize-qp "vc_position__gt" $vc_position_gt "scalar") (serialize-qp "vc_priority__n" $vc_priority_n "scalar") (serialize-qp "vc_priority__lte" $vc_priority_lte "scalar") (serialize-qp "vc_priority__lt" $vc_priority_lt "scalar") (serialize-qp "vc_priority__gte" $vc_priority_gte "scalar") (serialize-qp "vc_priority__gt" $vc_priority_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "manufacturer_id__n" $manufacturer_id_n "scalar") (serialize-qp "manufacturer__n" $manufacturer_n "scalar") (serialize-qp "device_type__n" $device_type_n "scalar") (serialize-qp "device_type_id__n" $device_type_id_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "parent_device_id__n" $parent_device_id_n "scalar") (serialize-qp "platform_id__n" $platform_id_n "scalar") (serialize-qp "platform__n" $platform_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "cluster_id__n" $cluster_id_n "scalar") (serialize-qp "model__n" $model_n "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "mac_address__n" $mac_address_n "scalar") (serialize-qp "mac_address__ic" $mac_address_ic "scalar") (serialize-qp "mac_address__nic" $mac_address_nic "scalar") (serialize-qp "mac_address__iew" $mac_address_iew "scalar") (serialize-qp "mac_address__niew" $mac_address_niew "scalar") (serialize-qp "mac_address__isw" $mac_address_isw "scalar") (serialize-qp "mac_address__nisw" $mac_address_nisw "scalar") (serialize-qp "mac_address__ie" $mac_address_ie "scalar") (serialize-qp "mac_address__nie" $mac_address_nie "scalar") (serialize-qp "serial__n" $serial_n "scalar") (serialize-qp "serial__ic" $serial_ic "scalar") (serialize-qp "serial__nic" $serial_nic "scalar") (serialize-qp "serial__iew" $serial_iew "scalar") (serialize-qp "serial__niew" $serial_niew "scalar") (serialize-qp "serial__isw" $serial_isw "scalar") (serialize-qp "serial__nisw" $serial_nisw "scalar") (serialize-qp "serial__ie" $serial_ie "scalar") (serialize-qp "serial__nie" $serial_nie "scalar") (serialize-qp "serial__empty" $serial_empty "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "primary_ip4_id__n" $primary_ip4_id_n "scalar") (serialize-qp "primary_ip6_id__n" $primary_ip6_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/devices/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "asset_tag": $asset_tag, "face": $face, "position": $position, "airflow": $airflow, "vc_position": $vc_position, "vc_priority": $vc_priority, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "local_context_data": $local_context_data, "manufacturer_id": $manufacturer_id, "manufacturer": $manufacturer, "device_type": $device_type, "device_type_id": $device_type_id, "role_id": $role_id, "role": $role, "parent_device_id": $parent_device_id, "platform_id": $platform_id, "platform": $platform, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "rack_id": $rack_id, "cluster_id": $cluster_id, "model": $model, "name": $name, "status": $status, "is_full_depth": $is_full_depth, "mac_address": $mac_address, "serial": $serial, "has_primary_ip": $has_primary_ip, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis_member": $virtual_chassis_member, "console_ports": $console_ports, "console_server_ports": $console_server_ports, "power_ports": $power_ports, "power_outlets": $power_outlets, "interfaces": $interfaces, "pass_through_ports": $pass_through_ports, "module_bays": $module_bays, "device_bays": $device_bays, "primary_ip4_id": $primary_ip4_id, "primary_ip6_id": $primary_ip6_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "asset_tag__n": $asset_tag_n, "asset_tag__ic": $asset_tag_ic, "asset_tag__nic": $asset_tag_nic, "asset_tag__iew": $asset_tag_iew, "asset_tag__niew": $asset_tag_niew, "asset_tag__isw": $asset_tag_isw, "asset_tag__nisw": $asset_tag_nisw, "asset_tag__ie": $asset_tag_ie, "asset_tag__nie": $asset_tag_nie, "asset_tag__empty": $asset_tag_empty, "face__n": $face_n, "position__n": $position_n, "position__lte": $position_lte, "position__lt": $position_lt, "position__gte": $position_gte, "position__gt": $position_gt, "airflow__n": $airflow_n, "vc_position__n": $vc_position_n, "vc_position__lte": $vc_position_lte, "vc_position__lt": $vc_position_lt, "vc_position__gte": $vc_position_gte, "vc_position__gt": $vc_position_gt, "vc_priority__n": $vc_priority_n, "vc_priority__lte": $vc_priority_lte, "vc_priority__lt": $vc_priority_lt, "vc_priority__gte": $vc_priority_gte, "vc_priority__gt": $vc_priority_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "manufacturer_id__n": $manufacturer_id_n, "manufacturer__n": $manufacturer_n, "device_type__n": $device_type_n, "device_type_id__n": $device_type_id_n, "role_id__n": $role_id_n, "role__n": $role_n, "parent_device_id__n": $parent_device_id_n, "platform_id__n": $platform_id_n, "platform__n": $platform_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "rack_id__n": $rack_id_n, "cluster_id__n": $cluster_id_n, "model__n": $model_n, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "status__n": $status_n, "mac_address__n": $mac_address_n, "mac_address__ic": $mac_address_ic, "mac_address__nic": $mac_address_nic, "mac_address__iew": $mac_address_iew, "mac_address__niew": $mac_address_niew, "mac_address__isw": $mac_address_isw, "mac_address__nisw": $mac_address_nisw, "mac_address__ie": $mac_address_ie, "mac_address__nie": $mac_address_nie, "serial__n": $serial_n, "serial__ic": $serial_ic, "serial__nic": $serial_nic, "serial__iew": $serial_iew, "serial__niew": $serial_niew, "serial__isw": $serial_isw, "serial__nisw": $serial_nisw, "serial__ie": $serial_ie, "serial__nie": $serial_nie, "serial__empty": $serial_empty, "virtual_chassis_id__n": $virtual_chassis_id_n, "primary_ip4_id__n": $primary_ip4_id_n, "primary_ip6_id__n": $primary_ip6_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/devices/
#
# operationId: dcim_devices_bulk_partial_update
# --parent_device shape: {name?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-devices update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device_role: int
  device_type: int
  --face: string@face-completer
  --local-context-data: record # nullable
  --location: int # nullable
  --name: string # nullable
  --parent-device: record # shape: {name?: string}
  --platform: int # nullable
  --position: float # nullable, format: decimal
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --rack: int # nullable
  --serial: string
  site: int
  --status: string@status-completer-2
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vc-position: int # nullable
  --vc-priority: int # nullable
  --virtual-chassis: int # nullable
]: any -> record<airflow: record<label: string, value: string>, asset_tag: string, cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device_role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, face: record<label: string, value: string>, id: int, last_updated: string, local_context_data: record, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, parent_device: record<display: string, id: int, name: string, url: string>, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, position: float, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vc_position: int, vc_priority: int, virtual_chassis: record<display: string, id: int, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/devices/")
  let req_body = {"airflow": $airflow, "asset_tag": $asset_tag, "cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device_role": $device_role, "device_type": $device_type, "face": $face, "local_context_data": $local_context_data, "location": $location, "name": $name, "parent_device": $parent_device, "platform": $platform, "position": $position, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "rack": $rack, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vc_position": $vc_position, "vc_priority": $vc_priority, "virtual_chassis": $virtual_chassis} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/devices/
#
# operationId: dcim_devices_create
# --parent_device shape: {name?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-devices create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device_role: int
  device_type: int
  --face: string@face-completer
  --local-context-data: record # nullable
  --location: int # nullable
  --name: string # nullable
  --parent-device: record # shape: {name?: string}
  --platform: int # nullable
  --position: float # nullable, format: decimal
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --rack: int # nullable
  --serial: string
  site: int
  --status: string@status-completer-2
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vc-position: int # nullable
  --vc-priority: int # nullable
  --virtual-chassis: int # nullable
]: any -> record<airflow: record<label: string, value: string>, asset_tag: string, cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device_role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, face: record<label: string, value: string>, id: int, last_updated: string, local_context_data: record, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, parent_device: record<display: string, id: int, name: string, url: string>, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, position: float, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vc_position: int, vc_priority: int, virtual_chassis: record<display: string, id: int, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/devices/")
  let req_body = {"airflow": $airflow, "asset_tag": $asset_tag, "cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device_role": $device_role, "device_type": $device_type, "face": $face, "local_context_data": $local_context_data, "location": $location, "name": $name, "parent_device": $parent_device, "platform": $platform, "position": $position, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "rack": $rack, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vc_position": $vc_position, "vc_priority": $vc_priority, "virtual_chassis": $virtual_chassis} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/devices/
#
# operationId: dcim_devices_bulk_update
# --parent_device shape: {name?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-devices update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device_role: int
  device_type: int
  --face: string@face-completer
  --local-context-data: record # nullable
  --location: int # nullable
  --name: string # nullable
  --parent-device: record # shape: {name?: string}
  --platform: int # nullable
  --position: float # nullable, format: decimal
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --rack: int # nullable
  --serial: string
  site: int
  --status: string@status-completer-2
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vc-position: int # nullable
  --vc-priority: int # nullable
  --virtual-chassis: int # nullable
]: any -> record<airflow: record<label: string, value: string>, asset_tag: string, cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device_role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, face: record<label: string, value: string>, id: int, last_updated: string, local_context_data: record, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, parent_device: record<display: string, id: int, name: string, url: string>, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, position: float, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vc_position: int, vc_priority: int, virtual_chassis: record<display: string, id: int, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/devices/")
  let req_body = {"airflow": $airflow, "asset_tag": $asset_tag, "cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device_role": $device_role, "device_type": $device_type, "face": $face, "local_context_data": $local_context_data, "location": $location, "name": $name, "parent_device": $parent_device, "platform": $platform, "position": $position, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "rack": $rack, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vc_position": $vc_position, "vc_priority": $vc_priority, "virtual_chassis": $virtual_chassis} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/devices/{id}/
#
# operationId: dcim_devices_delete
export def "dcim-devices delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/devices/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/devices/{id}/
#
# operationId: dcim_devices_read
export def "dcim-devices get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<airflow: record<label: string, value: string>, asset_tag: string, cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device_role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, face: record<label: string, value: string>, id: int, last_updated: string, local_context_data: record, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, parent_device: record<display: string, id: int, name: string, url: string>, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, position: float, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vc_position: int, vc_priority: int, virtual_chassis: record<display: string, id: int, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/devices/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/devices/{id}/
#
# operationId: dcim_devices_partial_update
# --parent_device shape: {name?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-devices update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device_role: int
  device_type: int
  --face: string@face-completer
  --local-context-data: record # nullable
  --location: int # nullable
  --name: string # nullable
  --parent-device: record # shape: {name?: string}
  --platform: int # nullable
  --position: float # nullable, format: decimal
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --rack: int # nullable
  --serial: string
  site: int
  --status: string@status-completer-2
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vc-position: int # nullable
  --vc-priority: int # nullable
  --virtual-chassis: int # nullable
]: any -> record<airflow: record<label: string, value: string>, asset_tag: string, cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device_role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, face: record<label: string, value: string>, id: int, last_updated: string, local_context_data: record, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, parent_device: record<display: string, id: int, name: string, url: string>, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, position: float, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vc_position: int, vc_priority: int, virtual_chassis: record<display: string, id: int, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/devices/{id}/"))
  let req_body = {"airflow": $airflow, "asset_tag": $asset_tag, "cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device_role": $device_role, "device_type": $device_type, "face": $face, "local_context_data": $local_context_data, "location": $location, "name": $name, "parent_device": $parent_device, "platform": $platform, "position": $position, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "rack": $rack, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vc_position": $vc_position, "vc_priority": $vc_priority, "virtual_chassis": $virtual_chassis} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/devices/{id}/
#
# operationId: dcim_devices_update
# --parent_device shape: {name?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-devices update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --airflow: string@airflow-completer
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device_role: int
  device_type: int
  --face: string@face-completer
  --local-context-data: record # nullable
  --location: int # nullable
  --name: string # nullable
  --parent-device: record # shape: {name?: string}
  --platform: int # nullable
  --position: float # nullable, format: decimal
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --rack: int # nullable
  --serial: string
  site: int
  --status: string@status-completer-2
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vc-position: int # nullable
  --vc-priority: int # nullable
  --virtual-chassis: int # nullable
]: any -> record<airflow: record<label: string, value: string>, asset_tag: string, cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device_role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, face: record<label: string, value: string>, id: int, last_updated: string, local_context_data: record, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, parent_device: record<display: string, id: int, name: string, url: string>, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, position: float, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vc_position: int, vc_priority: int, virtual_chassis: record<display: string, id: int, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/devices/{id}/"))
  let req_body = {"airflow": $airflow, "asset_tag": $asset_tag, "cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device_role": $device_role, "device_type": $device_type, "face": $face, "local_context_data": $local_context_data, "location": $location, "name": $name, "parent_device": $parent_device, "platform": $platform, "position": $position, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "rack": $rack, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vc_position": $vc_position, "vc_priority": $vc_priority, "virtual_chassis": $virtual_chassis} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Execute a NAPALM method on a Device
#
# GET /dcim/devices/{id}/napalm/
# operationId: dcim_devices_napalm
export def "dcim-devices-napalm get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --method: string
]: nothing -> record<method: record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let qp = [(serialize-qp "method" $method "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/devices/{id}/napalm/") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"method": $method} | compact), body: null}
}

# DELETE /dcim/front-port-templates/
#
# operationId: dcim_front-port-templates_bulk_delete
export def "dcim-front-port-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-port-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/front-port-templates/
#
# operationId: dcim_front-port-templates_list
export def "dcim-front-port-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type: string
  --color: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --moduletype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --type-n: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --moduletype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<color: string, created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, module_type: record, name: string, rear_port: record, rear_port_position: int, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "moduletype_id" $moduletype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "moduletype_id__n" $moduletype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/front-port-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type": $type, "color": $color, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "moduletype_id": $moduletype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "type__n": $type_n, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "moduletype_id__n": $moduletype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/front-port-templates/
#
# operationId: dcim_front-port-templates_bulk_partial_update
export def "dcim-front-port-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  rear_port: int
  --rear-port-position: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, rear_port: record<display: string, id: int, name: string, url: string>, rear_port_position: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-port-templates/")
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/front-port-templates/
#
# operationId: dcim_front-port-templates_create
export def "dcim-front-port-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  rear_port: int
  --rear-port-position: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, rear_port: record<display: string, id: int, name: string, url: string>, rear_port_position: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-port-templates/")
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/front-port-templates/
#
# operationId: dcim_front-port-templates_bulk_update
export def "dcim-front-port-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  rear_port: int
  --rear-port-position: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, rear_port: record<display: string, id: int, name: string, url: string>, rear_port_position: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-port-templates/")
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/front-port-templates/{id}/
#
# operationId: dcim_front-port-templates_delete
export def "dcim-front-port-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/front-port-templates/{id}/
#
# operationId: dcim_front-port-templates_read
export def "dcim-front-port-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, rear_port: record<display: string, id: int, name: string, url: string>, rear_port_position: int, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/front-port-templates/{id}/
#
# operationId: dcim_front-port-templates_partial_update
export def "dcim-front-port-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  rear_port: int
  --rear-port-position: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, rear_port: record<display: string, id: int, name: string, url: string>, rear_port_position: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-port-templates/{id}/"))
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/front-port-templates/{id}/
#
# operationId: dcim_front-port-templates_update
export def "dcim-front-port-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  rear_port: int
  --rear-port-position: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, rear_port: record<display: string, id: int, name: string, url: string>, rear_port_position: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-port-templates/{id}/"))
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/front-ports/
#
# operationId: dcim_front-ports_bulk_delete
export def "dcim-front-ports delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-ports/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/front-ports/
#
# operationId: dcim_front-ports_list
export def "dcim-front-ports list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --type: string
  --color: string
  --description: string
  --cable-end: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --module-id: string
  --created: string
  --last-updated: string
  --tag: string
  --cabled: string
  --occupied: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --type-n: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --module-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, cable: record, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, label: string, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, module: record, name: string, rear_port: record, rear_port_position: int, tags: list, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "module_id" $module_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "module_id__n" $module_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/front-ports/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "type": $type, "color": $color, "description": $description, "cable_end": $cable_end, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "module_id": $module_id, "created": $created, "last_updated": $last_updated, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "type__n": $type_n, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "module_id__n": $module_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/front-ports/
#
# operationId: dcim_front-ports_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-front-ports update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  rear_port: int
  --rear-port-position: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, rear_port: record<description: string, display: string, id: int, label: string, name: string, url: string>, rear_port_position: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-ports/")
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/front-ports/
#
# operationId: dcim_front-ports_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-front-ports create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  rear_port: int
  --rear-port-position: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, rear_port: record<description: string, display: string, id: int, label: string, name: string, url: string>, rear_port_position: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-ports/")
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/front-ports/
#
# operationId: dcim_front-ports_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-front-ports update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  rear_port: int
  --rear-port-position: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, rear_port: record<description: string, display: string, id: int, label: string, name: string, url: string>, rear_port_position: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/front-ports/")
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/front-ports/{id}/
#
# operationId: dcim_front-ports_delete
export def "dcim-front-ports delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/front-ports/{id}/
#
# operationId: dcim_front-ports_read
export def "dcim-front-ports get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, rear_port: record<description: string, display: string, id: int, label: string, name: string, url: string>, rear_port_position: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/front-ports/{id}/
#
# operationId: dcim_front-ports_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-front-ports update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  rear_port: int
  --rear-port-position: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, rear_port: record<description: string, display: string, id: int, label: string, name: string, url: string>, rear_port_position: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-ports/{id}/"))
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/front-ports/{id}/
#
# operationId: dcim_front-ports_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-front-ports update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  rear_port: int
  --rear-port-position: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, rear_port: record<description: string, display: string, id: int, label: string, name: string, url: string>, rear_port_position: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-ports/{id}/"))
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "rear_port": $rear_port, "rear_port_position": $rear_port_position, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Return all CablePaths which traverse a given pass-through port.
#
# GET /dcim/front-ports/{id}/paths/
# operationId: dcim_front-ports_paths
export def "dcim-front-ports-paths get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, rear_port: record<description: string, display: string, id: int, label: string, name: string, url: string>, rear_port_position: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/front-ports/{id}/paths/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/interface-templates/
#
# operationId: dcim_interface-templates_bulk_delete
export def "dcim-interface-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interface-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/interface-templates/
#
# operationId: dcim_interface-templates_list
export def "dcim-interface-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type: string
  --mgmt-only: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --moduletype-id: string
  --poe-mode: string
  --poe-type: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --type-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --moduletype-id-n: string
  --poe-mode-n: string
  --poe-type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, mgmt_only: bool, module_type: record, name: string, poe_mode: record, poe_type: record, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "mgmt_only" $mgmt_only "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "moduletype_id" $moduletype_id "scalar") (serialize-qp "poe_mode" $poe_mode "scalar") (serialize-qp "poe_type" $poe_type "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "moduletype_id__n" $moduletype_id_n "scalar") (serialize-qp "poe_mode__n" $poe_mode_n "scalar") (serialize-qp "poe_type__n" $poe_type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/interface-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type": $type, "mgmt_only": $mgmt_only, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "moduletype_id": $moduletype_id, "poe_mode": $poe_mode, "poe_type": $poe_type, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "type__n": $type_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "moduletype_id__n": $moduletype_id_n, "poe_mode__n": $poe_mode_n, "poe_type__n": $poe_type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/interface-templates/
#
# operationId: dcim_interface-templates_bulk_partial_update
export def "dcim-interface-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --mgmt-only: oneof<nothing, bool>
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  type: string@type-completer-3
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, mgmt_only: bool, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interface-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "mgmt_only": $mgmt_only, "module_type": $module_type, "name": $name, "poe_mode": $poe_mode, "poe_type": $poe_type, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/interface-templates/
#
# operationId: dcim_interface-templates_create
export def "dcim-interface-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --mgmt-only: oneof<nothing, bool>
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  type: string@type-completer-3
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, mgmt_only: bool, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interface-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "mgmt_only": $mgmt_only, "module_type": $module_type, "name": $name, "poe_mode": $poe_mode, "poe_type": $poe_type, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/interface-templates/
#
# operationId: dcim_interface-templates_bulk_update
export def "dcim-interface-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --mgmt-only: oneof<nothing, bool>
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  type: string@type-completer-3
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, mgmt_only: bool, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interface-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "mgmt_only": $mgmt_only, "module_type": $module_type, "name": $name, "poe_mode": $poe_mode, "poe_type": $poe_type, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/interface-templates/{id}/
#
# operationId: dcim_interface-templates_delete
export def "dcim-interface-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interface-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/interface-templates/{id}/
#
# operationId: dcim_interface-templates_read
export def "dcim-interface-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, mgmt_only: bool, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interface-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/interface-templates/{id}/
#
# operationId: dcim_interface-templates_partial_update
export def "dcim-interface-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --mgmt-only: oneof<nothing, bool>
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  type: string@type-completer-3
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, mgmt_only: bool, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interface-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "mgmt_only": $mgmt_only, "module_type": $module_type, "name": $name, "poe_mode": $poe_mode, "poe_type": $poe_type, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/interface-templates/{id}/
#
# operationId: dcim_interface-templates_update
export def "dcim-interface-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --mgmt-only: oneof<nothing, bool>
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  type: string@type-completer-3
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, mgmt_only: bool, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interface-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "mgmt_only": $mgmt_only, "module_type": $module_type, "name": $name, "poe_mode": $poe_mode, "poe_type": $poe_type, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/interfaces/
#
# operationId: dcim_interfaces_bulk_delete
export def "dcim-interfaces delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interfaces/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/interfaces/
#
# operationId: dcim_interfaces_list
export def "dcim-interfaces list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --type: string
  --enabled: string
  --mtu: string
  --mgmt-only: string
  --poe-mode: string
  --poe-type: string
  --mode: string
  --rf-role: string
  --rf-channel: string
  --rf-channel-frequency: string
  --rf-channel-width: string
  --tx-power: string
  --description: string
  --cable-end: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --module-id: string
  --created: string
  --last-updated: string
  --tag: string
  --cabled: string
  --occupied: string
  --connected: string
  --kind: string
  --parent-id: string
  --bridge-id: string
  --lag-id: string
  --speed: string
  --duplex: string
  --mac-address: string
  --wwn: string
  --vlan-id: string
  --vlan: string
  --vrf-id: string
  --vrf: string
  --vdc-id: string
  --vdc-identifier: string
  --vdc: string
  --l2vpn-id: string
  --l2vpn: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --type-n: string
  --mtu-n: string
  --mtu-lte: string
  --mtu-lt: string
  --mtu-gte: string
  --mtu-gt: string
  --poe-mode-n: string
  --poe-type-n: string
  --mode-n: string
  --rf-role-n: string
  --rf-channel-n: string
  --rf-channel-frequency-n: string
  --rf-channel-frequency-lte: string
  --rf-channel-frequency-lt: string
  --rf-channel-frequency-gte: string
  --rf-channel-frequency-gt: string
  --rf-channel-width-n: string
  --rf-channel-width-lte: string
  --rf-channel-width-lt: string
  --rf-channel-width-gte: string
  --rf-channel-width-gt: string
  --tx-power-n: string
  --tx-power-lte: string
  --tx-power-lt: string
  --tx-power-gte: string
  --tx-power-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --module-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --parent-id-n: string
  --bridge-id-n: string
  --lag-id-n: string
  --speed-n: string
  --speed-lte: string
  --speed-lt: string
  --speed-gte: string
  --speed-gt: string
  --duplex-n: string
  --mac-address-n: string
  --mac-address-ic: string
  --mac-address-nic: string
  --mac-address-iew: string
  --mac-address-niew: string
  --mac-address-isw: string
  --mac-address-nisw: string
  --mac-address-ie: string
  --mac-address-nie: string
  --wwn-n: string
  --wwn-ic: string
  --wwn-nic: string
  --wwn-iew: string
  --wwn-niew: string
  --wwn-isw: string
  --wwn-nisw: string
  --wwn-ie: string
  --wwn-nie: string
  --vrf-id-n: string
  --vrf-n: string
  --vdc-id-n: string
  --vdc-identifier-n: string
  --vdc-n: string
  --l2vpn-id-n: string
  --l2vpn-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, bridge: record, cable: record, cable_end: string, connected_endpoints: list, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record, display: string, duplex: record, enabled: bool, id: int, l2vpn_termination: record, label: string, lag: record, last_updated: string, link_peers: list, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record, module: record, mtu: int, name: string, parent: record, poe_mode: record, poe_type: record, rf_channel: record, rf_channel_frequency: float, rf_channel_width: float, rf_role: record, speed: int, tagged_vlans: list, tags: list, tx_power: int, type: record, untagged_vlan: record, url: string, vdcs: list, vrf: record, wireless_lans: list, wireless_link: record, wwn: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "enabled" $enabled "scalar") (serialize-qp "mtu" $mtu "scalar") (serialize-qp "mgmt_only" $mgmt_only "scalar") (serialize-qp "poe_mode" $poe_mode "scalar") (serialize-qp "poe_type" $poe_type "scalar") (serialize-qp "mode" $mode "scalar") (serialize-qp "rf_role" $rf_role "scalar") (serialize-qp "rf_channel" $rf_channel "scalar") (serialize-qp "rf_channel_frequency" $rf_channel_frequency "scalar") (serialize-qp "rf_channel_width" $rf_channel_width "scalar") (serialize-qp "tx_power" $tx_power "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "module_id" $module_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "connected" $connected "scalar") (serialize-qp "kind" $kind "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "bridge_id" $bridge_id "scalar") (serialize-qp "lag_id" $lag_id "scalar") (serialize-qp "speed" $speed "scalar") (serialize-qp "duplex" $duplex "scalar") (serialize-qp "mac_address" $mac_address "scalar") (serialize-qp "wwn" $wwn "scalar") (serialize-qp "vlan_id" $vlan_id "scalar") (serialize-qp "vlan" $vlan "scalar") (serialize-qp "vrf_id" $vrf_id "scalar") (serialize-qp "vrf" $vrf "scalar") (serialize-qp "vdc_id" $vdc_id "scalar") (serialize-qp "vdc_identifier" $vdc_identifier "scalar") (serialize-qp "vdc" $vdc "scalar") (serialize-qp "l2vpn_id" $l2vpn_id "scalar") (serialize-qp "l2vpn" $l2vpn "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "mtu__n" $mtu_n "scalar") (serialize-qp "mtu__lte" $mtu_lte "scalar") (serialize-qp "mtu__lt" $mtu_lt "scalar") (serialize-qp "mtu__gte" $mtu_gte "scalar") (serialize-qp "mtu__gt" $mtu_gt "scalar") (serialize-qp "poe_mode__n" $poe_mode_n "scalar") (serialize-qp "poe_type__n" $poe_type_n "scalar") (serialize-qp "mode__n" $mode_n "scalar") (serialize-qp "rf_role__n" $rf_role_n "scalar") (serialize-qp "rf_channel__n" $rf_channel_n "scalar") (serialize-qp "rf_channel_frequency__n" $rf_channel_frequency_n "scalar") (serialize-qp "rf_channel_frequency__lte" $rf_channel_frequency_lte "scalar") (serialize-qp "rf_channel_frequency__lt" $rf_channel_frequency_lt "scalar") (serialize-qp "rf_channel_frequency__gte" $rf_channel_frequency_gte "scalar") (serialize-qp "rf_channel_frequency__gt" $rf_channel_frequency_gt "scalar") (serialize-qp "rf_channel_width__n" $rf_channel_width_n "scalar") (serialize-qp "rf_channel_width__lte" $rf_channel_width_lte "scalar") (serialize-qp "rf_channel_width__lt" $rf_channel_width_lt "scalar") (serialize-qp "rf_channel_width__gte" $rf_channel_width_gte "scalar") (serialize-qp "rf_channel_width__gt" $rf_channel_width_gt "scalar") (serialize-qp "tx_power__n" $tx_power_n "scalar") (serialize-qp "tx_power__lte" $tx_power_lte "scalar") (serialize-qp "tx_power__lt" $tx_power_lt "scalar") (serialize-qp "tx_power__gte" $tx_power_gte "scalar") (serialize-qp "tx_power__gt" $tx_power_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "module_id__n" $module_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "bridge_id__n" $bridge_id_n "scalar") (serialize-qp "lag_id__n" $lag_id_n "scalar") (serialize-qp "speed__n" $speed_n "scalar") (serialize-qp "speed__lte" $speed_lte "scalar") (serialize-qp "speed__lt" $speed_lt "scalar") (serialize-qp "speed__gte" $speed_gte "scalar") (serialize-qp "speed__gt" $speed_gt "scalar") (serialize-qp "duplex__n" $duplex_n "scalar") (serialize-qp "mac_address__n" $mac_address_n "scalar") (serialize-qp "mac_address__ic" $mac_address_ic "scalar") (serialize-qp "mac_address__nic" $mac_address_nic "scalar") (serialize-qp "mac_address__iew" $mac_address_iew "scalar") (serialize-qp "mac_address__niew" $mac_address_niew "scalar") (serialize-qp "mac_address__isw" $mac_address_isw "scalar") (serialize-qp "mac_address__nisw" $mac_address_nisw "scalar") (serialize-qp "mac_address__ie" $mac_address_ie "scalar") (serialize-qp "mac_address__nie" $mac_address_nie "scalar") (serialize-qp "wwn__n" $wwn_n "scalar") (serialize-qp "wwn__ic" $wwn_ic "scalar") (serialize-qp "wwn__nic" $wwn_nic "scalar") (serialize-qp "wwn__iew" $wwn_iew "scalar") (serialize-qp "wwn__niew" $wwn_niew "scalar") (serialize-qp "wwn__isw" $wwn_isw "scalar") (serialize-qp "wwn__nisw" $wwn_nisw "scalar") (serialize-qp "wwn__ie" $wwn_ie "scalar") (serialize-qp "wwn__nie" $wwn_nie "scalar") (serialize-qp "vrf_id__n" $vrf_id_n "scalar") (serialize-qp "vrf__n" $vrf_n "scalar") (serialize-qp "vdc_id__n" $vdc_id_n "scalar") (serialize-qp "vdc_identifier__n" $vdc_identifier_n "scalar") (serialize-qp "vdc__n" $vdc_n "scalar") (serialize-qp "l2vpn_id__n" $l2vpn_id_n "scalar") (serialize-qp "l2vpn__n" $l2vpn_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/interfaces/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "type": $type, "enabled": $enabled, "mtu": $mtu, "mgmt_only": $mgmt_only, "poe_mode": $poe_mode, "poe_type": $poe_type, "mode": $mode, "rf_role": $rf_role, "rf_channel": $rf_channel, "rf_channel_frequency": $rf_channel_frequency, "rf_channel_width": $rf_channel_width, "tx_power": $tx_power, "description": $description, "cable_end": $cable_end, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "module_id": $module_id, "created": $created, "last_updated": $last_updated, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "connected": $connected, "kind": $kind, "parent_id": $parent_id, "bridge_id": $bridge_id, "lag_id": $lag_id, "speed": $speed, "duplex": $duplex, "mac_address": $mac_address, "wwn": $wwn, "vlan_id": $vlan_id, "vlan": $vlan, "vrf_id": $vrf_id, "vrf": $vrf, "vdc_id": $vdc_id, "vdc_identifier": $vdc_identifier, "vdc": $vdc, "l2vpn_id": $l2vpn_id, "l2vpn": $l2vpn, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "type__n": $type_n, "mtu__n": $mtu_n, "mtu__lte": $mtu_lte, "mtu__lt": $mtu_lt, "mtu__gte": $mtu_gte, "mtu__gt": $mtu_gt, "poe_mode__n": $poe_mode_n, "poe_type__n": $poe_type_n, "mode__n": $mode_n, "rf_role__n": $rf_role_n, "rf_channel__n": $rf_channel_n, "rf_channel_frequency__n": $rf_channel_frequency_n, "rf_channel_frequency__lte": $rf_channel_frequency_lte, "rf_channel_frequency__lt": $rf_channel_frequency_lt, "rf_channel_frequency__gte": $rf_channel_frequency_gte, "rf_channel_frequency__gt": $rf_channel_frequency_gt, "rf_channel_width__n": $rf_channel_width_n, "rf_channel_width__lte": $rf_channel_width_lte, "rf_channel_width__lt": $rf_channel_width_lt, "rf_channel_width__gte": $rf_channel_width_gte, "rf_channel_width__gt": $rf_channel_width_gt, "tx_power__n": $tx_power_n, "tx_power__lte": $tx_power_lte, "tx_power__lt": $tx_power_lt, "tx_power__gte": $tx_power_gte, "tx_power__gt": $tx_power_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "module_id__n": $module_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "parent_id__n": $parent_id_n, "bridge_id__n": $bridge_id_n, "lag_id__n": $lag_id_n, "speed__n": $speed_n, "speed__lte": $speed_lte, "speed__lt": $speed_lt, "speed__gte": $speed_gte, "speed__gt": $speed_gt, "duplex__n": $duplex_n, "mac_address__n": $mac_address_n, "mac_address__ic": $mac_address_ic, "mac_address__nic": $mac_address_nic, "mac_address__iew": $mac_address_iew, "mac_address__niew": $mac_address_niew, "mac_address__isw": $mac_address_isw, "mac_address__nisw": $mac_address_nisw, "mac_address__ie": $mac_address_ie, "mac_address__nie": $mac_address_nie, "wwn__n": $wwn_n, "wwn__ic": $wwn_ic, "wwn__nic": $wwn_nic, "wwn__iew": $wwn_iew, "wwn__niew": $wwn_niew, "wwn__isw": $wwn_isw, "wwn__nisw": $wwn_nisw, "wwn__ie": $wwn_ie, "wwn__nie": $wwn_nie, "vrf_id__n": $vrf_id_n, "vrf__n": $vrf_n, "vdc_id__n": $vdc_id_n, "vdc_identifier__n": $vdc_identifier_n, "vdc__n": $vdc_n, "l2vpn_id__n": $l2vpn_id_n, "l2vpn__n": $l2vpn_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/interfaces/
#
# operationId: dcim_interfaces_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-interfaces update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --duplex: string@duplex-completer # nullable
  --enabled: oneof<nothing, bool>
  --label: string # Physical label
  --lag: int # nullable
  --mac-address: string # nullable
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --mgmt-only: oneof<nothing, bool> # This interface is used only for out-of-band management
  --mode: string@mode-completer
  --body-module: int # nullable
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  --rf-channel: string@rf-channel-completer
  --rf-channel-frequency: float # nullable, format: decimal
  --rf-channel-width: float # nullable, format: decimal
  --rf-role: string@rf-role-completer
  --speed: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tx-power: int # nullable
  type: string@type-completer-3
  --untagged-vlan: int # nullable
  vdcs: list<int>
  --vrf: int # nullable
  --wireless-lans: list<int>
  --wireless-link: int # nullable
  --wwn: string # 64-bit World Wide Name (nullable)
]: any -> record<_occupied: bool, bridge: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, duplex: record<label: string, value: string>, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, label: string, lag: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, link_peers: list<string>, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record<label: string, value: string>, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, mtu: int, name: string, parent: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, rf_channel: record<label: string, value: string>, rf_channel_frequency: float, rf_channel_width: float, rf_role: record<label: string, value: string>, speed: int, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tx_power: int, type: record<label: string, value: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, vdcs: table<device: record, display: string, id: int, identifier: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>, wireless_lans: table<display: string, id: int, ssid: string, url: string>, wireless_link: record<display: string, id: int, ssid: string, url: string>, wwn: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interfaces/")
  let req_body = {"bridge": $bridge, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "duplex": $duplex, "enabled": $enabled, "label": $label, "lag": $lag, "mac_address": $mac_address, "mark_connected": $mark_connected, "mgmt_only": $mgmt_only, "mode": $mode, "module": $body_module, "mtu": $mtu, "name": $name, "parent": $parent, "poe_mode": $poe_mode, "poe_type": $poe_type, "rf_channel": $rf_channel, "rf_channel_frequency": $rf_channel_frequency, "rf_channel_width": $rf_channel_width, "rf_role": $rf_role, "speed": $speed, "tagged_vlans": $tagged_vlans, "tags": $tags, "tx_power": $tx_power, "type": $type, "untagged_vlan": $untagged_vlan, "vdcs": $vdcs, "vrf": $vrf, "wireless_lans": $wireless_lans, "wireless_link": $wireless_link, "wwn": $wwn} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/interfaces/
#
# operationId: dcim_interfaces_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-interfaces create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --duplex: string@duplex-completer # nullable
  --enabled: oneof<nothing, bool>
  --label: string # Physical label
  --lag: int # nullable
  --mac-address: string # nullable
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --mgmt-only: oneof<nothing, bool> # This interface is used only for out-of-band management
  --mode: string@mode-completer
  --body-module: int # nullable
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  --rf-channel: string@rf-channel-completer
  --rf-channel-frequency: float # nullable, format: decimal
  --rf-channel-width: float # nullable, format: decimal
  --rf-role: string@rf-role-completer
  --speed: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tx-power: int # nullable
  type: string@type-completer-3
  --untagged-vlan: int # nullable
  vdcs: list<int>
  --vrf: int # nullable
  --wireless-lans: list<int>
  --wireless-link: int # nullable
  --wwn: string # 64-bit World Wide Name (nullable)
]: any -> record<_occupied: bool, bridge: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, duplex: record<label: string, value: string>, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, label: string, lag: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, link_peers: list<string>, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record<label: string, value: string>, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, mtu: int, name: string, parent: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, rf_channel: record<label: string, value: string>, rf_channel_frequency: float, rf_channel_width: float, rf_role: record<label: string, value: string>, speed: int, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tx_power: int, type: record<label: string, value: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, vdcs: table<device: record, display: string, id: int, identifier: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>, wireless_lans: table<display: string, id: int, ssid: string, url: string>, wireless_link: record<display: string, id: int, ssid: string, url: string>, wwn: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interfaces/")
  let req_body = {"bridge": $bridge, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "duplex": $duplex, "enabled": $enabled, "label": $label, "lag": $lag, "mac_address": $mac_address, "mark_connected": $mark_connected, "mgmt_only": $mgmt_only, "mode": $mode, "module": $body_module, "mtu": $mtu, "name": $name, "parent": $parent, "poe_mode": $poe_mode, "poe_type": $poe_type, "rf_channel": $rf_channel, "rf_channel_frequency": $rf_channel_frequency, "rf_channel_width": $rf_channel_width, "rf_role": $rf_role, "speed": $speed, "tagged_vlans": $tagged_vlans, "tags": $tags, "tx_power": $tx_power, "type": $type, "untagged_vlan": $untagged_vlan, "vdcs": $vdcs, "vrf": $vrf, "wireless_lans": $wireless_lans, "wireless_link": $wireless_link, "wwn": $wwn} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/interfaces/
#
# operationId: dcim_interfaces_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-interfaces update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --duplex: string@duplex-completer # nullable
  --enabled: oneof<nothing, bool>
  --label: string # Physical label
  --lag: int # nullable
  --mac-address: string # nullable
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --mgmt-only: oneof<nothing, bool> # This interface is used only for out-of-band management
  --mode: string@mode-completer
  --body-module: int # nullable
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  --rf-channel: string@rf-channel-completer
  --rf-channel-frequency: float # nullable, format: decimal
  --rf-channel-width: float # nullable, format: decimal
  --rf-role: string@rf-role-completer
  --speed: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tx-power: int # nullable
  type: string@type-completer-3
  --untagged-vlan: int # nullable
  vdcs: list<int>
  --vrf: int # nullable
  --wireless-lans: list<int>
  --wireless-link: int # nullable
  --wwn: string # 64-bit World Wide Name (nullable)
]: any -> record<_occupied: bool, bridge: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, duplex: record<label: string, value: string>, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, label: string, lag: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, link_peers: list<string>, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record<label: string, value: string>, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, mtu: int, name: string, parent: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, rf_channel: record<label: string, value: string>, rf_channel_frequency: float, rf_channel_width: float, rf_role: record<label: string, value: string>, speed: int, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tx_power: int, type: record<label: string, value: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, vdcs: table<device: record, display: string, id: int, identifier: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>, wireless_lans: table<display: string, id: int, ssid: string, url: string>, wireless_link: record<display: string, id: int, ssid: string, url: string>, wwn: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/interfaces/")
  let req_body = {"bridge": $bridge, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "duplex": $duplex, "enabled": $enabled, "label": $label, "lag": $lag, "mac_address": $mac_address, "mark_connected": $mark_connected, "mgmt_only": $mgmt_only, "mode": $mode, "module": $body_module, "mtu": $mtu, "name": $name, "parent": $parent, "poe_mode": $poe_mode, "poe_type": $poe_type, "rf_channel": $rf_channel, "rf_channel_frequency": $rf_channel_frequency, "rf_channel_width": $rf_channel_width, "rf_role": $rf_role, "speed": $speed, "tagged_vlans": $tagged_vlans, "tags": $tags, "tx_power": $tx_power, "type": $type, "untagged_vlan": $untagged_vlan, "vdcs": $vdcs, "vrf": $vrf, "wireless_lans": $wireless_lans, "wireless_link": $wireless_link, "wwn": $wwn} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/interfaces/{id}/
#
# operationId: dcim_interfaces_delete
export def "dcim-interfaces delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interfaces/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/interfaces/{id}/
#
# operationId: dcim_interfaces_read
export def "dcim-interfaces get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, bridge: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, duplex: record<label: string, value: string>, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, label: string, lag: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, link_peers: list<string>, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record<label: string, value: string>, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, mtu: int, name: string, parent: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, rf_channel: record<label: string, value: string>, rf_channel_frequency: float, rf_channel_width: float, rf_role: record<label: string, value: string>, speed: int, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tx_power: int, type: record<label: string, value: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, vdcs: table<device: record, display: string, id: int, identifier: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>, wireless_lans: table<display: string, id: int, ssid: string, url: string>, wireless_link: record<display: string, id: int, ssid: string, url: string>, wwn: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interfaces/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/interfaces/{id}/
#
# operationId: dcim_interfaces_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-interfaces update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --duplex: string@duplex-completer # nullable
  --enabled: oneof<nothing, bool>
  --label: string # Physical label
  --lag: int # nullable
  --mac-address: string # nullable
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --mgmt-only: oneof<nothing, bool> # This interface is used only for out-of-band management
  --mode: string@mode-completer
  --body-module: int # nullable
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  --rf-channel: string@rf-channel-completer
  --rf-channel-frequency: float # nullable, format: decimal
  --rf-channel-width: float # nullable, format: decimal
  --rf-role: string@rf-role-completer
  --speed: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tx-power: int # nullable
  type: string@type-completer-3
  --untagged-vlan: int # nullable
  vdcs: list<int>
  --vrf: int # nullable
  --wireless-lans: list<int>
  --wireless-link: int # nullable
  --wwn: string # 64-bit World Wide Name (nullable)
]: any -> record<_occupied: bool, bridge: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, duplex: record<label: string, value: string>, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, label: string, lag: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, link_peers: list<string>, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record<label: string, value: string>, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, mtu: int, name: string, parent: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, rf_channel: record<label: string, value: string>, rf_channel_frequency: float, rf_channel_width: float, rf_role: record<label: string, value: string>, speed: int, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tx_power: int, type: record<label: string, value: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, vdcs: table<device: record, display: string, id: int, identifier: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>, wireless_lans: table<display: string, id: int, ssid: string, url: string>, wireless_link: record<display: string, id: int, ssid: string, url: string>, wwn: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interfaces/{id}/"))
  let req_body = {"bridge": $bridge, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "duplex": $duplex, "enabled": $enabled, "label": $label, "lag": $lag, "mac_address": $mac_address, "mark_connected": $mark_connected, "mgmt_only": $mgmt_only, "mode": $mode, "module": $body_module, "mtu": $mtu, "name": $name, "parent": $parent, "poe_mode": $poe_mode, "poe_type": $poe_type, "rf_channel": $rf_channel, "rf_channel_frequency": $rf_channel_frequency, "rf_channel_width": $rf_channel_width, "rf_role": $rf_role, "speed": $speed, "tagged_vlans": $tagged_vlans, "tags": $tags, "tx_power": $tx_power, "type": $type, "untagged_vlan": $untagged_vlan, "vdcs": $vdcs, "vrf": $vrf, "wireless_lans": $wireless_lans, "wireless_link": $wireless_link, "wwn": $wwn} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/interfaces/{id}/
#
# operationId: dcim_interfaces_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-interfaces update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --duplex: string@duplex-completer # nullable
  --enabled: oneof<nothing, bool>
  --label: string # Physical label
  --lag: int # nullable
  --mac-address: string # nullable
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --mgmt-only: oneof<nothing, bool> # This interface is used only for out-of-band management
  --mode: string@mode-completer
  --body-module: int # nullable
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --poe-mode: string@poe-mode-completer
  --poe-type: string@poe-type-completer
  --rf-channel: string@rf-channel-completer
  --rf-channel-frequency: float # nullable, format: decimal
  --rf-channel-width: float # nullable, format: decimal
  --rf-role: string@rf-role-completer
  --speed: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tx-power: int # nullable
  type: string@type-completer-3
  --untagged-vlan: int # nullable
  vdcs: list<int>
  --vrf: int # nullable
  --wireless-lans: list<int>
  --wireless-link: int # nullable
  --wwn: string # 64-bit World Wide Name (nullable)
]: any -> record<_occupied: bool, bridge: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, duplex: record<label: string, value: string>, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, label: string, lag: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, link_peers: list<string>, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record<label: string, value: string>, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, mtu: int, name: string, parent: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, rf_channel: record<label: string, value: string>, rf_channel_frequency: float, rf_channel_width: float, rf_role: record<label: string, value: string>, speed: int, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tx_power: int, type: record<label: string, value: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, vdcs: table<device: record, display: string, id: int, identifier: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>, wireless_lans: table<display: string, id: int, ssid: string, url: string>, wireless_link: record<display: string, id: int, ssid: string, url: string>, wwn: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interfaces/{id}/"))
  let req_body = {"bridge": $bridge, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "duplex": $duplex, "enabled": $enabled, "label": $label, "lag": $lag, "mac_address": $mac_address, "mark_connected": $mark_connected, "mgmt_only": $mgmt_only, "mode": $mode, "module": $body_module, "mtu": $mtu, "name": $name, "parent": $parent, "poe_mode": $poe_mode, "poe_type": $poe_type, "rf_channel": $rf_channel, "rf_channel_frequency": $rf_channel_frequency, "rf_channel_width": $rf_channel_width, "rf_role": $rf_role, "speed": $speed, "tagged_vlans": $tagged_vlans, "tags": $tags, "tx_power": $tx_power, "type": $type, "untagged_vlan": $untagged_vlan, "vdcs": $vdcs, "vrf": $vrf, "wireless_lans": $wireless_lans, "wireless_link": $wireless_link, "wwn": $wwn} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Trace a complete cable path and return each segment as a three-tuple of (termination, cable, termination).
#
# GET /dcim/interfaces/{id}/trace/
# operationId: dcim_interfaces_trace
export def "dcim-interfaces-trace get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, bridge: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, duplex: record<label: string, value: string>, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, label: string, lag: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, link_peers: list<string>, link_peers_type: string, mac_address: string, mark_connected: bool, mgmt_only: bool, mode: record<label: string, value: string>, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, mtu: int, name: string, parent: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, poe_mode: record<label: string, value: string>, poe_type: record<label: string, value: string>, rf_channel: record<label: string, value: string>, rf_channel_frequency: float, rf_channel_width: float, rf_role: record<label: string, value: string>, speed: int, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tx_power: int, type: record<label: string, value: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, vdcs: table<device: record, display: string, id: int, identifier: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>, wireless_lans: table<display: string, id: int, ssid: string, url: string>, wireless_link: record<display: string, id: int, ssid: string, url: string>, wwn: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/interfaces/{id}/trace/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/inventory-item-roles/
#
# operationId: dcim_inventory-item-roles_bulk_delete
export def "dcim-inventory-item-roles delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-roles/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/inventory-item-roles/
#
# operationId: dcim_inventory-item-roles_list
export def "dcim-inventory-item-roles list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --color: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<color: string, created: string, custom_fields: record, description: string, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/inventory-item-roles/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "color": $color, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/inventory-item-roles/
#
# operationId: dcim_inventory-item-roles_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-item-roles update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/inventory-item-roles/
#
# operationId: dcim_inventory-item-roles_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-item-roles create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/inventory-item-roles/
#
# operationId: dcim_inventory-item-roles_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-item-roles update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/inventory-item-roles/{id}/
#
# operationId: dcim_inventory-item-roles_delete
export def "dcim-inventory-item-roles delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/inventory-item-roles/{id}/
#
# operationId: dcim_inventory-item-roles_read
export def "dcim-inventory-item-roles get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/inventory-item-roles/{id}/
#
# operationId: dcim_inventory-item-roles_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-item-roles update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-roles/{id}/"))
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/inventory-item-roles/{id}/
#
# operationId: dcim_inventory-item-roles_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-item-roles update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-roles/{id}/"))
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/inventory-item-templates/
#
# operationId: dcim_inventory-item-templates_bulk_delete
export def "dcim-inventory-item-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/inventory-item-templates/
#
# operationId: dcim_inventory-item-templates_list
export def "dcim-inventory-item-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --part-id: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --parent-id: string
  --manufacturer-id: string
  --manufacturer: string
  --role-id: string
  --role: string
  --component-type: string
  --component-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --part-id-n: string
  --part-id-ic: string
  --part-id-nic: string
  --part-id-iew: string
  --part-id-niew: string
  --part-id-isw: string
  --part-id-nisw: string
  --part-id-ie: string
  --part-id-nie: string
  --part-id-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --parent-id-n: string
  --manufacturer-id-n: string
  --manufacturer-n: string
  --role-id-n: string
  --role-n: string
  --component-type-n: string
  --component-id-n: string
  --component-id-lte: string
  --component-id-lt: string
  --component-id-gte: string
  --component-id-gt: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, component: record, component_id: int, component_type: string, created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, manufacturer: record, name: string, parent: int, part_id: string, role: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "part_id" $part_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "manufacturer_id" $manufacturer_id "scalar") (serialize-qp "manufacturer" $manufacturer "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "component_type" $component_type "scalar") (serialize-qp "component_id" $component_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "part_id__n" $part_id_n "scalar") (serialize-qp "part_id__ic" $part_id_ic "scalar") (serialize-qp "part_id__nic" $part_id_nic "scalar") (serialize-qp "part_id__iew" $part_id_iew "scalar") (serialize-qp "part_id__niew" $part_id_niew "scalar") (serialize-qp "part_id__isw" $part_id_isw "scalar") (serialize-qp "part_id__nisw" $part_id_nisw "scalar") (serialize-qp "part_id__ie" $part_id_ie "scalar") (serialize-qp "part_id__nie" $part_id_nie "scalar") (serialize-qp "part_id__empty" $part_id_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "manufacturer_id__n" $manufacturer_id_n "scalar") (serialize-qp "manufacturer__n" $manufacturer_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "component_type__n" $component_type_n "scalar") (serialize-qp "component_id__n" $component_id_n "scalar") (serialize-qp "component_id__lte" $component_id_lte "scalar") (serialize-qp "component_id__lt" $component_id_lt "scalar") (serialize-qp "component_id__gte" $component_id_gte "scalar") (serialize-qp "component_id__gt" $component_id_gt "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/inventory-item-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "part_id": $part_id, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "parent_id": $parent_id, "manufacturer_id": $manufacturer_id, "manufacturer": $manufacturer, "role_id": $role_id, "role": $role, "component_type": $component_type, "component_id": $component_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "part_id__n": $part_id_n, "part_id__ic": $part_id_ic, "part_id__nic": $part_id_nic, "part_id__iew": $part_id_iew, "part_id__niew": $part_id_niew, "part_id__isw": $part_id_isw, "part_id__nisw": $part_id_nisw, "part_id__ie": $part_id_ie, "part_id__nie": $part_id_nie, "part_id__empty": $part_id_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "parent_id__n": $parent_id_n, "manufacturer_id__n": $manufacturer_id_n, "manufacturer__n": $manufacturer_n, "role_id__n": $role_id_n, "role__n": $role_n, "component_type__n": $component_type_n, "component_id__n": $component_id_n, "component_id__lte": $component_id_lte, "component_id__lt": $component_id_lt, "component_id__gte": $component_id_gte, "component_id__gt": $component_id_gt, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/inventory-item-templates/
#
# operationId: dcim_inventory-item-templates_bulk_partial_update
export def "dcim-inventory-item-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --component-id: int # nullable
  --component-type: string # nullable
  --description: string
  device_type: int
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
]: any -> record<_depth: int, component: record, component_id: int, component_type: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-templates/")
  let req_body = {"component_id": $component_id, "component_type": $component_type, "description": $description, "device_type": $device_type, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/inventory-item-templates/
#
# operationId: dcim_inventory-item-templates_create
export def "dcim-inventory-item-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --component-id: int # nullable
  --component-type: string # nullable
  --description: string
  device_type: int
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
]: any -> record<_depth: int, component: record, component_id: int, component_type: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-templates/")
  let req_body = {"component_id": $component_id, "component_type": $component_type, "description": $description, "device_type": $device_type, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/inventory-item-templates/
#
# operationId: dcim_inventory-item-templates_bulk_update
export def "dcim-inventory-item-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --component-id: int # nullable
  --component-type: string # nullable
  --description: string
  device_type: int
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
]: any -> record<_depth: int, component: record, component_id: int, component_type: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-item-templates/")
  let req_body = {"component_id": $component_id, "component_type": $component_type, "description": $description, "device_type": $device_type, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/inventory-item-templates/{id}/
#
# operationId: dcim_inventory-item-templates_delete
export def "dcim-inventory-item-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/inventory-item-templates/{id}/
#
# operationId: dcim_inventory-item-templates_read
export def "dcim-inventory-item-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, component: record, component_id: int, component_type: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/inventory-item-templates/{id}/
#
# operationId: dcim_inventory-item-templates_partial_update
export def "dcim-inventory-item-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --component-id: int # nullable
  --component-type: string # nullable
  --description: string
  device_type: int
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
]: any -> record<_depth: int, component: record, component_id: int, component_type: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-templates/{id}/"))
  let req_body = {"component_id": $component_id, "component_type": $component_type, "description": $description, "device_type": $device_type, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/inventory-item-templates/{id}/
#
# operationId: dcim_inventory-item-templates_update
export def "dcim-inventory-item-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --component-id: int # nullable
  --component-type: string # nullable
  --description: string
  device_type: int
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
]: any -> record<_depth: int, component: record, component_id: int, component_type: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-item-templates/{id}/"))
  let req_body = {"component_id": $component_id, "component_type": $component_type, "description": $description, "device_type": $device_type, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/inventory-items/
#
# operationId: dcim_inventory-items_bulk_delete
export def "dcim-inventory-items delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-items/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/inventory-items/
#
# operationId: dcim_inventory-items_list
export def "dcim-inventory-items list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --part-id: string
  --asset-tag: string
  --discovered: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --created: string
  --last-updated: string
  --tag: string
  --parent-id: string
  --manufacturer-id: string
  --manufacturer: string
  --role-id: string
  --role: string
  --component-type: string
  --component-id: string
  --serial: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --part-id-n: string
  --part-id-ic: string
  --part-id-nic: string
  --part-id-iew: string
  --part-id-niew: string
  --part-id-isw: string
  --part-id-nisw: string
  --part-id-ie: string
  --part-id-nie: string
  --part-id-empty: string
  --asset-tag-n: string
  --asset-tag-ic: string
  --asset-tag-nic: string
  --asset-tag-iew: string
  --asset-tag-niew: string
  --asset-tag-isw: string
  --asset-tag-nisw: string
  --asset-tag-ie: string
  --asset-tag-nie: string
  --asset-tag-empty: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --parent-id-n: string
  --manufacturer-id-n: string
  --manufacturer-n: string
  --role-id-n: string
  --role-n: string
  --component-type-n: string
  --component-id-n: string
  --component-id-lte: string
  --component-id-lt: string
  --component-id-gte: string
  --component-id-gt: string
  --serial-n: string
  --serial-ic: string
  --serial-nic: string
  --serial-iew: string
  --serial-niew: string
  --serial-isw: string
  --serial-nisw: string
  --serial-ie: string
  --serial-nie: string
  --serial-empty: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, asset_tag: string, component: record, component_id: int, component_type: string, created: string, custom_fields: record, description: string, device: record, discovered: bool, display: string, id: int, label: string, last_updated: string, manufacturer: record, name: string, parent: int, part_id: string, role: record, serial: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "part_id" $part_id "scalar") (serialize-qp "asset_tag" $asset_tag "scalar") (serialize-qp "discovered" $discovered "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "manufacturer_id" $manufacturer_id "scalar") (serialize-qp "manufacturer" $manufacturer "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "component_type" $component_type "scalar") (serialize-qp "component_id" $component_id "scalar") (serialize-qp "serial" $serial "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "part_id__n" $part_id_n "scalar") (serialize-qp "part_id__ic" $part_id_ic "scalar") (serialize-qp "part_id__nic" $part_id_nic "scalar") (serialize-qp "part_id__iew" $part_id_iew "scalar") (serialize-qp "part_id__niew" $part_id_niew "scalar") (serialize-qp "part_id__isw" $part_id_isw "scalar") (serialize-qp "part_id__nisw" $part_id_nisw "scalar") (serialize-qp "part_id__ie" $part_id_ie "scalar") (serialize-qp "part_id__nie" $part_id_nie "scalar") (serialize-qp "part_id__empty" $part_id_empty "scalar") (serialize-qp "asset_tag__n" $asset_tag_n "scalar") (serialize-qp "asset_tag__ic" $asset_tag_ic "scalar") (serialize-qp "asset_tag__nic" $asset_tag_nic "scalar") (serialize-qp "asset_tag__iew" $asset_tag_iew "scalar") (serialize-qp "asset_tag__niew" $asset_tag_niew "scalar") (serialize-qp "asset_tag__isw" $asset_tag_isw "scalar") (serialize-qp "asset_tag__nisw" $asset_tag_nisw "scalar") (serialize-qp "asset_tag__ie" $asset_tag_ie "scalar") (serialize-qp "asset_tag__nie" $asset_tag_nie "scalar") (serialize-qp "asset_tag__empty" $asset_tag_empty "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "manufacturer_id__n" $manufacturer_id_n "scalar") (serialize-qp "manufacturer__n" $manufacturer_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "component_type__n" $component_type_n "scalar") (serialize-qp "component_id__n" $component_id_n "scalar") (serialize-qp "component_id__lte" $component_id_lte "scalar") (serialize-qp "component_id__lt" $component_id_lt "scalar") (serialize-qp "component_id__gte" $component_id_gte "scalar") (serialize-qp "component_id__gt" $component_id_gt "scalar") (serialize-qp "serial__n" $serial_n "scalar") (serialize-qp "serial__ic" $serial_ic "scalar") (serialize-qp "serial__nic" $serial_nic "scalar") (serialize-qp "serial__iew" $serial_iew "scalar") (serialize-qp "serial__niew" $serial_niew "scalar") (serialize-qp "serial__isw" $serial_isw "scalar") (serialize-qp "serial__nisw" $serial_nisw "scalar") (serialize-qp "serial__ie" $serial_ie "scalar") (serialize-qp "serial__nie" $serial_nie "scalar") (serialize-qp "serial__empty" $serial_empty "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/inventory-items/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "part_id": $part_id, "asset_tag": $asset_tag, "discovered": $discovered, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "created": $created, "last_updated": $last_updated, "tag": $tag, "parent_id": $parent_id, "manufacturer_id": $manufacturer_id, "manufacturer": $manufacturer, "role_id": $role_id, "role": $role, "component_type": $component_type, "component_id": $component_id, "serial": $serial, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "part_id__n": $part_id_n, "part_id__ic": $part_id_ic, "part_id__nic": $part_id_nic, "part_id__iew": $part_id_iew, "part_id__niew": $part_id_niew, "part_id__isw": $part_id_isw, "part_id__nisw": $part_id_nisw, "part_id__ie": $part_id_ie, "part_id__nie": $part_id_nie, "part_id__empty": $part_id_empty, "asset_tag__n": $asset_tag_n, "asset_tag__ic": $asset_tag_ic, "asset_tag__nic": $asset_tag_nic, "asset_tag__iew": $asset_tag_iew, "asset_tag__niew": $asset_tag_niew, "asset_tag__isw": $asset_tag_isw, "asset_tag__nisw": $asset_tag_nisw, "asset_tag__ie": $asset_tag_ie, "asset_tag__nie": $asset_tag_nie, "asset_tag__empty": $asset_tag_empty, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "parent_id__n": $parent_id_n, "manufacturer_id__n": $manufacturer_id_n, "manufacturer__n": $manufacturer_n, "role_id__n": $role_id_n, "role__n": $role_n, "component_type__n": $component_type_n, "component_id__n": $component_id_n, "component_id__lte": $component_id_lte, "component_id__lt": $component_id_lt, "component_id__gte": $component_id_gte, "component_id__gt": $component_id_gt, "serial__n": $serial_n, "serial__ic": $serial_ic, "serial__nic": $serial_nic, "serial__iew": $serial_iew, "serial__niew": $serial_niew, "serial__isw": $serial_isw, "serial__nisw": $serial_nisw, "serial__ie": $serial_ie, "serial__nie": $serial_nie, "serial__empty": $serial_empty, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/inventory-items/
#
# operationId: dcim_inventory-items_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-items update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this item (nullable)
  --component-id: int # nullable
  --component-type: string # nullable
  --custom-fields: record # default: {}
  --description: string
  device: int
  --discovered: oneof<nothing, bool> # This item was automatically discovered
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
  --serial: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, asset_tag: string, component: record, component_id: int, component_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, discovered: bool, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, serial: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-items/")
  let req_body = {"asset_tag": $asset_tag, "component_id": $component_id, "component_type": $component_type, "custom_fields": $custom_fields, "description": $description, "device": $device, "discovered": $discovered, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role, "serial": $serial, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/inventory-items/
#
# operationId: dcim_inventory-items_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-items create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this item (nullable)
  --component-id: int # nullable
  --component-type: string # nullable
  --custom-fields: record # default: {}
  --description: string
  device: int
  --discovered: oneof<nothing, bool> # This item was automatically discovered
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
  --serial: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, asset_tag: string, component: record, component_id: int, component_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, discovered: bool, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, serial: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-items/")
  let req_body = {"asset_tag": $asset_tag, "component_id": $component_id, "component_type": $component_type, "custom_fields": $custom_fields, "description": $description, "device": $device, "discovered": $discovered, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role, "serial": $serial, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/inventory-items/
#
# operationId: dcim_inventory-items_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-items update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this item (nullable)
  --component-id: int # nullable
  --component-type: string # nullable
  --custom-fields: record # default: {}
  --description: string
  device: int
  --discovered: oneof<nothing, bool> # This item was automatically discovered
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
  --serial: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, asset_tag: string, component: record, component_id: int, component_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, discovered: bool, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, serial: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/inventory-items/")
  let req_body = {"asset_tag": $asset_tag, "component_id": $component_id, "component_type": $component_type, "custom_fields": $custom_fields, "description": $description, "device": $device, "discovered": $discovered, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role, "serial": $serial, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/inventory-items/{id}/
#
# operationId: dcim_inventory-items_delete
export def "dcim-inventory-items delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-items/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/inventory-items/{id}/
#
# operationId: dcim_inventory-items_read
export def "dcim-inventory-items get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, asset_tag: string, component: record, component_id: int, component_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, discovered: bool, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, serial: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-items/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/inventory-items/{id}/
#
# operationId: dcim_inventory-items_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-items update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this item (nullable)
  --component-id: int # nullable
  --component-type: string # nullable
  --custom-fields: record # default: {}
  --description: string
  device: int
  --discovered: oneof<nothing, bool> # This item was automatically discovered
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
  --serial: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, asset_tag: string, component: record, component_id: int, component_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, discovered: bool, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, serial: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-items/{id}/"))
  let req_body = {"asset_tag": $asset_tag, "component_id": $component_id, "component_type": $component_type, "custom_fields": $custom_fields, "description": $description, "device": $device, "discovered": $discovered, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role, "serial": $serial, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/inventory-items/{id}/
#
# operationId: dcim_inventory-items_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-inventory-items update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this item (nullable)
  --component-id: int # nullable
  --component-type: string # nullable
  --custom-fields: record # default: {}
  --description: string
  device: int
  --discovered: oneof<nothing, bool> # This item was automatically discovered
  --label: string # Physical label
  --manufacturer: int # nullable
  name: string
  --parent: int # nullable
  --part-id: string # Manufacturer-assigned part identifier
  --role: int # nullable
  --serial: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, asset_tag: string, component: record, component_id: int, component_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, discovered: bool, display: string, id: int, label: string, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, parent: int, part_id: string, role: record<display: string, id: int, inventoryitem_count: int, name: string, slug: string, url: string>, serial: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/inventory-items/{id}/"))
  let req_body = {"asset_tag": $asset_tag, "component_id": $component_id, "component_type": $component_type, "custom_fields": $custom_fields, "description": $description, "device": $device, "discovered": $discovered, "label": $label, "manufacturer": $manufacturer, "name": $name, "parent": $parent, "part_id": $part_id, "role": $role, "serial": $serial, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/locations/
#
# operationId: dcim_locations_bulk_delete
export def "dcim-locations delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/locations/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/locations/
#
# operationId: dcim_locations_list
export def "dcim-locations list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --status: string
  --description: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --parent-id: string
  --parent: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --status-n: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --parent-id-n: string
  --parent-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, parent: record, rack_count: int, site: record, slug: string, status: record, tags: list, tenant: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "parent" $parent "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "parent__n" $parent_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/locations/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "status": $status, "description": $description, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "parent_id": $parent_id, "parent": $parent, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "status__n": $status_n, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "parent_id__n": $parent_id_n, "parent__n": $parent_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/locations/
#
# operationId: dcim_locations_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-locations update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  site: int
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, rack_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/locations/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "site": $site, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/locations/
#
# operationId: dcim_locations_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-locations create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  site: int
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, rack_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/locations/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "site": $site, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/locations/
#
# operationId: dcim_locations_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-locations update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  site: int
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, rack_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/locations/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "site": $site, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/locations/{id}/
#
# operationId: dcim_locations_delete
export def "dcim-locations delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/locations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/locations/{id}/
#
# operationId: dcim_locations_read
export def "dcim-locations get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, rack_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/locations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/locations/{id}/
#
# operationId: dcim_locations_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-locations update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  site: int
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, rack_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/locations/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "site": $site, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/locations/{id}/
#
# operationId: dcim_locations_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-locations update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  site: int
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, rack_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/locations/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "site": $site, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/manufacturers/
#
# operationId: dcim_manufacturers_bulk_delete
export def "dcim-manufacturers delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/manufacturers/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/manufacturers/
#
# operationId: dcim_manufacturers_list
export def "dcim-manufacturers list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, custom_fields: record, description: string, devicetype_count: int, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, platform_count: int, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/manufacturers/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/manufacturers/
#
# operationId: dcim_manufacturers_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-manufacturers update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, devicetype_count: int, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, platform_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/manufacturers/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/manufacturers/
#
# operationId: dcim_manufacturers_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-manufacturers create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, devicetype_count: int, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, platform_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/manufacturers/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/manufacturers/
#
# operationId: dcim_manufacturers_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-manufacturers update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, devicetype_count: int, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, platform_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/manufacturers/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/manufacturers/{id}/
#
# operationId: dcim_manufacturers_delete
export def "dcim-manufacturers delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/manufacturers/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/manufacturers/{id}/
#
# operationId: dcim_manufacturers_read
export def "dcim-manufacturers get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, custom_fields: record, description: string, devicetype_count: int, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, platform_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/manufacturers/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/manufacturers/{id}/
#
# operationId: dcim_manufacturers_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-manufacturers update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, devicetype_count: int, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, platform_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/manufacturers/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/manufacturers/{id}/
#
# operationId: dcim_manufacturers_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-manufacturers update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, devicetype_count: int, display: string, id: int, inventoryitem_count: int, last_updated: string, name: string, platform_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/manufacturers/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/module-bay-templates/
#
# operationId: dcim_module-bay-templates_bulk_delete
export def "dcim-module-bay-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bay-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/module-bay-templates/
#
# operationId: dcim_module-bay-templates_list
export def "dcim-module-bay-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, name: string, position: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/module-bay-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/module-bay-templates/
#
# operationId: dcim_module-bay-templates_bulk_partial_update
export def "dcim-module-bay-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --position: string # Identifier to reference when renaming installed components
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, position: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bay-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name, "position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/module-bay-templates/
#
# operationId: dcim_module-bay-templates_create
export def "dcim-module-bay-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --position: string # Identifier to reference when renaming installed components
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, position: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bay-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name, "position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/module-bay-templates/
#
# operationId: dcim_module-bay-templates_bulk_update
export def "dcim-module-bay-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --position: string # Identifier to reference when renaming installed components
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, position: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bay-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name, "position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/module-bay-templates/{id}/
#
# operationId: dcim_module-bay-templates_delete
export def "dcim-module-bay-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bay-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/module-bay-templates/{id}/
#
# operationId: dcim_module-bay-templates_read
export def "dcim-module-bay-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, position: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bay-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/module-bay-templates/{id}/
#
# operationId: dcim_module-bay-templates_partial_update
export def "dcim-module-bay-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --position: string # Identifier to reference when renaming installed components
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, position: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bay-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name, "position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/module-bay-templates/{id}/
#
# operationId: dcim_module-bay-templates_update
export def "dcim-module-bay-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  device_type: int
  --label: string # Physical label
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --position: string # Identifier to reference when renaming installed components
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, name: string, position: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bay-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "label": $label, "name": $name, "position": $position} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/module-bays/
#
# operationId: dcim_module-bays_bulk_delete
export def "dcim-module-bays delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bays/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/module-bays/
#
# operationId: dcim_module-bays_list
export def "dcim-module-bays list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --description: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --created: string
  --last-updated: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, custom_fields: record, description: string, device: record, display: string, id: int, installed_module: record, label: string, last_updated: string, name: string, position: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/module-bays/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "description": $description, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "created": $created, "last_updated": $last_updated, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/module-bays/
#
# operationId: dcim_module-bays_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-bays update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  installed_module: int
  --label: string # Physical label
  name: string
  --position: string # Identifier to reference when renaming installed components
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_module: record<display: string, id: int, serial: string, url: string>, label: string, last_updated: string, name: string, position: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bays/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_module": $installed_module, "label": $label, "name": $name, "position": $position, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/module-bays/
#
# operationId: dcim_module-bays_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-bays create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  installed_module: int
  --label: string # Physical label
  name: string
  --position: string # Identifier to reference when renaming installed components
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_module: record<display: string, id: int, serial: string, url: string>, label: string, last_updated: string, name: string, position: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bays/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_module": $installed_module, "label": $label, "name": $name, "position": $position, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/module-bays/
#
# operationId: dcim_module-bays_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-bays update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  installed_module: int
  --label: string # Physical label
  name: string
  --position: string # Identifier to reference when renaming installed components
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_module: record<display: string, id: int, serial: string, url: string>, label: string, last_updated: string, name: string, position: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-bays/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_module": $installed_module, "label": $label, "name": $name, "position": $position, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/module-bays/{id}/
#
# operationId: dcim_module-bays_delete
export def "dcim-module-bays delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bays/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/module-bays/{id}/
#
# operationId: dcim_module-bays_read
export def "dcim-module-bays get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_module: record<display: string, id: int, serial: string, url: string>, label: string, last_updated: string, name: string, position: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bays/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/module-bays/{id}/
#
# operationId: dcim_module-bays_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-bays update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  installed_module: int
  --label: string # Physical label
  name: string
  --position: string # Identifier to reference when renaming installed components
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_module: record<display: string, id: int, serial: string, url: string>, label: string, last_updated: string, name: string, position: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bays/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_module": $installed_module, "label": $label, "name": $name, "position": $position, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/module-bays/{id}/
#
# operationId: dcim_module-bays_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-bays update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  device: int
  installed_module: int
  --label: string # Physical label
  name: string
  --position: string # Identifier to reference when renaming installed components
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, installed_module: record<display: string, id: int, serial: string, url: string>, label: string, last_updated: string, name: string, position: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-bays/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "device": $device, "installed_module": $installed_module, "label": $label, "name": $name, "position": $position, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/module-types/
#
# operationId: dcim_module-types_bulk_delete
export def "dcim-module-types delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-types/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/module-types/
#
# operationId: dcim_module-types_list
export def "dcim-module-types list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --model: string
  --part-number: string
  --weight: string
  --weight-unit: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --manufacturer-id: string
  --manufacturer: string
  --console-ports: string
  --console-server-ports: string
  --power-ports: string
  --power-outlets: string
  --interfaces: string
  --pass-through-ports: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --model-n: string
  --model-ic: string
  --model-nic: string
  --model-iew: string
  --model-niew: string
  --model-isw: string
  --model-nisw: string
  --model-ie: string
  --model-nie: string
  --model-empty: string
  --part-number-n: string
  --part-number-ic: string
  --part-number-nic: string
  --part-number-iew: string
  --part-number-niew: string
  --part-number-isw: string
  --part-number-nisw: string
  --part-number-ie: string
  --part-number-nie: string
  --part-number-empty: string
  --weight-n: string
  --weight-lte: string
  --weight-lt: string
  --weight-gte: string
  --weight-gt: string
  --weight-unit-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --manufacturer-id-n: string
  --manufacturer-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, manufacturer: record, model: string, part_number: string, tags: list, url: string, weight: float, weight_unit: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "model" $model "scalar") (serialize-qp "part_number" $part_number "scalar") (serialize-qp "weight" $weight "scalar") (serialize-qp "weight_unit" $weight_unit "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "manufacturer_id" $manufacturer_id "scalar") (serialize-qp "manufacturer" $manufacturer "scalar") (serialize-qp "console_ports" $console_ports "scalar") (serialize-qp "console_server_ports" $console_server_ports "scalar") (serialize-qp "power_ports" $power_ports "scalar") (serialize-qp "power_outlets" $power_outlets "scalar") (serialize-qp "interfaces" $interfaces "scalar") (serialize-qp "pass_through_ports" $pass_through_ports "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "model__n" $model_n "scalar") (serialize-qp "model__ic" $model_ic "scalar") (serialize-qp "model__nic" $model_nic "scalar") (serialize-qp "model__iew" $model_iew "scalar") (serialize-qp "model__niew" $model_niew "scalar") (serialize-qp "model__isw" $model_isw "scalar") (serialize-qp "model__nisw" $model_nisw "scalar") (serialize-qp "model__ie" $model_ie "scalar") (serialize-qp "model__nie" $model_nie "scalar") (serialize-qp "model__empty" $model_empty "scalar") (serialize-qp "part_number__n" $part_number_n "scalar") (serialize-qp "part_number__ic" $part_number_ic "scalar") (serialize-qp "part_number__nic" $part_number_nic "scalar") (serialize-qp "part_number__iew" $part_number_iew "scalar") (serialize-qp "part_number__niew" $part_number_niew "scalar") (serialize-qp "part_number__isw" $part_number_isw "scalar") (serialize-qp "part_number__nisw" $part_number_nisw "scalar") (serialize-qp "part_number__ie" $part_number_ie "scalar") (serialize-qp "part_number__nie" $part_number_nie "scalar") (serialize-qp "part_number__empty" $part_number_empty "scalar") (serialize-qp "weight__n" $weight_n "scalar") (serialize-qp "weight__lte" $weight_lte "scalar") (serialize-qp "weight__lt" $weight_lt "scalar") (serialize-qp "weight__gte" $weight_gte "scalar") (serialize-qp "weight__gt" $weight_gt "scalar") (serialize-qp "weight_unit__n" $weight_unit_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "manufacturer_id__n" $manufacturer_id_n "scalar") (serialize-qp "manufacturer__n" $manufacturer_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/module-types/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "model": $model, "part_number": $part_number, "weight": $weight, "weight_unit": $weight_unit, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "manufacturer_id": $manufacturer_id, "manufacturer": $manufacturer, "console_ports": $console_ports, "console_server_ports": $console_server_ports, "power_ports": $power_ports, "power_outlets": $power_outlets, "interfaces": $interfaces, "pass_through_ports": $pass_through_ports, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "model__n": $model_n, "model__ic": $model_ic, "model__nic": $model_nic, "model__iew": $model_iew, "model__niew": $model_niew, "model__isw": $model_isw, "model__nisw": $model_nisw, "model__ie": $model_ie, "model__nie": $model_nie, "model__empty": $model_empty, "part_number__n": $part_number_n, "part_number__ic": $part_number_ic, "part_number__nic": $part_number_nic, "part_number__iew": $part_number_iew, "part_number__niew": $part_number_niew, "part_number__isw": $part_number_isw, "part_number__nisw": $part_number_nisw, "part_number__ie": $part_number_ie, "part_number__nie": $part_number_nie, "part_number__empty": $part_number_empty, "weight__n": $weight_n, "weight__lte": $weight_lte, "weight__lt": $weight_lt, "weight__gte": $weight_gte, "weight__gt": $weight_gt, "weight_unit__n": $weight_unit_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "manufacturer_id__n": $manufacturer_id_n, "manufacturer__n": $manufacturer_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/module-types/
#
# operationId: dcim_module-types_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-types update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-types/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "tags": $tags, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/module-types/
#
# operationId: dcim_module-types_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-types create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-types/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "tags": $tags, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/module-types/
#
# operationId: dcim_module-types_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-types update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/module-types/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "tags": $tags, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/module-types/{id}/
#
# operationId: dcim_module-types_delete
export def "dcim-module-types delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/module-types/{id}/
#
# operationId: dcim_module-types_read
export def "dcim-module-types get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/module-types/{id}/
#
# operationId: dcim_module-types_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-types update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-types/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "tags": $tags, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/module-types/{id}/
#
# operationId: dcim_module-types_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-module-types update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  manufacturer: int
  model: string
  --part-number: string # Discrete part number (optional)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, part_number: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, weight: float, weight_unit: record<label: string, value: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/module-types/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "model": $model, "part_number": $part_number, "tags": $tags, "weight": $weight, "weight_unit": $weight_unit} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/modules/
#
# operationId: dcim_modules_bulk_delete
export def "dcim-modules delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/modules/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/modules/
#
# operationId: dcim_modules_list
export def "dcim-modules list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --status: string
  --asset-tag: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --manufacturer-id: string
  --manufacturer: string
  --module-type-id: string
  --module-type: string
  --module-bay-id: string
  --device-id: string
  --serial: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --status-n: string
  --asset-tag-n: string
  --asset-tag-ic: string
  --asset-tag-nic: string
  --asset-tag-iew: string
  --asset-tag-niew: string
  --asset-tag-isw: string
  --asset-tag-nisw: string
  --asset-tag-ie: string
  --asset-tag-nie: string
  --asset-tag-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --manufacturer-id-n: string
  --manufacturer-n: string
  --module-type-id-n: string
  --module-type-n: string
  --module-bay-id-n: string
  --device-id-n: string
  --serial-n: string
  --serial-ic: string
  --serial-nic: string
  --serial-iew: string
  --serial-niew: string
  --serial-isw: string
  --serial-nisw: string
  --serial-ie: string
  --serial-nie: string
  --serial-empty: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<asset_tag: string, comments: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, last_updated: string, module_bay: record, module_type: record, serial: string, status: record, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "asset_tag" $asset_tag "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "manufacturer_id" $manufacturer_id "scalar") (serialize-qp "manufacturer" $manufacturer "scalar") (serialize-qp "module_type_id" $module_type_id "scalar") (serialize-qp "module_type" $module_type "scalar") (serialize-qp "module_bay_id" $module_bay_id "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "serial" $serial "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "asset_tag__n" $asset_tag_n "scalar") (serialize-qp "asset_tag__ic" $asset_tag_ic "scalar") (serialize-qp "asset_tag__nic" $asset_tag_nic "scalar") (serialize-qp "asset_tag__iew" $asset_tag_iew "scalar") (serialize-qp "asset_tag__niew" $asset_tag_niew "scalar") (serialize-qp "asset_tag__isw" $asset_tag_isw "scalar") (serialize-qp "asset_tag__nisw" $asset_tag_nisw "scalar") (serialize-qp "asset_tag__ie" $asset_tag_ie "scalar") (serialize-qp "asset_tag__nie" $asset_tag_nie "scalar") (serialize-qp "asset_tag__empty" $asset_tag_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "manufacturer_id__n" $manufacturer_id_n "scalar") (serialize-qp "manufacturer__n" $manufacturer_n "scalar") (serialize-qp "module_type_id__n" $module_type_id_n "scalar") (serialize-qp "module_type__n" $module_type_n "scalar") (serialize-qp "module_bay_id__n" $module_bay_id_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "serial__n" $serial_n "scalar") (serialize-qp "serial__ic" $serial_ic "scalar") (serialize-qp "serial__nic" $serial_nic "scalar") (serialize-qp "serial__iew" $serial_iew "scalar") (serialize-qp "serial__niew" $serial_niew "scalar") (serialize-qp "serial__isw" $serial_isw "scalar") (serialize-qp "serial__nisw" $serial_nisw "scalar") (serialize-qp "serial__ie" $serial_ie "scalar") (serialize-qp "serial__nie" $serial_nie "scalar") (serialize-qp "serial__empty" $serial_empty "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/modules/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "status": $status, "asset_tag": $asset_tag, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "manufacturer_id": $manufacturer_id, "manufacturer": $manufacturer, "module_type_id": $module_type_id, "module_type": $module_type, "module_bay_id": $module_bay_id, "device_id": $device_id, "serial": $serial, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "status__n": $status_n, "asset_tag__n": $asset_tag_n, "asset_tag__ic": $asset_tag_ic, "asset_tag__nic": $asset_tag_nic, "asset_tag__iew": $asset_tag_iew, "asset_tag__niew": $asset_tag_niew, "asset_tag__isw": $asset_tag_isw, "asset_tag__nisw": $asset_tag_nisw, "asset_tag__ie": $asset_tag_ie, "asset_tag__nie": $asset_tag_nie, "asset_tag__empty": $asset_tag_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "manufacturer_id__n": $manufacturer_id_n, "manufacturer__n": $manufacturer_n, "module_type_id__n": $module_type_id_n, "module_type__n": $module_type_n, "module_bay_id__n": $module_bay_id_n, "device_id__n": $device_id_n, "serial__n": $serial_n, "serial__ic": $serial_ic, "serial__nic": $serial_nic, "serial__iew": $serial_iew, "serial__niew": $serial_niew, "serial__isw": $serial_isw, "serial__nisw": $serial_nisw, "serial__ie": $serial_ie, "serial__nie": $serial_nie, "serial__empty": $serial_empty, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/modules/
#
# operationId: dcim_modules_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-modules update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  module_bay: int
  module_type: int
  --serial: string
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, last_updated: string, module_bay: record<display: string, id: int, module: record<device: record, display: string, id: int, module_bay: record, module_type: record, url: string>, name: string, url: string>, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, serial: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/modules/")
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "module_bay": $module_bay, "module_type": $module_type, "serial": $serial, "status": $status, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/modules/
#
# operationId: dcim_modules_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-modules create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  module_bay: int
  module_type: int
  --serial: string
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, last_updated: string, module_bay: record<display: string, id: int, module: record<device: record, display: string, id: int, module_bay: record, module_type: record, url: string>, name: string, url: string>, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, serial: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/modules/")
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "module_bay": $module_bay, "module_type": $module_type, "serial": $serial, "status": $status, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/modules/
#
# operationId: dcim_modules_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-modules update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  module_bay: int
  module_type: int
  --serial: string
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, last_updated: string, module_bay: record<display: string, id: int, module: record<device: record, display: string, id: int, module_bay: record, module_type: record, url: string>, name: string, url: string>, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, serial: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/modules/")
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "module_bay": $module_bay, "module_type": $module_type, "serial": $serial, "status": $status, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/modules/{id}/
#
# operationId: dcim_modules_delete
export def "dcim-modules delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/modules/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/modules/{id}/
#
# operationId: dcim_modules_read
export def "dcim-modules get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<asset_tag: string, comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, last_updated: string, module_bay: record<display: string, id: int, module: record<device: record, display: string, id: int, module_bay: record, module_type: record, url: string>, name: string, url: string>, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, serial: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/modules/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/modules/{id}/
#
# operationId: dcim_modules_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-modules update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  module_bay: int
  module_type: int
  --serial: string
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, last_updated: string, module_bay: record<display: string, id: int, module: record<device: record, display: string, id: int, module_bay: record, module_type: record, url: string>, name: string, url: string>, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, serial: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/modules/{id}/"))
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "module_bay": $module_bay, "module_type": $module_type, "serial": $serial, "status": $status, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/modules/{id}/
#
# operationId: dcim_modules_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-modules update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this device (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  module_bay: int
  module_type: int
  --serial: string
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, last_updated: string, module_bay: record<display: string, id: int, module: record<device: record, display: string, id: int, module_bay: record, module_type: record, url: string>, name: string, url: string>, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, serial: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/modules/{id}/"))
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "module_bay": $module_bay, "module_type": $module_type, "serial": $serial, "status": $status, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/platforms/
#
# operationId: dcim_platforms_bulk_delete
export def "dcim-platforms delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/platforms/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/platforms/
#
# operationId: dcim_platforms_list
export def "dcim-platforms list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --napalm-driver: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --manufacturer-id: string
  --manufacturer: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --napalm-driver-n: string
  --napalm-driver-ic: string
  --napalm-driver-nic: string
  --napalm-driver-iew: string
  --napalm-driver-niew: string
  --napalm-driver-isw: string
  --napalm-driver-nisw: string
  --napalm-driver-ie: string
  --napalm-driver-nie: string
  --napalm-driver-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --manufacturer-id-n: string
  --manufacturer-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, manufacturer: record, name: string, napalm_args: record, napalm_driver: string, slug: string, tags: list, url: string, virtualmachine_count: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "napalm_driver" $napalm_driver "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "manufacturer_id" $manufacturer_id "scalar") (serialize-qp "manufacturer" $manufacturer "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "napalm_driver__n" $napalm_driver_n "scalar") (serialize-qp "napalm_driver__ic" $napalm_driver_ic "scalar") (serialize-qp "napalm_driver__nic" $napalm_driver_nic "scalar") (serialize-qp "napalm_driver__iew" $napalm_driver_iew "scalar") (serialize-qp "napalm_driver__niew" $napalm_driver_niew "scalar") (serialize-qp "napalm_driver__isw" $napalm_driver_isw "scalar") (serialize-qp "napalm_driver__nisw" $napalm_driver_nisw "scalar") (serialize-qp "napalm_driver__ie" $napalm_driver_ie "scalar") (serialize-qp "napalm_driver__nie" $napalm_driver_nie "scalar") (serialize-qp "napalm_driver__empty" $napalm_driver_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "manufacturer_id__n" $manufacturer_id_n "scalar") (serialize-qp "manufacturer__n" $manufacturer_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/platforms/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "napalm_driver": $napalm_driver, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "manufacturer_id": $manufacturer_id, "manufacturer": $manufacturer, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "napalm_driver__n": $napalm_driver_n, "napalm_driver__ic": $napalm_driver_ic, "napalm_driver__nic": $napalm_driver_nic, "napalm_driver__iew": $napalm_driver_iew, "napalm_driver__niew": $napalm_driver_niew, "napalm_driver__isw": $napalm_driver_isw, "napalm_driver__nisw": $napalm_driver_nisw, "napalm_driver__ie": $napalm_driver_ie, "napalm_driver__nie": $napalm_driver_nie, "napalm_driver__empty": $napalm_driver_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "manufacturer_id__n": $manufacturer_id_n, "manufacturer__n": $manufacturer_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/platforms/
#
# operationId: dcim_platforms_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-platforms update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --manufacturer: int # Optionally limit this platform to devices of a certain manufacturer (nullable)
  name: string
  --napalm-args: record # Additional arguments to pass when initiating the NAPALM driver (JSON format) (nullable)
  --napalm-driver: string # The name of the NAPALM driver to use when interacting with devices
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, napalm_args: record, napalm_driver: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/platforms/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "name": $name, "napalm_args": $napalm_args, "napalm_driver": $napalm_driver, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/platforms/
#
# operationId: dcim_platforms_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-platforms create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --manufacturer: int # Optionally limit this platform to devices of a certain manufacturer (nullable)
  name: string
  --napalm-args: record # Additional arguments to pass when initiating the NAPALM driver (JSON format) (nullable)
  --napalm-driver: string # The name of the NAPALM driver to use when interacting with devices
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, napalm_args: record, napalm_driver: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/platforms/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "name": $name, "napalm_args": $napalm_args, "napalm_driver": $napalm_driver, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/platforms/
#
# operationId: dcim_platforms_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-platforms update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --manufacturer: int # Optionally limit this platform to devices of a certain manufacturer (nullable)
  name: string
  --napalm-args: record # Additional arguments to pass when initiating the NAPALM driver (JSON format) (nullable)
  --napalm-driver: string # The name of the NAPALM driver to use when interacting with devices
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, napalm_args: record, napalm_driver: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/platforms/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "name": $name, "napalm_args": $napalm_args, "napalm_driver": $napalm_driver, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/platforms/{id}/
#
# operationId: dcim_platforms_delete
export def "dcim-platforms delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/platforms/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/platforms/{id}/
#
# operationId: dcim_platforms_read
export def "dcim-platforms get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, napalm_args: record, napalm_driver: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/platforms/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/platforms/{id}/
#
# operationId: dcim_platforms_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-platforms update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --manufacturer: int # Optionally limit this platform to devices of a certain manufacturer (nullable)
  name: string
  --napalm-args: record # Additional arguments to pass when initiating the NAPALM driver (JSON format) (nullable)
  --napalm-driver: string # The name of the NAPALM driver to use when interacting with devices
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, napalm_args: record, napalm_driver: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/platforms/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "name": $name, "napalm_args": $napalm_args, "napalm_driver": $napalm_driver, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/platforms/{id}/
#
# operationId: dcim_platforms_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-platforms update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --manufacturer: int # Optionally limit this platform to devices of a certain manufacturer (nullable)
  name: string
  --napalm-args: record # Additional arguments to pass when initiating the NAPALM driver (JSON format) (nullable)
  --napalm-driver: string # The name of the NAPALM driver to use when interacting with devices
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, device_count: int, display: string, id: int, last_updated: string, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, name: string, napalm_args: record, napalm_driver: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/platforms/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "manufacturer": $manufacturer, "name": $name, "napalm_args": $napalm_args, "napalm_driver": $napalm_driver, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-feeds/
#
# operationId: dcim_power-feeds_bulk_delete
export def "dcim-power-feeds delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-feeds/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-feeds/
#
# operationId: dcim_power-feeds_list
export def "dcim-power-feeds list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --status: string
  --type: string
  --supply: string
  --phase: string
  --voltage: string
  --amperage: string
  --max-utilization: string
  --cable-end: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --cabled: string
  --occupied: string
  --connected: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --power-panel-id: string
  --rack-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --status-n: string
  --type-n: string
  --supply-n: string
  --phase-n: string
  --voltage-n: string
  --voltage-lte: string
  --voltage-lt: string
  --voltage-gte: string
  --voltage-gt: string
  --amperage-n: string
  --amperage-lte: string
  --amperage-lt: string
  --amperage-gte: string
  --amperage-gt: string
  --max-utilization-n: string
  --max-utilization-lte: string
  --max-utilization-lt: string
  --max-utilization-gte: string
  --max-utilization-gt: string
  --cable-end-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --power-panel-id-n: string
  --rack-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, amperage: int, cable: record, cable_end: string, comments: string, connected_endpoints: list, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record, power_panel: record, rack: record, status: record, supply: record, tags: list, type: record, url: string, voltage: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "supply" $supply "scalar") (serialize-qp "phase" $phase "scalar") (serialize-qp "voltage" $voltage "scalar") (serialize-qp "amperage" $amperage "scalar") (serialize-qp "max_utilization" $max_utilization "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "connected" $connected "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "power_panel_id" $power_panel_id "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "supply__n" $supply_n "scalar") (serialize-qp "phase__n" $phase_n "scalar") (serialize-qp "voltage__n" $voltage_n "scalar") (serialize-qp "voltage__lte" $voltage_lte "scalar") (serialize-qp "voltage__lt" $voltage_lt "scalar") (serialize-qp "voltage__gte" $voltage_gte "scalar") (serialize-qp "voltage__gt" $voltage_gt "scalar") (serialize-qp "amperage__n" $amperage_n "scalar") (serialize-qp "amperage__lte" $amperage_lte "scalar") (serialize-qp "amperage__lt" $amperage_lt "scalar") (serialize-qp "amperage__gte" $amperage_gte "scalar") (serialize-qp "amperage__gt" $amperage_gt "scalar") (serialize-qp "max_utilization__n" $max_utilization_n "scalar") (serialize-qp "max_utilization__lte" $max_utilization_lte "scalar") (serialize-qp "max_utilization__lt" $max_utilization_lt "scalar") (serialize-qp "max_utilization__gte" $max_utilization_gte "scalar") (serialize-qp "max_utilization__gt" $max_utilization_gt "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "power_panel_id__n" $power_panel_id_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/power-feeds/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "status": $status, "type": $type, "supply": $supply, "phase": $phase, "voltage": $voltage, "amperage": $amperage, "max_utilization": $max_utilization, "cable_end": $cable_end, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "connected": $connected, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "power_panel_id": $power_panel_id, "rack_id": $rack_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "status__n": $status_n, "type__n": $type_n, "supply__n": $supply_n, "phase__n": $phase_n, "voltage__n": $voltage_n, "voltage__lte": $voltage_lte, "voltage__lt": $voltage_lt, "voltage__gte": $voltage_gte, "voltage__gt": $voltage_gt, "amperage__n": $amperage_n, "amperage__lte": $amperage_lte, "amperage__lt": $amperage_lt, "amperage__gte": $amperage_gte, "amperage__gt": $amperage_gt, "max_utilization__n": $max_utilization_n, "max_utilization__lte": $max_utilization_lte, "max_utilization__lt": $max_utilization_lt, "max_utilization__gte": $max_utilization_gte, "max_utilization__gt": $max_utilization_gt, "cable_end__n": $cable_end_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "power_panel_id__n": $power_panel_id_n, "rack_id__n": $rack_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/power-feeds/
#
# operationId: dcim_power-feeds_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-feeds update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --amperage: int
  --cable: record # shape: {label?: string}
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --max-utilization: int # Maximum permissible draw (percentage)
  name: string
  --phase: string@phase-completer
  power_panel: int
  --rack: int # nullable
  --status: string@status-completer-5
  --supply: string@supply-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-4
  --voltage: int
]: any -> record<_occupied: bool, amperage: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, comments: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record<label: string, value: string>, power_panel: record<display: string, id: int, name: string, powerfeed_count: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, status: record<label: string, value: string>, supply: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string, voltage: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-feeds/")
  let req_body = {"amperage": $amperage, "cable": $cable, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "max_utilization": $max_utilization, "name": $name, "phase": $phase, "power_panel": $power_panel, "rack": $rack, "status": $status, "supply": $supply, "tags": $tags, "type": $type, "voltage": $voltage} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/power-feeds/
#
# operationId: dcim_power-feeds_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-feeds create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --amperage: int
  --cable: record # shape: {label?: string}
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --max-utilization: int # Maximum permissible draw (percentage)
  name: string
  --phase: string@phase-completer
  power_panel: int
  --rack: int # nullable
  --status: string@status-completer-5
  --supply: string@supply-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-4
  --voltage: int
]: any -> record<_occupied: bool, amperage: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, comments: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record<label: string, value: string>, power_panel: record<display: string, id: int, name: string, powerfeed_count: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, status: record<label: string, value: string>, supply: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string, voltage: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-feeds/")
  let req_body = {"amperage": $amperage, "cable": $cable, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "max_utilization": $max_utilization, "name": $name, "phase": $phase, "power_panel": $power_panel, "rack": $rack, "status": $status, "supply": $supply, "tags": $tags, "type": $type, "voltage": $voltage} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-feeds/
#
# operationId: dcim_power-feeds_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-feeds update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --amperage: int
  --cable: record # shape: {label?: string}
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --max-utilization: int # Maximum permissible draw (percentage)
  name: string
  --phase: string@phase-completer
  power_panel: int
  --rack: int # nullable
  --status: string@status-completer-5
  --supply: string@supply-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-4
  --voltage: int
]: any -> record<_occupied: bool, amperage: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, comments: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record<label: string, value: string>, power_panel: record<display: string, id: int, name: string, powerfeed_count: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, status: record<label: string, value: string>, supply: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string, voltage: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-feeds/")
  let req_body = {"amperage": $amperage, "cable": $cable, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "max_utilization": $max_utilization, "name": $name, "phase": $phase, "power_panel": $power_panel, "rack": $rack, "status": $status, "supply": $supply, "tags": $tags, "type": $type, "voltage": $voltage} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-feeds/{id}/
#
# operationId: dcim_power-feeds_delete
export def "dcim-power-feeds delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-feeds/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-feeds/{id}/
#
# operationId: dcim_power-feeds_read
export def "dcim-power-feeds get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, amperage: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, comments: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record<label: string, value: string>, power_panel: record<display: string, id: int, name: string, powerfeed_count: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, status: record<label: string, value: string>, supply: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string, voltage: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-feeds/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/power-feeds/{id}/
#
# operationId: dcim_power-feeds_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-feeds update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --amperage: int
  --cable: record # shape: {label?: string}
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --max-utilization: int # Maximum permissible draw (percentage)
  name: string
  --phase: string@phase-completer
  power_panel: int
  --rack: int # nullable
  --status: string@status-completer-5
  --supply: string@supply-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-4
  --voltage: int
]: any -> record<_occupied: bool, amperage: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, comments: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record<label: string, value: string>, power_panel: record<display: string, id: int, name: string, powerfeed_count: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, status: record<label: string, value: string>, supply: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string, voltage: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-feeds/{id}/"))
  let req_body = {"amperage": $amperage, "cable": $cable, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "max_utilization": $max_utilization, "name": $name, "phase": $phase, "power_panel": $power_panel, "rack": $rack, "status": $status, "supply": $supply, "tags": $tags, "type": $type, "voltage": $voltage} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-feeds/{id}/
#
# operationId: dcim_power-feeds_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-feeds update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --amperage: int
  --cable: record # shape: {label?: string}
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --max-utilization: int # Maximum permissible draw (percentage)
  name: string
  --phase: string@phase-completer
  power_panel: int
  --rack: int # nullable
  --status: string@status-completer-5
  --supply: string@supply-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-4
  --voltage: int
]: any -> record<_occupied: bool, amperage: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, comments: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record<label: string, value: string>, power_panel: record<display: string, id: int, name: string, powerfeed_count: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, status: record<label: string, value: string>, supply: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string, voltage: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-feeds/{id}/"))
  let req_body = {"amperage": $amperage, "cable": $cable, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "mark_connected": $mark_connected, "max_utilization": $max_utilization, "name": $name, "phase": $phase, "power_panel": $power_panel, "rack": $rack, "status": $status, "supply": $supply, "tags": $tags, "type": $type, "voltage": $voltage} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Trace a complete cable path and return each segment as a three-tuple of (termination, cable, termination).
#
# GET /dcim/power-feeds/{id}/trace/
# operationId: dcim_power-feeds_trace
export def "dcim-power-feeds-trace get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, amperage: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, comments: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, max_utilization: int, name: string, phase: record<label: string, value: string>, power_panel: record<display: string, id: int, name: string, powerfeed_count: int, url: string>, rack: record<device_count: int, display: string, id: int, name: string, url: string>, status: record<label: string, value: string>, supply: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string, voltage: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-feeds/{id}/trace/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/power-outlet-templates/
#
# operationId: dcim_power-outlet-templates_bulk_delete
export def "dcim-power-outlet-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlet-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-outlet-templates/
#
# operationId: dcim_power-outlet-templates_list
export def "dcim-power-outlet-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type: string
  --feed-leg: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --moduletype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --type-n: string
  --feed-leg-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --moduletype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, description: string, device_type: record, display: string, feed_leg: record, id: int, label: string, last_updated: string, module_type: record, name: string, power_port: record, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "feed_leg" $feed_leg "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "moduletype_id" $moduletype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "feed_leg__n" $feed_leg_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "moduletype_id__n" $moduletype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/power-outlet-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type": $type, "feed_leg": $feed_leg, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "moduletype_id": $moduletype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "type__n": $type_n, "feed_leg__n": $feed_leg_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "moduletype_id__n": $moduletype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/power-outlet-templates/
#
# operationId: dcim_power-outlet-templates_bulk_partial_update
export def "dcim-power-outlet-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --power-port: int # nullable
  --type: string@type-completer-5
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, power_port: record<display: string, id: int, name: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlet-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "feed_leg": $feed_leg, "label": $label, "module_type": $module_type, "name": $name, "power_port": $power_port, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/power-outlet-templates/
#
# operationId: dcim_power-outlet-templates_create
export def "dcim-power-outlet-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --power-port: int # nullable
  --type: string@type-completer-5
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, power_port: record<display: string, id: int, name: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlet-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "feed_leg": $feed_leg, "label": $label, "module_type": $module_type, "name": $name, "power_port": $power_port, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-outlet-templates/
#
# operationId: dcim_power-outlet-templates_bulk_update
export def "dcim-power-outlet-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --power-port: int # nullable
  --type: string@type-completer-5
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, power_port: record<display: string, id: int, name: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlet-templates/")
  let req_body = {"description": $description, "device_type": $device_type, "feed_leg": $feed_leg, "label": $label, "module_type": $module_type, "name": $name, "power_port": $power_port, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-outlet-templates/{id}/
#
# operationId: dcim_power-outlet-templates_delete
export def "dcim-power-outlet-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlet-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-outlet-templates/{id}/
#
# operationId: dcim_power-outlet-templates_read
export def "dcim-power-outlet-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, power_port: record<display: string, id: int, name: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlet-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/power-outlet-templates/{id}/
#
# operationId: dcim_power-outlet-templates_partial_update
export def "dcim-power-outlet-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --power-port: int # nullable
  --type: string@type-completer-5
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, power_port: record<display: string, id: int, name: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlet-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "feed_leg": $feed_leg, "label": $label, "module_type": $module_type, "name": $name, "power_port": $power_port, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-outlet-templates/{id}/
#
# operationId: dcim_power-outlet-templates_update
export def "dcim-power-outlet-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --description: string
  --device-type: int # nullable
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --power-port: int # nullable
  --type: string@type-completer-5
]: any -> record<created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, power_port: record<display: string, id: int, name: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlet-templates/{id}/"))
  let req_body = {"description": $description, "device_type": $device_type, "feed_leg": $feed_leg, "label": $label, "module_type": $module_type, "name": $name, "power_port": $power_port, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-outlets/
#
# operationId: dcim_power-outlets_bulk_delete
export def "dcim-power-outlets delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlets/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-outlets/
#
# operationId: dcim_power-outlets_list
export def "dcim-power-outlets list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --feed-leg: string
  --description: string
  --cable-end: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --module-id: string
  --created: string
  --last-updated: string
  --tag: string
  --cabled: string
  --occupied: string
  --connected: string
  --type: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --feed-leg-n: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --module-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, cable: record, cable_end: string, connected_endpoints: list, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record, display: string, feed_leg: record, id: int, label: string, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, module: record, name: string, power_port: record, tags: list, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "feed_leg" $feed_leg "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "module_id" $module_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "connected" $connected "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "feed_leg__n" $feed_leg_n "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "module_id__n" $module_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/power-outlets/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "feed_leg": $feed_leg, "description": $description, "cable_end": $cable_end, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "module_id": $module_id, "created": $created, "last_updated": $last_updated, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "connected": $connected, "type": $type, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "feed_leg__n": $feed_leg_n, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "module_id__n": $module_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "type__n": $type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/power-outlets/
#
# operationId: dcim_power-outlets_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-outlets update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --power-port: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-5 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, power_port: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlets/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "feed_leg": $feed_leg, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "power_port": $power_port, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/power-outlets/
#
# operationId: dcim_power-outlets_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-outlets create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --power-port: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-5 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, power_port: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlets/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "feed_leg": $feed_leg, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "power_port": $power_port, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-outlets/
#
# operationId: dcim_power-outlets_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-outlets update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --power-port: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-5 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, power_port: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-outlets/")
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "feed_leg": $feed_leg, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "power_port": $power_port, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-outlets/{id}/
#
# operationId: dcim_power-outlets_delete
export def "dcim-power-outlets delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlets/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-outlets/{id}/
#
# operationId: dcim_power-outlets_read
export def "dcim-power-outlets get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, power_port: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlets/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/power-outlets/{id}/
#
# operationId: dcim_power-outlets_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-outlets update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --power-port: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-5 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, power_port: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlets/{id}/"))
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "feed_leg": $feed_leg, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "power_port": $power_port, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-outlets/{id}/
#
# operationId: dcim_power-outlets_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-outlets update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --feed-leg: string@feed-leg-completer # Phase (for three-phase feeds)
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --power-port: int # nullable
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-5 # Physical port type
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, power_port: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlets/{id}/"))
  let req_body = {"cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "feed_leg": $feed_leg, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "power_port": $power_port, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Trace a complete cable path and return each segment as a three-tuple of (termination, cable, termination).
#
# GET /dcim/power-outlets/{id}/trace/
# operationId: dcim_power-outlets_trace
export def "dcim-power-outlets-trace get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, feed_leg: record<label: string, value: string>, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, power_port: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-outlets/{id}/trace/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/power-panels/
#
# operationId: dcim_power-panels_bulk_delete
export def "dcim-power-panels delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-panels/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-panels/
#
# operationId: dcim_power-panels_list
export def "dcim-power-panels list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, location: record, name: string, powerfeed_count: int, site: record, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/power-panels/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/power-panels/
#
# operationId: dcim_power-panels_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-panels update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --location: int # nullable
  name: string
  site: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, powerfeed_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-panels/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "location": $location, "name": $name, "site": $site, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/power-panels/
#
# operationId: dcim_power-panels_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-panels create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --location: int # nullable
  name: string
  site: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, powerfeed_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-panels/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "location": $location, "name": $name, "site": $site, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-panels/
#
# operationId: dcim_power-panels_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-panels update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --location: int # nullable
  name: string
  site: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, powerfeed_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-panels/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "location": $location, "name": $name, "site": $site, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-panels/{id}/
#
# operationId: dcim_power-panels_delete
export def "dcim-power-panels delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-panels/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-panels/{id}/
#
# operationId: dcim_power-panels_read
export def "dcim-power-panels get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, powerfeed_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-panels/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/power-panels/{id}/
#
# operationId: dcim_power-panels_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-panels update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --location: int # nullable
  name: string
  site: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, powerfeed_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-panels/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "location": $location, "name": $name, "site": $site, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-panels/{id}/
#
# operationId: dcim_power-panels_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-panels update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --location: int # nullable
  name: string
  site: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, powerfeed_count: int, site: record<display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-panels/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "location": $location, "name": $name, "site": $site, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-port-templates/
#
# operationId: dcim_power-port-templates_bulk_delete
export def "dcim-power-port-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-port-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-port-templates/
#
# operationId: dcim_power-port-templates_list
export def "dcim-power-port-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type: string
  --maximum-draw: string
  --allocated-draw: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --moduletype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --type-n: string
  --maximum-draw-n: string
  --maximum-draw-lte: string
  --maximum-draw-lt: string
  --maximum-draw-gte: string
  --maximum-draw-gt: string
  --allocated-draw-n: string
  --allocated-draw-lte: string
  --allocated-draw-lt: string
  --allocated-draw-gte: string
  --allocated-draw-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --moduletype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<allocated_draw: int, created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, maximum_draw: int, module_type: record, name: string, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "maximum_draw" $maximum_draw "scalar") (serialize-qp "allocated_draw" $allocated_draw "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "moduletype_id" $moduletype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "maximum_draw__n" $maximum_draw_n "scalar") (serialize-qp "maximum_draw__lte" $maximum_draw_lte "scalar") (serialize-qp "maximum_draw__lt" $maximum_draw_lt "scalar") (serialize-qp "maximum_draw__gte" $maximum_draw_gte "scalar") (serialize-qp "maximum_draw__gt" $maximum_draw_gt "scalar") (serialize-qp "allocated_draw__n" $allocated_draw_n "scalar") (serialize-qp "allocated_draw__lte" $allocated_draw_lte "scalar") (serialize-qp "allocated_draw__lt" $allocated_draw_lt "scalar") (serialize-qp "allocated_draw__gte" $allocated_draw_gte "scalar") (serialize-qp "allocated_draw__gt" $allocated_draw_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "moduletype_id__n" $moduletype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/power-port-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type": $type, "maximum_draw": $maximum_draw, "allocated_draw": $allocated_draw, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "moduletype_id": $moduletype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "type__n": $type_n, "maximum_draw__n": $maximum_draw_n, "maximum_draw__lte": $maximum_draw_lte, "maximum_draw__lt": $maximum_draw_lt, "maximum_draw__gte": $maximum_draw_gte, "maximum_draw__gt": $maximum_draw_gt, "allocated_draw__n": $allocated_draw_n, "allocated_draw__lte": $allocated_draw_lte, "allocated_draw__lt": $allocated_draw_lt, "allocated_draw__gte": $allocated_draw_gte, "allocated_draw__gt": $allocated_draw_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "moduletype_id__n": $moduletype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/power-port-templates/
#
# operationId: dcim_power-port-templates_bulk_partial_update
export def "dcim-power-port-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-6
]: any -> record<allocated_draw: int, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, maximum_draw: int, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-port-templates/")
  let req_body = {"allocated_draw": $allocated_draw, "description": $description, "device_type": $device_type, "label": $label, "maximum_draw": $maximum_draw, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/power-port-templates/
#
# operationId: dcim_power-port-templates_create
export def "dcim-power-port-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-6
]: any -> record<allocated_draw: int, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, maximum_draw: int, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-port-templates/")
  let req_body = {"allocated_draw": $allocated_draw, "description": $description, "device_type": $device_type, "label": $label, "maximum_draw": $maximum_draw, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-port-templates/
#
# operationId: dcim_power-port-templates_bulk_update
export def "dcim-power-port-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-6
]: any -> record<allocated_draw: int, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, maximum_draw: int, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-port-templates/")
  let req_body = {"allocated_draw": $allocated_draw, "description": $description, "device_type": $device_type, "label": $label, "maximum_draw": $maximum_draw, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-port-templates/{id}/
#
# operationId: dcim_power-port-templates_delete
export def "dcim-power-port-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-port-templates/{id}/
#
# operationId: dcim_power-port-templates_read
export def "dcim-power-port-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allocated_draw: int, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, maximum_draw: int, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/power-port-templates/{id}/
#
# operationId: dcim_power-port-templates_partial_update
export def "dcim-power-port-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-6
]: any -> record<allocated_draw: int, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, maximum_draw: int, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-port-templates/{id}/"))
  let req_body = {"allocated_draw": $allocated_draw, "description": $description, "device_type": $device_type, "label": $label, "maximum_draw": $maximum_draw, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-port-templates/{id}/
#
# operationId: dcim_power-port-templates_update
export def "dcim-power-port-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --type: string@type-completer-6
]: any -> record<allocated_draw: int, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, maximum_draw: int, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-port-templates/{id}/"))
  let req_body = {"allocated_draw": $allocated_draw, "description": $description, "device_type": $device_type, "label": $label, "maximum_draw": $maximum_draw, "module_type": $module_type, "name": $name, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-ports/
#
# operationId: dcim_power-ports_bulk_delete
export def "dcim-power-ports delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-ports/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-ports/
#
# operationId: dcim_power-ports_list
export def "dcim-power-ports list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --maximum-draw: string
  --allocated-draw: string
  --description: string
  --cable-end: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --module-id: string
  --created: string
  --last-updated: string
  --tag: string
  --cabled: string
  --occupied: string
  --connected: string
  --type: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --maximum-draw-n: string
  --maximum-draw-lte: string
  --maximum-draw-lt: string
  --maximum-draw-gte: string
  --maximum-draw-gt: string
  --allocated-draw-n: string
  --allocated-draw-lte: string
  --allocated-draw-lt: string
  --allocated-draw-gte: string
  --allocated-draw-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --module-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, allocated_draw: int, cable: record, cable_end: string, connected_endpoints: list, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, label: string, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record, name: string, tags: list, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "maximum_draw" $maximum_draw "scalar") (serialize-qp "allocated_draw" $allocated_draw "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "module_id" $module_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "connected" $connected "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "maximum_draw__n" $maximum_draw_n "scalar") (serialize-qp "maximum_draw__lte" $maximum_draw_lte "scalar") (serialize-qp "maximum_draw__lt" $maximum_draw_lt "scalar") (serialize-qp "maximum_draw__gte" $maximum_draw_gte "scalar") (serialize-qp "maximum_draw__gt" $maximum_draw_gt "scalar") (serialize-qp "allocated_draw__n" $allocated_draw_n "scalar") (serialize-qp "allocated_draw__lte" $allocated_draw_lte "scalar") (serialize-qp "allocated_draw__lt" $allocated_draw_lt "scalar") (serialize-qp "allocated_draw__gte" $allocated_draw_gte "scalar") (serialize-qp "allocated_draw__gt" $allocated_draw_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "module_id__n" $module_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/power-ports/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "maximum_draw": $maximum_draw, "allocated_draw": $allocated_draw, "description": $description, "cable_end": $cable_end, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "module_id": $module_id, "created": $created, "last_updated": $last_updated, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "connected": $connected, "type": $type, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "maximum_draw__n": $maximum_draw_n, "maximum_draw__lte": $maximum_draw_lte, "maximum_draw__lt": $maximum_draw_lt, "maximum_draw__gte": $maximum_draw_gte, "maximum_draw__gt": $maximum_draw_gt, "allocated_draw__n": $allocated_draw_n, "allocated_draw__lte": $allocated_draw_lte, "allocated_draw__lt": $allocated_draw_lt, "allocated_draw__gte": $allocated_draw_gte, "allocated_draw__gt": $allocated_draw_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "module_id__n": $module_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "type__n": $type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/power-ports/
#
# operationId: dcim_power-ports_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-ports update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --body-module: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-6 # Physical port type
]: any -> record<_occupied: bool, allocated_draw: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-ports/")
  let req_body = {"allocated_draw": $allocated_draw, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "maximum_draw": $maximum_draw, "module": $body_module, "name": $name, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/power-ports/
#
# operationId: dcim_power-ports_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-ports create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --body-module: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-6 # Physical port type
]: any -> record<_occupied: bool, allocated_draw: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-ports/")
  let req_body = {"allocated_draw": $allocated_draw, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "maximum_draw": $maximum_draw, "module": $body_module, "name": $name, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-ports/
#
# operationId: dcim_power-ports_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-ports update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --body-module: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-6 # Physical port type
]: any -> record<_occupied: bool, allocated_draw: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/power-ports/")
  let req_body = {"allocated_draw": $allocated_draw, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "maximum_draw": $maximum_draw, "module": $body_module, "name": $name, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/power-ports/{id}/
#
# operationId: dcim_power-ports_delete
export def "dcim-power-ports delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/power-ports/{id}/
#
# operationId: dcim_power-ports_read
export def "dcim-power-ports get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, allocated_draw: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/power-ports/{id}/
#
# operationId: dcim_power-ports_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-ports update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --body-module: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-6 # Physical port type
]: any -> record<_occupied: bool, allocated_draw: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-ports/{id}/"))
  let req_body = {"allocated_draw": $allocated_draw, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "maximum_draw": $maximum_draw, "module": $body_module, "name": $name, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/power-ports/{id}/
#
# operationId: dcim_power-ports_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-power-ports update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allocated-draw: int # Allocated power draw (watts) (nullable)
  --cable: record # shape: {label?: string}
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --maximum-draw: int # Maximum power draw (watts) (nullable)
  --body-module: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --type: string@type-completer-6 # Physical port type
]: any -> record<_occupied: bool, allocated_draw: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-ports/{id}/"))
  let req_body = {"allocated_draw": $allocated_draw, "cable": $cable, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "maximum_draw": $maximum_draw, "module": $body_module, "name": $name, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Trace a complete cable path and return each segment as a three-tuple of (termination, cable, termination).
#
# GET /dcim/power-ports/{id}/trace/
# operationId: dcim_power-ports_trace
export def "dcim-power-ports-trace get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, allocated_draw: int, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, connected_endpoints: list<string>, connected_endpoints_reachable: bool, connected_endpoints_type: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, maximum_draw: int, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/power-ports/{id}/trace/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/rack-reservations/
#
# operationId: dcim_rack-reservations_bulk_delete
export def "dcim-rack-reservations delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-reservations/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rack-reservations/
#
# operationId: dcim_rack-reservations_list
export def "dcim-rack-reservations list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --created: string
  --description: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --rack-id: string
  --site-id: string
  --site: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --location-id: string
  --location: string
  --user-id: string
  --user: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --rack-id-n: string
  --site-id-n: string
  --site-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --location-id-n: string
  --location-n: string
  --user-id-n: string
  --user-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, rack: record, tags: list, tenant: record, units: list, url: string, user: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "user_id" $user_id "scalar") (serialize-qp "user" $user "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "user_id__n" $user_id_n "scalar") (serialize-qp "user__n" $user_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/rack-reservations/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "created": $created, "description": $description, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "rack_id": $rack_id, "site_id": $site_id, "site": $site, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "location_id": $location_id, "location": $location, "user_id": $user_id, "user": $user, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "rack_id__n": $rack_id_n, "site_id__n": $site_id_n, "site__n": $site_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "location_id__n": $location_id_n, "location__n": $location_n, "user_id__n": $user_id_n, "user__n": $user_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/rack-reservations/
#
# operationId: dcim_rack-reservations_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-reservations update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  description: string
  rack: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  units: list<int>
  user: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, rack: record<device_count: int, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, units: list<int>, url: string, user: record<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-reservations/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "rack": $rack, "tags": $tags, "tenant": $tenant, "units": $units, "user": $user} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/rack-reservations/
#
# operationId: dcim_rack-reservations_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-reservations create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  description: string
  rack: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  units: list<int>
  user: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, rack: record<device_count: int, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, units: list<int>, url: string, user: record<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-reservations/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "rack": $rack, "tags": $tags, "tenant": $tenant, "units": $units, "user": $user} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rack-reservations/
#
# operationId: dcim_rack-reservations_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-reservations update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  description: string
  rack: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  units: list<int>
  user: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, rack: record<device_count: int, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, units: list<int>, url: string, user: record<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-reservations/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "rack": $rack, "tags": $tags, "tenant": $tenant, "units": $units, "user": $user} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/rack-reservations/{id}/
#
# operationId: dcim_rack-reservations_delete
export def "dcim-rack-reservations delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-reservations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rack-reservations/{id}/
#
# operationId: dcim_rack-reservations_read
export def "dcim-rack-reservations get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, rack: record<device_count: int, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, units: list<int>, url: string, user: record<display: string, id: int, url: string, username: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-reservations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/rack-reservations/{id}/
#
# operationId: dcim_rack-reservations_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-reservations update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  description: string
  rack: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  units: list<int>
  user: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, rack: record<device_count: int, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, units: list<int>, url: string, user: record<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-reservations/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "rack": $rack, "tags": $tags, "tenant": $tenant, "units": $units, "user": $user} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rack-reservations/{id}/
#
# operationId: dcim_rack-reservations_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-reservations update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  description: string
  rack: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  units: list<int>
  user: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, rack: record<device_count: int, display: string, id: int, name: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, units: list<int>, url: string, user: record<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-reservations/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "rack": $rack, "tags": $tags, "tenant": $tenant, "units": $units, "user": $user} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/rack-roles/
#
# operationId: dcim_rack-roles_bulk_delete
export def "dcim-rack-roles delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-roles/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rack-roles/
#
# operationId: dcim_rack-roles_list
export def "dcim-rack-roles list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --color: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<color: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, rack_count: int, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/rack-roles/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "color": $color, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/rack-roles/
#
# operationId: dcim_rack-roles_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-roles update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, rack_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/rack-roles/
#
# operationId: dcim_rack-roles_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-roles create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, rack_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rack-roles/
#
# operationId: dcim_rack-roles_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-roles update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, rack_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rack-roles/")
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/rack-roles/{id}/
#
# operationId: dcim_rack-roles_delete
export def "dcim-rack-roles delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rack-roles/{id}/
#
# operationId: dcim_rack-roles_read
export def "dcim-rack-roles get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, rack_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/rack-roles/{id}/
#
# operationId: dcim_rack-roles_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-roles update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, rack_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-roles/{id}/"))
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rack-roles/{id}/
#
# operationId: dcim_rack-roles_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rack-roles update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<color: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, rack_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rack-roles/{id}/"))
  let req_body = {"color": $color, "custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/racks/
#
# operationId: dcim_racks_bulk_delete
export def "dcim-racks delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/racks/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/racks/
#
# operationId: dcim_racks_list
export def "dcim-racks list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --facility-id: string
  --asset-tag: string
  --u-height: string
  --desc-units: string
  --outer-width: string
  --outer-depth: string
  --outer-unit: string
  --mounting-depth: string
  --weight: string
  --max-weight: string
  --weight-unit: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --status: string
  --type: string
  --width: string
  --role-id: string
  --role: string
  --serial: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --facility-id-n: string
  --facility-id-ic: string
  --facility-id-nic: string
  --facility-id-iew: string
  --facility-id-niew: string
  --facility-id-isw: string
  --facility-id-nisw: string
  --facility-id-ie: string
  --facility-id-nie: string
  --facility-id-empty: string
  --asset-tag-n: string
  --asset-tag-ic: string
  --asset-tag-nic: string
  --asset-tag-iew: string
  --asset-tag-niew: string
  --asset-tag-isw: string
  --asset-tag-nisw: string
  --asset-tag-ie: string
  --asset-tag-nie: string
  --asset-tag-empty: string
  --u-height-n: string
  --u-height-lte: string
  --u-height-lt: string
  --u-height-gte: string
  --u-height-gt: string
  --outer-width-n: string
  --outer-width-lte: string
  --outer-width-lt: string
  --outer-width-gte: string
  --outer-width-gt: string
  --outer-depth-n: string
  --outer-depth-lte: string
  --outer-depth-lt: string
  --outer-depth-gte: string
  --outer-depth-gt: string
  --outer-unit-n: string
  --mounting-depth-n: string
  --mounting-depth-lte: string
  --mounting-depth-lt: string
  --mounting-depth-gte: string
  --mounting-depth-gt: string
  --weight-n: string
  --weight-lte: string
  --weight-lt: string
  --weight-gte: string
  --weight-gt: string
  --max-weight-n: string
  --max-weight-lte: string
  --max-weight-lt: string
  --max-weight-gte: string
  --max-weight-gt: string
  --weight-unit-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --status-n: string
  --type-n: string
  --width-n: string
  --role-id-n: string
  --role-n: string
  --serial-n: string
  --serial-ic: string
  --serial-nic: string
  --serial-iew: string
  --serial-niew: string
  --serial-isw: string
  --serial-nisw: string
  --serial-ie: string
  --serial-nie: string
  --serial-empty: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<asset_tag: string, comments: string, created: string, custom_fields: record, desc_units: bool, description: string, device_count: int, display: string, facility_id: string, id: int, last_updated: string, location: record, max_weight: int, mounting_depth: int, name: string, outer_depth: int, outer_unit: record, outer_width: int, powerfeed_count: int, role: record, serial: string, site: record, status: record, tags: list, tenant: record, type: record, u_height: int, url: string, weight: float, weight_unit: record, width: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "facility_id" $facility_id "scalar") (serialize-qp "asset_tag" $asset_tag "scalar") (serialize-qp "u_height" $u_height "scalar") (serialize-qp "desc_units" $desc_units "scalar") (serialize-qp "outer_width" $outer_width "scalar") (serialize-qp "outer_depth" $outer_depth "scalar") (serialize-qp "outer_unit" $outer_unit "scalar") (serialize-qp "mounting_depth" $mounting_depth "scalar") (serialize-qp "weight" $weight "scalar") (serialize-qp "max_weight" $max_weight "scalar") (serialize-qp "weight_unit" $weight_unit "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "width" $width "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "serial" $serial "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "facility_id__n" $facility_id_n "scalar") (serialize-qp "facility_id__ic" $facility_id_ic "scalar") (serialize-qp "facility_id__nic" $facility_id_nic "scalar") (serialize-qp "facility_id__iew" $facility_id_iew "scalar") (serialize-qp "facility_id__niew" $facility_id_niew "scalar") (serialize-qp "facility_id__isw" $facility_id_isw "scalar") (serialize-qp "facility_id__nisw" $facility_id_nisw "scalar") (serialize-qp "facility_id__ie" $facility_id_ie "scalar") (serialize-qp "facility_id__nie" $facility_id_nie "scalar") (serialize-qp "facility_id__empty" $facility_id_empty "scalar") (serialize-qp "asset_tag__n" $asset_tag_n "scalar") (serialize-qp "asset_tag__ic" $asset_tag_ic "scalar") (serialize-qp "asset_tag__nic" $asset_tag_nic "scalar") (serialize-qp "asset_tag__iew" $asset_tag_iew "scalar") (serialize-qp "asset_tag__niew" $asset_tag_niew "scalar") (serialize-qp "asset_tag__isw" $asset_tag_isw "scalar") (serialize-qp "asset_tag__nisw" $asset_tag_nisw "scalar") (serialize-qp "asset_tag__ie" $asset_tag_ie "scalar") (serialize-qp "asset_tag__nie" $asset_tag_nie "scalar") (serialize-qp "asset_tag__empty" $asset_tag_empty "scalar") (serialize-qp "u_height__n" $u_height_n "scalar") (serialize-qp "u_height__lte" $u_height_lte "scalar") (serialize-qp "u_height__lt" $u_height_lt "scalar") (serialize-qp "u_height__gte" $u_height_gte "scalar") (serialize-qp "u_height__gt" $u_height_gt "scalar") (serialize-qp "outer_width__n" $outer_width_n "scalar") (serialize-qp "outer_width__lte" $outer_width_lte "scalar") (serialize-qp "outer_width__lt" $outer_width_lt "scalar") (serialize-qp "outer_width__gte" $outer_width_gte "scalar") (serialize-qp "outer_width__gt" $outer_width_gt "scalar") (serialize-qp "outer_depth__n" $outer_depth_n "scalar") (serialize-qp "outer_depth__lte" $outer_depth_lte "scalar") (serialize-qp "outer_depth__lt" $outer_depth_lt "scalar") (serialize-qp "outer_depth__gte" $outer_depth_gte "scalar") (serialize-qp "outer_depth__gt" $outer_depth_gt "scalar") (serialize-qp "outer_unit__n" $outer_unit_n "scalar") (serialize-qp "mounting_depth__n" $mounting_depth_n "scalar") (serialize-qp "mounting_depth__lte" $mounting_depth_lte "scalar") (serialize-qp "mounting_depth__lt" $mounting_depth_lt "scalar") (serialize-qp "mounting_depth__gte" $mounting_depth_gte "scalar") (serialize-qp "mounting_depth__gt" $mounting_depth_gt "scalar") (serialize-qp "weight__n" $weight_n "scalar") (serialize-qp "weight__lte" $weight_lte "scalar") (serialize-qp "weight__lt" $weight_lt "scalar") (serialize-qp "weight__gte" $weight_gte "scalar") (serialize-qp "weight__gt" $weight_gt "scalar") (serialize-qp "max_weight__n" $max_weight_n "scalar") (serialize-qp "max_weight__lte" $max_weight_lte "scalar") (serialize-qp "max_weight__lt" $max_weight_lt "scalar") (serialize-qp "max_weight__gte" $max_weight_gte "scalar") (serialize-qp "max_weight__gt" $max_weight_gt "scalar") (serialize-qp "weight_unit__n" $weight_unit_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "width__n" $width_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "serial__n" $serial_n "scalar") (serialize-qp "serial__ic" $serial_ic "scalar") (serialize-qp "serial__nic" $serial_nic "scalar") (serialize-qp "serial__iew" $serial_iew "scalar") (serialize-qp "serial__niew" $serial_niew "scalar") (serialize-qp "serial__isw" $serial_isw "scalar") (serialize-qp "serial__nisw" $serial_nisw "scalar") (serialize-qp "serial__ie" $serial_ie "scalar") (serialize-qp "serial__nie" $serial_nie "scalar") (serialize-qp "serial__empty" $serial_empty "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/racks/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "facility_id": $facility_id, "asset_tag": $asset_tag, "u_height": $u_height, "desc_units": $desc_units, "outer_width": $outer_width, "outer_depth": $outer_depth, "outer_unit": $outer_unit, "mounting_depth": $mounting_depth, "weight": $weight, "max_weight": $max_weight, "weight_unit": $weight_unit, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "status": $status, "type": $type, "width": $width, "role_id": $role_id, "role": $role, "serial": $serial, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "facility_id__n": $facility_id_n, "facility_id__ic": $facility_id_ic, "facility_id__nic": $facility_id_nic, "facility_id__iew": $facility_id_iew, "facility_id__niew": $facility_id_niew, "facility_id__isw": $facility_id_isw, "facility_id__nisw": $facility_id_nisw, "facility_id__ie": $facility_id_ie, "facility_id__nie": $facility_id_nie, "facility_id__empty": $facility_id_empty, "asset_tag__n": $asset_tag_n, "asset_tag__ic": $asset_tag_ic, "asset_tag__nic": $asset_tag_nic, "asset_tag__iew": $asset_tag_iew, "asset_tag__niew": $asset_tag_niew, "asset_tag__isw": $asset_tag_isw, "asset_tag__nisw": $asset_tag_nisw, "asset_tag__ie": $asset_tag_ie, "asset_tag__nie": $asset_tag_nie, "asset_tag__empty": $asset_tag_empty, "u_height__n": $u_height_n, "u_height__lte": $u_height_lte, "u_height__lt": $u_height_lt, "u_height__gte": $u_height_gte, "u_height__gt": $u_height_gt, "outer_width__n": $outer_width_n, "outer_width__lte": $outer_width_lte, "outer_width__lt": $outer_width_lt, "outer_width__gte": $outer_width_gte, "outer_width__gt": $outer_width_gt, "outer_depth__n": $outer_depth_n, "outer_depth__lte": $outer_depth_lte, "outer_depth__lt": $outer_depth_lt, "outer_depth__gte": $outer_depth_gte, "outer_depth__gt": $outer_depth_gt, "outer_unit__n": $outer_unit_n, "mounting_depth__n": $mounting_depth_n, "mounting_depth__lte": $mounting_depth_lte, "mounting_depth__lt": $mounting_depth_lt, "mounting_depth__gte": $mounting_depth_gte, "mounting_depth__gt": $mounting_depth_gt, "weight__n": $weight_n, "weight__lte": $weight_lte, "weight__lt": $weight_lt, "weight__gte": $weight_gte, "weight__gt": $weight_gt, "max_weight__n": $max_weight_n, "max_weight__lte": $max_weight_lte, "max_weight__lt": $max_weight_lt, "max_weight__gte": $max_weight_gte, "max_weight__gt": $max_weight_gt, "weight_unit__n": $weight_unit_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "status__n": $status_n, "type__n": $type_n, "width__n": $width_n, "role_id__n": $role_id_n, "role__n": $role_n, "serial__n": $serial_n, "serial__ic": $serial_ic, "serial__nic": $serial_nic, "serial__iew": $serial_iew, "serial__niew": $serial_niew, "serial__isw": $serial_isw, "serial__nisw": $serial_nisw, "serial__ie": $serial_ie, "serial__nie": $serial_nie, "serial__empty": $serial_empty, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/racks/
#
# operationId: dcim_racks_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-racks update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this rack (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --desc-units: oneof<nothing, bool> # Units are numbered top-to-bottom
  --description: string
  --facility-id: string # nullable
  --location: int # nullable
  --max-weight: int # Maximum load capacity for the rack (nullable)
  --mounting-depth: int # Maximum depth of a mounted device, in millimeters. For four-post racks, this is the distance between the front and rear rails. (nullable)
  name: string
  --outer-depth: int # Outer dimension of rack (depth) (nullable)
  --outer-unit: string@outer-unit-completer
  --outer-width: int # Outer dimension of rack (width) (nullable)
  --role: int # Functional role (nullable)
  --serial: string
  site: int
  --status: string@status-completer-6
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer-7
  --u-height: int # Height in rack units
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
  --width: int@width-completer # Rail-to-rail width
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, desc_units: bool, description: string, device_count: int, display: string, facility_id: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, max_weight: int, mounting_depth: int, name: string, outer_depth: int, outer_unit: record<label: string, value: string>, outer_width: int, powerfeed_count: int, role: record<display: string, id: int, name: string, rack_count: int, slug: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, u_height: int, url: string, weight: float, weight_unit: record<label: string, value: string>, width: record<label: string, value: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/racks/")
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "desc_units": $desc_units, "description": $description, "facility_id": $facility_id, "location": $location, "max_weight": $max_weight, "mounting_depth": $mounting_depth, "name": $name, "outer_depth": $outer_depth, "outer_unit": $outer_unit, "outer_width": $outer_width, "role": $role, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit, "width": $width} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/racks/
#
# operationId: dcim_racks_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-racks create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this rack (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --desc-units: oneof<nothing, bool> # Units are numbered top-to-bottom
  --description: string
  --facility-id: string # nullable
  --location: int # nullable
  --max-weight: int # Maximum load capacity for the rack (nullable)
  --mounting-depth: int # Maximum depth of a mounted device, in millimeters. For four-post racks, this is the distance between the front and rear rails. (nullable)
  name: string
  --outer-depth: int # Outer dimension of rack (depth) (nullable)
  --outer-unit: string@outer-unit-completer
  --outer-width: int # Outer dimension of rack (width) (nullable)
  --role: int # Functional role (nullable)
  --serial: string
  site: int
  --status: string@status-completer-6
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer-7
  --u-height: int # Height in rack units
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
  --width: int@width-completer # Rail-to-rail width
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, desc_units: bool, description: string, device_count: int, display: string, facility_id: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, max_weight: int, mounting_depth: int, name: string, outer_depth: int, outer_unit: record<label: string, value: string>, outer_width: int, powerfeed_count: int, role: record<display: string, id: int, name: string, rack_count: int, slug: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, u_height: int, url: string, weight: float, weight_unit: record<label: string, value: string>, width: record<label: string, value: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/racks/")
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "desc_units": $desc_units, "description": $description, "facility_id": $facility_id, "location": $location, "max_weight": $max_weight, "mounting_depth": $mounting_depth, "name": $name, "outer_depth": $outer_depth, "outer_unit": $outer_unit, "outer_width": $outer_width, "role": $role, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit, "width": $width} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/racks/
#
# operationId: dcim_racks_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-racks update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this rack (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --desc-units: oneof<nothing, bool> # Units are numbered top-to-bottom
  --description: string
  --facility-id: string # nullable
  --location: int # nullable
  --max-weight: int # Maximum load capacity for the rack (nullable)
  --mounting-depth: int # Maximum depth of a mounted device, in millimeters. For four-post racks, this is the distance between the front and rear rails. (nullable)
  name: string
  --outer-depth: int # Outer dimension of rack (depth) (nullable)
  --outer-unit: string@outer-unit-completer
  --outer-width: int # Outer dimension of rack (width) (nullable)
  --role: int # Functional role (nullable)
  --serial: string
  site: int
  --status: string@status-completer-6
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer-7
  --u-height: int # Height in rack units
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
  --width: int@width-completer # Rail-to-rail width
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, desc_units: bool, description: string, device_count: int, display: string, facility_id: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, max_weight: int, mounting_depth: int, name: string, outer_depth: int, outer_unit: record<label: string, value: string>, outer_width: int, powerfeed_count: int, role: record<display: string, id: int, name: string, rack_count: int, slug: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, u_height: int, url: string, weight: float, weight_unit: record<label: string, value: string>, width: record<label: string, value: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/racks/")
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "desc_units": $desc_units, "description": $description, "facility_id": $facility_id, "location": $location, "max_weight": $max_weight, "mounting_depth": $mounting_depth, "name": $name, "outer_depth": $outer_depth, "outer_unit": $outer_unit, "outer_width": $outer_width, "role": $role, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit, "width": $width} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/racks/{id}/
#
# operationId: dcim_racks_delete
export def "dcim-racks delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/racks/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/racks/{id}/
#
# operationId: dcim_racks_read
export def "dcim-racks get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<asset_tag: string, comments: string, created: string, custom_fields: record, desc_units: bool, description: string, device_count: int, display: string, facility_id: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, max_weight: int, mounting_depth: int, name: string, outer_depth: int, outer_unit: record<label: string, value: string>, outer_width: int, powerfeed_count: int, role: record<display: string, id: int, name: string, rack_count: int, slug: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, u_height: int, url: string, weight: float, weight_unit: record<label: string, value: string>, width: record<label: string, value: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/racks/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/racks/{id}/
#
# operationId: dcim_racks_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-racks update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this rack (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --desc-units: oneof<nothing, bool> # Units are numbered top-to-bottom
  --description: string
  --facility-id: string # nullable
  --location: int # nullable
  --max-weight: int # Maximum load capacity for the rack (nullable)
  --mounting-depth: int # Maximum depth of a mounted device, in millimeters. For four-post racks, this is the distance between the front and rear rails. (nullable)
  name: string
  --outer-depth: int # Outer dimension of rack (depth) (nullable)
  --outer-unit: string@outer-unit-completer
  --outer-width: int # Outer dimension of rack (width) (nullable)
  --role: int # Functional role (nullable)
  --serial: string
  site: int
  --status: string@status-completer-6
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer-7
  --u-height: int # Height in rack units
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
  --width: int@width-completer # Rail-to-rail width
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, desc_units: bool, description: string, device_count: int, display: string, facility_id: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, max_weight: int, mounting_depth: int, name: string, outer_depth: int, outer_unit: record<label: string, value: string>, outer_width: int, powerfeed_count: int, role: record<display: string, id: int, name: string, rack_count: int, slug: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, u_height: int, url: string, weight: float, weight_unit: record<label: string, value: string>, width: record<label: string, value: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/racks/{id}/"))
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "desc_units": $desc_units, "description": $description, "facility_id": $facility_id, "location": $location, "max_weight": $max_weight, "mounting_depth": $mounting_depth, "name": $name, "outer_depth": $outer_depth, "outer_unit": $outer_unit, "outer_width": $outer_width, "role": $role, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit, "width": $width} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/racks/{id}/
#
# operationId: dcim_racks_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-racks update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asset-tag: string # A unique tag used to identify this rack (nullable)
  --comments: string
  --custom-fields: record # default: {}
  --desc-units: oneof<nothing, bool> # Units are numbered top-to-bottom
  --description: string
  --facility-id: string # nullable
  --location: int # nullable
  --max-weight: int # Maximum load capacity for the rack (nullable)
  --mounting-depth: int # Maximum depth of a mounted device, in millimeters. For four-post racks, this is the distance between the front and rear rails. (nullable)
  name: string
  --outer-depth: int # Outer dimension of rack (depth) (nullable)
  --outer-unit: string@outer-unit-completer
  --outer-width: int # Outer dimension of rack (width) (nullable)
  --role: int # Functional role (nullable)
  --serial: string
  site: int
  --status: string@status-completer-6
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --type: string@type-completer-7
  --u-height: int # Height in rack units
  --weight: float # nullable, format: decimal
  --weight-unit: string@weight-unit-completer
  --width: int@width-completer # Rail-to-rail width
]: any -> record<asset_tag: string, comments: string, created: string, custom_fields: record, desc_units: bool, description: string, device_count: int, display: string, facility_id: string, id: int, last_updated: string, location: record<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, max_weight: int, mounting_depth: int, name: string, outer_depth: int, outer_unit: record<label: string, value: string>, outer_width: int, powerfeed_count: int, role: record<display: string, id: int, name: string, rack_count: int, slug: string, url: string>, serial: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, u_height: int, url: string, weight: float, weight_unit: record<label: string, value: string>, width: record<label: string, value: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/racks/{id}/"))
  let req_body = {"asset_tag": $asset_tag, "comments": $comments, "custom_fields": $custom_fields, "desc_units": $desc_units, "description": $description, "facility_id": $facility_id, "location": $location, "max_weight": $max_weight, "mounting_depth": $mounting_depth, "name": $name, "outer_depth": $outer_depth, "outer_unit": $outer_unit, "outer_width": $outer_width, "role": $role, "serial": $serial, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type, "u_height": $u_height, "weight": $weight, "weight_unit": $weight_unit, "width": $width} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Rack elevation representing the list of rack units. Also supports rendering the elevation as an SVG.
#
# GET /dcim/racks/{id}/elevation/
# operationId: dcim_racks_elevation
export def "dcim-racks-elevation get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --q: string
  --face: string@face-completer # default: front
  --render: string@render-completer # default: json
  --unit-width: int # default: 220
  --unit-height: int # default: 22
  --legend-width: int # default: 30
  --margin-width: int # default: 15
  --exclude: int
  --expand-devices: oneof<nothing, bool> # default: true
  --include-images: oneof<nothing, bool> # default: true
]: nothing -> table<device: record<display: string, id: int, name: string, url: string>, display: string, face: record<label: string, value: string>, id: float, name: string, occupied: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let qp = [(serialize-qp "q" $q "scalar") (serialize-qp "face" $face "scalar") (serialize-qp "render" $render "scalar") (serialize-qp "unit_width" $unit_width "scalar") (serialize-qp "unit_height" $unit_height "scalar") (serialize-qp "legend_width" $legend_width "scalar") (serialize-qp "margin_width" $margin_width "scalar") (serialize-qp "exclude" $exclude "scalar") (serialize-qp "expand_devices" $expand_devices "scalar") (serialize-qp "include_images" $include_images "scalar")] | flatten | str join "&"
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/racks/{id}/elevation/") $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"q": $q, "face": $face, "render": $render, "unit_width": $unit_width, "unit_height": $unit_height, "legend_width": $legend_width, "margin_width": $margin_width, "exclude": $exclude, "expand_devices": $expand_devices, "include_images": $include_images} | compact), body: null}
}

# DELETE /dcim/rear-port-templates/
#
# operationId: dcim_rear-port-templates_bulk_delete
export def "dcim-rear-port-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-port-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rear-port-templates/
#
# operationId: dcim_rear-port-templates_list
export def "dcim-rear-port-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type: string
  --color: string
  --positions: string
  --created: string
  --last-updated: string
  --q: string
  --devicetype-id: string
  --moduletype-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --type-n: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --positions-n: string
  --positions-lte: string
  --positions-lt: string
  --positions-gte: string
  --positions-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --devicetype-id-n: string
  --moduletype-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<color: string, created: string, description: string, device_type: record, display: string, id: int, label: string, last_updated: string, module_type: record, name: string, positions: int, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "devicetype_id" $devicetype_id "scalar") (serialize-qp "moduletype_id" $moduletype_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "positions__n" $positions_n "scalar") (serialize-qp "positions__lte" $positions_lte "scalar") (serialize-qp "positions__lt" $positions_lt "scalar") (serialize-qp "positions__gte" $positions_gte "scalar") (serialize-qp "positions__gt" $positions_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "devicetype_id__n" $devicetype_id_n "scalar") (serialize-qp "moduletype_id__n" $moduletype_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/rear-port-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type": $type, "color": $color, "positions": $positions, "created": $created, "last_updated": $last_updated, "q": $q, "devicetype_id": $devicetype_id, "moduletype_id": $moduletype_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "type__n": $type_n, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "positions__n": $positions_n, "positions__lte": $positions_lte, "positions__lt": $positions_lt, "positions__gte": $positions_gte, "positions__gt": $positions_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "devicetype_id__n": $devicetype_id_n, "moduletype_id__n": $moduletype_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/rear-port-templates/
#
# operationId: dcim_rear-port-templates_bulk_partial_update
export def "dcim-rear-port-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --positions: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, positions: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-port-templates/")
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "positions": $positions, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/rear-port-templates/
#
# operationId: dcim_rear-port-templates_create
export def "dcim-rear-port-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --positions: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, positions: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-port-templates/")
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "positions": $positions, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rear-port-templates/
#
# operationId: dcim_rear-port-templates_bulk_update
export def "dcim-rear-port-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --positions: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, positions: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-port-templates/")
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "positions": $positions, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/rear-port-templates/{id}/
#
# operationId: dcim_rear-port-templates_delete
export def "dcim-rear-port-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rear-port-templates/{id}/
#
# operationId: dcim_rear-port-templates_read
export def "dcim-rear-port-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, positions: int, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-port-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/rear-port-templates/{id}/
#
# operationId: dcim_rear-port-templates_partial_update
export def "dcim-rear-port-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --positions: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, positions: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-port-templates/{id}/"))
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "positions": $positions, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rear-port-templates/{id}/
#
# operationId: dcim_rear-port-templates_update
export def "dcim-rear-port-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  --device-type: int # nullable
  --label: string # Physical label
  --module-type: int # nullable
  name: string # {module} is accepted as a substitution for the module bay position when attached to a module type.
  --positions: int
  type: string@type-completer-2
]: any -> record<color: string, created: string, description: string, device_type: record<device_count: int, display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, slug: string, url: string>, display: string, id: int, label: string, last_updated: string, module_type: record<display: string, id: int, manufacturer: record<devicetype_count: int, display: string, id: int, name: string, slug: string, url: string>, model: string, url: string>, name: string, positions: int, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-port-templates/{id}/"))
  let req_body = {"color": $color, "description": $description, "device_type": $device_type, "label": $label, "module_type": $module_type, "name": $name, "positions": $positions, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/rear-ports/
#
# operationId: dcim_rear-ports_bulk_delete
export def "dcim-rear-ports delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-ports/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rear-ports/
#
# operationId: dcim_rear-ports_list
export def "dcim-rear-ports list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --label: string
  --type: string
  --color: string
  --positions: string
  --description: string
  --cable-end: string
  --q: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --rack-id: string
  --rack: string
  --device-id: string
  --device: string
  --virtual-chassis-id: string
  --virtual-chassis: string
  --module-id: string
  --created: string
  --last-updated: string
  --tag: string
  --cabled: string
  --occupied: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --label-n: string
  --label-ic: string
  --label-nic: string
  --label-iew: string
  --label-niew: string
  --label-isw: string
  --label-nisw: string
  --label-ie: string
  --label-nie: string
  --label-empty: string
  --type-n: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --positions-n: string
  --positions-lte: string
  --positions-lt: string
  --positions-gte: string
  --positions-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --cable-end-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --rack-id-n: string
  --rack-n: string
  --device-id-n: string
  --device-n: string
  --virtual-chassis-id-n: string
  --virtual-chassis-n: string
  --module-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_occupied: bool, cable: record, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, label: string, last_updated: string, link_peers: list, link_peers_type: string, mark_connected: bool, module: record, name: string, positions: int, tags: list, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "label" $label "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "positions" $positions "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "cable_end" $cable_end "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack_id" $rack_id "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_chassis_id" $virtual_chassis_id "scalar") (serialize-qp "virtual_chassis" $virtual_chassis "scalar") (serialize-qp "module_id" $module_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cabled" $cabled "scalar") (serialize-qp "occupied" $occupied "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "label__n" $label_n "scalar") (serialize-qp "label__ic" $label_ic "scalar") (serialize-qp "label__nic" $label_nic "scalar") (serialize-qp "label__iew" $label_iew "scalar") (serialize-qp "label__niew" $label_niew "scalar") (serialize-qp "label__isw" $label_isw "scalar") (serialize-qp "label__nisw" $label_nisw "scalar") (serialize-qp "label__ie" $label_ie "scalar") (serialize-qp "label__nie" $label_nie "scalar") (serialize-qp "label__empty" $label_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "positions__n" $positions_n "scalar") (serialize-qp "positions__lte" $positions_lte "scalar") (serialize-qp "positions__lt" $positions_lt "scalar") (serialize-qp "positions__gte" $positions_gte "scalar") (serialize-qp "positions__gt" $positions_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "cable_end__n" $cable_end_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "rack_id__n" $rack_id_n "scalar") (serialize-qp "rack__n" $rack_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_chassis_id__n" $virtual_chassis_id_n "scalar") (serialize-qp "virtual_chassis__n" $virtual_chassis_n "scalar") (serialize-qp "module_id__n" $module_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/rear-ports/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "label": $label, "type": $type, "color": $color, "positions": $positions, "description": $description, "cable_end": $cable_end, "q": $q, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "rack_id": $rack_id, "rack": $rack, "device_id": $device_id, "device": $device, "virtual_chassis_id": $virtual_chassis_id, "virtual_chassis": $virtual_chassis, "module_id": $module_id, "created": $created, "last_updated": $last_updated, "tag": $tag, "cabled": $cabled, "occupied": $occupied, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "label__n": $label_n, "label__ic": $label_ic, "label__nic": $label_nic, "label__iew": $label_iew, "label__niew": $label_niew, "label__isw": $label_isw, "label__nisw": $label_nisw, "label__ie": $label_ie, "label__nie": $label_nie, "label__empty": $label_empty, "type__n": $type_n, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "positions__n": $positions_n, "positions__lte": $positions_lte, "positions__lt": $positions_lt, "positions__gte": $positions_gte, "positions__gt": $positions_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "cable_end__n": $cable_end_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "rack_id__n": $rack_id_n, "rack__n": $rack_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_chassis_id__n": $virtual_chassis_id_n, "virtual_chassis__n": $virtual_chassis_n, "module_id__n": $module_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/rear-ports/
#
# operationId: dcim_rear-ports_bulk_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rear-ports update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --positions: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, positions: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-ports/")
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "positions": $positions, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/rear-ports/
#
# operationId: dcim_rear-ports_create
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rear-ports create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --positions: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, positions: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-ports/")
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "positions": $positions, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rear-ports/
#
# operationId: dcim_rear-ports_bulk_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rear-ports update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --positions: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, positions: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/rear-ports/")
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "positions": $positions, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/rear-ports/{id}/
#
# operationId: dcim_rear-ports_delete
export def "dcim-rear-ports delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/rear-ports/{id}/
#
# operationId: dcim_rear-ports_read
export def "dcim-rear-ports get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, positions: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-ports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/rear-ports/{id}/
#
# operationId: dcim_rear-ports_partial_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rear-ports update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --positions: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, positions: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-ports/{id}/"))
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "positions": $positions, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/rear-ports/{id}/
#
# operationId: dcim_rear-ports_update
# --cable shape: {label?: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-rear-ports update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cable: record # shape: {label?: string}
  --color: string
  --custom-fields: record # default: {}
  --description: string
  device: int
  --label: string # Physical label
  --mark-connected: oneof<nothing, bool> # Treat as if a cable is connected
  --body-module: int # nullable
  name: string
  --positions: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  type: string@type-completer-2
]: any -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, positions: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-ports/{id}/"))
  let req_body = {"cable": $cable, "color": $color, "custom_fields": $custom_fields, "description": $description, "device": $device, "label": $label, "mark_connected": $mark_connected, "module": $body_module, "name": $name, "positions": $positions, "tags": $tags, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Return all CablePaths which traverse a given pass-through port.
#
# GET /dcim/rear-ports/{id}/paths/
# operationId: dcim_rear-ports_paths
export def "dcim-rear-ports-paths get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_occupied: bool, cable: record<display: string, id: int, label: string, url: string>, cable_end: string, color: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, label: string, last_updated: string, link_peers: list<string>, link_peers_type: string, mark_connected: bool, module: record<device: int, display: string, id: int, module_bay: record<display: string, id: int, name: string, url: string>, url: string>, name: string, positions: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/rear-ports/{id}/paths/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /dcim/regions/
#
# operationId: dcim_regions_bulk_delete
export def "dcim-regions delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/regions/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/regions/
#
# operationId: dcim_regions_list
export def "dcim-regions list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --parent-id: string
  --parent: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --parent-id-n: string
  --parent-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record, site_count: int, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "parent" $parent "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "parent__n" $parent_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/regions/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "parent_id": $parent_id, "parent": $parent, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "parent_id__n": $parent_id_n, "parent__n": $parent_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/regions/
#
# operationId: dcim_regions_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-regions update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/regions/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/regions/
#
# operationId: dcim_regions_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-regions create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/regions/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/regions/
#
# operationId: dcim_regions_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-regions update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/regions/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/regions/{id}/
#
# operationId: dcim_regions_delete
export def "dcim-regions delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/regions/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/regions/{id}/
#
# operationId: dcim_regions_read
export def "dcim-regions get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/regions/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/regions/{id}/
#
# operationId: dcim_regions_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-regions update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/regions/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/regions/{id}/
#
# operationId: dcim_regions_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-regions update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/regions/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/site-groups/
#
# operationId: dcim_site-groups_bulk_delete
export def "dcim-site-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/site-groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/site-groups/
#
# operationId: dcim_site-groups_list
export def "dcim-site-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --parent-id: string
  --parent: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --parent-id-n: string
  --parent-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record, site_count: int, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "parent" $parent "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "parent__n" $parent_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/site-groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "parent_id": $parent_id, "parent": $parent, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "parent_id__n": $parent_id_n, "parent__n": $parent_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/site-groups/
#
# operationId: dcim_site-groups_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-site-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/site-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/site-groups/
#
# operationId: dcim_site-groups_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-site-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/site-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/site-groups/
#
# operationId: dcim_site-groups_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-site-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/site-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/site-groups/{id}/
#
# operationId: dcim_site-groups_delete
export def "dcim-site-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/site-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/site-groups/{id}/
#
# operationId: dcim_site-groups_read
export def "dcim-site-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/site-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/site-groups/{id}/
#
# operationId: dcim_site-groups_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-site-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/site-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/site-groups/{id}/
#
# operationId: dcim_site-groups_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-site-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/site-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/sites/
#
# operationId: dcim_sites_bulk_delete
export def "dcim-sites delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/sites/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/sites/
#
# operationId: dcim_sites_list
export def "dcim-sites list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --facility: string
  --latitude: string
  --longitude: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --status: string
  --region-id: string
  --region: string
  --group-id: string
  --group: string
  --asn: string
  --asn-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --facility-n: string
  --facility-ic: string
  --facility-nic: string
  --facility-iew: string
  --facility-niew: string
  --facility-isw: string
  --facility-nisw: string
  --facility-ie: string
  --facility-nie: string
  --facility-empty: string
  --latitude-n: string
  --latitude-lte: string
  --latitude-lt: string
  --latitude-gte: string
  --latitude-gt: string
  --longitude-n: string
  --longitude-lte: string
  --longitude-lt: string
  --longitude-gte: string
  --longitude-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --status-n: string
  --region-id-n: string
  --region-n: string
  --group-id-n: string
  --group-n: string
  --asn-n: string
  --asn-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<asns: list, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, facility: string, group: record, id: int, last_updated: string, latitude: float, longitude: float, name: string, physical_address: string, prefix_count: int, rack_count: int, region: record, shipping_address: string, slug: string, status: record, tags: list, tenant: record, time_zone: string, url: string, virtualmachine_count: int, vlan_count: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "facility" $facility "scalar") (serialize-qp "latitude" $latitude "scalar") (serialize-qp "longitude" $longitude "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "asn" $asn "scalar") (serialize-qp "asn_id" $asn_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "facility__n" $facility_n "scalar") (serialize-qp "facility__ic" $facility_ic "scalar") (serialize-qp "facility__nic" $facility_nic "scalar") (serialize-qp "facility__iew" $facility_iew "scalar") (serialize-qp "facility__niew" $facility_niew "scalar") (serialize-qp "facility__isw" $facility_isw "scalar") (serialize-qp "facility__nisw" $facility_nisw "scalar") (serialize-qp "facility__ie" $facility_ie "scalar") (serialize-qp "facility__nie" $facility_nie "scalar") (serialize-qp "facility__empty" $facility_empty "scalar") (serialize-qp "latitude__n" $latitude_n "scalar") (serialize-qp "latitude__lte" $latitude_lte "scalar") (serialize-qp "latitude__lt" $latitude_lt "scalar") (serialize-qp "latitude__gte" $latitude_gte "scalar") (serialize-qp "latitude__gt" $latitude_gt "scalar") (serialize-qp "longitude__n" $longitude_n "scalar") (serialize-qp "longitude__lte" $longitude_lte "scalar") (serialize-qp "longitude__lt" $longitude_lt "scalar") (serialize-qp "longitude__gte" $longitude_gte "scalar") (serialize-qp "longitude__gt" $longitude_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "asn__n" $asn_n "scalar") (serialize-qp "asn_id__n" $asn_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/sites/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "facility": $facility, "latitude": $latitude, "longitude": $longitude, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "status": $status, "region_id": $region_id, "region": $region, "group_id": $group_id, "group": $group, "asn": $asn, "asn_id": $asn_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "facility__n": $facility_n, "facility__ic": $facility_ic, "facility__nic": $facility_nic, "facility__iew": $facility_iew, "facility__niew": $facility_niew, "facility__isw": $facility_isw, "facility__nisw": $facility_nisw, "facility__ie": $facility_ie, "facility__nie": $facility_nie, "facility__empty": $facility_empty, "latitude__n": $latitude_n, "latitude__lte": $latitude_lte, "latitude__lt": $latitude_lt, "latitude__gte": $latitude_gte, "latitude__gt": $latitude_gt, "longitude__n": $longitude_n, "longitude__lte": $longitude_lte, "longitude__lt": $longitude_lt, "longitude__gte": $longitude_gte, "longitude__gt": $longitude_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "status__n": $status_n, "region_id__n": $region_id_n, "region__n": $region_n, "group_id__n": $group_id_n, "group__n": $group_n, "asn__n": $asn_n, "asn_id__n": $asn_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/sites/
#
# operationId: dcim_sites_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-sites update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --facility: string # Local facility ID or description
  --group: int # nullable
  --latitude: float # GPS coordinate (latitude) (nullable, format: decimal)
  --longitude: float # GPS coordinate (longitude) (nullable, format: decimal)
  name: string
  --physical-address: string
  --region: int # nullable
  --shipping-address: string
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --time-zone: string # nullable
]: any -> record<asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, facility: string, group: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, id: int, last_updated: string, latitude: float, longitude: float, name: string, physical_address: string, prefix_count: int, rack_count: int, region: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, shipping_address: string, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, time_zone: string, url: string, virtualmachine_count: int, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/sites/")
  let req_body = {"asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "facility": $facility, "group": $group, "latitude": $latitude, "longitude": $longitude, "name": $name, "physical_address": $physical_address, "region": $region, "shipping_address": $shipping_address, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant, "time_zone": $time_zone} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/sites/
#
# operationId: dcim_sites_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-sites create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --facility: string # Local facility ID or description
  --group: int # nullable
  --latitude: float # GPS coordinate (latitude) (nullable, format: decimal)
  --longitude: float # GPS coordinate (longitude) (nullable, format: decimal)
  name: string
  --physical-address: string
  --region: int # nullable
  --shipping-address: string
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --time-zone: string # nullable
]: any -> record<asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, facility: string, group: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, id: int, last_updated: string, latitude: float, longitude: float, name: string, physical_address: string, prefix_count: int, rack_count: int, region: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, shipping_address: string, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, time_zone: string, url: string, virtualmachine_count: int, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/sites/")
  let req_body = {"asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "facility": $facility, "group": $group, "latitude": $latitude, "longitude": $longitude, "name": $name, "physical_address": $physical_address, "region": $region, "shipping_address": $shipping_address, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant, "time_zone": $time_zone} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/sites/
#
# operationId: dcim_sites_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-sites update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --facility: string # Local facility ID or description
  --group: int # nullable
  --latitude: float # GPS coordinate (latitude) (nullable, format: decimal)
  --longitude: float # GPS coordinate (longitude) (nullable, format: decimal)
  name: string
  --physical-address: string
  --region: int # nullable
  --shipping-address: string
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --time-zone: string # nullable
]: any -> record<asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, facility: string, group: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, id: int, last_updated: string, latitude: float, longitude: float, name: string, physical_address: string, prefix_count: int, rack_count: int, region: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, shipping_address: string, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, time_zone: string, url: string, virtualmachine_count: int, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/sites/")
  let req_body = {"asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "facility": $facility, "group": $group, "latitude": $latitude, "longitude": $longitude, "name": $name, "physical_address": $physical_address, "region": $region, "shipping_address": $shipping_address, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant, "time_zone": $time_zone} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/sites/{id}/
#
# operationId: dcim_sites_delete
export def "dcim-sites delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/sites/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/sites/{id}/
#
# operationId: dcim_sites_read
export def "dcim-sites get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, facility: string, group: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, id: int, last_updated: string, latitude: float, longitude: float, name: string, physical_address: string, prefix_count: int, rack_count: int, region: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, shipping_address: string, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, time_zone: string, url: string, virtualmachine_count: int, vlan_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/sites/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/sites/{id}/
#
# operationId: dcim_sites_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-sites update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --facility: string # Local facility ID or description
  --group: int # nullable
  --latitude: float # GPS coordinate (latitude) (nullable, format: decimal)
  --longitude: float # GPS coordinate (longitude) (nullable, format: decimal)
  name: string
  --physical-address: string
  --region: int # nullable
  --shipping-address: string
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --time-zone: string # nullable
]: any -> record<asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, facility: string, group: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, id: int, last_updated: string, latitude: float, longitude: float, name: string, physical_address: string, prefix_count: int, rack_count: int, region: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, shipping_address: string, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, time_zone: string, url: string, virtualmachine_count: int, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/sites/{id}/"))
  let req_body = {"asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "facility": $facility, "group": $group, "latitude": $latitude, "longitude": $longitude, "name": $name, "physical_address": $physical_address, "region": $region, "shipping_address": $shipping_address, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant, "time_zone": $time_zone} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/sites/{id}/
#
# operationId: dcim_sites_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-sites update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --asns: list<int>
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --facility: string # Local facility ID or description
  --group: int # nullable
  --latitude: float # GPS coordinate (latitude) (nullable, format: decimal)
  --longitude: float # GPS coordinate (longitude) (nullable, format: decimal)
  name: string
  --physical-address: string
  --region: int # nullable
  --shipping-address: string
  slug: string # format: slug
  --status: string@status-completer-3
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --time-zone: string # nullable
]: any -> record<asns: table<asn: int, display: string, id: int, url: string>, circuit_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, facility: string, group: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, id: int, last_updated: string, latitude: float, longitude: float, name: string, physical_address: string, prefix_count: int, rack_count: int, region: record<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, shipping_address: string, slug: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, time_zone: string, url: string, virtualmachine_count: int, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/sites/{id}/"))
  let req_body = {"asns": $asns, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "facility": $facility, "group": $group, "latitude": $latitude, "longitude": $longitude, "name": $name, "physical_address": $physical_address, "region": $region, "shipping_address": $shipping_address, "slug": $slug, "status": $status, "tags": $tags, "tenant": $tenant, "time_zone": $time_zone} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/virtual-chassis/
#
# operationId: dcim_virtual-chassis_bulk_delete
export def "dcim-virtual-chassis delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-chassis/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/virtual-chassis/
#
# operationId: dcim_virtual-chassis_list
export def "dcim-virtual-chassis list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --domain: string
  --name: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --master-id: string
  --master: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --tenant-id: string
  --tenant: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --domain-n: string
  --domain-ic: string
  --domain-nic: string
  --domain-iew: string
  --domain-niew: string
  --domain-isw: string
  --domain-nisw: string
  --domain-ie: string
  --domain-nie: string
  --domain-empty: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --master-id-n: string
  --master-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --tenant-id-n: string
  --tenant-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, domain: string, id: int, last_updated: string, master: record, member_count: int, name: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "domain" $domain "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "master_id" $master_id "scalar") (serialize-qp "master" $master "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "domain__n" $domain_n "scalar") (serialize-qp "domain__ic" $domain_ic "scalar") (serialize-qp "domain__nic" $domain_nic "scalar") (serialize-qp "domain__iew" $domain_iew "scalar") (serialize-qp "domain__niew" $domain_niew "scalar") (serialize-qp "domain__isw" $domain_isw "scalar") (serialize-qp "domain__nisw" $domain_nisw "scalar") (serialize-qp "domain__ie" $domain_ie "scalar") (serialize-qp "domain__nie" $domain_nie "scalar") (serialize-qp "domain__empty" $domain_empty "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "master_id__n" $master_id_n "scalar") (serialize-qp "master__n" $master_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/virtual-chassis/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "domain": $domain, "name": $name, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "master_id": $master_id, "master": $master, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "tenant_id": $tenant_id, "tenant": $tenant, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "domain__n": $domain_n, "domain__ic": $domain_ic, "domain__nic": $domain_nic, "domain__iew": $domain_iew, "domain__niew": $domain_niew, "domain__isw": $domain_isw, "domain__nisw": $domain_nisw, "domain__ie": $domain_ie, "domain__nie": $domain_nie, "domain__empty": $domain_empty, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "master_id__n": $master_id_n, "master__n": $master_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/virtual-chassis/
#
# operationId: dcim_virtual-chassis_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-chassis update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --domain: string
  --master: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, domain: string, id: int, last_updated: string, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-chassis/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "domain": $domain, "master": $master, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/virtual-chassis/
#
# operationId: dcim_virtual-chassis_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-chassis create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --domain: string
  --master: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, domain: string, id: int, last_updated: string, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-chassis/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "domain": $domain, "master": $master, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/virtual-chassis/
#
# operationId: dcim_virtual-chassis_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-chassis update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --domain: string
  --master: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, domain: string, id: int, last_updated: string, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-chassis/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "domain": $domain, "master": $master, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/virtual-chassis/{id}/
#
# operationId: dcim_virtual-chassis_delete
export def "dcim-virtual-chassis delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-chassis/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/virtual-chassis/{id}/
#
# operationId: dcim_virtual-chassis_read
export def "dcim-virtual-chassis get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, domain: string, id: int, last_updated: string, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-chassis/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/virtual-chassis/{id}/
#
# operationId: dcim_virtual-chassis_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-chassis update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --domain: string
  --master: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, domain: string, id: int, last_updated: string, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-chassis/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "domain": $domain, "master": $master, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/virtual-chassis/{id}/
#
# operationId: dcim_virtual-chassis_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-chassis update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --domain: string
  --master: int # nullable
  name: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, domain: string, id: int, last_updated: string, master: record<display: string, id: int, name: string, url: string>, member_count: int, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-chassis/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "domain": $domain, "master": $master, "name": $name, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/virtual-device-contexts/
#
# operationId: dcim_virtual-device-contexts_bulk_delete
export def "dcim-virtual-device-contexts delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-device-contexts/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/virtual-device-contexts/
#
# operationId: dcim_virtual-device-contexts_list
export def "dcim-virtual-device-contexts list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --device: string
  --name: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --device-id: string
  --status: string
  --has-primary-ip: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --device-n: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --device-id-n: string
  --status-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, identifier: int, interface_count: int, last_updated: string, name: string, primary_ip: record, primary_ip4: record, primary_ip6: record, status: string, tags: list, tenant: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "has_primary_ip" $has_primary_ip "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/dcim/virtual-device-contexts/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "device": $device, "name": $name, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "device_id": $device_id, "status": $status, "has_primary_ip": $has_primary_ip, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "device__n": $device_n, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "device_id__n": $device_id_n, "status__n": $status_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /dcim/virtual-device-contexts/
#
# operationId: dcim_virtual-device-contexts_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-device-contexts update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --identifier: int # Numeric identifier unique to the parent device (nullable)
  name: string
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  status: string@status-completer-7
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, identifier: int, interface_count: int, last_updated: string, name: string, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, status: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-device-contexts/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "identifier": $identifier, "name": $name, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /dcim/virtual-device-contexts/
#
# operationId: dcim_virtual-device-contexts_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-device-contexts create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --identifier: int # Numeric identifier unique to the parent device (nullable)
  name: string
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  status: string@status-completer-7
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, identifier: int, interface_count: int, last_updated: string, name: string, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, status: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-device-contexts/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "identifier": $identifier, "name": $name, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/virtual-device-contexts/
#
# operationId: dcim_virtual-device-contexts_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-device-contexts update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --identifier: int # Numeric identifier unique to the parent device (nullable)
  name: string
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  status: string@status-completer-7
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, identifier: int, interface_count: int, last_updated: string, name: string, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, status: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/dcim/virtual-device-contexts/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "identifier": $identifier, "name": $name, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /dcim/virtual-device-contexts/{id}/
#
# operationId: dcim_virtual-device-contexts_delete
export def "dcim-virtual-device-contexts delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-device-contexts/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /dcim/virtual-device-contexts/{id}/
#
# operationId: dcim_virtual-device-contexts_read
export def "dcim-virtual-device-contexts get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, identifier: int, interface_count: int, last_updated: string, name: string, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, status: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-device-contexts/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /dcim/virtual-device-contexts/{id}/
#
# operationId: dcim_virtual-device-contexts_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-device-contexts update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --identifier: int # Numeric identifier unique to the parent device (nullable)
  name: string
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  status: string@status-completer-7
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, identifier: int, interface_count: int, last_updated: string, name: string, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, status: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-device-contexts/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "identifier": $identifier, "name": $name, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /dcim/virtual-device-contexts/{id}/
#
# operationId: dcim_virtual-device-contexts_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "dcim-virtual-device-contexts update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --identifier: int # Numeric identifier unique to the parent device (nullable)
  name: string
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  status: string@status-completer-7
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, identifier: int, interface_count: int, last_updated: string, name: string, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, status: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/dcim/virtual-device-contexts/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "identifier": $identifier, "name": $name, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/config-contexts/
#
# operationId: extras_config-contexts_bulk_delete
export def "extras-config-contexts delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/config-contexts/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/config-contexts/
#
# operationId: extras_config-contexts_list
export def "extras-config-contexts list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --is-active: string
  --created: string
  --last-updated: string
  --q: string
  --region-id: string
  --region: string
  --site-group: string
  --site-group-id: string
  --site-id: string
  --site: string
  --location-id: string
  --location: string
  --device-type-id: string
  --role-id: string
  --role: string
  --platform-id: string
  --platform: string
  --cluster-type-id: string
  --cluster-type: string
  --cluster-group-id: string
  --cluster-group: string
  --cluster-id: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --tag-id: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --region-id-n: string
  --region-n: string
  --site-group-n: string
  --site-group-id-n: string
  --site-id-n: string
  --site-n: string
  --location-id-n: string
  --location-n: string
  --device-type-id-n: string
  --role-id-n: string
  --role-n: string
  --platform-id-n: string
  --platform-n: string
  --cluster-type-id-n: string
  --cluster-type-n: string
  --cluster-group-id-n: string
  --cluster-group-n: string
  --cluster-id-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --tag-id-n: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<cluster_groups: list, cluster_types: list, clusters: list, created: string, data: record, description: string, device_types: list, display: string, id: int, is_active: bool, last_updated: string, locations: list, name: string, platforms: list, regions: list, roles: list, site_groups: list, sites: list, tags: list, tenant_groups: list, tenants: list, url: string, weight: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "is_active" $is_active "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location_id" $location_id "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "device_type_id" $device_type_id "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "platform_id" $platform_id "scalar") (serialize-qp "platform" $platform "scalar") (serialize-qp "cluster_type_id" $cluster_type_id "scalar") (serialize-qp "cluster_type" $cluster_type "scalar") (serialize-qp "cluster_group_id" $cluster_group_id "scalar") (serialize-qp "cluster_group" $cluster_group "scalar") (serialize-qp "cluster_id" $cluster_id "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "tag_id" $tag_id "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "location_id__n" $location_id_n "scalar") (serialize-qp "location__n" $location_n "scalar") (serialize-qp "device_type_id__n" $device_type_id_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "platform_id__n" $platform_id_n "scalar") (serialize-qp "platform__n" $platform_n "scalar") (serialize-qp "cluster_type_id__n" $cluster_type_id_n "scalar") (serialize-qp "cluster_type__n" $cluster_type_n "scalar") (serialize-qp "cluster_group_id__n" $cluster_group_id_n "scalar") (serialize-qp "cluster_group__n" $cluster_group_n "scalar") (serialize-qp "cluster_id__n" $cluster_id_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "tag_id__n" $tag_id_n "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/config-contexts/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "is_active": $is_active, "created": $created, "last_updated": $last_updated, "q": $q, "region_id": $region_id, "region": $region, "site_group": $site_group, "site_group_id": $site_group_id, "site_id": $site_id, "site": $site, "location_id": $location_id, "location": $location, "device_type_id": $device_type_id, "role_id": $role_id, "role": $role, "platform_id": $platform_id, "platform": $platform, "cluster_type_id": $cluster_type_id, "cluster_type": $cluster_type, "cluster_group_id": $cluster_group_id, "cluster_group": $cluster_group, "cluster_id": $cluster_id, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "tag_id": $tag_id, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "region_id__n": $region_id_n, "region__n": $region_n, "site_group__n": $site_group_n, "site_group_id__n": $site_group_id_n, "site_id__n": $site_id_n, "site__n": $site_n, "location_id__n": $location_id_n, "location__n": $location_n, "device_type_id__n": $device_type_id_n, "role_id__n": $role_id_n, "role__n": $role_n, "platform_id__n": $platform_id_n, "platform__n": $platform_n, "cluster_type_id__n": $cluster_type_id_n, "cluster_type__n": $cluster_type_n, "cluster_group_id__n": $cluster_group_id_n, "cluster_group__n": $cluster_group_n, "cluster_id__n": $cluster_id_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "tag_id__n": $tag_id_n, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/config-contexts/
#
# operationId: extras_config-contexts_bulk_partial_update
export def "extras-config-contexts update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster-groups: list<int>
  --cluster-types: list<int>
  --clusters: list<int>
  data: record
  --description: string
  --device-types: list<int>
  --is-active: oneof<nothing, bool>
  --locations: list<int>
  name: string
  --platforms: list<int>
  --regions: list<int>
  --roles: list<int>
  --site-groups: list<int>
  --sites: list<int>
  --tags: list<string>
  --tenant-groups: list<int>
  --tenants: list<int>
  --weight: int
]: any -> record<cluster_groups: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, cluster_types: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, clusters: table<display: string, id: int, name: string, url: string, virtualmachine_count: int>, created: string, data: record, description: string, device_types: table<device_count: int, display: string, id: int, manufacturer: record, model: string, slug: string, url: string>, display: string, id: int, is_active: bool, last_updated: string, locations: table<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, platforms: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, regions: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, roles: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site_groups: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, sites: table<display: string, id: int, name: string, slug: string, url: string>, tags: list<string>, tenant_groups: table<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, tenants: table<display: string, id: int, name: string, slug: string, url: string>, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/config-contexts/")
  let req_body = {"cluster_groups": $cluster_groups, "cluster_types": $cluster_types, "clusters": $clusters, "data": $data, "description": $description, "device_types": $device_types, "is_active": $is_active, "locations": $locations, "name": $name, "platforms": $platforms, "regions": $regions, "roles": $roles, "site_groups": $site_groups, "sites": $sites, "tags": $tags, "tenant_groups": $tenant_groups, "tenants": $tenants, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/config-contexts/
#
# operationId: extras_config-contexts_create
export def "extras-config-contexts create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster-groups: list<int>
  --cluster-types: list<int>
  --clusters: list<int>
  data: record
  --description: string
  --device-types: list<int>
  --is-active: oneof<nothing, bool>
  --locations: list<int>
  name: string
  --platforms: list<int>
  --regions: list<int>
  --roles: list<int>
  --site-groups: list<int>
  --sites: list<int>
  --tags: list<string>
  --tenant-groups: list<int>
  --tenants: list<int>
  --weight: int
]: any -> record<cluster_groups: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, cluster_types: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, clusters: table<display: string, id: int, name: string, url: string, virtualmachine_count: int>, created: string, data: record, description: string, device_types: table<device_count: int, display: string, id: int, manufacturer: record, model: string, slug: string, url: string>, display: string, id: int, is_active: bool, last_updated: string, locations: table<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, platforms: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, regions: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, roles: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site_groups: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, sites: table<display: string, id: int, name: string, slug: string, url: string>, tags: list<string>, tenant_groups: table<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, tenants: table<display: string, id: int, name: string, slug: string, url: string>, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/config-contexts/")
  let req_body = {"cluster_groups": $cluster_groups, "cluster_types": $cluster_types, "clusters": $clusters, "data": $data, "description": $description, "device_types": $device_types, "is_active": $is_active, "locations": $locations, "name": $name, "platforms": $platforms, "regions": $regions, "roles": $roles, "site_groups": $site_groups, "sites": $sites, "tags": $tags, "tenant_groups": $tenant_groups, "tenants": $tenants, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/config-contexts/
#
# operationId: extras_config-contexts_bulk_update
export def "extras-config-contexts update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster-groups: list<int>
  --cluster-types: list<int>
  --clusters: list<int>
  data: record
  --description: string
  --device-types: list<int>
  --is-active: oneof<nothing, bool>
  --locations: list<int>
  name: string
  --platforms: list<int>
  --regions: list<int>
  --roles: list<int>
  --site-groups: list<int>
  --sites: list<int>
  --tags: list<string>
  --tenant-groups: list<int>
  --tenants: list<int>
  --weight: int
]: any -> record<cluster_groups: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, cluster_types: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, clusters: table<display: string, id: int, name: string, url: string, virtualmachine_count: int>, created: string, data: record, description: string, device_types: table<device_count: int, display: string, id: int, manufacturer: record, model: string, slug: string, url: string>, display: string, id: int, is_active: bool, last_updated: string, locations: table<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, platforms: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, regions: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, roles: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site_groups: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, sites: table<display: string, id: int, name: string, slug: string, url: string>, tags: list<string>, tenant_groups: table<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, tenants: table<display: string, id: int, name: string, slug: string, url: string>, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/config-contexts/")
  let req_body = {"cluster_groups": $cluster_groups, "cluster_types": $cluster_types, "clusters": $clusters, "data": $data, "description": $description, "device_types": $device_types, "is_active": $is_active, "locations": $locations, "name": $name, "platforms": $platforms, "regions": $regions, "roles": $roles, "site_groups": $site_groups, "sites": $sites, "tags": $tags, "tenant_groups": $tenant_groups, "tenants": $tenants, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/config-contexts/{id}/
#
# operationId: extras_config-contexts_delete
export def "extras-config-contexts delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/config-contexts/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/config-contexts/{id}/
#
# operationId: extras_config-contexts_read
export def "extras-config-contexts get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cluster_groups: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, cluster_types: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, clusters: table<display: string, id: int, name: string, url: string, virtualmachine_count: int>, created: string, data: record, description: string, device_types: table<device_count: int, display: string, id: int, manufacturer: record, model: string, slug: string, url: string>, display: string, id: int, is_active: bool, last_updated: string, locations: table<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, platforms: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, regions: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, roles: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site_groups: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, sites: table<display: string, id: int, name: string, slug: string, url: string>, tags: list<string>, tenant_groups: table<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, tenants: table<display: string, id: int, name: string, slug: string, url: string>, url: string, weight: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/config-contexts/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/config-contexts/{id}/
#
# operationId: extras_config-contexts_partial_update
export def "extras-config-contexts update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster-groups: list<int>
  --cluster-types: list<int>
  --clusters: list<int>
  data: record
  --description: string
  --device-types: list<int>
  --is-active: oneof<nothing, bool>
  --locations: list<int>
  name: string
  --platforms: list<int>
  --regions: list<int>
  --roles: list<int>
  --site-groups: list<int>
  --sites: list<int>
  --tags: list<string>
  --tenant-groups: list<int>
  --tenants: list<int>
  --weight: int
]: any -> record<cluster_groups: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, cluster_types: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, clusters: table<display: string, id: int, name: string, url: string, virtualmachine_count: int>, created: string, data: record, description: string, device_types: table<device_count: int, display: string, id: int, manufacturer: record, model: string, slug: string, url: string>, display: string, id: int, is_active: bool, last_updated: string, locations: table<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, platforms: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, regions: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, roles: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site_groups: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, sites: table<display: string, id: int, name: string, slug: string, url: string>, tags: list<string>, tenant_groups: table<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, tenants: table<display: string, id: int, name: string, slug: string, url: string>, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/config-contexts/{id}/"))
  let req_body = {"cluster_groups": $cluster_groups, "cluster_types": $cluster_types, "clusters": $clusters, "data": $data, "description": $description, "device_types": $device_types, "is_active": $is_active, "locations": $locations, "name": $name, "platforms": $platforms, "regions": $regions, "roles": $roles, "site_groups": $site_groups, "sites": $sites, "tags": $tags, "tenant_groups": $tenant_groups, "tenants": $tenants, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/config-contexts/{id}/
#
# operationId: extras_config-contexts_update
export def "extras-config-contexts update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster-groups: list<int>
  --cluster-types: list<int>
  --clusters: list<int>
  data: record
  --description: string
  --device-types: list<int>
  --is-active: oneof<nothing, bool>
  --locations: list<int>
  name: string
  --platforms: list<int>
  --regions: list<int>
  --roles: list<int>
  --site-groups: list<int>
  --sites: list<int>
  --tags: list<string>
  --tenant-groups: list<int>
  --tenants: list<int>
  --weight: int
]: any -> record<cluster_groups: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, cluster_types: table<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, clusters: table<display: string, id: int, name: string, url: string, virtualmachine_count: int>, created: string, data: record, description: string, device_types: table<device_count: int, display: string, id: int, manufacturer: record, model: string, slug: string, url: string>, display: string, id: int, is_active: bool, last_updated: string, locations: table<_depth: int, display: string, id: int, name: string, rack_count: int, slug: string, url: string>, name: string, platforms: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, regions: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, roles: table<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site_groups: table<_depth: int, display: string, id: int, name: string, site_count: int, slug: string, url: string>, sites: table<display: string, id: int, name: string, slug: string, url: string>, tags: list<string>, tenant_groups: table<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, tenants: table<display: string, id: int, name: string, slug: string, url: string>, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/config-contexts/{id}/"))
  let req_body = {"cluster_groups": $cluster_groups, "cluster_types": $cluster_types, "clusters": $clusters, "data": $data, "description": $description, "device_types": $device_types, "is_active": $is_active, "locations": $locations, "name": $name, "platforms": $platforms, "regions": $regions, "roles": $roles, "site_groups": $site_groups, "sites": $sites, "tags": $tags, "tenant_groups": $tenant_groups, "tenants": $tenants, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Read-only list of ContentTypes. Limit results to ContentTypes pertinent to NetBox objects.
#
# GET /extras/content-types/
# operationId: extras_content-types_list
export def "extras-content-types list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: float
  --app-label: string
  --model: string
  --q: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<app_label: string, display: string, id: int, model: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "app_label" $app_label "scalar") (serialize-qp "model" $model "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/content-types/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "app_label": $app_label, "model": $model, "q": $q, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# Read-only list of ContentTypes. Limit results to ContentTypes pertinent to NetBox objects.
#
# GET /extras/content-types/{id}/
# operationId: extras_content-types_read
export def "extras-content-types get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<app_label: string, display: string, id: int, model: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/content-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /extras/custom-fields/
#
# operationId: extras_custom-fields_bulk_delete
export def "extras-custom-fields delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-fields/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/custom-fields/
#
# operationId: extras_custom-fields_list
export def "extras-custom-fields list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --content-types: string
  --name: string
  --group-name: string
  --required: string
  --search-weight: string
  --filter-logic: string
  --ui-visibility: string
  --weight: string
  --description: string
  --q: string
  --type: string
  --content-type-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --content-types-n: string
  --content-types-ic: string
  --content-types-nic: string
  --content-types-iew: string
  --content-types-niew: string
  --content-types-isw: string
  --content-types-nisw: string
  --content-types-ie: string
  --content-types-nie: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --group-name-n: string
  --group-name-ic: string
  --group-name-nic: string
  --group-name-iew: string
  --group-name-niew: string
  --group-name-isw: string
  --group-name-nisw: string
  --group-name-ie: string
  --group-name-nie: string
  --group-name-empty: string
  --search-weight-n: string
  --search-weight-lte: string
  --search-weight-lt: string
  --search-weight-gte: string
  --search-weight-gt: string
  --filter-logic-n: string
  --ui-visibility-n: string
  --weight-n: string
  --weight-lte: string
  --weight-lt: string
  --weight-gte: string
  --weight-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --type-n: string
  --content-type-id-n: string
  --content-type-id-lte: string
  --content-type-id-lt: string
  --content-type-id-gte: string
  --content-type-id-gt: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<choices: list, content_types: list, created: string, data_type: string, default: record, description: string, display: string, filter_logic: record, group_name: string, id: int, label: string, last_updated: string, name: string, object_type: string, required: bool, search_weight: int, type: record, ui_visibility: record, url: string, validation_maximum: int, validation_minimum: int, validation_regex: string, weight: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "content_types" $content_types "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "group_name" $group_name "scalar") (serialize-qp "required" $required "scalar") (serialize-qp "search_weight" $search_weight "scalar") (serialize-qp "filter_logic" $filter_logic "scalar") (serialize-qp "ui_visibility" $ui_visibility "scalar") (serialize-qp "weight" $weight "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "content_types__n" $content_types_n "scalar") (serialize-qp "content_types__ic" $content_types_ic "scalar") (serialize-qp "content_types__nic" $content_types_nic "scalar") (serialize-qp "content_types__iew" $content_types_iew "scalar") (serialize-qp "content_types__niew" $content_types_niew "scalar") (serialize-qp "content_types__isw" $content_types_isw "scalar") (serialize-qp "content_types__nisw" $content_types_nisw "scalar") (serialize-qp "content_types__ie" $content_types_ie "scalar") (serialize-qp "content_types__nie" $content_types_nie "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "group_name__n" $group_name_n "scalar") (serialize-qp "group_name__ic" $group_name_ic "scalar") (serialize-qp "group_name__nic" $group_name_nic "scalar") (serialize-qp "group_name__iew" $group_name_iew "scalar") (serialize-qp "group_name__niew" $group_name_niew "scalar") (serialize-qp "group_name__isw" $group_name_isw "scalar") (serialize-qp "group_name__nisw" $group_name_nisw "scalar") (serialize-qp "group_name__ie" $group_name_ie "scalar") (serialize-qp "group_name__nie" $group_name_nie "scalar") (serialize-qp "group_name__empty" $group_name_empty "scalar") (serialize-qp "search_weight__n" $search_weight_n "scalar") (serialize-qp "search_weight__lte" $search_weight_lte "scalar") (serialize-qp "search_weight__lt" $search_weight_lt "scalar") (serialize-qp "search_weight__gte" $search_weight_gte "scalar") (serialize-qp "search_weight__gt" $search_weight_gt "scalar") (serialize-qp "filter_logic__n" $filter_logic_n "scalar") (serialize-qp "ui_visibility__n" $ui_visibility_n "scalar") (serialize-qp "weight__n" $weight_n "scalar") (serialize-qp "weight__lte" $weight_lte "scalar") (serialize-qp "weight__lt" $weight_lt "scalar") (serialize-qp "weight__gte" $weight_gte "scalar") (serialize-qp "weight__gt" $weight_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "content_type_id__n" $content_type_id_n "scalar") (serialize-qp "content_type_id__lte" $content_type_id_lte "scalar") (serialize-qp "content_type_id__lt" $content_type_id_lt "scalar") (serialize-qp "content_type_id__gte" $content_type_id_gte "scalar") (serialize-qp "content_type_id__gt" $content_type_id_gt "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/custom-fields/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "content_types": $content_types, "name": $name, "group_name": $group_name, "required": $required, "search_weight": $search_weight, "filter_logic": $filter_logic, "ui_visibility": $ui_visibility, "weight": $weight, "description": $description, "q": $q, "type": $type, "content_type_id": $content_type_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "content_types__n": $content_types_n, "content_types__ic": $content_types_ic, "content_types__nic": $content_types_nic, "content_types__iew": $content_types_iew, "content_types__niew": $content_types_niew, "content_types__isw": $content_types_isw, "content_types__nisw": $content_types_nisw, "content_types__ie": $content_types_ie, "content_types__nie": $content_types_nie, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "group_name__n": $group_name_n, "group_name__ic": $group_name_ic, "group_name__nic": $group_name_nic, "group_name__iew": $group_name_iew, "group_name__niew": $group_name_niew, "group_name__isw": $group_name_isw, "group_name__nisw": $group_name_nisw, "group_name__ie": $group_name_ie, "group_name__nie": $group_name_nie, "group_name__empty": $group_name_empty, "search_weight__n": $search_weight_n, "search_weight__lte": $search_weight_lte, "search_weight__lt": $search_weight_lt, "search_weight__gte": $search_weight_gte, "search_weight__gt": $search_weight_gt, "filter_logic__n": $filter_logic_n, "ui_visibility__n": $ui_visibility_n, "weight__n": $weight_n, "weight__lte": $weight_lte, "weight__lt": $weight_lt, "weight__gte": $weight_gte, "weight__gt": $weight_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "type__n": $type_n, "content_type_id__n": $content_type_id_n, "content_type_id__lte": $content_type_id_lte, "content_type_id__lt": $content_type_id_lt, "content_type_id__gte": $content_type_id_gte, "content_type_id__gt": $content_type_id_gt, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/custom-fields/
#
# operationId: extras_custom-fields_bulk_partial_update
export def "extras-custom-fields update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --choices: list<string> # Comma-separated list of available choices (for selection fields) (nullable)
  content_types: list<string>
  --default: record # Default value for the field (must be a JSON value). Encapsulate strings with double quotes (e.g. "Foo"). (nullable)
  --description: string
  --filter-logic: string@filter-logic-completer # Loose matches any instance of a given string; exact matches the entire field.
  --group-name: string # Custom fields within the same group will be displayed together
  --label: string # Name of the field as displayed to users (if not provided, the field's name will be used)
  name: string # Internal field name
  --object-type: string
  --required: oneof<nothing, bool> # If true, this field is required when creating new objects or editing an existing object.
  --search-weight: int # Weighting for search. Lower values are considered more important. Fields with a search weight of zero will be ignored.
  --type: string@type-completer-8 # The type of data this custom field holds
  --ui-visibility: string@ui-visibility-completer # Specifies the visibility of custom field in the UI
  --validation-maximum: int # Maximum allowed value (for numeric fields) (nullable)
  --validation-minimum: int # Minimum allowed value (for numeric fields) (nullable)
  --validation-regex: string # Regular expression to enforce on text field values. Use ^ and $ to force matching of entire string. For example, ^[A-Z]{3}$ will limit values to exactly three uppercase letters.
  --weight: int # Fields with higher weights appear lower in a form.
]: any -> record<choices: list<string>, content_types: list<string>, created: string, data_type: string, default: record, description: string, display: string, filter_logic: record<label: string, value: string>, group_name: string, id: int, label: string, last_updated: string, name: string, object_type: string, required: bool, search_weight: int, type: record<label: string, value: string>, ui_visibility: record<label: string, value: string>, url: string, validation_maximum: int, validation_minimum: int, validation_regex: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-fields/")
  let req_body = {"choices": $choices, "content_types": $content_types, "default": $default, "description": $description, "filter_logic": $filter_logic, "group_name": $group_name, "label": $label, "name": $name, "object_type": $object_type, "required": $required, "search_weight": $search_weight, "type": $type, "ui_visibility": $ui_visibility, "validation_maximum": $validation_maximum, "validation_minimum": $validation_minimum, "validation_regex": $validation_regex, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/custom-fields/
#
# operationId: extras_custom-fields_create
export def "extras-custom-fields create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --choices: list<string> # Comma-separated list of available choices (for selection fields) (nullable)
  content_types: list<string>
  --default: record # Default value for the field (must be a JSON value). Encapsulate strings with double quotes (e.g. "Foo"). (nullable)
  --description: string
  --filter-logic: string@filter-logic-completer # Loose matches any instance of a given string; exact matches the entire field.
  --group-name: string # Custom fields within the same group will be displayed together
  --label: string # Name of the field as displayed to users (if not provided, the field's name will be used)
  name: string # Internal field name
  --object-type: string
  --required: oneof<nothing, bool> # If true, this field is required when creating new objects or editing an existing object.
  --search-weight: int # Weighting for search. Lower values are considered more important. Fields with a search weight of zero will be ignored.
  --type: string@type-completer-8 # The type of data this custom field holds
  --ui-visibility: string@ui-visibility-completer # Specifies the visibility of custom field in the UI
  --validation-maximum: int # Maximum allowed value (for numeric fields) (nullable)
  --validation-minimum: int # Minimum allowed value (for numeric fields) (nullable)
  --validation-regex: string # Regular expression to enforce on text field values. Use ^ and $ to force matching of entire string. For example, ^[A-Z]{3}$ will limit values to exactly three uppercase letters.
  --weight: int # Fields with higher weights appear lower in a form.
]: any -> record<choices: list<string>, content_types: list<string>, created: string, data_type: string, default: record, description: string, display: string, filter_logic: record<label: string, value: string>, group_name: string, id: int, label: string, last_updated: string, name: string, object_type: string, required: bool, search_weight: int, type: record<label: string, value: string>, ui_visibility: record<label: string, value: string>, url: string, validation_maximum: int, validation_minimum: int, validation_regex: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-fields/")
  let req_body = {"choices": $choices, "content_types": $content_types, "default": $default, "description": $description, "filter_logic": $filter_logic, "group_name": $group_name, "label": $label, "name": $name, "object_type": $object_type, "required": $required, "search_weight": $search_weight, "type": $type, "ui_visibility": $ui_visibility, "validation_maximum": $validation_maximum, "validation_minimum": $validation_minimum, "validation_regex": $validation_regex, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/custom-fields/
#
# operationId: extras_custom-fields_bulk_update
export def "extras-custom-fields update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --choices: list<string> # Comma-separated list of available choices (for selection fields) (nullable)
  content_types: list<string>
  --default: record # Default value for the field (must be a JSON value). Encapsulate strings with double quotes (e.g. "Foo"). (nullable)
  --description: string
  --filter-logic: string@filter-logic-completer # Loose matches any instance of a given string; exact matches the entire field.
  --group-name: string # Custom fields within the same group will be displayed together
  --label: string # Name of the field as displayed to users (if not provided, the field's name will be used)
  name: string # Internal field name
  --object-type: string
  --required: oneof<nothing, bool> # If true, this field is required when creating new objects or editing an existing object.
  --search-weight: int # Weighting for search. Lower values are considered more important. Fields with a search weight of zero will be ignored.
  --type: string@type-completer-8 # The type of data this custom field holds
  --ui-visibility: string@ui-visibility-completer # Specifies the visibility of custom field in the UI
  --validation-maximum: int # Maximum allowed value (for numeric fields) (nullable)
  --validation-minimum: int # Minimum allowed value (for numeric fields) (nullable)
  --validation-regex: string # Regular expression to enforce on text field values. Use ^ and $ to force matching of entire string. For example, ^[A-Z]{3}$ will limit values to exactly three uppercase letters.
  --weight: int # Fields with higher weights appear lower in a form.
]: any -> record<choices: list<string>, content_types: list<string>, created: string, data_type: string, default: record, description: string, display: string, filter_logic: record<label: string, value: string>, group_name: string, id: int, label: string, last_updated: string, name: string, object_type: string, required: bool, search_weight: int, type: record<label: string, value: string>, ui_visibility: record<label: string, value: string>, url: string, validation_maximum: int, validation_minimum: int, validation_regex: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-fields/")
  let req_body = {"choices": $choices, "content_types": $content_types, "default": $default, "description": $description, "filter_logic": $filter_logic, "group_name": $group_name, "label": $label, "name": $name, "object_type": $object_type, "required": $required, "search_weight": $search_weight, "type": $type, "ui_visibility": $ui_visibility, "validation_maximum": $validation_maximum, "validation_minimum": $validation_minimum, "validation_regex": $validation_regex, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/custom-fields/{id}/
#
# operationId: extras_custom-fields_delete
export def "extras-custom-fields delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-fields/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/custom-fields/{id}/
#
# operationId: extras_custom-fields_read
export def "extras-custom-fields get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<choices: list<string>, content_types: list<string>, created: string, data_type: string, default: record, description: string, display: string, filter_logic: record<label: string, value: string>, group_name: string, id: int, label: string, last_updated: string, name: string, object_type: string, required: bool, search_weight: int, type: record<label: string, value: string>, ui_visibility: record<label: string, value: string>, url: string, validation_maximum: int, validation_minimum: int, validation_regex: string, weight: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-fields/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/custom-fields/{id}/
#
# operationId: extras_custom-fields_partial_update
export def "extras-custom-fields update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --choices: list<string> # Comma-separated list of available choices (for selection fields) (nullable)
  content_types: list<string>
  --default: record # Default value for the field (must be a JSON value). Encapsulate strings with double quotes (e.g. "Foo"). (nullable)
  --description: string
  --filter-logic: string@filter-logic-completer # Loose matches any instance of a given string; exact matches the entire field.
  --group-name: string # Custom fields within the same group will be displayed together
  --label: string # Name of the field as displayed to users (if not provided, the field's name will be used)
  name: string # Internal field name
  --object-type: string
  --required: oneof<nothing, bool> # If true, this field is required when creating new objects or editing an existing object.
  --search-weight: int # Weighting for search. Lower values are considered more important. Fields with a search weight of zero will be ignored.
  --type: string@type-completer-8 # The type of data this custom field holds
  --ui-visibility: string@ui-visibility-completer # Specifies the visibility of custom field in the UI
  --validation-maximum: int # Maximum allowed value (for numeric fields) (nullable)
  --validation-minimum: int # Minimum allowed value (for numeric fields) (nullable)
  --validation-regex: string # Regular expression to enforce on text field values. Use ^ and $ to force matching of entire string. For example, ^[A-Z]{3}$ will limit values to exactly three uppercase letters.
  --weight: int # Fields with higher weights appear lower in a form.
]: any -> record<choices: list<string>, content_types: list<string>, created: string, data_type: string, default: record, description: string, display: string, filter_logic: record<label: string, value: string>, group_name: string, id: int, label: string, last_updated: string, name: string, object_type: string, required: bool, search_weight: int, type: record<label: string, value: string>, ui_visibility: record<label: string, value: string>, url: string, validation_maximum: int, validation_minimum: int, validation_regex: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-fields/{id}/"))
  let req_body = {"choices": $choices, "content_types": $content_types, "default": $default, "description": $description, "filter_logic": $filter_logic, "group_name": $group_name, "label": $label, "name": $name, "object_type": $object_type, "required": $required, "search_weight": $search_weight, "type": $type, "ui_visibility": $ui_visibility, "validation_maximum": $validation_maximum, "validation_minimum": $validation_minimum, "validation_regex": $validation_regex, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/custom-fields/{id}/
#
# operationId: extras_custom-fields_update
export def "extras-custom-fields update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --choices: list<string> # Comma-separated list of available choices (for selection fields) (nullable)
  content_types: list<string>
  --default: record # Default value for the field (must be a JSON value). Encapsulate strings with double quotes (e.g. "Foo"). (nullable)
  --description: string
  --filter-logic: string@filter-logic-completer # Loose matches any instance of a given string; exact matches the entire field.
  --group-name: string # Custom fields within the same group will be displayed together
  --label: string # Name of the field as displayed to users (if not provided, the field's name will be used)
  name: string # Internal field name
  --object-type: string
  --required: oneof<nothing, bool> # If true, this field is required when creating new objects or editing an existing object.
  --search-weight: int # Weighting for search. Lower values are considered more important. Fields with a search weight of zero will be ignored.
  --type: string@type-completer-8 # The type of data this custom field holds
  --ui-visibility: string@ui-visibility-completer # Specifies the visibility of custom field in the UI
  --validation-maximum: int # Maximum allowed value (for numeric fields) (nullable)
  --validation-minimum: int # Minimum allowed value (for numeric fields) (nullable)
  --validation-regex: string # Regular expression to enforce on text field values. Use ^ and $ to force matching of entire string. For example, ^[A-Z]{3}$ will limit values to exactly three uppercase letters.
  --weight: int # Fields with higher weights appear lower in a form.
]: any -> record<choices: list<string>, content_types: list<string>, created: string, data_type: string, default: record, description: string, display: string, filter_logic: record<label: string, value: string>, group_name: string, id: int, label: string, last_updated: string, name: string, object_type: string, required: bool, search_weight: int, type: record<label: string, value: string>, ui_visibility: record<label: string, value: string>, url: string, validation_maximum: int, validation_minimum: int, validation_regex: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-fields/{id}/"))
  let req_body = {"choices": $choices, "content_types": $content_types, "default": $default, "description": $description, "filter_logic": $filter_logic, "group_name": $group_name, "label": $label, "name": $name, "object_type": $object_type, "required": $required, "search_weight": $search_weight, "type": $type, "ui_visibility": $ui_visibility, "validation_maximum": $validation_maximum, "validation_minimum": $validation_minimum, "validation_regex": $validation_regex, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/custom-links/
#
# operationId: extras_custom-links_bulk_delete
export def "extras-custom-links delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-links/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/custom-links/
#
# operationId: extras_custom-links_list
export def "extras-custom-links list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --content-types: string
  --name: string
  --enabled: string
  --link-text: string
  --link-url: string
  --weight: string
  --group-name: string
  --new-window: string
  --q: string
  --content-type-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --content-types-n: string
  --content-types-ic: string
  --content-types-nic: string
  --content-types-iew: string
  --content-types-niew: string
  --content-types-isw: string
  --content-types-nisw: string
  --content-types-ie: string
  --content-types-nie: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --link-text-n: string
  --link-text-ic: string
  --link-text-nic: string
  --link-text-iew: string
  --link-text-niew: string
  --link-text-isw: string
  --link-text-nisw: string
  --link-text-ie: string
  --link-text-nie: string
  --link-url-n: string
  --link-url-ic: string
  --link-url-nic: string
  --link-url-iew: string
  --link-url-niew: string
  --link-url-isw: string
  --link-url-nisw: string
  --link-url-ie: string
  --link-url-nie: string
  --weight-n: string
  --weight-lte: string
  --weight-lt: string
  --weight-gte: string
  --weight-gt: string
  --group-name-n: string
  --group-name-ic: string
  --group-name-nic: string
  --group-name-iew: string
  --group-name-niew: string
  --group-name-isw: string
  --group-name-nisw: string
  --group-name-ie: string
  --group-name-nie: string
  --group-name-empty: string
  --content-type-id-n: string
  --content-type-id-lte: string
  --content-type-id-lt: string
  --content-type-id-gte: string
  --content-type-id-gt: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<button_class: string, content_types: list, created: string, display: string, enabled: bool, group_name: string, id: int, last_updated: string, link_text: string, link_url: string, name: string, new_window: bool, url: string, weight: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "content_types" $content_types "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "enabled" $enabled "scalar") (serialize-qp "link_text" $link_text "scalar") (serialize-qp "link_url" $link_url "scalar") (serialize-qp "weight" $weight "scalar") (serialize-qp "group_name" $group_name "scalar") (serialize-qp "new_window" $new_window "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "content_types__n" $content_types_n "scalar") (serialize-qp "content_types__ic" $content_types_ic "scalar") (serialize-qp "content_types__nic" $content_types_nic "scalar") (serialize-qp "content_types__iew" $content_types_iew "scalar") (serialize-qp "content_types__niew" $content_types_niew "scalar") (serialize-qp "content_types__isw" $content_types_isw "scalar") (serialize-qp "content_types__nisw" $content_types_nisw "scalar") (serialize-qp "content_types__ie" $content_types_ie "scalar") (serialize-qp "content_types__nie" $content_types_nie "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "link_text__n" $link_text_n "scalar") (serialize-qp "link_text__ic" $link_text_ic "scalar") (serialize-qp "link_text__nic" $link_text_nic "scalar") (serialize-qp "link_text__iew" $link_text_iew "scalar") (serialize-qp "link_text__niew" $link_text_niew "scalar") (serialize-qp "link_text__isw" $link_text_isw "scalar") (serialize-qp "link_text__nisw" $link_text_nisw "scalar") (serialize-qp "link_text__ie" $link_text_ie "scalar") (serialize-qp "link_text__nie" $link_text_nie "scalar") (serialize-qp "link_url__n" $link_url_n "scalar") (serialize-qp "link_url__ic" $link_url_ic "scalar") (serialize-qp "link_url__nic" $link_url_nic "scalar") (serialize-qp "link_url__iew" $link_url_iew "scalar") (serialize-qp "link_url__niew" $link_url_niew "scalar") (serialize-qp "link_url__isw" $link_url_isw "scalar") (serialize-qp "link_url__nisw" $link_url_nisw "scalar") (serialize-qp "link_url__ie" $link_url_ie "scalar") (serialize-qp "link_url__nie" $link_url_nie "scalar") (serialize-qp "weight__n" $weight_n "scalar") (serialize-qp "weight__lte" $weight_lte "scalar") (serialize-qp "weight__lt" $weight_lt "scalar") (serialize-qp "weight__gte" $weight_gte "scalar") (serialize-qp "weight__gt" $weight_gt "scalar") (serialize-qp "group_name__n" $group_name_n "scalar") (serialize-qp "group_name__ic" $group_name_ic "scalar") (serialize-qp "group_name__nic" $group_name_nic "scalar") (serialize-qp "group_name__iew" $group_name_iew "scalar") (serialize-qp "group_name__niew" $group_name_niew "scalar") (serialize-qp "group_name__isw" $group_name_isw "scalar") (serialize-qp "group_name__nisw" $group_name_nisw "scalar") (serialize-qp "group_name__ie" $group_name_ie "scalar") (serialize-qp "group_name__nie" $group_name_nie "scalar") (serialize-qp "group_name__empty" $group_name_empty "scalar") (serialize-qp "content_type_id__n" $content_type_id_n "scalar") (serialize-qp "content_type_id__lte" $content_type_id_lte "scalar") (serialize-qp "content_type_id__lt" $content_type_id_lt "scalar") (serialize-qp "content_type_id__gte" $content_type_id_gte "scalar") (serialize-qp "content_type_id__gt" $content_type_id_gt "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/custom-links/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "content_types": $content_types, "name": $name, "enabled": $enabled, "link_text": $link_text, "link_url": $link_url, "weight": $weight, "group_name": $group_name, "new_window": $new_window, "q": $q, "content_type_id": $content_type_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "content_types__n": $content_types_n, "content_types__ic": $content_types_ic, "content_types__nic": $content_types_nic, "content_types__iew": $content_types_iew, "content_types__niew": $content_types_niew, "content_types__isw": $content_types_isw, "content_types__nisw": $content_types_nisw, "content_types__ie": $content_types_ie, "content_types__nie": $content_types_nie, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "link_text__n": $link_text_n, "link_text__ic": $link_text_ic, "link_text__nic": $link_text_nic, "link_text__iew": $link_text_iew, "link_text__niew": $link_text_niew, "link_text__isw": $link_text_isw, "link_text__nisw": $link_text_nisw, "link_text__ie": $link_text_ie, "link_text__nie": $link_text_nie, "link_url__n": $link_url_n, "link_url__ic": $link_url_ic, "link_url__nic": $link_url_nic, "link_url__iew": $link_url_iew, "link_url__niew": $link_url_niew, "link_url__isw": $link_url_isw, "link_url__nisw": $link_url_nisw, "link_url__ie": $link_url_ie, "link_url__nie": $link_url_nie, "weight__n": $weight_n, "weight__lte": $weight_lte, "weight__lt": $weight_lt, "weight__gte": $weight_gte, "weight__gt": $weight_gt, "group_name__n": $group_name_n, "group_name__ic": $group_name_ic, "group_name__nic": $group_name_nic, "group_name__iew": $group_name_iew, "group_name__niew": $group_name_niew, "group_name__isw": $group_name_isw, "group_name__nisw": $group_name_nisw, "group_name__ie": $group_name_ie, "group_name__nie": $group_name_nie, "group_name__empty": $group_name_empty, "content_type_id__n": $content_type_id_n, "content_type_id__lte": $content_type_id_lte, "content_type_id__lt": $content_type_id_lt, "content_type_id__gte": $content_type_id_gte, "content_type_id__gt": $content_type_id_gt, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/custom-links/
#
# operationId: extras_custom-links_bulk_partial_update
export def "extras-custom-links update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --button-class: string@button-class-completer # The class of the first link in a group will be used for the dropdown button
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --group-name: string # Links with the same group will appear as a dropdown menu
  link_text: string # Jinja2 template code for link text
  link_url: string # Jinja2 template code for link URL
  name: string
  --new-window: oneof<nothing, bool> # Force link to open in a new window
  --weight: int
]: any -> record<button_class: string, content_types: list<string>, created: string, display: string, enabled: bool, group_name: string, id: int, last_updated: string, link_text: string, link_url: string, name: string, new_window: bool, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-links/")
  let req_body = {"button_class": $button_class, "content_types": $content_types, "enabled": $enabled, "group_name": $group_name, "link_text": $link_text, "link_url": $link_url, "name": $name, "new_window": $new_window, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/custom-links/
#
# operationId: extras_custom-links_create
export def "extras-custom-links create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --button-class: string@button-class-completer # The class of the first link in a group will be used for the dropdown button
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --group-name: string # Links with the same group will appear as a dropdown menu
  link_text: string # Jinja2 template code for link text
  link_url: string # Jinja2 template code for link URL
  name: string
  --new-window: oneof<nothing, bool> # Force link to open in a new window
  --weight: int
]: any -> record<button_class: string, content_types: list<string>, created: string, display: string, enabled: bool, group_name: string, id: int, last_updated: string, link_text: string, link_url: string, name: string, new_window: bool, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-links/")
  let req_body = {"button_class": $button_class, "content_types": $content_types, "enabled": $enabled, "group_name": $group_name, "link_text": $link_text, "link_url": $link_url, "name": $name, "new_window": $new_window, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/custom-links/
#
# operationId: extras_custom-links_bulk_update
export def "extras-custom-links update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --button-class: string@button-class-completer # The class of the first link in a group will be used for the dropdown button
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --group-name: string # Links with the same group will appear as a dropdown menu
  link_text: string # Jinja2 template code for link text
  link_url: string # Jinja2 template code for link URL
  name: string
  --new-window: oneof<nothing, bool> # Force link to open in a new window
  --weight: int
]: any -> record<button_class: string, content_types: list<string>, created: string, display: string, enabled: bool, group_name: string, id: int, last_updated: string, link_text: string, link_url: string, name: string, new_window: bool, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/custom-links/")
  let req_body = {"button_class": $button_class, "content_types": $content_types, "enabled": $enabled, "group_name": $group_name, "link_text": $link_text, "link_url": $link_url, "name": $name, "new_window": $new_window, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/custom-links/{id}/
#
# operationId: extras_custom-links_delete
export def "extras-custom-links delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-links/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/custom-links/{id}/
#
# operationId: extras_custom-links_read
export def "extras-custom-links get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<button_class: string, content_types: list<string>, created: string, display: string, enabled: bool, group_name: string, id: int, last_updated: string, link_text: string, link_url: string, name: string, new_window: bool, url: string, weight: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-links/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/custom-links/{id}/
#
# operationId: extras_custom-links_partial_update
export def "extras-custom-links update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --button-class: string@button-class-completer # The class of the first link in a group will be used for the dropdown button
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --group-name: string # Links with the same group will appear as a dropdown menu
  link_text: string # Jinja2 template code for link text
  link_url: string # Jinja2 template code for link URL
  name: string
  --new-window: oneof<nothing, bool> # Force link to open in a new window
  --weight: int
]: any -> record<button_class: string, content_types: list<string>, created: string, display: string, enabled: bool, group_name: string, id: int, last_updated: string, link_text: string, link_url: string, name: string, new_window: bool, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-links/{id}/"))
  let req_body = {"button_class": $button_class, "content_types": $content_types, "enabled": $enabled, "group_name": $group_name, "link_text": $link_text, "link_url": $link_url, "name": $name, "new_window": $new_window, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/custom-links/{id}/
#
# operationId: extras_custom-links_update
export def "extras-custom-links update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --button-class: string@button-class-completer # The class of the first link in a group will be used for the dropdown button
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --group-name: string # Links with the same group will appear as a dropdown menu
  link_text: string # Jinja2 template code for link text
  link_url: string # Jinja2 template code for link URL
  name: string
  --new-window: oneof<nothing, bool> # Force link to open in a new window
  --weight: int
]: any -> record<button_class: string, content_types: list<string>, created: string, display: string, enabled: bool, group_name: string, id: int, last_updated: string, link_text: string, link_url: string, name: string, new_window: bool, url: string, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/custom-links/{id}/"))
  let req_body = {"button_class": $button_class, "content_types": $content_types, "enabled": $enabled, "group_name": $group_name, "link_text": $link_text, "link_url": $link_url, "name": $name, "new_window": $new_window, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/export-templates/
#
# operationId: extras_export-templates_bulk_delete
export def "extras-export-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/export-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/export-templates/
#
# operationId: extras_export-templates_list
export def "extras-export-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --content-types: string
  --name: string
  --description: string
  --q: string
  --content-type-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --content-types-n: string
  --content-types-ic: string
  --content-types-nic: string
  --content-types-iew: string
  --content-types-niew: string
  --content-types-isw: string
  --content-types-nisw: string
  --content-types-ie: string
  --content-types-nie: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --content-type-id-n: string
  --content-type-id-lte: string
  --content-type-id-lt: string
  --content-type-id-gte: string
  --content-type-id-gt: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<as_attachment: bool, content_types: list, created: string, description: string, display: string, file_extension: string, id: int, last_updated: string, mime_type: string, name: string, template_code: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "content_types" $content_types "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "content_types__n" $content_types_n "scalar") (serialize-qp "content_types__ic" $content_types_ic "scalar") (serialize-qp "content_types__nic" $content_types_nic "scalar") (serialize-qp "content_types__iew" $content_types_iew "scalar") (serialize-qp "content_types__niew" $content_types_niew "scalar") (serialize-qp "content_types__isw" $content_types_isw "scalar") (serialize-qp "content_types__nisw" $content_types_nisw "scalar") (serialize-qp "content_types__ie" $content_types_ie "scalar") (serialize-qp "content_types__nie" $content_types_nie "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "content_type_id__n" $content_type_id_n "scalar") (serialize-qp "content_type_id__lte" $content_type_id_lte "scalar") (serialize-qp "content_type_id__lt" $content_type_id_lt "scalar") (serialize-qp "content_type_id__gte" $content_type_id_gte "scalar") (serialize-qp "content_type_id__gt" $content_type_id_gt "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/export-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "content_types": $content_types, "name": $name, "description": $description, "q": $q, "content_type_id": $content_type_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "content_types__n": $content_types_n, "content_types__ic": $content_types_ic, "content_types__nic": $content_types_nic, "content_types__iew": $content_types_iew, "content_types__niew": $content_types_niew, "content_types__isw": $content_types_isw, "content_types__nisw": $content_types_nisw, "content_types__ie": $content_types_ie, "content_types__nie": $content_types_nie, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "content_type_id__n": $content_type_id_n, "content_type_id__lte": $content_type_id_lte, "content_type_id__lt": $content_type_id_lt, "content_type_id__gte": $content_type_id_gte, "content_type_id__gt": $content_type_id_gt, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/export-templates/
#
# operationId: extras_export-templates_bulk_partial_update
export def "extras-export-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --as-attachment: oneof<nothing, bool> # Download file as attachment
  content_types: list<string>
  --description: string
  --file-extension: string # Extension to append to the rendered filename
  --mime-type: string # Defaults to text/plain
  name: string
  template_code: string # Jinja2 template code. The list of objects being exported is passed as a context variable named queryset.
]: any -> record<as_attachment: bool, content_types: list<string>, created: string, description: string, display: string, file_extension: string, id: int, last_updated: string, mime_type: string, name: string, template_code: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/export-templates/")
  let req_body = {"as_attachment": $as_attachment, "content_types": $content_types, "description": $description, "file_extension": $file_extension, "mime_type": $mime_type, "name": $name, "template_code": $template_code} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/export-templates/
#
# operationId: extras_export-templates_create
export def "extras-export-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --as-attachment: oneof<nothing, bool> # Download file as attachment
  content_types: list<string>
  --description: string
  --file-extension: string # Extension to append to the rendered filename
  --mime-type: string # Defaults to text/plain
  name: string
  template_code: string # Jinja2 template code. The list of objects being exported is passed as a context variable named queryset.
]: any -> record<as_attachment: bool, content_types: list<string>, created: string, description: string, display: string, file_extension: string, id: int, last_updated: string, mime_type: string, name: string, template_code: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/export-templates/")
  let req_body = {"as_attachment": $as_attachment, "content_types": $content_types, "description": $description, "file_extension": $file_extension, "mime_type": $mime_type, "name": $name, "template_code": $template_code} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/export-templates/
#
# operationId: extras_export-templates_bulk_update
export def "extras-export-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --as-attachment: oneof<nothing, bool> # Download file as attachment
  content_types: list<string>
  --description: string
  --file-extension: string # Extension to append to the rendered filename
  --mime-type: string # Defaults to text/plain
  name: string
  template_code: string # Jinja2 template code. The list of objects being exported is passed as a context variable named queryset.
]: any -> record<as_attachment: bool, content_types: list<string>, created: string, description: string, display: string, file_extension: string, id: int, last_updated: string, mime_type: string, name: string, template_code: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/export-templates/")
  let req_body = {"as_attachment": $as_attachment, "content_types": $content_types, "description": $description, "file_extension": $file_extension, "mime_type": $mime_type, "name": $name, "template_code": $template_code} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/export-templates/{id}/
#
# operationId: extras_export-templates_delete
export def "extras-export-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/export-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/export-templates/{id}/
#
# operationId: extras_export-templates_read
export def "extras-export-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<as_attachment: bool, content_types: list<string>, created: string, description: string, display: string, file_extension: string, id: int, last_updated: string, mime_type: string, name: string, template_code: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/export-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/export-templates/{id}/
#
# operationId: extras_export-templates_partial_update
export def "extras-export-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --as-attachment: oneof<nothing, bool> # Download file as attachment
  content_types: list<string>
  --description: string
  --file-extension: string # Extension to append to the rendered filename
  --mime-type: string # Defaults to text/plain
  name: string
  template_code: string # Jinja2 template code. The list of objects being exported is passed as a context variable named queryset.
]: any -> record<as_attachment: bool, content_types: list<string>, created: string, description: string, display: string, file_extension: string, id: int, last_updated: string, mime_type: string, name: string, template_code: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/export-templates/{id}/"))
  let req_body = {"as_attachment": $as_attachment, "content_types": $content_types, "description": $description, "file_extension": $file_extension, "mime_type": $mime_type, "name": $name, "template_code": $template_code} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/export-templates/{id}/
#
# operationId: extras_export-templates_update
export def "extras-export-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --as-attachment: oneof<nothing, bool> # Download file as attachment
  content_types: list<string>
  --description: string
  --file-extension: string # Extension to append to the rendered filename
  --mime-type: string # Defaults to text/plain
  name: string
  template_code: string # Jinja2 template code. The list of objects being exported is passed as a context variable named queryset.
]: any -> record<as_attachment: bool, content_types: list<string>, created: string, description: string, display: string, file_extension: string, id: int, last_updated: string, mime_type: string, name: string, template_code: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/export-templates/{id}/"))
  let req_body = {"as_attachment": $as_attachment, "content_types": $content_types, "description": $description, "file_extension": $file_extension, "mime_type": $mime_type, "name": $name, "template_code": $template_code} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/image-attachments/
#
# operationId: extras_image-attachments_bulk_delete
export def "extras-image-attachments delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/image-attachments/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/image-attachments/
#
# operationId: extras_image-attachments_list
export def "extras-image-attachments list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --content-type-id: string
  --object-id: string
  --name: string
  --q: string
  --created: string
  --content-type: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --content-type-id-n: string
  --object-id-n: string
  --object-id-lte: string
  --object-id-lt: string
  --object-id-gte: string
  --object-id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --content-type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<content_type: string, created: string, display: string, id: int, image: string, image_height: int, image_width: int, last_updated: string, name: string, object_id: int, parent: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "object_id" $object_id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "content_type" $content_type "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "content_type_id__n" $content_type_id_n "scalar") (serialize-qp "object_id__n" $object_id_n "scalar") (serialize-qp "object_id__lte" $object_id_lte "scalar") (serialize-qp "object_id__lt" $object_id_lt "scalar") (serialize-qp "object_id__gte" $object_id_gte "scalar") (serialize-qp "object_id__gt" $object_id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "content_type__n" $content_type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/image-attachments/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "content_type_id": $content_type_id, "object_id": $object_id, "name": $name, "q": $q, "created": $created, "content_type": $content_type, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "content_type_id__n": $content_type_id_n, "object_id__n": $object_id_n, "object_id__lte": $object_id_lte, "object_id__lt": $object_id_lt, "object_id__gte": $object_id_gte, "object_id__gt": $object_id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "content_type__n": $content_type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/image-attachments/
#
# operationId: extras_image-attachments_bulk_partial_update
export def "extras-image-attachments update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_type: string
  image_height: int
  image_width: int
  --name: string
  object_id: int
]: any -> record<content_type: string, created: string, display: string, id: int, image: string, image_height: int, image_width: int, last_updated: string, name: string, object_id: int, parent: record, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/image-attachments/")
  let req_body = {"content_type": $content_type, "image_height": $image_height, "image_width": $image_width, "name": $name, "object_id": $object_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/image-attachments/
#
# operationId: extras_image-attachments_create
export def "extras-image-attachments create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_type: string
  image_height: int
  image_width: int
  --name: string
  object_id: int
]: any -> record<content_type: string, created: string, display: string, id: int, image: string, image_height: int, image_width: int, last_updated: string, name: string, object_id: int, parent: record, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/image-attachments/")
  let req_body = {"content_type": $content_type, "image_height": $image_height, "image_width": $image_width, "name": $name, "object_id": $object_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/image-attachments/
#
# operationId: extras_image-attachments_bulk_update
export def "extras-image-attachments update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_type: string
  image_height: int
  image_width: int
  --name: string
  object_id: int
]: any -> record<content_type: string, created: string, display: string, id: int, image: string, image_height: int, image_width: int, last_updated: string, name: string, object_id: int, parent: record, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/image-attachments/")
  let req_body = {"content_type": $content_type, "image_height": $image_height, "image_width": $image_width, "name": $name, "object_id": $object_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/image-attachments/{id}/
#
# operationId: extras_image-attachments_delete
export def "extras-image-attachments delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/image-attachments/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/image-attachments/{id}/
#
# operationId: extras_image-attachments_read
export def "extras-image-attachments get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<content_type: string, created: string, display: string, id: int, image: string, image_height: int, image_width: int, last_updated: string, name: string, object_id: int, parent: record, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/image-attachments/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/image-attachments/{id}/
#
# operationId: extras_image-attachments_partial_update
export def "extras-image-attachments update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_type: string
  image_height: int
  image_width: int
  --name: string
  object_id: int
]: any -> record<content_type: string, created: string, display: string, id: int, image: string, image_height: int, image_width: int, last_updated: string, name: string, object_id: int, parent: record, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/image-attachments/{id}/"))
  let req_body = {"content_type": $content_type, "image_height": $image_height, "image_width": $image_width, "name": $name, "object_id": $object_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/image-attachments/{id}/
#
# operationId: extras_image-attachments_update
export def "extras-image-attachments update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_type: string
  image_height: int
  image_width: int
  --name: string
  object_id: int
]: any -> record<content_type: string, created: string, display: string, id: int, image: string, image_height: int, image_width: int, last_updated: string, name: string, object_id: int, parent: record, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/image-attachments/{id}/"))
  let req_body = {"content_type": $content_type, "image_height": $image_height, "image_width": $image_width, "name": $name, "object_id": $object_id} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Retrieve a list of job results
#
# GET /extras/job-results/
# operationId: extras_job-results_list
export def "extras-job-results list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --interval: string
  --status: string
  --user: string
  --obj-type: string
  --name: string
  --q: string
  --created: string
  --created-before: string
  --created-after: string
  --scheduled: string
  --scheduled-before: string
  --scheduled-after: string
  --started: string
  --started-before: string
  --started-after: string
  --completed: string
  --completed-before: string
  --completed-after: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --interval-n: string
  --interval-lte: string
  --interval-lt: string
  --interval-gte: string
  --interval-gt: string
  --status-n: string
  --user-n: string
  --obj-type-n: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<completed: string, created: string, data: record, display: string, id: int, interval: int, job_id: string, name: string, obj_type: string, scheduled: string, started: string, status: record, url: string, user: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "interval" $interval "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "user" $user "scalar") (serialize-qp "obj_type" $obj_type "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "created__before" $created_before "scalar") (serialize-qp "created__after" $created_after "scalar") (serialize-qp "scheduled" $scheduled "scalar") (serialize-qp "scheduled__before" $scheduled_before "scalar") (serialize-qp "scheduled__after" $scheduled_after "scalar") (serialize-qp "started" $started "scalar") (serialize-qp "started__before" $started_before "scalar") (serialize-qp "started__after" $started_after "scalar") (serialize-qp "completed" $completed "scalar") (serialize-qp "completed__before" $completed_before "scalar") (serialize-qp "completed__after" $completed_after "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "interval__n" $interval_n "scalar") (serialize-qp "interval__lte" $interval_lte "scalar") (serialize-qp "interval__lt" $interval_lt "scalar") (serialize-qp "interval__gte" $interval_gte "scalar") (serialize-qp "interval__gt" $interval_gt "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "user__n" $user_n "scalar") (serialize-qp "obj_type__n" $obj_type_n "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/job-results/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "interval": $interval, "status": $status, "user": $user, "obj_type": $obj_type, "name": $name, "q": $q, "created": $created, "created__before": $created_before, "created__after": $created_after, "scheduled": $scheduled, "scheduled__before": $scheduled_before, "scheduled__after": $scheduled_after, "started": $started, "started__before": $started_before, "started__after": $started_after, "completed": $completed, "completed__before": $completed_before, "completed__after": $completed_after, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "interval__n": $interval_n, "interval__lte": $interval_lte, "interval__lt": $interval_lt, "interval__gte": $interval_gte, "interval__gt": $interval_gt, "status__n": $status_n, "user__n": $user_n, "obj_type__n": $obj_type_n, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# Retrieve a list of job results
#
# GET /extras/job-results/{id}/
# operationId: extras_job-results_read
export def "extras-job-results get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<completed: string, created: string, data: record, display: string, id: int, interval: int, job_id: string, name: string, obj_type: string, scheduled: string, started: string, status: record<label: string, value: string>, url: string, user: record<display: string, id: int, url: string, username: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/job-results/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /extras/journal-entries/
#
# operationId: extras_journal-entries_bulk_delete
export def "extras-journal-entries delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/journal-entries/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/journal-entries/
#
# operationId: extras_journal-entries_list
export def "extras-journal-entries list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --assigned-object-type-id: string
  --assigned-object-id: string
  --created: string
  --kind: string
  --last-updated: string
  --q: string
  --tag: string
  --assigned-object-type: string
  --created-by-id: string
  --created-by: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --assigned-object-type-id-n: string
  --assigned-object-id-n: string
  --assigned-object-id-lte: string
  --assigned-object-id-lt: string
  --assigned-object-id-gte: string
  --assigned-object-id-gt: string
  --kind-n: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --assigned-object-type-n: string
  --created-by-id-n: string
  --created-by-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, created_by: int, custom_fields: record, display: string, id: int, kind: record, last_updated: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "assigned_object_type_id" $assigned_object_type_id "scalar") (serialize-qp "assigned_object_id" $assigned_object_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "kind" $kind "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "assigned_object_type" $assigned_object_type "scalar") (serialize-qp "created_by_id" $created_by_id "scalar") (serialize-qp "created_by" $created_by "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "assigned_object_type_id__n" $assigned_object_type_id_n "scalar") (serialize-qp "assigned_object_id__n" $assigned_object_id_n "scalar") (serialize-qp "assigned_object_id__lte" $assigned_object_id_lte "scalar") (serialize-qp "assigned_object_id__lt" $assigned_object_id_lt "scalar") (serialize-qp "assigned_object_id__gte" $assigned_object_id_gte "scalar") (serialize-qp "assigned_object_id__gt" $assigned_object_id_gt "scalar") (serialize-qp "kind__n" $kind_n "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "assigned_object_type__n" $assigned_object_type_n "scalar") (serialize-qp "created_by_id__n" $created_by_id_n "scalar") (serialize-qp "created_by__n" $created_by_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/journal-entries/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "assigned_object_type_id": $assigned_object_type_id, "assigned_object_id": $assigned_object_id, "created": $created, "kind": $kind, "last_updated": $last_updated, "q": $q, "tag": $tag, "assigned_object_type": $assigned_object_type, "created_by_id": $created_by_id, "created_by": $created_by, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "assigned_object_type_id__n": $assigned_object_type_id_n, "assigned_object_id__n": $assigned_object_id_n, "assigned_object_id__lte": $assigned_object_id_lte, "assigned_object_id__lt": $assigned_object_id_lt, "assigned_object_id__gte": $assigned_object_id_gte, "assigned_object_id__gt": $assigned_object_id_gt, "kind__n": $kind_n, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "assigned_object_type__n": $assigned_object_type_n, "created_by_id__n": $created_by_id_n, "created_by__n": $created_by_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/journal-entries/
#
# operationId: extras_journal-entries_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "extras-journal-entries update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  comments: string
  --created-by: int # nullable
  --custom-fields: record # default: {}
  --kind: string@kind-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, created_by: int, custom_fields: record, display: string, id: int, kind: record<label: string, value: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/journal-entries/")
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "created_by": $created_by, "custom_fields": $custom_fields, "kind": $kind, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/journal-entries/
#
# operationId: extras_journal-entries_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "extras-journal-entries create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  comments: string
  --created-by: int # nullable
  --custom-fields: record # default: {}
  --kind: string@kind-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, created_by: int, custom_fields: record, display: string, id: int, kind: record<label: string, value: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/journal-entries/")
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "created_by": $created_by, "custom_fields": $custom_fields, "kind": $kind, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/journal-entries/
#
# operationId: extras_journal-entries_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "extras-journal-entries update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  comments: string
  --created-by: int # nullable
  --custom-fields: record # default: {}
  --kind: string@kind-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, created_by: int, custom_fields: record, display: string, id: int, kind: record<label: string, value: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/journal-entries/")
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "created_by": $created_by, "custom_fields": $custom_fields, "kind": $kind, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/journal-entries/{id}/
#
# operationId: extras_journal-entries_delete
export def "extras-journal-entries delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/journal-entries/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/journal-entries/{id}/
#
# operationId: extras_journal-entries_read
export def "extras-journal-entries get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, created_by: int, custom_fields: record, display: string, id: int, kind: record<label: string, value: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/journal-entries/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/journal-entries/{id}/
#
# operationId: extras_journal-entries_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "extras-journal-entries update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  comments: string
  --created-by: int # nullable
  --custom-fields: record # default: {}
  --kind: string@kind-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, created_by: int, custom_fields: record, display: string, id: int, kind: record<label: string, value: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/journal-entries/{id}/"))
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "created_by": $created_by, "custom_fields": $custom_fields, "kind": $kind, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/journal-entries/{id}/
#
# operationId: extras_journal-entries_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "extras-journal-entries update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  comments: string
  --created-by: int # nullable
  --custom-fields: record # default: {}
  --kind: string@kind-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, created_by: int, custom_fields: record, display: string, id: int, kind: record<label: string, value: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/journal-entries/{id}/"))
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "created_by": $created_by, "custom_fields": $custom_fields, "kind": $kind, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Retrieve a list of recent changes.
#
# GET /extras/object-changes/
# operationId: extras_object-changes_list
export def "extras-object-changes list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --user: string
  --user-name: string
  --request-id: string
  --action: string
  --changed-object-type-id: string
  --changed-object-id: string
  --object-repr: string
  --q: string
  --time: string
  --changed-object-type: string
  --user-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --user-n: string
  --user-name-n: string
  --user-name-ic: string
  --user-name-nic: string
  --user-name-iew: string
  --user-name-niew: string
  --user-name-isw: string
  --user-name-nisw: string
  --user-name-ie: string
  --user-name-nie: string
  --user-name-empty: string
  --action-n: string
  --changed-object-type-id-n: string
  --changed-object-id-n: string
  --changed-object-id-lte: string
  --changed-object-id-lt: string
  --changed-object-id-gte: string
  --changed-object-id-gt: string
  --object-repr-n: string
  --object-repr-ic: string
  --object-repr-nic: string
  --object-repr-iew: string
  --object-repr-niew: string
  --object-repr-isw: string
  --object-repr-nisw: string
  --object-repr-ie: string
  --object-repr-nie: string
  --object-repr-empty: string
  --changed-object-type-n: string
  --user-id-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<action: record, changed_object: record, changed_object_id: int, changed_object_type: string, display: string, id: int, postchange_data: record, prechange_data: record, request_id: string, time: string, url: string, user: record, user_name: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "user" $user "scalar") (serialize-qp "user_name" $user_name "scalar") (serialize-qp "request_id" $request_id "scalar") (serialize-qp "action" $action "scalar") (serialize-qp "changed_object_type_id" $changed_object_type_id "scalar") (serialize-qp "changed_object_id" $changed_object_id "scalar") (serialize-qp "object_repr" $object_repr "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "time" $time "scalar") (serialize-qp "changed_object_type" $changed_object_type "scalar") (serialize-qp "user_id" $user_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "user__n" $user_n "scalar") (serialize-qp "user_name__n" $user_name_n "scalar") (serialize-qp "user_name__ic" $user_name_ic "scalar") (serialize-qp "user_name__nic" $user_name_nic "scalar") (serialize-qp "user_name__iew" $user_name_iew "scalar") (serialize-qp "user_name__niew" $user_name_niew "scalar") (serialize-qp "user_name__isw" $user_name_isw "scalar") (serialize-qp "user_name__nisw" $user_name_nisw "scalar") (serialize-qp "user_name__ie" $user_name_ie "scalar") (serialize-qp "user_name__nie" $user_name_nie "scalar") (serialize-qp "user_name__empty" $user_name_empty "scalar") (serialize-qp "action__n" $action_n "scalar") (serialize-qp "changed_object_type_id__n" $changed_object_type_id_n "scalar") (serialize-qp "changed_object_id__n" $changed_object_id_n "scalar") (serialize-qp "changed_object_id__lte" $changed_object_id_lte "scalar") (serialize-qp "changed_object_id__lt" $changed_object_id_lt "scalar") (serialize-qp "changed_object_id__gte" $changed_object_id_gte "scalar") (serialize-qp "changed_object_id__gt" $changed_object_id_gt "scalar") (serialize-qp "object_repr__n" $object_repr_n "scalar") (serialize-qp "object_repr__ic" $object_repr_ic "scalar") (serialize-qp "object_repr__nic" $object_repr_nic "scalar") (serialize-qp "object_repr__iew" $object_repr_iew "scalar") (serialize-qp "object_repr__niew" $object_repr_niew "scalar") (serialize-qp "object_repr__isw" $object_repr_isw "scalar") (serialize-qp "object_repr__nisw" $object_repr_nisw "scalar") (serialize-qp "object_repr__ie" $object_repr_ie "scalar") (serialize-qp "object_repr__nie" $object_repr_nie "scalar") (serialize-qp "object_repr__empty" $object_repr_empty "scalar") (serialize-qp "changed_object_type__n" $changed_object_type_n "scalar") (serialize-qp "user_id__n" $user_id_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/object-changes/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "user": $user, "user_name": $user_name, "request_id": $request_id, "action": $action, "changed_object_type_id": $changed_object_type_id, "changed_object_id": $changed_object_id, "object_repr": $object_repr, "q": $q, "time": $time, "changed_object_type": $changed_object_type, "user_id": $user_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "user__n": $user_n, "user_name__n": $user_name_n, "user_name__ic": $user_name_ic, "user_name__nic": $user_name_nic, "user_name__iew": $user_name_iew, "user_name__niew": $user_name_niew, "user_name__isw": $user_name_isw, "user_name__nisw": $user_name_nisw, "user_name__ie": $user_name_ie, "user_name__nie": $user_name_nie, "user_name__empty": $user_name_empty, "action__n": $action_n, "changed_object_type_id__n": $changed_object_type_id_n, "changed_object_id__n": $changed_object_id_n, "changed_object_id__lte": $changed_object_id_lte, "changed_object_id__lt": $changed_object_id_lt, "changed_object_id__gte": $changed_object_id_gte, "changed_object_id__gt": $changed_object_id_gt, "object_repr__n": $object_repr_n, "object_repr__ic": $object_repr_ic, "object_repr__nic": $object_repr_nic, "object_repr__iew": $object_repr_iew, "object_repr__niew": $object_repr_niew, "object_repr__isw": $object_repr_isw, "object_repr__nisw": $object_repr_nisw, "object_repr__ie": $object_repr_ie, "object_repr__nie": $object_repr_nie, "object_repr__empty": $object_repr_empty, "changed_object_type__n": $changed_object_type_n, "user_id__n": $user_id_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# Retrieve a list of recent changes.
#
# GET /extras/object-changes/{id}/
# operationId: extras_object-changes_read
export def "extras-object-changes get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<action: record<label: string, value: string>, changed_object: record, changed_object_id: int, changed_object_type: string, display: string, id: int, postchange_data: record, prechange_data: record, request_id: string, time: string, url: string, user: record<display: string, id: int, url: string, username: string>, user_name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/object-changes/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# Compile all reports and their related results (if any). Result data is deferred in the list view.
#
# GET /extras/reports/
# operationId: extras_reports_list
export def "extras-reports list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/reports/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# Retrieve a single Report identified as ".".
#
# GET /extras/reports/{id}/
# operationId: extras_reports_read
export def "extras-reports get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/reports/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# Run a Report identified as "." and return the pending JobResult as the result
#
# POST /extras/reports/{id}/run/
# operationId: extras_reports_run
export def "extras-reports-run create" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/reports/{id}/run/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /extras/saved-filters/
#
# operationId: extras_saved-filters_bulk_delete
export def "extras-saved-filters delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/saved-filters/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/saved-filters/
#
# operationId: extras_saved-filters_list
export def "extras-saved-filters list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --content-types: string
  --name: string
  --slug: string
  --description: string
  --enabled: string
  --shared: string
  --weight: string
  --q: string
  --content-type-id: string
  --user-id: string
  --user: string
  --usable: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --content-types-n: string
  --content-types-ic: string
  --content-types-nic: string
  --content-types-iew: string
  --content-types-niew: string
  --content-types-isw: string
  --content-types-nisw: string
  --content-types-ie: string
  --content-types-nie: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --weight-n: string
  --weight-lte: string
  --weight-lt: string
  --weight-gte: string
  --weight-gt: string
  --content-type-id-n: string
  --content-type-id-lte: string
  --content-type-id-lt: string
  --content-type-id-gte: string
  --content-type-id-gt: string
  --user-id-n: string
  --user-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<content_types: list, created: string, description: string, display: string, enabled: bool, id: int, last_updated: string, name: string, parameters: record, shared: bool, slug: string, url: string, user: int, weight: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "content_types" $content_types "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "enabled" $enabled "scalar") (serialize-qp "shared" $shared "scalar") (serialize-qp "weight" $weight "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "user_id" $user_id "scalar") (serialize-qp "user" $user "scalar") (serialize-qp "usable" $usable "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "content_types__n" $content_types_n "scalar") (serialize-qp "content_types__ic" $content_types_ic "scalar") (serialize-qp "content_types__nic" $content_types_nic "scalar") (serialize-qp "content_types__iew" $content_types_iew "scalar") (serialize-qp "content_types__niew" $content_types_niew "scalar") (serialize-qp "content_types__isw" $content_types_isw "scalar") (serialize-qp "content_types__nisw" $content_types_nisw "scalar") (serialize-qp "content_types__ie" $content_types_ie "scalar") (serialize-qp "content_types__nie" $content_types_nie "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "weight__n" $weight_n "scalar") (serialize-qp "weight__lte" $weight_lte "scalar") (serialize-qp "weight__lt" $weight_lt "scalar") (serialize-qp "weight__gte" $weight_gte "scalar") (serialize-qp "weight__gt" $weight_gt "scalar") (serialize-qp "content_type_id__n" $content_type_id_n "scalar") (serialize-qp "content_type_id__lte" $content_type_id_lte "scalar") (serialize-qp "content_type_id__lt" $content_type_id_lt "scalar") (serialize-qp "content_type_id__gte" $content_type_id_gte "scalar") (serialize-qp "content_type_id__gt" $content_type_id_gt "scalar") (serialize-qp "user_id__n" $user_id_n "scalar") (serialize-qp "user__n" $user_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/saved-filters/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "content_types": $content_types, "name": $name, "slug": $slug, "description": $description, "enabled": $enabled, "shared": $shared, "weight": $weight, "q": $q, "content_type_id": $content_type_id, "user_id": $user_id, "user": $user, "usable": $usable, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "content_types__n": $content_types_n, "content_types__ic": $content_types_ic, "content_types__nic": $content_types_nic, "content_types__iew": $content_types_iew, "content_types__niew": $content_types_niew, "content_types__isw": $content_types_isw, "content_types__nisw": $content_types_nisw, "content_types__ie": $content_types_ie, "content_types__nie": $content_types_nie, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "weight__n": $weight_n, "weight__lte": $weight_lte, "weight__lt": $weight_lt, "weight__gte": $weight_gte, "weight__gt": $weight_gt, "content_type_id__n": $content_type_id_n, "content_type_id__lte": $content_type_id_lte, "content_type_id__lt": $content_type_id_lt, "content_type_id__gte": $content_type_id_gte, "content_type_id__gt": $content_type_id_gt, "user_id__n": $user_id_n, "user__n": $user_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/saved-filters/
#
# operationId: extras_saved-filters_bulk_partial_update
export def "extras-saved-filters update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_types: list<string>
  --description: string
  --enabled: oneof<nothing, bool>
  name: string
  parameters: record
  --shared: oneof<nothing, bool>
  slug: string # format: slug
  --user: int # nullable
  --weight: int
]: any -> record<content_types: list<string>, created: string, description: string, display: string, enabled: bool, id: int, last_updated: string, name: string, parameters: record, shared: bool, slug: string, url: string, user: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/saved-filters/")
  let req_body = {"content_types": $content_types, "description": $description, "enabled": $enabled, "name": $name, "parameters": $parameters, "shared": $shared, "slug": $slug, "user": $user, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/saved-filters/
#
# operationId: extras_saved-filters_create
export def "extras-saved-filters create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_types: list<string>
  --description: string
  --enabled: oneof<nothing, bool>
  name: string
  parameters: record
  --shared: oneof<nothing, bool>
  slug: string # format: slug
  --user: int # nullable
  --weight: int
]: any -> record<content_types: list<string>, created: string, description: string, display: string, enabled: bool, id: int, last_updated: string, name: string, parameters: record, shared: bool, slug: string, url: string, user: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/saved-filters/")
  let req_body = {"content_types": $content_types, "description": $description, "enabled": $enabled, "name": $name, "parameters": $parameters, "shared": $shared, "slug": $slug, "user": $user, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/saved-filters/
#
# operationId: extras_saved-filters_bulk_update
export def "extras-saved-filters update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_types: list<string>
  --description: string
  --enabled: oneof<nothing, bool>
  name: string
  parameters: record
  --shared: oneof<nothing, bool>
  slug: string # format: slug
  --user: int # nullable
  --weight: int
]: any -> record<content_types: list<string>, created: string, description: string, display: string, enabled: bool, id: int, last_updated: string, name: string, parameters: record, shared: bool, slug: string, url: string, user: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/saved-filters/")
  let req_body = {"content_types": $content_types, "description": $description, "enabled": $enabled, "name": $name, "parameters": $parameters, "shared": $shared, "slug": $slug, "user": $user, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/saved-filters/{id}/
#
# operationId: extras_saved-filters_delete
export def "extras-saved-filters delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/saved-filters/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/saved-filters/{id}/
#
# operationId: extras_saved-filters_read
export def "extras-saved-filters get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<content_types: list<string>, created: string, description: string, display: string, enabled: bool, id: int, last_updated: string, name: string, parameters: record, shared: bool, slug: string, url: string, user: int, weight: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/saved-filters/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/saved-filters/{id}/
#
# operationId: extras_saved-filters_partial_update
export def "extras-saved-filters update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_types: list<string>
  --description: string
  --enabled: oneof<nothing, bool>
  name: string
  parameters: record
  --shared: oneof<nothing, bool>
  slug: string # format: slug
  --user: int # nullable
  --weight: int
]: any -> record<content_types: list<string>, created: string, description: string, display: string, enabled: bool, id: int, last_updated: string, name: string, parameters: record, shared: bool, slug: string, url: string, user: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/saved-filters/{id}/"))
  let req_body = {"content_types": $content_types, "description": $description, "enabled": $enabled, "name": $name, "parameters": $parameters, "shared": $shared, "slug": $slug, "user": $user, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/saved-filters/{id}/
#
# operationId: extras_saved-filters_update
export def "extras-saved-filters update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  content_types: list<string>
  --description: string
  --enabled: oneof<nothing, bool>
  name: string
  parameters: record
  --shared: oneof<nothing, bool>
  slug: string # format: slug
  --user: int # nullable
  --weight: int
]: any -> record<content_types: list<string>, created: string, description: string, display: string, enabled: bool, id: int, last_updated: string, name: string, parameters: record, shared: bool, slug: string, url: string, user: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/saved-filters/{id}/"))
  let req_body = {"content_types": $content_types, "description": $description, "enabled": $enabled, "name": $name, "parameters": $parameters, "shared": $shared, "slug": $slug, "user": $user, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# GET /extras/scripts/
#
# operationId: extras_scripts_list
export def "extras-scripts list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/scripts/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/scripts/{id}/
#
# operationId: extras_scripts_read
export def "extras-scripts get" [
  id: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/scripts/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /extras/tags/
#
# operationId: extras_tags_bulk_delete
export def "extras-tags delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/tags/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/tags/
#
# operationId: extras_tags_list
export def "extras-tags list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --color: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --content-type: string
  --content-type-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --color-n: string
  --color-ic: string
  --color-nic: string
  --color-iew: string
  --color-niew: string
  --color-isw: string
  --color-nisw: string
  --color-ie: string
  --color-nie: string
  --color-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<color: string, created: string, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tagged_items: int, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "color" $color "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "content_type" $content_type "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "color__n" $color_n "scalar") (serialize-qp "color__ic" $color_ic "scalar") (serialize-qp "color__nic" $color_nic "scalar") (serialize-qp "color__iew" $color_iew "scalar") (serialize-qp "color__niew" $color_niew "scalar") (serialize-qp "color__isw" $color_isw "scalar") (serialize-qp "color__nisw" $color_nisw "scalar") (serialize-qp "color__ie" $color_ie "scalar") (serialize-qp "color__nie" $color_nie "scalar") (serialize-qp "color__empty" $color_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/tags/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "color": $color, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "content_type": $content_type, "content_type_id": $content_type_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "color__n": $color_n, "color__ic": $color_ic, "color__nic": $color_nic, "color__iew": $color_iew, "color__niew": $color_niew, "color__isw": $color_isw, "color__nisw": $color_nisw, "color__ie": $color_ie, "color__nie": $color_nie, "color__empty": $color_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/tags/
#
# operationId: extras_tags_bulk_partial_update
export def "extras-tags update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  name: string
  slug: string # format: slug
]: any -> record<color: string, created: string, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tagged_items: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/tags/")
  let req_body = {"color": $color, "description": $description, "name": $name, "slug": $slug} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/tags/
#
# operationId: extras_tags_create
export def "extras-tags create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  name: string
  slug: string # format: slug
]: any -> record<color: string, created: string, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tagged_items: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/tags/")
  let req_body = {"color": $color, "description": $description, "name": $name, "slug": $slug} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/tags/
#
# operationId: extras_tags_bulk_update
export def "extras-tags update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  name: string
  slug: string # format: slug
]: any -> record<color: string, created: string, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tagged_items: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/tags/")
  let req_body = {"color": $color, "description": $description, "name": $name, "slug": $slug} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/tags/{id}/
#
# operationId: extras_tags_delete
export def "extras-tags delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/tags/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/tags/{id}/
#
# operationId: extras_tags_read
export def "extras-tags get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<color: string, created: string, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tagged_items: int, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/tags/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/tags/{id}/
#
# operationId: extras_tags_partial_update
export def "extras-tags update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  name: string
  slug: string # format: slug
]: any -> record<color: string, created: string, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tagged_items: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/tags/{id}/"))
  let req_body = {"color": $color, "description": $description, "name": $name, "slug": $slug} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/tags/{id}/
#
# operationId: extras_tags_update
export def "extras-tags update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --color: string
  --description: string
  name: string
  slug: string # format: slug
]: any -> record<color: string, created: string, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tagged_items: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/tags/{id}/"))
  let req_body = {"color": $color, "description": $description, "name": $name, "slug": $slug} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/webhooks/
#
# operationId: extras_webhooks_bulk_delete
export def "extras-webhooks delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/webhooks/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/webhooks/
#
# operationId: extras_webhooks_list
export def "extras-webhooks list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --type-create: string
  --type-update: string
  --type-delete: string
  --payload-url: string
  --enabled: string
  --http-method: string
  --http-content-type: string
  --secret: string
  --ssl-verification: string
  --ca-file-path: string
  --q: string
  --content-type-id: string
  --content-types: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --payload-url-n: string
  --payload-url-ic: string
  --payload-url-nic: string
  --payload-url-iew: string
  --payload-url-niew: string
  --payload-url-isw: string
  --payload-url-nisw: string
  --payload-url-ie: string
  --payload-url-nie: string
  --payload-url-empty: string
  --http-method-n: string
  --http-content-type-n: string
  --http-content-type-ic: string
  --http-content-type-nic: string
  --http-content-type-iew: string
  --http-content-type-niew: string
  --http-content-type-isw: string
  --http-content-type-nisw: string
  --http-content-type-ie: string
  --http-content-type-nie: string
  --http-content-type-empty: string
  --secret-n: string
  --secret-ic: string
  --secret-nic: string
  --secret-iew: string
  --secret-niew: string
  --secret-isw: string
  --secret-nisw: string
  --secret-ie: string
  --secret-nie: string
  --secret-empty: string
  --ca-file-path-n: string
  --ca-file-path-ic: string
  --ca-file-path-nic: string
  --ca-file-path-iew: string
  --ca-file-path-niew: string
  --ca-file-path-isw: string
  --ca-file-path-nisw: string
  --ca-file-path-ie: string
  --ca-file-path-nie: string
  --ca-file-path-empty: string
  --content-type-id-n: string
  --content-type-id-lte: string
  --content-type-id-lt: string
  --content-type-id-gte: string
  --content-type-id-gt: string
  --content-types-n: string
  --content-types-ic: string
  --content-types-nic: string
  --content-types-iew: string
  --content-types-niew: string
  --content-types-isw: string
  --content-types-nisw: string
  --content-types-ie: string
  --content-types-nie: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<additional_headers: string, body_template: string, ca_file_path: string, conditions: record, content_types: list, created: string, display: string, enabled: bool, http_content_type: string, http_method: string, id: int, last_updated: string, name: string, payload_url: string, secret: string, ssl_verification: bool, type_create: bool, type_delete: bool, type_update: bool, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "type_create" $type_create "scalar") (serialize-qp "type_update" $type_update "scalar") (serialize-qp "type_delete" $type_delete "scalar") (serialize-qp "payload_url" $payload_url "scalar") (serialize-qp "enabled" $enabled "scalar") (serialize-qp "http_method" $http_method "scalar") (serialize-qp "http_content_type" $http_content_type "scalar") (serialize-qp "secret" $secret "scalar") (serialize-qp "ssl_verification" $ssl_verification "scalar") (serialize-qp "ca_file_path" $ca_file_path "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "content_types" $content_types "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "payload_url__n" $payload_url_n "scalar") (serialize-qp "payload_url__ic" $payload_url_ic "scalar") (serialize-qp "payload_url__nic" $payload_url_nic "scalar") (serialize-qp "payload_url__iew" $payload_url_iew "scalar") (serialize-qp "payload_url__niew" $payload_url_niew "scalar") (serialize-qp "payload_url__isw" $payload_url_isw "scalar") (serialize-qp "payload_url__nisw" $payload_url_nisw "scalar") (serialize-qp "payload_url__ie" $payload_url_ie "scalar") (serialize-qp "payload_url__nie" $payload_url_nie "scalar") (serialize-qp "payload_url__empty" $payload_url_empty "scalar") (serialize-qp "http_method__n" $http_method_n "scalar") (serialize-qp "http_content_type__n" $http_content_type_n "scalar") (serialize-qp "http_content_type__ic" $http_content_type_ic "scalar") (serialize-qp "http_content_type__nic" $http_content_type_nic "scalar") (serialize-qp "http_content_type__iew" $http_content_type_iew "scalar") (serialize-qp "http_content_type__niew" $http_content_type_niew "scalar") (serialize-qp "http_content_type__isw" $http_content_type_isw "scalar") (serialize-qp "http_content_type__nisw" $http_content_type_nisw "scalar") (serialize-qp "http_content_type__ie" $http_content_type_ie "scalar") (serialize-qp "http_content_type__nie" $http_content_type_nie "scalar") (serialize-qp "http_content_type__empty" $http_content_type_empty "scalar") (serialize-qp "secret__n" $secret_n "scalar") (serialize-qp "secret__ic" $secret_ic "scalar") (serialize-qp "secret__nic" $secret_nic "scalar") (serialize-qp "secret__iew" $secret_iew "scalar") (serialize-qp "secret__niew" $secret_niew "scalar") (serialize-qp "secret__isw" $secret_isw "scalar") (serialize-qp "secret__nisw" $secret_nisw "scalar") (serialize-qp "secret__ie" $secret_ie "scalar") (serialize-qp "secret__nie" $secret_nie "scalar") (serialize-qp "secret__empty" $secret_empty "scalar") (serialize-qp "ca_file_path__n" $ca_file_path_n "scalar") (serialize-qp "ca_file_path__ic" $ca_file_path_ic "scalar") (serialize-qp "ca_file_path__nic" $ca_file_path_nic "scalar") (serialize-qp "ca_file_path__iew" $ca_file_path_iew "scalar") (serialize-qp "ca_file_path__niew" $ca_file_path_niew "scalar") (serialize-qp "ca_file_path__isw" $ca_file_path_isw "scalar") (serialize-qp "ca_file_path__nisw" $ca_file_path_nisw "scalar") (serialize-qp "ca_file_path__ie" $ca_file_path_ie "scalar") (serialize-qp "ca_file_path__nie" $ca_file_path_nie "scalar") (serialize-qp "ca_file_path__empty" $ca_file_path_empty "scalar") (serialize-qp "content_type_id__n" $content_type_id_n "scalar") (serialize-qp "content_type_id__lte" $content_type_id_lte "scalar") (serialize-qp "content_type_id__lt" $content_type_id_lt "scalar") (serialize-qp "content_type_id__gte" $content_type_id_gte "scalar") (serialize-qp "content_type_id__gt" $content_type_id_gt "scalar") (serialize-qp "content_types__n" $content_types_n "scalar") (serialize-qp "content_types__ic" $content_types_ic "scalar") (serialize-qp "content_types__nic" $content_types_nic "scalar") (serialize-qp "content_types__iew" $content_types_iew "scalar") (serialize-qp "content_types__niew" $content_types_niew "scalar") (serialize-qp "content_types__isw" $content_types_isw "scalar") (serialize-qp "content_types__nisw" $content_types_nisw "scalar") (serialize-qp "content_types__ie" $content_types_ie "scalar") (serialize-qp "content_types__nie" $content_types_nie "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/extras/webhooks/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "type_create": $type_create, "type_update": $type_update, "type_delete": $type_delete, "payload_url": $payload_url, "enabled": $enabled, "http_method": $http_method, "http_content_type": $http_content_type, "secret": $secret, "ssl_verification": $ssl_verification, "ca_file_path": $ca_file_path, "q": $q, "content_type_id": $content_type_id, "content_types": $content_types, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "payload_url__n": $payload_url_n, "payload_url__ic": $payload_url_ic, "payload_url__nic": $payload_url_nic, "payload_url__iew": $payload_url_iew, "payload_url__niew": $payload_url_niew, "payload_url__isw": $payload_url_isw, "payload_url__nisw": $payload_url_nisw, "payload_url__ie": $payload_url_ie, "payload_url__nie": $payload_url_nie, "payload_url__empty": $payload_url_empty, "http_method__n": $http_method_n, "http_content_type__n": $http_content_type_n, "http_content_type__ic": $http_content_type_ic, "http_content_type__nic": $http_content_type_nic, "http_content_type__iew": $http_content_type_iew, "http_content_type__niew": $http_content_type_niew, "http_content_type__isw": $http_content_type_isw, "http_content_type__nisw": $http_content_type_nisw, "http_content_type__ie": $http_content_type_ie, "http_content_type__nie": $http_content_type_nie, "http_content_type__empty": $http_content_type_empty, "secret__n": $secret_n, "secret__ic": $secret_ic, "secret__nic": $secret_nic, "secret__iew": $secret_iew, "secret__niew": $secret_niew, "secret__isw": $secret_isw, "secret__nisw": $secret_nisw, "secret__ie": $secret_ie, "secret__nie": $secret_nie, "secret__empty": $secret_empty, "ca_file_path__n": $ca_file_path_n, "ca_file_path__ic": $ca_file_path_ic, "ca_file_path__nic": $ca_file_path_nic, "ca_file_path__iew": $ca_file_path_iew, "ca_file_path__niew": $ca_file_path_niew, "ca_file_path__isw": $ca_file_path_isw, "ca_file_path__nisw": $ca_file_path_nisw, "ca_file_path__ie": $ca_file_path_ie, "ca_file_path__nie": $ca_file_path_nie, "ca_file_path__empty": $ca_file_path_empty, "content_type_id__n": $content_type_id_n, "content_type_id__lte": $content_type_id_lte, "content_type_id__lt": $content_type_id_lt, "content_type_id__gte": $content_type_id_gte, "content_type_id__gt": $content_type_id_gt, "content_types__n": $content_types_n, "content_types__ic": $content_types_ic, "content_types__nic": $content_types_nic, "content_types__iew": $content_types_iew, "content_types__niew": $content_types_niew, "content_types__isw": $content_types_isw, "content_types__nisw": $content_types_nisw, "content_types__ie": $content_types_ie, "content_types__nie": $content_types_nie, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /extras/webhooks/
#
# operationId: extras_webhooks_bulk_partial_update
export def "extras-webhooks update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --additional-headers: string # User-supplied HTTP headers to be sent with the request in addition to the HTTP content type. Headers should be defined in the format Name: Value. Jinja2 template processing is supported with the same context as the request body (below).
  --body-template: string # Jinja2 template for a custom request body. If blank, a JSON object representing the change will be included. Available context data includes: event, model, timestamp, username, request_id, and data.
  --ca-file-path: string # The specific CA certificate file to use for SSL verification. Leave blank to use the system defaults. (nullable)
  --conditions: record # A set of conditions which determine whether the webhook will be generated. (nullable)
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --http-content-type: string # The complete list of official content types is available here (https://www.iana.org/assignments/media-types/media-types.xhtml).
  --http-method: string@http-method-completer
  name: string
  payload_url: string # This URL will be called using the HTTP method defined when the webhook is called. Jinja2 template processing is supported with the same context as the request body.
  --secret: string # When provided, the request will include a 'X-Hook-Signature' header containing a HMAC hex digest of the payload body using the secret as the key. The secret is not transmitted in the request.
  --ssl-verification: oneof<nothing, bool> # Enable SSL certificate verification. Disable with caution!
  --type-create: oneof<nothing, bool> # Call this webhook when a matching object is created.
  --type-delete: oneof<nothing, bool> # Call this webhook when a matching object is deleted.
  --type-update: oneof<nothing, bool> # Call this webhook when a matching object is updated.
]: any -> record<additional_headers: string, body_template: string, ca_file_path: string, conditions: record, content_types: list<string>, created: string, display: string, enabled: bool, http_content_type: string, http_method: string, id: int, last_updated: string, name: string, payload_url: string, secret: string, ssl_verification: bool, type_create: bool, type_delete: bool, type_update: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/webhooks/")
  let req_body = {"additional_headers": $additional_headers, "body_template": $body_template, "ca_file_path": $ca_file_path, "conditions": $conditions, "content_types": $content_types, "enabled": $enabled, "http_content_type": $http_content_type, "http_method": $http_method, "name": $name, "payload_url": $payload_url, "secret": $secret, "ssl_verification": $ssl_verification, "type_create": $type_create, "type_delete": $type_delete, "type_update": $type_update} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /extras/webhooks/
#
# operationId: extras_webhooks_create
export def "extras-webhooks create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --additional-headers: string # User-supplied HTTP headers to be sent with the request in addition to the HTTP content type. Headers should be defined in the format Name: Value. Jinja2 template processing is supported with the same context as the request body (below).
  --body-template: string # Jinja2 template for a custom request body. If blank, a JSON object representing the change will be included. Available context data includes: event, model, timestamp, username, request_id, and data.
  --ca-file-path: string # The specific CA certificate file to use for SSL verification. Leave blank to use the system defaults. (nullable)
  --conditions: record # A set of conditions which determine whether the webhook will be generated. (nullable)
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --http-content-type: string # The complete list of official content types is available here (https://www.iana.org/assignments/media-types/media-types.xhtml).
  --http-method: string@http-method-completer
  name: string
  payload_url: string # This URL will be called using the HTTP method defined when the webhook is called. Jinja2 template processing is supported with the same context as the request body.
  --secret: string # When provided, the request will include a 'X-Hook-Signature' header containing a HMAC hex digest of the payload body using the secret as the key. The secret is not transmitted in the request.
  --ssl-verification: oneof<nothing, bool> # Enable SSL certificate verification. Disable with caution!
  --type-create: oneof<nothing, bool> # Call this webhook when a matching object is created.
  --type-delete: oneof<nothing, bool> # Call this webhook when a matching object is deleted.
  --type-update: oneof<nothing, bool> # Call this webhook when a matching object is updated.
]: any -> record<additional_headers: string, body_template: string, ca_file_path: string, conditions: record, content_types: list<string>, created: string, display: string, enabled: bool, http_content_type: string, http_method: string, id: int, last_updated: string, name: string, payload_url: string, secret: string, ssl_verification: bool, type_create: bool, type_delete: bool, type_update: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/webhooks/")
  let req_body = {"additional_headers": $additional_headers, "body_template": $body_template, "ca_file_path": $ca_file_path, "conditions": $conditions, "content_types": $content_types, "enabled": $enabled, "http_content_type": $http_content_type, "http_method": $http_method, "name": $name, "payload_url": $payload_url, "secret": $secret, "ssl_verification": $ssl_verification, "type_create": $type_create, "type_delete": $type_delete, "type_update": $type_update} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/webhooks/
#
# operationId: extras_webhooks_bulk_update
export def "extras-webhooks update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --additional-headers: string # User-supplied HTTP headers to be sent with the request in addition to the HTTP content type. Headers should be defined in the format Name: Value. Jinja2 template processing is supported with the same context as the request body (below).
  --body-template: string # Jinja2 template for a custom request body. If blank, a JSON object representing the change will be included. Available context data includes: event, model, timestamp, username, request_id, and data.
  --ca-file-path: string # The specific CA certificate file to use for SSL verification. Leave blank to use the system defaults. (nullable)
  --conditions: record # A set of conditions which determine whether the webhook will be generated. (nullable)
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --http-content-type: string # The complete list of official content types is available here (https://www.iana.org/assignments/media-types/media-types.xhtml).
  --http-method: string@http-method-completer
  name: string
  payload_url: string # This URL will be called using the HTTP method defined when the webhook is called. Jinja2 template processing is supported with the same context as the request body.
  --secret: string # When provided, the request will include a 'X-Hook-Signature' header containing a HMAC hex digest of the payload body using the secret as the key. The secret is not transmitted in the request.
  --ssl-verification: oneof<nothing, bool> # Enable SSL certificate verification. Disable with caution!
  --type-create: oneof<nothing, bool> # Call this webhook when a matching object is created.
  --type-delete: oneof<nothing, bool> # Call this webhook when a matching object is deleted.
  --type-update: oneof<nothing, bool> # Call this webhook when a matching object is updated.
]: any -> record<additional_headers: string, body_template: string, ca_file_path: string, conditions: record, content_types: list<string>, created: string, display: string, enabled: bool, http_content_type: string, http_method: string, id: int, last_updated: string, name: string, payload_url: string, secret: string, ssl_verification: bool, type_create: bool, type_delete: bool, type_update: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/extras/webhooks/")
  let req_body = {"additional_headers": $additional_headers, "body_template": $body_template, "ca_file_path": $ca_file_path, "conditions": $conditions, "content_types": $content_types, "enabled": $enabled, "http_content_type": $http_content_type, "http_method": $http_method, "name": $name, "payload_url": $payload_url, "secret": $secret, "ssl_verification": $ssl_verification, "type_create": $type_create, "type_delete": $type_delete, "type_update": $type_update} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /extras/webhooks/{id}/
#
# operationId: extras_webhooks_delete
export def "extras-webhooks delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/webhooks/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /extras/webhooks/{id}/
#
# operationId: extras_webhooks_read
export def "extras-webhooks get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<additional_headers: string, body_template: string, ca_file_path: string, conditions: record, content_types: list<string>, created: string, display: string, enabled: bool, http_content_type: string, http_method: string, id: int, last_updated: string, name: string, payload_url: string, secret: string, ssl_verification: bool, type_create: bool, type_delete: bool, type_update: bool, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/webhooks/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /extras/webhooks/{id}/
#
# operationId: extras_webhooks_partial_update
export def "extras-webhooks update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --additional-headers: string # User-supplied HTTP headers to be sent with the request in addition to the HTTP content type. Headers should be defined in the format Name: Value. Jinja2 template processing is supported with the same context as the request body (below).
  --body-template: string # Jinja2 template for a custom request body. If blank, a JSON object representing the change will be included. Available context data includes: event, model, timestamp, username, request_id, and data.
  --ca-file-path: string # The specific CA certificate file to use for SSL verification. Leave blank to use the system defaults. (nullable)
  --conditions: record # A set of conditions which determine whether the webhook will be generated. (nullable)
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --http-content-type: string # The complete list of official content types is available here (https://www.iana.org/assignments/media-types/media-types.xhtml).
  --http-method: string@http-method-completer
  name: string
  payload_url: string # This URL will be called using the HTTP method defined when the webhook is called. Jinja2 template processing is supported with the same context as the request body.
  --secret: string # When provided, the request will include a 'X-Hook-Signature' header containing a HMAC hex digest of the payload body using the secret as the key. The secret is not transmitted in the request.
  --ssl-verification: oneof<nothing, bool> # Enable SSL certificate verification. Disable with caution!
  --type-create: oneof<nothing, bool> # Call this webhook when a matching object is created.
  --type-delete: oneof<nothing, bool> # Call this webhook when a matching object is deleted.
  --type-update: oneof<nothing, bool> # Call this webhook when a matching object is updated.
]: any -> record<additional_headers: string, body_template: string, ca_file_path: string, conditions: record, content_types: list<string>, created: string, display: string, enabled: bool, http_content_type: string, http_method: string, id: int, last_updated: string, name: string, payload_url: string, secret: string, ssl_verification: bool, type_create: bool, type_delete: bool, type_update: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/webhooks/{id}/"))
  let req_body = {"additional_headers": $additional_headers, "body_template": $body_template, "ca_file_path": $ca_file_path, "conditions": $conditions, "content_types": $content_types, "enabled": $enabled, "http_content_type": $http_content_type, "http_method": $http_method, "name": $name, "payload_url": $payload_url, "secret": $secret, "ssl_verification": $ssl_verification, "type_create": $type_create, "type_delete": $type_delete, "type_update": $type_update} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /extras/webhooks/{id}/
#
# operationId: extras_webhooks_update
export def "extras-webhooks update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --additional-headers: string # User-supplied HTTP headers to be sent with the request in addition to the HTTP content type. Headers should be defined in the format Name: Value. Jinja2 template processing is supported with the same context as the request body (below).
  --body-template: string # Jinja2 template for a custom request body. If blank, a JSON object representing the change will be included. Available context data includes: event, model, timestamp, username, request_id, and data.
  --ca-file-path: string # The specific CA certificate file to use for SSL verification. Leave blank to use the system defaults. (nullable)
  --conditions: record # A set of conditions which determine whether the webhook will be generated. (nullable)
  content_types: list<string>
  --enabled: oneof<nothing, bool>
  --http-content-type: string # The complete list of official content types is available here (https://www.iana.org/assignments/media-types/media-types.xhtml).
  --http-method: string@http-method-completer
  name: string
  payload_url: string # This URL will be called using the HTTP method defined when the webhook is called. Jinja2 template processing is supported with the same context as the request body.
  --secret: string # When provided, the request will include a 'X-Hook-Signature' header containing a HMAC hex digest of the payload body using the secret as the key. The secret is not transmitted in the request.
  --ssl-verification: oneof<nothing, bool> # Enable SSL certificate verification. Disable with caution!
  --type-create: oneof<nothing, bool> # Call this webhook when a matching object is created.
  --type-delete: oneof<nothing, bool> # Call this webhook when a matching object is deleted.
  --type-update: oneof<nothing, bool> # Call this webhook when a matching object is updated.
]: any -> record<additional_headers: string, body_template: string, ca_file_path: string, conditions: record, content_types: list<string>, created: string, display: string, enabled: bool, http_content_type: string, http_method: string, id: int, last_updated: string, name: string, payload_url: string, secret: string, ssl_verification: bool, type_create: bool, type_delete: bool, type_update: bool, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/extras/webhooks/{id}/"))
  let req_body = {"additional_headers": $additional_headers, "body_template": $body_template, "ca_file_path": $ca_file_path, "conditions": $conditions, "content_types": $content_types, "enabled": $enabled, "http_content_type": $http_content_type, "http_method": $http_method, "name": $name, "payload_url": $payload_url, "secret": $secret, "ssl_verification": $ssl_verification, "type_create": $type_create, "type_delete": $type_delete, "type_update": $type_update} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/aggregates/
#
# operationId: ipam_aggregates_bulk_delete
export def "ipam-aggregates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/aggregates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/aggregates/
#
# operationId: ipam_aggregates_list
export def "ipam-aggregates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --date-added: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --family: float
  --prefix: string
  --rir-id: string
  --rir: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --date-added-n: string
  --date-added-lte: string
  --date-added-lt: string
  --date-added-gte: string
  --date-added-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --rir-id-n: string
  --rir-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, date_added: string, description: string, display: string, family: record, id: int, last_updated: string, prefix: string, rir: record, tags: list, tenant: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "date_added" $date_added "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "family" $family "scalar") (serialize-qp "prefix" $prefix "scalar") (serialize-qp "rir_id" $rir_id "scalar") (serialize-qp "rir" $rir "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "date_added__n" $date_added_n "scalar") (serialize-qp "date_added__lte" $date_added_lte "scalar") (serialize-qp "date_added__lt" $date_added_lt "scalar") (serialize-qp "date_added__gte" $date_added_gte "scalar") (serialize-qp "date_added__gt" $date_added_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "rir_id__n" $rir_id_n "scalar") (serialize-qp "rir__n" $rir_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/aggregates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "date_added": $date_added, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "family": $family, "prefix": $prefix, "rir_id": $rir_id, "rir": $rir, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "date_added__n": $date_added_n, "date_added__lte": $date_added_lte, "date_added__lt": $date_added_lt, "date_added__gte": $date_added_gte, "date_added__gt": $date_added_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "rir_id__n": $rir_id_n, "rir__n": $rir_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/aggregates/
#
# operationId: ipam_aggregates_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-aggregates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --date-added: string # nullable, format: date
  --description: string
  prefix: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, date_added: string, description: string, display: string, family: record<label: string, value: int>, id: int, last_updated: string, prefix: string, rir: record<aggregate_count: int, display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/aggregates/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "date_added": $date_added, "description": $description, "prefix": $prefix, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/aggregates/
#
# operationId: ipam_aggregates_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-aggregates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --date-added: string # nullable, format: date
  --description: string
  prefix: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, date_added: string, description: string, display: string, family: record<label: string, value: int>, id: int, last_updated: string, prefix: string, rir: record<aggregate_count: int, display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/aggregates/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "date_added": $date_added, "description": $description, "prefix": $prefix, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/aggregates/
#
# operationId: ipam_aggregates_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-aggregates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --date-added: string # nullable, format: date
  --description: string
  prefix: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, date_added: string, description: string, display: string, family: record<label: string, value: int>, id: int, last_updated: string, prefix: string, rir: record<aggregate_count: int, display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/aggregates/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "date_added": $date_added, "description": $description, "prefix": $prefix, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/aggregates/{id}/
#
# operationId: ipam_aggregates_delete
export def "ipam-aggregates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/aggregates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/aggregates/{id}/
#
# operationId: ipam_aggregates_read
export def "ipam-aggregates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, date_added: string, description: string, display: string, family: record<label: string, value: int>, id: int, last_updated: string, prefix: string, rir: record<aggregate_count: int, display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/aggregates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/aggregates/{id}/
#
# operationId: ipam_aggregates_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-aggregates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --date-added: string # nullable, format: date
  --description: string
  prefix: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, date_added: string, description: string, display: string, family: record<label: string, value: int>, id: int, last_updated: string, prefix: string, rir: record<aggregate_count: int, display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/aggregates/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "date_added": $date_added, "description": $description, "prefix": $prefix, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/aggregates/{id}/
#
# operationId: ipam_aggregates_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-aggregates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --date-added: string # nullable, format: date
  --description: string
  prefix: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, date_added: string, description: string, display: string, family: record<label: string, value: int>, id: int, last_updated: string, prefix: string, rir: record<aggregate_count: int, display: string, id: int, name: string, slug: string, url: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/aggregates/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "date_added": $date_added, "description": $description, "prefix": $prefix, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/asns/
#
# operationId: ipam_asns_bulk_delete
export def "ipam-asns delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/asns/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/asns/
#
# operationId: ipam_asns_list
export def "ipam-asns list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --asn: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --rir-id: string
  --rir: string
  --site-id: string
  --site: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --asn-n: string
  --asn-lte: string
  --asn-lt: string
  --asn-gte: string
  --asn-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --rir-id-n: string
  --rir-n: string
  --site-id-n: string
  --site-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<asn: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, provider_count: int, rir: int, site_count: int, tags: list, tenant: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "asn" $asn "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "rir_id" $rir_id "scalar") (serialize-qp "rir" $rir "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "asn__n" $asn_n "scalar") (serialize-qp "asn__lte" $asn_lte "scalar") (serialize-qp "asn__lt" $asn_lt "scalar") (serialize-qp "asn__gte" $asn_gte "scalar") (serialize-qp "asn__gt" $asn_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "rir_id__n" $rir_id_n "scalar") (serialize-qp "rir__n" $rir_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/asns/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "asn": $asn, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "rir_id": $rir_id, "rir": $rir, "site_id": $site_id, "site": $site, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "asn__n": $asn_n, "asn__lte": $asn_lte, "asn__lt": $asn_lt, "asn__gte": $asn_gte, "asn__gt": $asn_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "rir_id__n": $rir_id_n, "rir__n": $rir_n, "site_id__n": $site_id_n, "site__n": $site_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/asns/
#
# operationId: ipam_asns_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-asns update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  asn: int # 32-bit autonomous system number
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<asn: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, provider_count: int, rir: int, site_count: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/asns/")
  let req_body = {"asn": $asn, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/asns/
#
# operationId: ipam_asns_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-asns create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  asn: int # 32-bit autonomous system number
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<asn: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, provider_count: int, rir: int, site_count: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/asns/")
  let req_body = {"asn": $asn, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/asns/
#
# operationId: ipam_asns_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-asns update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  asn: int # 32-bit autonomous system number
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<asn: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, provider_count: int, rir: int, site_count: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/asns/")
  let req_body = {"asn": $asn, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/asns/{id}/
#
# operationId: ipam_asns_delete
export def "ipam-asns delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/asns/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/asns/{id}/
#
# operationId: ipam_asns_read
export def "ipam-asns get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<asn: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, provider_count: int, rir: int, site_count: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/asns/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/asns/{id}/
#
# operationId: ipam_asns_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-asns update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  asn: int # 32-bit autonomous system number
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<asn: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, provider_count: int, rir: int, site_count: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/asns/{id}/"))
  let req_body = {"asn": $asn, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/asns/{id}/
#
# operationId: ipam_asns_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-asns update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  asn: int # 32-bit autonomous system number
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  rir: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<asn: int, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, provider_count: int, rir: int, site_count: int, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/asns/{id}/"))
  let req_body = {"asn": $asn, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "rir": $rir, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/fhrp-group-assignments/
#
# operationId: ipam_fhrp-group-assignments_bulk_delete
export def "ipam-fhrp-group-assignments delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-group-assignments/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/fhrp-group-assignments/
#
# operationId: ipam_fhrp-group-assignments_list
export def "ipam-fhrp-group-assignments list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --group-id: string
  --interface-type: string
  --interface-id: string
  --priority: string
  --created: string
  --last-updated: string
  --device: string
  --device-id: string
  --virtual-machine: string
  --virtual-machine-id: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --group-id-n: string
  --interface-type-n: string
  --interface-id-n: string
  --interface-id-lte: string
  --interface-id-lt: string
  --interface-id-gte: string
  --interface-id-gt: string
  --priority-n: string
  --priority-lte: string
  --priority-lt: string
  --priority-gte: string
  --priority-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, display: string, group: record, id: int, interface: record, interface_id: int, interface_type: string, last_updated: string, priority: int, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "interface_type" $interface_type "scalar") (serialize-qp "interface_id" $interface_id "scalar") (serialize-qp "priority" $priority "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "virtual_machine" $virtual_machine "scalar") (serialize-qp "virtual_machine_id" $virtual_machine_id "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "interface_type__n" $interface_type_n "scalar") (serialize-qp "interface_id__n" $interface_id_n "scalar") (serialize-qp "interface_id__lte" $interface_id_lte "scalar") (serialize-qp "interface_id__lt" $interface_id_lt "scalar") (serialize-qp "interface_id__gte" $interface_id_gte "scalar") (serialize-qp "interface_id__gt" $interface_id_gt "scalar") (serialize-qp "priority__n" $priority_n "scalar") (serialize-qp "priority__lte" $priority_lte "scalar") (serialize-qp "priority__lt" $priority_lt "scalar") (serialize-qp "priority__gte" $priority_gte "scalar") (serialize-qp "priority__gt" $priority_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/fhrp-group-assignments/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "group_id": $group_id, "interface_type": $interface_type, "interface_id": $interface_id, "priority": $priority, "created": $created, "last_updated": $last_updated, "device": $device, "device_id": $device_id, "virtual_machine": $virtual_machine, "virtual_machine_id": $virtual_machine_id, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "group_id__n": $group_id_n, "interface_type__n": $interface_type_n, "interface_id__n": $interface_id_n, "interface_id__lte": $interface_id_lte, "interface_id__lt": $interface_id_lt, "interface_id__gte": $interface_id_gte, "interface_id__gt": $interface_id_gt, "priority__n": $priority_n, "priority__lte": $priority_lte, "priority__lt": $priority_lt, "priority__gte": $priority_gte, "priority__gt": $priority_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/fhrp-group-assignments/
#
# operationId: ipam_fhrp-group-assignments_bulk_partial_update
export def "ipam-fhrp-group-assignments update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  group: int
  interface_id: int
  interface_type: string
  priority: int
]: any -> record<created: string, display: string, group: record<display: string, group_id: int, id: int, protocol: string, url: string>, id: int, interface: record, interface_id: int, interface_type: string, last_updated: string, priority: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-group-assignments/")
  let req_body = {"group": $group, "interface_id": $interface_id, "interface_type": $interface_type, "priority": $priority} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/fhrp-group-assignments/
#
# operationId: ipam_fhrp-group-assignments_create
export def "ipam-fhrp-group-assignments create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  group: int
  interface_id: int
  interface_type: string
  priority: int
]: any -> record<created: string, display: string, group: record<display: string, group_id: int, id: int, protocol: string, url: string>, id: int, interface: record, interface_id: int, interface_type: string, last_updated: string, priority: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-group-assignments/")
  let req_body = {"group": $group, "interface_id": $interface_id, "interface_type": $interface_type, "priority": $priority} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/fhrp-group-assignments/
#
# operationId: ipam_fhrp-group-assignments_bulk_update
export def "ipam-fhrp-group-assignments update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  group: int
  interface_id: int
  interface_type: string
  priority: int
]: any -> record<created: string, display: string, group: record<display: string, group_id: int, id: int, protocol: string, url: string>, id: int, interface: record, interface_id: int, interface_type: string, last_updated: string, priority: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-group-assignments/")
  let req_body = {"group": $group, "interface_id": $interface_id, "interface_type": $interface_type, "priority": $priority} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/fhrp-group-assignments/{id}/
#
# operationId: ipam_fhrp-group-assignments_delete
export def "ipam-fhrp-group-assignments delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-group-assignments/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/fhrp-group-assignments/{id}/
#
# operationId: ipam_fhrp-group-assignments_read
export def "ipam-fhrp-group-assignments get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, display: string, group: record<display: string, group_id: int, id: int, protocol: string, url: string>, id: int, interface: record, interface_id: int, interface_type: string, last_updated: string, priority: int, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-group-assignments/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/fhrp-group-assignments/{id}/
#
# operationId: ipam_fhrp-group-assignments_partial_update
export def "ipam-fhrp-group-assignments update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  group: int
  interface_id: int
  interface_type: string
  priority: int
]: any -> record<created: string, display: string, group: record<display: string, group_id: int, id: int, protocol: string, url: string>, id: int, interface: record, interface_id: int, interface_type: string, last_updated: string, priority: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-group-assignments/{id}/"))
  let req_body = {"group": $group, "interface_id": $interface_id, "interface_type": $interface_type, "priority": $priority} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/fhrp-group-assignments/{id}/
#
# operationId: ipam_fhrp-group-assignments_update
export def "ipam-fhrp-group-assignments update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  group: int
  interface_id: int
  interface_type: string
  priority: int
]: any -> record<created: string, display: string, group: record<display: string, group_id: int, id: int, protocol: string, url: string>, id: int, interface: record, interface_id: int, interface_type: string, last_updated: string, priority: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-group-assignments/{id}/"))
  let req_body = {"group": $group, "interface_id": $interface_id, "interface_type": $interface_type, "priority": $priority} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/fhrp-groups/
#
# operationId: ipam_fhrp-groups_bulk_delete
export def "ipam-fhrp-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/fhrp-groups/
#
# operationId: ipam_fhrp-groups_list
export def "ipam-fhrp-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --group-id: string
  --name: string
  --auth-key: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --protocol: string
  --auth-type: string
  --related-ip: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --group-id-n: string
  --group-id-lte: string
  --group-id-lt: string
  --group-id-gte: string
  --group-id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --auth-key-n: string
  --auth-key-ic: string
  --auth-key-nic: string
  --auth-key-iew: string
  --auth-key-niew: string
  --auth-key-isw: string
  --auth-key-nisw: string
  --auth-key-ie: string
  --auth-key-nie: string
  --auth-key-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --protocol-n: string
  --auth-type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<auth_key: string, auth_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, group_id: int, id: int, ip_addresses: list, last_updated: string, name: string, protocol: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "auth_key" $auth_key "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "protocol" $protocol "scalar") (serialize-qp "auth_type" $auth_type "scalar") (serialize-qp "related_ip" $related_ip "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group_id__lte" $group_id_lte "scalar") (serialize-qp "group_id__lt" $group_id_lt "scalar") (serialize-qp "group_id__gte" $group_id_gte "scalar") (serialize-qp "group_id__gt" $group_id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "auth_key__n" $auth_key_n "scalar") (serialize-qp "auth_key__ic" $auth_key_ic "scalar") (serialize-qp "auth_key__nic" $auth_key_nic "scalar") (serialize-qp "auth_key__iew" $auth_key_iew "scalar") (serialize-qp "auth_key__niew" $auth_key_niew "scalar") (serialize-qp "auth_key__isw" $auth_key_isw "scalar") (serialize-qp "auth_key__nisw" $auth_key_nisw "scalar") (serialize-qp "auth_key__ie" $auth_key_ie "scalar") (serialize-qp "auth_key__nie" $auth_key_nie "scalar") (serialize-qp "auth_key__empty" $auth_key_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "protocol__n" $protocol_n "scalar") (serialize-qp "auth_type__n" $auth_type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/fhrp-groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "group_id": $group_id, "name": $name, "auth_key": $auth_key, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "protocol": $protocol, "auth_type": $auth_type, "related_ip": $related_ip, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "group_id__n": $group_id_n, "group_id__lte": $group_id_lte, "group_id__lt": $group_id_lt, "group_id__gte": $group_id_gte, "group_id__gt": $group_id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "auth_key__n": $auth_key_n, "auth_key__ic": $auth_key_ic, "auth_key__nic": $auth_key_nic, "auth_key__iew": $auth_key_iew, "auth_key__niew": $auth_key_niew, "auth_key__isw": $auth_key_isw, "auth_key__nisw": $auth_key_nisw, "auth_key__ie": $auth_key_ie, "auth_key__nie": $auth_key_nie, "auth_key__empty": $auth_key_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "protocol__n": $protocol_n, "auth_type__n": $auth_type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/fhrp-groups/
#
# operationId: ipam_fhrp-groups_bulk_partial_update
# --ip_addresses item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-fhrp-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-key: string
  --auth-type: string@auth-type-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  group_id: int
  --name: string
  protocol: string@protocol-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<auth_key: string, auth_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, group_id: int, id: int, ip_addresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, protocol: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-groups/")
  let req_body = {"auth_key": $auth_key, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group_id": $group_id, "name": $name, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/fhrp-groups/
#
# operationId: ipam_fhrp-groups_create
# --ip_addresses item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-fhrp-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-key: string
  --auth-type: string@auth-type-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  group_id: int
  --name: string
  protocol: string@protocol-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<auth_key: string, auth_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, group_id: int, id: int, ip_addresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, protocol: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-groups/")
  let req_body = {"auth_key": $auth_key, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group_id": $group_id, "name": $name, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/fhrp-groups/
#
# operationId: ipam_fhrp-groups_bulk_update
# --ip_addresses item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-fhrp-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-key: string
  --auth-type: string@auth-type-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  group_id: int
  --name: string
  protocol: string@protocol-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<auth_key: string, auth_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, group_id: int, id: int, ip_addresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, protocol: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/fhrp-groups/")
  let req_body = {"auth_key": $auth_key, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group_id": $group_id, "name": $name, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/fhrp-groups/{id}/
#
# operationId: ipam_fhrp-groups_delete
export def "ipam-fhrp-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/fhrp-groups/{id}/
#
# operationId: ipam_fhrp-groups_read
export def "ipam-fhrp-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<auth_key: string, auth_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, group_id: int, id: int, ip_addresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, protocol: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/fhrp-groups/{id}/
#
# operationId: ipam_fhrp-groups_partial_update
# --ip_addresses item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-fhrp-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-key: string
  --auth-type: string@auth-type-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  group_id: int
  --name: string
  protocol: string@protocol-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<auth_key: string, auth_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, group_id: int, id: int, ip_addresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, protocol: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-groups/{id}/"))
  let req_body = {"auth_key": $auth_key, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group_id": $group_id, "name": $name, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/fhrp-groups/{id}/
#
# operationId: ipam_fhrp-groups_update
# --ip_addresses item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-fhrp-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-key: string
  --auth-type: string@auth-type-completer
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  group_id: int
  --name: string
  protocol: string@protocol-completer
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<auth_key: string, auth_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, group_id: int, id: int, ip_addresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, protocol: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/fhrp-groups/{id}/"))
  let req_body = {"auth_key": $auth_key, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group_id": $group_id, "name": $name, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/ip-addresses/
#
# operationId: ipam_ip-addresses_bulk_delete
export def "ipam-ip-addresses delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-addresses/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/ip-addresses/
#
# operationId: ipam_ip-addresses_list
export def "ipam-ip-addresses list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --dns-name: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --family: float
  --parent: string
  --address: string
  --mask-length: float
  --vrf-id: string
  --vrf: string
  --present-in-vrf-id: string
  --present-in-vrf: string
  --device: string
  --device-id: string
  --virtual-machine: string
  --virtual-machine-id: string
  --interface: string
  --interface-id: string
  --vminterface: string
  --vminterface-id: string
  --fhrpgroup-id: string
  --assigned-to-interface: string
  --status: string
  --role: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --dns-name-n: string
  --dns-name-ic: string
  --dns-name-nic: string
  --dns-name-iew: string
  --dns-name-niew: string
  --dns-name-isw: string
  --dns-name-nisw: string
  --dns-name-ie: string
  --dns-name-nie: string
  --dns-name-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --vrf-id-n: string
  --vrf-n: string
  --interface-n: string
  --interface-id-n: string
  --vminterface-n: string
  --vminterface-id-n: string
  --fhrpgroup-id-n: string
  --status-n: string
  --role-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record, id: int, last_updated: string, nat_inside: record, nat_outside: list, role: record, status: record, tags: list, tenant: record, url: string, vrf: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "dns_name" $dns_name "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "family" $family "scalar") (serialize-qp "parent" $parent "scalar") (serialize-qp "address" $address "scalar") (serialize-qp "mask_length" $mask_length "scalar") (serialize-qp "vrf_id" $vrf_id "scalar") (serialize-qp "vrf" $vrf "scalar") (serialize-qp "present_in_vrf_id" $present_in_vrf_id "scalar") (serialize-qp "present_in_vrf" $present_in_vrf "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "virtual_machine" $virtual_machine "scalar") (serialize-qp "virtual_machine_id" $virtual_machine_id "scalar") (serialize-qp "interface" $interface "scalar") (serialize-qp "interface_id" $interface_id "scalar") (serialize-qp "vminterface" $vminterface "scalar") (serialize-qp "vminterface_id" $vminterface_id "scalar") (serialize-qp "fhrpgroup_id" $fhrpgroup_id "scalar") (serialize-qp "assigned_to_interface" $assigned_to_interface "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "dns_name__n" $dns_name_n "scalar") (serialize-qp "dns_name__ic" $dns_name_ic "scalar") (serialize-qp "dns_name__nic" $dns_name_nic "scalar") (serialize-qp "dns_name__iew" $dns_name_iew "scalar") (serialize-qp "dns_name__niew" $dns_name_niew "scalar") (serialize-qp "dns_name__isw" $dns_name_isw "scalar") (serialize-qp "dns_name__nisw" $dns_name_nisw "scalar") (serialize-qp "dns_name__ie" $dns_name_ie "scalar") (serialize-qp "dns_name__nie" $dns_name_nie "scalar") (serialize-qp "dns_name__empty" $dns_name_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "vrf_id__n" $vrf_id_n "scalar") (serialize-qp "vrf__n" $vrf_n "scalar") (serialize-qp "interface__n" $interface_n "scalar") (serialize-qp "interface_id__n" $interface_id_n "scalar") (serialize-qp "vminterface__n" $vminterface_n "scalar") (serialize-qp "vminterface_id__n" $vminterface_id_n "scalar") (serialize-qp "fhrpgroup_id__n" $fhrpgroup_id_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/ip-addresses/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "dns_name": $dns_name, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "family": $family, "parent": $parent, "address": $address, "mask_length": $mask_length, "vrf_id": $vrf_id, "vrf": $vrf, "present_in_vrf_id": $present_in_vrf_id, "present_in_vrf": $present_in_vrf, "device": $device, "device_id": $device_id, "virtual_machine": $virtual_machine, "virtual_machine_id": $virtual_machine_id, "interface": $interface, "interface_id": $interface_id, "vminterface": $vminterface, "vminterface_id": $vminterface_id, "fhrpgroup_id": $fhrpgroup_id, "assigned_to_interface": $assigned_to_interface, "status": $status, "role": $role, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "dns_name__n": $dns_name_n, "dns_name__ic": $dns_name_ic, "dns_name__nic": $dns_name_nic, "dns_name__iew": $dns_name_iew, "dns_name__niew": $dns_name_niew, "dns_name__isw": $dns_name_isw, "dns_name__nisw": $dns_name_nisw, "dns_name__ie": $dns_name_ie, "dns_name__nie": $dns_name_nie, "dns_name__empty": $dns_name_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "vrf_id__n": $vrf_id_n, "vrf__n": $vrf_n, "interface__n": $interface_n, "interface_id__n": $interface_id_n, "vminterface__n": $vminterface_n, "vminterface_id__n": $vminterface_id_n, "fhrpgroup_id__n": $fhrpgroup_id_n, "status__n": $status_n, "role__n": $role_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/ip-addresses/
#
# operationId: ipam_ip-addresses_bulk_partial_update
# --nat_outside item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-addresses update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  address: string # IPv4 or IPv6 address (with mask)
  --assigned-object-id: int # nullable
  --assigned-object-type: string # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --dns-name: string # Hostname or FQDN (not case-sensitive)
  --nat-inside: int # The IP for which this address is the "outside" IP (nullable)
  --role: string@role-completer # The functional role of this IP
  --status: string@status-completer-8 # The operational status of this IP
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: table<address: string, display: string, family: int, id: int, url: string>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-addresses/")
  let req_body = {"address": $address, "assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "dns_name": $dns_name, "nat_inside": $nat_inside, "role": $role, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/ip-addresses/
#
# operationId: ipam_ip-addresses_create
# --nat_outside item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-addresses create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  address: string # IPv4 or IPv6 address (with mask)
  --assigned-object-id: int # nullable
  --assigned-object-type: string # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --dns-name: string # Hostname or FQDN (not case-sensitive)
  --nat-inside: int # The IP for which this address is the "outside" IP (nullable)
  --role: string@role-completer # The functional role of this IP
  --status: string@status-completer-8 # The operational status of this IP
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: table<address: string, display: string, family: int, id: int, url: string>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-addresses/")
  let req_body = {"address": $address, "assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "dns_name": $dns_name, "nat_inside": $nat_inside, "role": $role, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/ip-addresses/
#
# operationId: ipam_ip-addresses_bulk_update
# --nat_outside item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-addresses update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  address: string # IPv4 or IPv6 address (with mask)
  --assigned-object-id: int # nullable
  --assigned-object-type: string # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --dns-name: string # Hostname or FQDN (not case-sensitive)
  --nat-inside: int # The IP for which this address is the "outside" IP (nullable)
  --role: string@role-completer # The functional role of this IP
  --status: string@status-completer-8 # The operational status of this IP
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: table<address: string, display: string, family: int, id: int, url: string>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-addresses/")
  let req_body = {"address": $address, "assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "dns_name": $dns_name, "nat_inside": $nat_inside, "role": $role, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/ip-addresses/{id}/
#
# operationId: ipam_ip-addresses_delete
export def "ipam-ip-addresses delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-addresses/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/ip-addresses/{id}/
#
# operationId: ipam_ip-addresses_read
export def "ipam-ip-addresses get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: table<address: string, display: string, family: int, id: int, url: string>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-addresses/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/ip-addresses/{id}/
#
# operationId: ipam_ip-addresses_partial_update
# --nat_outside item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-addresses update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  address: string # IPv4 or IPv6 address (with mask)
  --assigned-object-id: int # nullable
  --assigned-object-type: string # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --dns-name: string # Hostname or FQDN (not case-sensitive)
  --nat-inside: int # The IP for which this address is the "outside" IP (nullable)
  --role: string@role-completer # The functional role of this IP
  --status: string@status-completer-8 # The operational status of this IP
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: table<address: string, display: string, family: int, id: int, url: string>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-addresses/{id}/"))
  let req_body = {"address": $address, "assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "dns_name": $dns_name, "nat_inside": $nat_inside, "role": $role, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/ip-addresses/{id}/
#
# operationId: ipam_ip-addresses_update
# --nat_outside item shape: {address: string}
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-addresses update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  address: string # IPv4 or IPv6 address (with mask)
  --assigned-object-id: int # nullable
  --assigned-object-type: string # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --dns-name: string # Hostname or FQDN (not case-sensitive)
  --nat-inside: int # The IP for which this address is the "outside" IP (nullable)
  --role: string@role-completer # The functional role of this IP
  --status: string@status-completer-8 # The operational status of this IP
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: table<address: string, display: string, family: int, id: int, url: string>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-addresses/{id}/"))
  let req_body = {"address": $address, "assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "dns_name": $dns_name, "nat_inside": $nat_inside, "role": $role, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/ip-ranges/
#
# operationId: ipam_ip-ranges_bulk_delete
export def "ipam-ip-ranges delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-ranges/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/ip-ranges/
#
# operationId: ipam_ip-ranges_list
export def "ipam-ip-ranges list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --description: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --family: float
  --start-address: string
  --end-address: string
  --contains: string
  --vrf-id: string
  --vrf: string
  --role-id: string
  --role: string
  --status: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --vrf-id-n: string
  --vrf-n: string
  --role-id-n: string
  --role-n: string
  --status-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<children: int, comments: string, created: string, custom_fields: record, description: string, display: string, end_address: string, family: record, id: int, last_updated: string, role: record, size: int, start_address: string, status: record, tags: list, tenant: record, url: string, vrf: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "family" $family "scalar") (serialize-qp "start_address" $start_address "scalar") (serialize-qp "end_address" $end_address "scalar") (serialize-qp "contains" $contains "scalar") (serialize-qp "vrf_id" $vrf_id "scalar") (serialize-qp "vrf" $vrf "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "vrf_id__n" $vrf_id_n "scalar") (serialize-qp "vrf__n" $vrf_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/ip-ranges/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "description": $description, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "family": $family, "start_address": $start_address, "end_address": $end_address, "contains": $contains, "vrf_id": $vrf_id, "vrf": $vrf, "role_id": $role_id, "role": $role, "status": $status, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "vrf_id__n": $vrf_id_n, "vrf__n": $vrf_n, "role_id__n": $role_id_n, "role__n": $role_n, "status__n": $status_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/ip-ranges/
#
# operationId: ipam_ip-ranges_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-ranges update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  end_address: string # IPv4 or IPv6 address (with mask)
  --role: int # The primary function of this range (nullable)
  start_address: string # IPv4 or IPv6 address (with mask)
  --status: string@status-completer-9 # Operational status of this range
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<children: int, comments: string, created: string, custom_fields: record, description: string, display: string, end_address: string, family: record<label: string, value: int>, id: int, last_updated: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, size: int, start_address: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-ranges/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "end_address": $end_address, "role": $role, "start_address": $start_address, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/ip-ranges/
#
# operationId: ipam_ip-ranges_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-ranges create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  end_address: string # IPv4 or IPv6 address (with mask)
  --role: int # The primary function of this range (nullable)
  start_address: string # IPv4 or IPv6 address (with mask)
  --status: string@status-completer-9 # Operational status of this range
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<children: int, comments: string, created: string, custom_fields: record, description: string, display: string, end_address: string, family: record<label: string, value: int>, id: int, last_updated: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, size: int, start_address: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-ranges/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "end_address": $end_address, "role": $role, "start_address": $start_address, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/ip-ranges/
#
# operationId: ipam_ip-ranges_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-ranges update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  end_address: string # IPv4 or IPv6 address (with mask)
  --role: int # The primary function of this range (nullable)
  start_address: string # IPv4 or IPv6 address (with mask)
  --status: string@status-completer-9 # Operational status of this range
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<children: int, comments: string, created: string, custom_fields: record, description: string, display: string, end_address: string, family: record<label: string, value: int>, id: int, last_updated: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, size: int, start_address: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/ip-ranges/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "end_address": $end_address, "role": $role, "start_address": $start_address, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/ip-ranges/{id}/
#
# operationId: ipam_ip-ranges_delete
export def "ipam-ip-ranges delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-ranges/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/ip-ranges/{id}/
#
# operationId: ipam_ip-ranges_read
export def "ipam-ip-ranges get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<children: int, comments: string, created: string, custom_fields: record, description: string, display: string, end_address: string, family: record<label: string, value: int>, id: int, last_updated: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, size: int, start_address: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-ranges/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/ip-ranges/{id}/
#
# operationId: ipam_ip-ranges_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-ranges update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  end_address: string # IPv4 or IPv6 address (with mask)
  --role: int # The primary function of this range (nullable)
  start_address: string # IPv4 or IPv6 address (with mask)
  --status: string@status-completer-9 # Operational status of this range
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<children: int, comments: string, created: string, custom_fields: record, description: string, display: string, end_address: string, family: record<label: string, value: int>, id: int, last_updated: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, size: int, start_address: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-ranges/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "end_address": $end_address, "role": $role, "start_address": $start_address, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/ip-ranges/{id}/
#
# operationId: ipam_ip-ranges_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-ip-ranges update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  end_address: string # IPv4 or IPv6 address (with mask)
  --role: int # The primary function of this range (nullable)
  start_address: string # IPv4 or IPv6 address (with mask)
  --status: string@status-completer-9 # Operational status of this range
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vrf: int # nullable
]: any -> record<children: int, comments: string, created: string, custom_fields: record, description: string, display: string, end_address: string, family: record<label: string, value: int>, id: int, last_updated: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, size: int, start_address: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-ranges/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "end_address": $end_address, "role": $role, "start_address": $start_address, "status": $status, "tags": $tags, "tenant": $tenant, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# GET /ipam/ip-ranges/{id}/available-ips/
#
# operationId: ipam_ip-ranges_available-ips_list
export def "ipam-ip-ranges-available-ips list" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<address: string, family: int, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-ranges/{id}/available-ips/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# POST /ipam/ip-ranges/{id}/available-ips/
#
# operationId: ipam_ip-ranges_available-ips_create
export def "ipam-ip-ranges-available-ips create" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> table<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: list<record>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: list<record>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/ip-ranges/{id}/available-ips/"))
  let req_body = $body
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/l2vpn-terminations/
#
# operationId: ipam_l2vpn-terminations_bulk_delete
export def "ipam-l2vpn-terminations delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpn-terminations/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/l2vpn-terminations/
#
# operationId: ipam_l2vpn-terminations_list
export def "ipam-l2vpn-terminations list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --assigned-object-type-id: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --l2vpn-id: string
  --l2vpn: string
  --region: string
  --region-id: string
  --site: string
  --site-id: string
  --device: string
  --device-id: string
  --virtual-machine: string
  --virtual-machine-id: string
  --interface: string
  --interface-id: string
  --vminterface: string
  --vminterface-id: string
  --vlan: string
  --vlan-vid: float
  --vlan-id: string
  --assigned-object-type: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --assigned-object-type-id-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --l2vpn-id-n: string
  --l2vpn-n: string
  --device-n: string
  --device-id-n: string
  --virtual-machine-n: string
  --virtual-machine-id-n: string
  --interface-n: string
  --interface-id-n: string
  --vminterface-n: string
  --vminterface-id-n: string
  --vlan-n: string
  --vlan-vid-n: float
  --vlan-vid-lte: float
  --vlan-vid-lt: float
  --vlan-vid-gte: float
  --vlan-vid-gt: float
  --vlan-id-n: string
  --assigned-object-type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<assigned_object: record, assigned_object_id: int, assigned_object_type: string, created: string, custom_fields: record, display: string, id: int, l2vpn: record, last_updated: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "assigned_object_type_id" $assigned_object_type_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "l2vpn_id" $l2vpn_id "scalar") (serialize-qp "l2vpn" $l2vpn "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "virtual_machine" $virtual_machine "scalar") (serialize-qp "virtual_machine_id" $virtual_machine_id "scalar") (serialize-qp "interface" $interface "scalar") (serialize-qp "interface_id" $interface_id "scalar") (serialize-qp "vminterface" $vminterface "scalar") (serialize-qp "vminterface_id" $vminterface_id "scalar") (serialize-qp "vlan" $vlan "scalar") (serialize-qp "vlan_vid" $vlan_vid "scalar") (serialize-qp "vlan_id" $vlan_id "scalar") (serialize-qp "assigned_object_type" $assigned_object_type "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "assigned_object_type_id__n" $assigned_object_type_id_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "l2vpn_id__n" $l2vpn_id_n "scalar") (serialize-qp "l2vpn__n" $l2vpn_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "virtual_machine__n" $virtual_machine_n "scalar") (serialize-qp "virtual_machine_id__n" $virtual_machine_id_n "scalar") (serialize-qp "interface__n" $interface_n "scalar") (serialize-qp "interface_id__n" $interface_id_n "scalar") (serialize-qp "vminterface__n" $vminterface_n "scalar") (serialize-qp "vminterface_id__n" $vminterface_id_n "scalar") (serialize-qp "vlan__n" $vlan_n "scalar") (serialize-qp "vlan_vid__n" $vlan_vid_n "scalar") (serialize-qp "vlan_vid__lte" $vlan_vid_lte "scalar") (serialize-qp "vlan_vid__lt" $vlan_vid_lt "scalar") (serialize-qp "vlan_vid__gte" $vlan_vid_gte "scalar") (serialize-qp "vlan_vid__gt" $vlan_vid_gt "scalar") (serialize-qp "vlan_id__n" $vlan_id_n "scalar") (serialize-qp "assigned_object_type__n" $assigned_object_type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/l2vpn-terminations/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "assigned_object_type_id": $assigned_object_type_id, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "l2vpn_id": $l2vpn_id, "l2vpn": $l2vpn, "region": $region, "region_id": $region_id, "site": $site, "site_id": $site_id, "device": $device, "device_id": $device_id, "virtual_machine": $virtual_machine, "virtual_machine_id": $virtual_machine_id, "interface": $interface, "interface_id": $interface_id, "vminterface": $vminterface, "vminterface_id": $vminterface_id, "vlan": $vlan, "vlan_vid": $vlan_vid, "vlan_id": $vlan_id, "assigned_object_type": $assigned_object_type, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "assigned_object_type_id__n": $assigned_object_type_id_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "l2vpn_id__n": $l2vpn_id_n, "l2vpn__n": $l2vpn_n, "device__n": $device_n, "device_id__n": $device_id_n, "virtual_machine__n": $virtual_machine_n, "virtual_machine_id__n": $virtual_machine_id_n, "interface__n": $interface_n, "interface_id__n": $interface_id_n, "vminterface__n": $vminterface_n, "vminterface_id__n": $vminterface_id_n, "vlan__n": $vlan_n, "vlan_vid__n": $vlan_vid_n, "vlan_vid__lte": $vlan_vid_lte, "vlan_vid__lt": $vlan_vid_lt, "vlan_vid__gte": $vlan_vid_gte, "vlan_vid__gt": $vlan_vid_gt, "vlan_id__n": $vlan_id_n, "assigned_object_type__n": $assigned_object_type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/l2vpn-terminations/
#
# operationId: ipam_l2vpn-terminations_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpn-terminations update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  --custom-fields: record # default: {}
  l2vpn: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, created: string, custom_fields: record, display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpn-terminations/")
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "custom_fields": $custom_fields, "l2vpn": $l2vpn, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/l2vpn-terminations/
#
# operationId: ipam_l2vpn-terminations_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpn-terminations create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  --custom-fields: record # default: {}
  l2vpn: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, created: string, custom_fields: record, display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpn-terminations/")
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "custom_fields": $custom_fields, "l2vpn": $l2vpn, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/l2vpn-terminations/
#
# operationId: ipam_l2vpn-terminations_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpn-terminations update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  --custom-fields: record # default: {}
  l2vpn: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, created: string, custom_fields: record, display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpn-terminations/")
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "custom_fields": $custom_fields, "l2vpn": $l2vpn, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/l2vpn-terminations/{id}/
#
# operationId: ipam_l2vpn-terminations_delete
export def "ipam-l2vpn-terminations delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpn-terminations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/l2vpn-terminations/{id}/
#
# operationId: ipam_l2vpn-terminations_read
export def "ipam-l2vpn-terminations get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, created: string, custom_fields: record, display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpn-terminations/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/l2vpn-terminations/{id}/
#
# operationId: ipam_l2vpn-terminations_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpn-terminations update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  --custom-fields: record # default: {}
  l2vpn: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, created: string, custom_fields: record, display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpn-terminations/{id}/"))
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "custom_fields": $custom_fields, "l2vpn": $l2vpn, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/l2vpn-terminations/{id}/
#
# operationId: ipam_l2vpn-terminations_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpn-terminations update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  assigned_object_id: int
  assigned_object_type: string
  --custom-fields: record # default: {}
  l2vpn: int
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<assigned_object: record, assigned_object_id: int, assigned_object_type: string, created: string, custom_fields: record, display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, last_updated: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpn-terminations/{id}/"))
  let req_body = {"assigned_object_id": $assigned_object_id, "assigned_object_type": $assigned_object_type, "custom_fields": $custom_fields, "l2vpn": $l2vpn, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/l2vpns/
#
# operationId: ipam_l2vpns_bulk_delete
export def "ipam-l2vpns delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpns/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/l2vpns/
#
# operationId: ipam_l2vpns_list
export def "ipam-l2vpns list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --identifier: string
  --name: string
  --slug: string
  --type: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --import-target-id: string
  --import-target: string
  --export-target-id: string
  --export-target: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --identifier-n: string
  --identifier-lte: string
  --identifier-lt: string
  --identifier-gte: string
  --identifier-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --type-n: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --import-target-id-n: string
  --import-target-n: string
  --export-target-id-n: string
  --export-target-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, export_targets: list, id: int, identifier: int, import_targets: list, last_updated: string, name: string, slug: string, tags: list, tenant: record, type: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "identifier" $identifier "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "import_target_id" $import_target_id "scalar") (serialize-qp "import_target" $import_target "scalar") (serialize-qp "export_target_id" $export_target_id "scalar") (serialize-qp "export_target" $export_target "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "identifier__n" $identifier_n "scalar") (serialize-qp "identifier__lte" $identifier_lte "scalar") (serialize-qp "identifier__lt" $identifier_lt "scalar") (serialize-qp "identifier__gte" $identifier_gte "scalar") (serialize-qp "identifier__gt" $identifier_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "import_target_id__n" $import_target_id_n "scalar") (serialize-qp "import_target__n" $import_target_n "scalar") (serialize-qp "export_target_id__n" $export_target_id_n "scalar") (serialize-qp "export_target__n" $export_target_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/l2vpns/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "identifier": $identifier, "name": $name, "slug": $slug, "type": $type, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "import_target_id": $import_target_id, "import_target": $import_target, "export_target_id": $export_target_id, "export_target": $export_target, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "identifier__n": $identifier_n, "identifier__lte": $identifier_lte, "identifier__lt": $identifier_lt, "identifier__gte": $identifier_gte, "identifier__gt": $identifier_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "type__n": $type_n, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "import_target_id__n": $import_target_id_n, "import_target__n": $import_target_n, "export_target_id__n": $export_target_id_n, "export_target__n": $export_target_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/l2vpns/
#
# operationId: ipam_l2vpns_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpns update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --export-targets: list<int>
  --identifier: int # nullable
  --import-targets: list<int>
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: string@type-completer-9
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, export_targets: table<display: string, id: int, name: string, url: string>, id: int, identifier: int, import_targets: table<display: string, id: int, name: string, url: string>, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpns/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "export_targets": $export_targets, "identifier": $identifier, "import_targets": $import_targets, "name": $name, "slug": $slug, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/l2vpns/
#
# operationId: ipam_l2vpns_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpns create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --export-targets: list<int>
  --identifier: int # nullable
  --import-targets: list<int>
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: string@type-completer-9
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, export_targets: table<display: string, id: int, name: string, url: string>, id: int, identifier: int, import_targets: table<display: string, id: int, name: string, url: string>, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpns/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "export_targets": $export_targets, "identifier": $identifier, "import_targets": $import_targets, "name": $name, "slug": $slug, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/l2vpns/
#
# operationId: ipam_l2vpns_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpns update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --export-targets: list<int>
  --identifier: int # nullable
  --import-targets: list<int>
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: string@type-completer-9
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, export_targets: table<display: string, id: int, name: string, url: string>, id: int, identifier: int, import_targets: table<display: string, id: int, name: string, url: string>, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/l2vpns/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "export_targets": $export_targets, "identifier": $identifier, "import_targets": $import_targets, "name": $name, "slug": $slug, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/l2vpns/{id}/
#
# operationId: ipam_l2vpns_delete
export def "ipam-l2vpns delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpns/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/l2vpns/{id}/
#
# operationId: ipam_l2vpns_read
export def "ipam-l2vpns get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, export_targets: table<display: string, id: int, name: string, url: string>, id: int, identifier: int, import_targets: table<display: string, id: int, name: string, url: string>, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpns/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/l2vpns/{id}/
#
# operationId: ipam_l2vpns_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpns update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --export-targets: list<int>
  --identifier: int # nullable
  --import-targets: list<int>
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: string@type-completer-9
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, export_targets: table<display: string, id: int, name: string, url: string>, id: int, identifier: int, import_targets: table<display: string, id: int, name: string, url: string>, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpns/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "export_targets": $export_targets, "identifier": $identifier, "import_targets": $import_targets, "name": $name, "slug": $slug, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/l2vpns/{id}/
#
# operationId: ipam_l2vpns_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-l2vpns update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --export-targets: list<int>
  --identifier: int # nullable
  --import-targets: list<int>
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: string@type-completer-9
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, export_targets: table<display: string, id: int, name: string, url: string>, id: int, identifier: int, import_targets: table<display: string, id: int, name: string, url: string>, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<label: string, value: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/l2vpns/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "export_targets": $export_targets, "identifier": $identifier, "import_targets": $import_targets, "name": $name, "slug": $slug, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/prefixes/
#
# operationId: ipam_prefixes_bulk_delete
export def "ipam-prefixes delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/prefixes/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/prefixes/
#
# operationId: ipam_prefixes_list
export def "ipam-prefixes list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --is-pool: string
  --mark-utilized: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --family: float
  --prefix: string
  --within: string
  --within-include: string
  --contains: string
  --depth: string
  --children: string
  --mask-length: string
  --mask-length-gte: float
  --mask-length-lte: float
  --vrf-id: string
  --vrf: string
  --present-in-vrf-id: string
  --present-in-vrf: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --vlan-id: string
  --vlan-vid: float
  --role-id: string
  --role: string
  --status: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --depth-n: string
  --depth-lte: string
  --depth-lt: string
  --depth-gte: string
  --depth-gt: string
  --children-n: string
  --children-lte: string
  --children-lt: string
  --children-gte: string
  --children-gt: string
  --vrf-id-n: string
  --vrf-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --vlan-id-n: string
  --vlan-vid-n: float
  --vlan-vid-lte: float
  --vlan-vid-lt: float
  --vlan-vid-gte: float
  --vlan-vid-gt: float
  --role-id-n: string
  --role-n: string
  --status-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record, site: record, status: record, tags: list, tenant: record, url: string, vlan: record, vrf: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "is_pool" $is_pool "scalar") (serialize-qp "mark_utilized" $mark_utilized "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "family" $family "scalar") (serialize-qp "prefix" $prefix "scalar") (serialize-qp "within" $within "scalar") (serialize-qp "within_include" $within_include "scalar") (serialize-qp "contains" $contains "scalar") (serialize-qp "depth" $depth "scalar") (serialize-qp "children" $children "scalar") (serialize-qp "mask_length" $mask_length "scalar") (serialize-qp "mask_length__gte" $mask_length_gte "scalar") (serialize-qp "mask_length__lte" $mask_length_lte "scalar") (serialize-qp "vrf_id" $vrf_id "scalar") (serialize-qp "vrf" $vrf "scalar") (serialize-qp "present_in_vrf_id" $present_in_vrf_id "scalar") (serialize-qp "present_in_vrf" $present_in_vrf "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "vlan_id" $vlan_id "scalar") (serialize-qp "vlan_vid" $vlan_vid "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "depth__n" $depth_n "scalar") (serialize-qp "depth__lte" $depth_lte "scalar") (serialize-qp "depth__lt" $depth_lt "scalar") (serialize-qp "depth__gte" $depth_gte "scalar") (serialize-qp "depth__gt" $depth_gt "scalar") (serialize-qp "children__n" $children_n "scalar") (serialize-qp "children__lte" $children_lte "scalar") (serialize-qp "children__lt" $children_lt "scalar") (serialize-qp "children__gte" $children_gte "scalar") (serialize-qp "children__gt" $children_gt "scalar") (serialize-qp "vrf_id__n" $vrf_id_n "scalar") (serialize-qp "vrf__n" $vrf_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "vlan_id__n" $vlan_id_n "scalar") (serialize-qp "vlan_vid__n" $vlan_vid_n "scalar") (serialize-qp "vlan_vid__lte" $vlan_vid_lte "scalar") (serialize-qp "vlan_vid__lt" $vlan_vid_lt "scalar") (serialize-qp "vlan_vid__gte" $vlan_vid_gte "scalar") (serialize-qp "vlan_vid__gt" $vlan_vid_gt "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/prefixes/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "is_pool": $is_pool, "mark_utilized": $mark_utilized, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "family": $family, "prefix": $prefix, "within": $within, "within_include": $within_include, "contains": $contains, "depth": $depth, "children": $children, "mask_length": $mask_length, "mask_length__gte": $mask_length_gte, "mask_length__lte": $mask_length_lte, "vrf_id": $vrf_id, "vrf": $vrf, "present_in_vrf_id": $present_in_vrf_id, "present_in_vrf": $present_in_vrf, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "vlan_id": $vlan_id, "vlan_vid": $vlan_vid, "role_id": $role_id, "role": $role, "status": $status, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "depth__n": $depth_n, "depth__lte": $depth_lte, "depth__lt": $depth_lt, "depth__gte": $depth_gte, "depth__gt": $depth_gt, "children__n": $children_n, "children__lte": $children_lte, "children__lt": $children_lt, "children__gte": $children_gte, "children__gt": $children_gt, "vrf_id__n": $vrf_id_n, "vrf__n": $vrf_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "vlan_id__n": $vlan_id_n, "vlan_vid__n": $vlan_vid_n, "vlan_vid__lte": $vlan_vid_lte, "vlan_vid__lt": $vlan_vid_lt, "vlan_vid__gte": $vlan_vid_gte, "vlan_vid__gt": $vlan_vid_gt, "role_id__n": $role_id_n, "role__n": $role_n, "status__n": $status_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/prefixes/
#
# operationId: ipam_prefixes_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-prefixes update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-pool: oneof<nothing, bool> # All IP addresses within this prefix are considered usable
  --mark-utilized: oneof<nothing, bool> # Treat as 100% utilized
  prefix: string # IPv4 or IPv6 network with mask
  --role: int # The primary function of this prefix (nullable)
  --site: int # nullable
  --status: string@status-completer-10 # Operational status of this prefix
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
  --vrf: int # nullable
]: any -> record<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record<label: string, value: int>, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/prefixes/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_pool": $is_pool, "mark_utilized": $mark_utilized, "prefix": $prefix, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/prefixes/
#
# operationId: ipam_prefixes_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-prefixes create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-pool: oneof<nothing, bool> # All IP addresses within this prefix are considered usable
  --mark-utilized: oneof<nothing, bool> # Treat as 100% utilized
  prefix: string # IPv4 or IPv6 network with mask
  --role: int # The primary function of this prefix (nullable)
  --site: int # nullable
  --status: string@status-completer-10 # Operational status of this prefix
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
  --vrf: int # nullable
]: any -> record<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record<label: string, value: int>, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/prefixes/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_pool": $is_pool, "mark_utilized": $mark_utilized, "prefix": $prefix, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/prefixes/
#
# operationId: ipam_prefixes_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-prefixes update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-pool: oneof<nothing, bool> # All IP addresses within this prefix are considered usable
  --mark-utilized: oneof<nothing, bool> # Treat as 100% utilized
  prefix: string # IPv4 or IPv6 network with mask
  --role: int # The primary function of this prefix (nullable)
  --site: int # nullable
  --status: string@status-completer-10 # Operational status of this prefix
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
  --vrf: int # nullable
]: any -> record<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record<label: string, value: int>, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/prefixes/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_pool": $is_pool, "mark_utilized": $mark_utilized, "prefix": $prefix, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/prefixes/{id}/
#
# operationId: ipam_prefixes_delete
export def "ipam-prefixes delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/prefixes/{id}/
#
# operationId: ipam_prefixes_read
export def "ipam-prefixes get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record<label: string, value: int>, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/prefixes/{id}/
#
# operationId: ipam_prefixes_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-prefixes update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-pool: oneof<nothing, bool> # All IP addresses within this prefix are considered usable
  --mark-utilized: oneof<nothing, bool> # Treat as 100% utilized
  prefix: string # IPv4 or IPv6 network with mask
  --role: int # The primary function of this prefix (nullable)
  --site: int # nullable
  --status: string@status-completer-10 # Operational status of this prefix
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
  --vrf: int # nullable
]: any -> record<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record<label: string, value: int>, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_pool": $is_pool, "mark_utilized": $mark_utilized, "prefix": $prefix, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/prefixes/{id}/
#
# operationId: ipam_prefixes_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-prefixes update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --is-pool: oneof<nothing, bool> # All IP addresses within this prefix are considered usable
  --mark-utilized: oneof<nothing, bool> # Treat as 100% utilized
  prefix: string # IPv4 or IPv6 network with mask
  --role: int # The primary function of this prefix (nullable)
  --site: int # nullable
  --status: string@status-completer-10 # Operational status of this prefix
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
  --vrf: int # nullable
]: any -> record<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record<label: string, value: int>, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "is_pool": $is_pool, "mark_utilized": $mark_utilized, "prefix": $prefix, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# GET /ipam/prefixes/{id}/available-ips/
#
# operationId: ipam_prefixes_available-ips_list
export def "ipam-prefixes-available-ips list" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<address: string, family: int, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/available-ips/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# POST /ipam/prefixes/{id}/available-ips/
#
# operationId: ipam_prefixes_available-ips_create
export def "ipam-prefixes-available-ips create" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --body: record
]: any -> table<address: string, assigned_object: record, assigned_object_id: int, assigned_object_type: string, comments: string, created: string, custom_fields: record, description: string, display: string, dns_name: string, family: record<label: string, value: int>, id: int, last_updated: string, nat_inside: record<address: string, display: string, family: int, id: int, url: string>, nat_outside: list<record>, role: record<label: string, value: string>, status: record<label: string, value: string>, tags: list<record>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/available-ips/"))
  let req_body = $body
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# GET /ipam/prefixes/{id}/available-prefixes/
#
# operationId: ipam_prefixes_available-prefixes_list
export def "ipam-prefixes-available-prefixes list" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<family: int, prefix: string, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/available-prefixes/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# POST /ipam/prefixes/{id}/available-prefixes/
#
# operationId: ipam_prefixes_available-prefixes_create
export def "ipam-prefixes-available-prefixes create" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  prefix_length: int
]: any -> table<_depth: int, children: int, comments: string, created: string, custom_fields: record, description: string, display: string, family: record<label: string, value: int>, id: int, is_pool: bool, last_updated: string, mark_utilized: bool, prefix: string, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: list<record>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/prefixes/{id}/available-prefixes/"))
  let req_body = {"prefix_length": $prefix_length} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/rirs/
#
# operationId: ipam_rirs_bulk_delete
export def "ipam-rirs delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/rirs/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/rirs/
#
# operationId: ipam_rirs_list
export def "ipam-rirs list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --is-private: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<aggregate_count: int, created: string, custom_fields: record, description: string, display: string, id: int, is_private: bool, last_updated: string, name: string, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "is_private" $is_private "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/rirs/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "is_private": $is_private, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/rirs/
#
# operationId: ipam_rirs_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-rirs update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --is-private: oneof<nothing, bool> # IP space managed by this RIR is considered private
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<aggregate_count: int, created: string, custom_fields: record, description: string, display: string, id: int, is_private: bool, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/rirs/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "is_private": $is_private, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/rirs/
#
# operationId: ipam_rirs_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-rirs create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --is-private: oneof<nothing, bool> # IP space managed by this RIR is considered private
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<aggregate_count: int, created: string, custom_fields: record, description: string, display: string, id: int, is_private: bool, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/rirs/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "is_private": $is_private, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/rirs/
#
# operationId: ipam_rirs_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-rirs update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --is-private: oneof<nothing, bool> # IP space managed by this RIR is considered private
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<aggregate_count: int, created: string, custom_fields: record, description: string, display: string, id: int, is_private: bool, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/rirs/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "is_private": $is_private, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/rirs/{id}/
#
# operationId: ipam_rirs_delete
export def "ipam-rirs delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/rirs/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/rirs/{id}/
#
# operationId: ipam_rirs_read
export def "ipam-rirs get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<aggregate_count: int, created: string, custom_fields: record, description: string, display: string, id: int, is_private: bool, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/rirs/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/rirs/{id}/
#
# operationId: ipam_rirs_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-rirs update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --is-private: oneof<nothing, bool> # IP space managed by this RIR is considered private
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<aggregate_count: int, created: string, custom_fields: record, description: string, display: string, id: int, is_private: bool, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/rirs/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "is_private": $is_private, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/rirs/{id}/
#
# operationId: ipam_rirs_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-rirs update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --is-private: oneof<nothing, bool> # IP space managed by this RIR is considered private
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<aggregate_count: int, created: string, custom_fields: record, description: string, display: string, id: int, is_private: bool, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/rirs/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "is_private": $is_private, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/roles/
#
# operationId: ipam_roles_bulk_delete
export def "ipam-roles delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/roles/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/roles/
#
# operationId: ipam_roles_list
export def "ipam-roles list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, prefix_count: int, slug: string, tags: list, url: string, vlan_count: int, weight: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/roles/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/roles/
#
# operationId: ipam_roles_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-roles update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: int
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, prefix_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/roles/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/roles/
#
# operationId: ipam_roles_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-roles create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: int
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, prefix_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/roles/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/roles/
#
# operationId: ipam_roles_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-roles update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: int
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, prefix_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/roles/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/roles/{id}/
#
# operationId: ipam_roles_delete
export def "ipam-roles delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/roles/{id}/
#
# operationId: ipam_roles_read
export def "ipam-roles get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, prefix_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int, weight: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/roles/{id}/
#
# operationId: ipam_roles_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-roles update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: int
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, prefix_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/roles/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/roles/{id}/
#
# operationId: ipam_roles_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-roles update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --weight: int
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, prefix_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int, weight: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/roles/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags, "weight": $weight} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/route-targets/
#
# operationId: ipam_route-targets_bulk_delete
export def "ipam-route-targets delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/route-targets/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/route-targets/
#
# operationId: ipam_route-targets_list
export def "ipam-route-targets list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --importing-vrf-id: string
  --importing-vrf: string
  --exporting-vrf-id: string
  --exporting-vrf: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --importing-vrf-id-n: string
  --importing-vrf-n: string
  --exporting-vrf-id-n: string
  --exporting-vrf-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, tags: list, tenant: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "importing_vrf_id" $importing_vrf_id "scalar") (serialize-qp "importing_vrf" $importing_vrf "scalar") (serialize-qp "exporting_vrf_id" $exporting_vrf_id "scalar") (serialize-qp "exporting_vrf" $exporting_vrf "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "importing_vrf_id__n" $importing_vrf_id_n "scalar") (serialize-qp "importing_vrf__n" $importing_vrf_n "scalar") (serialize-qp "exporting_vrf_id__n" $exporting_vrf_id_n "scalar") (serialize-qp "exporting_vrf__n" $exporting_vrf_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/route-targets/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "importing_vrf_id": $importing_vrf_id, "importing_vrf": $importing_vrf, "exporting_vrf_id": $exporting_vrf_id, "exporting_vrf": $exporting_vrf, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "importing_vrf_id__n": $importing_vrf_id_n, "importing_vrf__n": $importing_vrf_n, "exporting_vrf_id__n": $exporting_vrf_id_n, "exporting_vrf__n": $exporting_vrf_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/route-targets/
#
# operationId: ipam_route-targets_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-route-targets update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string # Route target value (formatted in accordance with RFC 4360)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/route-targets/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/route-targets/
#
# operationId: ipam_route-targets_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-route-targets create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string # Route target value (formatted in accordance with RFC 4360)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/route-targets/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/route-targets/
#
# operationId: ipam_route-targets_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-route-targets update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string # Route target value (formatted in accordance with RFC 4360)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/route-targets/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/route-targets/{id}/
#
# operationId: ipam_route-targets_delete
export def "ipam-route-targets delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/route-targets/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/route-targets/{id}/
#
# operationId: ipam_route-targets_read
export def "ipam-route-targets get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/route-targets/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/route-targets/{id}/
#
# operationId: ipam_route-targets_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-route-targets update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string # Route target value (formatted in accordance with RFC 4360)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/route-targets/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/route-targets/{id}/
#
# operationId: ipam_route-targets_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-route-targets update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string # Route target value (formatted in accordance with RFC 4360)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/route-targets/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/service-templates/
#
# operationId: ipam_service-templates_bulk_delete
export def "ipam-service-templates delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/service-templates/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/service-templates/
#
# operationId: ipam_service-templates_list
export def "ipam-service-templates list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --protocol: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --port: float
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --protocol-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, ports: list, protocol: record, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "protocol" $protocol "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "port" $port "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "protocol__n" $protocol_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/service-templates/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "protocol": $protocol, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "port": $port, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "protocol__n": $protocol_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/service-templates/
#
# operationId: ipam_service-templates_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-service-templates update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/service-templates/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/service-templates/
#
# operationId: ipam_service-templates_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-service-templates create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/service-templates/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/service-templates/
#
# operationId: ipam_service-templates_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-service-templates update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/service-templates/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/service-templates/{id}/
#
# operationId: ipam_service-templates_delete
export def "ipam-service-templates delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/service-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/service-templates/{id}/
#
# operationId: ipam_service-templates_read
export def "ipam-service-templates get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/service-templates/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/service-templates/{id}/
#
# operationId: ipam_service-templates_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-service-templates update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/service-templates/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/service-templates/{id}/
#
# operationId: ipam_service-templates_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-service-templates update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/service-templates/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/services/
#
# operationId: ipam_services_bulk_delete
export def "ipam-services delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/services/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/services/
#
# operationId: ipam_services_list
export def "ipam-services list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --protocol: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --device-id: string
  --device: string
  --virtual-machine-id: string
  --virtual-machine: string
  --ipaddress-id: string
  --ipaddress: string
  --port: float
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --protocol-n: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --device-id-n: string
  --device-n: string
  --virtual-machine-id-n: string
  --virtual-machine-n: string
  --ipaddress-id-n: string
  --ipaddress-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, device: record, display: string, id: int, ipaddresses: list, last_updated: string, name: string, ports: list, protocol: record, tags: list, url: string, virtual_machine: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "protocol" $protocol "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "virtual_machine_id" $virtual_machine_id "scalar") (serialize-qp "virtual_machine" $virtual_machine "scalar") (serialize-qp "ipaddress_id" $ipaddress_id "scalar") (serialize-qp "ipaddress" $ipaddress "scalar") (serialize-qp "port" $port "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "protocol__n" $protocol_n "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "virtual_machine_id__n" $virtual_machine_id_n "scalar") (serialize-qp "virtual_machine__n" $virtual_machine_n "scalar") (serialize-qp "ipaddress_id__n" $ipaddress_id_n "scalar") (serialize-qp "ipaddress__n" $ipaddress_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/services/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "protocol": $protocol, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "device_id": $device_id, "device": $device, "virtual_machine_id": $virtual_machine_id, "virtual_machine": $virtual_machine, "ipaddress_id": $ipaddress_id, "ipaddress": $ipaddress, "port": $port, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "protocol__n": $protocol_n, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "device_id__n": $device_id_n, "device__n": $device_n, "virtual_machine_id__n": $virtual_machine_id_n, "virtual_machine__n": $virtual_machine_n, "ipaddress_id__n": $ipaddress_id_n, "ipaddress__n": $ipaddress_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/services/
#
# operationId: ipam_services_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-services update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --ipaddresses: list<int>
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --virtual-machine: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, ipaddresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/services/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "ipaddresses": $ipaddresses, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags, "virtual_machine": $virtual_machine} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/services/
#
# operationId: ipam_services_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-services create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --ipaddresses: list<int>
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --virtual-machine: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, ipaddresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/services/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "ipaddresses": $ipaddresses, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags, "virtual_machine": $virtual_machine} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/services/
#
# operationId: ipam_services_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-services update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --ipaddresses: list<int>
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --virtual-machine: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, ipaddresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/services/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "ipaddresses": $ipaddresses, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags, "virtual_machine": $virtual_machine} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/services/{id}/
#
# operationId: ipam_services_delete
export def "ipam-services delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/services/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/services/{id}/
#
# operationId: ipam_services_read
export def "ipam-services get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, ipaddresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/services/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/services/{id}/
#
# operationId: ipam_services_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-services update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --ipaddresses: list<int>
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --virtual-machine: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, ipaddresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/services/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "ipaddresses": $ipaddresses, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags, "virtual_machine": $virtual_machine} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/services/{id}/
#
# operationId: ipam_services_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-services update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --ipaddresses: list<int>
  name: string
  ports: list<int>
  protocol: string@protocol-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --virtual-machine: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, ipaddresses: table<address: string, display: string, family: int, id: int, url: string>, last_updated: string, name: string, ports: list<int>, protocol: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/services/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "ipaddresses": $ipaddresses, "name": $name, "ports": $ports, "protocol": $protocol, "tags": $tags, "virtual_machine": $virtual_machine} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/vlan-groups/
#
# operationId: ipam_vlan-groups_bulk_delete
export def "ipam-vlan-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlan-groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/vlan-groups/
#
# operationId: ipam_vlan-groups_list
export def "ipam-vlan-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --min-vid: string
  --max-vid: string
  --description: string
  --scope-id: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --scope-type: string
  --region: float
  --sitegroup: float
  --site: float
  --location: float
  --rack: float
  --clustergroup: float
  --cluster: float
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --min-vid-n: string
  --min-vid-lte: string
  --min-vid-lt: string
  --min-vid-gte: string
  --min-vid-gt: string
  --max-vid-n: string
  --max-vid-lte: string
  --max-vid-lt: string
  --max-vid-gte: string
  --max-vid-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --scope-id-n: string
  --scope-id-lte: string
  --scope-id-lt: string
  --scope-id-gte: string
  --scope-id-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --scope-type-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, max_vid: int, min_vid: int, name: string, scope: record, scope_id: int, scope_type: string, slug: string, tags: list, url: string, vlan_count: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "min_vid" $min_vid "scalar") (serialize-qp "max_vid" $max_vid "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "scope_id" $scope_id "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "scope_type" $scope_type "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "sitegroup" $sitegroup "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "location" $location "scalar") (serialize-qp "rack" $rack "scalar") (serialize-qp "clustergroup" $clustergroup "scalar") (serialize-qp "cluster" $cluster "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "min_vid__n" $min_vid_n "scalar") (serialize-qp "min_vid__lte" $min_vid_lte "scalar") (serialize-qp "min_vid__lt" $min_vid_lt "scalar") (serialize-qp "min_vid__gte" $min_vid_gte "scalar") (serialize-qp "min_vid__gt" $min_vid_gt "scalar") (serialize-qp "max_vid__n" $max_vid_n "scalar") (serialize-qp "max_vid__lte" $max_vid_lte "scalar") (serialize-qp "max_vid__lt" $max_vid_lt "scalar") (serialize-qp "max_vid__gte" $max_vid_gte "scalar") (serialize-qp "max_vid__gt" $max_vid_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "scope_id__n" $scope_id_n "scalar") (serialize-qp "scope_id__lte" $scope_id_lte "scalar") (serialize-qp "scope_id__lt" $scope_id_lt "scalar") (serialize-qp "scope_id__gte" $scope_id_gte "scalar") (serialize-qp "scope_id__gt" $scope_id_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "scope_type__n" $scope_type_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/vlan-groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "min_vid": $min_vid, "max_vid": $max_vid, "description": $description, "scope_id": $scope_id, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "scope_type": $scope_type, "region": $region, "sitegroup": $sitegroup, "site": $site, "location": $location, "rack": $rack, "clustergroup": $clustergroup, "cluster": $cluster, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "min_vid__n": $min_vid_n, "min_vid__lte": $min_vid_lte, "min_vid__lt": $min_vid_lt, "min_vid__gte": $min_vid_gte, "min_vid__gt": $min_vid_gt, "max_vid__n": $max_vid_n, "max_vid__lte": $max_vid_lte, "max_vid__lt": $max_vid_lt, "max_vid__gte": $max_vid_gte, "max_vid__gt": $max_vid_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "scope_id__n": $scope_id_n, "scope_id__lte": $scope_id_lte, "scope_id__lt": $scope_id_lt, "scope_id__gte": $scope_id_gte, "scope_id__gt": $scope_id_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "scope_type__n": $scope_type_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/vlan-groups/
#
# operationId: ipam_vlan-groups_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlan-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --max-vid: int # Highest permissible ID of a child VLAN
  --min-vid: int # Lowest permissible ID of a child VLAN
  name: string
  --scope-id: int # nullable
  --scope-type: string # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, max_vid: int, min_vid: int, name: string, scope: record, scope_id: int, scope_type: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlan-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "max_vid": $max_vid, "min_vid": $min_vid, "name": $name, "scope_id": $scope_id, "scope_type": $scope_type, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/vlan-groups/
#
# operationId: ipam_vlan-groups_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlan-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --max-vid: int # Highest permissible ID of a child VLAN
  --min-vid: int # Lowest permissible ID of a child VLAN
  name: string
  --scope-id: int # nullable
  --scope-type: string # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, max_vid: int, min_vid: int, name: string, scope: record, scope_id: int, scope_type: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlan-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "max_vid": $max_vid, "min_vid": $min_vid, "name": $name, "scope_id": $scope_id, "scope_type": $scope_type, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/vlan-groups/
#
# operationId: ipam_vlan-groups_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlan-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --max-vid: int # Highest permissible ID of a child VLAN
  --min-vid: int # Lowest permissible ID of a child VLAN
  name: string
  --scope-id: int # nullable
  --scope-type: string # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, max_vid: int, min_vid: int, name: string, scope: record, scope_id: int, scope_type: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlan-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "max_vid": $max_vid, "min_vid": $min_vid, "name": $name, "scope_id": $scope_id, "scope_type": $scope_type, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/vlan-groups/{id}/
#
# operationId: ipam_vlan-groups_delete
export def "ipam-vlan-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlan-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/vlan-groups/{id}/
#
# operationId: ipam_vlan-groups_read
export def "ipam-vlan-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, max_vid: int, min_vid: int, name: string, scope: record, scope_id: int, scope_type: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlan-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/vlan-groups/{id}/
#
# operationId: ipam_vlan-groups_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlan-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --max-vid: int # Highest permissible ID of a child VLAN
  --min-vid: int # Lowest permissible ID of a child VLAN
  name: string
  --scope-id: int # nullable
  --scope-type: string # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, max_vid: int, min_vid: int, name: string, scope: record, scope_id: int, scope_type: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlan-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "max_vid": $max_vid, "min_vid": $min_vid, "name": $name, "scope_id": $scope_id, "scope_type": $scope_type, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/vlan-groups/{id}/
#
# operationId: ipam_vlan-groups_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlan-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  --max-vid: int # Highest permissible ID of a child VLAN
  --min-vid: int # Lowest permissible ID of a child VLAN
  name: string
  --scope-id: int # nullable
  --scope-type: string # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, max_vid: int, min_vid: int, name: string, scope: record, scope_id: int, scope_type: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, vlan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlan-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "max_vid": $max_vid, "min_vid": $min_vid, "name": $name, "scope_id": $scope_id, "scope_type": $scope_type, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# GET /ipam/vlan-groups/{id}/available-vlans/
#
# operationId: ipam_vlan-groups_available-vlans_list
export def "ipam-vlan-groups-available-vlans list" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> table<group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, vid: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlan-groups/{id}/available-vlans/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# POST /ipam/vlan-groups/{id}/available-vlans/
#
# operationId: ipam_vlan-groups_available-vlans_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlan-groups-available-vlans create" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-9
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> table<comments: string, created: string, custom_fields: record, description: string, display: string, group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record, url: string>, last_updated: string, name: string, prefix_count: int, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: list<record>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vid: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlan-groups/{id}/available-vlans/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/vlans/
#
# operationId: ipam_vlans_bulk_delete
export def "ipam-vlans delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlans/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/vlans/
#
# operationId: ipam_vlans_list
export def "ipam-vlans list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --vid: string
  --name: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --group-id: string
  --group: string
  --role-id: string
  --role: string
  --status: string
  --available-on-device: string
  --available-on-virtualmachine: string
  --l2vpn-id: string
  --l2vpn: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --vid-n: string
  --vid-lte: string
  --vid-lt: string
  --vid-gte: string
  --vid-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --group-id-n: string
  --group-n: string
  --role-id-n: string
  --role-n: string
  --status-n: string
  --l2vpn-id-n: string
  --l2vpn-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, group: record, id: int, l2vpn_termination: record, last_updated: string, name: string, prefix_count: int, role: record, site: record, status: record, tags: list, tenant: record, url: string, vid: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "vid" $vid "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "available_on_device" $available_on_device "scalar") (serialize-qp "available_on_virtualmachine" $available_on_virtualmachine "scalar") (serialize-qp "l2vpn_id" $l2vpn_id "scalar") (serialize-qp "l2vpn" $l2vpn "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "vid__n" $vid_n "scalar") (serialize-qp "vid__lte" $vid_lte "scalar") (serialize-qp "vid__lt" $vid_lt "scalar") (serialize-qp "vid__gte" $vid_gte "scalar") (serialize-qp "vid__gt" $vid_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "l2vpn_id__n" $l2vpn_id_n "scalar") (serialize-qp "l2vpn__n" $l2vpn_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/vlans/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "vid": $vid, "name": $name, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "group_id": $group_id, "group": $group, "role_id": $role_id, "role": $role, "status": $status, "available_on_device": $available_on_device, "available_on_virtualmachine": $available_on_virtualmachine, "l2vpn_id": $l2vpn_id, "l2vpn": $l2vpn, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "vid__n": $vid_n, "vid__lte": $vid_lte, "vid__lt": $vid_lt, "vid__gte": $vid_gte, "vid__gt": $vid_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "group_id__n": $group_id_n, "group__n": $group_n, "role_id__n": $role_id_n, "role__n": $role_n, "status__n": $status_n, "l2vpn_id__n": $l2vpn_id_n, "l2vpn__n": $l2vpn_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/vlans/
#
# operationId: ipam_vlans_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlans update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-9
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  vid: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, name: string, prefix_count: int, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vid: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlans/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vid": $vid} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/vlans/
#
# operationId: ipam_vlans_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlans create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-9
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  vid: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, name: string, prefix_count: int, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vid: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlans/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vid": $vid} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/vlans/
#
# operationId: ipam_vlans_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlans update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-9
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  vid: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, name: string, prefix_count: int, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vid: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vlans/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vid": $vid} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/vlans/{id}/
#
# operationId: ipam_vlans_delete
export def "ipam-vlans delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlans/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/vlans/{id}/
#
# operationId: ipam_vlans_read
export def "ipam-vlans get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, name: string, prefix_count: int, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vid: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlans/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/vlans/{id}/
#
# operationId: ipam_vlans_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlans update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-9
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  vid: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, name: string, prefix_count: int, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vid: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlans/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vid": $vid} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/vlans/{id}/
#
# operationId: ipam_vlans_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vlans update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-9
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  vid: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, group: record<display: string, id: int, name: string, slug: string, url: string, vlan_count: int>, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, name: string, prefix_count: int, role: record<display: string, id: int, name: string, prefix_count: int, slug: string, url: string, vlan_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vid: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vlans/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vid": $vid} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/vrfs/
#
# operationId: ipam_vrfs_bulk_delete
export def "ipam-vrfs delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vrfs/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/vrfs/
#
# operationId: ipam_vrfs_list
export def "ipam-vrfs list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --rd: string
  --enforce-unique: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --import-target-id: string
  --import-target: string
  --export-target-id: string
  --export-target: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --rd-n: string
  --rd-ic: string
  --rd-nic: string
  --rd-iew: string
  --rd-niew: string
  --rd-isw: string
  --rd-nisw: string
  --rd-ie: string
  --rd-nie: string
  --rd-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --import-target-id-n: string
  --import-target-n: string
  --export-target-id-n: string
  --export-target-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, display: string, enforce_unique: bool, export_targets: list, id: int, import_targets: list, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rd: string, tags: list, tenant: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "rd" $rd "scalar") (serialize-qp "enforce_unique" $enforce_unique "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "import_target_id" $import_target_id "scalar") (serialize-qp "import_target" $import_target "scalar") (serialize-qp "export_target_id" $export_target_id "scalar") (serialize-qp "export_target" $export_target "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "rd__n" $rd_n "scalar") (serialize-qp "rd__ic" $rd_ic "scalar") (serialize-qp "rd__nic" $rd_nic "scalar") (serialize-qp "rd__iew" $rd_iew "scalar") (serialize-qp "rd__niew" $rd_niew "scalar") (serialize-qp "rd__isw" $rd_isw "scalar") (serialize-qp "rd__nisw" $rd_nisw "scalar") (serialize-qp "rd__ie" $rd_ie "scalar") (serialize-qp "rd__nie" $rd_nie "scalar") (serialize-qp "rd__empty" $rd_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "import_target_id__n" $import_target_id_n "scalar") (serialize-qp "import_target__n" $import_target_n "scalar") (serialize-qp "export_target_id__n" $export_target_id_n "scalar") (serialize-qp "export_target__n" $export_target_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/ipam/vrfs/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "rd": $rd, "enforce_unique": $enforce_unique, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "import_target_id": $import_target_id, "import_target": $import_target, "export_target_id": $export_target_id, "export_target": $export_target, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "rd__n": $rd_n, "rd__ic": $rd_ic, "rd__nic": $rd_nic, "rd__iew": $rd_iew, "rd__niew": $rd_niew, "rd__isw": $rd_isw, "rd__nisw": $rd_nisw, "rd__ie": $rd_ie, "rd__nie": $rd_nie, "rd__empty": $rd_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "import_target_id__n": $import_target_id_n, "import_target__n": $import_target_n, "export_target_id__n": $export_target_id_n, "export_target__n": $export_target_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /ipam/vrfs/
#
# operationId: ipam_vrfs_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vrfs update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --enforce-unique: oneof<nothing, bool> # Prevent duplicate prefixes/IP addresses within this VRF
  --export-targets: list<int>
  --import-targets: list<int>
  name: string
  --rd: string # Unique route distinguisher (as defined in RFC 4364) (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, enforce_unique: bool, export_targets: table<display: string, id: int, name: string, url: string>, id: int, import_targets: table<display: string, id: int, name: string, url: string>, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rd: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vrfs/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "enforce_unique": $enforce_unique, "export_targets": $export_targets, "import_targets": $import_targets, "name": $name, "rd": $rd, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /ipam/vrfs/
#
# operationId: ipam_vrfs_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vrfs create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --enforce-unique: oneof<nothing, bool> # Prevent duplicate prefixes/IP addresses within this VRF
  --export-targets: list<int>
  --import-targets: list<int>
  name: string
  --rd: string # Unique route distinguisher (as defined in RFC 4364) (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, enforce_unique: bool, export_targets: table<display: string, id: int, name: string, url: string>, id: int, import_targets: table<display: string, id: int, name: string, url: string>, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rd: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vrfs/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "enforce_unique": $enforce_unique, "export_targets": $export_targets, "import_targets": $import_targets, "name": $name, "rd": $rd, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/vrfs/
#
# operationId: ipam_vrfs_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vrfs update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --enforce-unique: oneof<nothing, bool> # Prevent duplicate prefixes/IP addresses within this VRF
  --export-targets: list<int>
  --import-targets: list<int>
  name: string
  --rd: string # Unique route distinguisher (as defined in RFC 4364) (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, enforce_unique: bool, export_targets: table<display: string, id: int, name: string, url: string>, id: int, import_targets: table<display: string, id: int, name: string, url: string>, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rd: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/ipam/vrfs/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "enforce_unique": $enforce_unique, "export_targets": $export_targets, "import_targets": $import_targets, "name": $name, "rd": $rd, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /ipam/vrfs/{id}/
#
# operationId: ipam_vrfs_delete
export def "ipam-vrfs delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vrfs/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /ipam/vrfs/{id}/
#
# operationId: ipam_vrfs_read
export def "ipam-vrfs get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, display: string, enforce_unique: bool, export_targets: table<display: string, id: int, name: string, url: string>, id: int, import_targets: table<display: string, id: int, name: string, url: string>, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rd: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vrfs/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /ipam/vrfs/{id}/
#
# operationId: ipam_vrfs_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vrfs update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --enforce-unique: oneof<nothing, bool> # Prevent duplicate prefixes/IP addresses within this VRF
  --export-targets: list<int>
  --import-targets: list<int>
  name: string
  --rd: string # Unique route distinguisher (as defined in RFC 4364) (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, enforce_unique: bool, export_targets: table<display: string, id: int, name: string, url: string>, id: int, import_targets: table<display: string, id: int, name: string, url: string>, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rd: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vrfs/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "enforce_unique": $enforce_unique, "export_targets": $export_targets, "import_targets": $import_targets, "name": $name, "rd": $rd, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /ipam/vrfs/{id}/
#
# operationId: ipam_vrfs_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "ipam-vrfs update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --enforce-unique: oneof<nothing, bool> # Prevent duplicate prefixes/IP addresses within this VRF
  --export-targets: list<int>
  --import-targets: list<int>
  name: string
  --rd: string # Unique route distinguisher (as defined in RFC 4364) (nullable)
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<comments: string, created: string, custom_fields: record, description: string, display: string, enforce_unique: bool, export_targets: table<display: string, id: int, name: string, url: string>, id: int, import_targets: table<display: string, id: int, name: string, url: string>, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rd: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/ipam/vrfs/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "enforce_unique": $enforce_unique, "export_targets": $export_targets, "import_targets": $import_targets, "name": $name, "rd": $rd, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# A lightweight read-only endpoint for conveying NetBox's current operational status.
#
# GET /status/
# operationId: status_list
export def "status list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/status/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /tenancy/contact-assignments/
#
# operationId: tenancy_contact-assignments_bulk_delete
export def "tenancy-contact-assignments delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-assignments/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contact-assignments/
#
# operationId: tenancy_contact-assignments_list
export def "tenancy-contact-assignments list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --content-type-id: string
  --object-id: string
  --priority: string
  --created: string
  --last-updated: string
  --content-type: string
  --contact-id: string
  --role-id: string
  --role: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --content-type-id-n: string
  --object-id-n: string
  --object-id-lte: string
  --object-id-lt: string
  --object-id-gte: string
  --object-id-gt: string
  --priority-n: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --content-type-n: string
  --contact-id-n: string
  --role-id-n: string
  --role-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<contact: record, content_type: string, created: string, display: string, id: int, last_updated: string, object: record, object_id: int, priority: record, role: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "content_type_id" $content_type_id "scalar") (serialize-qp "object_id" $object_id "scalar") (serialize-qp "priority" $priority "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "content_type" $content_type "scalar") (serialize-qp "contact_id" $contact_id "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "content_type_id__n" $content_type_id_n "scalar") (serialize-qp "object_id__n" $object_id_n "scalar") (serialize-qp "object_id__lte" $object_id_lte "scalar") (serialize-qp "object_id__lt" $object_id_lt "scalar") (serialize-qp "object_id__gte" $object_id_gte "scalar") (serialize-qp "object_id__gt" $object_id_gt "scalar") (serialize-qp "priority__n" $priority_n "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "content_type__n" $content_type_n "scalar") (serialize-qp "contact_id__n" $contact_id_n "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tenancy/contact-assignments/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "content_type_id": $content_type_id, "object_id": $object_id, "priority": $priority, "created": $created, "last_updated": $last_updated, "content_type": $content_type, "contact_id": $contact_id, "role_id": $role_id, "role": $role, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "content_type_id__n": $content_type_id_n, "object_id__n": $object_id_n, "object_id__lte": $object_id_lte, "object_id__lt": $object_id_lt, "object_id__gte": $object_id_gte, "object_id__gt": $object_id_gt, "priority__n": $priority_n, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "content_type__n": $content_type_n, "contact_id__n": $contact_id_n, "role_id__n": $role_id_n, "role__n": $role_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /tenancy/contact-assignments/
#
# operationId: tenancy_contact-assignments_bulk_partial_update
export def "tenancy-contact-assignments update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  contact: int
  content_type: string
  object_id: int
  --priority: string@priority-completer
  role: int
]: any -> record<contact: record<display: string, id: int, name: string, url: string>, content_type: string, created: string, display: string, id: int, last_updated: string, object: record, object_id: int, priority: record<label: string, value: string>, role: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-assignments/")
  let req_body = {"contact": $contact, "content_type": $content_type, "object_id": $object_id, "priority": $priority, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /tenancy/contact-assignments/
#
# operationId: tenancy_contact-assignments_create
export def "tenancy-contact-assignments create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  contact: int
  content_type: string
  object_id: int
  --priority: string@priority-completer
  role: int
]: any -> record<contact: record<display: string, id: int, name: string, url: string>, content_type: string, created: string, display: string, id: int, last_updated: string, object: record, object_id: int, priority: record<label: string, value: string>, role: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-assignments/")
  let req_body = {"contact": $contact, "content_type": $content_type, "object_id": $object_id, "priority": $priority, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contact-assignments/
#
# operationId: tenancy_contact-assignments_bulk_update
export def "tenancy-contact-assignments update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  contact: int
  content_type: string
  object_id: int
  --priority: string@priority-completer
  role: int
]: any -> record<contact: record<display: string, id: int, name: string, url: string>, content_type: string, created: string, display: string, id: int, last_updated: string, object: record, object_id: int, priority: record<label: string, value: string>, role: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-assignments/")
  let req_body = {"contact": $contact, "content_type": $content_type, "object_id": $object_id, "priority": $priority, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/contact-assignments/{id}/
#
# operationId: tenancy_contact-assignments_delete
export def "tenancy-contact-assignments delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-assignments/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contact-assignments/{id}/
#
# operationId: tenancy_contact-assignments_read
export def "tenancy-contact-assignments get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<contact: record<display: string, id: int, name: string, url: string>, content_type: string, created: string, display: string, id: int, last_updated: string, object: record, object_id: int, priority: record<label: string, value: string>, role: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-assignments/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /tenancy/contact-assignments/{id}/
#
# operationId: tenancy_contact-assignments_partial_update
export def "tenancy-contact-assignments update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  contact: int
  content_type: string
  object_id: int
  --priority: string@priority-completer
  role: int
]: any -> record<contact: record<display: string, id: int, name: string, url: string>, content_type: string, created: string, display: string, id: int, last_updated: string, object: record, object_id: int, priority: record<label: string, value: string>, role: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-assignments/{id}/"))
  let req_body = {"contact": $contact, "content_type": $content_type, "object_id": $object_id, "priority": $priority, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contact-assignments/{id}/
#
# operationId: tenancy_contact-assignments_update
export def "tenancy-contact-assignments update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  contact: int
  content_type: string
  object_id: int
  --priority: string@priority-completer
  role: int
]: any -> record<contact: record<display: string, id: int, name: string, url: string>, content_type: string, created: string, display: string, id: int, last_updated: string, object: record, object_id: int, priority: record<label: string, value: string>, role: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-assignments/{id}/"))
  let req_body = {"contact": $contact, "content_type": $content_type, "object_id": $object_id, "priority": $priority, "role": $role} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/contact-groups/
#
# operationId: tenancy_contact-groups_bulk_delete
export def "tenancy-contact-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contact-groups/
#
# operationId: tenancy_contact-groups_list
export def "tenancy-contact-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --parent-id: string
  --parent: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --parent-id-n: string
  --parent-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, contact_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "parent" $parent "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "parent__n" $parent_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tenancy/contact-groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "parent_id": $parent_id, "parent": $parent, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "parent_id__n": $parent_id_n, "parent__n": $parent_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /tenancy/contact-groups/
#
# operationId: tenancy_contact-groups_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, contact_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /tenancy/contact-groups/
#
# operationId: tenancy_contact-groups_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, contact_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contact-groups/
#
# operationId: tenancy_contact-groups_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, contact_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/contact-groups/{id}/
#
# operationId: tenancy_contact-groups_delete
export def "tenancy-contact-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contact-groups/{id}/
#
# operationId: tenancy_contact-groups_read
export def "tenancy-contact-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, contact_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /tenancy/contact-groups/{id}/
#
# operationId: tenancy_contact-groups_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, contact_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contact-groups/{id}/
#
# operationId: tenancy_contact-groups_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, contact_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/contact-roles/
#
# operationId: tenancy_contact-roles_bulk_delete
export def "tenancy-contact-roles delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-roles/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contact-roles/
#
# operationId: tenancy_contact-roles_list
export def "tenancy-contact-roles list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tenancy/contact-roles/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /tenancy/contact-roles/
#
# operationId: tenancy_contact-roles_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-roles update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-roles/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /tenancy/contact-roles/
#
# operationId: tenancy_contact-roles_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-roles create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-roles/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contact-roles/
#
# operationId: tenancy_contact-roles_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-roles update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contact-roles/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/contact-roles/{id}/
#
# operationId: tenancy_contact-roles_delete
export def "tenancy-contact-roles delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contact-roles/{id}/
#
# operationId: tenancy_contact-roles_read
export def "tenancy-contact-roles get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-roles/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /tenancy/contact-roles/{id}/
#
# operationId: tenancy_contact-roles_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-roles update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-roles/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contact-roles/{id}/
#
# operationId: tenancy_contact-roles_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contact-roles update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contact-roles/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/contacts/
#
# operationId: tenancy_contacts_bulk_delete
export def "tenancy-contacts delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contacts/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contacts/
#
# operationId: tenancy_contacts_list
export def "tenancy-contacts list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --title: string
  --phone: string
  --email: string
  --address: string
  --link: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --group-id: string
  --group: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --title-n: string
  --title-ic: string
  --title-nic: string
  --title-iew: string
  --title-niew: string
  --title-isw: string
  --title-nisw: string
  --title-ie: string
  --title-nie: string
  --title-empty: string
  --phone-n: string
  --phone-ic: string
  --phone-nic: string
  --phone-iew: string
  --phone-niew: string
  --phone-isw: string
  --phone-nisw: string
  --phone-ie: string
  --phone-nie: string
  --phone-empty: string
  --email-n: string
  --email-ic: string
  --email-nic: string
  --email-iew: string
  --email-niew: string
  --email-isw: string
  --email-nisw: string
  --email-ie: string
  --email-nie: string
  --email-empty: string
  --address-n: string
  --address-ic: string
  --address-nic: string
  --address-iew: string
  --address-niew: string
  --address-isw: string
  --address-nisw: string
  --address-ie: string
  --address-nie: string
  --address-empty: string
  --link-n: string
  --link-ic: string
  --link-nic: string
  --link-iew: string
  --link-niew: string
  --link-isw: string
  --link-nisw: string
  --link-ie: string
  --link-nie: string
  --link-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --group-id-n: string
  --group-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<address: string, comments: string, created: string, custom_fields: record, description: string, display: string, email: string, group: record, id: int, last_updated: string, link: string, name: string, phone: string, tags: list, title: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "title" $title "scalar") (serialize-qp "phone" $phone "scalar") (serialize-qp "email" $email "scalar") (serialize-qp "address" $address "scalar") (serialize-qp "link" $link "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "title__n" $title_n "scalar") (serialize-qp "title__ic" $title_ic "scalar") (serialize-qp "title__nic" $title_nic "scalar") (serialize-qp "title__iew" $title_iew "scalar") (serialize-qp "title__niew" $title_niew "scalar") (serialize-qp "title__isw" $title_isw "scalar") (serialize-qp "title__nisw" $title_nisw "scalar") (serialize-qp "title__ie" $title_ie "scalar") (serialize-qp "title__nie" $title_nie "scalar") (serialize-qp "title__empty" $title_empty "scalar") (serialize-qp "phone__n" $phone_n "scalar") (serialize-qp "phone__ic" $phone_ic "scalar") (serialize-qp "phone__nic" $phone_nic "scalar") (serialize-qp "phone__iew" $phone_iew "scalar") (serialize-qp "phone__niew" $phone_niew "scalar") (serialize-qp "phone__isw" $phone_isw "scalar") (serialize-qp "phone__nisw" $phone_nisw "scalar") (serialize-qp "phone__ie" $phone_ie "scalar") (serialize-qp "phone__nie" $phone_nie "scalar") (serialize-qp "phone__empty" $phone_empty "scalar") (serialize-qp "email__n" $email_n "scalar") (serialize-qp "email__ic" $email_ic "scalar") (serialize-qp "email__nic" $email_nic "scalar") (serialize-qp "email__iew" $email_iew "scalar") (serialize-qp "email__niew" $email_niew "scalar") (serialize-qp "email__isw" $email_isw "scalar") (serialize-qp "email__nisw" $email_nisw "scalar") (serialize-qp "email__ie" $email_ie "scalar") (serialize-qp "email__nie" $email_nie "scalar") (serialize-qp "email__empty" $email_empty "scalar") (serialize-qp "address__n" $address_n "scalar") (serialize-qp "address__ic" $address_ic "scalar") (serialize-qp "address__nic" $address_nic "scalar") (serialize-qp "address__iew" $address_iew "scalar") (serialize-qp "address__niew" $address_niew "scalar") (serialize-qp "address__isw" $address_isw "scalar") (serialize-qp "address__nisw" $address_nisw "scalar") (serialize-qp "address__ie" $address_ie "scalar") (serialize-qp "address__nie" $address_nie "scalar") (serialize-qp "address__empty" $address_empty "scalar") (serialize-qp "link__n" $link_n "scalar") (serialize-qp "link__ic" $link_ic "scalar") (serialize-qp "link__nic" $link_nic "scalar") (serialize-qp "link__iew" $link_iew "scalar") (serialize-qp "link__niew" $link_niew "scalar") (serialize-qp "link__isw" $link_isw "scalar") (serialize-qp "link__nisw" $link_nisw "scalar") (serialize-qp "link__ie" $link_ie "scalar") (serialize-qp "link__nie" $link_nie "scalar") (serialize-qp "link__empty" $link_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tenancy/contacts/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "title": $title, "phone": $phone, "email": $email, "address": $address, "link": $link, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "group_id": $group_id, "group": $group, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "title__n": $title_n, "title__ic": $title_ic, "title__nic": $title_nic, "title__iew": $title_iew, "title__niew": $title_niew, "title__isw": $title_isw, "title__nisw": $title_nisw, "title__ie": $title_ie, "title__nie": $title_nie, "title__empty": $title_empty, "phone__n": $phone_n, "phone__ic": $phone_ic, "phone__nic": $phone_nic, "phone__iew": $phone_iew, "phone__niew": $phone_niew, "phone__isw": $phone_isw, "phone__nisw": $phone_nisw, "phone__ie": $phone_ie, "phone__nie": $phone_nie, "phone__empty": $phone_empty, "email__n": $email_n, "email__ic": $email_ic, "email__nic": $email_nic, "email__iew": $email_iew, "email__niew": $email_niew, "email__isw": $email_isw, "email__nisw": $email_nisw, "email__ie": $email_ie, "email__nie": $email_nie, "email__empty": $email_empty, "address__n": $address_n, "address__ic": $address_ic, "address__nic": $address_nic, "address__iew": $address_iew, "address__niew": $address_niew, "address__isw": $address_isw, "address__nisw": $address_nisw, "address__ie": $address_ie, "address__nie": $address_nie, "address__empty": $address_empty, "link__n": $link_n, "link__ic": $link_ic, "link__nic": $link_nic, "link__iew": $link_iew, "link__niew": $link_niew, "link__isw": $link_isw, "link__nisw": $link_nisw, "link__ie": $link_ie, "link__nie": $link_nie, "link__empty": $link_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "group_id__n": $group_id_n, "group__n": $group_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /tenancy/contacts/
#
# operationId: tenancy_contacts_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contacts update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --address: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --email: string # format: email
  --group: int # nullable
  --link: string # format: uri
  name: string
  --phone: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --title: string
]: any -> record<address: string, comments: string, created: string, custom_fields: record, description: string, display: string, email: string, group: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, link: string, name: string, phone: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, title: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contacts/")
  let req_body = {"address": $address, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "email": $email, "group": $group, "link": $link, "name": $name, "phone": $phone, "tags": $tags, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /tenancy/contacts/
#
# operationId: tenancy_contacts_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contacts create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --address: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --email: string # format: email
  --group: int # nullable
  --link: string # format: uri
  name: string
  --phone: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --title: string
]: any -> record<address: string, comments: string, created: string, custom_fields: record, description: string, display: string, email: string, group: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, link: string, name: string, phone: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, title: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contacts/")
  let req_body = {"address": $address, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "email": $email, "group": $group, "link": $link, "name": $name, "phone": $phone, "tags": $tags, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contacts/
#
# operationId: tenancy_contacts_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contacts update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --address: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --email: string # format: email
  --group: int # nullable
  --link: string # format: uri
  name: string
  --phone: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --title: string
]: any -> record<address: string, comments: string, created: string, custom_fields: record, description: string, display: string, email: string, group: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, link: string, name: string, phone: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, title: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/contacts/")
  let req_body = {"address": $address, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "email": $email, "group": $group, "link": $link, "name": $name, "phone": $phone, "tags": $tags, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/contacts/{id}/
#
# operationId: tenancy_contacts_delete
export def "tenancy-contacts delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contacts/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/contacts/{id}/
#
# operationId: tenancy_contacts_read
export def "tenancy-contacts get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<address: string, comments: string, created: string, custom_fields: record, description: string, display: string, email: string, group: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, link: string, name: string, phone: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, title: string, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contacts/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /tenancy/contacts/{id}/
#
# operationId: tenancy_contacts_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contacts update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --address: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --email: string # format: email
  --group: int # nullable
  --link: string # format: uri
  name: string
  --phone: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --title: string
]: any -> record<address: string, comments: string, created: string, custom_fields: record, description: string, display: string, email: string, group: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, link: string, name: string, phone: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, title: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contacts/{id}/"))
  let req_body = {"address": $address, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "email": $email, "group": $group, "link": $link, "name": $name, "phone": $phone, "tags": $tags, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/contacts/{id}/
#
# operationId: tenancy_contacts_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-contacts update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --address: string
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --email: string # format: email
  --group: int # nullable
  --link: string # format: uri
  name: string
  --phone: string
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --title: string
]: any -> record<address: string, comments: string, created: string, custom_fields: record, description: string, display: string, email: string, group: record<_depth: int, contact_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, link: string, name: string, phone: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, title: string, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/contacts/{id}/"))
  let req_body = {"address": $address, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "email": $email, "group": $group, "link": $link, "name": $name, "phone": $phone, "tags": $tags, "title": $title} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/tenant-groups/
#
# operationId: tenancy_tenant-groups_bulk_delete
export def "tenancy-tenant-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenant-groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/tenant-groups/
#
# operationId: tenancy_tenant-groups_list
export def "tenancy-tenant-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --parent-id: string
  --parent: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --parent-id-n: string
  --parent-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record, slug: string, tags: list, tenant_count: int, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "parent" $parent "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "parent__n" $parent_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tenancy/tenant-groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "parent_id": $parent_id, "parent": $parent, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "parent_id__n": $parent_id_n, "parent__n": $parent_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /tenancy/tenant-groups/
#
# operationId: tenancy_tenant-groups_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenant-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant_count: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenant-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /tenancy/tenant-groups/
#
# operationId: tenancy_tenant-groups_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenant-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant_count: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenant-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/tenant-groups/
#
# operationId: tenancy_tenant-groups_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenant-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant_count: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenant-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/tenant-groups/{id}/
#
# operationId: tenancy_tenant-groups_delete
export def "tenancy-tenant-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenant-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/tenant-groups/{id}/
#
# operationId: tenancy_tenant-groups_read
export def "tenancy-tenant-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant_count: int, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenant-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /tenancy/tenant-groups/{id}/
#
# operationId: tenancy_tenant-groups_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenant-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant_count: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenant-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/tenant-groups/{id}/
#
# operationId: tenancy_tenant-groups_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenant-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant_count: int, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenant-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/tenants/
#
# operationId: tenancy_tenants_bulk_delete
export def "tenancy-tenants delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenants/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/tenants/
#
# operationId: tenancy_tenants_list
export def "tenancy-tenants list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --group-id: string
  --group: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --group-id-n: string
  --group-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<circuit_count: int, cluster_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record, id: int, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rack_count: int, site_count: int, slug: string, tags: list, url: string, virtualmachine_count: int, vlan_count: int, vrf_count: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/tenancy/tenants/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "group_id": $group_id, "group": $group, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "group_id__n": $group_id_n, "group__n": $group_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /tenancy/tenants/
#
# operationId: tenancy_tenants_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenants update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, cluster_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, id: int, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rack_count: int, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vlan_count: int, vrf_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenants/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /tenancy/tenants/
#
# operationId: tenancy_tenants_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenants create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, cluster_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, id: int, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rack_count: int, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vlan_count: int, vrf_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenants/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/tenants/
#
# operationId: tenancy_tenants_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenants update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, cluster_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, id: int, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rack_count: int, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vlan_count: int, vrf_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/tenancy/tenants/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /tenancy/tenants/{id}/
#
# operationId: tenancy_tenants_delete
export def "tenancy-tenants delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenants/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /tenancy/tenants/{id}/
#
# operationId: tenancy_tenants_read
export def "tenancy-tenants get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<circuit_count: int, cluster_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, id: int, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rack_count: int, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vlan_count: int, vrf_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenants/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /tenancy/tenants/{id}/
#
# operationId: tenancy_tenants_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenants update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, cluster_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, id: int, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rack_count: int, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vlan_count: int, vrf_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenants/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /tenancy/tenants/{id}/
#
# operationId: tenancy_tenants_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "tenancy-tenants update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<circuit_count: int, cluster_count: int, comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, tenant_count: int, url: string>, id: int, ipaddress_count: int, last_updated: string, name: string, prefix_count: int, rack_count: int, site_count: int, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int, vlan_count: int, vrf_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/tenancy/tenants/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Return the UserConfig for the currently authenticated User.
#
# GET /users/config/
# operationId: users_config_list
export def "users-config list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/config/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /users/groups/
#
# operationId: users_groups_bulk_delete
export def "users-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/groups/
#
# operationId: users_groups_list
export def "users-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --q: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<display: string, id: int, name: string, url: string, user_count: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/users/groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "q": $q, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /users/groups/
#
# operationId: users_groups_bulk_partial_update
export def "users-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string
]: any -> record<display: string, id: int, name: string, url: string, user_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/groups/")
  let req_body = {"name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /users/groups/
#
# operationId: users_groups_create
export def "users-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string
]: any -> record<display: string, id: int, name: string, url: string, user_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/groups/")
  let req_body = {"name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/groups/
#
# operationId: users_groups_bulk_update
export def "users-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string
]: any -> record<display: string, id: int, name: string, url: string, user_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/groups/")
  let req_body = {"name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /users/groups/{id}/
#
# operationId: users_groups_delete
export def "users-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/groups/{id}/
#
# operationId: users_groups_read
export def "users-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<display: string, id: int, name: string, url: string, user_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /users/groups/{id}/
#
# operationId: users_groups_partial_update
export def "users-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string
]: any -> record<display: string, id: int, name: string, url: string, user_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/groups/{id}/"))
  let req_body = {"name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/groups/{id}/
#
# operationId: users_groups_update
export def "users-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  name: string
]: any -> record<display: string, id: int, name: string, url: string, user_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/groups/{id}/"))
  let req_body = {"name": $name} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /users/permissions/
#
# operationId: users_permissions_bulk_delete
export def "users-permissions delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/permissions/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/permissions/
#
# operationId: users_permissions_list
export def "users-permissions list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --enabled: string
  --object-types: string
  --description: string
  --q: string
  --user-id: string
  --user: string
  --group-id: string
  --group: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --object-types-n: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --user-id-n: string
  --user-n: string
  --group-id-n: string
  --group-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<actions: list, constraints: record, description: string, display: string, enabled: bool, groups: list, id: int, name: string, object_types: list, url: string, users: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "enabled" $enabled "scalar") (serialize-qp "object_types" $object_types "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "user_id" $user_id "scalar") (serialize-qp "user" $user "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "object_types__n" $object_types_n "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "user_id__n" $user_id_n "scalar") (serialize-qp "user__n" $user_n "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/users/permissions/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "enabled": $enabled, "object_types": $object_types, "description": $description, "q": $q, "user_id": $user_id, "user": $user, "group_id": $group_id, "group": $group, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "object_types__n": $object_types_n, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "user_id__n": $user_id_n, "user__n": $user_n, "group_id__n": $group_id_n, "group__n": $group_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /users/permissions/
#
# operationId: users_permissions_bulk_partial_update
export def "users-permissions update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  actions: list<string> # The list of actions granted by this permission
  --constraints: record # Queryset filter matching the applicable objects of the selected type(s) (nullable)
  --description: string
  --enabled: oneof<nothing, bool>
  --groups: list<int>
  name: string
  object_types: list<string>
  --users: list<int>
]: any -> record<actions: list<string>, constraints: record, description: string, display: string, enabled: bool, groups: table<display: string, id: int, name: string, url: string>, id: int, name: string, object_types: list<string>, url: string, users: table<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/permissions/")
  let req_body = {"actions": $actions, "constraints": $constraints, "description": $description, "enabled": $enabled, "groups": $groups, "name": $name, "object_types": $object_types, "users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /users/permissions/
#
# operationId: users_permissions_create
export def "users-permissions create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  actions: list<string> # The list of actions granted by this permission
  --constraints: record # Queryset filter matching the applicable objects of the selected type(s) (nullable)
  --description: string
  --enabled: oneof<nothing, bool>
  --groups: list<int>
  name: string
  object_types: list<string>
  --users: list<int>
]: any -> record<actions: list<string>, constraints: record, description: string, display: string, enabled: bool, groups: table<display: string, id: int, name: string, url: string>, id: int, name: string, object_types: list<string>, url: string, users: table<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/permissions/")
  let req_body = {"actions": $actions, "constraints": $constraints, "description": $description, "enabled": $enabled, "groups": $groups, "name": $name, "object_types": $object_types, "users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/permissions/
#
# operationId: users_permissions_bulk_update
export def "users-permissions update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  actions: list<string> # The list of actions granted by this permission
  --constraints: record # Queryset filter matching the applicable objects of the selected type(s) (nullable)
  --description: string
  --enabled: oneof<nothing, bool>
  --groups: list<int>
  name: string
  object_types: list<string>
  --users: list<int>
]: any -> record<actions: list<string>, constraints: record, description: string, display: string, enabled: bool, groups: table<display: string, id: int, name: string, url: string>, id: int, name: string, object_types: list<string>, url: string, users: table<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/permissions/")
  let req_body = {"actions": $actions, "constraints": $constraints, "description": $description, "enabled": $enabled, "groups": $groups, "name": $name, "object_types": $object_types, "users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /users/permissions/{id}/
#
# operationId: users_permissions_delete
export def "users-permissions delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/permissions/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/permissions/{id}/
#
# operationId: users_permissions_read
export def "users-permissions get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<actions: list<string>, constraints: record, description: string, display: string, enabled: bool, groups: table<display: string, id: int, name: string, url: string>, id: int, name: string, object_types: list<string>, url: string, users: table<display: string, id: int, url: string, username: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/permissions/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /users/permissions/{id}/
#
# operationId: users_permissions_partial_update
export def "users-permissions update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  actions: list<string> # The list of actions granted by this permission
  --constraints: record # Queryset filter matching the applicable objects of the selected type(s) (nullable)
  --description: string
  --enabled: oneof<nothing, bool>
  --groups: list<int>
  name: string
  object_types: list<string>
  --users: list<int>
]: any -> record<actions: list<string>, constraints: record, description: string, display: string, enabled: bool, groups: table<display: string, id: int, name: string, url: string>, id: int, name: string, object_types: list<string>, url: string, users: table<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/permissions/{id}/"))
  let req_body = {"actions": $actions, "constraints": $constraints, "description": $description, "enabled": $enabled, "groups": $groups, "name": $name, "object_types": $object_types, "users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/permissions/{id}/
#
# operationId: users_permissions_update
export def "users-permissions update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  actions: list<string> # The list of actions granted by this permission
  --constraints: record # Queryset filter matching the applicable objects of the selected type(s) (nullable)
  --description: string
  --enabled: oneof<nothing, bool>
  --groups: list<int>
  name: string
  object_types: list<string>
  --users: list<int>
]: any -> record<actions: list<string>, constraints: record, description: string, display: string, enabled: bool, groups: table<display: string, id: int, name: string, url: string>, id: int, name: string, object_types: list<string>, url: string, users: table<display: string, id: int, url: string, username: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/permissions/{id}/"))
  let req_body = {"actions": $actions, "constraints": $constraints, "description": $description, "enabled": $enabled, "groups": $groups, "name": $name, "object_types": $object_types, "users": $users} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /users/tokens/
#
# operationId: users_tokens_bulk_delete
export def "users-tokens delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/tokens/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/tokens/
#
# operationId: users_tokens_list
export def "users-tokens list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --key: string
  --write-enabled: string
  --description: string
  --q: string
  --user-id: string
  --user: string
  --created: string
  --created-gte: string
  --created-lte: string
  --expires: string
  --expires-gte: string
  --expires-lte: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --key-n: string
  --key-ic: string
  --key-nic: string
  --key-iew: string
  --key-niew: string
  --key-isw: string
  --key-nisw: string
  --key-ie: string
  --key-nie: string
  --key-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --user-id-n: string
  --user-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<allowed_ips: list, created: string, description: string, display: string, expires: string, id: int, key: string, last_used: string, url: string, user: record, write_enabled: bool>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "key" $key "scalar") (serialize-qp "write_enabled" $write_enabled "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "user_id" $user_id "scalar") (serialize-qp "user" $user "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "expires" $expires "scalar") (serialize-qp "expires__gte" $expires_gte "scalar") (serialize-qp "expires__lte" $expires_lte "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "key__n" $key_n "scalar") (serialize-qp "key__ic" $key_ic "scalar") (serialize-qp "key__nic" $key_nic "scalar") (serialize-qp "key__iew" $key_iew "scalar") (serialize-qp "key__niew" $key_niew "scalar") (serialize-qp "key__isw" $key_isw "scalar") (serialize-qp "key__nisw" $key_nisw "scalar") (serialize-qp "key__ie" $key_ie "scalar") (serialize-qp "key__nie" $key_nie "scalar") (serialize-qp "key__empty" $key_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "user_id__n" $user_id_n "scalar") (serialize-qp "user__n" $user_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/users/tokens/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "key": $key, "write_enabled": $write_enabled, "description": $description, "q": $q, "user_id": $user_id, "user": $user, "created": $created, "created__gte": $created_gte, "created__lte": $created_lte, "expires": $expires, "expires__gte": $expires_gte, "expires__lte": $expires_lte, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "key__n": $key_n, "key__ic": $key_ic, "key__nic": $key_nic, "key__iew": $key_iew, "key__niew": $key_niew, "key__isw": $key_isw, "key__nisw": $key_nisw, "key__ie": $key_ie, "key__nie": $key_nie, "key__empty": $key_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "user_id__n": $user_id_n, "user__n": $user_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /users/tokens/
#
# operationId: users_tokens_bulk_partial_update
export def "users-tokens update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allowed-ips: list # default: []
  --description: string
  --expires: string # nullable, format: date-time
  --key: string
  --last-used: string # nullable, format: date-time
  user: int
  --write-enabled: oneof<nothing, bool> # Permit create/update/delete operations using this key
]: any -> record<allowed_ips: list<record>, created: string, description: string, display: string, expires: string, id: int, key: string, last_used: string, url: string, user: record<display: string, id: int, url: string, username: string>, write_enabled: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/tokens/")
  let req_body = {"allowed_ips": $allowed_ips, "description": $description, "expires": $expires, "key": $key, "last_used": $last_used, "user": $user, "write_enabled": $write_enabled} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /users/tokens/
#
# operationId: users_tokens_create
export def "users-tokens create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allowed-ips: list # default: []
  --description: string
  --expires: string # nullable, format: date-time
  --key: string
  --last-used: string # nullable, format: date-time
  user: int
  --write-enabled: oneof<nothing, bool> # Permit create/update/delete operations using this key
]: any -> record<allowed_ips: list<record>, created: string, description: string, display: string, expires: string, id: int, key: string, last_used: string, url: string, user: record<display: string, id: int, url: string, username: string>, write_enabled: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/tokens/")
  let req_body = {"allowed_ips": $allowed_ips, "description": $description, "expires": $expires, "key": $key, "last_used": $last_used, "user": $user, "write_enabled": $write_enabled} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/tokens/
#
# operationId: users_tokens_bulk_update
export def "users-tokens update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allowed-ips: list # default: []
  --description: string
  --expires: string # nullable, format: date-time
  --key: string
  --last-used: string # nullable, format: date-time
  user: int
  --write-enabled: oneof<nothing, bool> # Permit create/update/delete operations using this key
]: any -> record<allowed_ips: list<record>, created: string, description: string, display: string, expires: string, id: int, key: string, last_used: string, url: string, user: record<display: string, id: int, url: string, username: string>, write_enabled: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/tokens/")
  let req_body = {"allowed_ips": $allowed_ips, "description": $description, "expires": $expires, "key": $key, "last_used": $last_used, "user": $user, "write_enabled": $write_enabled} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# Non-authenticated REST API endpoint via which a user may create a Token.
#
# POST /users/tokens/provision/
# operationId: users_tokens_provision_create
export def "users-tokens-provision create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/tokens/provision/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# DELETE /users/tokens/{id}/
#
# operationId: users_tokens_delete
export def "users-tokens delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/tokens/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/tokens/{id}/
#
# operationId: users_tokens_read
export def "users-tokens get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<allowed_ips: list<record>, created: string, description: string, display: string, expires: string, id: int, key: string, last_used: string, url: string, user: record<display: string, id: int, url: string, username: string>, write_enabled: bool> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/tokens/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /users/tokens/{id}/
#
# operationId: users_tokens_partial_update
export def "users-tokens update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allowed-ips: list # default: []
  --description: string
  --expires: string # nullable, format: date-time
  --key: string
  --last-used: string # nullable, format: date-time
  user: int
  --write-enabled: oneof<nothing, bool> # Permit create/update/delete operations using this key
]: any -> record<allowed_ips: list<record>, created: string, description: string, display: string, expires: string, id: int, key: string, last_used: string, url: string, user: record<display: string, id: int, url: string, username: string>, write_enabled: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/tokens/{id}/"))
  let req_body = {"allowed_ips": $allowed_ips, "description": $description, "expires": $expires, "key": $key, "last_used": $last_used, "user": $user, "write_enabled": $write_enabled} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/tokens/{id}/
#
# operationId: users_tokens_update
export def "users-tokens update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --allowed-ips: list # default: []
  --description: string
  --expires: string # nullable, format: date-time
  --key: string
  --last-used: string # nullable, format: date-time
  user: int
  --write-enabled: oneof<nothing, bool> # Permit create/update/delete operations using this key
]: any -> record<allowed_ips: list<record>, created: string, description: string, display: string, expires: string, id: int, key: string, last_used: string, url: string, user: record<display: string, id: int, url: string, username: string>, write_enabled: bool> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/tokens/{id}/"))
  let req_body = {"allowed_ips": $allowed_ips, "description": $description, "expires": $expires, "key": $key, "last_used": $last_used, "user": $user, "write_enabled": $write_enabled} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /users/users/
#
# operationId: users_users_bulk_delete
export def "users-users delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/users/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/users/
#
# operationId: users_users_list
export def "users-users list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --username: string
  --first-name: string
  --last-name: string
  --email: string
  --is-staff: string
  --is-active: string
  --q: string
  --group-id: string
  --group: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --username-n: string
  --username-ic: string
  --username-nic: string
  --username-iew: string
  --username-niew: string
  --username-isw: string
  --username-nisw: string
  --username-ie: string
  --username-nie: string
  --username-empty: string
  --first-name-n: string
  --first-name-ic: string
  --first-name-nic: string
  --first-name-iew: string
  --first-name-niew: string
  --first-name-isw: string
  --first-name-nisw: string
  --first-name-ie: string
  --first-name-nie: string
  --first-name-empty: string
  --last-name-n: string
  --last-name-ic: string
  --last-name-nic: string
  --last-name-iew: string
  --last-name-niew: string
  --last-name-isw: string
  --last-name-nisw: string
  --last-name-ie: string
  --last-name-nie: string
  --last-name-empty: string
  --email-n: string
  --email-ic: string
  --email-nic: string
  --email-iew: string
  --email-niew: string
  --email-isw: string
  --email-nisw: string
  --email-ie: string
  --email-nie: string
  --email-empty: string
  --group-id-n: string
  --group-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<date_joined: string, display: string, email: string, first_name: string, groups: list, id: int, is_active: bool, is_staff: bool, last_name: string, password: string, url: string, username: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "username" $username "scalar") (serialize-qp "first_name" $first_name "scalar") (serialize-qp "last_name" $last_name "scalar") (serialize-qp "email" $email "scalar") (serialize-qp "is_staff" $is_staff "scalar") (serialize-qp "is_active" $is_active "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "username__n" $username_n "scalar") (serialize-qp "username__ic" $username_ic "scalar") (serialize-qp "username__nic" $username_nic "scalar") (serialize-qp "username__iew" $username_iew "scalar") (serialize-qp "username__niew" $username_niew "scalar") (serialize-qp "username__isw" $username_isw "scalar") (serialize-qp "username__nisw" $username_nisw "scalar") (serialize-qp "username__ie" $username_ie "scalar") (serialize-qp "username__nie" $username_nie "scalar") (serialize-qp "username__empty" $username_empty "scalar") (serialize-qp "first_name__n" $first_name_n "scalar") (serialize-qp "first_name__ic" $first_name_ic "scalar") (serialize-qp "first_name__nic" $first_name_nic "scalar") (serialize-qp "first_name__iew" $first_name_iew "scalar") (serialize-qp "first_name__niew" $first_name_niew "scalar") (serialize-qp "first_name__isw" $first_name_isw "scalar") (serialize-qp "first_name__nisw" $first_name_nisw "scalar") (serialize-qp "first_name__ie" $first_name_ie "scalar") (serialize-qp "first_name__nie" $first_name_nie "scalar") (serialize-qp "first_name__empty" $first_name_empty "scalar") (serialize-qp "last_name__n" $last_name_n "scalar") (serialize-qp "last_name__ic" $last_name_ic "scalar") (serialize-qp "last_name__nic" $last_name_nic "scalar") (serialize-qp "last_name__iew" $last_name_iew "scalar") (serialize-qp "last_name__niew" $last_name_niew "scalar") (serialize-qp "last_name__isw" $last_name_isw "scalar") (serialize-qp "last_name__nisw" $last_name_nisw "scalar") (serialize-qp "last_name__ie" $last_name_ie "scalar") (serialize-qp "last_name__nie" $last_name_nie "scalar") (serialize-qp "last_name__empty" $last_name_empty "scalar") (serialize-qp "email__n" $email_n "scalar") (serialize-qp "email__ic" $email_ic "scalar") (serialize-qp "email__nic" $email_nic "scalar") (serialize-qp "email__iew" $email_iew "scalar") (serialize-qp "email__niew" $email_niew "scalar") (serialize-qp "email__isw" $email_isw "scalar") (serialize-qp "email__nisw" $email_nisw "scalar") (serialize-qp "email__ie" $email_ie "scalar") (serialize-qp "email__nie" $email_nie "scalar") (serialize-qp "email__empty" $email_empty "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/users/users/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "username": $username, "first_name": $first_name, "last_name": $last_name, "email": $email, "is_staff": $is_staff, "is_active": $is_active, "q": $q, "group_id": $group_id, "group": $group, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "username__n": $username_n, "username__ic": $username_ic, "username__nic": $username_nic, "username__iew": $username_iew, "username__niew": $username_niew, "username__isw": $username_isw, "username__nisw": $username_nisw, "username__ie": $username_ie, "username__nie": $username_nie, "username__empty": $username_empty, "first_name__n": $first_name_n, "first_name__ic": $first_name_ic, "first_name__nic": $first_name_nic, "first_name__iew": $first_name_iew, "first_name__niew": $first_name_niew, "first_name__isw": $first_name_isw, "first_name__nisw": $first_name_nisw, "first_name__ie": $first_name_ie, "first_name__nie": $first_name_nie, "first_name__empty": $first_name_empty, "last_name__n": $last_name_n, "last_name__ic": $last_name_ic, "last_name__nic": $last_name_nic, "last_name__iew": $last_name_iew, "last_name__niew": $last_name_niew, "last_name__isw": $last_name_isw, "last_name__nisw": $last_name_nisw, "last_name__ie": $last_name_ie, "last_name__nie": $last_name_nie, "last_name__empty": $last_name_empty, "email__n": $email_n, "email__ic": $email_ic, "email__nic": $email_nic, "email__iew": $email_iew, "email__niew": $email_niew, "email__isw": $email_isw, "email__nisw": $email_nisw, "email__ie": $email_ie, "email__nie": $email_nie, "email__empty": $email_empty, "group_id__n": $group_id_n, "group__n": $group_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /users/users/
#
# operationId: users_users_bulk_partial_update
export def "users-users update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --date-joined: string # format: date-time
  --email: string # format: email
  --first-name: string
  --groups: list<int> # The groups this user belongs to. A user will get all permissions granted to each of their groups.
  --is-active: oneof<nothing, bool> # Designates whether this user should be treated as active. Unselect this instead of deleting accounts.
  --is-staff: oneof<nothing, bool> # Designates whether the user can log into this admin site.
  --last-name: string
  password: string
  username: string # Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.
]: any -> record<date_joined: string, display: string, email: string, first_name: string, groups: table<display: string, id: int, name: string, url: string>, id: int, is_active: bool, is_staff: bool, last_name: string, password: string, url: string, username: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/users/")
  let req_body = {"date_joined": $date_joined, "email": $email, "first_name": $first_name, "groups": $groups, "is_active": $is_active, "is_staff": $is_staff, "last_name": $last_name, "password": $password, "username": $username} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /users/users/
#
# operationId: users_users_create
export def "users-users create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --date-joined: string # format: date-time
  --email: string # format: email
  --first-name: string
  --groups: list<int> # The groups this user belongs to. A user will get all permissions granted to each of their groups.
  --is-active: oneof<nothing, bool> # Designates whether this user should be treated as active. Unselect this instead of deleting accounts.
  --is-staff: oneof<nothing, bool> # Designates whether the user can log into this admin site.
  --last-name: string
  password: string
  username: string # Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.
]: any -> record<date_joined: string, display: string, email: string, first_name: string, groups: table<display: string, id: int, name: string, url: string>, id: int, is_active: bool, is_staff: bool, last_name: string, password: string, url: string, username: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/users/")
  let req_body = {"date_joined": $date_joined, "email": $email, "first_name": $first_name, "groups": $groups, "is_active": $is_active, "is_staff": $is_staff, "last_name": $last_name, "password": $password, "username": $username} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/users/
#
# operationId: users_users_bulk_update
export def "users-users update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --date-joined: string # format: date-time
  --email: string # format: email
  --first-name: string
  --groups: list<int> # The groups this user belongs to. A user will get all permissions granted to each of their groups.
  --is-active: oneof<nothing, bool> # Designates whether this user should be treated as active. Unselect this instead of deleting accounts.
  --is-staff: oneof<nothing, bool> # Designates whether the user can log into this admin site.
  --last-name: string
  password: string
  username: string # Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.
]: any -> record<date_joined: string, display: string, email: string, first_name: string, groups: table<display: string, id: int, name: string, url: string>, id: int, is_active: bool, is_staff: bool, last_name: string, password: string, url: string, username: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/users/users/")
  let req_body = {"date_joined": $date_joined, "email": $email, "first_name": $first_name, "groups": $groups, "is_active": $is_active, "is_staff": $is_staff, "last_name": $last_name, "password": $password, "username": $username} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /users/users/{id}/
#
# operationId: users_users_delete
export def "users-users delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/users/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /users/users/{id}/
#
# operationId: users_users_read
export def "users-users get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<date_joined: string, display: string, email: string, first_name: string, groups: table<display: string, id: int, name: string, url: string>, id: int, is_active: bool, is_staff: bool, last_name: string, password: string, url: string, username: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/users/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /users/users/{id}/
#
# operationId: users_users_partial_update
export def "users-users update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --date-joined: string # format: date-time
  --email: string # format: email
  --first-name: string
  --groups: list<int> # The groups this user belongs to. A user will get all permissions granted to each of their groups.
  --is-active: oneof<nothing, bool> # Designates whether this user should be treated as active. Unselect this instead of deleting accounts.
  --is-staff: oneof<nothing, bool> # Designates whether the user can log into this admin site.
  --last-name: string
  password: string
  username: string # Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.
]: any -> record<date_joined: string, display: string, email: string, first_name: string, groups: table<display: string, id: int, name: string, url: string>, id: int, is_active: bool, is_staff: bool, last_name: string, password: string, url: string, username: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/users/{id}/"))
  let req_body = {"date_joined": $date_joined, "email": $email, "first_name": $first_name, "groups": $groups, "is_active": $is_active, "is_staff": $is_staff, "last_name": $last_name, "password": $password, "username": $username} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /users/users/{id}/
#
# operationId: users_users_update
export def "users-users update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --date-joined: string # format: date-time
  --email: string # format: email
  --first-name: string
  --groups: list<int> # The groups this user belongs to. A user will get all permissions granted to each of their groups.
  --is-active: oneof<nothing, bool> # Designates whether this user should be treated as active. Unselect this instead of deleting accounts.
  --is-staff: oneof<nothing, bool> # Designates whether the user can log into this admin site.
  --last-name: string
  password: string
  username: string # Required. 150 characters or fewer. Letters, digits and @/./+/-/_ only.
]: any -> record<date_joined: string, display: string, email: string, first_name: string, groups: table<display: string, id: int, name: string, url: string>, id: int, is_active: bool, is_staff: bool, last_name: string, password: string, url: string, username: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/users/users/{id}/"))
  let req_body = {"date_joined": $date_joined, "email": $email, "first_name": $first_name, "groups": $groups, "is_active": $is_active, "is_staff": $is_staff, "last_name": $last_name, "password": $password, "username": $username} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/cluster-groups/
#
# operationId: virtualization_cluster-groups_bulk_delete
export def "virtualization-cluster-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/cluster-groups/
#
# operationId: virtualization_cluster-groups_list
export def "virtualization-cluster-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/virtualization/cluster-groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /virtualization/cluster-groups/
#
# operationId: virtualization_cluster-groups_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /virtualization/cluster-groups/
#
# operationId: virtualization_cluster-groups_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/cluster-groups/
#
# operationId: virtualization_cluster-groups_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/cluster-groups/{id}/
#
# operationId: virtualization_cluster-groups_delete
export def "virtualization-cluster-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/cluster-groups/{id}/
#
# operationId: virtualization_cluster-groups_read
export def "virtualization-cluster-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /virtualization/cluster-groups/{id}/
#
# operationId: virtualization_cluster-groups_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/cluster-groups/{id}/
#
# operationId: virtualization_cluster-groups_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/cluster-types/
#
# operationId: virtualization_cluster-types_bulk_delete
export def "virtualization-cluster-types delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-types/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/cluster-types/
#
# operationId: virtualization_cluster-types_list
export def "virtualization-cluster-types list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: list, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/virtualization/cluster-types/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /virtualization/cluster-types/
#
# operationId: virtualization_cluster-types_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-types update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-types/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /virtualization/cluster-types/
#
# operationId: virtualization_cluster-types_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-types create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-types/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/cluster-types/
#
# operationId: virtualization_cluster-types_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-types update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/cluster-types/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/cluster-types/{id}/
#
# operationId: virtualization_cluster-types_delete
export def "virtualization-cluster-types delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/cluster-types/{id}/
#
# operationId: virtualization_cluster-types_read
export def "virtualization-cluster-types get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-types/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /virtualization/cluster-types/{id}/
#
# operationId: virtualization_cluster-types_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-types update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-types/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/cluster-types/{id}/
#
# operationId: virtualization_cluster-types_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-cluster-types update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<cluster_count: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/cluster-types/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/clusters/
#
# operationId: virtualization_clusters_bulk_delete
export def "virtualization-clusters delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/clusters/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/clusters/
#
# operationId: virtualization_clusters_list
export def "virtualization-clusters list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --group-id: string
  --group: string
  --type-id: string
  --type: string
  --status: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --group-id-n: string
  --group-n: string
  --type-id-n: string
  --type-n: string
  --status-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record, id: int, last_updated: string, name: string, site: record, status: record, tags: list, tenant: record, type: record, url: string, virtualmachine_count: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "type_id" $type_id "scalar") (serialize-qp "type" $type "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "type_id__n" $type_id_n "scalar") (serialize-qp "type__n" $type_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/virtualization/clusters/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "group_id": $group_id, "group": $group, "type_id": $type_id, "type": $type, "status": $status, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "group_id__n": $group_id_n, "group__n": $group_n, "type_id__n": $type_id_n, "type__n": $type_n, "status__n": $status_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /virtualization/clusters/
#
# operationId: virtualization_clusters_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-clusters update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --site: int # nullable
  --status: string@status-completer-11
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, name: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/clusters/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /virtualization/clusters/
#
# operationId: virtualization_clusters_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-clusters create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --site: int # nullable
  --status: string@status-completer-11
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, name: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/clusters/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/clusters/
#
# operationId: virtualization_clusters_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-clusters update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --site: int # nullable
  --status: string@status-completer-11
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, name: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/clusters/")
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/clusters/{id}/
#
# operationId: virtualization_clusters_delete
export def "virtualization-clusters delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/clusters/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/clusters/{id}/
#
# operationId: virtualization_clusters_read
export def "virtualization-clusters get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, name: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/clusters/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /virtualization/clusters/{id}/
#
# operationId: virtualization_clusters_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-clusters update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --site: int # nullable
  --status: string@status-completer-11
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, name: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/clusters/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/clusters/{id}/
#
# operationId: virtualization_clusters_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-clusters update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  name: string
  --site: int # nullable
  --status: string@status-completer-11
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  type: int
]: any -> record<comments: string, created: string, custom_fields: record, description: string, device_count: int, display: string, group: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, id: int, last_updated: string, name: string, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, type: record<cluster_count: int, display: string, id: int, name: string, slug: string, url: string>, url: string, virtualmachine_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/clusters/{id}/"))
  let req_body = {"comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "name": $name, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "type": $type} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/interfaces/
#
# operationId: virtualization_interfaces_bulk_delete
export def "virtualization-interfaces delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/interfaces/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/interfaces/
#
# operationId: virtualization_interfaces_list
export def "virtualization-interfaces list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --enabled: string
  --mtu: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --cluster-id: string
  --cluster: string
  --virtual-machine-id: string
  --virtual-machine: string
  --parent-id: string
  --bridge-id: string
  --mac-address: string
  --vrf-id: string
  --vrf: string
  --l2vpn-id: string
  --l2vpn: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --mtu-n: string
  --mtu-lte: string
  --mtu-lt: string
  --mtu-gte: string
  --mtu-gt: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --cluster-id-n: string
  --cluster-n: string
  --virtual-machine-id-n: string
  --virtual-machine-n: string
  --parent-id-n: string
  --bridge-id-n: string
  --mac-address-n: string
  --mac-address-ic: string
  --mac-address-nic: string
  --mac-address-iew: string
  --mac-address-niew: string
  --mac-address-isw: string
  --mac-address-nisw: string
  --mac-address-ie: string
  --mac-address-nie: string
  --vrf-id-n: string
  --vrf-n: string
  --l2vpn-id-n: string
  --l2vpn-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<bridge: record, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, display: string, enabled: bool, id: int, l2vpn_termination: record, last_updated: string, mac_address: string, mode: record, mtu: int, name: string, parent: record, tagged_vlans: list, tags: list, untagged_vlan: record, url: string, virtual_machine: record, vrf: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "enabled" $enabled "scalar") (serialize-qp "mtu" $mtu "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "cluster_id" $cluster_id "scalar") (serialize-qp "cluster" $cluster "scalar") (serialize-qp "virtual_machine_id" $virtual_machine_id "scalar") (serialize-qp "virtual_machine" $virtual_machine "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "bridge_id" $bridge_id "scalar") (serialize-qp "mac_address" $mac_address "scalar") (serialize-qp "vrf_id" $vrf_id "scalar") (serialize-qp "vrf" $vrf "scalar") (serialize-qp "l2vpn_id" $l2vpn_id "scalar") (serialize-qp "l2vpn" $l2vpn "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "mtu__n" $mtu_n "scalar") (serialize-qp "mtu__lte" $mtu_lte "scalar") (serialize-qp "mtu__lt" $mtu_lt "scalar") (serialize-qp "mtu__gte" $mtu_gte "scalar") (serialize-qp "mtu__gt" $mtu_gt "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "cluster_id__n" $cluster_id_n "scalar") (serialize-qp "cluster__n" $cluster_n "scalar") (serialize-qp "virtual_machine_id__n" $virtual_machine_id_n "scalar") (serialize-qp "virtual_machine__n" $virtual_machine_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "bridge_id__n" $bridge_id_n "scalar") (serialize-qp "mac_address__n" $mac_address_n "scalar") (serialize-qp "mac_address__ic" $mac_address_ic "scalar") (serialize-qp "mac_address__nic" $mac_address_nic "scalar") (serialize-qp "mac_address__iew" $mac_address_iew "scalar") (serialize-qp "mac_address__niew" $mac_address_niew "scalar") (serialize-qp "mac_address__isw" $mac_address_isw "scalar") (serialize-qp "mac_address__nisw" $mac_address_nisw "scalar") (serialize-qp "mac_address__ie" $mac_address_ie "scalar") (serialize-qp "mac_address__nie" $mac_address_nie "scalar") (serialize-qp "vrf_id__n" $vrf_id_n "scalar") (serialize-qp "vrf__n" $vrf_n "scalar") (serialize-qp "l2vpn_id__n" $l2vpn_id_n "scalar") (serialize-qp "l2vpn__n" $l2vpn_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/virtualization/interfaces/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "enabled": $enabled, "mtu": $mtu, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "cluster_id": $cluster_id, "cluster": $cluster, "virtual_machine_id": $virtual_machine_id, "virtual_machine": $virtual_machine, "parent_id": $parent_id, "bridge_id": $bridge_id, "mac_address": $mac_address, "vrf_id": $vrf_id, "vrf": $vrf, "l2vpn_id": $l2vpn_id, "l2vpn": $l2vpn, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "mtu__n": $mtu_n, "mtu__lte": $mtu_lte, "mtu__lt": $mtu_lt, "mtu__gte": $mtu_gte, "mtu__gt": $mtu_gt, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "cluster_id__n": $cluster_id_n, "cluster__n": $cluster_n, "virtual_machine_id__n": $virtual_machine_id_n, "virtual_machine__n": $virtual_machine_n, "parent_id__n": $parent_id_n, "bridge_id__n": $bridge_id_n, "mac_address__n": $mac_address_n, "mac_address__ic": $mac_address_ic, "mac_address__nic": $mac_address_nic, "mac_address__iew": $mac_address_iew, "mac_address__niew": $mac_address_niew, "mac_address__isw": $mac_address_isw, "mac_address__nisw": $mac_address_nisw, "mac_address__ie": $mac_address_ie, "mac_address__nie": $mac_address_nie, "vrf_id__n": $vrf_id_n, "vrf__n": $vrf_n, "l2vpn_id__n": $l2vpn_id_n, "l2vpn__n": $l2vpn_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /virtualization/interfaces/
#
# operationId: virtualization_interfaces_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-interfaces update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --enabled: oneof<nothing, bool>
  --mac-address: string # nullable
  --mode: string@mode-completer
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --untagged-vlan: int # nullable
  virtual_machine: int
  --vrf: int # nullable
]: any -> record<bridge: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, display: string, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, mac_address: string, mode: record<label: string, value: string>, mtu: int, name: string, parent: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/interfaces/")
  let req_body = {"bridge": $bridge, "custom_fields": $custom_fields, "description": $description, "enabled": $enabled, "mac_address": $mac_address, "mode": $mode, "mtu": $mtu, "name": $name, "parent": $parent, "tagged_vlans": $tagged_vlans, "tags": $tags, "untagged_vlan": $untagged_vlan, "virtual_machine": $virtual_machine, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /virtualization/interfaces/
#
# operationId: virtualization_interfaces_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-interfaces create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --enabled: oneof<nothing, bool>
  --mac-address: string # nullable
  --mode: string@mode-completer
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --untagged-vlan: int # nullable
  virtual_machine: int
  --vrf: int # nullable
]: any -> record<bridge: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, display: string, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, mac_address: string, mode: record<label: string, value: string>, mtu: int, name: string, parent: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/interfaces/")
  let req_body = {"bridge": $bridge, "custom_fields": $custom_fields, "description": $description, "enabled": $enabled, "mac_address": $mac_address, "mode": $mode, "mtu": $mtu, "name": $name, "parent": $parent, "tagged_vlans": $tagged_vlans, "tags": $tags, "untagged_vlan": $untagged_vlan, "virtual_machine": $virtual_machine, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/interfaces/
#
# operationId: virtualization_interfaces_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-interfaces update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --enabled: oneof<nothing, bool>
  --mac-address: string # nullable
  --mode: string@mode-completer
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --untagged-vlan: int # nullable
  virtual_machine: int
  --vrf: int # nullable
]: any -> record<bridge: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, display: string, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, mac_address: string, mode: record<label: string, value: string>, mtu: int, name: string, parent: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/interfaces/")
  let req_body = {"bridge": $bridge, "custom_fields": $custom_fields, "description": $description, "enabled": $enabled, "mac_address": $mac_address, "mode": $mode, "mtu": $mtu, "name": $name, "parent": $parent, "tagged_vlans": $tagged_vlans, "tags": $tags, "untagged_vlan": $untagged_vlan, "virtual_machine": $virtual_machine, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/interfaces/{id}/
#
# operationId: virtualization_interfaces_delete
export def "virtualization-interfaces delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/interfaces/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/interfaces/{id}/
#
# operationId: virtualization_interfaces_read
export def "virtualization-interfaces get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<bridge: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, display: string, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, mac_address: string, mode: record<label: string, value: string>, mtu: int, name: string, parent: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/interfaces/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /virtualization/interfaces/{id}/
#
# operationId: virtualization_interfaces_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-interfaces update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --enabled: oneof<nothing, bool>
  --mac-address: string # nullable
  --mode: string@mode-completer
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --untagged-vlan: int # nullable
  virtual_machine: int
  --vrf: int # nullable
]: any -> record<bridge: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, display: string, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, mac_address: string, mode: record<label: string, value: string>, mtu: int, name: string, parent: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/interfaces/{id}/"))
  let req_body = {"bridge": $bridge, "custom_fields": $custom_fields, "description": $description, "enabled": $enabled, "mac_address": $mac_address, "mode": $mode, "mtu": $mtu, "name": $name, "parent": $parent, "tagged_vlans": $tagged_vlans, "tags": $tags, "untagged_vlan": $untagged_vlan, "virtual_machine": $virtual_machine, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/interfaces/{id}/
#
# operationId: virtualization_interfaces_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-interfaces update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --bridge: int # nullable
  --custom-fields: record # default: {}
  --description: string
  --enabled: oneof<nothing, bool>
  --mac-address: string # nullable
  --mode: string@mode-completer
  --mtu: int # nullable
  name: string
  --parent: int # nullable
  --tagged-vlans: list<int>
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --untagged-vlan: int # nullable
  virtual_machine: int
  --vrf: int # nullable
]: any -> record<bridge: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, count_fhrp_groups: int, count_ipaddresses: int, created: string, custom_fields: record, description: string, display: string, enabled: bool, id: int, l2vpn_termination: record<display: string, id: int, l2vpn: record<display: string, id: int, identifier: int, name: string, slug: string, type: string, url: string>, url: string>, last_updated: string, mac_address: string, mode: record<label: string, value: string>, mtu: int, name: string, parent: record<display: string, id: int, name: string, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>>, tagged_vlans: table<display: string, id: int, name: string, url: string, vid: int>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, untagged_vlan: record<display: string, id: int, name: string, url: string, vid: int>, url: string, virtual_machine: record<display: string, id: int, name: string, url: string>, vrf: record<display: string, id: int, name: string, prefix_count: int, rd: string, url: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/interfaces/{id}/"))
  let req_body = {"bridge": $bridge, "custom_fields": $custom_fields, "description": $description, "enabled": $enabled, "mac_address": $mac_address, "mode": $mode, "mtu": $mtu, "name": $name, "parent": $parent, "tagged_vlans": $tagged_vlans, "tags": $tags, "untagged_vlan": $untagged_vlan, "virtual_machine": $virtual_machine, "vrf": $vrf} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/virtual-machines/
#
# operationId: virtualization_virtual-machines_bulk_delete
export def "virtualization-virtual-machines delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/virtual-machines/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/virtual-machines/
#
# operationId: virtualization_virtual-machines_list
export def "virtualization-virtual-machines list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --cluster: string
  --vcpus: string
  --memory: string
  --disk: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --contact: string
  --contact-role: string
  --contact-group: string
  --local-context-data: string
  --status: string
  --cluster-group-id: string
  --cluster-group: string
  --cluster-type-id: string
  --cluster-type: string
  --cluster-id: string
  --device-id: string
  --device: string
  --region-id: string
  --region: string
  --site-group-id: string
  --site-group: string
  --site-id: string
  --site: string
  --name: string
  --role-id: string
  --role: string
  --platform-id: string
  --platform: string
  --mac-address: string
  --has-primary-ip: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --cluster-n: string
  --vcpus-n: string
  --vcpus-lte: string
  --vcpus-lt: string
  --vcpus-gte: string
  --vcpus-gt: string
  --memory-n: string
  --memory-lte: string
  --memory-lt: string
  --memory-gte: string
  --memory-gt: string
  --disk-n: string
  --disk-lte: string
  --disk-lt: string
  --disk-gte: string
  --disk-gt: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --contact-n: string
  --contact-role-n: string
  --contact-group-n: string
  --status-n: string
  --cluster-group-id-n: string
  --cluster-group-n: string
  --cluster-type-id-n: string
  --cluster-type-n: string
  --cluster-id-n: string
  --device-id-n: string
  --device-n: string
  --region-id-n: string
  --region-n: string
  --site-group-id-n: string
  --site-group-n: string
  --site-id-n: string
  --site-n: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --role-id-n: string
  --role-n: string
  --platform-id-n: string
  --platform-n: string
  --mac-address-n: string
  --mac-address-ic: string
  --mac-address-nic: string
  --mac-address-iew: string
  --mac-address-niew: string
  --mac-address-isw: string
  --mac-address-nisw: string
  --mac-address-ie: string
  --mac-address-nie: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<cluster: record, comments: string, config_context: record, created: string, custom_fields: record, description: string, device: record, disk: int, display: string, id: int, last_updated: string, local_context_data: record, memory: int, name: string, platform: record, primary_ip: record, primary_ip4: record, primary_ip6: record, role: record, site: record, status: record, tags: list, tenant: record, url: string, vcpus: float>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "cluster" $cluster "scalar") (serialize-qp "vcpus" $vcpus "scalar") (serialize-qp "memory" $memory "scalar") (serialize-qp "disk" $disk "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "contact" $contact "scalar") (serialize-qp "contact_role" $contact_role "scalar") (serialize-qp "contact_group" $contact_group "scalar") (serialize-qp "local_context_data" $local_context_data "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "cluster_group_id" $cluster_group_id "scalar") (serialize-qp "cluster_group" $cluster_group "scalar") (serialize-qp "cluster_type_id" $cluster_type_id "scalar") (serialize-qp "cluster_type" $cluster_type "scalar") (serialize-qp "cluster_id" $cluster_id "scalar") (serialize-qp "device_id" $device_id "scalar") (serialize-qp "device" $device "scalar") (serialize-qp "region_id" $region_id "scalar") (serialize-qp "region" $region "scalar") (serialize-qp "site_group_id" $site_group_id "scalar") (serialize-qp "site_group" $site_group "scalar") (serialize-qp "site_id" $site_id "scalar") (serialize-qp "site" $site "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "role_id" $role_id "scalar") (serialize-qp "role" $role "scalar") (serialize-qp "platform_id" $platform_id "scalar") (serialize-qp "platform" $platform "scalar") (serialize-qp "mac_address" $mac_address "scalar") (serialize-qp "has_primary_ip" $has_primary_ip "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "cluster__n" $cluster_n "scalar") (serialize-qp "vcpus__n" $vcpus_n "scalar") (serialize-qp "vcpus__lte" $vcpus_lte "scalar") (serialize-qp "vcpus__lt" $vcpus_lt "scalar") (serialize-qp "vcpus__gte" $vcpus_gte "scalar") (serialize-qp "vcpus__gt" $vcpus_gt "scalar") (serialize-qp "memory__n" $memory_n "scalar") (serialize-qp "memory__lte" $memory_lte "scalar") (serialize-qp "memory__lt" $memory_lt "scalar") (serialize-qp "memory__gte" $memory_gte "scalar") (serialize-qp "memory__gt" $memory_gt "scalar") (serialize-qp "disk__n" $disk_n "scalar") (serialize-qp "disk__lte" $disk_lte "scalar") (serialize-qp "disk__lt" $disk_lt "scalar") (serialize-qp "disk__gte" $disk_gte "scalar") (serialize-qp "disk__gt" $disk_gt "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "contact__n" $contact_n "scalar") (serialize-qp "contact_role__n" $contact_role_n "scalar") (serialize-qp "contact_group__n" $contact_group_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "cluster_group_id__n" $cluster_group_id_n "scalar") (serialize-qp "cluster_group__n" $cluster_group_n "scalar") (serialize-qp "cluster_type_id__n" $cluster_type_id_n "scalar") (serialize-qp "cluster_type__n" $cluster_type_n "scalar") (serialize-qp "cluster_id__n" $cluster_id_n "scalar") (serialize-qp "device_id__n" $device_id_n "scalar") (serialize-qp "device__n" $device_n "scalar") (serialize-qp "region_id__n" $region_id_n "scalar") (serialize-qp "region__n" $region_n "scalar") (serialize-qp "site_group_id__n" $site_group_id_n "scalar") (serialize-qp "site_group__n" $site_group_n "scalar") (serialize-qp "site_id__n" $site_id_n "scalar") (serialize-qp "site__n" $site_n "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "role_id__n" $role_id_n "scalar") (serialize-qp "role__n" $role_n "scalar") (serialize-qp "platform_id__n" $platform_id_n "scalar") (serialize-qp "platform__n" $platform_n "scalar") (serialize-qp "mac_address__n" $mac_address_n "scalar") (serialize-qp "mac_address__ic" $mac_address_ic "scalar") (serialize-qp "mac_address__nic" $mac_address_nic "scalar") (serialize-qp "mac_address__iew" $mac_address_iew "scalar") (serialize-qp "mac_address__niew" $mac_address_niew "scalar") (serialize-qp "mac_address__isw" $mac_address_isw "scalar") (serialize-qp "mac_address__nisw" $mac_address_nisw "scalar") (serialize-qp "mac_address__ie" $mac_address_ie "scalar") (serialize-qp "mac_address__nie" $mac_address_nie "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/virtualization/virtual-machines/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "cluster": $cluster, "vcpus": $vcpus, "memory": $memory, "disk": $disk, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "contact": $contact, "contact_role": $contact_role, "contact_group": $contact_group, "local_context_data": $local_context_data, "status": $status, "cluster_group_id": $cluster_group_id, "cluster_group": $cluster_group, "cluster_type_id": $cluster_type_id, "cluster_type": $cluster_type, "cluster_id": $cluster_id, "device_id": $device_id, "device": $device, "region_id": $region_id, "region": $region, "site_group_id": $site_group_id, "site_group": $site_group, "site_id": $site_id, "site": $site, "name": $name, "role_id": $role_id, "role": $role, "platform_id": $platform_id, "platform": $platform, "mac_address": $mac_address, "has_primary_ip": $has_primary_ip, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "cluster__n": $cluster_n, "vcpus__n": $vcpus_n, "vcpus__lte": $vcpus_lte, "vcpus__lt": $vcpus_lt, "vcpus__gte": $vcpus_gte, "vcpus__gt": $vcpus_gt, "memory__n": $memory_n, "memory__lte": $memory_lte, "memory__lt": $memory_lt, "memory__gte": $memory_gte, "memory__gt": $memory_gt, "disk__n": $disk_n, "disk__lte": $disk_lte, "disk__lt": $disk_lt, "disk__gte": $disk_gte, "disk__gt": $disk_gt, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "contact__n": $contact_n, "contact_role__n": $contact_role_n, "contact_group__n": $contact_group_n, "status__n": $status_n, "cluster_group_id__n": $cluster_group_id_n, "cluster_group__n": $cluster_group_n, "cluster_type_id__n": $cluster_type_id_n, "cluster_type__n": $cluster_type_n, "cluster_id__n": $cluster_id_n, "device_id__n": $device_id_n, "device__n": $device_n, "region_id__n": $region_id_n, "region__n": $region_n, "site_group_id__n": $site_group_id_n, "site_group__n": $site_group_n, "site_id__n": $site_id_n, "site__n": $site_n, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "role_id__n": $role_id_n, "role__n": $role_n, "platform_id__n": $platform_id_n, "platform__n": $platform_n, "mac_address__n": $mac_address_n, "mac_address__ic": $mac_address_ic, "mac_address__nic": $mac_address_nic, "mac_address__iew": $mac_address_iew, "mac_address__niew": $mac_address_niew, "mac_address__isw": $mac_address_isw, "mac_address__nisw": $mac_address_nisw, "mac_address__ie": $mac_address_ie, "mac_address__nie": $mac_address_nie, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /virtualization/virtual-machines/
#
# operationId: virtualization_virtual-machines_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-virtual-machines update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --disk: int # nullable
  --local-context-data: record # nullable
  --memory: int # nullable
  name: string
  --platform: int # nullable
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vcpus: float # nullable, format: decimal
]: any -> record<cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, disk: int, display: string, id: int, last_updated: string, local_context_data: record, memory: int, name: string, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vcpus: float> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/virtual-machines/")
  let req_body = {"cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "disk": $disk, "local_context_data": $local_context_data, "memory": $memory, "name": $name, "platform": $platform, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vcpus": $vcpus} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /virtualization/virtual-machines/
#
# operationId: virtualization_virtual-machines_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-virtual-machines create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --disk: int # nullable
  --local-context-data: record # nullable
  --memory: int # nullable
  name: string
  --platform: int # nullable
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vcpus: float # nullable, format: decimal
]: any -> record<cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, disk: int, display: string, id: int, last_updated: string, local_context_data: record, memory: int, name: string, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vcpus: float> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/virtual-machines/")
  let req_body = {"cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "disk": $disk, "local_context_data": $local_context_data, "memory": $memory, "name": $name, "platform": $platform, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vcpus": $vcpus} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/virtual-machines/
#
# operationId: virtualization_virtual-machines_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-virtual-machines update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --disk: int # nullable
  --local-context-data: record # nullable
  --memory: int # nullable
  name: string
  --platform: int # nullable
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vcpus: float # nullable, format: decimal
]: any -> record<cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, disk: int, display: string, id: int, last_updated: string, local_context_data: record, memory: int, name: string, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vcpus: float> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/virtualization/virtual-machines/")
  let req_body = {"cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "disk": $disk, "local_context_data": $local_context_data, "memory": $memory, "name": $name, "platform": $platform, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vcpus": $vcpus} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /virtualization/virtual-machines/{id}/
#
# operationId: virtualization_virtual-machines_delete
export def "virtualization-virtual-machines delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/virtual-machines/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /virtualization/virtual-machines/{id}/
#
# operationId: virtualization_virtual-machines_read
export def "virtualization-virtual-machines get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, disk: int, display: string, id: int, last_updated: string, local_context_data: record, memory: int, name: string, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vcpus: float> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/virtual-machines/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /virtualization/virtual-machines/{id}/
#
# operationId: virtualization_virtual-machines_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-virtual-machines update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --disk: int # nullable
  --local-context-data: record # nullable
  --memory: int # nullable
  name: string
  --platform: int # nullable
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vcpus: float # nullable, format: decimal
]: any -> record<cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, disk: int, display: string, id: int, last_updated: string, local_context_data: record, memory: int, name: string, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vcpus: float> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/virtual-machines/{id}/"))
  let req_body = {"cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "disk": $disk, "local_context_data": $local_context_data, "memory": $memory, "name": $name, "platform": $platform, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vcpus": $vcpus} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /virtualization/virtual-machines/{id}/
#
# operationId: virtualization_virtual-machines_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "virtualization-virtual-machines update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --cluster: int # nullable
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --device: int # nullable
  --disk: int # nullable
  --local-context-data: record # nullable
  --memory: int # nullable
  name: string
  --platform: int # nullable
  --primary-ip4: int # nullable
  --primary-ip6: int # nullable
  --role: int # nullable
  --site: int # nullable
  --status: string@status-completer-4
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vcpus: float # nullable, format: decimal
]: any -> record<cluster: record<display: string, id: int, name: string, url: string, virtualmachine_count: int>, comments: string, config_context: record, created: string, custom_fields: record, description: string, device: record<display: string, id: int, name: string, url: string>, disk: int, display: string, id: int, last_updated: string, local_context_data: record, memory: int, name: string, platform: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, primary_ip: record<address: string, display: string, family: int, id: int, url: string>, primary_ip4: record<address: string, display: string, family: int, id: int, url: string>, primary_ip6: record<address: string, display: string, family: int, id: int, url: string>, role: record<device_count: int, display: string, id: int, name: string, slug: string, url: string, virtualmachine_count: int>, site: record<display: string, id: int, name: string, slug: string, url: string>, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vcpus: float> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/virtualization/virtual-machines/{id}/"))
  let req_body = {"cluster": $cluster, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "device": $device, "disk": $disk, "local_context_data": $local_context_data, "memory": $memory, "name": $name, "platform": $platform, "primary_ip4": $primary_ip4, "primary_ip6": $primary_ip6, "role": $role, "site": $site, "status": $status, "tags": $tags, "tenant": $tenant, "vcpus": $vcpus} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /wireless/wireless-lan-groups/
#
# operationId: wireless_wireless-lan-groups_bulk_delete
export def "wireless-wireless-lan-groups delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lan-groups/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /wireless/wireless-lan-groups/
#
# operationId: wireless_wireless-lan-groups_list
export def "wireless-wireless-lan-groups list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --name: string
  --slug: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --parent-id: string
  --parent: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --name-n: string
  --name-ic: string
  --name-nic: string
  --name-iew: string
  --name-niew: string
  --name-isw: string
  --name-nisw: string
  --name-ie: string
  --name-nie: string
  --name-empty: string
  --slug-n: string
  --slug-ic: string
  --slug-nic: string
  --slug-iew: string
  --slug-niew: string
  --slug-isw: string
  --slug-nisw: string
  --slug-ie: string
  --slug-nie: string
  --slug-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --parent-id-n: string
  --parent-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record, slug: string, tags: list, url: string, wirelesslan_count: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "name" $name "scalar") (serialize-qp "slug" $slug "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "parent_id" $parent_id "scalar") (serialize-qp "parent" $parent "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "name__n" $name_n "scalar") (serialize-qp "name__ic" $name_ic "scalar") (serialize-qp "name__nic" $name_nic "scalar") (serialize-qp "name__iew" $name_iew "scalar") (serialize-qp "name__niew" $name_niew "scalar") (serialize-qp "name__isw" $name_isw "scalar") (serialize-qp "name__nisw" $name_nisw "scalar") (serialize-qp "name__ie" $name_ie "scalar") (serialize-qp "name__nie" $name_nie "scalar") (serialize-qp "name__empty" $name_empty "scalar") (serialize-qp "slug__n" $slug_n "scalar") (serialize-qp "slug__ic" $slug_ic "scalar") (serialize-qp "slug__nic" $slug_nic "scalar") (serialize-qp "slug__iew" $slug_iew "scalar") (serialize-qp "slug__niew" $slug_niew "scalar") (serialize-qp "slug__isw" $slug_isw "scalar") (serialize-qp "slug__nisw" $slug_nisw "scalar") (serialize-qp "slug__ie" $slug_ie "scalar") (serialize-qp "slug__nie" $slug_nie "scalar") (serialize-qp "slug__empty" $slug_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "parent_id__n" $parent_id_n "scalar") (serialize-qp "parent__n" $parent_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/wireless/wireless-lan-groups/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "name": $name, "slug": $slug, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "parent_id": $parent_id, "parent": $parent, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "name__n": $name_n, "name__ic": $name_ic, "name__nic": $name_nic, "name__iew": $name_iew, "name__niew": $name_niew, "name__isw": $name_isw, "name__nisw": $name_nisw, "name__ie": $name_ie, "name__nie": $name_nie, "name__empty": $name_empty, "slug__n": $slug_n, "slug__ic": $slug_ic, "slug__nic": $slug_nic, "slug__iew": $slug_iew, "slug__niew": $slug_niew, "slug__isw": $slug_isw, "slug__nisw": $slug_nisw, "slug__ie": $slug_ie, "slug__nie": $slug_nie, "slug__empty": $slug_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "parent_id__n": $parent_id_n, "parent__n": $parent_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /wireless/wireless-lan-groups/
#
# operationId: wireless_wireless-lan-groups_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lan-groups update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, wirelesslan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lan-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /wireless/wireless-lan-groups/
#
# operationId: wireless_wireless-lan-groups_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lan-groups create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, wirelesslan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lan-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /wireless/wireless-lan-groups/
#
# operationId: wireless_wireless-lan-groups_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lan-groups update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, wirelesslan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lan-groups/")
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /wireless/wireless-lan-groups/{id}/
#
# operationId: wireless_wireless-lan-groups_delete
export def "wireless-wireless-lan-groups delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lan-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /wireless/wireless-lan-groups/{id}/
#
# operationId: wireless_wireless-lan-groups_read
export def "wireless-wireless-lan-groups get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, wirelesslan_count: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lan-groups/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /wireless/wireless-lan-groups/{id}/
#
# operationId: wireless_wireless-lan-groups_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lan-groups update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, wirelesslan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lan-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /wireless/wireless-lan-groups/{id}/
#
# operationId: wireless_wireless-lan-groups_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lan-groups update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --custom-fields: record # default: {}
  --description: string
  name: string
  --parent: int # nullable
  slug: string # format: slug
  --tags: list # item shape: {color?: string, name: string, slug: string}
]: any -> record<_depth: int, created: string, custom_fields: record, description: string, display: string, id: int, last_updated: string, name: string, parent: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, slug: string, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, url: string, wirelesslan_count: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lan-groups/{id}/"))
  let req_body = {"custom_fields": $custom_fields, "description": $description, "name": $name, "parent": $parent, "slug": $slug, "tags": $tags} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /wireless/wireless-lans/
#
# operationId: wireless_wireless-lans_bulk_delete
export def "wireless-wireless-lans delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lans/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /wireless/wireless-lans/
#
# operationId: wireless_wireless-lans_list
export def "wireless-wireless-lans list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --ssid: string
  --auth-psk: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --group-id: string
  --group: string
  --status: string
  --vlan-id: string
  --auth-type: string
  --auth-cipher: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --ssid-n: string
  --ssid-ic: string
  --ssid-nic: string
  --ssid-iew: string
  --ssid-niew: string
  --ssid-isw: string
  --ssid-nisw: string
  --ssid-ie: string
  --ssid-nie: string
  --ssid-empty: string
  --auth-psk-n: string
  --auth-psk-ic: string
  --auth-psk-nic: string
  --auth-psk-iew: string
  --auth-psk-niew: string
  --auth-psk-isw: string
  --auth-psk-nisw: string
  --auth-psk-ie: string
  --auth-psk-nie: string
  --auth-psk-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --group-id-n: string
  --group-n: string
  --status-n: string
  --vlan-id-n: string
  --auth-type-n: string
  --auth-cipher-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<auth_cipher: record, auth_psk: string, auth_type: record, comments: string, created: string, custom_fields: record, description: string, display: string, group: record, id: int, last_updated: string, ssid: string, status: record, tags: list, tenant: record, url: string, vlan: record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "ssid" $ssid "scalar") (serialize-qp "auth_psk" $auth_psk "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "group_id" $group_id "scalar") (serialize-qp "group" $group "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "vlan_id" $vlan_id "scalar") (serialize-qp "auth_type" $auth_type "scalar") (serialize-qp "auth_cipher" $auth_cipher "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "ssid__n" $ssid_n "scalar") (serialize-qp "ssid__ic" $ssid_ic "scalar") (serialize-qp "ssid__nic" $ssid_nic "scalar") (serialize-qp "ssid__iew" $ssid_iew "scalar") (serialize-qp "ssid__niew" $ssid_niew "scalar") (serialize-qp "ssid__isw" $ssid_isw "scalar") (serialize-qp "ssid__nisw" $ssid_nisw "scalar") (serialize-qp "ssid__ie" $ssid_ie "scalar") (serialize-qp "ssid__nie" $ssid_nie "scalar") (serialize-qp "ssid__empty" $ssid_empty "scalar") (serialize-qp "auth_psk__n" $auth_psk_n "scalar") (serialize-qp "auth_psk__ic" $auth_psk_ic "scalar") (serialize-qp "auth_psk__nic" $auth_psk_nic "scalar") (serialize-qp "auth_psk__iew" $auth_psk_iew "scalar") (serialize-qp "auth_psk__niew" $auth_psk_niew "scalar") (serialize-qp "auth_psk__isw" $auth_psk_isw "scalar") (serialize-qp "auth_psk__nisw" $auth_psk_nisw "scalar") (serialize-qp "auth_psk__ie" $auth_psk_ie "scalar") (serialize-qp "auth_psk__nie" $auth_psk_nie "scalar") (serialize-qp "auth_psk__empty" $auth_psk_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "group_id__n" $group_id_n "scalar") (serialize-qp "group__n" $group_n "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "vlan_id__n" $vlan_id_n "scalar") (serialize-qp "auth_type__n" $auth_type_n "scalar") (serialize-qp "auth_cipher__n" $auth_cipher_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/wireless/wireless-lans/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "ssid": $ssid, "auth_psk": $auth_psk, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "group_id": $group_id, "group": $group, "status": $status, "vlan_id": $vlan_id, "auth_type": $auth_type, "auth_cipher": $auth_cipher, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "ssid__n": $ssid_n, "ssid__ic": $ssid_ic, "ssid__nic": $ssid_nic, "ssid__iew": $ssid_iew, "ssid__niew": $ssid_niew, "ssid__isw": $ssid_isw, "ssid__nisw": $ssid_nisw, "ssid__ie": $ssid_ie, "ssid__nie": $ssid_nie, "ssid__empty": $ssid_empty, "auth_psk__n": $auth_psk_n, "auth_psk__ic": $auth_psk_ic, "auth_psk__nic": $auth_psk_nic, "auth_psk__iew": $auth_psk_iew, "auth_psk__niew": $auth_psk_niew, "auth_psk__isw": $auth_psk_isw, "auth_psk__nisw": $auth_psk_nisw, "auth_psk__ie": $auth_psk_ie, "auth_psk__nie": $auth_psk_nie, "auth_psk__empty": $auth_psk_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "group_id__n": $group_id_n, "group__n": $group_n, "status__n": $status_n, "vlan_id__n": $vlan_id_n, "auth_type__n": $auth_type_n, "auth_cipher__n": $auth_cipher_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /wireless/wireless-lans/
#
# operationId: wireless_wireless-lans_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lans update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  ssid: string
  --status: string@status-completer-12
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, id: int, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lans/")
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /wireless/wireless-lans/
#
# operationId: wireless_wireless-lans_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lans create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  ssid: string
  --status: string@status-completer-12
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, id: int, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lans/")
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /wireless/wireless-lans/
#
# operationId: wireless_wireless-lans_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lans update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  ssid: string
  --status: string@status-completer-12
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, id: int, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-lans/")
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /wireless/wireless-lans/{id}/
#
# operationId: wireless_wireless-lans_delete
export def "wireless-wireless-lans delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lans/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /wireless/wireless-lans/{id}/
#
# operationId: wireless_wireless-lans_read
export def "wireless-wireless-lans get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, id: int, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lans/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /wireless/wireless-lans/{id}/
#
# operationId: wireless_wireless-lans_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lans update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  ssid: string
  --status: string@status-completer-12
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, id: int, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lans/{id}/"))
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /wireless/wireless-lans/{id}/
#
# operationId: wireless_wireless-lans_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-lans update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  --group: int # nullable
  ssid: string
  --status: string@status-completer-12
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
  --vlan: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, group: record<_depth: int, display: string, id: int, name: string, slug: string, url: string, wirelesslan_count: int>, id: int, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string, vlan: record<display: string, id: int, name: string, url: string, vid: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-lans/{id}/"))
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "group": $group, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant, "vlan": $vlan} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /wireless/wireless-links/
#
# operationId: wireless_wireless-links_bulk_delete
export def "wireless-wireless-links delete-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-links/")
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /wireless/wireless-links/
#
# operationId: wireless_wireless-links_list
export def "wireless-wireless-links list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --id: string
  --ssid: string
  --auth-psk: string
  --description: string
  --created: string
  --last-updated: string
  --q: string
  --tag: string
  --tenant-group-id: string
  --tenant-group: string
  --tenant-id: string
  --tenant: string
  --interface-a-id: string
  --interface-b-id: string
  --status: string
  --auth-type: string
  --auth-cipher: string
  --id-n: string
  --id-lte: string
  --id-lt: string
  --id-gte: string
  --id-gt: string
  --ssid-n: string
  --ssid-ic: string
  --ssid-nic: string
  --ssid-iew: string
  --ssid-niew: string
  --ssid-isw: string
  --ssid-nisw: string
  --ssid-ie: string
  --ssid-nie: string
  --ssid-empty: string
  --auth-psk-n: string
  --auth-psk-ic: string
  --auth-psk-nic: string
  --auth-psk-iew: string
  --auth-psk-niew: string
  --auth-psk-isw: string
  --auth-psk-nisw: string
  --auth-psk-ie: string
  --auth-psk-nie: string
  --auth-psk-empty: string
  --description-n: string
  --description-ic: string
  --description-nic: string
  --description-iew: string
  --description-niew: string
  --description-isw: string
  --description-nisw: string
  --description-ie: string
  --description-nie: string
  --description-empty: string
  --created-n: string
  --created-lte: string
  --created-lt: string
  --created-gte: string
  --created-gt: string
  --last-updated-n: string
  --last-updated-lte: string
  --last-updated-lt: string
  --last-updated-gte: string
  --last-updated-gt: string
  --tag-n: string
  --tenant-group-id-n: string
  --tenant-group-n: string
  --tenant-id-n: string
  --tenant-n: string
  --interface-a-id-n: string
  --interface-a-id-lte: string
  --interface-a-id-lt: string
  --interface-a-id-gte: string
  --interface-a-id-gt: string
  --interface-b-id-n: string
  --interface-b-id-lte: string
  --interface-b-id-lt: string
  --interface-b-id-gte: string
  --interface-b-id-gt: string
  --status-n: string
  --auth-type-n: string
  --auth-cipher-n: string
  --ordering: string # Which field to use when ordering the results.
  --limit: int # Number of results to return per page.
  --offset: int # The initial index from which to return the results.
]: nothing -> record<count: int, next: string, previous: string, results: table<auth_cipher: record, auth_psk: string, auth_type: record, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, interface_a: record, interface_b: record, last_updated: string, ssid: string, status: record, tags: list, tenant: record, url: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let qp = [(serialize-qp "id" $id "scalar") (serialize-qp "ssid" $ssid "scalar") (serialize-qp "auth_psk" $auth_psk "scalar") (serialize-qp "description" $description "scalar") (serialize-qp "created" $created "scalar") (serialize-qp "last_updated" $last_updated "scalar") (serialize-qp "q" $q "scalar") (serialize-qp "tag" $tag "scalar") (serialize-qp "tenant_group_id" $tenant_group_id "scalar") (serialize-qp "tenant_group" $tenant_group "scalar") (serialize-qp "tenant_id" $tenant_id "scalar") (serialize-qp "tenant" $tenant "scalar") (serialize-qp "interface_a_id" $interface_a_id "scalar") (serialize-qp "interface_b_id" $interface_b_id "scalar") (serialize-qp "status" $status "scalar") (serialize-qp "auth_type" $auth_type "scalar") (serialize-qp "auth_cipher" $auth_cipher "scalar") (serialize-qp "id__n" $id_n "scalar") (serialize-qp "id__lte" $id_lte "scalar") (serialize-qp "id__lt" $id_lt "scalar") (serialize-qp "id__gte" $id_gte "scalar") (serialize-qp "id__gt" $id_gt "scalar") (serialize-qp "ssid__n" $ssid_n "scalar") (serialize-qp "ssid__ic" $ssid_ic "scalar") (serialize-qp "ssid__nic" $ssid_nic "scalar") (serialize-qp "ssid__iew" $ssid_iew "scalar") (serialize-qp "ssid__niew" $ssid_niew "scalar") (serialize-qp "ssid__isw" $ssid_isw "scalar") (serialize-qp "ssid__nisw" $ssid_nisw "scalar") (serialize-qp "ssid__ie" $ssid_ie "scalar") (serialize-qp "ssid__nie" $ssid_nie "scalar") (serialize-qp "ssid__empty" $ssid_empty "scalar") (serialize-qp "auth_psk__n" $auth_psk_n "scalar") (serialize-qp "auth_psk__ic" $auth_psk_ic "scalar") (serialize-qp "auth_psk__nic" $auth_psk_nic "scalar") (serialize-qp "auth_psk__iew" $auth_psk_iew "scalar") (serialize-qp "auth_psk__niew" $auth_psk_niew "scalar") (serialize-qp "auth_psk__isw" $auth_psk_isw "scalar") (serialize-qp "auth_psk__nisw" $auth_psk_nisw "scalar") (serialize-qp "auth_psk__ie" $auth_psk_ie "scalar") (serialize-qp "auth_psk__nie" $auth_psk_nie "scalar") (serialize-qp "auth_psk__empty" $auth_psk_empty "scalar") (serialize-qp "description__n" $description_n "scalar") (serialize-qp "description__ic" $description_ic "scalar") (serialize-qp "description__nic" $description_nic "scalar") (serialize-qp "description__iew" $description_iew "scalar") (serialize-qp "description__niew" $description_niew "scalar") (serialize-qp "description__isw" $description_isw "scalar") (serialize-qp "description__nisw" $description_nisw "scalar") (serialize-qp "description__ie" $description_ie "scalar") (serialize-qp "description__nie" $description_nie "scalar") (serialize-qp "description__empty" $description_empty "scalar") (serialize-qp "created__n" $created_n "scalar") (serialize-qp "created__lte" $created_lte "scalar") (serialize-qp "created__lt" $created_lt "scalar") (serialize-qp "created__gte" $created_gte "scalar") (serialize-qp "created__gt" $created_gt "scalar") (serialize-qp "last_updated__n" $last_updated_n "scalar") (serialize-qp "last_updated__lte" $last_updated_lte "scalar") (serialize-qp "last_updated__lt" $last_updated_lt "scalar") (serialize-qp "last_updated__gte" $last_updated_gte "scalar") (serialize-qp "last_updated__gt" $last_updated_gt "scalar") (serialize-qp "tag__n" $tag_n "scalar") (serialize-qp "tenant_group_id__n" $tenant_group_id_n "scalar") (serialize-qp "tenant_group__n" $tenant_group_n "scalar") (serialize-qp "tenant_id__n" $tenant_id_n "scalar") (serialize-qp "tenant__n" $tenant_n "scalar") (serialize-qp "interface_a_id__n" $interface_a_id_n "scalar") (serialize-qp "interface_a_id__lte" $interface_a_id_lte "scalar") (serialize-qp "interface_a_id__lt" $interface_a_id_lt "scalar") (serialize-qp "interface_a_id__gte" $interface_a_id_gte "scalar") (serialize-qp "interface_a_id__gt" $interface_a_id_gt "scalar") (serialize-qp "interface_b_id__n" $interface_b_id_n "scalar") (serialize-qp "interface_b_id__lte" $interface_b_id_lte "scalar") (serialize-qp "interface_b_id__lt" $interface_b_id_lt "scalar") (serialize-qp "interface_b_id__gte" $interface_b_id_gte "scalar") (serialize-qp "interface_b_id__gt" $interface_b_id_gt "scalar") (serialize-qp "status__n" $status_n "scalar") (serialize-qp "auth_type__n" $auth_type_n "scalar") (serialize-qp "auth_cipher__n" $auth_cipher_n "scalar") (serialize-qp "ordering" $ordering "scalar") (serialize-qp "limit" $limit "scalar") (serialize-qp "offset" $offset "scalar")] | flatten | str join "&"
  let full_url = (build-url $base "/wireless/wireless-links/" $qp)
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: ({"id": $id, "ssid": $ssid, "auth_psk": $auth_psk, "description": $description, "created": $created, "last_updated": $last_updated, "q": $q, "tag": $tag, "tenant_group_id": $tenant_group_id, "tenant_group": $tenant_group, "tenant_id": $tenant_id, "tenant": $tenant, "interface_a_id": $interface_a_id, "interface_b_id": $interface_b_id, "status": $status, "auth_type": $auth_type, "auth_cipher": $auth_cipher, "id__n": $id_n, "id__lte": $id_lte, "id__lt": $id_lt, "id__gte": $id_gte, "id__gt": $id_gt, "ssid__n": $ssid_n, "ssid__ic": $ssid_ic, "ssid__nic": $ssid_nic, "ssid__iew": $ssid_iew, "ssid__niew": $ssid_niew, "ssid__isw": $ssid_isw, "ssid__nisw": $ssid_nisw, "ssid__ie": $ssid_ie, "ssid__nie": $ssid_nie, "ssid__empty": $ssid_empty, "auth_psk__n": $auth_psk_n, "auth_psk__ic": $auth_psk_ic, "auth_psk__nic": $auth_psk_nic, "auth_psk__iew": $auth_psk_iew, "auth_psk__niew": $auth_psk_niew, "auth_psk__isw": $auth_psk_isw, "auth_psk__nisw": $auth_psk_nisw, "auth_psk__ie": $auth_psk_ie, "auth_psk__nie": $auth_psk_nie, "auth_psk__empty": $auth_psk_empty, "description__n": $description_n, "description__ic": $description_ic, "description__nic": $description_nic, "description__iew": $description_iew, "description__niew": $description_niew, "description__isw": $description_isw, "description__nisw": $description_nisw, "description__ie": $description_ie, "description__nie": $description_nie, "description__empty": $description_empty, "created__n": $created_n, "created__lte": $created_lte, "created__lt": $created_lt, "created__gte": $created_gte, "created__gt": $created_gt, "last_updated__n": $last_updated_n, "last_updated__lte": $last_updated_lte, "last_updated__lt": $last_updated_lt, "last_updated__gte": $last_updated_gte, "last_updated__gt": $last_updated_gt, "tag__n": $tag_n, "tenant_group_id__n": $tenant_group_id_n, "tenant_group__n": $tenant_group_n, "tenant_id__n": $tenant_id_n, "tenant__n": $tenant_n, "interface_a_id__n": $interface_a_id_n, "interface_a_id__lte": $interface_a_id_lte, "interface_a_id__lt": $interface_a_id_lt, "interface_a_id__gte": $interface_a_id_gte, "interface_a_id__gt": $interface_a_id_gt, "interface_b_id__n": $interface_b_id_n, "interface_b_id__lte": $interface_b_id_lte, "interface_b_id__lt": $interface_b_id_lt, "interface_b_id__gte": $interface_b_id_gte, "interface_b_id__gt": $interface_b_id_gt, "status__n": $status_n, "auth_type__n": $auth_type_n, "auth_cipher__n": $auth_cipher_n, "ordering": $ordering, "limit": $limit, "offset": $offset} | compact), body: null}
}

# PATCH /wireless/wireless-links/
#
# operationId: wireless_wireless-links_bulk_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-links update-bulk" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  interface_a: int
  interface_b: int
  --ssid: string
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, interface_a: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, interface_b: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-links/")
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "interface_a": $interface_a, "interface_b": $interface_b, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# POST /wireless/wireless-links/
#
# operationId: wireless_wireless-links_create
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-links create" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  interface_a: int
  interface_b: int
  --ssid: string
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, interface_a: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, interface_b: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-links/")
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "interface_a": $interface_a, "interface_b": $interface_b, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /wireless/wireless-links/
#
# operationId: wireless_wireless-links_bulk_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-links update-bulk-1" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  interface_a: int
  interface_b: int
  --ssid: string
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, interface_a: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, interface_b: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/wireless/wireless-links/")
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "interface_a": $interface_a, "interface_b": $interface_b, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# DELETE /wireless/wireless-links/{id}/
#
# operationId: wireless_wireless-links_delete
export def "wireless-wireless-links delete" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> any {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-links/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# GET /wireless/wireless-links/{id}/
#
# operationId: wireless_wireless-links_read
export def "wireless-wireless-links get" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
]: nothing -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, interface_a: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, interface_b: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-links/{id}/"))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" null {query: {}, body: null}
}

# PATCH /wireless/wireless-links/{id}/
#
# operationId: wireless_wireless-links_partial_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-links update-by-id" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  interface_a: int
  interface_b: int
  --ssid: string
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, interface_a: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, interface_b: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-links/{id}/"))
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "interface_a": $interface_a, "interface_b": $interface_b, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "patch" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}

# PUT /wireless/wireless-links/{id}/
#
# operationId: wireless_wireless-links_update
# --tags item shape: {color?: string, name: string, slug: string}
export def "wireless-wireless-links update-by-id-1" [
  id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --full(-F) # Return full response record {status, headers, body} while still raising on 4xx/5xx
  --dry-run(-n) # Return the request that would be sent without executing it
  --auth-cipher: string@auth-cipher-completer
  --auth-psk: string
  --auth-type: string@auth-type-completer-1
  --comments: string
  --custom-fields: record # default: {}
  --description: string
  interface_a: int
  interface_b: int
  --ssid: string
  --status: string@status-completer-1
  --tags: list # item shape: {color?: string, name: string, slug: string}
  --tenant: int # nullable
]: any -> record<auth_cipher: record<label: string, value: string>, auth_psk: string, auth_type: record<label: string, value: string>, comments: string, created: string, custom_fields: record, description: string, display: string, id: int, interface_a: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, interface_b: record<_occupied: bool, cable: int, device: record<display: string, id: int, name: string, url: string>, display: string, id: int, name: string, url: string>, last_updated: string, ssid: string, status: record<label: string, value: string>, tags: table<color: string, display: string, id: int, name: string, slug: string, url: string>, tenant: record<display: string, id: int, name: string, slug: string, url: string>, url: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  if ($id | is-empty) { error make --unspanned { msg: "path parameter 'id' must be non-empty" } }
  let full_url = (build-url $base ({id: (encode-path-segment $id)} | format pattern "/wireless/wireless-links/{id}/"))
  let req_body = {"auth_cipher": $auth_cipher, "auth_psk": $auth_psk, "auth_type": $auth_type, "comments": $comments, "custom_fields": $custom_fields, "description": $description, "interface_a": $interface_a, "interface_b": $interface_b, "ssid": $ssid, "status": $status, "tags": $tags, "tenant": $tenant} | compact
  let req_body = if ($input | describe | str starts-with "record") { $input | merge deep ($req_body | default {}) } else { $req_body }
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors $full "application/json" $req_body {query: {}, body: $req_body}
}
