# Auto-generated client for bunq API v1.0
# Source: https://api.apis.guru/v2/specs/bunq.com/1.0/openapi.json
# Auth: --token flag or $env.BUNQ_API_TOKEN

const BASE_URL = "https://public-api.sandbox.bunq.com/v1"
const DEFAULT_AUTH = "bearer"

# Build auth: returns {headers: record, query: string}
def build-auth [token?: string, auth_scheme?: string]: nothing -> record {
  let token_val = if ($token != null) and ($token | is-not-empty) { $token } else { $env | get -o BUNQ_API_TOKEN | default "" }
  let scheme = ($auth_scheme | default "bearer")
  if ($scheme == "none") or ($token_val | is-empty) { return {headers: {}, query: ""} }
  match $scheme {
    "none" => { {headers: {}, query: ""} }
    _ => { {headers: {Authorization: $"Bearer ($token_val)"}, query: ""} }
  }
}

# Serialize a single query parameter based on collection style
def serialize-qp [name: string, value: any, style: string]: nothing -> list<string> {
  if ($value == null) { return [] }
  let n = ($name | url encode)
  let is_list = ($value | describe | str starts-with "list")
  if ($value | describe | str starts-with "record") { return ($value | transpose k v | each { $"($n)[($in.k | into string | url encode)]=($in.v | into string | url encode)" }) }
  if not $is_list { return [$"($n)=($value | into string | url encode)"] }
  match $style {
    "multi" => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
    "csv" => { let joined = ($value | each { $in | into string | url encode } | str join ","); [$"($n)=($joined)"] }
    "ssv" => { let joined = ($value | each { $in | into string | url encode } | str join "%20"); [$"($n)=($joined)"] }
    "tsv" => { let joined = ($value | each { $in | into string | url encode } | str join "%09"); [$"($n)=($joined)"] }
    "pipes" => { let joined = ($value | each { $in | into string | url encode } | str join "|"); [$"($n)=($joined)"] }
    "deepObject" => { $value | each {|v| $"($n)[]=($v | into string | url encode)" } }
    _ => { $value | each {|v| $"($n)=($v | into string | url encode)" } }
  }
}

# Build URL from base, path, and optional query string
def build-url [base: string, path: string, query?: string]: nothing -> string {
  let parsed = ($base | url parse | reject params)
  let full_path = if ($path | is-empty) { $parsed.path } else { [$parsed.path $path] | str join "/" | str replace --all --regex '/+' '/' }
  let result = ($parsed | upsert path $full_path)
  if ($query != null) and ($query | is-not-empty) { $result | upsert query $query | url join } else { $result | url join }
}

# Execute HTTP request with method dispatch
def do-request [method: string, url: string, auth: record, insecure: bool, raw: bool, dry_run: bool, max_time?: duration, allow_errors?: bool, content_type?: string, body?: any]: nothing -> any {
  let req_url = if ($auth.query | is-not-empty) { if ($url | str contains "?") { $"($url)&($auth.query)" } else { $"($url)?($auth.query)" } } else { $url }
  let timeout = ($max_time | default 30min)
  let ct = ($content_type | default "application/json")
  if $dry_run { return {method: $method, url: $req_url, headers: $auth.headers, query_string: $auth.query, content_type: $ct, timeout: $timeout, body: $body} }
  let resp = match $method {
    "get" => { http get --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url }
    "head" => { http head --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "options" => { http options --headers $auth.headers --max-time $timeout --insecure=$insecure $req_url }
    "post" => { http post --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "put" => { http put --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "patch" => { http patch --headers $auth.headers --content-type $ct --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url ($body | default {}) }
    "delete" => { if ($body | is-empty) { http delete --headers $auth.headers --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } else { http delete --headers $auth.headers --content-type $ct --data $body --full --allow-errors --max-time $timeout --insecure=$insecure --raw=$raw $req_url } }
  }
  if ($method in ["head" "options"]) { return $resp }
  if $allow_errors { $resp } else if $resp.status == 204 { null } else if $resp.status >= 400 { error make --unspanned { msg: $"HTTP ($resp.status): ($resp.body)" } } else { $resp.body }
}

def base-url-completer [] { ["https://public-api.sandbox.bunq.com/v1" "https://api.bunq.com/v1"] }
def auth-scheme-completer [] { ["bearer"] }


# List all available API commands with their parameters
export def commands []: nothing -> table {
  let builtin_flags = ["base-url" "token" "auth-scheme" "insecure" "max-time" "raw" "allow-errors" "dry-run" "accept" "help"]
  let mod_name = (scope modules | where { $in.commands | any { $in.name == "attachment-public attach-ment" } } | get name | first)
  let mod_cmds = (scope modules | where name == $mod_name | get commands | first)
  let cmd_ids = ($mod_cmds | where name not-in [$mod_name "commands"] | get decl_id)
  scope commands | where decl_id in $cmd_ids | each {|cmd|
    let sig = $cmd.signatures | values | first
    let params = $sig
      | where parameter_type not-in ["input" "output"]
      | where parameter_name not-in $builtin_flags
      | select parameter_name parameter_type syntax_shape is_optional description
    let return_type = ($sig | where parameter_type == "output" | get -o syntax_shape | first | default "any")
    {
      name: ($cmd.name | str replace $"($mod_name) " "")
      description: $cmd.description
      extra_description: $cmd.extra_description
      return_type: $return_type
      params: $params
    }
  }
}

# Create a new public attachment. Create a POST request with a payload that contains a binary representation of the file, without any JSON wrapping. Make sure you define the MIME type (i.e. image/jpeg, or image/png) in the Content-Type header. You are required to provide a description of the attachment using the X-Bunq-Attachment-Description header.
#
# POST /attachment-public
# operationId: CREATE_AttachmentPublic
export def "attachment-public attach-ment" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/attachment-public")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the raw content of a specific attachment.
#
# GET /attachment-public/{attachment-publicUUID}/content
# operationId: List_all_Content_for_AttachmentPublic
export def "attachment-public-content attach-ment" [
  attachment_public_uuid: string
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({attachment_public_uuid: $attachment_public_uuid} | format pattern "/attachment-public/{attachment_public_uuid}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific attachment's metadata through its UUID. The Content-Type header of the response will describe the MIME type of the attachment file.
#
# GET /attachment-public/{itemId}
# operationId: READ_AttachmentPublic
export def "attachment-public attach-ment-by-itemId" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: record<content_type: string, description: string, urls: list<record>>, created: string, updated: string, uuid: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/attachment-public/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Avatars are public images used to represent you or your company. Avatars are used to represent users, monetary accounts and cash registers. Avatars cannot be deleted, only replaced. Avatars can be updated after uploading the image you would like to use through AttachmentPublic. Using the attachment_public_uuid which is returned you can update your Avatar. Avatars used for cash registers and company accounts will be reviewed by bunq.
#
# POST /avatar
# operationId: CREATE_Avatar
export def "avatar post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --uuid: string # The public UUID of the avatar.
]: any -> record<uuid: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/avatar")
  let body = {"uuid": $uuid} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Avatars are public images used to represent you or your company. Avatars are used to represent users, monetary accounts and cash registers. Avatars cannot be deleted, only replaced. Avatars can be updated after uploading the image you would like to use through AttachmentPublic. Using the attachment_public_uuid which is returned you can update your Avatar. Avatars used for cash registers and company accounts will be reviewed by bunq.
#
# GET /avatar/{itemId}
# operationId: READ_Avatar
export def "avatar get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<image: table<attachment_public_uuid: string, content_type: string, height: int, width: int>, uuid: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/avatar/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a collection of Devices. A Device is either a DevicePhone or a DeviceServer.
#
# GET /device
# operationId: List_all_Device
export def "device list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<DeviceServer: record<description: string, permitted_ips: list, secret: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/device")
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a collection of all the DeviceServers you have created.
#
# GET /device-server
# operationId: List_all_DeviceServer
export def "device-server list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, description: string, id: int, ip: string, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/device-server")
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new DeviceServer providing the installation token in the header and signing the request with the private part of the key you used to create the installation. The API Key that you are using will be bound to the IP address of the DeviceServer which you have created.<br/><br/>Using a Wildcard API Key gives you the freedom to make API calls even if the IP address has changed after the POST device-server.<br/><br/>Find out more at this link <a href="https:/bunq.com/en/apikey-dynamic-ip" target="_blank">https:/bunq.com/en/apikey-dynamic-ip</a>.
#
# POST /device-server
# operationId: CREATE_DeviceServer
export def "device-server post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  description: string # The description of the DeviceServer. This is only for your own reference when reading the DeviceServer again.
  --permitted-ips: list # An array of IPs (v4 or v6) this DeviceServer will be able to do calls from. These will be linked to the API key.
  secret: string # The API key. You can request an API key in the bunq app.
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/device-server")
  let body = {"description": $description, "permitted_ips": $permitted_ips, "secret": $secret} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get one of your DeviceServers.
#
# GET /device-server/{itemId}
# operationId: READ_DeviceServer
export def "device-server get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, description: string, id: int, ip: string, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/device-server/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a single Device. A Device is either a DevicePhone or a DeviceServer.
#
# GET /device/{itemId}
# operationId: READ_Device
export def "device get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<DeviceServer: record<description: string, permitted_ips: list<string>, secret: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/device/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# You must have an active session to make this call. This call returns the Id of the the Installation you are using in your session.
#
# GET /installation
# operationId: List_all_Installation
export def "installation list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/installation")
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# This is the only API call that does not require you to use the "X-Bunq-Client-Authentication" and "X-Bunq-Client-Signature" headers.  You provide the server with the public part of the key pair that you are going to use to create the value of the signature header for all future API calls. The server creates an installation for you. Store the Installation Token and ServerPublicKey from the response. This token is used in the "X-Bunq-Client-Authentication" header for the creation of a DeviceServer and SessionServer.
#
# POST /installation
# operationId: CREATE_Installation
export def "installation post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  client_public_key: string # Your public key. This is the public part of the key pair that you are going to use to create value of the "X-Bunq-Client-Signature" header for all future API calls.
]: any -> record<Id: record<id: int>, ServerPublicKey: record<server_public_key: string>, Token: record<created: string, id: int, token: string, updated: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/installation")
  let body = {"client_public_key": $client_public_key} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Show the ServerPublicKey for this Installation.
#
# GET /installation/{installationID}/server-public-key
# operationId: List_all_ServerPublicKey_for_Installation
export def "installation-server-public-key get" [
  installation_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<server_public_key: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({installation_id: $installation_id} | format pattern "/installation/{installation_id}/server-public-key"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# You must have an active session to make this call. This call is used to check whether the Id you provide is the Id of your current installation or not.
#
# GET /installation/{itemId}
# operationId: READ_Installation
export def "installation get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/installation/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Register a Payment Service Provider and provide credentials
#
# POST /payment-service-provider-credential
# operationId: CREATE_PaymentServiceProviderCredential
export def "payment-service-provider-credential post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  client_payment_service_provider_certificate: string # Payment Services Directive 2 compatible QSEAL certificate
  client_payment_service_provider_certificate_chain: string # Intermediate and root certificate belonging to the provided certificate.
  client_public_key_signature: string # The Base64 encoded signature of the public key provided during installation and with the installation token appended as a nonce. Signed with the private key belonging to the QSEAL certificate.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/payment-service-provider-credential")
  let body = {"client_payment_service_provider_certificate": $client_payment_service_provider_certificate, "client_payment_service_provider_certificate_chain": $client_payment_service_provider_certificate_chain, "client_public_key_signature": $client_public_key_signature} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Register a Payment Service Provider and provide credentials
#
# GET /payment-service-provider-credential/{itemId}
# operationId: READ_PaymentServiceProviderCredential
export def "payment-service-provider-credential get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, expiry_time: string, id: int, permitted_device: record<description: string, ip: string>, status: string, token_value: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/payment-service-provider-credential/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# View endpoint for place opening periods.
#
# GET /place-lookup/{place-lookupID}/photo/{photoID}/content
# operationId: List_all_Content_for_PlaceLookup_Photo
export def "place-lookup-photo-content get" [
  place_lookup_id: int
  photo_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({place_lookup_id: $place_lookup_id, photo_id: $photo_id} | format pattern "/place-lookup/{place_lookup_id}/photo/{photo_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create a sandbox userCompany.
#
# POST /sandbox-user-company
# operationId: CREATE_SandboxUserCompany
export def "sandbox-user-company post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/sandbox-user-company")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to create a sandbox userPerson.
#
# POST /sandbox-user-person
# operationId: CREATE_SandboxUserPerson
export def "sandbox-user-person post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/sandbox-user-person")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# An endpoint that will always throw an error.
#
# POST /server-error
# operationId: CREATE_ServerError
export def "server-error post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/server-error")
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a new session for a DeviceServer. Provide the Installation token in the "X-Bunq-Client-Authentication" header. And don't forget to create the "X-Bunq-Client-Signature" header. The response contains a Session token that should be used for as the "X-Bunq-Client-Authentication" header for all future API calls. The ip address making this call needs to match the ip address bound to your API key.
#
# POST /session-server
# operationId: CREATE_SessionServer
export def "session-server post" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  secret: string # The API key of the user you want to login. If your API key has not been used before, it will be bound to the ip address of this DeviceServer.
]: any -> record<Id: record<id: int>, Token: record<id: int, token: string>, UserApiKey: record<created: string, granted_by_user: record<UserCompany: record, UserPaymentServiceProvider: record, UserPerson: record>, id: int, requested_by_user: record<UserCompany: record, UserPaymentServiceProvider: record, UserPerson: record>, updated: string>, UserCompany: record<address_main: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_postal: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, billing_contract: list<record>, chamber_of_commerce_number: string, counter_bank_iban: string, country: string, created: string, customer: record<billing_account_id: string, created: string, id: int, invoice_notification_preference: string, updated: string>, customer_limit: record<limit_amount_monthly: record, limit_card_debit_maestro: int, limit_card_debit_mastercard: int, limit_card_debit_wildcard: int, limit_card_replacement: int, limit_card_wildcard: int, limit_monetary_account: int, limit_monetary_account_remaining: int, spent_amount_monthly: record>, daily_limit_without_confirmation_login: record<currency: string, value: string>, deny_reason: string, directors: list<record>, display_name: string, id: int, language: string, legal_form: string, name: string, notification_filters: list<record>, public_nick_name: string, public_uuid: string, region: string, relations: list<record>, sector_of_industry: string, session_timeout: int, status: string, sub_status: string, tax_resident: list<record>, type_of_business_entity: string, ubo: list<record>, updated: string, version_terms_of_service: string>, UserPaymentServiceProvider: record<alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, certificate_distinguished_name: string, created: string, display_name: string, id: int, language: string, public_nick_name: string, public_uuid: string, region: string, session_timeout: int, status: string, sub_status: string, updated: string>, UserPerson: record<address_main: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_postal: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, country_of_birth: string, created: string, daily_limit_without_confirmation_login: record<currency: string, value: string>, date_of_birth: string, display_name: string, document_back_attachment_id: int, document_country_of_issuance: string, document_front_attachment_id: int, document_number: string, document_type: string, first_name: string, gender: string, id: int, language: string, last_name: string, legal_guardian_alias: record<name: string, service: string, type: string, value: string>, legal_name: string, middle_name: string, nationality: string, notification_filters: list<record>, place_of_birth: string, public_nick_name: string, public_uuid: string, region: string, relations: list<record>, session_timeout: int, signup_track_type: string, status: string, sub_status: string, subscription_type: string, tax_resident: list<record>, updated: string, version_terms_of_service: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/session-server")
  let body = {"secret": $secret} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Deletes the current session.
#
# DELETE /session/{itemId}
# operationId: DELETE_Session
export def "session delete" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/session/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a collection of all available users.
#
# GET /user
# operationId: List_all_User
export def "user list" [
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<UserApiKey: record<created: string, granted_by_user: record, id: int, requested_by_user: record, updated: string>, UserCompany: record<address_main: record, address_postal: record, alias: list, avatar: record, avatar_uuid: string, billing_contract: list, chamber_of_commerce_number: string, counter_bank_iban: string, country: string, created: string, customer: record, customer_limit: record, daily_limit_without_confirmation_login: record, deny_reason: string, directors: list, display_name: string, id: int, language: string, legal_form: string, name: string, notification_filters: list, public_nick_name: string, public_uuid: string, region: string, relations: list, sector_of_industry: string, session_timeout: int, status: string, sub_status: string, tax_resident: list, type_of_business_entity: string, ubo: list, updated: string, version_terms_of_service: string>, UserPaymentServiceProvider: record<alias: list, avatar: record, certificate_distinguished_name: string, created: string, display_name: string, id: int, language: string, public_nick_name: string, public_uuid: string, region: string, session_timeout: int, status: string, sub_status: string, updated: string>, UserPerson: record<address_main: record, address_postal: record, alias: list, avatar: record, avatar_uuid: string, country_of_birth: string, created: string, daily_limit_without_confirmation_login: record, date_of_birth: string, display_name: string, document_back_attachment_id: int, document_country_of_issuance: string, document_front_attachment_id: int, document_number: string, document_type: string, first_name: string, gender: string, id: int, language: string, last_name: string, legal_guardian_alias: record, legal_name: string, middle_name: string, nationality: string, notification_filters: list, place_of_birth: string, public_nick_name: string, public_uuid: string, region: string, relations: list, session_timeout: int, signup_track_type: string, status: string, sub_status: string, subscription_type: string, tax_resident: list, updated: string, version_terms_of_service: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base "/user")
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific company.
#
# GET /user-company/{itemId}
# operationId: READ_UserCompany
export def "user-company get-by-itemId" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<address_main: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_postal: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: table<name: string, service: string, type: string, value: string>, avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, billing_contract: table<contract_date_end: string, contract_date_start: string, contract_version: int, created: string, id: int, status: string, sub_status: string, subscription_type: string, subscription_type_downgrade: string, updated: string>, chamber_of_commerce_number: string, counter_bank_iban: string, country: string, created: string, customer: record<billing_account_id: string, created: string, id: int, invoice_notification_preference: string, updated: string>, customer_limit: record<limit_amount_monthly: record<currency: string, value: string>, limit_card_debit_maestro: int, limit_card_debit_mastercard: int, limit_card_debit_wildcard: int, limit_card_replacement: int, limit_card_wildcard: int, limit_monetary_account: int, limit_monetary_account_remaining: int, spent_amount_monthly: record<currency: string, value: string>>, daily_limit_without_confirmation_login: record<currency: string, value: string>, deny_reason: string, directors: table<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, display_name: string, id: int, language: string, legal_form: string, name: string, notification_filters: table<category: string, notification_delivery_method: string, notification_target: string>, public_nick_name: string, public_uuid: string, region: string, relations: table<counter_label_user: record, counter_user_id: string, counter_user_status: string, label_user: record, relationship: string, status: string, user_id: string, user_status: string>, sector_of_industry: string, session_timeout: int, status: string, sub_status: string, tax_resident: table<country: string, status: string, tax_number: string>, type_of_business_entity: string, ubo: table<date_of_birth: string, name: string, nationality: string>, updated: string, version_terms_of_service: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/user-company/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Modify a specific company's data.
#
# PUT /user-company/{itemId}
# operationId: UPDATE_UserCompany
# --address_main shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_postal shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --alias item shape: {name?: string, service?: string, type?: string, value?: string}
# --avatar shape: {uuid?: string}
# --billing_contract item shape: {subscription_type?: string}
# --customer shape: {billing_account_id?: string, invoice_notification_preference?: string}
# --customer_limit shape: {limit_amount_monthly?: record, spent_amount_monthly?: record}
# --daily_limit_without_confirmation_login shape: {currency?: string, value?: string}
# --directors item shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
# --notification_filters item shape: {category?: string, notification_delivery_method?: string, notification_target?: string}
# --relations item shape: {counter_label_user?: record, label_user?: record}
# --tax_resident item shape: {country?: string, status?: string, tax_number?: string}
# --ubo item shape: {date_of_birth?: string, name?: string, nationality?: string}
export def "user-company put-by-itemId" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --address-main: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --address-postal: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --avatar: record # shape: {uuid?: string}
  --avatar-uuid: string # The public UUID of the company's avatar.
  --chamber-of-commerce-number: string # The company's chamber of commerce number.
  --country: string # The country as an ISO 3166-1 alpha-2 country code.
  --customer: record # shape: {billing_account_id?: string, invoice_notification_preference?: string}
  --customer-limit: record # shape: {limit_amount_monthly?: record, spent_amount_monthly?: record}
  --daily-limit-without-confirmation-login: record # shape: {currency?: string, value?: string}
  --language: string # The person's preferred language. Formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, seperated by an underscore.
  --legal-form: string # The company's legal form.
  --name: string # The company name.
  --public-nick-name: string # The company's public nick name.
  --region: string # The person's preferred region. Formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, seperated by an underscore.
  --session-timeout: int # The setting for the session timeout of the company in seconds.
  --status: string # The user status. Can be: ACTIVE, SIGNUP, RECOVERY.
  --sub-status: string # The user sub-status. Can be: NONE, FACE_RESET, APPROVAL, APPROVAL_DIRECTOR, APPROVAL_PARENT, APPROVAL_SUPPORT, COUNTER_IBAN, IDEAL or SUBMIT.
  --ubo: list # The names of the company's ultimate beneficiary owners. Minimum zero, maximum four. — item shape: {date_of_birth?: string, name?: string, nationality?: string}
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/user-company/{item_id}"))
  let body = {"address_main": $address_main, "address_postal": $address_postal, "avatar": $avatar, "avatar_uuid": $avatar_uuid, "chamber_of_commerce_number": $chamber_of_commerce_number, "country": $country, "customer": $customer, "customer_limit": $customer_limit, "daily_limit_without_confirmation_login": $daily_limit_without_confirmation_login, "language": $language, "legal_form": $legal_form, "name": $name, "public_nick_name": $public_nick_name, "region": $region, "session_timeout": $session_timeout, "status": $status, "sub_status": $sub_status, "ubo": $ubo} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Return all the known (trade) names for a specific user company.
#
# GET /user-company/{user-companyID}/name
# operationId: List_all_Name_for_UserCompany
export def "user-company-name get" [
  user_company_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<name_array: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_company_id: $user_company_id} | format pattern "/user-company/{user_company_id}/name"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view UserPaymentServiceProvider for session creation.
#
# GET /user-payment-service-provider/{itemId}
# operationId: READ_UserPaymentServiceProvider
export def "user-payment-service-provider get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: table<name: string, service: string, type: string, value: string>, avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, certificate_distinguished_name: string, created: string, display_name: string, id: int, language: string, public_nick_name: string, public_uuid: string, region: string, session_timeout: int, status: string, sub_status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/user-payment-service-provider/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific person.
#
# GET /user-person/{itemId}
# operationId: READ_UserPerson
export def "user-person get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<address_main: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_postal: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: table<name: string, service: string, type: string, value: string>, avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, country_of_birth: string, created: string, daily_limit_without_confirmation_login: record<currency: string, value: string>, date_of_birth: string, display_name: string, first_name: string, gender: string, id: int, language: string, last_name: string, legal_name: string, middle_name: string, nationality: string, notification_filters: table<category: string, notification_delivery_method: string, notification_target: string>, place_of_birth: string, public_nick_name: string, public_uuid: string, region: string, relations: table<counter_label_user: record, counter_user_id: string, counter_user_status: string, label_user: record, relationship: string, status: string, user_id: string, user_status: string>, session_timeout: int, status: string, sub_status: string, tax_resident: table<country: string, status: string, tax_number: string>, updated: string, version_terms_of_service: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/user-person/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Modify a specific person object's data.
#
# PUT /user-person/{itemId}
# operationId: UPDATE_UserPerson
# --address_main shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_postal shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --alias item shape: {name?: string, service?: string, type?: string, value?: string}
# --avatar shape: {uuid?: string}
# --daily_limit_without_confirmation_login shape: {currency?: string, value?: string}
# --legal_guardian_alias shape: {name?: string, service?: string, type?: string, value?: string}
# --notification_filters item shape: {category?: string, notification_delivery_method?: string, notification_target?: string}
# --relations item shape: {counter_label_user?: record, label_user?: record}
# --tax_resident item shape: {country?: string, status?: string, tax_number?: string}
export def "user-person put" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --address-main: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --address-postal: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --avatar: record # shape: {uuid?: string}
  --avatar-uuid: string # The public UUID of the user's avatar.
  --daily-limit-without-confirmation-login: record # shape: {currency?: string, value?: string}
  --date-of-birth: string # The person's date of birth. Accepts ISO8601 date formats.
  --display-name: string # The display name for the person.
  --document-back-attachment-id: int # The reference to the uploaded picture/scan of the back side of the identification document.
  --document-country-of-issuance: string # The country which issued the identification document the person registered with.
  --document-front-attachment-id: int # The reference to the uploaded picture/scan of the front side of the identification document.
  --document-number: string # The identification document number the person registered with.
  --document-type: string # The type of identification document the person registered with.
  --first-name: string # The person's first name.
  --gender: string # The person's gender. Can be MALE, FEMALE or UNKNOWN.
  --language: string # The person's preferred language. Formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, seperated by an underscore.
  --last-name: string # The person's last name.
  --legal-guardian-alias: record # shape: {name?: string, service?: string, type?: string, value?: string}
  --middle-name: string # The person's middle name.
  --nationality: string # The person's nationality. Formatted as a SO 3166-1 alpha-2 country code.
  --public-nick-name: string # The public nick name for the person.
  --region: string # The person's preferred region. Formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, seperated by an underscore.
  --session-timeout: int # The setting for the session timeout of the user in seconds.
  --signup-track-type: string # The type of signup track the user is following.
  --status: string # The user status. The user status. Can be: ACTIVE, BLOCKED, SIGNUP, RECOVERY, DENIED or ABORTED.
  --sub-status: string # The user sub-status. Can be: NONE, FACE_RESET, APPROVAL, APPROVAL_DIRECTOR, APPROVAL_PARENT, APPROVAL_SUPPORT, COUNTER_IBAN, IDEAL or SUBMIT.
  --subscription-type: string # The subscription type the user should start on.
  --tax-resident: list # The user's tax residence numbers for different countries. — item shape: {country?: string, status?: string, tax_number?: string}
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/user-person/{item_id}"))
  let body = {"address_main": $address_main, "address_postal": $address_postal, "avatar": $avatar, "avatar_uuid": $avatar_uuid, "daily_limit_without_confirmation_login": $daily_limit_without_confirmation_login, "date_of_birth": $date_of_birth, "display_name": $display_name, "document_back_attachment_id": $document_back_attachment_id, "document_country_of_issuance": $document_country_of_issuance, "document_front_attachment_id": $document_front_attachment_id, "document_number": $document_number, "document_type": $document_type, "first_name": $first_name, "gender": $gender, "language": $language, "last_name": $last_name, "legal_guardian_alias": $legal_guardian_alias, "middle_name": $middle_name, "nationality": $nationality, "public_nick_name": $public_nick_name, "region": $region, "session_timeout": $session_timeout, "signup_track_type": $signup_track_type, "status": $status, "sub_status": $sub_status, "subscription_type": $subscription_type, "tax_resident": $tax_resident} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific user.
#
# GET /user/{itemId}
# operationId: READ_User
export def "user get" [
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<UserApiKey: record<created: string, granted_by_user: record<UserCompany: record, UserPaymentServiceProvider: record, UserPerson: record>, id: int, requested_by_user: record<UserCompany: record, UserPaymentServiceProvider: record, UserPerson: record>, updated: string>, UserCompany: record<address_main: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_postal: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, billing_contract: list<record>, chamber_of_commerce_number: string, counter_bank_iban: string, country: string, created: string, customer: record<billing_account_id: string, created: string, id: int, invoice_notification_preference: string, updated: string>, customer_limit: record<limit_amount_monthly: record, limit_card_debit_maestro: int, limit_card_debit_mastercard: int, limit_card_debit_wildcard: int, limit_card_replacement: int, limit_card_wildcard: int, limit_monetary_account: int, limit_monetary_account_remaining: int, spent_amount_monthly: record>, daily_limit_without_confirmation_login: record<currency: string, value: string>, deny_reason: string, directors: list<record>, display_name: string, id: int, language: string, legal_form: string, name: string, notification_filters: list<record>, public_nick_name: string, public_uuid: string, region: string, relations: list<record>, sector_of_industry: string, session_timeout: int, status: string, sub_status: string, tax_resident: list<record>, type_of_business_entity: string, ubo: list<record>, updated: string, version_terms_of_service: string>, UserPaymentServiceProvider: record<alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, certificate_distinguished_name: string, created: string, display_name: string, id: int, language: string, public_nick_name: string, public_uuid: string, region: string, session_timeout: int, status: string, sub_status: string, updated: string>, UserPerson: record<address_main: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_postal: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, country_of_birth: string, created: string, daily_limit_without_confirmation_login: record<currency: string, value: string>, date_of_birth: string, display_name: string, document_back_attachment_id: int, document_country_of_issuance: string, document_front_attachment_id: int, document_number: string, document_type: string, first_name: string, gender: string, id: int, language: string, last_name: string, legal_guardian_alias: record<name: string, service: string, type: string, value: string>, legal_name: string, middle_name: string, nationality: string, notification_filters: list<record>, place_of_birth: string, public_nick_name: string, public_uuid: string, region: string, relations: list<record>, session_timeout: int, signup_track_type: string, status: string, sub_status: string, subscription_type: string, tax_resident: list<record>, updated: string, version_terms_of_service: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({item_id: $item_id} | format pattern "/user/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the raw content of a specific attachment.
#
# GET /user/{userID}/attachment/{attachmentID}/content
# operationId: List_all_Content_for_User_Attachment
export def "user-attachment-content attach-ment" [
  user_id: int
  attachment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, attachment_id: $attachment_id} | format pattern "/user/{user_id}/attachment/{attachment_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific attachment. The header of the response contains the content-type of the attachment.
#
# GET /user/{userID}/attachment/{itemId}
# operationId: READ_Attachment_for_User
export def "user-attachment get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: record<content_type: string, description: string, urls: list<record>>, created: string, id: int, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all subscription billing contract for the authenticated user.
#
# GET /user/{userID}/billing-contract-subscription
# operationId: List_all_BillingContractSubscription_for_User
export def "user-billing-contract-subscription get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<contract_date_end: string, contract_date_start: string, contract_version: int, created: string, id: int, status: string, sub_status: string, subscription_type: string, subscription_type_downgrade: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/billing-contract-subscription"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# bunq.me public profile of the user.
#
# GET /user/{userID}/bunqme-fundraiser-profile
# operationId: List_all_BunqmeFundraiserProfile_for_User
export def "user-bunqme-fundraiser-profile list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, attachment: record, color: string, currency: string, description: string, monetary_account_id: int, owner_user_id: int, pointer: record<name: string, service: string, type: string, value: string>, redirect_url: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/bunqme-fundraiser-profile"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# bunq.me public profile of the user.
#
# GET /user/{userID}/bunqme-fundraiser-profile/{itemId}
# operationId: READ_BunqmeFundraiserProfile_for_User
export def "user-bunqme-fundraiser-profile get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, attachment: record, color: string, currency: string, description: string, monetary_account_id: int, owner_user_id: int, pointer: record<name: string, service: string, type: string, value: string>, redirect_url: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/bunqme-fundraiser-profile/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return all the cards available to the user.
#
# GET /user/{userID}/card
# operationId: List_all_Card_for_User
export def "user-card list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<card_limit: record<currency: string, value: string>, card_limit_atm: record<currency: string, value: string>, card_shipment_tracking_url: string, country: string, country_permission: list<record>, created: string, expiry_date: string, id: int, label_monetary_account_current: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, label_monetary_account_ordered: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, monetary_account_id_fallback: int, name_on_card: string, order_status: string, payment_account_reference: string, pin_code_assignment: list<record>, primary_account_numbers: list<record>, public_uuid: string, second_line: string, status: string, sub_status: string, sub_type: string, type: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/card"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to update multiple cards in a batch.
#
# POST /user/{userID}/card-batch
# operationId: CREATE_CardBatch_for_User
# --cards item shape: {card_limit?: record, card_limit_atm?: record, country_permission?: list, id: int, monetary_account_id_fallback?: int, status?: string}
export def "user-card-batch post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  cards: list # The cards that need to be updated. — item shape: {card_limit?: record, card_limit_atm?: record, country_permission?: list, id: int, monetary_account_id_fallback?: int, status?: string}
]: any -> record<updated_card_ids: table<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/card-batch"))
  let body = {"cards": $cards} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to replace multiple cards in a batch.
#
# POST /user/{userID}/card-batch-replace
# operationId: CREATE_CardBatchReplace_for_User
# --cards item shape: {id: int, name_on_card?: string, pin_code_assignment?: list, second_line?: string}
export def "user-card-batch-replace post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  cards: list # The cards that need to be replaced. — item shape: {id: int, name_on_card?: string, pin_code_assignment?: list, second_line?: string}
]: any -> record<updated_card_ids: table<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/card-batch-replace"))
  let body = {"cards": $cards} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a new credit card request.
#
# POST /user/{userID}/card-credit
# operationId: CREATE_CardCredit_for_User
# --alias shape: {name?: string, service?: string, type?: string, value?: string}
# --pin_code_assignment item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
export def "user-card-credit post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --alias: record # shape: {name?: string, service?: string, type?: string, value?: string}
  --monetary-account-id-fallback: int # ID of the MA to be used as fallback for this card if insufficient balance. Fallback account is removed if not supplied.
  name_on_card: string # The user's name as it will be on the card. Check 'card-name' for the available card names for a user.
  --order-status: string # The order status of this card. Can be CARD_REQUEST_PENDING or VIRTUAL_DELIVERY.
  --pin-code-assignment: list # Array of Types, PINs, account IDs assigned to the card. — item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
  --preferred-name-on-card: string # The user's preferred name that can be put on the card.
  product_type: string # The product type of the card to order.
  second_line: string # The second line of text on the card, used as name/description for it. It can contain at most 17 characters and it can be empty.
  type: string # The type of card to order. Can be MASTERCARD.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/card-credit"))
  let body = {"alias": $alias, "monetary_account_id_fallback": $monetary_account_id_fallback, "name_on_card": $name_on_card, "order_status": $order_status, "pin_code_assignment": $pin_code_assignment, "preferred_name_on_card": $preferred_name_on_card, "product_type": $product_type, "second_line": $second_line, "type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a new debit card request.
#
# POST /user/{userID}/card-debit
# operationId: CREATE_CardDebit_for_User
# --alias shape: {name?: string, service?: string, type?: string, value?: string}
# --pin_code_assignment item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
export def "user-card-debit post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --alias: record # shape: {name?: string, service?: string, type?: string, value?: string}
  --monetary-account-id-fallback: int # ID of the MA to be used as fallback for this card if insufficient balance. Fallback account is removed if not supplied.
  name_on_card: string # The user's name as it will be on the card. Check 'card-name' for the available card names for a user.
  --order-status: string # The order status of this card. Can be CARD_REQUEST_PENDING or VIRTUAL_DELIVERY.
  --pin-code-assignment: list # Array of Types, PINs, account IDs assigned to the card. — item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
  --preferred-name-on-card: string # The user's preferred name that can be put on the card.
  product_type: string # The product type of the card to order.
  second_line: string # The second line of text on the card, used as name/description for it. It can contain at most 17 characters and it can be empty.
  type: string # The type of card to order. Can be MAESTRO or MASTERCARD.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/card-debit"))
  let body = {"alias": $alias, "monetary_account_id_fallback": $monetary_account_id_fallback, "name_on_card": $name_on_card, "order_status": $order_status, "pin_code_assignment": $pin_code_assignment, "preferred_name_on_card": $preferred_name_on_card, "product_type": $product_type, "second_line": $second_line, "type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Return all the accepted card names for a specific user.
#
# GET /user/{userID}/card-name
# operationId: List_all_CardName_for_User
export def "user-card-name get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<possible_card_name_array: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/card-name"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing card statement exports. Statement exports can be created in either CSV or PDF file format.
#
# GET /user/{userID}/card/{cardID}/export-statement-card
# operationId: List_all_ExportStatementCard_for_User_Card
export def "user-card-export-statement-card list" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<card_id: int, created: string, date_end: string, date_start: string, id: int, regional_format: string, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to serialize ExportStatementCardCsv
#
# GET /user/{userID}/card/{cardID}/export-statement-card-csv
# operationId: List_all_ExportStatementCardCsv_for_User_Card
export def "user-card-export-statement-card-csv list" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<card_id: int, created: string, date_end: string, date_start: string, id: int, regional_format: string, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-csv"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to serialize ExportStatementCardCsv
#
# POST /user/{userID}/card/{cardID}/export-statement-card-csv
# operationId: CREATE_ExportStatementCardCsv_for_User_Card
export def "user-card-export-statement-card-csv post" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  date_end: string # The end date for making statements.
  date_start: string # The start date for making statements.
  regional_format: string # Required for CSV exports. The regional format of the statement, can be UK_US (comma-separated) or EUROPEAN (semicolon-separated).
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-csv"))
  let body = {"date_end": $date_end, "date_start": $date_start, "regional_format": $regional_format} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to serialize ExportStatementCardCsv
#
# DELETE /user/{userID}/card/{cardID}/export-statement-card-csv/{itemId}
# operationId: DELETE_ExportStatementCardCsv_for_User_Card
export def "user-card-export-statement-card-csv delete" [
  user_id: int
  card_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-csv/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to serialize ExportStatementCardCsv
#
# GET /user/{userID}/card/{cardID}/export-statement-card-csv/{itemId}
# operationId: READ_ExportStatementCardCsv_for_User_Card
export def "user-card-export-statement-card-csv get" [
  user_id: int
  card_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<card_id: int, created: string, date_end: string, date_start: string, id: int, regional_format: string, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-csv/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to serialize ExportStatementCardPdf
#
# GET /user/{userID}/card/{cardID}/export-statement-card-pdf
# operationId: List_all_ExportStatementCardPdf_for_User_Card
export def "user-card-export-statement-card-pdf list" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<card_id: int, created: string, date_end: string, date_start: string, id: int, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-pdf"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to serialize ExportStatementCardPdf
#
# POST /user/{userID}/card/{cardID}/export-statement-card-pdf
# operationId: CREATE_ExportStatementCardPdf_for_User_Card
export def "user-card-export-statement-card-pdf post" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  date_end: string # The end date for making statements.
  date_start: string # The start date for making statements.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-pdf"))
  let body = {"date_end": $date_end, "date_start": $date_start} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to serialize ExportStatementCardPdf
#
# DELETE /user/{userID}/card/{cardID}/export-statement-card-pdf/{itemId}
# operationId: DELETE_ExportStatementCardPdf_for_User_Card
export def "user-card-export-statement-card-pdf delete" [
  user_id: int
  card_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-pdf/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to serialize ExportStatementCardPdf
#
# GET /user/{userID}/card/{cardID}/export-statement-card-pdf/{itemId}
# operationId: READ_ExportStatementCardPdf_for_User_Card
export def "user-card-export-statement-card-pdf get" [
  user_id: int
  card_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<card_id: int, created: string, date_end: string, date_start: string, id: int, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card-pdf/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Fetch the raw content of a card statement export. The returned file format could be CSV or PDF depending on the statement format specified during the statement creation. The doc won't display the response of a request to get the content of a statement export.
#
# GET /user/{userID}/card/{cardID}/export-statement-card/{export-statement-cardID}/content
# operationId: List_all_Content_for_User_Card_ExportStatementCard
export def "user-card-export-statement-card-content export" [
  user_id: int
  card_id: int
  export_statement_card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, export_statement_card_id: $export_statement_card_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card/{export_statement_card_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing card statement exports. Statement exports can be created in either CSV or PDF file format.
#
# GET /user/{userID}/card/{cardID}/export-statement-card/{itemId}
# operationId: READ_ExportStatementCard_for_User_Card
export def "user-card-export-statement-card get" [
  user_id: int
  card_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<card_id: int, created: string, date_end: string, date_start: string, id: int, regional_format: string, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{card_id}/export-statement-card/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all generated CVC2 codes for a card.
#
# GET /user/{userID}/card/{cardID}/generated-cvc2
# operationId: List_all_GeneratedCvc2_for_User_Card
export def "user-card-generated-cvc2 list" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, cvc2: string, expiry_time: string, id: int, status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/generated-cvc2"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Generate a new CVC2 code for a card.
#
# POST /user/{userID}/card/{cardID}/generated-cvc2
# operationId: CREATE_GeneratedCvc2_for_User_Card
export def "user-card-generated-cvc2 post" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --type: string # The type of generated cvc2. Can be STATIC or GENERATED.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/generated-cvc2"))
  let body = {"type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the details for a specific generated CVC2 code.
#
# GET /user/{userID}/card/{cardID}/generated-cvc2/{itemId}
# operationId: READ_GeneratedCvc2_for_User_Card
export def "user-card-generated-cvc2 get" [
  user_id: int
  card_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, cvc2: string, expiry_time: string, id: int, status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{card_id}/generated-cvc2/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for generating and retrieving a new CVC2 code.
#
# PUT /user/{userID}/card/{cardID}/generated-cvc2/{itemId}
# operationId: UPDATE_GeneratedCvc2_for_User_Card
export def "user-card-generated-cvc2 put" [
  user_id: int
  card_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --type: string # The type of generated cvc2. Can be STATIC or GENERATED.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{card_id}/generated-cvc2/{item_id}"))
  let body = {"type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Request a card replacement.
#
# POST /user/{userID}/card/{cardID}/replace
# operationId: CREATE_Replace_for_User_Card
# --pin_code_assignment item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
export def "user-card-replace post" [
  user_id: int
  card_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --name-on-card: string # The user's name as it will be on the card. Check 'card-name' for the available card names for a user.
  --pin-code-assignment: list # Array of Types, PINs, account IDs assigned to the card. — item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
  --preferred-name-on-card: string # The user's preferred name that can be put on the card.
  --second-line: string # The second line on the card.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, card_id: $card_id} | format pattern "/user/{user_id}/card/{card_id}/replace"))
  let body = {"name_on_card": $name_on_card, "pin_code_assignment": $pin_code_assignment, "preferred_name_on_card": $preferred_name_on_card, "second_line": $second_line} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Return the details of a specific card.
#
# GET /user/{userID}/card/{itemId}
# operationId: READ_Card_for_User
export def "user-card get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<card_limit: record<currency: string, value: string>, card_limit_atm: record<currency: string, value: string>, card_shipment_tracking_url: string, country: string, country_permission: table<country: string, expiry_time: string, id: int>, created: string, expiry_date: string, id: int, label_monetary_account_current: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, label_monetary_account_ordered: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, monetary_account_id_fallback: int, name_on_card: string, order_status: string, payment_account_reference: string, pin_code_assignment: table<monetary_account_id: int, pin_code: string, routing_type: string, type: string>, primary_account_numbers: table<description: string, four_digit: string, id: int, monetary_account_id: int, status: string, uuid: string>, public_uuid: string, second_line: string, status: string, sub_status: string, sub_type: string, type: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update the card details. Allow to change pin code, status, limits, country permissions and the monetary account connected to the card. When the card has been received, it can be also activated through this endpoint.
#
# PUT /user/{userID}/card/{itemId}
# operationId: UPDATE_Card_for_User
# --card_limit shape: {currency?: string, value?: string}
# --card_limit_atm shape: {currency?: string, value?: string}
# --country_permission item shape: {country?: string, expiry_time?: string}
# --pin_code_assignment item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
# --primary_account_numbers item shape: {description?: string, id?: int, monetary_account_id?: int, status?: string}
export def "user-card put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --activation-code: string # DEPRECATED: Activate a card by setting status to ACTIVE when the order_status is ACCEPTED_FOR_PRODUCTION.
  --card-limit: record # shape: {currency?: string, value?: string}
  --card-limit-atm: record # shape: {currency?: string, value?: string}
  --country-permission: list # The countries for which to grant (temporary) permissions to use the card. — item shape: {country?: string, expiry_time?: string}
  --monetary-account-id-fallback: int # ID of the MA to be used as fallback for this card if insufficient balance. Fallback account is removed if not supplied.
  --order-status: string # The order status to set for the card. Set to CARD_REQUEST_PENDING to get a virtual card produced.
  --pin-code: string # The plaintext pin code. Requests require encryption to be enabled.
  --pin-code-assignment: list # Array of Types, PINs, account IDs assigned to the card. — item shape: {monetary_account_id?: int, pin_code?: string, routing_type?: string, type?: string}
  --primary-account-numbers: list # Array of PANs and their attributes. — item shape: {description?: string, id?: int, monetary_account_id?: int, status?: string}
  --status: string # The status to set for the card. Can be ACTIVE, DEACTIVATED, LOST, STOLEN or CANCELLED, and can only be set to LOST/STOLEN/CANCELLED when order status is ACCEPTED_FOR_PRODUCTION/DELIVERED_TO_CUSTOMER/CARD_UPDATE_REQUESTED/CARD_UPDATE_SENT/CARD_UPDATE_ACCEPTED. Can only be set to DEACTIVATED after initial activation, i.e. order_status is DELIVERED_TO_CUSTOMER/CARD_UPDATE_REQUESTED/CARD_UPDATE_SENT/CARD_UPDATE_ACCEPTED. Mind that all the possible choices (apart from ACTIVE and DEACTIVATED) are permanent and cannot be changed after.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/card/{item_id}"))
  let body = {"activation_code": $activation_code, "card_limit": $card_limit, "card_limit_atm": $card_limit_atm, "country_permission": $country_permission, "monetary_account_id_fallback": $monetary_account_id_fallback, "order_status": $order_status, "pin_code": $pin_code, "pin_code_assignment": $pin_code_assignment, "primary_account_numbers": $primary_account_numbers, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# List all the pinned certificate chain for the given user.
#
# GET /user/{userID}/certificate-pinned
# operationId: List_all_CertificatePinned_for_User
export def "user-certificate-pinned list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<certificate_chain: string, id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/certificate-pinned"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Pin the certificate chain.
#
# POST /user/{userID}/certificate-pinned
# operationId: CREATE_CertificatePinned_for_User
# --certificate_chain item shape: {certificate?: string}
export def "user-certificate-pinned post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  certificate_chain: list # The certificate chain in .PEM format. — item shape: {certificate?: string}
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/certificate-pinned"))
  let body = {"certificate_chain": $certificate_chain} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Remove the pinned certificate chain with the specific ID.
#
# DELETE /user/{userID}/certificate-pinned/{itemId}
# operationId: DELETE_CertificatePinned_for_User
export def "user-certificate-pinned delete" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/certificate-pinned/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the pinned certificate chain with the specified ID.
#
# GET /user/{userID}/certificate-pinned/{itemId}
# operationId: READ_CertificatePinned_for_User
export def "user-certificate-pinned get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<certificate_chain: string, id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/certificate-pinned/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for apps to fetch a challenge request.
#
# GET /user/{userID}/challenge-request/{itemId}
# operationId: READ_ChallengeRequest_for_User
export def "user-challenge-request get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount: string, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, description: string, event_id: int, expiry_time: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/challenge-request/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for apps to fetch a challenge request.
#
# PUT /user/{userID}/challenge-request/{itemId}
# operationId: UPDATE_ChallengeRequest_for_User
export def "user-challenge-request put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  status: string # The status of the identity check. Can be ACCEPTED_PENDING_RESPONSE or REJECTED_PENDING_RESPONSE.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/challenge-request/{item_id}"))
  let body = {"status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the raw content of a specific attachment.
#
# GET /user/{userID}/chat-conversation/{chat-conversationID}/attachment/{attachmentID}/content
# operationId: List_all_Content_for_User_ChatConversation_Attachment
export def "user-chat-conversation-attachment-content attach-ment" [
  user_id: int
  chat_conversation_id: int
  attachment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, chat_conversation_id: $chat_conversation_id, attachment_id: $attachment_id} | format pattern "/user/{user_id}/chat-conversation/{chat_conversation_id}/attachment/{attachment_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create and manage companies.
#
# GET /user/{userID}/company
# operationId: List_all_Company_for_User
export def "user-company get-by-userID" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<UserCompany: record<address_main: record, address_postal: record, alias: list, avatar: record, avatar_uuid: string, billing_contract: list, chamber_of_commerce_number: string, counter_bank_iban: string, country: string, created: string, customer: record, customer_limit: record, daily_limit_without_confirmation_login: record, deny_reason: string, directors: list, display_name: string, id: int, language: string, legal_form: string, name: string, notification_filters: list, public_nick_name: string, public_uuid: string, region: string, relations: list, sector_of_industry: string, session_timeout: int, status: string, sub_status: string, tax_resident: list, type_of_business_entity: string, ubo: list, updated: string, version_terms_of_service: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/company"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create and manage companies.
#
# POST /user/{userID}/company
# operationId: CREATE_Company_for_User
# --address_main shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_postal shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --ubo item shape: {date_of_birth?: string, name?: string, nationality?: string}
# --vat_number shape: {country?: string, value?: string}
export def "user-company post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  address_main: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  address_postal: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --avatar-uuid: string # The public UUID of the company's avatar.
  --chamber-of-commerce-number: string # The company's chamber of commerce number.
  country: string # The country where the company is registered.
  legal_form: string # The company's legal form.
  name: string # The company name.
  --signup-track-type: string # The type of signup track the user is following.
  subscription_type: string # The subscription type for the company.
  --ubo: list # The names and birth dates of the company's ultimate beneficiary owners. Minimum zero, maximum four. — item shape: {date_of_birth?: string, name?: string, nationality?: string}
  --vat-number: record # shape: {country?: string, value?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/company"))
  let body = {"address_main": $address_main, "address_postal": $address_postal, "avatar_uuid": $avatar_uuid, "chamber_of_commerce_number": $chamber_of_commerce_number, "country": $country, "legal_form": $legal_form, "name": $name, "signup_track_type": $signup_track_type, "subscription_type": $subscription_type, "ubo": $ubo, "vat_number": $vat_number} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create and manage companies.
#
# GET /user/{userID}/company/{itemId}
# operationId: READ_Company_for_User
export def "user-company get-by-userID-itemId" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<UserCompany: record<address_main: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_postal: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, billing_contract: list<record>, chamber_of_commerce_number: string, counter_bank_iban: string, country: string, created: string, customer: record<billing_account_id: string, created: string, id: int, invoice_notification_preference: string, updated: string>, customer_limit: record<limit_amount_monthly: record, limit_card_debit_maestro: int, limit_card_debit_mastercard: int, limit_card_debit_wildcard: int, limit_card_replacement: int, limit_card_wildcard: int, limit_monetary_account: int, limit_monetary_account_remaining: int, spent_amount_monthly: record>, daily_limit_without_confirmation_login: record<currency: string, value: string>, deny_reason: string, directors: list<record>, display_name: string, id: int, language: string, legal_form: string, name: string, notification_filters: list<record>, public_nick_name: string, public_uuid: string, region: string, relations: list<record>, sector_of_industry: string, session_timeout: int, status: string, sub_status: string, tax_resident: list<record>, type_of_business_entity: string, ubo: list<record>, updated: string, version_terms_of_service: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/company/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create and manage companies.
#
# PUT /user/{userID}/company/{itemId}
# operationId: UPDATE_Company_for_User
# --address_main shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_postal shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --ubo item shape: {date_of_birth?: string, name?: string, nationality?: string}
# --vat_number shape: {country?: string, value?: string}
export def "user-company put-by-userID-itemId" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  address_main: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  address_postal: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --avatar-uuid: string # The public UUID of the company's avatar.
  --chamber-of-commerce-number: string # The company's chamber of commerce number.
  country: string # The country where the company is registered.
  legal_form: string # The company's legal form.
  name: string # The company name.
  --signup-track-type: string # The type of signup track the user is following.
  subscription_type: string # The subscription type for the company.
  --ubo: list # The names and birth dates of the company's ultimate beneficiary owners. Minimum zero, maximum four. — item shape: {date_of_birth?: string, name?: string, nationality?: string}
  --vat-number: record # shape: {country?: string, value?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/company/{item_id}"))
  let body = {"address_main": $address_main, "address_postal": $address_postal, "avatar_uuid": $avatar_uuid, "chamber_of_commerce_number": $chamber_of_commerce_number, "country": $country, "legal_form": $legal_form, "name": $name, "signup_track_type": $signup_track_type, "subscription_type": $subscription_type, "ubo": $ubo, "vat_number": $vat_number} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to confirm availability of funds on an account.
#
# POST /user/{userID}/confirmation-of-funds
# operationId: CREATE_ConfirmationOfFunds_for_User
# --amount shape: {currency?: string, value?: string}
# --pointer_iban shape: {name?: string, service?: string, type?: string, value?: string}
export def "user-confirmation-of-funds post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  amount: record # shape: {currency?: string, value?: string}
  pointer_iban: record # shape: {name?: string, service?: string, type?: string, value?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/confirmation-of-funds"))
  let body = {"amount": $amount, "pointer_iban": $pointer_iban} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a credential of a user for server authentication, or delete the credential of a user for server authentication.
#
# GET /user/{userID}/credential-password-ip
# operationId: List_all_CredentialPasswordIp_for_User
export def "user-credential-password-ip list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, expiry_time: string, id: int, permitted_device: record<description: string, ip: string>, status: string, token_value: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/credential-password-ip"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the IPs which may be used for a credential of a user for server authentication.
#
# GET /user/{userID}/credential-password-ip/{credential-password-ipID}/ip
# operationId: List_all_Ip_for_User_CredentialPasswordIp
export def "user-credential-password-ip-ip list" [
  user_id: int
  credential_password_ip_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<ip: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, credential_password_ip_id: $credential_password_ip_id} | format pattern "/user/{user_id}/credential-password-ip/{credential_password_ip_id}/ip"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the IPs which may be used for a credential of a user for server authentication.
#
# POST /user/{userID}/credential-password-ip/{credential-password-ipID}/ip
# operationId: CREATE_Ip_for_User_CredentialPasswordIp
export def "user-credential-password-ip-ip post" [
  user_id: int
  credential_password_ip_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  ip: string # The IP address.
  --status: string # The status of the IP. May be "ACTIVE" or "INACTIVE". It is only possible to make requests from "ACTIVE" IP addresses. Only "ACTIVE" IPs will be billed.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, credential_password_ip_id: $credential_password_ip_id} | format pattern "/user/{user_id}/credential-password-ip/{credential_password_ip_id}/ip"))
  let body = {"ip": $ip, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the IPs which may be used for a credential of a user for server authentication.
#
# GET /user/{userID}/credential-password-ip/{credential-password-ipID}/ip/{itemId}
# operationId: READ_Ip_for_User_CredentialPasswordIp
export def "user-credential-password-ip-ip get" [
  user_id: int
  credential_password_ip_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<ip: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, credential_password_ip_id: $credential_password_ip_id, item_id: $item_id} | format pattern "/user/{user_id}/credential-password-ip/{credential_password_ip_id}/ip/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the IPs which may be used for a credential of a user for server authentication.
#
# PUT /user/{userID}/credential-password-ip/{credential-password-ipID}/ip/{itemId}
# operationId: UPDATE_Ip_for_User_CredentialPasswordIp
export def "user-credential-password-ip-ip put" [
  user_id: int
  credential_password_ip_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  ip: string # The IP address.
  --status: string # The status of the IP. May be "ACTIVE" or "INACTIVE". It is only possible to make requests from "ACTIVE" IP addresses. Only "ACTIVE" IPs will be billed.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, credential_password_ip_id: $credential_password_ip_id, item_id: $item_id} | format pattern "/user/{user_id}/credential-password-ip/{credential_password_ip_id}/ip/{item_id}"))
  let body = {"ip": $ip, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a credential of a user for server authentication, or delete the credential of a user for server authentication.
#
# GET /user/{userID}/credential-password-ip/{itemId}
# operationId: READ_CredentialPasswordIp_for_User
export def "user-credential-password-ip get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, expiry_time: string, id: int, permitted_device: record<description: string, ip: string>, status: string, token_value: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/credential-password-ip/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint to manage CurrencyCloud beneficiaries.
#
# GET /user/{userID}/currency-cloud-beneficiary
# operationId: List_all_CurrencyCloudBeneficiary_for_User
export def "user-currency-cloud-beneficiary list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<account_number: string, created: string, currency: string, external_identifier: string, id: int, name: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/currency-cloud-beneficiary"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint to manage CurrencyCloud beneficiaries.
#
# POST /user/{userID}/currency-cloud-beneficiary
# operationId: CREATE_CurrencyCloudBeneficiary_for_User
export def "user-currency-cloud-beneficiary post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  all_field: list # All fields that were required by CurrencyCloud. Obtained through the CurrencyCloudBeneficiaryRequirement listing.
  country: string # The country of the beneficiary.
  currency: string # The currency of the beneficiary.
  legal_entity_type: string # The legal entity type of the beneficiary.
  name: string # The name of the beneficiary.
  payment_type: string # The payment type this requirement is for.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/currency-cloud-beneficiary"))
  let body = {"all_field": $all_field, "country": $country, "currency": $currency, "legal_entity_type": $legal_entity_type, "name": $name, "payment_type": $payment_type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint to list requirements for CurrencyCloud beneficiaries.
#
# GET /user/{userID}/currency-cloud-beneficiary-requirement
# operationId: List_all_CurrencyCloudBeneficiaryRequirement_for_User
export def "user-currency-cloud-beneficiary-requirement get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<all_field: list<record>, legal_entity_type: string, payment_type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/currency-cloud-beneficiary-requirement"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint to manage CurrencyCloud beneficiaries.
#
# GET /user/{userID}/currency-cloud-beneficiary/{itemId}
# operationId: READ_CurrencyCloudBeneficiary_for_User
export def "user-currency-cloud-beneficiary get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<account_number: string, created: string, currency: string, external_identifier: string, id: int, name: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/currency-cloud-beneficiary/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a collection of events for a given user. You can add query the parameters monetary_account_id, status and/or display_user_event to filter the response. When monetary_account_id={id,id} is provided only events that relate to these monetary account ids are returned. When status={AWAITING_REPLY/FINALIZED} is provided the response only contains events with the status AWAITING_REPLY or FINALIZED. When display_user_event={true/false} is set to false user events are excluded from the response, when not provided user events are displayed. User events are events that are not related to a monetary account (for example: connect invites).
#
# GET /user/{userID}/event
# operationId: List_all_Event_for_User
export def "user-event list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<action: string, created: string, id: int, monetary_account_id: string, object: record<BankSwitchServiceNetherlandsIncomingPayment: record, BunqMeFundraiserResult: record, BunqMeTab: record, BunqMeTabResultResponse: record, Card: record, CardDebit: record, DraftPayment: record, FeatureAnnouncement: record, IdealMerchantTransaction: record, Invoice: record, MasterCardAction: record, Payment: record, PaymentBatch: record, RequestInquiry: record, RequestInquiryBatch: record, RequestResponse: record, RewardRecipient: record, RewardSender: record, ScheduledInstance: record, ScheduledPayment: record, ScheduledPaymentBatch: record, ShareInviteBankInquiry: record, ShareInviteBankResponse: record, SofortMerchantTransaction: record, TransferwisePayment: record>, status: string, updated: string, user_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/event"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific event for a given user.
#
# GET /user/{userID}/event/{itemId}
# operationId: READ_Event_for_User
export def "user-event get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<action: string, created: string, id: int, monetary_account_id: string, object: record<BankSwitchServiceNetherlandsIncomingPayment: record<bank_switch_service: record, payment: record>, BunqMeFundraiserResult: record<bunqme_fundraiser_profile: record, created: string, id: int, payments: list, updated: string>, BunqMeTab: record<bunqme_tab_entry: record, status: string>, BunqMeTabResultResponse: record<payment: record>, Card: record<activation_code: string, card_limit: record, card_limit_atm: record, country_permission: list, monetary_account_id_fallback: int, order_status: string, pin_code: string, pin_code_assignment: list, primary_account_numbers: list, status: string>, CardDebit: record<alias: record, monetary_account_id_fallback: int, name_on_card: string, order_status: string, pin_code_assignment: list, preferred_name_on_card: string, product_type: string, second_line: string, type: string>, DraftPayment: record<entries: list, number_of_required_accepts: int, previous_updated_timestamp: string, schedule: record, status: string>, FeatureAnnouncement: record<avatar: record, sub_title: string, title: string, type: string>, IdealMerchantTransaction: record<alias: record, amount_guaranteed: record, amount_requested: record, counterparty_alias: record, expiration: string, issuer: string, issuer_authentication_url: string, issuer_name: string, monetary_account_id: int, purchase_identifier: string, status: string, status_timestamp: string, transaction_identifier: string>, Invoice: record<address: record, alias: record, category: string, chamber_of_commerce_number: string, counterparty_address: record, counterparty_alias: record, created: string, description: string, external_url: string, group: list, id: int, invoice_date: string, invoice_number: string, request_reference_split_the_bill: list, status: string, total_vat: record, total_vat_exclusive: record, total_vat_inclusive: record, updated: string, vat_number: string>, MasterCardAction: record<alias: record, all_mastercard_action_refund: list, amount_billing: record, amount_converted: record, amount_fee: record, amount_local: record, amount_original_billing: record, amount_original_local: record, applied_limit: string, authorisation_status: string, authorisation_type: string, card_authorisation_id_response: string, card_id: int, city: string, clearing_expiry_time: string, clearing_status: string, counterparty_alias: record, decision: string, decision_description: string, decision_description_translated: string, decision_together_url: string, description: string, eligible_whitelist_id: int, id: int, label_card: record, maturity_date: string, monetary_account_id: int, pan_entry_mode_user: string, payment_status: string, pos_card_holder_presence: string, pos_card_presence: string, request_reference_split_the_bill: list, reservation_expiry_time: string, secure_code_id: int, settlement_status: string, token_status: string, wallet_provider_id: string>, Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>, RequestInquiry: record<address_billing: record, address_shipping: record, allow_amount_higher: bool, allow_amount_lower: bool, allow_bunqme: bool, amount_inquired: record, amount_responded: record, attachment: list, batch_id: int, bunqme_share_url: string, counterparty_alias: record, created: string, description: string, event_id: int, geolocation: record, id: int, merchant_reference: string, minimum_age: int, monetary_account_id: int, redirect_url: string, reference_split_the_bill: record, require_address: string, scheduled_id: int, status: string, time_expiry: string, time_responded: string, updated: string, user_alias_created: record, user_alias_revoked: record, want_tip: bool>, RequestInquiryBatch: record<event_id: int, reference_split_the_bill: record, request_inquiries: list, status: string, total_amount_inquired: record>, RequestResponse: record<address_billing: record, address_shipping: record, alias: record, amount_inquired: record, amount_responded: record, attachment: list, counterparty_alias: record, created: string, credit_scheme_identifier: string, description: string, eligible_whitelist_id: int, event_id: int, geolocation: record, id: int, mandate_identifier: string, minimum_age: int, monetary_account_id: int, redirect_url: string, request_reference_split_the_bill: list, require_address: string, status: string, sub_type: string, time_expiry: string, time_refund_requested: string, time_refunded: string, time_responded: string, type: string, updated: string, user_refund_requested: record>, RewardRecipient: record<amount_reward: record, counterparty_alias: record, created: string, id: int, status: string, sub_status: string, type: string, updated: string>, RewardSender: record<amount_reward: record, counterparty_alias: record, created: string, id: int, status: string, sub_status: string, type: string, updated: string>, ScheduledInstance: record<error_message: list, request_reference_split_the_bill: list, result_object: record, scheduled_object: record, state: string, time_end: string, time_start: string>, ScheduledPayment: record<payment: record, schedule: record, status: string>, ScheduledPaymentBatch: record<payments: list, schedule: record>, ShareInviteBankInquiry: record<access_type: string, alias: record, counter_user_alias: record, draft_share_invite_bank_id: int, end_date: string, id: int, monetary_account_id: int, relationship: string, share_detail: record, share_type: string, start_date: string, status: string, user_alias_created: record, user_alias_revoked: record>, ShareInviteBankResponse: record<access_type: string, card_id: int, counter_alias: record, created: string, description: string, draft_share_invite_bank_id: int, end_date: string, id: int, monetary_account_id: int, relation_user: record, share_detail: record, share_type: string, start_date: string, status: string, updated: string, user_alias_cancelled: record>, SofortMerchantTransaction: record<alias: record, amount_guaranteed: record, amount_requested: record, counterparty_alias: record, error_message: list, issuer: string, issuer_authentication_url: string, monetary_account_id: int, status: string, transaction_identifier: string>, TransferwisePayment: record<alias: record, amount_source: record, amount_target: record, counterparty_alias: record, monetary_account_id: string, pay_in_reference: string, quote: record, rate: string, recipient_id: string, reference: string, status: string, status_transferwise: string, status_transferwise_issue: string, sub_status: string, time_delivery_estimate: string>>, status: string, updated: string, user_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/event/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List all the annual overviews for a user.
#
# GET /user/{userID}/export-annual-overview
# operationId: List_all_ExportAnnualOverview_for_User
export def "user-export-annual-overview list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, updated: string, year: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/export-annual-overview"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new annual overview for a specific year. An overview can be generated only for a past year.
#
# POST /user/{userID}/export-annual-overview
# operationId: CREATE_ExportAnnualOverview_for_User
export def "user-export-annual-overview post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  year: int # The year for which the overview is.
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/export-annual-overview"))
  let body = {"year": $year} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to retrieve the raw content of an annual overview.
#
# GET /user/{userID}/export-annual-overview/{export-annual-overviewID}/content
# operationId: List_all_Content_for_User_ExportAnnualOverview
export def "user-export-annual-overview-content export" [
  user_id: int
  export_annual_overview_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, export_annual_overview_id: $export_annual_overview_id} | format pattern "/user/{user_id}/export-annual-overview/{export_annual_overview_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing annual overviews of all the user's monetary accounts. Once created, annual overviews can be downloaded in PDF format via the 'export-annual-overview/{id}/content' endpoint.
#
# DELETE /user/{userID}/export-annual-overview/{itemId}
# operationId: DELETE_ExportAnnualOverview_for_User
export def "user-export-annual-overview delete" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/export-annual-overview/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get an annual overview for a user by its id.
#
# GET /user/{userID}/export-annual-overview/{itemId}
# operationId: READ_ExportAnnualOverview_for_User
export def "user-export-annual-overview get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias_user: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, updated: string, year: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/export-annual-overview/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# view for updating the feature display.
#
# GET /user/{userID}/feature-announcement/{itemId}
# operationId: READ_FeatureAnnouncement_for_User
export def "user-feature-announcement get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, sub_title: string, title: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/feature-announcement/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to allow users to set insight/budget preferences.
#
# GET /user/{userID}/insight-preference-date
# operationId: List_all_InsightPreferenceDate_for_User
export def "user-insight-preference-date get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<day_of_month: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/insight-preference-date"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to get insights about transactions between given time range.
#
# GET /user/{userID}/insights
# operationId: List_all_Insights_for_User
export def "user-insights get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<amount_total: record<currency: string, value: string>, category: string, category_translated: string, number_of_transactions: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/insights"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to get events based on time and insight category.
#
# GET /user/{userID}/insights-search
# operationId: List_all_InsightsSearch_for_User
export def "user-insights-search get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<action: string, created: string, id: int, monetary_account_id: string, object: record<BankSwitchServiceNetherlandsIncomingPayment: record, BunqMeFundraiserResult: record, BunqMeTab: record, BunqMeTabResultResponse: record, Card: record, CardDebit: record, DraftPayment: record, FeatureAnnouncement: record, IdealMerchantTransaction: record, Invoice: record, MasterCardAction: record, Payment: record, PaymentBatch: record, RequestInquiry: record, RequestInquiryBatch: record, RequestResponse: record, RewardRecipient: record, RewardSender: record, ScheduledInstance: record, ScheduledPayment: record, ScheduledPaymentBatch: record, ShareInviteBankInquiry: record, ShareInviteBankResponse: record, SofortMerchantTransaction: record, TransferwisePayment: record>, status: string, updated: string, user_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/insights-search"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to list bunq invoices by user.
#
# GET /user/{userID}/invoice
# operationId: List_all_Invoice_for_User
export def "user-invoice list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, chamber_of_commerce_number: string, counterparty_address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, group: list<record>, id: int, invoice_date: string, invoice_number: string, status: string, total_vat: record<currency: string, value: string>, total_vat_exclusive: record<currency: string, value: string>, total_vat_inclusive: record<currency: string, value: string>, updated: string, vat_number: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/invoice"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a PDF export of an invoice.
#
# GET /user/{userID}/invoice/{invoiceID}/pdf-content
# operationId: List_all_PdfContent_for_User_Invoice
export def "user-invoice-pdf-content get" [
  user_id: int
  invoice_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, invoice_id: $invoice_id} | format pattern "/user/{user_id}/invoice/{invoice_id}/pdf-content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to list bunq invoices by user.
#
# GET /user/{userID}/invoice/{itemId}
# operationId: READ_Invoice_for_User
export def "user-invoice get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, chamber_of_commerce_number: string, counterparty_address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, group: table<instance_description: string, item: list, product_vat_exclusive: record, product_vat_inclusive: record, type: string, type_description: string, type_description_translated: string>, id: int, invoice_date: string, invoice_number: string, status: string, total_vat: record<currency: string, value: string>, total_vat_exclusive: record<currency: string, value: string>, total_vat_inclusive: record<currency: string, value: string>, updated: string, vat_number: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/invoice/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for getting available legal names that can be used by the user.
#
# GET /user/{userID}/legal-name
# operationId: List_all_LegalName_for_User
export def "user-legal-name get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<legal_names: list<string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/legal-name"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get all limits for the authenticated user.
#
# GET /user/{userID}/limit
# operationId: List_all_Limit_for_User
export def "user-limit get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<limit_amount_monthly: record<currency: string, value: string>, limit_card_debit_maestro: int, limit_card_debit_mastercard: int, limit_card_debit_wildcard: int, limit_card_replacement: int, limit_card_wildcard: int, limit_monetary_account: int, limit_monetary_account_remaining: int, spent_amount_monthly: record<currency: string, value: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/limit"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a collection of all your MonetaryAccounts.
#
# GET /user/{userID}/monetary-account
# operationId: List_all_MonetaryAccount_for_User
export def "user-monetary-account list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<MonetaryAccountBank: record<avatar_uuid: string, country_iban: string, currency: string, daily_limit: record, description: string, display_name: string, reason: string, reason_description: string, setting: record, status: string, sub_status: string>, MonetaryAccountExternal: record<alias: list, all_auto_save_id: list, avatar: record, avatar_uuid: string, balance: record, created: string, currency: string, daily_limit: record, description: string, display_name: string, id: int, monetary_account_profile: record, overdraft_limit: record, public_uuid: string, reason: string, reason_description: string, setting: record, status: string, sub_status: string, updated: string, user_id: int>, MonetaryAccountInvestment: record<alias: list, all_auto_save_id: list, avatar: record, avatar_uuid: string, balance: record, birdee_investment_portfolio: record, created: string, currency: string, daily_limit: record, description: string, display_name: string, id: int, monetary_account_profile: record, provider: string, public_uuid: string, reason: string, reason_description: string, setting: record, status: string, sub_status: string, updated: string, user_id: int>, MonetaryAccountJoint: record<alias: list, all_co_owner: list, avatar_uuid: string, currency: string, daily_limit: record, description: string, overdraft_limit: record, reason: string, reason_description: string, setting: record, status: string, sub_status: string>, MonetaryAccountLight: record<alias: list, avatar: record, avatar_uuid: string, balance: record, balance_maximum: record, budget_month_maximum: record, budget_month_used: record, budget_withdrawal_year_maximum: record, budget_withdrawal_year_used: record, budget_year_maximum: record, budget_year_used: record, created: string, currency: string, daily_limit: record, description: string, id: int, public_uuid: string, reason: string, reason_description: string, setting: record, status: string, sub_status: string, updated: string, user_id: int>, MonetaryAccountSavings: record<all_co_owner: list, avatar_uuid: string, currency: string, daily_limit: record, description: string, reason: string, reason_description: string, savings_goal: record, setting: record, status: string, sub_status: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Gets a listing of all MonetaryAccountBanks of a given user.
#
# GET /user/{userID}/monetary-account-bank
# operationId: List_all_MonetaryAccountBank_for_User
export def "user-monetary-account-bank list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: list<record>, all_auto_save_id: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, display_name: string, id: int, monetary_account_profile: record<profile_drain: record, profile_fill: record>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account-bank"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create new MonetaryAccountBank.
#
# POST /user/{userID}/monetary-account-bank
# operationId: CREATE_MonetaryAccountBank_for_User
# --daily_limit shape: {currency?: string, value?: string}
# --setting shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
export def "user-monetary-account-bank post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --avatar-uuid: string # The UUID of the Avatar of the MonetaryAccountBank.
  --country-iban: string # The country of the monetary account IBAN.
  currency: string # The currency of the MonetaryAccountBank as an ISO 4217 formatted currency code.
  --daily-limit: record # shape: {currency?: string, value?: string}
  --description: string # The description of the MonetaryAccountBank. Defaults to 'bunq account'.
  --display-name: string # The legal name of the user / company using this monetary account.
  --reason: string # The reason for voluntarily cancelling (closing) the MonetaryAccountBank, can only be OTHER. Should only be specified if updating the status to CANCELLED.
  --reason-description: string # The optional free-form reason for voluntarily cancelling (closing) the MonetaryAccountBank. Can be any user provided message. Should only be specified if updating the status to CANCELLED.
  --setting: record # shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
  --status: string # The status of the MonetaryAccountBank. Ignored in POST requests (always set to ACTIVE) can be CANCELLED or PENDING_REOPEN in PUT requests to cancel (close) or reopen the MonetaryAccountBank. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
  --sub-status: string # The sub-status of the MonetaryAccountBank providing extra information regarding the status. Should be ignored for POST requests. In case of PUT requests with status CANCELLED it can only be REDEMPTION_VOLUNTARY, while with status PENDING_REOPEN it can only be NONE. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account-bank"))
  let body = {"avatar_uuid": $avatar_uuid, "country_iban": $country_iban, "currency": $currency, "daily_limit": $daily_limit, "description": $description, "display_name": $display_name, "reason": $reason, "reason_description": $reason_description, "setting": $setting, "status": $status, "sub_status": $sub_status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific MonetaryAccountBank.
#
# GET /user/{userID}/monetary-account-bank/{itemId}
# operationId: READ_MonetaryAccountBank_for_User
export def "user-monetary-account-bank get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: table<name: string, service: string, type: string, value: string>, all_auto_save_id: table<id: int>, avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, display_name: string, id: int, monetary_account_profile: record<profile_drain: record<balance_preferred: record, balance_threshold_high: record, savings_account_alias: record, status: string>, profile_fill: record<balance_preferred: record, balance_threshold_low: record, issuer: record, method_fill: string, status: string>>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account-bank/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a specific existing MonetaryAccountBank.
#
# PUT /user/{userID}/monetary-account-bank/{itemId}
# operationId: UPDATE_MonetaryAccountBank_for_User
# --daily_limit shape: {currency?: string, value?: string}
# --setting shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
export def "user-monetary-account-bank put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --avatar-uuid: string # The UUID of the Avatar of the MonetaryAccountBank.
  --country-iban: string # The country of the monetary account IBAN.
  currency: string # The currency of the MonetaryAccountBank as an ISO 4217 formatted currency code.
  --daily-limit: record # shape: {currency?: string, value?: string}
  --description: string # The description of the MonetaryAccountBank. Defaults to 'bunq account'.
  --display-name: string # The legal name of the user / company using this monetary account.
  --reason: string # The reason for voluntarily cancelling (closing) the MonetaryAccountBank, can only be OTHER. Should only be specified if updating the status to CANCELLED.
  --reason-description: string # The optional free-form reason for voluntarily cancelling (closing) the MonetaryAccountBank. Can be any user provided message. Should only be specified if updating the status to CANCELLED.
  --setting: record # shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
  --status: string # The status of the MonetaryAccountBank. Ignored in POST requests (always set to ACTIVE) can be CANCELLED or PENDING_REOPEN in PUT requests to cancel (close) or reopen the MonetaryAccountBank. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
  --sub-status: string # The sub-status of the MonetaryAccountBank providing extra information regarding the status. Should be ignored for POST requests. In case of PUT requests with status CANCELLED it can only be REDEMPTION_VOLUNTARY, while with status PENDING_REOPEN it can only be NONE. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account-bank/{item_id}"))
  let body = {"avatar_uuid": $avatar_uuid, "country_iban": $country_iban, "currency": $currency, "daily_limit": $daily_limit, "description": $description, "display_name": $display_name, "reason": $reason, "reason_description": $reason_description, "setting": $setting, "status": $status, "sub_status": $sub_status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint for managing monetary accounts which are connected to external services.
#
# GET /user/{userID}/monetary-account-external
# operationId: List_all_MonetaryAccountExternal_for_User
export def "user-monetary-account-external list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: list<record>, all_auto_save_id: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, display_name: string, id: int, monetary_account_profile: record<profile_drain: record, profile_fill: record>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account-external"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for managing monetary accounts which are connected to external services.
#
# GET /user/{userID}/monetary-account-external/{itemId}
# operationId: READ_MonetaryAccountExternal_for_User
export def "user-monetary-account-external get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: table<name: string, service: string, type: string, value: string>, all_auto_save_id: table<id: int>, avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, display_name: string, id: int, monetary_account_profile: record<profile_drain: record<balance_preferred: record, balance_threshold_high: record, savings_account_alias: record, status: string>, profile_fill: record<balance_preferred: record, balance_threshold_low: record, issuer: record, method_fill: string, status: string>>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account-external/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# The endpoint for joint monetary accounts.
#
# GET /user/{userID}/monetary-account-joint
# operationId: List_all_MonetaryAccountJoint_for_User
export def "user-monetary-account-joint list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: list<record>, all_auto_save_id: list<record>, all_co_owner: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, id: int, monetary_account_profile: record<profile_drain: record, profile_fill: record>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account-joint"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# The endpoint for joint monetary accounts.
#
# POST /user/{userID}/monetary-account-joint
# operationId: CREATE_MonetaryAccountJoint_for_User
# --alias item shape: {name?: string, service?: string, type?: string, value?: string}
# --all_co_owner item shape: {alias?: record}
# --daily_limit shape: {currency?: string, value?: string}
# --overdraft_limit shape: {currency?: string, value?: string}
# --setting shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
export def "user-monetary-account-joint post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --alias: list # The Aliases to add to MonetaryAccountJoint. Must all be confirmed first. Can mostly be ignored. — item shape: {name?: string, service?: string, type?: string, value?: string}
  all_co_owner: list # The users the account will be joint with. — item shape: {alias?: record}
  --avatar-uuid: string # The UUID of the Avatar of the MonetaryAccountJoint.
  currency: string # The currency of the MonetaryAccountJoint as an ISO 4217 formatted currency code.
  --daily-limit: record # shape: {currency?: string, value?: string}
  --description: string # The description of the MonetaryAccountJoint. Defaults to 'bunq account'.
  --overdraft-limit: record # shape: {currency?: string, value?: string}
  --reason: string # The reason for voluntarily cancelling (closing) the MonetaryAccountJoint, can only be OTHER. Should only be specified if updating the status to CANCELLED.
  --reason-description: string # The optional free-form reason for voluntarily cancelling (closing) the MonetaryAccountJoint. Can be any user provided message. Should only be specified if updating the status to CANCELLED.
  --setting: record # shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
  --status: string # The status of the MonetaryAccountJoint. Ignored in POST requests (always set to ACTIVE) can be CANCELLED or PENDING_REOPEN in PUT requests to cancel (close) or reopen the MonetaryAccountJoint. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
  --sub-status: string # The sub-status of the MonetaryAccountJoint providing extra information regarding the status. Should be ignored for POST requests. In case of PUT requests with status CANCELLED it can only be REDEMPTION_VOLUNTARY, while with status PENDING_REOPEN it can only be NONE. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account-joint"))
  let body = {"alias": $alias, "all_co_owner": $all_co_owner, "avatar_uuid": $avatar_uuid, "currency": $currency, "daily_limit": $daily_limit, "description": $description, "overdraft_limit": $overdraft_limit, "reason": $reason, "reason_description": $reason_description, "setting": $setting, "status": $status, "sub_status": $sub_status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# The endpoint for joint monetary accounts.
#
# GET /user/{userID}/monetary-account-joint/{itemId}
# operationId: READ_MonetaryAccountJoint_for_User
export def "user-monetary-account-joint get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: table<name: string, service: string, type: string, value: string>, all_auto_save_id: table<id: int>, all_co_owner: table<alias: record, status: string>, avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, id: int, monetary_account_profile: record<profile_drain: record<balance_preferred: record, balance_threshold_high: record, savings_account_alias: record, status: string>, profile_fill: record<balance_preferred: record, balance_threshold_low: record, issuer: record, method_fill: string, status: string>>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account-joint/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# The endpoint for joint monetary accounts.
#
# PUT /user/{userID}/monetary-account-joint/{itemId}
# operationId: UPDATE_MonetaryAccountJoint_for_User
# --alias item shape: {name?: string, service?: string, type?: string, value?: string}
# --all_co_owner item shape: {alias?: record}
# --daily_limit shape: {currency?: string, value?: string}
# --overdraft_limit shape: {currency?: string, value?: string}
# --setting shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
export def "user-monetary-account-joint put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --alias: list # The Aliases to add to MonetaryAccountJoint. Must all be confirmed first. Can mostly be ignored. — item shape: {name?: string, service?: string, type?: string, value?: string}
  all_co_owner: list # The users the account will be joint with. — item shape: {alias?: record}
  --avatar-uuid: string # The UUID of the Avatar of the MonetaryAccountJoint.
  currency: string # The currency of the MonetaryAccountJoint as an ISO 4217 formatted currency code.
  --daily-limit: record # shape: {currency?: string, value?: string}
  --description: string # The description of the MonetaryAccountJoint. Defaults to 'bunq account'.
  --overdraft-limit: record # shape: {currency?: string, value?: string}
  --reason: string # The reason for voluntarily cancelling (closing) the MonetaryAccountJoint, can only be OTHER. Should only be specified if updating the status to CANCELLED.
  --reason-description: string # The optional free-form reason for voluntarily cancelling (closing) the MonetaryAccountJoint. Can be any user provided message. Should only be specified if updating the status to CANCELLED.
  --setting: record # shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
  --status: string # The status of the MonetaryAccountJoint. Ignored in POST requests (always set to ACTIVE) can be CANCELLED or PENDING_REOPEN in PUT requests to cancel (close) or reopen the MonetaryAccountJoint. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
  --sub-status: string # The sub-status of the MonetaryAccountJoint providing extra information regarding the status. Should be ignored for POST requests. In case of PUT requests with status CANCELLED it can only be REDEMPTION_VOLUNTARY, while with status PENDING_REOPEN it can only be NONE. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account-joint/{item_id}"))
  let body = {"alias": $alias, "all_co_owner": $all_co_owner, "avatar_uuid": $avatar_uuid, "currency": $currency, "daily_limit": $daily_limit, "description": $description, "overdraft_limit": $overdraft_limit, "reason": $reason, "reason_description": $reason_description, "setting": $setting, "status": $status, "sub_status": $sub_status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Gets a listing of all MonetaryAccountSavingss of a given user.
#
# GET /user/{userID}/monetary-account-savings
# operationId: List_all_MonetaryAccountSavings_for_User
export def "user-monetary-account-savings list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: list<record>, all_auto_save_id: list<record>, all_co_owner: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, id: int, monetary_account_profile: record<profile_drain: record, profile_fill: record>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, savings_goal: record<currency: string, value: string>, savings_goal_progress: int, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account-savings"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create new MonetaryAccountSavings.
#
# POST /user/{userID}/monetary-account-savings
# operationId: CREATE_MonetaryAccountSavings_for_User
# --all_co_owner item shape: {alias?: record}
# --daily_limit shape: {currency?: string, value?: string}
# --savings_goal shape: {currency?: string, value?: string}
# --setting shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
export def "user-monetary-account-savings post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --all-co-owner: list # The users the account will be joint with. — item shape: {alias?: record}
  --avatar-uuid: string # The UUID of the Avatar of the MonetaryAccountSavings.
  currency: string # The currency of the MonetaryAccountSavings as an ISO 4217 formatted currency code.
  --daily-limit: record # shape: {currency?: string, value?: string}
  --description: string # The description of the MonetaryAccountSavings. Defaults to 'bunq account'.
  --reason: string # The reason for voluntarily cancelling (closing) the MonetaryAccountSavings, can only be OTHER. Should only be specified if updating the status to CANCELLED.
  --reason-description: string # The optional free-form reason for voluntarily cancelling (closing) the MonetaryAccountSavings. Can be any user provided message. Should only be specified if updating the status to CANCELLED.
  --savings-goal: record # shape: {currency?: string, value?: string}
  --setting: record # shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
  --status: string # The status of the MonetaryAccountSavings. Ignored in POST requests (always set to ACTIVE) can be CANCELLED or PENDING_REOPEN in PUT requests to cancel (close) or reopen the MonetaryAccountSavings. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
  --sub-status: string # The sub-status of the MonetaryAccountSavings providing extra information regarding the status. Should be ignored for POST requests. In case of PUT requests with status CANCELLED it can only be REDEMPTION_VOLUNTARY, while with status PENDING_REOPEN it can only be NONE. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/monetary-account-savings"))
  let body = {"all_co_owner": $all_co_owner, "avatar_uuid": $avatar_uuid, "currency": $currency, "daily_limit": $daily_limit, "description": $description, "reason": $reason, "reason_description": $reason_description, "savings_goal": $savings_goal, "setting": $setting, "status": $status, "sub_status": $sub_status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific MonetaryAccountSavings.
#
# GET /user/{userID}/monetary-account-savings/{itemId}
# operationId: READ_MonetaryAccountSavings_for_User
export def "user-monetary-account-savings get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: table<name: string, service: string, type: string, value: string>, all_auto_save_id: table<id: int>, all_co_owner: table<alias: record, status: string>, avatar: record<anchor_uuid: string, image: list<record>, style: string, uuid: string>, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, id: int, monetary_account_profile: record<profile_drain: record<balance_preferred: record, balance_threshold_high: record, savings_account_alias: record, status: string>, profile_fill: record<balance_preferred: record, balance_threshold_low: record, issuer: record, method_fill: string, status: string>>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, savings_goal: record<currency: string, value: string>, savings_goal_progress: int, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account-savings/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a specific existing MonetaryAccountSavings.
#
# PUT /user/{userID}/monetary-account-savings/{itemId}
# operationId: UPDATE_MonetaryAccountSavings_for_User
# --all_co_owner item shape: {alias?: record}
# --daily_limit shape: {currency?: string, value?: string}
# --savings_goal shape: {currency?: string, value?: string}
# --setting shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
export def "user-monetary-account-savings put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --all-co-owner: list # The users the account will be joint with. — item shape: {alias?: record}
  --avatar-uuid: string # The UUID of the Avatar of the MonetaryAccountSavings.
  currency: string # The currency of the MonetaryAccountSavings as an ISO 4217 formatted currency code.
  --daily-limit: record # shape: {currency?: string, value?: string}
  --description: string # The description of the MonetaryAccountSavings. Defaults to 'bunq account'.
  --reason: string # The reason for voluntarily cancelling (closing) the MonetaryAccountSavings, can only be OTHER. Should only be specified if updating the status to CANCELLED.
  --reason-description: string # The optional free-form reason for voluntarily cancelling (closing) the MonetaryAccountSavings. Can be any user provided message. Should only be specified if updating the status to CANCELLED.
  --savings-goal: record # shape: {currency?: string, value?: string}
  --setting: record # shape: {color?: string, default_avatar_status?: string, icon?: string, restriction_chat?: string, sdd_expiration_action?: string}
  --status: string # The status of the MonetaryAccountSavings. Ignored in POST requests (always set to ACTIVE) can be CANCELLED or PENDING_REOPEN in PUT requests to cancel (close) or reopen the MonetaryAccountSavings. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
  --sub-status: string # The sub-status of the MonetaryAccountSavings providing extra information regarding the status. Should be ignored for POST requests. In case of PUT requests with status CANCELLED it can only be REDEMPTION_VOLUNTARY, while with status PENDING_REOPEN it can only be NONE. When updating the status and/or sub_status no other fields can be updated in the same request (and vice versa).
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account-savings/{item_id}"))
  let body = {"all_co_owner": $all_co_owner, "avatar_uuid": $avatar_uuid, "currency": $currency, "daily_limit": $daily_limit, "description": $description, "reason": $reason, "reason_description": $reason_description, "savings_goal": $savings_goal, "setting": $setting, "status": $status, "sub_status": $sub_status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific MonetaryAccount.
#
# GET /user/{userID}/monetary-account/{itemId}
# operationId: READ_MonetaryAccount_for_User
export def "user-monetary-account get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<MonetaryAccountBank: record<avatar_uuid: string, country_iban: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, display_name: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string>, MonetaryAccountExternal: record<alias: list<record>, all_auto_save_id: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, balance: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, display_name: string, id: int, monetary_account_profile: record<profile_drain: record, profile_fill: record>, overdraft_limit: record<currency: string, value: string>, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int>, MonetaryAccountInvestment: record<alias: list<record>, all_auto_save_id: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, balance: record<currency: string, value: string>, birdee_investment_portfolio: record<allocations: list, balance: record, goal: record, investment_theme: string, name: string, number_of_strategy_change_annual_maximum: int, number_of_strategy_change_annual_used: int, risk_profile_type: string, status: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, display_name: string, id: int, monetary_account_profile: record<profile_drain: record, profile_fill: record>, provider: string, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int>, MonetaryAccountJoint: record<alias: list<record>, all_co_owner: list<record>, avatar_uuid: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, overdraft_limit: record<currency: string, value: string>, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string>, MonetaryAccountLight: record<alias: list<record>, avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, avatar_uuid: string, balance: record<currency: string, value: string>, balance_maximum: record<currency: string, value: string>, budget_month_maximum: record<currency: string, value: string>, budget_month_used: record<currency: string, value: string>, budget_withdrawal_year_maximum: record<currency: string, value: string>, budget_withdrawal_year_used: record<currency: string, value: string>, budget_year_maximum: record<currency: string, value: string>, budget_year_used: record<currency: string, value: string>, created: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, id: int, public_uuid: string, reason: string, reason_description: string, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string, updated: string, user_id: int>, MonetaryAccountSavings: record<all_co_owner: list<record>, avatar_uuid: string, currency: string, daily_limit: record<currency: string, value: string>, description: string, reason: string, reason_description: string, savings_goal: record<currency: string, value: string>, setting: record<color: string, default_avatar_status: string, icon: string, restriction_chat: string, sdd_expiration_action: string>, status: string, sub_status: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new monetary account attachment. Create a POST request with a payload that contains the binary representation of the file, without any JSON wrapping. Make sure you define the MIME type (i.e. image/jpeg) in the Content-Type header. You are required to provide a description of the attachment using the X-Bunq-Attachment-Description header.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/attachment
# operationId: CREATE_Attachment_for_User_MonetaryAccount
export def "user-monetary-account-attachment post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/attachment"))
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the raw content of a specific attachment.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/attachment/{attachmentID}/content
# operationId: List_all_Content_for_User_MonetaryAccount_Attachment
export def "user-monetary-account-attachment-content attach-ment" [
  user_id: int
  monetary_account_id: int
  attachment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, attachment_id: $attachment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/attachment/{attachment_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-attachment list" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-attachment post" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-attachment get" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-attachment put" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-text list" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-text post" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-text delete" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-text get" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{bunqme-fundraiser-resultID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_BunqmeFundraiserResult
export def "user-monetary-account-bunqme-fundraiser-result-note-text put" [
  user_id: int
  monetary_account_id: int
  bunqme_fundraiser_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, bunqme_fundraiser_result_id: $bunqme_fundraiser_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{bunqme_fundraiser_result_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# bunq.me fundraiser result containing all payments.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-fundraiser-result/{itemId}
# operationId: READ_BunqmeFundraiserResult_for_User_MonetaryAccount
export def "user-monetary-account-bunqme-fundraiser-result get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<bunqme_fundraiser_profile: record<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, attachment: record, color: string, currency: string, description: string, invite_profile_name: string, pointer: record<name: string, service: string, type: string, value: string>, redirect_url: string, status: string>, created: string, id: int, payments: table<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-fundraiser-result/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# bunq.me tabs allows you to create a payment request and share the link through e-mail, chat, etc. Multiple persons are able to respond to the payment request and pay through bunq, iDeal or SOFORT.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-tab
# operationId: List_all_BunqmeTab_for_User_MonetaryAccount
export def "user-monetary-account-bunqme-tab list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias_monetary_account: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, bunqme_tab_entries: list<record>, bunqme_tab_entry: record<alias: record, amount_inquired: record, description: string, invite_profile_name: string, merchant_available: list, redirect_url: string, status: string, uuid: string>, bunqme_tab_share_url: string, created: string, id: int, monetary_account_id: int, result_inquiries: list<record>, status: string, time_expiry: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-tab"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# bunq.me tabs allows you to create a payment request and share the link through e-mail, chat, etc. Multiple persons are able to respond to the payment request and pay through bunq, iDeal or SOFORT.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/bunqme-tab
# operationId: CREATE_BunqmeTab_for_User_MonetaryAccount
# --bunqme_tab_entry shape: {alias?: record, amount_inquired?: record, description?: string, redirect_url?: string}
export def "user-monetary-account-bunqme-tab post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  bunqme_tab_entry: record # shape: {alias?: record, amount_inquired?: record, description?: string, redirect_url?: string}
  --status: string # The status of the bunq.me. Ignored in POST requests but can be used for cancelling the bunq.me by setting status as CANCELLED with a PUT request.
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-tab"))
  let body = {"bunqme_tab_entry": $bunqme_tab_entry, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to view bunq.me TabResultResponse objects belonging to a tab. A TabResultResponse is an object that holds details on a tab which has been paid from the provided monetary account.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-tab-result-response/{itemId}
# operationId: READ_BunqmeTabResultResponse_for_User_MonetaryAccount
export def "user-monetary-account-bunqme-tab-result-response get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<payment: record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, allow_bunqto: bool, amount: record<currency: string, value: string>, attachment: list<record>, balance_after_mutation: record<currency: string, value: string>, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record<created: string, error_message: list, id: int, payment_auto_allocate_id: int, payment_batch: record, payment_id: int, status: string, updated: string>, request_reference_split_the_bill: list<record>, scheduled_id: int, sub_type: string, type: string, updated: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-tab-result-response/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# bunq.me tabs allows you to create a payment request and share the link through e-mail, chat, etc. Multiple persons are able to respond to the payment request and pay through bunq, iDeal or SOFORT.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/bunqme-tab/{itemId}
# operationId: READ_BunqmeTab_for_User_MonetaryAccount
export def "user-monetary-account-bunqme-tab get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias_monetary_account: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, bunqme_tab_entries: table<alias: record, amount_inquired: record, description: string, invite_profile_name: string, merchant_available: list, redirect_url: string, status: string, uuid: string>, bunqme_tab_entry: record<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_inquired: record<currency: string, value: string>, description: string, invite_profile_name: string, merchant_available: list<record>, redirect_url: string, status: string, uuid: string>, bunqme_tab_share_url: string, created: string, id: int, monetary_account_id: int, result_inquiries: table<bunq_me_tab_id: int, payment: record>, status: string, time_expiry: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-tab/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# bunq.me tabs allows you to create a payment request and share the link through e-mail, chat, etc. Multiple persons are able to respond to the payment request and pay through bunq, iDeal or SOFORT.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/bunqme-tab/{itemId}
# operationId: UPDATE_BunqmeTab_for_User_MonetaryAccount
# --bunqme_tab_entry shape: {alias?: record, amount_inquired?: record, description?: string, redirect_url?: string}
export def "user-monetary-account-bunqme-tab put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  bunqme_tab_entry: record # shape: {alias?: record, amount_inquired?: record, description?: string, redirect_url?: string}
  --status: string # The status of the bunq.me. Ignored in POST requests but can be used for cancelling the bunq.me by setting status as CANCELLED with a PUT request.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/bunqme-tab/{item_id}"))
  let body = {"bunqme_tab_entry": $bunqme_tab_entry, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint for managing currency conversions.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/currency-cloud-payment-quote
# operationId: CREATE_CurrencyCloudPaymentQuote_for_User_MonetaryAccount
# --pointers item shape: {name?: string, service?: string, type?: string, value?: string}
export def "user-monetary-account-currency-cloud-payment-quote post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  pointers: list # The points we want to know the fees for. — item shape: {name?: string, service?: string, type?: string, value?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/currency-cloud-payment-quote"))
  let body = {"pointers": $pointers} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint for managing currency conversions.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/currency-conversion
# operationId: List_all_CurrencyConversion_for_User_MonetaryAccount
export def "user-monetary-account-currency-conversion list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<amount: record<currency: string, value: string>, counter_amount: record<currency: string, value: string>, counter_label_monetary_account: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, date_delivery_expected: string, group_uuid: string, id: int, label_monetary_account: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, rate: string, status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/currency-conversion"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint to create a quote for currency conversions.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/currency-conversion-quote
# operationId: CREATE_CurrencyConversionQuote_for_User_MonetaryAccount
# --amount shape: {currency?: string, value?: string}
# --counterparty_alias shape: {name?: string, service?: string, type?: string, value?: string}
export def "user-monetary-account-currency-conversion-quote post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  amount: record # shape: {currency?: string, value?: string}
  counterparty_alias: record # shape: {name?: string, service?: string, type?: string, value?: string}
  currency_source: string # The currency we are converting.
  currency_target: string # The currency we are converting towards.
  --status: string # The status of the quote.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/currency-conversion-quote"))
  let body = {"amount": $amount, "counterparty_alias": $counterparty_alias, "currency_source": $currency_source, "currency_target": $currency_target, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint to create a quote for currency conversions.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/currency-conversion-quote/{itemId}
# operationId: READ_CurrencyConversionQuote_for_User_MonetaryAccount
export def "user-monetary-account-currency-conversion-quote get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount_source: record<currency: string, value: string>, amount_target: record<currency: string, value: string>, created: string, id: int, rate: string, status: string, time_expiry: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/currency-conversion-quote/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint to create a quote for currency conversions.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/currency-conversion-quote/{itemId}
# operationId: UPDATE_CurrencyConversionQuote_for_User_MonetaryAccount
# --amount shape: {currency?: string, value?: string}
# --counterparty_alias shape: {name?: string, service?: string, type?: string, value?: string}
export def "user-monetary-account-currency-conversion-quote put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  amount: record # shape: {currency?: string, value?: string}
  counterparty_alias: record # shape: {name?: string, service?: string, type?: string, value?: string}
  currency_source: string # The currency we are converting.
  currency_target: string # The currency we are converting towards.
  --status: string # The status of the quote.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/currency-conversion-quote/{item_id}"))
  let body = {"amount": $amount, "counterparty_alias": $counterparty_alias, "currency_source": $currency_source, "currency_target": $currency_target, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint for managing currency conversions.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/currency-conversion/{itemId}
# operationId: READ_CurrencyConversion_for_User_MonetaryAccount
export def "user-monetary-account-currency-conversion get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount: record<currency: string, value: string>, counter_amount: record<currency: string, value: string>, counter_label_monetary_account: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, date_delivery_expected: string, group_uuid: string, id: int, label_monetary_account: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, payment: record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, allow_bunqto: bool, amount: record<currency: string, value: string>, attachment: list<record>, balance_after_mutation: record<currency: string, value: string>, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record<created: string, error_message: list, id: int, payment_auto_allocate_id: int, payment_batch: record, payment_id: int, status: string, updated: string>, request_reference_split_the_bill: list<record>, scheduled_id: int, sub_type: string, type: string, updated: string>, rate: string, status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/currency-conversion/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing statement exports. Statement exports can be created in either CSV, MT940 or PDF file format.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/customer-statement
# operationId: List_all_CustomerStatement_for_User_MonetaryAccount
export def "user-monetary-account-customer-statement list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias_monetary_account: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, date_end: string, date_start: string, id: int, regional_format: string, statement_format: string, statement_number: int, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/customer-statement"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing statement exports. Statement exports can be created in either CSV, MT940 or PDF file format.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/customer-statement
# operationId: CREATE_CustomerStatement_for_User_MonetaryAccount
export def "user-monetary-account-customer-statement post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  date_end: string # The end date for making statements.
  date_start: string # The start date for making statements.
  --include-attachment: oneof<nothing, bool> # Only for PDF exports. Includes attachments to mutations in the export, such as scanned receipts.
  --regional-format: string # Required for CSV exports. The regional format of the statement, can be UK_US (comma-separated) or EUROPEAN (semicolon-separated).
  statement_format: string # The format type of statement. Allowed values: MT940, CSV, PDF.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/customer-statement"))
  let body = {"date_end": $date_end, "date_start": $date_start, "include_attachment": $include_attachment, "regional_format": $regional_format, "statement_format": $statement_format} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Fetch the raw content of a statement export. The returned file format could be MT940, CSV or PDF depending on the statement format specified during the statement creation. The doc won't display the response of a request to get the content of a statement export.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/customer-statement/{customer-statementID}/content
# operationId: List_all_Content_for_User_MonetaryAccount_CustomerStatement
export def "user-monetary-account-customer-statement-content get" [
  user_id: int
  monetary_account_id: int
  customer_statement_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, customer_statement_id: $customer_statement_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/customer-statement/{customer_statement_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing statement exports. Statement exports can be created in either CSV, MT940 or PDF file format.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/customer-statement/{itemId}
# operationId: DELETE_CustomerStatement_for_User_MonetaryAccount
export def "user-monetary-account-customer-statement delete" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/customer-statement/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing statement exports. Statement exports can be created in either CSV, MT940 or PDF file format.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/customer-statement/{itemId}
# operationId: READ_CustomerStatement_for_User_MonetaryAccount
export def "user-monetary-account-customer-statement get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias_monetary_account: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, date_end: string, date_start: string, id: int, regional_format: string, statement_format: string, statement_number: int, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/customer-statement/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a listing of all DraftPayments from a given MonetaryAccount.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/draft-payment
# operationId: List_all_DraftPayment_for_User_MonetaryAccount
export def "user-monetary-account-draft-payment list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<entries: list<record>, id: int, monetary_account_id: int, object: record<Payment: record, PaymentBatch: record>, request_reference_split_the_bill: list<record>, responses: list<record>, schedule: record<object: record, recurrence_size: int, recurrence_unit: string, status: string, time_end: string, time_start: string>, status: string, type: string, user_alias_created: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new DraftPayment.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/draft-payment
# operationId: CREATE_DraftPayment_for_User_MonetaryAccount
# --entries item shape: {alias?: record, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
# --schedule shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
export def "user-monetary-account-draft-payment post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  entries: list # The list of entries in the DraftPayment. Each entry will result in a payment when the DraftPayment is accepted. — item shape: {alias?: record, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
  number_of_required_accepts: int # The number of accepts that are required for the draft payment to receive status ACCEPTED. Currently only 1 is valid.
  --previous-updated-timestamp: string # The last updated_timestamp that you received for this DraftPayment. This needs to be provided to prevent race conditions.
  --schedule: record # shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
  --status: string # The status of the DraftPayment.
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment"))
  let body = {"entries": $entries, "number_of_required_accepts": $number_of_required_accepts, "previous_updated_timestamp": $previous_updated_timestamp, "schedule": $schedule, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-attachment list" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-attachment post" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-attachment get" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-attachment put" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-text list" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-text post" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-text delete" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-text get" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{draft-paymentID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_DraftPayment
export def "user-monetary-account-draft-payment-note-text put" [
  user_id: int
  monetary_account_id: int
  draft_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, draft_payment_id: $draft_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{draft_payment_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific DraftPayment.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{itemId}
# operationId: READ_DraftPayment_for_User_MonetaryAccount
export def "user-monetary-account-draft-payment get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<entries: table<alias: record, amount: record, attachment: list, counterparty_alias: record, description: string, id: int, merchant_reference: string, type: string>, id: int, monetary_account_id: int, object: record<Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>>, request_reference_split_the_bill: table<id: int, type: string>, responses: table<status: string, user_alias_created: record>, schedule: record<object: record<Payment: record, PaymentBatch: record>, recurrence_size: int, recurrence_unit: string, status: string, time_end: string, time_start: string>, status: string, type: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update a DraftPayment.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/draft-payment/{itemId}
# operationId: UPDATE_DraftPayment_for_User_MonetaryAccount
# --entries item shape: {alias?: record, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
# --schedule shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
export def "user-monetary-account-draft-payment put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  entries: list # The list of entries in the DraftPayment. Each entry will result in a payment when the DraftPayment is accepted. — item shape: {alias?: record, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
  number_of_required_accepts: int # The number of accepts that are required for the draft payment to receive status ACCEPTED. Currently only 1 is valid.
  --previous-updated-timestamp: string # The last updated_timestamp that you received for this DraftPayment. This needs to be provided to prevent race conditions.
  --schedule: record # shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
  --status: string # The status of the DraftPayment.
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/draft-payment/{item_id}"))
  let body = {"entries": $entries, "number_of_required_accepts": $number_of_required_accepts, "previous_updated_timestamp": $previous_updated_timestamp, "schedule": $schedule, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to create a statement export of a single payment.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/event/{eventID}/statement
# operationId: CREATE_Statement_for_User_MonetaryAccount_Event
export def "user-monetary-account-event-statement post" [
  user_id: int
  monetary_account_id: int
  event_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, event_id: $event_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/event/{event_id}/statement"))
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to create a statement export of a single payment.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/event/{eventID}/statement/{itemId}
# operationId: READ_Statement_for_User_MonetaryAccount_Event
export def "user-monetary-account-event-statement get" [
  user_id: int
  monetary_account_id: int
  event_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, id: int, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, event_id: $event_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/event/{event_id}/statement/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Fetch the raw content of a payment statement export.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/event/{eventID}/statement/{statementID}/content
# operationId: List_all_Content_for_User_MonetaryAccount_Event_Statement
export def "user-monetary-account-event-statement-content get" [
  user_id: int
  monetary_account_id: int
  event_id: int
  statement_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, event_id: $event_id, statement_id: $statement_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/event/{event_id}/statement/{statement_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List all the RIBs for a monetary account.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/export-rib
# operationId: List_all_ExportRib_for_User_MonetaryAccount
export def "user-monetary-account-export-rib list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, id: int, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/export-rib"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new RIB.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/export-rib
# operationId: CREATE_ExportRib_for_User_MonetaryAccount
export def "user-monetary-account-export-rib post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/export-rib"))
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to retrieve the raw content of an RIB.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/export-rib/{export-ribID}/content
# operationId: List_all_Content_for_User_MonetaryAccount_ExportRib
export def "user-monetary-account-export-rib-content export" [
  user_id: int
  monetary_account_id: int
  export_rib_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, export_rib_id: $export_rib_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/export-rib/{export_rib_id}/content"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create new and read existing RIBs of a monetary account
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/export-rib/{itemId}
# operationId: DELETE_ExportRib_for_User_MonetaryAccount
export def "user-monetary-account-export-rib delete" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/export-rib/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a RIB for a monetary account by its id.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/export-rib/{itemId}
# operationId: READ_ExportRib_for_User_MonetaryAccount
export def "user-monetary-account-export-rib get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, id: int, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/export-rib/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# View for requesting iDEAL transactions and polling their status.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction
# operationId: List_all_IdealMerchantTransaction_for_User_MonetaryAccount
export def "user-monetary-account-ideal-merchant-transaction list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_guaranteed: record<currency: string, value: string>, amount_requested: record<currency: string, value: string>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, expiration: string, issuer: string, issuer_authentication_url: string, issuer_name: string, monetary_account_id: int, purchase_identifier: string, status: string, status_timestamp: string, transaction_identifier: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# View for requesting iDEAL transactions and polling their status.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction
# operationId: CREATE_IdealMerchantTransaction_for_User_MonetaryAccount
# --alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --amount_guaranteed shape: {currency?: string, value?: string}
# --amount_requested shape: {currency?: string, value?: string}
# --counterparty_alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
export def "user-monetary-account-ideal-merchant-transaction post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --amount-guaranteed: record # shape: {currency?: string, value?: string}
  --amount-requested: record # shape: {currency?: string, value?: string}
  --counterparty-alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --issuer: string # The BIC of the issuer.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction"))
  let body = {"alias": $alias, "amount_guaranteed": $amount_guaranteed, "amount_requested": $amount_requested, "counterparty_alias": $counterparty_alias, "issuer": $issuer} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-attachment list" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-attachment post" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-attachment get" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-attachment put" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-text list" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-text post" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-text delete" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-text get" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{ideal-merchant-transactionID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_IdealMerchantTransaction
export def "user-monetary-account-ideal-merchant-transaction-note-text put" [
  user_id: int
  monetary_account_id: int
  ideal_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, ideal_merchant_transaction_id: $ideal_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{ideal_merchant_transaction_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# View for requesting iDEAL transactions and polling their status.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/ideal-merchant-transaction/{itemId}
# operationId: READ_IdealMerchantTransaction_for_User_MonetaryAccount
export def "user-monetary-account-ideal-merchant-transaction get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_guaranteed: record<currency: string, value: string>, amount_requested: record<currency: string, value: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, expiration: string, issuer: string, issuer_authentication_url: string, issuer_name: string, monetary_account_id: int, purchase_identifier: string, status: string, status_timestamp: string, transaction_identifier: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/ideal-merchant-transaction/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view a bunq invoice.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/invoice
# operationId: List_all_Invoice_for_User_MonetaryAccount
export def "user-monetary-account-invoice list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, category: string, chamber_of_commerce_number: string, counterparty_address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, group: list<record>, id: int, invoice_date: string, invoice_number: string, request_reference_split_the_bill: list<record>, status: string, total_vat: record<currency: string, value: string>, total_vat_exclusive: record<currency: string, value: string>, total_vat_inclusive: record<currency: string, value: string>, updated: string, vat_number: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/invoice"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view a bunq invoice.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/invoice/{itemId}
# operationId: READ_Invoice_for_User_MonetaryAccount
export def "user-monetary-account-invoice get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, category: string, chamber_of_commerce_number: string, counterparty_address: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, group: table<instance_description: string, item: list, product_vat_exclusive: record, product_vat_inclusive: record, type: string, type_description: string, type_description_translated: string>, id: int, invoice_date: string, invoice_number: string, request_reference_split_the_bill: table<id: int, type: string>, status: string, total_vat: record<currency: string, value: string>, total_vat_exclusive: record<currency: string, value: string>, total_vat_inclusive: record<currency: string, value: string>, updated: string, vat_number: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/invoice/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# MasterCard transaction view.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action
# operationId: List_all_MastercardAction_for_User_MonetaryAccount
export def "user-monetary-account-mastercard-action list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, all_mastercard_action_refund: list<record>, amount_billing: record<currency: string, value: string>, amount_converted: record<currency: string, value: string>, amount_fee: record<currency: string, value: string>, amount_local: record<currency: string, value: string>, amount_original_billing: record<currency: string, value: string>, amount_original_local: record<currency: string, value: string>, applied_limit: string, authorisation_status: string, authorisation_type: string, card_authorisation_id_response: string, card_id: int, city: string, clearing_expiry_time: string, clearing_status: string, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, decision: string, decision_description: string, decision_description_translated: string, decision_together_url: string, description: string, eligible_whitelist_id: int, id: int, label_card: record<expiry_date: string, label_user: record, second_line: string, status: string, type: string, uuid: string>, maturity_date: string, monetary_account_id: int, pan_entry_mode_user: string, payment_status: string, pos_card_holder_presence: string, pos_card_presence: string, request_reference_split_the_bill: list<record>, reservation_expiry_time: string, secure_code_id: int, settlement_status: string, token_status: string, wallet_provider_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# MasterCard transaction view.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{itemId}
# operationId: READ_MastercardAction_for_User_MonetaryAccount
export def "user-monetary-account-mastercard-action get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, all_mastercard_action_refund: table<additional_information: record, alias: record, amount: record, attachment: list, category: string, comment: string, counterparty_alias: record, created: string, description: string, id: int, label_card: record, label_user_creator: record, mastercard_action_id: int, reason: string, reference_mastercard_action_event: list, status: string, status_description: string, status_description_translated: string, status_together_url: string, sub_type: string, terms_and_conditions: string, time_refund: string, type: string, updated: string>, amount_billing: record<currency: string, value: string>, amount_converted: record<currency: string, value: string>, amount_fee: record<currency: string, value: string>, amount_local: record<currency: string, value: string>, amount_original_billing: record<currency: string, value: string>, amount_original_local: record<currency: string, value: string>, applied_limit: string, authorisation_status: string, authorisation_type: string, card_authorisation_id_response: string, card_id: int, city: string, clearing_expiry_time: string, clearing_status: string, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, decision: string, decision_description: string, decision_description_translated: string, decision_together_url: string, description: string, eligible_whitelist_id: int, id: int, label_card: record<expiry_date: string, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, second_line: string, status: string, type: string, uuid: string>, maturity_date: string, monetary_account_id: int, pan_entry_mode_user: string, payment_status: string, pos_card_holder_presence: string, pos_card_presence: string, request_reference_split_the_bill: table<id: int, type: string>, reservation_expiry_time: string, secure_code_id: int, settlement_status: string, token_status: string, wallet_provider_id: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-attachment list" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-attachment post" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-attachment get" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-attachment put" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-text list" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-text post" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-text delete" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-text get" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-note-text put" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# MasterCard transaction view.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/mastercard-action/{mastercard-actionID}/payment
# operationId: List_all_Payment_for_User_MonetaryAccount_MastercardAction
export def "user-monetary-account-mastercard-action-payment get" [
  user_id: int
  monetary_account_id: int
  mastercard_action_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, mastercard_action_id: $mastercard_action_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/mastercard-action/{mastercard_action_id}/payment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the url notification filters for a user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/notification-filter-url
# operationId: List_all_NotificationFilterUrl_for_User_MonetaryAccount
export def "user-monetary-account-notification-filter-url get" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<notification_filters: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/notification-filter-url"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the url notification filters for a user.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/notification-filter-url
# operationId: CREATE_NotificationFilterUrl_for_User_MonetaryAccount
# --notification_filters item shape: {notification_filters?: list}
export def "user-monetary-account-notification-filter-url post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --notification-filters: list # The types of notifications that will result in a url notification for this monetary account. — item shape: {notification_filters?: list}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/notification-filter-url"))
  let body = {"notification_filters": $notification_filters} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a listing of all Payments performed on a given MonetaryAccount (incoming and outgoing).
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment
# operationId: List_all_Payment_for_User_MonetaryAccount
export def "user-monetary-account-payment list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount: record<currency: string, value: string>, attachment: list<record>, balance_after_mutation: record<currency: string, value: string>, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record<created: string, error_message: list, id: int, payment_auto_allocate_id: int, payment_batch: record, payment_id: int, status: string, updated: string>, request_reference_split_the_bill: list<record>, scheduled_id: int, sub_type: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new Payment.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/payment
# operationId: CREATE_Payment_for_User_MonetaryAccount
# --address_billing shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_shipping shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --amount shape: {currency?: string, value?: string}
# --attachment item shape: {id?: int}
# --balance_after_mutation shape: {currency?: string, value?: string}
# --counterparty_alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --geolocation shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
# --payment_auto_allocate_instance shape: {payment_batch?: record}
export def "user-monetary-account-payment post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --address-billing: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --address-shipping: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --allow-bunqto: oneof<nothing, bool> # Whether or not sending a bunq.to payment is allowed.
  --amount: record # shape: {currency?: string, value?: string}
  --attachment: list # The Attachments attached to the Payment. — item shape: {id?: int}
  --balance-after-mutation: record # shape: {currency?: string, value?: string}
  --counterparty-alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --description: string # The description for the Payment. Maximum 140 characters for Payments to external IBANs, 9000 characters for Payments to only other bunq MonetaryAccounts.
  --geolocation: record # shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
  --merchant-reference: string # Optional data included with the Payment specific to the merchant.
  --payment-auto-allocate-instance: record # shape: {payment_batch?: record}
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment"))
  let body = {"address_billing": $address_billing, "address_shipping": $address_shipping, "alias": $alias, "allow_bunqto": $allow_bunqto, "amount": $amount, "attachment": $attachment, "balance_after_mutation": $balance_after_mutation, "counterparty_alias": $counterparty_alias, "description": $description, "geolocation": $geolocation, "merchant_reference": $merchant_reference, "payment_auto_allocate_instance": $payment_auto_allocate_instance} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage a users automatic payment auto allocated settings.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate
# operationId: List_all_PaymentAutoAllocate_for_User_MonetaryAccount
export def "user-monetary-account-payment-auto-allocate list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, id: int, payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, status: string, trigger_amount: record<currency: string, value: string>, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage a users automatic payment auto allocated settings.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate
# operationId: CREATE_PaymentAutoAllocate_for_User_MonetaryAccount
# --definition item shape: {amount?: record, counterparty_alias?: record, description?: string, fraction?: int, type: string}
export def "user-monetary-account-payment-auto-allocate post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  definition: list # The definition of how the money should be allocated. — item shape: {amount?: record, counterparty_alias?: record, description?: string, fraction?: int, type: string}
  payment_id: int # The payment that should be used to define the triggers for the payment auto allocate.
  type: string # Whether a payment should be sorted ONCE or RECURRING.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate"))
  let body = {"definition": $definition, "payment_id": $payment_id, "type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage a users automatic payment auto allocated settings.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate/{itemId}
# operationId: DELETE_PaymentAutoAllocate_for_User_MonetaryAccount
export def "user-monetary-account-payment-auto-allocate delete" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage a users automatic payment auto allocated settings.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate/{itemId}
# operationId: READ_PaymentAutoAllocate_for_User_MonetaryAccount
export def "user-monetary-account-payment-auto-allocate get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, id: int, payment: record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, allow_bunqto: bool, amount: record<currency: string, value: string>, attachment: list<record>, balance_after_mutation: record<currency: string, value: string>, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record<created: string, error_message: list, id: int, payment_auto_allocate_id: int, payment_batch: record, payment_id: int, status: string, updated: string>, request_reference_split_the_bill: list<record>, scheduled_id: int, sub_type: string, type: string, updated: string>, status: string, trigger_amount: record<currency: string, value: string>, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage a users automatic payment auto allocated settings.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate/{itemId}
# operationId: UPDATE_PaymentAutoAllocate_for_User_MonetaryAccount
# --definition item shape: {amount?: record, counterparty_alias?: record, description?: string, fraction?: int, type: string}
export def "user-monetary-account-payment-auto-allocate put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  definition: list # The definition of how the money should be allocated. — item shape: {amount?: record, counterparty_alias?: record, description?: string, fraction?: int, type: string}
  payment_id: int # The payment that should be used to define the triggers for the payment auto allocate.
  type: string # Whether a payment should be sorted ONCE or RECURRING.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate/{item_id}"))
  let body = {"definition": $definition, "payment_id": $payment_id, "type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# List all the definitions in a payment auto allocate.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate/{payment-auto-allocateID}/definition
# operationId: List_all_Definition_for_User_MonetaryAccount_PaymentAutoAllocate
export def "user-monetary-account-payment-auto-allocate-definition get" [
  user_id: int
  monetary_account_id: int
  payment_auto_allocate_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<amount: record<currency: string, value: string>, counterparty_alias: record<name: string, service: string, type: string, value: string>, created: string, description: string, fraction: int, id: int, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_auto_allocate_id: $payment_auto_allocate_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate/{payment_auto_allocate_id}/definition"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List all the times a users payment was automatically allocated.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate/{payment-auto-allocateID}/instance
# operationId: List_all_Instance_for_User_MonetaryAccount_PaymentAutoAllocate
export def "user-monetary-account-payment-auto-allocate-instance list" [
  user_id: int
  monetary_account_id: int
  payment_auto_allocate_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, error_message: list<list>, id: int, payment_auto_allocate_id: int, payment_batch: record<payments: record>, payment_id: int, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_auto_allocate_id: $payment_auto_allocate_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate/{payment_auto_allocate_id}/instance"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# List all the times a users payment was automatically allocated.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-auto-allocate/{payment-auto-allocateID}/instance/{itemId}
# operationId: READ_Instance_for_User_MonetaryAccount_PaymentAutoAllocate
export def "user-monetary-account-payment-auto-allocate-instance get" [
  user_id: int
  monetary_account_id: int
  payment_auto_allocate_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, error_message: list<list<record>>, id: int, payment_auto_allocate_id: int, payment_batch: record<payments: record<Payment: list>>, payment_id: int, status: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_auto_allocate_id: $payment_auto_allocate_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-auto-allocate/{payment_auto_allocate_id}/instance/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return all the payment batches for a monetary account.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-batch
# operationId: List_all_PaymentBatch_for_User_MonetaryAccount
export def "user-monetary-account-payment-batch list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<payments: record<Payment: list>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a payment batch by sending an array of single payment objects, that will become part of the batch.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/payment-batch
# operationId: CREATE_PaymentBatch_for_User_MonetaryAccount
export def "user-monetary-account-payment-batch post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --payments: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch"))
  let body = {"payments": $payments} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Return the details of a specific payment batch.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{itemId}
# operationId: READ_PaymentBatch_for_User_MonetaryAccount
export def "user-monetary-account-payment-batch get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<payments: record<Payment: list<record>>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Revoke a bunq.to payment batch. The status of all the payments will be set to REVOKED.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{itemId}
# operationId: UPDATE_PaymentBatch_for_User_MonetaryAccount
export def "user-monetary-account-payment-batch put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --payments: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{item_id}"))
  let body = {"payments": $payments} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-attachment list" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-attachment post" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-attachment get" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-attachment put" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-text list" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-text post" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-text delete" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-text get" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/payment-batch/{payment-batchID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_PaymentBatch
export def "user-monetary-account-payment-batch-note-text put" [
  user_id: int
  monetary_account_id: int
  payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_batch_id: $payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment-batch/{payment_batch_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific previous Payment.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment/{itemId}
# operationId: READ_Payment_for_User_MonetaryAccount
export def "user-monetary-account-payment get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount: record<currency: string, value: string>, attachment: table<id: int, monetary_account_id: int>, balance_after_mutation: record<currency: string, value: string>, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record<created: string, error_message: list<list>, id: int, payment_auto_allocate_id: int, payment_batch: record<payments: record>, payment_id: int, status: string, updated: string>, request_reference_split_the_bill: table<id: int, type: string>, scheduled_id: int, sub_type: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-attachment list" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-attachment post" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-attachment get" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-attachment put" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-text list" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-text post" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-text delete" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-text get" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/payment/{paymentID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_Payment
export def "user-monetary-account-payment-note-text put" [
  user_id: int
  monetary_account_id: int
  payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, payment_id: $payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/payment/{payment_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get all payment requests for a user's monetary account. bunqme_share_url is always null if the counterparty is a bunq user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry
# operationId: List_all_RequestInquiry_for_User_MonetaryAccount
export def "user-monetary-account-request-inquiry list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, amount_inquired: record<currency: string, value: string>, amount_responded: record<currency: string, value: string>, attachment: list<record>, batch_id: int, bunqme_share_url: string, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, minimum_age: int, monetary_account_id: int, redirect_url: string, reference_split_the_bill: record<BillingInvoice: record, DraftPayment: record, MasterCardAction: record, Payment: record, PaymentBatch: record, RequestResponse: record, ScheduleInstance: record, TransferwisePayment: record, WhitelistResult: record>, require_address: string, scheduled_id: int, status: string, time_expiry: string, time_responded: string, updated: string, user_alias_created: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, user_alias_revoked: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new payment request.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry
# operationId: CREATE_RequestInquiry_for_User_MonetaryAccount
# --address_billing shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_shipping shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --amount_inquired shape: {currency?: string, value?: string}
# --amount_responded shape: {currency?: string, value?: string}
# --attachment item shape: {id?: int}
# --counterparty_alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --geolocation shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
# --reference_split_the_bill shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
# --user_alias_created shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
# --user_alias_revoked shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
export def "user-monetary-account-request-inquiry post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --address-billing: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --address-shipping: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --allow-amount-higher: oneof<nothing, bool> # [DEPRECATED] Whether or not the accepting user can choose to accept with a higher amount than requested. Defaults to false.
  --allow-amount-lower: oneof<nothing, bool> # [DEPRECATED] Whether or not the accepting user can choose to accept with a lower amount than requested. Defaults to false.
  --allow-bunqme: oneof<nothing, bool> # Whether or not sending a bunq.me request is allowed.
  --amount-inquired: record # shape: {currency?: string, value?: string}
  --amount-responded: record # shape: {currency?: string, value?: string}
  --attachment: list # The attachments attached to the payment. — item shape: {id?: int}
  --counterparty-alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --description: string # The description of the inquiry.
  --event-id: int # The ID of the associated event if the request was made using 'split the bill'.
  --geolocation: record # shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
  --merchant-reference: string # The client's custom reference that was attached to the request and the mutation.
  --minimum-age: int # The minimum age the user accepting the RequestInquiry must have.
  --redirect-url: string # The URL which the user is sent to after accepting or rejecting the Request.
  --reference-split-the-bill: record # shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
  --require-address: string # Whether or not an address must be provided on accept.
  --status: string # The status of the request.
  --user-alias-created: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --user-alias-revoked: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --want-tip: oneof<nothing, bool> # [DEPRECATED] Whether or not the accepting user can give an extra tip on top of the requested Amount. Defaults to false.
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry"))
  let body = {"address_billing": $address_billing, "address_shipping": $address_shipping, "allow_amount_higher": $allow_amount_higher, "allow_amount_lower": $allow_amount_lower, "allow_bunqme": $allow_bunqme, "amount_inquired": $amount_inquired, "amount_responded": $amount_responded, "attachment": $attachment, "counterparty_alias": $counterparty_alias, "description": $description, "event_id": $event_id, "geolocation": $geolocation, "merchant_reference": $merchant_reference, "minimum_age": $minimum_age, "redirect_url": $redirect_url, "reference_split_the_bill": $reference_split_the_bill, "require_address": $require_address, "status": $status, "user_alias_created": $user_alias_created, "user_alias_revoked": $user_alias_revoked, "want_tip": $want_tip} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Return all the request batches for a monetary account.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch
# operationId: List_all_RequestInquiryBatch_for_User_MonetaryAccount
export def "user-monetary-account-request-inquiry-batch list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<reference_split_the_bill: record<BillingInvoice: record, DraftPayment: record, MasterCardAction: record, Payment: record, PaymentBatch: record, RequestResponse: record, ScheduleInstance: record, TransferwisePayment: record, WhitelistResult: record>, request_inquiries: list<record>, total_amount_inquired: record<currency: string, value: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a request batch by sending an array of single request objects, that will become part of the batch.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch
# operationId: CREATE_RequestInquiryBatch_for_User_MonetaryAccount
# --reference_split_the_bill shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
# --request_inquiries item shape: {address_billing?: record, address_shipping?: record, allow_amount_higher?: bool, allow_amount_lower?: bool, allow_bunqme: bool, amount_inquired?: record, amount_responded?: record, attachment?: list, counterparty_alias?: record, description?: string, event_id?: int, geolocation?: record, merchant_reference?: string, minimum_age?: int, redirect_url?: string, reference_split_the_bill?: record, require_address?: string, status?: string, user_alias_created?: record, user_alias_revoked?: record, want_tip?: bool}
# --total_amount_inquired shape: {currency?: string, value?: string}
export def "user-monetary-account-request-inquiry-batch post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --event-id: int # The ID of the associated event if the request batch was made using 'split the bill'.
  --reference-split-the-bill: record # shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
  --request-inquiries: list # The list of requests that were made. — item shape: {address_billing?: record, address_shipping?: record, allow_amount_higher?: bool, allow_amount_lower?: bool, allow_bunqme: bool, amount_inquired?: record, amount_responded?: record, attachment?: list, counterparty_alias?: record, description?: string, event_id?: int, geolocation?: record, merchant_reference?: string, minimum_age?: int, redirect_url?: string, reference_split_the_bill?: record, require_address?: string, status?: string, user_alias_created?: record, user_alias_revoked?: record, want_tip?: bool}
  --status: string # The status of the request.
  --total-amount-inquired: record # shape: {currency?: string, value?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch"))
  let body = {"event_id": $event_id, "reference_split_the_bill": $reference_split_the_bill, "request_inquiries": $request_inquiries, "status": $status, "total_amount_inquired": $total_amount_inquired} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Return the details of a specific request batch.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{itemId}
# operationId: READ_RequestInquiryBatch_for_User_MonetaryAccount
export def "user-monetary-account-request-inquiry-batch get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<reference_split_the_bill: record<BillingInvoice: record<address: record, alias: record, category: string, chamber_of_commerce_number: string, counterparty_address: record, counterparty_alias: record, created: string, description: string, external_url: string, group: list, id: int, invoice_date: string, invoice_number: string, request_reference_split_the_bill: list, status: string, total_vat: record, total_vat_exclusive: record, total_vat_inclusive: record, updated: string, vat_number: string>, DraftPayment: record<entries: list, number_of_required_accepts: int, previous_updated_timestamp: string, schedule: record, status: string>, MasterCardAction: record<alias: record, all_mastercard_action_refund: list, amount_billing: record, amount_converted: record, amount_fee: record, amount_local: record, amount_original_billing: record, amount_original_local: record, applied_limit: string, authorisation_status: string, authorisation_type: string, card_authorisation_id_response: string, card_id: int, city: string, clearing_expiry_time: string, clearing_status: string, counterparty_alias: record, decision: string, decision_description: string, decision_description_translated: string, decision_together_url: string, description: string, eligible_whitelist_id: int, id: int, label_card: record, maturity_date: string, monetary_account_id: int, pan_entry_mode_user: string, payment_status: string, pos_card_holder_presence: string, pos_card_presence: string, request_reference_split_the_bill: list, reservation_expiry_time: string, secure_code_id: int, settlement_status: string, token_status: string, wallet_provider_id: string>, Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>, RequestResponse: record<address_billing: record, address_shipping: record, alias: record, amount_inquired: record, amount_responded: record, attachment: list, counterparty_alias: record, created: string, credit_scheme_identifier: string, description: string, eligible_whitelist_id: int, event_id: int, geolocation: record, id: int, mandate_identifier: string, minimum_age: int, monetary_account_id: int, redirect_url: string, request_reference_split_the_bill: list, require_address: string, status: string, sub_type: string, time_expiry: string, time_refund_requested: string, time_refunded: string, time_responded: string, type: string, updated: string, user_refund_requested: record>, ScheduleInstance: record<error_message: list, request_reference_split_the_bill: list, result_object: record, scheduled_object: record, state: string, time_end: string, time_start: string>, TransferwisePayment: record<alias: record, amount_source: record, amount_target: record, counterparty_alias: record, monetary_account_id: string, pay_in_reference: string, quote: record, rate: string, recipient_id: string, reference: string, status: string, status_transferwise: string, status_transferwise_issue: string, sub_status: string, time_delivery_estimate: string>, WhitelistResult: record<error_message: list, id: int, monetary_account_paying_id: int, object: record, request_reference_split_the_bill: list, status: string, sub_status: string, whitelist: record>>, request_inquiries: table<address_billing: record, address_shipping: record, allow_amount_higher: bool, allow_amount_lower: bool, allow_bunqme: bool, amount_inquired: record, amount_responded: record, attachment: list, batch_id: int, bunqme_share_url: string, counterparty_alias: record, created: string, description: string, event_id: int, geolocation: record, id: int, merchant_reference: string, minimum_age: int, monetary_account_id: int, redirect_url: string, reference_split_the_bill: record, require_address: string, scheduled_id: int, status: string, time_expiry: string, time_responded: string, updated: string, user_alias_created: record, user_alias_revoked: record, want_tip: bool>, total_amount_inquired: record<currency: string, value: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Revoke a request batch. The status of all the requests will be set to REVOKED.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{itemId}
# operationId: UPDATE_RequestInquiryBatch_for_User_MonetaryAccount
# --reference_split_the_bill shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
# --request_inquiries item shape: {address_billing?: record, address_shipping?: record, allow_amount_higher?: bool, allow_amount_lower?: bool, allow_bunqme: bool, amount_inquired?: record, amount_responded?: record, attachment?: list, counterparty_alias?: record, description?: string, event_id?: int, geolocation?: record, merchant_reference?: string, minimum_age?: int, redirect_url?: string, reference_split_the_bill?: record, require_address?: string, status?: string, user_alias_created?: record, user_alias_revoked?: record, want_tip?: bool}
# --total_amount_inquired shape: {currency?: string, value?: string}
export def "user-monetary-account-request-inquiry-batch put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --event-id: int # The ID of the associated event if the request batch was made using 'split the bill'.
  --reference-split-the-bill: record # shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
  --request-inquiries: list # The list of requests that were made. — item shape: {address_billing?: record, address_shipping?: record, allow_amount_higher?: bool, allow_amount_lower?: bool, allow_bunqme: bool, amount_inquired?: record, amount_responded?: record, attachment?: list, counterparty_alias?: record, description?: string, event_id?: int, geolocation?: record, merchant_reference?: string, minimum_age?: int, redirect_url?: string, reference_split_the_bill?: record, require_address?: string, status?: string, user_alias_created?: record, user_alias_revoked?: record, want_tip?: bool}
  --status: string # The status of the request.
  --total-amount-inquired: record # shape: {currency?: string, value?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{item_id}"))
  let body = {"event_id": $event_id, "reference_split_the_bill": $reference_split_the_bill, "request_inquiries": $request_inquiries, "status": $status, "total_amount_inquired": $total_amount_inquired} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-attachment request-by-userID-monetary_accountID-request_inquiry_batchID" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-attachment request-by-userID-monetary_accountID-request_inquiry_batchID-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-attachment request-by-userID-monetary_accountID-request_inquiry_batchID-itemId" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-attachment request-by-userID-monetary_accountID-request_inquiry_batchID-itemId-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-attachment request-by-userID-monetary_accountID-request_inquiry_batchID-itemId-2" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-text request-by-userID-monetary_accountID-request_inquiry_batchID" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-text request-by-userID-monetary_accountID-request_inquiry_batchID-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-text request-by-userID-monetary_accountID-request_inquiry_batchID-itemId" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-text request-by-userID-monetary_accountID-request_inquiry_batchID-itemId-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry-batch/{request-inquiry-batchID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_RequestInquiryBatch
export def "user-monetary-account-request-inquiry-batch-note-text request-by-userID-monetary_accountID-request_inquiry_batchID-itemId-2" [
  user_id: int
  monetary_account_id: int
  request_inquiry_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_batch_id: $request_inquiry_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry-batch/{request_inquiry_batch_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get the details of a specific payment request, including its status. bunqme_share_url is always null if the counterparty is a bunq user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{itemId}
# operationId: READ_RequestInquiry_for_User_MonetaryAccount
export def "user-monetary-account-request-inquiry get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, amount_inquired: record<currency: string, value: string>, amount_responded: record<currency: string, value: string>, attachment: table<id: int>, batch_id: int, bunqme_share_url: string, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, minimum_age: int, monetary_account_id: int, redirect_url: string, reference_split_the_bill: record<BillingInvoice: record<address: record, alias: record, category: string, chamber_of_commerce_number: string, counterparty_address: record, counterparty_alias: record, created: string, description: string, external_url: string, group: list, id: int, invoice_date: string, invoice_number: string, request_reference_split_the_bill: list, status: string, total_vat: record, total_vat_exclusive: record, total_vat_inclusive: record, updated: string, vat_number: string>, DraftPayment: record<entries: list, number_of_required_accepts: int, previous_updated_timestamp: string, schedule: record, status: string>, MasterCardAction: record<alias: record, all_mastercard_action_refund: list, amount_billing: record, amount_converted: record, amount_fee: record, amount_local: record, amount_original_billing: record, amount_original_local: record, applied_limit: string, authorisation_status: string, authorisation_type: string, card_authorisation_id_response: string, card_id: int, city: string, clearing_expiry_time: string, clearing_status: string, counterparty_alias: record, decision: string, decision_description: string, decision_description_translated: string, decision_together_url: string, description: string, eligible_whitelist_id: int, id: int, label_card: record, maturity_date: string, monetary_account_id: int, pan_entry_mode_user: string, payment_status: string, pos_card_holder_presence: string, pos_card_presence: string, request_reference_split_the_bill: list, reservation_expiry_time: string, secure_code_id: int, settlement_status: string, token_status: string, wallet_provider_id: string>, Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>, RequestResponse: record<address_billing: record, address_shipping: record, alias: record, amount_inquired: record, amount_responded: record, attachment: list, counterparty_alias: record, created: string, credit_scheme_identifier: string, description: string, eligible_whitelist_id: int, event_id: int, geolocation: record, id: int, mandate_identifier: string, minimum_age: int, monetary_account_id: int, redirect_url: string, request_reference_split_the_bill: list, require_address: string, status: string, sub_type: string, time_expiry: string, time_refund_requested: string, time_refunded: string, time_responded: string, type: string, updated: string, user_refund_requested: record>, ScheduleInstance: record<error_message: list, request_reference_split_the_bill: list, result_object: record, scheduled_object: record, state: string, time_end: string, time_start: string>, TransferwisePayment: record<alias: record, amount_source: record, amount_target: record, counterparty_alias: record, monetary_account_id: string, pay_in_reference: string, quote: record, rate: string, recipient_id: string, reference: string, status: string, status_transferwise: string, status_transferwise_issue: string, sub_status: string, time_delivery_estimate: string>, WhitelistResult: record<error_message: list, id: int, monetary_account_paying_id: int, object: record, request_reference_split_the_bill: list, status: string, sub_status: string, whitelist: record>>, require_address: string, scheduled_id: int, status: string, time_expiry: string, time_responded: string, updated: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, user_alias_revoked: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Revoke a request for payment, by updating the status to REVOKED.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{itemId}
# operationId: UPDATE_RequestInquiry_for_User_MonetaryAccount
# --address_billing shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_shipping shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --amount_inquired shape: {currency?: string, value?: string}
# --amount_responded shape: {currency?: string, value?: string}
# --attachment item shape: {id?: int}
# --counterparty_alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --geolocation shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
# --reference_split_the_bill shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
# --user_alias_created shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
# --user_alias_revoked shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
export def "user-monetary-account-request-inquiry put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --address-billing: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --address-shipping: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --allow-amount-higher: oneof<nothing, bool> # [DEPRECATED] Whether or not the accepting user can choose to accept with a higher amount than requested. Defaults to false.
  --allow-amount-lower: oneof<nothing, bool> # [DEPRECATED] Whether or not the accepting user can choose to accept with a lower amount than requested. Defaults to false.
  --allow-bunqme: oneof<nothing, bool> # Whether or not sending a bunq.me request is allowed.
  --amount-inquired: record # shape: {currency?: string, value?: string}
  --amount-responded: record # shape: {currency?: string, value?: string}
  --attachment: list # The attachments attached to the payment. — item shape: {id?: int}
  --counterparty-alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --description: string # The description of the inquiry.
  --event-id: int # The ID of the associated event if the request was made using 'split the bill'.
  --geolocation: record # shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
  --merchant-reference: string # The client's custom reference that was attached to the request and the mutation.
  --minimum-age: int # The minimum age the user accepting the RequestInquiry must have.
  --redirect-url: string # The URL which the user is sent to after accepting or rejecting the Request.
  --reference-split-the-bill: record # shape: {BillingInvoice?: record, DraftPayment?: record, MasterCardAction?: record, Payment?: record, PaymentBatch?: record, RequestResponse?: record, ScheduleInstance?: record, TransferwisePayment?: record, WhitelistResult?: record}
  --require-address: string # Whether or not an address must be provided on accept.
  --status: string # The status of the request.
  --user-alias-created: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --user-alias-revoked: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --want-tip: oneof<nothing, bool> # [DEPRECATED] Whether or not the accepting user can give an extra tip on top of the requested Amount. Defaults to false.
]: any -> record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, amount_inquired: record<currency: string, value: string>, amount_responded: record<currency: string, value: string>, attachment: table<id: int>, batch_id: int, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, minimum_age: int, monetary_account_id: int, reference_split_the_bill: record<BillingInvoice: record<address: record, alias: record, category: string, chamber_of_commerce_number: string, counterparty_address: record, counterparty_alias: record, created: string, description: string, external_url: string, group: list, id: int, invoice_date: string, invoice_number: string, request_reference_split_the_bill: list, status: string, total_vat: record, total_vat_exclusive: record, total_vat_inclusive: record, updated: string, vat_number: string>, DraftPayment: record<entries: list, number_of_required_accepts: int, previous_updated_timestamp: string, schedule: record, status: string>, MasterCardAction: record<alias: record, all_mastercard_action_refund: list, amount_billing: record, amount_converted: record, amount_fee: record, amount_local: record, amount_original_billing: record, amount_original_local: record, applied_limit: string, authorisation_status: string, authorisation_type: string, card_authorisation_id_response: string, card_id: int, city: string, clearing_expiry_time: string, clearing_status: string, counterparty_alias: record, decision: string, decision_description: string, decision_description_translated: string, decision_together_url: string, description: string, eligible_whitelist_id: int, id: int, label_card: record, maturity_date: string, monetary_account_id: int, pan_entry_mode_user: string, payment_status: string, pos_card_holder_presence: string, pos_card_presence: string, request_reference_split_the_bill: list, reservation_expiry_time: string, secure_code_id: int, settlement_status: string, token_status: string, wallet_provider_id: string>, Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>, RequestResponse: record<address_billing: record, address_shipping: record, alias: record, amount_inquired: record, amount_responded: record, attachment: list, counterparty_alias: record, created: string, credit_scheme_identifier: string, description: string, eligible_whitelist_id: int, event_id: int, geolocation: record, id: int, mandate_identifier: string, minimum_age: int, monetary_account_id: int, redirect_url: string, request_reference_split_the_bill: list, require_address: string, status: string, sub_type: string, time_expiry: string, time_refund_requested: string, time_refunded: string, time_responded: string, type: string, updated: string, user_refund_requested: record>, ScheduleInstance: record<error_message: list, request_reference_split_the_bill: list, result_object: record, scheduled_object: record, state: string, time_end: string, time_start: string>, TransferwisePayment: record<alias: record, amount_source: record, amount_target: record, counterparty_alias: record, monetary_account_id: string, pay_in_reference: string, quote: record, rate: string, recipient_id: string, reference: string, status: string, status_transferwise: string, status_transferwise_issue: string, sub_status: string, time_delivery_estimate: string>, WhitelistResult: record<error_message: list, id: int, monetary_account_paying_id: int, object: record, request_reference_split_the_bill: list, status: string, sub_status: string, whitelist: record>>, require_address: string, scheduled_id: int, status: string, time_expiry: string, time_responded: string, updated: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, user_alias_revoked: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{item_id}"))
  let body = {"address_billing": $address_billing, "address_shipping": $address_shipping, "allow_amount_higher": $allow_amount_higher, "allow_amount_lower": $allow_amount_lower, "allow_bunqme": $allow_bunqme, "amount_inquired": $amount_inquired, "amount_responded": $amount_responded, "attachment": $attachment, "counterparty_alias": $counterparty_alias, "description": $description, "event_id": $event_id, "geolocation": $geolocation, "merchant_reference": $merchant_reference, "minimum_age": $minimum_age, "redirect_url": $redirect_url, "reference_split_the_bill": $reference_split_the_bill, "require_address": $require_address, "status": $status, "user_alias_created": $user_alias_created, "user_alias_revoked": $user_alias_revoked, "want_tip": $want_tip} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-attachment request-by-userID-monetary_accountID-request_inquiryID" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-attachment request-by-userID-monetary_accountID-request_inquiryID-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-attachment request-by-userID-monetary_accountID-request_inquiryID-itemId" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-attachment request-by-userID-monetary_accountID-request_inquiryID-itemId-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-attachment request-by-userID-monetary_accountID-request_inquiryID-itemId-2" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-text request-by-userID-monetary_accountID-request_inquiryID" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-text request-by-userID-monetary_accountID-request_inquiryID-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-text request-by-userID-monetary_accountID-request_inquiryID-itemId" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-text request-by-userID-monetary_accountID-request_inquiryID-itemId-1" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-inquiry/{request-inquiryID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_RequestInquiry
export def "user-monetary-account-request-inquiry-note-text request-by-userID-monetary_accountID-request_inquiryID-itemId-2" [
  user_id: int
  monetary_account_id: int
  request_inquiry_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_inquiry_id: $request_inquiry_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-inquiry/{request_inquiry_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get all RequestResponses for a MonetaryAccount.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-response
# operationId: List_all_RequestResponse_for_User_MonetaryAccount
export def "user-monetary-account-request-response list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_inquired: record<currency: string, value: string>, amount_responded: record<currency: string, value: string>, attachment: list<record>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, credit_scheme_identifier: string, description: string, eligible_whitelist_id: int, event_id: int, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, mandate_identifier: string, minimum_age: int, monetary_account_id: int, redirect_url: string, request_reference_split_the_bill: list<record>, require_address: string, status: string, sub_type: string, time_expiry: string, time_refund_requested: string, time_refunded: string, time_responded: string, type: string, updated: string, user_refund_requested: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get the details for a specific existing RequestResponse.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-response/{itemId}
# operationId: READ_RequestResponse_for_User_MonetaryAccount
export def "user-monetary-account-request-response get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_inquired: record<currency: string, value: string>, amount_responded: record<currency: string, value: string>, attachment: table<content_type: string, description: string, urls: list>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, credit_scheme_identifier: string, description: string, eligible_whitelist_id: int, event_id: int, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, mandate_identifier: string, minimum_age: int, monetary_account_id: int, redirect_url: string, request_reference_split_the_bill: table<id: int, type: string>, require_address: string, status: string, sub_type: string, time_expiry: string, time_refund_requested: string, time_refunded: string, time_responded: string, type: string, updated: string, user_refund_requested: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Update the status to accept or reject the RequestResponse.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-response/{itemId}
# operationId: UPDATE_RequestResponse_for_User_MonetaryAccount
# --address_billing shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --address_shipping shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
# --alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --amount_inquired shape: {currency?: string, value?: string}
# --amount_responded shape: {currency?: string, value?: string}
# --counterparty_alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --geolocation shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
# --user_refund_requested shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
export def "user-monetary-account-request-response put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --address-billing: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --address-shipping: record # shape: {city?: string, country?: string, extra?: string, house_number?: string, mailbox_name?: string, po_box?: string, postal_code?: string, street?: string}
  --alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --amount-inquired: record # shape: {currency?: string, value?: string}
  --amount-responded: record # shape: {currency?: string, value?: string}
  --counterparty-alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --geolocation: record # shape: {altitude?: int, latitude?: int, longitude?: int, radius?: int}
  --status: string # The status of the RequestResponse. Can be ACCEPTED, PENDING, REJECTED, REFUND_REQUESTED, REFUNDED or REVOKED.
  --user-refund-requested: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{item_id}"))
  let body = {"address_billing": $address_billing, "address_shipping": $address_shipping, "alias": $alias, "amount_inquired": $amount_inquired, "amount_responded": $amount_responded, "counterparty_alias": $counterparty_alias, "geolocation": $geolocation, "status": $status, "user_refund_requested": $user_refund_requested} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-attachment request-by-userID-monetary_accountID-request_responseID" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-attachment request-by-userID-monetary_accountID-request_responseID-1" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-attachment request-by-userID-monetary_accountID-request_responseID-itemId" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-attachment request-by-userID-monetary_accountID-request_responseID-itemId-1" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-attachment request-by-userID-monetary_accountID-request_responseID-itemId-2" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-text request-by-userID-monetary_accountID-request_responseID" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-text request-by-userID-monetary_accountID-request_responseID-1" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-text request-by-userID-monetary_accountID-request_responseID-itemId" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-text request-by-userID-monetary_accountID-request_responseID-itemId-1" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/request-response/{request-responseID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_RequestResponse
export def "user-monetary-account-request-response-note-text request-by-userID-monetary_accountID-request_responseID-itemId-2" [
  user_id: int
  monetary_account_id: int
  request_response_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, request_response_id: $request_response_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/request-response/{request_response_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a collection of scheduled definition for a given monetary account. You can add the parameter type to filter the response. When type={SCHEDULE_DEFINITION_PAYMENT,SCHEDULE_DEFINITION_PAYMENT_BATCH} is provided only schedule definition object that relate to these definitions are returned.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule
# operationId: List_all_Schedule_for_User_MonetaryAccount
export def "user-monetary-account-schedule list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<object: record<Payment: record, PaymentBatch: record>, recurrence_size: int, recurrence_unit: string, status: string, time_end: string, time_start: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for schedule payments.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment
# operationId: List_all_SchedulePayment_for_User_MonetaryAccount
export def "user-monetary-account-schedule-payment list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<payment: record<alias: record, allow_bunqto: bool, amount: record, attachment: list, counterparty_alias: record, description: string, merchant_reference: string>, schedule: record<object: record, recurrence_size: int, recurrence_unit: string, status: string, time_end: string, time_start: string>, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for schedule payments.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment
# operationId: CREATE_SchedulePayment_for_User_MonetaryAccount
# --payment shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
# --schedule shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
export def "user-monetary-account-schedule-payment post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --payment: record # shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
  --schedule: record # shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment"))
  let body = {"payment": $payment, "schedule": $schedule} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint for schedule payment batches.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch
# operationId: CREATE_SchedulePaymentBatch_for_User_MonetaryAccount
# --payments item shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
# --schedule shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
export def "user-monetary-account-schedule-payment-batch post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --payments: list # The payment details. — item shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
  --schedule: record # shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch"))
  let body = {"payments": $payments, "schedule": $schedule} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint for schedule payment batches.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{itemId}
# operationId: DELETE_SchedulePaymentBatch_for_User_MonetaryAccount
export def "user-monetary-account-schedule-payment-batch delete" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for schedule payment batches.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{itemId}
# operationId: READ_SchedulePaymentBatch_for_User_MonetaryAccount
export def "user-monetary-account-schedule-payment-batch get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<payments: table<alias: record, allow_bunqto: bool, amount: record, attachment: list, counterparty_alias: record, description: string, merchant_reference: string>, schedule: record<object: record<Payment: record, PaymentBatch: record>, recurrence_size: int, recurrence_unit: string, status: string, time_end: string, time_start: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for schedule payment batches.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{itemId}
# operationId: UPDATE_SchedulePaymentBatch_for_User_MonetaryAccount
# --payments item shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
# --schedule shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
export def "user-monetary-account-schedule-payment-batch put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --payments: list # The payment details. — item shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
  --schedule: record # shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{item_id}"))
  let body = {"payments": $payments, "schedule": $schedule} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-attachment list" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-attachment post" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-attachment get" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-attachment put" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-text list" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-text post" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-text delete" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-text get" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment-batch/{schedule-payment-batchID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_SchedulePaymentBatch
export def "user-monetary-account-schedule-payment-batch-note-text put" [
  user_id: int
  monetary_account_id: int
  schedule_payment_batch_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_batch_id: $schedule_payment_batch_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment-batch/{schedule_payment_batch_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Endpoint for schedule payments.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{itemId}
# operationId: DELETE_SchedulePayment_for_User_MonetaryAccount
export def "user-monetary-account-schedule-payment delete" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for schedule payments.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{itemId}
# operationId: READ_SchedulePayment_for_User_MonetaryAccount
export def "user-monetary-account-schedule-payment get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<payment: record<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, allow_bunqto: bool, amount: record<currency: string, value: string>, attachment: list<record>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, description: string, merchant_reference: string>, schedule: record<object: record<Payment: record, PaymentBatch: record>, recurrence_size: int, recurrence_unit: string, status: string, time_end: string, time_start: string>, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Endpoint for schedule payments.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{itemId}
# operationId: UPDATE_SchedulePayment_for_User_MonetaryAccount
# --payment shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
# --schedule shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
export def "user-monetary-account-schedule-payment put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --payment: record # shape: {alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, counterparty_alias?: record, description?: string, merchant_reference?: string}
  --schedule: record # shape: {object?: record, recurrence_size?: int, recurrence_unit?: string, time_end?: string, time_start?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{item_id}"))
  let body = {"payment": $payment, "schedule": $schedule} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-attachment list" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-attachment post" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-attachment get" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-attachment put" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-text list" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-text post" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-text delete" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-text get" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule-payment/{schedule-paymentID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_SchedulePayment
export def "user-monetary-account-schedule-payment-note-text put" [
  user_id: int
  monetary_account_id: int
  schedule_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_payment_id: $schedule_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule-payment/{schedule_payment_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific schedule definition for a given monetary account.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule/{itemId}
# operationId: READ_Schedule_for_User_MonetaryAccount
export def "user-monetary-account-schedule get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<object: record<Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>>, recurrence_size: int, recurrence_unit: string, status: string, time_end: string, time_start: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# view for reading, updating and listing the scheduled instance.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance
# operationId: List_all_ScheduleInstance_for_User_MonetaryAccount_Schedule
export def "user-monetary-account-schedule-schedule-instance list" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<error_message: list<list>, request_reference_split_the_bill: list<record>, result_object: record<Payment: record, PaymentBatch: record>, scheduled_object: record<Payment: record, PaymentBatch: record>, state: string, time_end: string, time_start: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# view for reading, updating and listing the scheduled instance.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{itemId}
# operationId: READ_ScheduleInstance_for_User_MonetaryAccount_Schedule
export def "user-monetary-account-schedule-schedule-instance get" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<error_message: list<list<record>>, request_reference_split_the_bill: table<id: int, type: string>, result_object: record<Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>>, scheduled_object: record<Payment: record<address_billing: record, address_shipping: record, alias: record, allow_bunqto: bool, amount: record, attachment: list, balance_after_mutation: record, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record, created: string, description: string, geolocation: record, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record, request_reference_split_the_bill: list, scheduled_id: int, sub_type: string, type: string, updated: string>, PaymentBatch: record<payments: record>>, state: string, time_end: string, time_start: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# view for reading, updating and listing the scheduled instance.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{itemId}
# operationId: UPDATE_ScheduleInstance_for_User_MonetaryAccount_Schedule
# --result_object shape: {Payment?: record, PaymentBatch?: record}
# --scheduled_object shape: {Payment?: record, PaymentBatch?: record}
export def "user-monetary-account-schedule-schedule-instance put" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --result-object: record # shape: {Payment?: record, PaymentBatch?: record}
  --scheduled-object: record # shape: {Payment?: record, PaymentBatch?: record}
  --state: string # The state of the scheduleInstance. (FINISHED_SUCCESSFULLY, RETRY, FAILED_USER_ERROR)
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{item_id}"))
  let body = {"result_object": $result_object, "scheduled_object": $scheduled_object, "state": $state} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-attachment list" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-attachment post" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-attachment get" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-attachment put" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-text list" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-text post" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-text delete" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-text get" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/schedule/{scheduleID}/schedule-instance/{schedule-instanceID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_Schedule_ScheduleInstance
export def "user-monetary-account-schedule-schedule-instance-note-text put" [
  user_id: int
  monetary_account_id: int
  schedule_id: int
  schedule_instance_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, schedule_id: $schedule_id, schedule_instance_id: $schedule_instance_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/schedule/{schedule_id}/schedule-instance/{schedule_instance_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# [DEPRECATED - use /share-invite-monetary-account-response] Get a list with all the share inquiries for a monetary account, only if the requesting user has permission to change the details of the various ones.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/share-invite-monetary-account-inquiry
# operationId: List_all_ShareInviteMonetaryAccountInquiry_for_User_MonetaryAccount
export def "user-monetary-account-share-invite-monetary-account-inquiry list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<access_type: string, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, counter_user_alias: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, id: int, monetary_account_id: int, relationship: string, status: string, user_alias_created: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, user_alias_revoked: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/share-invite-monetary-account-inquiry"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# [DEPRECATED - use /share-invite-monetary-account-response] Create a new share inquiry for a monetary account, specifying the permission the other bunq user will have on it.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/share-invite-monetary-account-inquiry
# operationId: CREATE_ShareInviteMonetaryAccountInquiry_for_User_MonetaryAccount
# --alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --counter_user_alias shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
# --share_detail shape: {draft_payment?: record, payment?: record, read_only?: record}
# --user_alias_created shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
# --user_alias_revoked shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
export def "user-monetary-account-share-invite-monetary-account-inquiry post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --access-type: string # Type of access that is in place.
  --alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --counter-user-alias: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --draft-share-invite-bank-id: int # DEPRECATED: USE `access_type` INSTEAD | The id of the draft share invite bank.
  --end-date: string # DEPRECATED: USE `access_type` INSTEAD | The expiration date of this share.
  --relationship: string # The relationship: COMPANY_DIRECTOR, COMPANY_EMPLOYEE, etc
  --share-detail: record # shape: {draft_payment?: record, payment?: record, read_only?: record}
  --share-type: string # DEPRECATED: USE `access_type` INSTEAD | The share type, either STANDARD or MUTUAL.
  --start-date: string # DEPRECATED: USE `access_type` INSTEAD | The start date of this share.
  --status: string # The status of the share. Can be ACTIVE, REVOKED, REJECTED.
  --user-alias-created: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --user-alias-revoked: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
]: any -> record<id: int> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/share-invite-monetary-account-inquiry"))
  let body = {"access_type": $access_type, "alias": $alias, "counter_user_alias": $counter_user_alias, "draft_share_invite_bank_id": $draft_share_invite_bank_id, "end_date": $end_date, "relationship": $relationship, "share_detail": $share_detail, "share_type": $share_type, "start_date": $start_date, "status": $status, "user_alias_created": $user_alias_created, "user_alias_revoked": $user_alias_revoked} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# [DEPRECATED - use /share-invite-monetary-account-response] Get the details of a specific share inquiry.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/share-invite-monetary-account-inquiry/{itemId}
# operationId: READ_ShareInviteMonetaryAccountInquiry_for_User_MonetaryAccount
export def "user-monetary-account-share-invite-monetary-account-inquiry get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<access_type: string, alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, counter_user_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, id: int, monetary_account_id: int, relationship: string, status: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, user_alias_revoked: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/share-invite-monetary-account-inquiry/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# [DEPRECATED - use /share-invite-monetary-account-response] Update the details of a share. This includes updating status (revoking or cancelling it), granted permission and validity period of this share.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/share-invite-monetary-account-inquiry/{itemId}
# operationId: UPDATE_ShareInviteMonetaryAccountInquiry_for_User_MonetaryAccount
# --alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --counter_user_alias shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
# --share_detail shape: {draft_payment?: record, payment?: record, read_only?: record}
# --user_alias_created shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
# --user_alias_revoked shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
export def "user-monetary-account-share-invite-monetary-account-inquiry put" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --access-type: string # Type of access that is in place.
  --alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --counter-user-alias: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --draft-share-invite-bank-id: int # DEPRECATED: USE `access_type` INSTEAD | The id of the draft share invite bank.
  --end-date: string # DEPRECATED: USE `access_type` INSTEAD | The expiration date of this share.
  --relationship: string # The relationship: COMPANY_DIRECTOR, COMPANY_EMPLOYEE, etc
  --share-detail: record # shape: {draft_payment?: record, payment?: record, read_only?: record}
  --share-type: string # DEPRECATED: USE `access_type` INSTEAD | The share type, either STANDARD or MUTUAL.
  --start-date: string # DEPRECATED: USE `access_type` INSTEAD | The start date of this share.
  --status: string # The status of the share. Can be ACTIVE, REVOKED, REJECTED.
  --user-alias-created: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
  --user-alias-revoked: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/share-invite-monetary-account-inquiry/{item_id}"))
  let body = {"access_type": $access_type, "alias": $alias, "counter_user_alias": $counter_user_alias, "draft_share_invite_bank_id": $draft_share_invite_bank_id, "end_date": $end_date, "relationship": $relationship, "share_detail": $share_detail, "share_type": $share_type, "start_date": $start_date, "status": $status, "user_alias_created": $user_alias_created, "user_alias_revoked": $user_alias_revoked} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# View for requesting Sofort transactions and polling their status.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction
# operationId: List_all_SofortMerchantTransaction_for_User_MonetaryAccount
export def "user-monetary-account-sofort-merchant-transaction list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_guaranteed: record<currency: string, value: string>, amount_requested: record<currency: string, value: string>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, error_message: list<list>, issuer: string, issuer_authentication_url: string, monetary_account_id: int, status: string, transaction_identifier: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# View for requesting Sofort transactions and polling their status.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{itemId}
# operationId: READ_SofortMerchantTransaction_for_User_MonetaryAccount
export def "user-monetary-account-sofort-merchant-transaction get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_guaranteed: record<currency: string, value: string>, amount_requested: record<currency: string, value: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, error_message: list<list<record>>, issuer: string, issuer_authentication_url: string, monetary_account_id: int, status: string, transaction_identifier: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-attachment list" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-attachment post" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-attachment get" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-attachment put" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-text list" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-text post" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-text delete" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-text get" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/sofort-merchant-transaction/{sofort-merchant-transactionID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_SofortMerchantTransaction
export def "user-monetary-account-sofort-merchant-transaction-note-text put" [
  user_id: int
  monetary_account_id: int
  sofort_merchant_transaction_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, sofort_merchant_transaction_id: $sofort_merchant_transaction_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/sofort-merchant-transaction/{sofort_merchant_transaction_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# An incoming payment made towards an account of an external bank and redirected to a bunq account via switch service.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{itemId}
# operationId: READ_SwitchServicePayment_for_User_MonetaryAccount
export def "user-monetary-account-switch-service-payment get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<bank_switch_service: record<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, attachment: record<content_type: string, description: string, urls: list>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, status: string, sub_status: string, time_end: string, time_start_actual: string, time_start_desired: string, user_alias: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>>, payment: record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, allow_bunqto: bool, amount: record<currency: string, value: string>, attachment: list<record>, balance_after_mutation: record<currency: string, value: string>, batch_id: int, bunqto_expiry: string, bunqto_share_url: string, bunqto_status: string, bunqto_sub_status: string, bunqto_time_responded: string, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, merchant_reference: string, monetary_account_id: int, payment_auto_allocate_instance: record<created: string, error_message: list, id: int, payment_auto_allocate_id: int, payment_batch: record, payment_id: int, status: string, updated: string>, request_reference_split_the_bill: list<record>, scheduled_id: int, sub_type: string, type: string, updated: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-attachment list" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-attachment post" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-attachment get" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-attachment put" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-text list" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-text post" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-text delete" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-text get" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/switch-service-payment/{switch-service-paymentID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_SwitchServicePayment
export def "user-monetary-account-switch-service-payment-note-text put" [
  user_id: int
  monetary_account_id: int
  switch_service_payment_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, switch_service_payment_id: $switch_service_payment_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/switch-service-payment/{switch_service_payment_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to create translink transactions.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/translink-transaction
# operationId: List_all_TranslinkTransaction_for_User_MonetaryAccount
export def "user-monetary-account-translink-transaction list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount: record<currency: string, value: string>, description: string, entries: list<record>, failure_reason: list<list>, payments: record<Payment: list>, reference: string, status: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/translink-transaction"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create translink transactions.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/translink-transaction
# operationId: CREATE_TranslinkTransaction_for_User_MonetaryAccount
# --payments item shape: {address_billing?: record, address_shipping?: record, alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, balance_after_mutation?: record, counterparty_alias?: record, description?: string, geolocation?: record, merchant_reference?: string, payment_auto_allocate_instance?: record}
export def "user-monetary-account-translink-transaction post" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  description: string # Description of the payment request.
  payments: list # The list of payments we want to send in a single transaction. — item shape: {address_billing?: record, address_shipping?: record, alias?: record, allow_bunqto?: bool, amount?: record, attachment?: list, balance_after_mutation?: record, counterparty_alias?: record, description?: string, geolocation?: record, merchant_reference?: string, payment_auto_allocate_instance?: record}
  reference: string # The request reference.
  type: string # Type of transaction, can be TRIP, REFUND, WITHDRAWAL or TOP_UP.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/translink-transaction"))
  let body = {"description": $description, "payments": $payments, "reference": $reference, "type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to create translink transactions.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/translink-transaction/{itemId}
# operationId: READ_TranslinkTransaction_for_User_MonetaryAccount
export def "user-monetary-account-translink-transaction get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount: record<currency: string, value: string>, description: string, entries: table<alias: record, amount: record, attachment: list, counterparty_alias: record, description: string, id: int, merchant_reference: string, type: string>, failure_reason: list<list<record>>, payments: record<Payment: list<record>>, reference: string, status: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/translink-transaction/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a listing of all SDD whitelist entries for a target monetary account.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/whitelist-sdd
# operationId: List_all_WhitelistSdd_for_User_MonetaryAccount
export def "user-monetary-account-whitelist-sdd list" [
  user_id: int
  monetary_account_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, mandate_identifier: string, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist-sdd"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific SDD whitelist entry.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/whitelist-sdd/{itemId}
# operationId: READ_WhitelistSdd_for_User_MonetaryAccount
export def "user-monetary-account-whitelist-sdd get" [
  user_id: int
  monetary_account_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, mandate_identifier: string, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist-sdd/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-attachment
# operationId: List_all_NoteAttachment_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-attachment list" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<attachment: list<record>, created: string, description: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-attachment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-attachment
# operationId: CREATE_NoteAttachment_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-attachment post" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-attachment"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage attachment notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-attachment/{itemId}
# operationId: DELETE_NoteAttachment_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-attachment delete" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-attachment/{itemId}
# operationId: READ_NoteAttachment_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-attachment get" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<attachment: table<id: int, monetary_account_id: int>, created: string, description: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-attachment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage attachment notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-attachment/{itemId}
# operationId: UPDATE_NoteAttachment_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-attachment put" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  attachment_id: int # The reference to the uploaded file to attach to this note.
  --description: string # Optional description of the attachment.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-attachment/{item_id}"))
  let body = {"attachment_id": $attachment_id, "description": $description} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the notes for a given user.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-text
# operationId: List_all_NoteText_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-text list" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<content: string, created: string, id: int, label_user_creator: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-text"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# POST /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-text
# operationId: CREATE_NoteText_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-text post" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-text"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage text notes.
#
# DELETE /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-text/{itemId}
# operationId: DELETE_NoteText_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-text delete" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# GET /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-text/{itemId}
# operationId: READ_NoteText_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-text get" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<content: string, created: string, id: int, label_user_creator: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-text/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage text notes.
#
# PUT /user/{userID}/monetary-account/{monetary-accountID}/whitelist/{whitelistID}/whitelist-result/{whitelist-resultID}/note-text/{itemId}
# operationId: UPDATE_NoteText_for_User_MonetaryAccount_Whitelist_WhitelistResult
export def "user-monetary-account-whitelist-whitelist-result-note-text put" [
  user_id: int
  monetary_account_id: int
  whitelist_id: int
  whitelist_result_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --content: string # The content of the note.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, monetary_account_id: $monetary_account_id, whitelist_id: $whitelist_id, whitelist_result_id: $whitelist_result_id, item_id: $item_id} | format pattern "/user/{user_id}/monetary-account/{monetary_account_id}/whitelist/{whitelist_id}/whitelist-result/{whitelist_result_id}/note-text/{item_id}"))
  let body = {"content": $content} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the email notification filters for a user.
#
# GET /user/{userID}/notification-filter-email
# operationId: List_all_NotificationFilterEmail_for_User
export def "user-notification-filter-email get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<notification_filters: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/notification-filter-email"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the email notification filters for a user.
#
# POST /user/{userID}/notification-filter-email
# operationId: CREATE_NotificationFilterEmail_for_User
# --notification_filters item shape: {notification_filters?: list}
export def "user-notification-filter-email post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --notification-filters: list # The types of notifications that will result in a email notification for this user. — item shape: {notification_filters?: list}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/notification-filter-email"))
  let body = {"notification_filters": $notification_filters} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the push notification filters for a user.
#
# GET /user/{userID}/notification-filter-push
# operationId: List_all_NotificationFilterPush_for_User
export def "user-notification-filter-push get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<notification_filters: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/notification-filter-push"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the push notification filters for a user.
#
# POST /user/{userID}/notification-filter-push
# operationId: CREATE_NotificationFilterPush_for_User
# --notification_filters item shape: {notification_filters?: list}
export def "user-notification-filter-push post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --notification-filters: list # The types of notifications that will result in a push notification for this user. — item shape: {notification_filters?: list}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/notification-filter-push"))
  let body = {"notification_filters": $notification_filters} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the url notification filters for a user.
#
# GET /user/{userID}/notification-filter-url
# operationId: List_all_NotificationFilterUrl_for_User
export def "user-notification-filter-url get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<notification_filters: list<record>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/notification-filter-url"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the url notification filters for a user.
#
# POST /user/{userID}/notification-filter-url
# operationId: CREATE_NotificationFilterUrl_for_User
# --notification_filters item shape: {notification_filters?: list}
export def "user-notification-filter-url post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --notification-filters: list # The types of notifications that will result in a url notification for this user. — item shape: {notification_filters?: list}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/notification-filter-url"))
  let body = {"notification_filters": $notification_filters} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used for managing OAuth Clients.
#
# GET /user/{userID}/oauth-client
# operationId: List_all_OauthClient_for_User
export def "user-oauth-client list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<callback_url: list<record>, client_id: string, display_name: string, id: int, secret: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/oauth-client"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used for managing OAuth Clients.
#
# POST /user/{userID}/oauth-client
# operationId: CREATE_OauthClient_for_User
export def "user-oauth-client post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --status: string # The status of the Oauth Client, can be ACTIVE or CANCELLED.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/oauth-client"))
  let body = {"status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used for managing OAuth Clients.
#
# GET /user/{userID}/oauth-client/{itemId}
# operationId: READ_OauthClient_for_User
export def "user-oauth-client get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<callback_url: table<url: string>, client_id: string, display_name: string, id: int, secret: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/oauth-client/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used for managing OAuth Clients.
#
# PUT /user/{userID}/oauth-client/{itemId}
# operationId: UPDATE_OauthClient_for_User
export def "user-oauth-client put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --status: string # The status of the Oauth Client, can be ACTIVE or CANCELLED.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/oauth-client/{item_id}"))
  let body = {"status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used for managing OAuth Client Callback URLs.
#
# GET /user/{userID}/oauth-client/{oauth-clientID}/callback-url
# operationId: List_all_CallbackUrl_for_User_OauthClient
export def "user-oauth-client-callback-url list" [
  user_id: int
  oauth_client_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, oauth_client_id: $oauth_client_id} | format pattern "/user/{user_id}/oauth-client/{oauth_client_id}/callback-url"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used for managing OAuth Client Callback URLs.
#
# POST /user/{userID}/oauth-client/{oauth-clientID}/callback-url
# operationId: CREATE_CallbackUrl_for_User_OauthClient
export def "user-oauth-client-callback-url post" [
  user_id: int
  oauth_client_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body-url: string # The URL for this callback.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, oauth_client_id: $oauth_client_id} | format pattern "/user/{user_id}/oauth-client/{oauth_client_id}/callback-url"))
  let body = {"url": $body_url} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used for managing OAuth Client Callback URLs.
#
# DELETE /user/{userID}/oauth-client/{oauth-clientID}/callback-url/{itemId}
# operationId: DELETE_CallbackUrl_for_User_OauthClient
export def "user-oauth-client-callback-url delete" [
  user_id: int
  oauth_client_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, oauth_client_id: $oauth_client_id, item_id: $item_id} | format pattern "/user/{user_id}/oauth-client/{oauth_client_id}/callback-url/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used for managing OAuth Client Callback URLs.
#
# GET /user/{userID}/oauth-client/{oauth-clientID}/callback-url/{itemId}
# operationId: READ_CallbackUrl_for_User_OauthClient
export def "user-oauth-client-callback-url get" [
  user_id: int
  oauth_client_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<url: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, oauth_client_id: $oauth_client_id, item_id: $item_id} | format pattern "/user/{user_id}/oauth-client/{oauth_client_id}/callback-url/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used for managing OAuth Client Callback URLs.
#
# PUT /user/{userID}/oauth-client/{oauth-clientID}/callback-url/{itemId}
# operationId: UPDATE_CallbackUrl_for_User_OauthClient
export def "user-oauth-client-callback-url put" [
  user_id: int
  oauth_client_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body-url: string # The URL for this callback.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, oauth_client_id: $oauth_client_id, item_id: $item_id} | format pattern "/user/{user_id}/oauth-client/{oauth_client_id}/callback-url/{item_id}"))
  let body = {"url": $body_url} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# List a users automatic payment auto allocated settings.
#
# GET /user/{userID}/payment-auto-allocate
# operationId: List_all_PaymentAutoAllocate_for_User
export def "user-payment-auto-allocate get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<PaymentAutoAllocate: record<definition: list, payment_id: int, type: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/payment-auto-allocate"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the PaymentServiceProviderDraftPayment's for a PISP.
#
# GET /user/{userID}/payment-service-provider-draft-payment
# operationId: List_all_PaymentServiceProviderDraftPayment_for_User
export def "user-payment-service-provider-draft-payment list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<amount: record<currency: string, value: string>, receiver_iban: string, sender_iban: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/payment-service-provider-draft-payment"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the PaymentServiceProviderDraftPayment's for a PISP.
#
# POST /user/{userID}/payment-service-provider-draft-payment
# operationId: CREATE_PaymentServiceProviderDraftPayment_for_User
# --amount shape: {currency?: string, value?: string}
export def "user-payment-service-provider-draft-payment post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  amount: record # shape: {currency?: string, value?: string}
  counterparty_iban: string # The IBAN of the counterparty.
  counterparty_name: string # The name of the counterparty.
  description: string # Description of the payment.
  sender_iban: string # The IBAN of the sender.
  --sender-name: string # The name of the sender.
  --status: string # The new status of the Draft Payment. Can only be set to REJECTED or CANCELLED by update.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/payment-service-provider-draft-payment"))
  let body = {"amount": $amount, "counterparty_iban": $counterparty_iban, "counterparty_name": $counterparty_name, "description": $description, "sender_iban": $sender_iban, "sender_name": $sender_name, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Manage the PaymentServiceProviderDraftPayment's for a PISP.
#
# GET /user/{userID}/payment-service-provider-draft-payment/{itemId}
# operationId: READ_PaymentServiceProviderDraftPayment_for_User
export def "user-payment-service-provider-draft-payment get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount: record<currency: string, value: string>, receiver_iban: string, sender_iban: string, status: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/payment-service-provider-draft-payment/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Manage the PaymentServiceProviderDraftPayment's for a PISP.
#
# PUT /user/{userID}/payment-service-provider-draft-payment/{itemId}
# operationId: UPDATE_PaymentServiceProviderDraftPayment_for_User
# --amount shape: {currency?: string, value?: string}
export def "user-payment-service-provider-draft-payment put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  amount: record # shape: {currency?: string, value?: string}
  counterparty_iban: string # The IBAN of the counterparty.
  counterparty_name: string # The name of the counterparty.
  description: string # Description of the payment.
  sender_iban: string # The IBAN of the sender.
  --sender-name: string # The name of the sender.
  --status: string # The new status of the Draft Payment. Can only be set to REJECTED or CANCELLED by update.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/payment-service-provider-draft-payment/{item_id}"))
  let body = {"amount": $amount, "counterparty_iban": $counterparty_iban, "counterparty_name": $counterparty_name, "description": $description, "sender_iban": $sender_iban, "sender_name": $sender_name, "status": $status} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a listing of all Slice group settlements.
#
# GET /user/{userID}/registry/{registryID}/registry-settlement
# operationId: List_all_RegistrySettlement_for_User_Registry
export def "user-registry-registry-settlement list" [
  user_id: int
  registry_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, id: int, items: list<record>, membership_settled: record<alias: record, auto_add_card_transaction: string, balance: record, invitor: record, registry_id: int, registry_title: string, status: string, status_settlement: string, total_amount_spent: record, uuid: string>, number_of_entries: int, settled_by_alias: record<alias: record, auto_add_card_transaction: string, balance: record, invitor: record, registry_id: int, registry_title: string, status: string, status_settlement: string, total_amount_spent: record, uuid: string>, settlement_time: string, total_amount_spent: record<currency: string, value: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, registry_id: $registry_id} | format pattern "/user/{user_id}/registry/{registry_id}/registry-settlement"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new Slice group settlement.
#
# POST /user/{userID}/registry/{registryID}/registry-settlement
# operationId: CREATE_RegistrySettlement_for_User_Registry
export def "user-registry-registry-settlement post" [
  user_id: int
  registry_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body: record
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, registry_id: $registry_id} | format pattern "/user/{user_id}/registry/{registry_id}/registry-settlement"))
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific Slice group settlement.
#
# GET /user/{userID}/registry/{registryID}/registry-settlement/{itemId}
# operationId: READ_RegistrySettlement_for_User_Registry
export def "user-registry-registry-settlement get" [
  user_id: int
  registry_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<created: string, id: int, items: table<amount: record, membership_paying: record, membership_receiving: record, paying_user_alias: record, receiving_user_alias: record>, membership_settled: record<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, auto_add_card_transaction: string, balance: record<currency: string, value: string>, invitor: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, registry_id: int, registry_title: string, status: string, status_settlement: string, total_amount_spent: record<currency: string, value: string>, uuid: string>, number_of_entries: int, settled_by_alias: record<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, auto_add_card_transaction: string, balance: record<currency: string, value: string>, invitor: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, registry_id: int, registry_title: string, status: string, status_settlement: string, total_amount_spent: record<currency: string, value: string>, uuid: string>, settlement_time: string, total_amount_spent: record<currency: string, value: string>, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, registry_id: $registry_id, item_id: $item_id} | format pattern "/user/{user_id}/registry/{registry_id}/registry-settlement/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view Rewards.
#
# GET /user/{userID}/reward
# operationId: List_all_Reward_for_User
export def "user-reward list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<amount_reward: record<currency: string, value: string>, counterparty_alias: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, status: string, sub_status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/reward"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view Rewards.
#
# GET /user/{userID}/reward-recipient
# operationId: List_all_RewardRecipient_for_User
export def "user-reward-recipient list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<amount_reward: record<currency: string, value: string>, counterparty_alias: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, status: string, sub_status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/reward-recipient"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view Rewards.
#
# GET /user/{userID}/reward-recipient/{itemId}
# operationId: READ_RewardRecipient_for_User
export def "user-reward-recipient get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount_reward: record<currency: string, value: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, status: string, sub_status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/reward-recipient/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view Rewards.
#
# GET /user/{userID}/reward-sender
# operationId: List_all_RewardSender_for_User
export def "user-reward-sender list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<amount_reward: record<currency: string, value: string>, counterparty_alias: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, status: string, sub_status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/reward-sender"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view Rewards.
#
# GET /user/{userID}/reward-sender/{itemId}
# operationId: READ_RewardSender_for_User
export def "user-reward-sender get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount_reward: record<currency: string, value: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, status: string, sub_status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/reward-sender/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to view Rewards.
#
# GET /user/{userID}/reward/{itemId}
# operationId: READ_Reward_for_User
export def "user-reward get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount_reward: record<currency: string, value: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>, created: string, id: int, status: string, sub_status: string, type: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/reward/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a collection of scheduled definition for all accessible monetary accounts of the user. You can add the parameter type to filter the response. When type={SCHEDULE_DEFINITION_PAYMENT,SCHEDULE_DEFINITION_PAYMENT_BATCH} is provided only schedule definition object that relate to these definitions are returned.
#
# GET /user/{userID}/schedule
# operationId: List_all_Schedule_for_User
export def "user-schedule get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> list<record> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/schedule"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return all the shares a user was invited to.
#
# GET /user/{userID}/share-invite-monetary-account-response
# operationId: List_all_ShareInviteMonetaryAccountResponse_for_User
export def "user-share-invite-monetary-account-response list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<access_type: string, counter_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, draft_share_invite_bank_id: int, end_date: string, id: int, monetary_account_id: int, relation_user: record<counter_label_user: record, counter_user_id: string, counter_user_status: string, label_user: record, relationship: string, status: string, user_id: string, user_status: string>, share_detail: record<draft_payment: record, payment: record, read_only: record>, share_type: string, start_date: string, status: string, updated: string, user_alias_cancelled: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/share-invite-monetary-account-response"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Return the details of a specific share a user was invited to.
#
# GET /user/{userID}/share-invite-monetary-account-response/{itemId}
# operationId: READ_ShareInviteMonetaryAccountResponse_for_User
export def "user-share-invite-monetary-account-response get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<access_type: string, counter_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, created: string, description: string, draft_share_invite_bank_id: int, end_date: string, id: int, monetary_account_id: int, relation_user: record<counter_label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, counter_user_id: string, counter_user_status: string, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, relationship: string, status: string, user_id: string, user_status: string>, share_detail: record<draft_payment: record<make_draft_payments: bool, view_balance: bool, view_new_events: bool, view_old_events: bool>, payment: record<make_draft_payments: bool, make_payments: bool, view_balance: bool, view_new_events: bool, view_old_events: bool>, read_only: record<view_balance: bool, view_new_events: bool, view_old_events: bool>>, share_type: string, start_date: string, status: string, updated: string, user_alias_cancelled: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/share-invite-monetary-account-response/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Accept or reject a share a user was invited to.
#
# PUT /user/{userID}/share-invite-monetary-account-response/{itemId}
# operationId: UPDATE_ShareInviteMonetaryAccountResponse_for_User
# --counter_alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --relation_user shape: {counter_label_user?: record, label_user?: record}
# --share_detail shape: {draft_payment?: record, payment?: record, read_only?: record}
# --user_alias_cancelled shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
export def "user-share-invite-monetary-account-response put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --card-id: int # The card to link to the shared monetary account. Used only if share_detail is ShareDetailCardPayment.
  --counter-alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --relation-user: record # shape: {counter_label_user?: record, label_user?: record}
  --share-detail: record # shape: {draft_payment?: record, payment?: record, read_only?: record}
  --status: string # The status of the share. Can be ACTIVE, REVOKED, REJECTED.
  --user-alias-cancelled: record # shape: {avatar?: record, country?: string, display_name?: string, uuid?: string}
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/share-invite-monetary-account-response/{item_id}"))
  let body = {"card_id": $card_id, "counter_alias": $counter_alias, "relation_user": $relation_user, "share_detail": $share_detail, "status": $status, "user_alias_cancelled": $user_alias_cancelled} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a request from an ideal transaction.
#
# POST /user/{userID}/token-qr-request-ideal
# operationId: CREATE_TokenQrRequestIdeal_for_User
export def "user-token-qr-request-ideal post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body-token: string # The token passed from a site or read from a QR code.
]: any -> record<address_billing: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, address_shipping: record<city: string, country: string, extra: string, house_number: string, is_user_address_updated: bool, mailbox_name: string, po_box: string, postal_code: string, province: string, street: string>, alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_inquired: record<currency: string, value: string>, amount_responded: record<currency: string, value: string>, attachment: table<content_type: string, description: string, urls: list>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, description: string, eligible_whitelist_id: int, geolocation: record<altitude: int, latitude: int, longitude: int, radius: int>, id: int, minimum_age: int, monetary_account_id: int, redirect_url: string, require_address: string, status: string, sub_type: string, time_expiry: string, time_responded: string, type: string> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/token-qr-request-ideal"))
  let body = {"token": $body_token} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Create a request from an SOFORT transaction.
#
# POST /user/{userID}/token-qr-request-sofort
# operationId: CREATE_TokenQrRequestSofort_for_User
export def "user-token-qr-request-sofort post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --body-token: string # The token passed from a site or read from a QR code.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/token-qr-request-sofort"))
  let body = {"token": $body_token} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to get a list of supported currencies for Transferwise.
#
# GET /user/{userID}/transferwise-currency
# operationId: List_all_TransferwiseCurrency_for_User
export def "user-transferwise-currency get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<country: string, currency: string, name: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/transferwise-currency"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to get quotes from Transferwise. These can be used to initiate payments.
#
# POST /user/{userID}/transferwise-quote
# operationId: CREATE_TransferwiseQuote_for_User
# --amount_fee shape: {currency?: string, value?: string}
# --amount_source shape: {currency?: string, value?: string}
# --amount_target shape: {currency?: string, value?: string}
export def "user-transferwise-quote post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --amount-fee: record # shape: {currency?: string, value?: string}
  --amount-source: record # shape: {currency?: string, value?: string}
  --amount-target: record # shape: {currency?: string, value?: string}
  currency_source: string # The source currency.
  currency_target: string # The target currency.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/transferwise-quote"))
  let body = {"amount_fee": $amount_fee, "amount_source": $amount_source, "amount_target": $amount_target, "currency_source": $currency_source, "currency_target": $currency_target} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to get temporary quotes from Transferwise. These cannot be used to initiate payments
#
# POST /user/{userID}/transferwise-quote-temporary
# operationId: CREATE_TransferwiseQuoteTemporary_for_User
# --amount_source shape: {currency?: string, value?: string}
# --amount_target shape: {currency?: string, value?: string}
export def "user-transferwise-quote-temporary post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --amount-source: record # shape: {currency?: string, value?: string}
  --amount-target: record # shape: {currency?: string, value?: string}
  currency_source: string # The source currency.
  currency_target: string # The target currency.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/transferwise-quote-temporary"))
  let body = {"amount_source": $amount_source, "amount_target": $amount_target, "currency_source": $currency_source, "currency_target": $currency_target} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to get temporary quotes from Transferwise. These cannot be used to initiate payments
#
# GET /user/{userID}/transferwise-quote-temporary/{itemId}
# operationId: READ_TransferwiseQuoteTemporary_for_User
export def "user-transferwise-quote-temporary get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount_source: record<currency: string, value: string>, amount_target: record<currency: string, value: string>, created: string, id: int, quote_id: string, rate: string, time_expiry: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/transferwise-quote-temporary/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to get quotes from Transferwise. These can be used to initiate payments.
#
# GET /user/{userID}/transferwise-quote/{itemId}
# operationId: READ_TransferwiseQuote_for_User
export def "user-transferwise-quote get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<amount_fee: record<currency: string, value: string>, amount_source: record<currency: string, value: string>, amount_target: record<currency: string, value: string>, created: string, id: int, quote_id: string, rate: string, time_delivery_estimate: string, time_expiry: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/transferwise-quote/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage recipient accounts with Transferwise.
#
# GET /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-recipient
# operationId: List_all_TransferwiseRecipient_for_User_TransferwiseQuote
export def "user-transferwise-quote-transferwise-recipient list" [
  user_id: int
  transferwise_quote_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<account_id: string, account_number: string, bank_code: string, country: string, currency: string, name_account_holder: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-recipient"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage recipient accounts with Transferwise.
#
# POST /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-recipient
# operationId: CREATE_TransferwiseRecipient_for_User_TransferwiseQuote
# --detail item shape: {group?: record, key: string, value: string}
export def "user-transferwise-quote-transferwise-recipient post" [
  user_id: int
  transferwise_quote_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --country: string # The country of the receiving account.
  --detail: list # The fields which were specified as "required" and have since been filled by the user. Always provide the full list. — item shape: {group?: record, key: string, value: string}
  name_account_holder: string # The name of the account holder.
  type: string # The chosen recipient account type. The possible options are provided dynamically in the response endpoint.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-recipient"))
  let body = {"country": $country, "detail": $detail, "name_account_holder": $name_account_holder, "type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to determine the recipient account requirements for Transferwise transfers.
#
# GET /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-recipient-requirement
# operationId: List_all_TransferwiseRecipientRequirement_for_User_TransferwiseQuote
export def "user-transferwise-quote-transferwise-recipient-requirement get" [
  user_id: int
  transferwise_quote_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<fields: list<record>, label: string, type: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-recipient-requirement"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to determine the recipient account requirements for Transferwise transfers.
#
# POST /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-recipient-requirement
# operationId: CREATE_TransferwiseRecipientRequirement_for_User_TransferwiseQuote
# --detail item shape: {group?: record, key: string, value: string}
export def "user-transferwise-quote-transferwise-recipient-requirement post" [
  user_id: int
  transferwise_quote_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --country: string # The country of the receiving account.
  --detail: list # The fields which were specified as "required" and have since been filled by the user. Always provide the full list. — item shape: {group?: record, key: string, value: string}
  name_account_holder: string # The name of the account holder.
  type: string # The chosen recipient account type. The possible options are provided dynamically in the response endpoint.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-recipient-requirement"))
  let body = {"country": $country, "detail": $detail, "name_account_holder": $name_account_holder, "type": $type} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to manage recipient accounts with Transferwise.
#
# DELETE /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-recipient/{itemId}
# operationId: DELETE_TransferwiseRecipient_for_User_TransferwiseQuote
export def "user-transferwise-quote-transferwise-recipient delete" [
  user_id: int
  transferwise_quote_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id, item_id: $item_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-recipient/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage recipient accounts with Transferwise.
#
# GET /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-recipient/{itemId}
# operationId: READ_TransferwiseRecipient_for_User_TransferwiseQuote
export def "user-transferwise-quote-transferwise-recipient get" [
  user_id: int
  transferwise_quote_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<account_id: string, account_number: string, bank_code: string, country: string, currency: string, name_account_holder: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id, item_id: $item_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-recipient/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create Transferwise payments.
#
# GET /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-transfer
# operationId: List_all_TransferwiseTransfer_for_User_TransferwiseQuote
export def "user-transferwise-quote-transferwise-transfer list" [
  user_id: int
  transferwise_quote_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_source: record<currency: string, value: string>, amount_target: record<currency: string, value: string>, counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, pay_in_reference: string, quote: record<amount_fee: record, amount_source: record, amount_target: record, created: string, currency_source: string, currency_target: string, id: int, quote_id: string, rate: string, time_delivery_estimate: string, time_expiry: string, updated: string>, rate: string, reference: string, status: string, status_transferwise: string, status_transferwise_issue: string, sub_status: string, time_delivery_estimate: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-transfer"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to create Transferwise payments.
#
# POST /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-transfer
# operationId: CREATE_TransferwiseTransfer_for_User_TransferwiseQuote
# --alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --amount_source shape: {currency?: string, value?: string}
# --amount_target shape: {currency?: string, value?: string}
# --counterparty_alias shape: {avatar?: record, bunq_me?: record, label_user?: record}
# --quote shape: {amount_fee?: record, amount_source?: record, amount_target?: record, currency_source: string, currency_target: string}
export def "user-transferwise-quote-transferwise-transfer post" [
  user_id: int
  transferwise_quote_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  --amount-source: record # shape: {currency?: string, value?: string}
  --amount-target: record # shape: {currency?: string, value?: string}
  --counterparty-alias: record # shape: {avatar?: record, bunq_me?: record, label_user?: record}
  monetary_account_id: string # The id of the monetary account the payment should be made from.
  --quote: record # shape: {amount_fee?: record, amount_source?: record, amount_target?: record, currency_source: string, currency_target: string}
  recipient_id: string # The id of the target account.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-transfer"))
  let body = {"alias": $alias, "amount_source": $amount_source, "amount_target": $amount_target, "counterparty_alias": $counterparty_alias, "monetary_account_id": $monetary_account_id, "quote": $quote, "recipient_id": $recipient_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to determine the account requirements for Transferwise transfers.
#
# POST /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-transfer-requirement
# operationId: CREATE_TransferwiseTransferRequirement_for_User_TransferwiseQuote
# --detail item shape: {group?: record, key: string, value: string}
export def "user-transferwise-quote-transferwise-transfer-requirement post" [
  user_id: int
  transferwise_quote_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --detail: list # The fields which were specified as "required" and have since been filled by the user. Always provide the full list. — item shape: {group?: record, key: string, value: string}
  recipient_id: string # The id of the target account.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-transfer-requirement"))
  let body = {"detail": $detail, "recipient_id": $recipient_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Used to create Transferwise payments.
#
# GET /user/{userID}/transferwise-quote/{transferwise-quoteID}/transferwise-transfer/{itemId}
# operationId: READ_TransferwiseTransfer_for_User_TransferwiseQuote
export def "user-transferwise-quote-transferwise-transfer get" [
  user_id: int
  transferwise_quote_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, amount_source: record<currency: string, value: string>, amount_target: record<currency: string, value: string>, counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, pay_in_reference: string, quote: record<amount_fee: record<currency: string, value: string>, amount_source: record<currency: string, value: string>, amount_target: record<currency: string, value: string>, created: string, currency_source: string, currency_target: string, id: int, quote_id: string, rate: string, time_delivery_estimate: string, time_expiry: string, updated: string>, rate: string, reference: string, status: string, status_transferwise: string, status_transferwise_issue: string, sub_status: string, time_delivery_estimate: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, transferwise_quote_id: $transferwise_quote_id, item_id: $item_id} | format pattern "/user/{user_id}/transferwise-quote/{transferwise_quote_id}/transferwise-transfer/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage Transferwise users.
#
# GET /user/{userID}/transferwise-user
# operationId: List_all_TransferwiseUser_for_User
export def "user-transferwise-user get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<created: string, email: string, id: int, name: string, source: string, updated: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/transferwise-user"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Used to manage Transferwise users.
#
# POST /user/{userID}/transferwise-user
# operationId: CREATE_TransferwiseUser_for_User
export def "user-transferwise-user post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --oauth-code: string # The OAuth code returned by Transferwise we should be using to gain access to the user's Transferwise account.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/transferwise-user"))
  let body = {"oauth_code": $oauth_code} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# See how many trees this user has planted.
#
# GET /user/{userID}/tree-progress
# operationId: List_all_TreeProgress_for_User
export def "user-tree-progress get" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, number_of_tree: int, progress_tree_next: int, url_invite_profile: string> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/tree-progress"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a listing of all recurring SDD whitelist entries for a target monetary account.
#
# GET /user/{userID}/whitelist-sdd
# operationId: List_all_WhitelistSdd_for_User
export def "user-whitelist-sdd list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, mandate_identifier: string, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/whitelist-sdd"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a listing of all one off SDD whitelist entries for a target monetary account.
#
# GET /user/{userID}/whitelist-sdd-one-off
# operationId: List_all_WhitelistSddOneOff_for_User
export def "user-whitelist-sdd-one-off list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/whitelist-sdd-one-off"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new one off SDD whitelist entry.
#
# POST /user/{userID}/whitelist-sdd-one-off
# operationId: CREATE_WhitelistSddOneOff_for_User
# --maximum_amount_per_month shape: {currency?: string, value?: string}
export def "user-whitelist-sdd-one-off post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --maximum-amount-per-month: record # shape: {currency?: string, value?: string}
  monetary_account_paying_id: int # ID of the monetary account of which you want to pay from.
  request_id: int # ID of the request for which you want to whitelist the originating SDD.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/whitelist-sdd-one-off"))
  let body = {"maximum_amount_per_month": $maximum_amount_per_month, "monetary_account_paying_id": $monetary_account_paying_id, "request_id": $request_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Whitelist an one off SDD so that when another one off SDD from the creditor comes in, it is automatically accepted.
#
# DELETE /user/{userID}/whitelist-sdd-one-off/{itemId}
# operationId: DELETE_WhitelistSddOneOff_for_User
export def "user-whitelist-sdd-one-off delete" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/whitelist-sdd-one-off/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific one off SDD whitelist entry.
#
# GET /user/{userID}/whitelist-sdd-one-off/{itemId}
# operationId: READ_WhitelistSddOneOff_for_User
export def "user-whitelist-sdd-one-off get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/whitelist-sdd-one-off/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Whitelist an one off SDD so that when another one off SDD from the creditor comes in, it is automatically accepted.
#
# PUT /user/{userID}/whitelist-sdd-one-off/{itemId}
# operationId: UPDATE_WhitelistSddOneOff_for_User
# --maximum_amount_per_month shape: {currency?: string, value?: string}
export def "user-whitelist-sdd-one-off put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --maximum-amount-per-month: record # shape: {currency?: string, value?: string}
  monetary_account_paying_id: int # ID of the monetary account of which you want to pay from.
  request_id: int # ID of the request for which you want to whitelist the originating SDD.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/whitelist-sdd-one-off/{item_id}"))
  let body = {"maximum_amount_per_month": $maximum_amount_per_month, "monetary_account_paying_id": $monetary_account_paying_id, "request_id": $request_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a listing of all recurring SDD whitelist entries for a target monetary account.
#
# GET /user/{userID}/whitelist-sdd-recurring
# operationId: List_all_WhitelistSddRecurring_for_User
export def "user-whitelist-sdd-recurring list" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> table<counterparty_alias: record<avatar: record, bunq_me: record, country: string, display_name: string, iban: string, is_light: bool, label_user: record, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, mandate_identifier: string, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/whitelist-sdd-recurring"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Create a new recurring SDD whitelist entry.
#
# POST /user/{userID}/whitelist-sdd-recurring
# operationId: CREATE_WhitelistSddRecurring_for_User
# --maximum_amount_per_month shape: {currency?: string, value?: string}
export def "user-whitelist-sdd-recurring post" [
  user_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --maximum-amount-per-month: record # shape: {currency?: string, value?: string}
  monetary_account_paying_id: int # ID of the monetary account of which you want to pay from.
  request_id: int # ID of the request for which you want to whitelist the originating SDD.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id} | format pattern "/user/{user_id}/whitelist-sdd-recurring"))
  let body = {"maximum_amount_per_month": $maximum_amount_per_month, "monetary_account_paying_id": $monetary_account_paying_id, "request_id": $request_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "post" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Whitelist a recurring SDD so that when another recurrence comes in, it is automatically accepted.
#
# DELETE /user/{userID}/whitelist-sdd-recurring/{itemId}
# operationId: DELETE_WhitelistSddRecurring_for_User
export def "user-whitelist-sdd-recurring delete" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/whitelist-sdd-recurring/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "delete" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Get a specific recurring SDD whitelist entry.
#
# GET /user/{userID}/whitelist-sdd-recurring/{itemId}
# operationId: READ_WhitelistSddRecurring_for_User
export def "user-whitelist-sdd-recurring get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, mandate_identifier: string, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/whitelist-sdd-recurring/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}

# Whitelist a recurring SDD so that when another recurrence comes in, it is automatically accepted.
#
# PUT /user/{userID}/whitelist-sdd-recurring/{itemId}
# operationId: UPDATE_WhitelistSddRecurring_for_User
# --maximum_amount_per_month shape: {currency?: string, value?: string}
export def "user-whitelist-sdd-recurring put" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
  --maximum-amount-per-month: record # shape: {currency?: string, value?: string}
  monetary_account_paying_id: int # ID of the monetary account of which you want to pay from.
  request_id: int # ID of the request for which you want to whitelist the originating SDD.
]: any -> record<Id: record<id: int>> {
  let input = $in
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/whitelist-sdd-recurring/{item_id}"))
  let body = {"maximum_amount_per_month": $maximum_amount_per_month, "monetary_account_paying_id": $monetary_account_paying_id, "request_id": $request_id} | compact
  let body = if ($input | describe | str starts-with "record") { $input | merge deep ($body | default {}) } else { $body }
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "put" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json" $body
}

# Get a specific recurring SDD whitelist entry.
#
# GET /user/{userID}/whitelist-sdd/{itemId}
# operationId: READ_WhitelistSdd_for_User
export def "user-whitelist-sdd get" [
  user_id: int
  item_id: int
  --base-url(-b): string@base-url-completer # API base URL
  --token(-t): string # Auth token
  --auth-scheme(-a): string@auth-scheme-completer # Auth scheme
  --insecure(-k) # Skip TLS verification
  --max-time(-m): duration # Timeout
  --raw(-r) # Fetch as text
  --allow-errors(-e) # Return full response without error handling
  --dry-run(-n) # Return the request that would be sent without executing it
  --cache-control: string # The standard HTTP Cache-Control header is required for all signed requests.
  --user-agent: string # The User-Agent header field should contain information about the user agent originating the request. There are no restrictions on the value of this header.
  --x-bunq-language: string # The X-Bunq-Language header must contain a preferred language indication. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore. Currently only the languages en_US and nl_NL are supported. Anything else will default to en_US.
  --x-bunq-region: string # The X-Bunq-Region header must contain the region (country) of the client device. The value of this header is formatted as a ISO 639-1 language code plus a ISO 3166-1 alpha-2 country code, separated by an underscore.
  --x-bunq-client-request-id: string # This header must specify an ID with each request that is unique for the logged in user. There are no restrictions for the format of this ID. However, the server will respond with an error when the same ID is used again on the same DeviceServer.
  --x-bunq-geolocation: string # This header must specify the geolocation of the device. The format of this value is longitude latitude altitude radius country. The country is expected to be formatted of an ISO 3166-1 alpha-2 country code. When no geolocation is available or known the header must still be included but can be zero valued.
  --x-bunq-client-authentication: string # The authentication token is used to authenticate the source of the API call. It is required by all API calls except for POST /v1/installation. It is important to note that the device and session calls are using the token from the response of the installation call, while all the other calls use the token from the response of the session-server call
]: nothing -> record<counterparty_alias: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, bunq_me: record<name: string, service: string, type: string, value: string>, country: string, display_name: string, iban: string, is_light: bool, label_user: record<avatar: record, country: string, display_name: string, public_nick_name: string, uuid: string>, merchant_category_code: string, swift_account_number: string, swift_bic: string, transferwise_account_number: string, transferwise_bank_code: string>, credit_scheme_identifier: string, id: int, mandate_identifier: string, maximum_amount_per_month: record<currency: string, value: string>, monetary_account_incoming_id: int, monetary_account_paying_id: int, status: string, type: string, user_alias_created: record<avatar: record<anchor_uuid: string, image: list, style: string, uuid: string>, country: string, display_name: string, public_nick_name: string, uuid: string>> {
  let auth = (build-auth $token ($auth_scheme | default "bearer"))
  let base = ($base_url | default $BASE_URL)
  let full_url = (build-url $base ({user_id: $user_id, item_id: $item_id} | format pattern "/user/{user_id}/whitelist-sdd/{item_id}"))
  let extra_headers = {"Cache-Control": $cache_control, "User-Agent": $user_agent, "X-Bunq-Language": $x_bunq_language, "X-Bunq-Region": $x_bunq_region, "X-Bunq-Client-Request-Id": $x_bunq_client_request_id, "X-Bunq-Geolocation": $x_bunq_geolocation, "X-Bunq-Client-Authentication": $x_bunq_client_authentication} | compact
  let auth = ($auth | update headers ($auth.headers | merge $extra_headers))
  let accept_val = "application/json"
  let auth = ($auth | update headers ($auth.headers | merge {Accept: $accept_val}))
  do-request "get" $full_url $auth $insecure $raw $dry_run $max_time $allow_errors "application/json"
}
